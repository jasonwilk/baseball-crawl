# Vision Signals

Raw parking lot for vision signals -- statements about what this project will become. Any agent appends here when they notice a signal in conversation. Format: date, one or two sentences, optional source context.

## Signals

- **2026-03-07**: Jason envisions an LLM-powered chat agent built into the dashboard where coaches can ask questions about matchups and get strategy insights. *(conversation during vision discussion)*
- **2026-03-09**: Codex treated as an agent LLM you drive, not a separate tool. Full repo access assumed (CLAUDE.md, agent defs, rules, everything). Two modes (spec review, code review) x two paths (headless dispatch, prompt generation). The mental model is: Claude dispatches to Codex the way it dispatches to any implementing agent. *(E-080 redesign discussion)*
- **2026-03-09**: Fuzzy opponent resolution using a light LLM (Haiku): compare rosters (mostly same players, jersey numbers), cross-reference shared game scores, and confirm "these are the same team" when the automated progenitor_team_id chain is unavailable. Useful for hand-created opponents with no GC link. *(opponent data model discussion)*
- **2026-03-11**: The `/search/opponent-import` endpoint could unlock opponent UUID resolution without the 403 barrier on the reverse bridge. Combined with the athlete-profile cluster (`/athlete-profile/{id}/career-stats`), this opens a path to full opponent data parity and cross-team longitudinal player tracking. Connects to IDEA-009 and IDEA-019. *(E-094 refinement team review)*
- **2026-03-12**: The complete opponent scouting chain has been verified end-to-end: public_id -> reverse bridge -> season stats + boxscores in 4 API calls per opponent. The core scouting value proposition is now technically achievable -- a coach can get full season stats for every player on an upcoming opponent. This is the inflection point where the project moves from "data collection" to "competitive intelligence." *(E-097 discovery -- live walkthrough of Lincoln Southwest Varsity data)*
- **2026-03-13**: Coach interview: "The schedule and the scouting report should be the same view." Game day is the primary context. The schedule page IS the scouting entry point -- each game row links directly to the opponent scouting report. No separate opponents navigation needed. *(E-100 coach consultation via UXD)*
- **2026-03-13**: Coach interview: Rate stats are mandatory, counting stats alone are unusable. "The system shows me raw stats and I have to calculate K/9 myself." All displayed stats must be rates: K/9, BB/9, OBP, K%, ERA, SB%. *(E-100 coach consultation via UXD)*
- **2026-03-13**: Coach interview: Proactive flags are non-negotiable. System must auto-surface: short rest (≤3 days), high pitch count (>85), hot/cold hitter streaks, high walk rate. "Their ace threw 98 pitches 4 days ago" -- coaches shouldn't have to calculate this. *(E-100 coach consultation via UXD)*
- **2026-03-13**: Coach interview: Wants a "clean one-pager that summarizes the opponent in plain English" for bench use. PDF export preferred over browser print. B&W printable, no color dependency. *(E-100 coach consultation via UXD)*
- **2026-03-13**: Coach interview: System-generated matchup suggestions based on L/R splits and recent form. "Consider moving [Player A] up — .380 OBP vs. lefties." Validate suggestion logic before building lineup builder UI. *(E-100 coach consultation via UXD)*
- **2026-03-13**: Recency tapering for cross-season stats: when players carry over between seasons (e.g., HS -> Legion with ~80% roster overlap), prior season data should be visible early in the new season but taper in prominence as current season games accumulate. Not a hard wall between seasons -- recency does the work. This will shape how stats are displayed and weighted. *(vision curation session -- user correction to program-first model)*
- **2026-03-13**: "Each season is a fresh start." Kids change between seasons -- they develop, flourish under a new coach, or taper as peers catch up physically. Past seasons are relevant context but the system must not overindex on them. Current season is the main story; historical data is background, available when you ask but never leading. The system's job is to help the coach make the most of THIS team, THIS season. Cross-team player identity should be accessible ("know it's there") but subordinate to current-season data. Guard rail against the historical/longitudinal layer becoming the tail that wags the dog. *(vision curation session -- coaching philosophy on historical data)*
- **2026-03-14**: Data model parity with GameChanger: the schema should include every stat GC tracks, populated by direct API pull or crawled/compiled from play-by-play. The goal is to make our data model as rich as GC's and maintain parity over time. Stats not yet available via API get nullable columns now, filled when the data source becomes available. "What is the reason we wouldn't store a stat?" *(E-100 schema design session)*
- **2026-03-14**: Opponent stat provenance and data blending: opponent's own stats are canonical (even for games against us), but our boxscore data can supplement with fields they don't expose. Per-game merging — "keep theirs as canonical for that game, pull ours in as supplement." Needs a provenance mechanism so coaches know what's definitive vs. computed. *(E-100 refinement — coach/SE/DE discussion)*
- **2026-03-16**: "I'm itching to make this project pay off for our coaches." Urgency to deliver tangible coaching value. E-100 is the foundation -- user wants to nail it comprehensively so follow-up epics can deliver real scouting and game-prep capabilities. *(E-100 refinement session)*
- **2026-03-16**: Player handedness (bats/throws) may not be populated in GameChanger by most teams. L/R splits may require operator-entered data rather than API-sourced. "I don't think teams enter it often. Unless we populate it ourselves as we watch games." This reframes IDEA-029 from API field mapping to a manual data entry + API hybrid approach. *(E-100 Codex review triage — IDEA-028/029 scoping)*
- **2026-03-16**: Coach consultation: the most valuable HS coaching splits are query-time computations over per-game data — recent form (last 5-7 games), doubleheader fatigue (game 1 vs 2), season phase (early/mid/late). Traditional home/away splits have low value at HS level (no real home field advantage, small samples). Per-game stat population (E-117) is the prerequisite for all high-value analytics — pre-aggregated columns are secondary. *(baseball-coach consultation during IDEA-028 promotion)*
- **2026-03-16**: Stat compilation pyramid: boxscore + play-by-play can derive ALL stats GC computes, achieving stat parity between owned and opponent teams without the season-stats API. Pipeline vision: (1) boxscore for basic counting stats, (2) play-by-play parsing for advanced stats (QAB, pitches seen per batter, contact quality), (3) validate play-by-play against boxscore, (4) compile season aggregates from game-level data. This makes play-by-play parsing a keystone future epic for full opponent data parity. *(E-117 refinement — user articulating data compilation strategy)*
- **2026-03-18**: HTTP header fidelity gap: web profile sends `Accept-Encoding: gzip, deflate` but real Chrome sends `gzip, deflate, br, zstd`. iOS user-agent version is `2026.9.0` now, not `2026.7.0` as in our headers. Both are minor fidelity gaps that could trigger detection if GC starts fingerprinting. Consider periodic header refresh from proxy captures. *(E-127 proxy data refinement -- api-scout findings)*
- **2026-03-18**: Opponent dashboard should lead with pitching, not batting. Coach's first question for any opponent is "Who am I facing on the mound?" -- likely starters, K/BB ratio, recent outings, pitch count/availability. Own-team view correctly leads with batting (lineup decisions). The two views should eventually have distinct content ordering. Priority: (1) key pitchers + availability, (2) team batting tendencies (K%, BB%, OBP), (3) power vs contact breakdown, (4) record + recent form, (5) familiar faces sidebar. *(E-127 refinement -- baseball-coach consultation on season selector placement)*
