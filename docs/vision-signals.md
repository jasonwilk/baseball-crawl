# Vision Signals

Raw parking lot for vision signals -- statements about what this project will become. Any agent appends here when they notice a signal in conversation. Format: date, one or two sentences, optional source context.

## Signals

- **2026-03-07**: Jason envisions an LLM-powered chat agent built into the dashboard where coaches can ask questions about matchups and get strategy insights. *(conversation during vision discussion)*
- **2026-03-09**: Codex treated as an agent LLM you drive, not a separate tool. Full repo access assumed (CLAUDE.md, agent defs, rules, everything). Two modes (spec review, code review) x two paths (headless dispatch, prompt generation). The mental model is: Claude dispatches to Codex the way it dispatches to any implementing agent. *(E-080 redesign discussion)*
- **2026-03-25**: Admin "combine teams" feature: when the same real-world opponent appears as multiple teams rows (e.g., schedule stubs vs. resolved GC teams), the admin should be able to merge them -- pick a canonical row, reassign all games/stats/links from the duplicate, and delete the stub. This is a coaching-critical data quality issue: Lincoln East appears multiple times in the schedule with the completed game not linking to the scouted version. *(coaching feedback on E-153 dashboard)*
- **2026-03-25**: Opponent identity should be school-level, not GC-registration-level. Multiple `root_team_id` values from GameChanger can map to the same real opponent. The system needs a deduplication layer that works across schedule stubs, opponent_links entries, and resolved GC teams. *(coaching feedback on schedule opponent fragmentation)*
- **2026-03-09**: Fuzzy opponent resolution using a light LLM (Haiku): compare rosters (mostly same players, jersey numbers), cross-reference shared game scores, and confirm "these are the same team" when the automated progenitor_team_id chain is unavailable. Useful for hand-created opponents with no GC link. *(opponent data model discussion)*
- **2026-03-11**: The `/search/opponent-import` endpoint could unlock opponent UUID resolution without the 403 barrier on the reverse bridge. Combined with the athlete-profile cluster (`/athlete-profile/{id}/career-stats`), this opens a path to full opponent data parity and cross-team longitudinal player tracking. Connects to IDEA-009 and IDEA-019. *(E-094 refinement team review)*
- **2026-03-12**: The complete opponent scouting chain has been verified end-to-end: public_id -> reverse bridge -> season stats + boxscores in 4 API calls per opponent. The core scouting value proposition is now technically achievable -- a coach can get full season stats for every player on an upcoming opponent. This is the inflection point where the project moves from "data collection" to "competitive intelligence." *(E-097 discovery -- live walkthrough of Lincoln Southwest Varsity data)*
- **2026-03-13**: Coach interview: "The schedule and the scouting report should be the same view." Game day is the primary context. The schedule page IS the scouting entry point -- each game row links directly to the opponent scouting report. No separate opponents navigation needed. *(E-100 coach consultation via UXD)*
- **2026-03-13**: Coach interview: Rate stats are mandatory, counting stats alone are unusable. "The system shows me raw stats and I have to calculate K/9 myself." All displayed stats must be rates: K/9, BB/9, OBP, K%, ERA, SB%. *(E-100 coach consultation via UXD)*
- **2026-03-13**: Coach interview: Proactive flags are non-negotiable. System must auto-surface: short rest (≤3 days), high pitch count (>85), hot/cold hitter streaks, high walk rate. "Their ace threw 98 pitches 4 days ago" -- coaches shouldn't have to calculate this. *(E-100 coach consultation via UXD)*
- **2026-03-13**: Coach interview: Wants a "clean one-pager that summarizes the opponent in plain English" for bench use. PDF export preferred over browser print. B&W printable, no color dependency. *(E-100 coach consultation via UXD)*
- **2026-03-13**: Coach interview: System-generated matchup suggestions based on L/R splits and recent form. "Consider moving [Player A] up — .380 OBP vs. lefties." Validate suggestion logic before building lineup builder UI. *(E-100 coach consultation via UXD)*
- **2026-03-13**: Recency tapering for cross-season stats: when players carry over between seasons (e.g., HS -> Legion with ~80% roster overlap), prior season data should be visible early in the new season but taper in prominence as current season games accumulate. Not a hard wall between seasons -- recency does the work. This will shape how stats are displayed and weighted. *(vision curation session -- user correction to program-first model)*
- **2026-03-13**: "Each season is a fresh start." Kids change between seasons -- they develop, flourish under a new coach, or taper as peers catch up physically. Past seasons are relevant context but the system must not overindex on them. Current season is the main story; historical data is background, available when you ask but never leading. The system's job is to help the coach make the most of THIS team, THIS season. Cross-team player identity should be accessible ("know it's there") but subordinate to current-season data. Guard rail against the historical/longitudinal layer becoming the tail that wags the dog. *(vision curation session -- coaching philosophy on historical data)*
- **2026-03-14**: Data model parity with GameChanger: the schema should include every stat GC tracks, populated by direct API pull or crawled/compiled from play-by-play. The goal is to make our data model as rich as GC's and maintain parity over time. Stats not yet available via API get nullable columns now, filled when the data source becomes available. "What is the reason we wouldn't store a stat?" *(E-100 schema design session)*
- **2026-03-14**: Opponent stat provenance and data blending: opponent's own stats are canonical (even for games against us), but our boxscore data can supplement with fields they don't expose. Per-game merging — "keep theirs as canonical for that game, pull ours in as supplement." Needs a provenance mechanism so coaches know what's definitive vs. computed. *(E-100 refinement — coach/SE/DE discussion)*
- **2026-03-16**: "I'm itching to make this project pay off for our coaches." Urgency to deliver tangible coaching value. E-100 is the foundation -- user wants to nail it comprehensively so follow-up epics can deliver real scouting and game-prep capabilities. *(E-100 refinement session)*
- **2026-03-16**: Player handedness (bats/throws) may not be populated in GameChanger by most teams. L/R splits may require operator-entered data rather than API-sourced. "I don't think teams enter it often. Unless we populate it ourselves as we watch games." This reframes IDEA-029 from API field mapping to a manual data entry + API hybrid approach. *(E-100 Codex review triage — IDEA-028/029 scoping)*
- **2026-03-16**: Coach consultation: the most valuable HS coaching splits are query-time computations over per-game data — recent form (last 5-7 games), doubleheader fatigue (game 1 vs 2), season phase (early/mid/late). Traditional home/away splits have low value at HS level (no real home field advantage, small samples). Per-game stat population (E-117) is the prerequisite for all high-value analytics — pre-aggregated columns are secondary. *(baseball-coach consultation during IDEA-028 promotion)*
- **2026-03-16**: Stat compilation pyramid: boxscore + play-by-play can derive ALL stats GC computes, achieving stat parity between owned and opponent teams without the season-stats API. Pipeline vision: (1) boxscore for basic counting stats, (2) play-by-play parsing for advanced stats (QAB, pitches seen per batter, contact quality), (3) validate play-by-play against boxscore, (4) compile season aggregates from game-level data. This makes play-by-play parsing a keystone future epic for full opponent data parity. *(E-117 refinement — user articulating data compilation strategy)*
- **2026-03-18**: HTTP header fidelity gap: web profile sends `Accept-Encoding: gzip, deflate` but real Chrome sends `gzip, deflate, br, zstd`. iOS user-agent version is `2026.9.0` now, not `2026.7.0` as in our headers. Both are minor fidelity gaps that could trigger detection if GC starts fingerprinting. Consider periodic header refresh from proxy captures. *(E-127 proxy data refinement -- api-scout findings)*
- **2026-03-23**: Claude code-reviewer misses API contract enforcement (required headers, field paths), deploy-time safety (code referencing columns that don't exist yet), and function contract preservation (return-value guarantees dropped during rewrites). Codex catches these systematically because it cross-references endpoint docs, migration state, and function docstrings. The CR agent definition needs enhancement: (1) cross-reference API endpoint docs when reviewing API calls, (2) verify function contracts (docstring promises vs implementation) on rewrites, (3) check deploy-time column dependencies against migration state. These are rubric gaps, not agent capability gaps -- CR can do this work if instructed. *(E-147 implementation -- 4 Codex rounds found 10 findings CR missed)*
- **2026-03-22**: Coach-curated spaces: each coach gets a tailored view that feels like "theirs." Admin curates the experience initially -- manages teams, opponents, and linkages per coach. Admin can "see what they see" from the admin area. Over time, coaches gain self-service capabilities (add teams, link opponents). Dashboard may evolve into the coach's workspace, not just a stats viewer. The admin role is "experience curator" -- building each coach's world for them, then gradually handing over controls. *(conversation during E-147 planning -- user describing the next major UX evolution)*
- **2026-03-25**: May adopt Cloudflare Email OTP as the app's primary auth mechanism (replacing or supplementing E-023 magic links). CF Access is present on bbstats.ai but not currently enforcing auth -- the app handles auth internally. CF Email OTP would shift auth responsibility to Cloudflare's edge, simplifying the app layer. *(E-157 planning -- user decision during Cloudflare deployment setup)*
- **2026-03-24**: GC opponents come in two flavors: (1) linked to a real GC team (imported roster, bridgeable for stats), and (2) manually-entered name only (no roster, no GC link -- e.g., "Lincoln Southeast" added by coach without linking). The system must handle both: linked opponents get auto-scouting, unlinked opponents need a manual linking path. Importantly, the roster imported via GC linking is "my copy" -- edits only affect the local opponent record, not the source team. The coaching dashboard must be organized around "I am coaching [my team]" → "here are my upcoming opponents from the schedule" → "click to scout." Schedule-based opponent discovery (not just completed games) is the missing pipeline step. *(conversation during E-150 dispatch -- user describing real coaching workflow)*
- **2026-03-18**: Opponent dashboard should lead with pitching, not batting. Coach's first question for any opponent is "Who am I facing on the mound?" -- likely starters, K/BB ratio, recent outings, pitch count/availability. Own-team view correctly leads with batting (lineup decisions). The two views should eventually have distinct content ordering. Priority: (1) key pitchers + availability, (2) team batting tendencies (K%, BB%, OBP), (3) power vs contact breakdown, (4) record + recent form, (5) familiar faces sidebar. *(E-127 refinement -- baseball-coach consultation on season selector placement)*
- **2026-03-26**: Coaches want full-season opponent spray charts — all BIP events from all of the opponent's games, not just games played against LSB. Current pipeline only collects spray data from LSB's own games. Requires scouting pipeline to also fetch player-stats spray data per opponent game. Threshold on opponent detail page (≥20 BIP) should be lowered/removed — show whatever data exists. *(user feedback during dev session)*
- **2026-03-29**: Display thresholds must be calibrated for youth/HS season length, not pro baseball. A 25-game season is normal at 8U-14U; even HS varsity may only play 30. Current thresholds (10 BIP for spray, 10 AB for heat maps) are too conservative -- they suppress useful data for most of the season. The system should show whatever data exists at a pragmatically reasonable minimum and improve as depth grows. "One BIP probably shouldn't generate a spray chart, but 3-4 should." This coaching-reality calibration must be baked into the context layer so every agent building thresholds thinks in terms of 25-game seasons, not 162. *(user feedback after E-186 report generation -- Millard South had 5 games, no heat maps despite 18 batters)*
- **2026-03-29**: Never suppress rate stats just because the sample is small. A 189.0 ERA on 0.1 IP tells the coach "this kid got shelled" -- that's useful information, not noise. Show all stats, mark small samples with context (asterisk + footnote), let the coach decide what matters. The system presents data; the coach is the analyst. Rate stat suppression at low sample sizes (hiding behind "--") removes signal a coach would want. *(user feedback on Lincoln Southwest Freshman report -- C Johnson's 189.0 ERA on 0.1 IP)*
- **2026-04-02**: Reports MUST include plays-derived stats (FPS%, QAB, pitch counts, pitch sequences). The plays pipeline (E-195) and reconciliation engine (E-198) exist but their output is not surfaced in standalone reports -- the primary coaching delivery tool. Reports currently show only boxscore aggregates. This is the #1 delivery gap. *(User: "the only part of the system that I use right now")*
- **2026-03-27**: `POST /search` could replace the entire multi-step opponent resolution chain (`docs/api/flows/opponent-resolution.md`). Returns both UUID and `public_id` in one call, works for ALL opponents regardless of `progenitor_team_id` presence, eliminates the follow-bridge-unfollow hack for null-progenitor opponents (~14%). The auto-resolver could search by name instead of relying on `progenitor_team_id`. Supersedes 2026-03-11 signal about `/search/opponent-import`. *(discovered during E-167 dispatch -- live capture of POST /search response schema)*
- **2026-04-12**: Coaches need a trust-verification mechanism so bbstats.ai reports can be spot-checked against the GC app view. Current reports highlight OBP-framed leaders (Top Bat callout, spray chart PA counts) while the GC app surfaces an AVG-ranked batting table — correct data, hard to eyeball against. When these two were compared for Gretna Varsity (report generated 2026-04-11 with data through Apr 10, GC app screenshot 2026-04-11 4:53 PM), the apparent "they differ quite a bit" feeling was fully explained by (1) different sort metrics, (2) PA vs AB framing, (3) a one-day freshness gap — no data contamination, no architecture problem. But the perception gap is itself the signal: coaches need to be able to build trust in the data without doing arithmetic by hand. Candidates: full AVG-ranked batting table in reports matching the GC layout; a `bb data verify <public_id>` cross-check tool that fetches GC public data and diffs against our DB; more prominent "through Apr 10 · 13 games · 8-5" freshness header. *(session debugging divergent-stats concern during E-220 → E-221 → E-222 recovery, 2026-04-12)*
- **2026-06-12**: Reports-first strategic reframe (supersedes parts of VISION.md Layers 2-4). The product as actually used is: log in → generate a one-off scouting report for a GameChanger `public_id` → share the link. Member-team sync, the dashboard, and tracked-opponent management are unused and slated for quarantine-then-removal (~25-30% of the codebase). The forward feature is a scheduling system that generates a fresh opponent report the morning of each game and delivers it to coaches. Explicit non-goals now: cross-team player identity (E-104 de-scoped), multi-season/longitudinal rollups, member-team season-management product, standing opponent registry, multi-user team-scoped permissions, distributed job infra. Captured in `docs/ROADMAP.md` (epic sequence A-E, protected core, safety gates). *(architecture evaluation session 2026-06-09→06-12; docs/ROADMAP.md §7)*
- **2026-06-08**: If the Tier-2 LLM narrative becomes a regular part of coaches' pre-game workflow, revisit the no-coach-visible-label decision from E-233 — Tier-2 silently disappearing on LLM-failure days can erode trust in report consistency over time. Mitigation in the meantime: keep Tier-1 quality high so Tier-2 reads as "bonus context" rather than a load-bearing section. *(baseball-coach consultation during E-233 planning)*
- **2026-06-15**: Once Epic E runs reports unattended (morning-of-game cron), the operator run record (`report_generation_runs`) and the coach-facing footer / no-games page become the SOLE trustworthiness signal — there is no human eyeballing each report anymore. E-236 hardened both surfaces to be honest (per-stage `partial`/`failed` statuses, count columns, the all-blocked→`failed` outcome, the two-case no_games copy, the derived operator-degraded flag) precisely so Epic E's monitoring/alerting can be built ON these signals: an unattended run is "trustworthy" iff `overall_status='completed'` AND no per-stage status is `partial`/`failed`. Epic E should surface/alert on `overall_status` + per-stage statuses + the derived operator-degraded flag rather than re-deriving report health from raw data. *(E-236 closure 2026-06-15 — the self-reporting honesty layer is the trust substrate for unattended operation)*
- **2026-06-14**: Season scoping (and the `season_fallback` trust flag from E-235) likely matters much less in the report-only direction. In the earlier vision `season_id` was the load-bearing partition key for multi-season rollups, longitudinal tracking, and cross-team identity — all now explicit non-goals. In report-only, `season_id` is mostly a within-report game filter ("which of this team's games go in this one shared report"), and year-only scoping is the correct, complete window for the common single-season travel/USSSA team. The `season_fallback` flag (fires whenever `program_type` is missing) therefore over-fires for the whole travel-team class, risking operator alarm fatigue. The only case where season scoping still genuinely matters is a single `public_id` that blends distinct programs in one calendar year (e.g., spring-HS + summer legion) — a narrow risk. Open question for vision/coach: does cross-program blending actually occur for the teams LSB scouts? If not, season scoping is largely vestigial in report-only and the flag is noise. *(dev session 2026-06-14, generating MBA Top Dogg Gold 14U report — season_fallback fired with anchor identity + 48/48 coverage; user: "I'm not sure that it matters as much as it did in an earlier vision")*
- **2026-06-17**: Coach email as summary, not just a link. Coaches checking phones pre-game want the key numbers in the email body itself (top 2 pitchers, OBP, steal rate) without clicking through to the report. Beyond Epic E scope but a natural next step once scheduled delivery is established. *(baseball-coach consultation during E-240 discovery)*
- **2026-06-17**: Pre-season dry-run ritual. `bb report morning-run --dry-run` run before Opening Day surfaces every unresolved opponent so the operator can `map-opponent` all of them before the first game. Operator workflow worth documenting/onboarding, possibly a coached guide. *(baseball-coach consultation during E-240 discovery)*
- **2026-06-17**: "Regenerate now" button on the admin reports page. If a scheduled report came out empty (early season) or a coach wants a refresh for a late-added opponent, one-click regenerate from the admin UI beats the manual CLI. *(baseball-coach consultation during E-240 discovery)*
- **2026-06-20**: The serving surface is becoming a **collection of tools, not an admin panel**. Scouting reports (`/reports`) are one tool among several, and the current `/admin/reports` placement feels wrong — these aren't admin functions, they're tools a coach/operator *uses*. Emerging model: a **tools hub** with a **two-axis auth model** — login gates (a) **discoverability** (logged-in users see the tool list in nav) and (b) **privileged actions** (generate / delete / manage), but NOT (c) **basic access** — tools are publicly reachable by direct link even when logged out. This mirrors how `/reports/{slug}` already serves with no auth; the new GC stats aggregator will be the same (in nav when logged in, still reachable when not). Implication: split genuine operator/system config (the real "admin") from coach-facing tools, and reframe auth around *visibility + management* rather than an *access wall*. Wants a UX/IA pass to design the hub nav + the two-axis auth model. *(user musing 2026-06-20, post-E-240: "/reports just don't feel like /admin tools")*
- **2026-06-20**: Future tools beyond scouting reports, all peers in the tools hub: (1) a **GC stats aggregator** that ingests CSVs exported from multiple GameChanger seasons and combines them — including merging two teams with *mostly* the same roster into one wholistic stat set (roster-continuity merge); a Claude skill prototype already does this. NOTE this is deliberately **multi-season** and is NOT in tension with E-241 (which removes cross-season from the reports *core*) — it's a separate, explicitly cross-season offline tool, which is exactly where multi-season logic should live now that the report core is single-season-snapshot. (2) A **report deep-dive**: take a generated scouting report and have an LLM conversation about it (mechanism TBD). *(user 2026-06-20)*
- **2026-06-28**: Per-pitch **pitch-type / pitch-selection** data is available from GameChanger when the scorekeeper charts it — it arrives as a trailing parenthetical on the plays template (`"Strike 1 looking (Curveball)"`, `"Ball 1 (Fastball)"`). Currently uncommon in scouted opponents' games, but the user would love to surface it for scouting (pitch mix, sequencing tendencies) if it proves more prevalent. Capture/persist it now since it's free (already in `play_events.raw_template`), measure prevalence across the DB, and revisit surfacing it in reports once we see how often opponents chart it. *(dev session 2026-06-28, diagnosing the Empire Netting FPS%/P-PA dilution — the pitch-type suffix was the root cause of the broken stats; user: "GREAT that they include pitch selection … I'd love to use it if we find it being more prevalent")*
- **2026-06-28** [PROMOTED-to-VISION 2026-06-28 — now the "Always Get Closer to Byte-Identical Play Ingestion" north star in docs/VISION.md]: **North-star goal — "byte-identical" play ingestion.** Get the plays parser accurate enough that plays-derived box scores reconcile against GameChanger's official box scores across an ENTIRE SEASON as closely as mathematically possible. The user explicitly de-prioritized speed ("I don't need to fix stats in a hurry — I'd rather do it right the first time"). Implies two structural moves: (1) a **reconciliation scoreboard** that derives box-score stats from plays and diffs them, per stat, against the official box scores over the full season — the measurable target for ingestion fidelity (extends the existing `src/reconciliation/` BF/pitcher-attribution engine and `bb report verify-aggregates` from boxscore→season into plays→boxscore); (2) adopting the **structured `game-streams/{game_stream_id}/events` endpoint** (typed `result`/`speed`/`style`/`createdAt`/`speedProvider`, no text parsing) as the higher-fidelity pitch-level source instead of regex-parsing the flattened plays templates. The FPS%/P-PA dilution bug (pitch-type suffix silently dropped) is one *symptom* of low-fidelity text parsing — fixing it is necessary but not the goal. The goal is measured ingestion fidelity. *(dev session 2026-06-28, diagnosing the Empire Netting stats — user wants to set this as a /goal)*
