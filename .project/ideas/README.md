# Ideas Backlog

This directory holds pre-epic ideas: directions, problems, and future plans that are worth remembering but are not yet ready to be structured as epics.

An idea becomes an epic when:
1. A dependency clears (e.g., a blocking epic completes)
2. The pain becomes real (we hit the problem the idea was meant to solve)
3. A strategic decision makes it a priority

Ideas do NOT have stories, acceptance criteria, or assignees. They are low-friction captures.

## Review Cadence
Review this list every 90 days, or when completing an epic. Ask:
- Is any CANDIDATE now unblocked?
- Should any CANDIDATE be promoted or discarded?
- Are there ideas we've already solved implicitly?

## Index

| ID | Title | Status | Review By |
|----|-------|--------|-----------|
| [IDEA-001](IDEA-001-local-cloudflare-dev-container.md) | Local Cloudflare Dev Container | DISCARDED | 2026-02-28 -- superseded by E-009 |
| [IDEA-002](IDEA-002-web-scraping-fallback.md) | Web Scraping Fallback Strategy | CANDIDATE | 2026-05-29 |
| [IDEA-003](IDEA-003-github-epics.md) | Work Management as Agent Interface | CANDIDATE | 2026-05-29 |
| [IDEA-004](IDEA-004-pii-protection.md) | Hard Data Boundaries and PII Protection | PROMOTED | 2026-03-02 -- promoted to E-019 |
| [IDEA-005](IDEA-005-intent-nodes.md) | Directory-Scoped Intent Nodes at src/ Module Boundaries | CANDIDATE | 2026-06-01 |
| [IDEA-006](IDEA-006-epic-lanes-convention.md) | Epic Lanes Convention for Multi-Workstream Epics | CANDIDATE | 2026-06-01 |
| [IDEA-007](IDEA-007-dispatch-coordinator-guardrail.md) | Dispatch Coordinator Guardrail -- Prevent Team-Lead-as-PM Bypass | DISCARDED | 2026-03-07 -- resolved by E-065 |
| [IDEA-008](IDEA-008-plays-and-line-scores.md) | Pitch-by-Pitch Plays and Inning Line Scores Crawling | PROMOTED | 2026-07-12 -- delivered by E-195 + E-245 |
| [IDEA-009](IDEA-009-per-player-game-stats-spray-charts.md) | Per-Player Per-Game Stats and Spray Charts | PROMOTED | 2026-07-12 -- delivered by E-158 + E-166 |
| [IDEA-010](IDEA-010-docs-port-map-consistency.md) | Docs Port Map Consistency for Devcontainer + Compose | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-07) |
| [IDEA-011](IDEA-011-investigate-500-endpoints.md) | Investigate HTTP 500 Endpoint Failures | DISCARDED | 2026-07-12 -- org-level/bulk-opponent surfaces removed by the E-239 reports-first descope |
| [IDEA-012](IDEA-012-crawl-orchestration-system.md) | Crawl Orchestration and Scheduling System | DISCARDED | 2026-07-08 -- member-sync pipeline removed (E-239); forward scheduling delivered by E-240 (`bb report morning-run` cron-CLI); ROADMAP §7 bars a standing orchestration system |
| [IDEA-013](IDEA-013-cmux-evaluation.md) | cmux Evaluation for Agent Teams | DISCARDED | 2026-07-12 -- operator ruled discard: its own discard condition ("no concrete problem surfaces") has fired |
| [IDEA-014](IDEA-014-mobile-web-api-doc-split.md) | Mobile vs. Web API Documentation Split | CANDIDATE | 2026-06-05 |
| [IDEA-015](IDEA-015-programmatic-auth-module.md) | Programmatic Auth Module | PROMOTED | 2026-03-08 -- promoted to E-077 |
| [IDEA-016](IDEA-016-codex-hardening-and-validation.md) | Codex Hardening and Validation Trail Map | CANDIDATE | 2026-06-07 |
| [IDEA-017](IDEA-017-api-relationship-documentation.md) | API Relationship and Chain Documentation | CANDIDATE | 2026-06-07 |
| [IDEA-018](IDEA-018-fuzzy-llm-opponent-resolution.md) | Fuzzy LLM Opponent Resolution | DISCARDED | 2026-07-12 -- opponent_resolver deleted (E-239); reports take a public_id directly |
| [IDEA-019](IDEA-019-retroactive-opponent-stat-crawling.md) | Retroactive Opponent Stat Crawling | PROMOTED | 2026-03-12 -- promoted to E-097 |
| [IDEA-020](IDEA-020-public-endpoint-opponent-ingestion.md) | Public Endpoint Opponent Data Ingestion | PROMOTED | 2026-03-12 -- promoted to E-097 |
| [IDEA-021](IDEA-021-database-migration-process.md) | Database Migration Process Definition | DISCARDED | 2026-07-12 -- forcing function de-scoped; migration process solved in practice (E-100/E-220/E-239) |
| [IDEA-022](IDEA-022-scouting-flow-doc-schema-mismatch.md) | Scouting Flow Doc / Schema Stat Mismatch | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-09) |
| [IDEA-023](IDEA-023-env-and-db-backup-automation.md) | Automated .env and app.db Backup | CANDIDATE | 2026-06-13 |
| [IDEA-024](IDEA-024-postcreatecommand-refactor.md) | Refactor postCreateCommand into Bootstrap Script | CANDIDATE | 2026-06-13 |
| [IDEA-025](IDEA-025-test-fixture-migration-driven.md) | Migration-Driven Test Fixtures | CANDIDATE | 2026-06-14 |
| [IDEA-026](IDEA-026-context-layer-placement-audit.md) | Context Layer Placement Audit -- Phase 2 | CANDIDATE | 2026-06-15 |
| [IDEA-027](IDEA-027-unified-team-lifecycle.md) | Unified Team Lifecycle -- Consult → Refine → Dispatch in One Team | PROMOTED | 2026-03-19 -- promoted to E-140 |
| [IDEA-028](IDEA-028-loader-stat-population.md) | Loader Stat Population (Per-Game + Season) | PROMOTED | 2026-03-16 -- promoted to E-117 |
| [IDEA-029](IDEA-029-lr-split-population.md) | L/R Split Data Population | CANDIDATE | 2026-06-14 |
| [IDEA-030](IDEA-030-fielding-catcher-pitch-type-tables.md) | Fielding, Catcher, and Pitch Type Tables | CANDIDATE | 2026-06-14 |
| [IDEA-031](IDEA-031-stat-blending-logic.md) | Stat Blending Logic | DISCARDED | 2026-07-12 -- season_stats_loader + member season-stats write path deleted (E-239); reports derive from boxscore/plays only |
| [IDEA-032](IDEA-032-multi-credential-per-program.md) | Multi-Credential per Program | DISCARDED | 2026-07-12 -- multi-program de-scoped (ROADMAP §7); single credential suffices |
| [IDEA-033](IDEA-033-bulk-team-import.md) | Bulk Team Import from /me/teams | DISCARDED | 2026-06-16 -- member-team sync quarantined + member-team season-management product de-scoped (reports-first reframe) |
| [IDEA-034](IDEA-034-program-crud-admin.md) | Program CRUD Admin Page | DISCARDED | 2026-07-08 -- admin trimmed to reports-only (E-239); no program-management product in the reports-first reframe; single seeded `lsb-hs` suffices, `programs` inert/FK-only (IDEA-091) |
| [IDEA-035](IDEA-035-opponent-page-redesign.md) | Opponent Page Redesign | DISCARDED | 2026-06-16 -- opponent discovery quarantined + tracked-opponent registry de-scoped (reports-first reframe) |
| [IDEA-036](IDEA-036-dashboard-program-awareness.md) | Dashboard Program Awareness | DISCARDED | 2026-06-16 -- dashboard quarantined + multi-program de-scoped (reports-first reframe) |
| [IDEA-037](IDEA-037-scouting-report-redesign.md) | Scouting Report Redesign | PROMOTED | 2026-07-12 -- delivered by E-185 (+ E-159, E-192) |
| [IDEA-038](IDEA-038-query-time-splits-and-streaks.md) | Query-Time Splits and Streaks | CANDIDATE | 2026-06-14 |
| [IDEA-039](IDEA-039-game-metadata-enrichment.md) | Game Metadata Enrichment | CANDIDATE | 2026-06-14 |
| [IDEA-040](IDEA-040-optimistic-pitching-column-audit.md) | Optimistic Pitching Column API Audit | DISCARDED | 2026-07-12 -- the loader whose column mapping it audits was deleted (E-239) |
| [IDEA-041](IDEA-041-play-by-play-stat-compilation.md) | Play-by-Play Stat Compilation Pipeline | PROMOTED | 2026-07-12 -- delivered by E-195 + E-245 + E-198 |
| [IDEA-042](IDEA-042-bulk-create-opponents-missing-links.md) | bulk_create_opponents Should Create team_opponents Links | DISCARDED | 2026-06-16 -- opponent discovery + dashboard quarantined; reports need no team_opponents link |
| [IDEA-043](IDEA-043-fuzzy-duplicate-detection.md) | Fuzzy Duplicate Team Detection | CANDIDATE | 2026-06-23 |
| [IDEA-044](IDEA-044-prevent-duplicate-team-creation.md) | Prevent Duplicate Team Creation | PROMOTED | 2026-03-27 -- promoted to E-167 |
| [IDEA-045](IDEA-045-worktree-divergence-detection.md) | Detect Main-Branch Divergence Before Epic Closure Patch | CANDIDATE | 2026-06-24 |
| [IDEA-046](IDEA-046-resolver-duplicate-gc-uuid.md) | OpponentResolver Creates Duplicate gc_uuid Team Instead of Merging | PROMOTED | 2026-03-26 -- promoted to E-162 |
| [IDEA-047](IDEA-047-worktree-diff-phantom-deletions.md) | Epic Worktree `git diff main` Shows Phantom File Deletions | DISCARDED | 2026-07-12 -- prime cause (rtk rewriting git output) removed by E-229; closure uses --cached; likely unreproducible |
| [IDEA-048](IDEA-048-spray-chart-fielder-zones.md) | Fielder Position Labels/Zones on Spray Charts | CANDIDATE | 2026-06-25 |
| [IDEA-049](IDEA-049-spray-chart-pull-center-oppo.md) | Pull/Center/Oppo Tendency Summary on Spray Charts | CANDIDATE | 2026-06-25 |
| [IDEA-050](IDEA-050-spray-chart-hot-cold-zones.md) | Count Overlay / Hot-Cold Zones on Spray Charts | CANDIDATE | 2026-06-25 |
| [IDEA-051](IDEA-051-spray-chart-title-stats.md) | Title with Stats on Spray Charts | CANDIDATE | 2026-06-25 |
| [IDEA-052](IDEA-052-familiar-faces-indicator.md) | Familiar Faces Indicator on Opponent Rosters | DISCARDED | 2026-06-16 -- depends on cross-team player identity + longitudinal tracking (permanent §7 non-goals) |
| [IDEA-053](IDEA-053-opponent-workflow-fix.md) | Fix Opponent Scouting Workflow End-to-End | PROMOTED | 2026-03-28 -- delivered by E-173 |
| [IDEA-054](IDEA-054-worktree-guard-cross-contamination.md) | Worktree Guard Should Prevent Cross-Epic Contamination | CANDIDATE | 2026-06-26 |
| [IDEA-055](IDEA-055-auto-sync-and-experience-polish.md) | Auto-Sync and Experience Polish | PROMOTED | 2026-06-27 |
| [IDEA-056](IDEA-056-search-fallback-team-return-bug.md) | Fix _search_fallback_team Return Type Bug | DISCARDED | 2026-07-12 -- crawlers/opponent_resolver.py deleted by E-239-05; _search_fallback_team gone from src/ |
| [IDEA-057](IDEA-057-report-flow-orphan-team-stubs.md) | Report Flow Orphan Team Stubs | PROMOTED | 2026-03-29 -- promoted to E-188 |
| [IDEA-058](IDEA-058-pyproject-dependency-management.md) | Proper Python Dependency Management via pyproject.toml | PROMOTED | 2026-03-29 -- promoted to E-190 |
| [IDEA-059](IDEA-059-opponent-flow-spray-gaps.md) | Opponent Flow Spray Chart and Display Gaps | PROMOTED | 2026-03-29 -- promoted to E-189 |
| [IDEA-060](IDEA-060-flow-testing-and-validation.md) | Comprehensive Flow Testing and Validation | DISCARDED | 2026-07-12 -- 9 of 14 flows target E-239-deleted surfaces; surviving report flows covered by E-234 guards |
| [IDEA-061](IDEA-061-season-id-from-team-context.md) | Derive season_id from Team Context, Not Filesystem Path | PROMOTED | 2026-06-30 | → E-197 |
| [IDEA-062](IDEA-062-plays-boxscore-reconciliation.md) | Plays-vs-Boxscore Reconciliation Engine | PROMOTED | 2026-04-01 |
| [IDEA-063](IDEA-063-dump-game-skill.md) | /dump-game Diagnostic Skill | CANDIDATE | 2026-04-02 |
| [IDEA-064](IDEA-064-dashboard-report-parity.md) | Dashboard-Report Feature Parity | DISCARDED | 2026-06-16 -- dashboard quarantined; reports are the sole forward scouting surface |
| [IDEA-065](IDEA-065-llm-eval-harness.md) | LLM Starter Prediction Evaluation Harness | CANDIDATE | 2026-07-03 |
| [IDEA-066](IDEA-066-league-level-detection.md) | League/Level Detection for Pitch Rules | PROMOTED | 2026-07-06 | -> E-218 |
| [IDEA-067](IDEA-067-catcher-pitcher-restriction.md) | Catcher-Pitcher Restriction (NSAA) | CANDIDATE | 2026-07-06 |
| [IDEA-068](IDEA-068-evaluate-main-session-dispatch-behaviors.md) | Evaluate Main-Session Dispatch Behaviors for Codification | DISCARDED | 2026-07-12 -- operator ruled discard: meta-layer FROZEN post-E-260; codifying dispatch behaviors is overtaken |
| [IDEA-069](IDEA-069-consolidate-cascade-delete-logic.md) | Consolidate Cascade Delete Logic (admin + reports) | PROMOTED | 2026-04-13 -- absorbed into E-221-05 |
| [IDEA-070](IDEA-070-admin-ui-delete-path-reports-gap.md) | Admin-UI Delete-Team Path Does Not Clean `reports.team_id` | DISCARDED | 2026-07-12 -- the admin delete-team path it patches was deleted by E-239 |
| [IDEA-071](IDEA-071-e220-adopter-audit-fix-pre-provenance-code.md) | E-220 Adopter Audit — Fix Pre-Provenance Code Paths | PROMOTED | 2026-04-13 -- promoted to E-223 |
| [IDEA-072](IDEA-072-rtk-compression-retrospective-audit.md) | RTK Compression Retrospective Audit | CANDIDATE | 2026-08-28 |
| [IDEA-073](IDEA-073-full-suite-ci-gate.md) | Full-Suite CI Gate (GitHub Actions or Equivalent) | CANDIDATE | 2026-08-29 |
| [IDEA-074](IDEA-074-starlette-deprecation-migration.md) | Migrate Starlette Deprecations Before a Framework Upgrade Breaks Them | PROMOTED | 2026-06-07 -- promoted to E-232 |
| [IDEA-075](IDEA-075-harness-output-reliability.md) | Harness Output-Reliability Fix (stop the garble/drop/silent-edit thrash) → E-231 | PROMOTED | 2026-08-29 |
| [IDEA-076](IDEA-076-spray-value-regression-guard.md) | Spray-Value Regression Guard (E-234-05 left spray shape-only) | CANDIDATE | 2026-09-11 |
| [IDEA-077](IDEA-077-reevaluate-season-fallback-trust-flag.md) | Re-evaluate season_fallback Trust Flag — Option A coach-visible line DELIVERED by E-236-06; machinery removal residual → ROADMAP D2 | PROMOTED | 2026-06-15 -- Option A delivered by E-236-06 |
| [IDEA-078](IDEA-078-coaching-docs-dashboard-staleness.md) | Coaching docs still sell a dashboard-first / longitudinal product — reports-first rewrite (bounded docs cleanup; surfaced by drift scour) | DISCARDED | 2026-07-12 -- already resolved by E-239; dropped in E-262 review (story 08 ABANDONED) |
| [IDEA-079](IDEA-079-rich-starter-narrative.md) | Reliably Rich Predicted-Starter & Bullpen Narrative — pin-vs-stabilize the HEAD-vs-prod richness gap (diff prompt across tag range first) | CANDIDATE | 2026-09-14 |
| [IDEA-080](IDEA-080-coach-facing-scheduled-report-delivery.md) | Coach-Facing Scheduled Report Delivery — email links to coaches the morning of the game (deferred from E-240; carries coach email-content MUST-HAVEs + the stable-URL/extended-expiry option) | CANDIDATE | 2026-09-15 |
| [IDEA-081](IDEA-081-post-e241-dead-code-stale-example-sweep.md) | Post-E-241 dead-code + stale-example sweep (scout_all-orphaned freshness-gating cluster + dead format_season_display + stale compound-slug example comments; deferred-whole from E-241) | PROMOTED | 2026-07-12 -- delivered by E-246-02 |
| [IDEA-082](IDEA-082-twin-athlete-uuid-resolution.md) | GameChanger active/removed athlete-UUID "twin" resolution (one human split across two UUIDs fragments opponent stats; deterministic status-gated merge, re-base detection off team_rosters; blockers: re-validate blast-radius on clean DB + DE ratifies X-vs-Y) | CANDIDATE | 2026-09-25 |
| [IDEA-083](IDEA-083-per-arm-estimate-marker.md) | Per-arm estimate marker for IP-proxied arms in non-estimate probable-starter sections (deferred from E-243 by UXD "section-level estimate suffices, Simple first"; promote if proxied-arm-in-varsity proves common or a coach is misled) | CANDIDATE | 2026-09-25 |
| [IDEA-084](IDEA-084-scouting-coverage-fill.md) | Scouting-coverage fill to lift probable-starter accuracy (lever A: report-time opponent completed-schedule fill via existing no-auth public pipeline; ~40%→50-55% top-2, bounded by committee entropy; memo `.project/research/scout-coverage-lever.md`; open: report-time vs scheduled backfill + fetch budget; lever C cross-season is project non-goal) | CANDIDATE | 2026-09-26 |
| [IDEA-085](IDEA-085-richer-llm-data-block-field-translations.md) | Richer LLM data-block field-translations to match Variant A SOT exactly (E-243-04 conscious-accepts: null-pitch IP-proxy `pitch_display` numeric form + structured UNAVAILABLE rows; both AC-compliant + jargon-free today, refinement needs richer engine output) | CANDIDATE | 2026-09-26 |
| [IDEA-086](IDEA-086-leverage-pitch-selection-velocity.md) | Leverage pitch selection + velocity in scouting (E-245 stores per-pitch `pitch_type` + `pitch_speed_mph`; future pitch-mix/sequencing/velocity in reports; scorekeeper-coverage dependent; overlaps IDEA-030) | CANDIDATE | 2026-09-27 |
| [IDEA-087](IDEA-087-multi-pitcher-boundary-attribution-drift.md) | Multi-pitcher-boundary attribution drift (cause-4; +23 BF outlier `e283438c`, NOT a self-game; within-game pitcher-boundary mis-assignment; scoped OUT of E-245; likely a recon-engine BF-corrector gap) | CANDIDATE | 2026-09-27 |
| [IDEA-088](IDEA-088-per-game-sentinel-no-name-opponents.md) | Per-game sentinel for genuinely no-name unresolvable opponents (E-245-04 shared "Unknown Opponent" stub + `_find_duplicate_game` natural-key dedup could conflate two no-name opponents on same team+date; reviewer awareness-only, NOT a within-AC defect; reuse the loader's game-suffixed sentinel technique; real 23 self-games resolve by name so unreached today) | CANDIDATE | 2026-09-27 |
| [IDEA-089](IDEA-089-terminal-cooccurrence-fork-disambiguation.md) | Terminal co-occurrence fork disambiguation — Tier 2 of E-249: use same-game co-occurrence between component terminals to auto-collapse genuine same-human forks (Jo/John/Jon) while still refusing true two-human forks (O/<NAME-REDACTED>/Owen). E-249 conservatively refuses ALL forks; this recovers same-human residuals + may add durable operator surfacing. Blocked by E-249 + live team_id=196 validation | CANDIDATE | 2026-09-28 |
| [IDEA-090](IDEA-090-codex-review-script-modernization.md) | Codex review/spec-review script modernization (v0.142.4) — 4 independent cleanups from the CA+SE tooling A/B (KEEP-custom decision already made): refresh stale v0.107.0 headers→v0.142.4; re-verify the now-maybe-obsolete CODEX_SANDBOX_OFF branch; adopt `-o/--output-last-message` to harden the read-receipt gate; fix `codex-review.sh` uncommitted-mode under-feeding untracked file CONTENTS (not just names). CA owns skill-side impl when promoted | CANDIDATE | 2026-09-28 |
| [IDEA-091](IDEA-091-e250-descope-leftovers.md) | E-250 de-scope leftovers — carved OUT of E-250: (a) never-read `programs` org-hierarchy machinery + `detect_league_level` unused `program_type`/`classification` params; (b) `_derive_season_id` `min(years)` rule (no driving problem on single-season data, may be DISCARD-worthy). Third E-250 out-of-scope item (season-agnostic overlap-confidence) already handled inside E-250-01 as a comment tweak. Related IDEA-066, IDEA-081 | CANDIDATE | 2026-10-01 |
| [IDEA-092](IDEA-092-de-agent-def-core-entities-stale.md) | `.claude/agents/data-engineer.md` Core Entities table is broadly stale vs the live schema (names non-existent tables like `PlayerTeamSeason`, mis-describes `Lineup`/`PlateAppearance`). E-250-04 removes ONLY the cross-season/`PlayerTeamSeason` cells; this captures the broader table refresh, kept OUT of E-250 to avoid a full agent-def rewrite. Surfaced by CA inventory (Flag B). CA-owned when promoted | DISCARDED | 2026-07-12 -- defect already fixed by E-250-04; residual is freeze-barred enrichment; dropped in E-262 review |
| [IDEA-093](IDEA-093-magic-link-token-in-access-logs.md) | Remove the raw magic-link token from GET-verify access logs — E-254-02's GET/POST split closes the mail-scanner-prefetch threat but the token still rides the GET URL into access logs (log-reader could POST-replay); full fix needs fragment/JS delivery. Surfaced by SE during E-254 consult | CANDIDATE | 2026-10-04 |
| [IDEA-094](IDEA-094-admin-router-dependency-refactor.md) | Router-level admin `Depends` on the reports_admin router (defense-in-depth) — E-254-05 adds a sweep test; this prevents an unguarded admin route from existing by construction. Main design Q: FastAPI raise-HTTPException vs. the current return-Response `_require_admin` shape. Surfaced by SE during E-254 consult | CANDIDATE | 2026-10-04 |
| [IDEA-095](IDEA-095-login-timing-repeated-probe-residual.md) | Constant-time login across all three `post_login` paths — E-254-03 equalizes fresh-known vs unknown (single-probe vector); the rate-limit-suppression branch is lighter, leaving a repeated-probe registered-vs-unregistered timing differential. Accept-and-noted in E-254-03; cheapest fix = same dummy work in the rate-limited branch. Surfaced by SE during E-254 Codex triage | CANDIDATE | 2026-10-04 |
| [IDEA-096](IDEA-096-docs-api-systematic-pii-sweep.md) | Systematic `docs/api/` PII sweep — E-254-07 scrubs the 24 audited files; api-scout found a broader tail (~30 more docs with unredacted real UUIDs + additional real names incl. a likely minor). Blanket UUID→placeholder + a positive "example JSON uses taxonomy placeholders" rule, EXCLUDING GC app-identity client IDs (auth/headers/post-auth). Surfaced by api-scout during E-254 Codex triage | CANDIDATE | 2026-10-04 |
| [IDEA-097](IDEA-097-team-resolver-proxy-pacing-posture.md) | `team_resolver` public calls should adopt the proxy/pacing HTTP posture — `resolve_team`/`discover_opponents` pass `proxy_url=None`, `min_delay_ms=0`, bypassing the Bright Data + pacing posture `http-discipline.md` wants for GC requests. E-252-09 fixed only the ConnectError exception contract; this is the deferred posture half. api-scout domain | CANDIDATE | 2026-10-04 |
| [IDEA-098](IDEA-098-unify-prod-detection-is-production.md) | Unify prod-detection through `is_production()` — narrowly `csrf.py:135`'s inline `APP_ENV=="production"` idiom (E-252-03 scoped csrf.py/main.py OUT). CAVEATS: `auth.py:355` `.lower()` is DIFFERENT semantics (must NOT fold); `main.py` only logs; `reset.py` is a DB util that shouldn't import from api/helpers. SE domain | PROMOTED | 2026-07-12 -- delivered by E-254-01 |
| [IDEA-099](IDEA-099-busy-timeout-non-triad-writers.md) | Broaden `busy_timeout` coverage to non-triad SQLite writers — `bb data` commands (`cli/data.py` ×5) + loaders/crawlers still hand-roll `sqlite3.connect` (some cwd-relative `./data/app.db`), no busy_timeout — same WAL file, outside E-252-06's scheduled-reports triad scope. DE/SE domain | CANDIDATE | 2026-10-04 |
| [IDEA-100](IDEA-100-stat-completeness-guard-inspect-and-narrow.md) | Inspect-and-narrow the three-state `stat_completeness` provenance guards — post-E-239 the member-write path is deleted so the guards never fire on the forward path, but member rows are API-authoritative/not re-derivable and retention was a deliberate E-239 decision (DE finding S3). Inspect live-DB provenance first, then narrow — do NOT delete blind. May be subsumed by E-256's aggregate cutover. Surfaced by the 2026-07-03 platform audit §3. DE domain | CANDIDATE | 2026-10-04 |
| [IDEA-101](IDEA-101-reset-db-guard-whitespace-bypass.md) | Close the `bb db reset` production-guard whitespace bypass — `reset.py:49` uses `.lower()`-only so `APP_ENV=" production "` bypasses the destructive-reset guard (runs without `--force`). Same fail-open class E-254-01 fixed; canonical `is_production()` now exists. Layering caveat: may fix inline `.strip().lower()` rather than import `api/helpers`. Out of E-254's app-security-gate scope. SE/DE domain | CANDIDATE | 2026-10-05 |
| [IDEA-102](IDEA-102-committed-artifact-pii-gap.md) | Close the committed-artifact PII gap — real PII in `epics/`/`.project/` idea/epic/note files is UNGATED: the pre-commit scanner has both dirs in SKIP_PATHS (+ can't detect names anyway), and the E-254-07 byte-gate only covers `docs/api/`. A near-miss real-name leak in a committed idea file (Codex Phase-4b, 2026-07-07) proves it. Extend the denylist sweep + a positive no-real-PII-in-artifacts rule. CA + SE/api-scout | PROMOTED | 2026-07-09 -- promoted to E-256 (story E-256-14) |
| [IDEA-103](IDEA-103-dead-table-retention.md) | Dead-table retention — `crawl_jobs` + `coaching_assignments` are inert (cascade-DELETE only, never INSERT/SELECT); record retention rationale + drop-blocker (cascade rewrite + migration) for a future removal epic. Exclusions: `user_team_access` LIVE, `team_opponents` dropped mig 008, `programs` FK-load-bearing. DE+PM verified. (E-255-06 AC-4) | CANDIDATE | 2026-10-06 |
| [IDEA-104](IDEA-104-docs-api-denylist-completeness-gap.md) | `docs/api/` byte-gate denylist-completeness gap — real-looking identifiers (public_ids, UUIDs, team name "Kearney Mavericks 14U") sit in committed endpoint docs that `check_doc_pii.sh` passes GREEN on = not denylisted. Distinct from IDEA-102 (that = UNGATED paths; this = GATED docs/api, incomplete denylist). First determine real-vs-fake. Surfaced by api-scout in E-255-04. Related IDEA-096 | CANDIDATE | 2026-10-06 |
| [IDEA-105](IDEA-105-architecture-schema-changelog-rewrite.md) | Full rewrite of `docs/admin/architecture.md` "Schema Changes" historical changelog — still cites pre-E-220 migration numbers that no longer map to files. E-255-05 added a clarifying note + fixed the one false claim; full historical rewrite deferred (reference material, not an executable step). Low priority. Surfaced by docs-writer in E-255-05 | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-07) |
| [IDEA-106](IDEA-106-plays-pipeline-analytics.md) | Plays-pipeline analytics menu (renumbered from the unindexed `plays-pipeline-analytics.md` in E-255-06) — pipeline + FPS%/QAB now shipped (E-195/E-245); forward menu = situational hitting, baserunning, contact quality, count splits, two-strike approach. Scorekeeper-coverage dependent. Overlaps IDEA-086/030/038/041/062 | CANDIDATE | 2026-10-06 |
| [IDEA-107](IDEA-107-endpoint-doc-path-variable-naming.md) | Normalize `/game-stream-processing/` endpoint-doc path-variable naming (`game_stream_id` vs `event_id` filenames/placeholders; both take `event_id`). E-255-04 fixed all PROSE routing claims but left the coupled filename+frontmatter+see_also rename. The un-addressed residual when E-073 was archived. Cosmetic, low priority. api-scout owns docs/api | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-09) |
| [IDEA-108](IDEA-108-plausibility-signal-run-record-persistence.md) | Persist E-257-03's report-time plausibility signal as a queryable operator trust flag on `report_generation_runs` (surfaced in `/admin/reports`, E-236 honesty-split pattern) instead of the shipped WARNING-log-only form. Deferred out of E-257-03 (SE+lead recommended simple-first WARNING); promote if operators miss log-only warnings, esp. on unattended morning-run gens. Needs migration + `_RUN_RECORD_COLUMNS` allowlist + round-trip test → DE+SE | CANDIDATE | 2026-10-06 |
| [IDEA-109](IDEA-109-smoke-test-retarget-reports-endpoints.md) | Retarget `scripts/smoke_test.py` at the endpoints the reports product actually uses — it currently probes three member-team endpoints E-239 abandoned, so it proves liveness against a surface nobody uses. E-256 keeps it as-is (records keep-not-delete); this retargets it at the reports dependency chain. Surfaced by SE in E-256 planning. Domain: SE | CANDIDATE | 2026-10-07 |
| [IDEA-110](IDEA-110-starlette-testclient-httpx-deprecation.md) | Resolve the `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated` surfaced by E-256's fastapi 0.139/starlette 1.3.1 upgrade — non-fatal now (suite green), future-breaking. Fix before the next starlette bump turns it red. Surfaced by SE in E-256 planning. Domain: SE | CANDIDATE | 2026-10-07 |
| [IDEA-111](IDEA-111-operations-runbook-creds-check-collapse.md) | Collapse the `operations.md:884` credential-recovery recipe now that `bb creds check` is itself an end-to-end `/me/user` probe — the recipe may carry redundant/superseded steps. Runbook hygiene, interacts with IDEA-109. Surfaced by CA in E-256 planning. Domain: docs-writer | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-07) |
| [IDEA-112](IDEA-112-narrow-pii-suppressors-credential-patterns.md) | Narrow the PII suppressors (`# pii-ok`, `synthetic-test-data`) so they can't silence CREDENTIAL patterns (email/phone stay suppressible) — closes the staged-blob hole (a real token behind a marker is certified clean; CA ruled §4g does NOT close it). **First story = MEASUREMENT** (re-scan the ~30 marked files with suppression off; SE's prior scan skipped them). Sibling of E-254, NOT E-256. Domain: SE + CA | CANDIDATE | 2026-10-07 |
| [IDEA-113](IDEA-113-season-aggregates-module-misnomer.md) | Rename/fold `src/db/season_aggregates.py` post-E-259 — after the cutover it no longer computes aggregates (only holds the SUM projection a reader consumes); the name is a misnomer. Fold the builders + `*_RECOMPUTE_KEYS` into `src/api/db.py` next to their sole consumer, or rename. Cosmetic/orientation. Surfaced by DE in E-259 holistic. Domain: DE | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-04) |
| [IDEA-114](IDEA-114-clarify-morning-run-dryrun-summary-prose.md) | Clarify CLAUDE.md's morning-run `--dry-run` prose — it reads as if dry-run sends the always-sent summary email, but `src/cli/report.py:581`/`:666` exempt dry-run. This exact ambiguity misled the CR spec-audit (finding 6, refuted by code) during E-256/E-259 planning. One-line fix; NOT folded into either epic (scope creep). Domain: CA | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-05) |
| [IDEA-115](IDEA-115-grep-and-reconcile-eviction-discipline.md) | Codify grep-and-reconcile as the standing eviction discipline — hand-listed eviction sets systematically undercount (proven 3× in one session: DE-memory 1→2→4, backfill docs 2→5→11). PARTIALLY DISCHARGED: the agent-memory-dir half ships in E-259-05 AC-6; residual = a planning-time grep-and-reconcile heuristic for NON-memory story ACs (SEED not ceiling, routed by ownership). Home: `agent-routing.md` / PM checklist. Domain: CA | CANDIDATE | 2026-10-07 |
| [IDEA-116](IDEA-116-worktree-attribution-and-e256-resume-guard.md) | cwd-based worktree attribution for the dispatch hooks (`context-ratchet.sh` + `send-message-counter.sh` + `worktree-guard.sh` share a `head -1` glob that mis-attributes when 2+ worktrees coexist) + the coupled E-256 resume guard (before resuming parked E-256 after E-260 merges, `rm -rf` its stray `.dispatch-log/` so `git add -A` doesn't sweep mis-written counter/.tsv files). Operator ruled ship-E-260-as-is with this follow-up. Domain: CA | CANDIDATE | 2026-10-10 |
| [IDEA-117](IDEA-117-scope-multi-agent-patterns-never-summarize.md) | Scope `multi-agent-patterns/SKILL.md:24`'s bare "Never summarize" to the dispatch context block (its `:26` already scopes it; the standalone `:24` reads as a blanket verbatim-relay mandate — the over-broad reading E-260 removed elsewhere). Mirror E-260-04's `:203` scoping. Retained-class, ruled OUT of E-260-01 scope. Domain: CA | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-05) |
| [IDEA-118](IDEA-118-refresh-context-fundamentals-ambient-figures.md) | Refresh the stale ambient per-session figures in `context-fundamentals/SKILL.md` (`:28` "614-886 lines" + `:185`/`:193` example budget) that E-260-04's `:74-85` table re-derivation left behind — non-contradicting (ambient SUBSET, not whole-layer) but stale; drop the post-E-213 provenance. Domain: CA | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-05) |
| [IDEA-119](IDEA-119-reconcile-closure-sequence-diff-base-to-merge-base.md) | Reconcile the whole Step 8 closure sequence to ONE merge-base diff base. **DISCARDED/MOOT 2026-07-12** — E-260-01 AC-6 already shipped this on main (whole sequence uses merge-base); the sub-step-9 residual was a stale pre-E-260-worktree reading (same landmine as story-11 finding #1). Surfaced by PM in E-256 story-11 AC verification. Domain: CA | DISCARDED | — |
| [IDEA-120](IDEA-120-pytest-dev-cve-pytest-asyncio-migration.md) | Close the dev-only `pytest 8.3.4` / PYSEC-2026-1845 CVE — blocked by a real resolver conflict (`pytest-asyncio 0.25.0` declares `pytest<9`), so it needs a pytest-asyncio migration + async-suite revalidation, not a pin bump. **Safe to defer: verified the dev lockfile never ships** (`Dockerfile:21-22` installs `requirements.txt` only). Surfaced by SE in E-256-07, which closed the other 32 vulns. Promote if pytest-asyncio unblocks, or if CI adds `pip-audit` on the dev lockfile. Domain: SE | CANDIDATE | 2026-10-09 |
| [IDEA-121](IDEA-121-webauthn-exclude-credentials.md) | Passkey registration never passes `exclude_credentials` to `generate_registration_options()`, so a user can silently re-register an authenticator they already have (duplicate `passkey_credentials` rows). **Not a vulnerability** — no auth bypass; a hygiene/UX defect the WebAuthn spec's `excludeCredentials` exists to prevent. Verified: zero `exclude_credentials` matches repo-wide; the library parameter exists; `auth.py:640`'s comment states the unfulfilled intent. Filed by PM in E-256-08 **before** the dead block was deleted, so the intent outlives the code. Domain: SE | CANDIDATE | 2026-10-10 |
| [IDEA-122](IDEA-122-bb-creds-check-false-green-step1d-preflight.md) | `bb creds check` exits 0 even on dead/expired creds, so the Step 1d closure-smoke env-preflight (which keys on its exit code) proceeds on dead creds and mis-attributes an env failure as an epic failure. Fix: (a) make `bb creds check` exit non-zero when auth fails (root, SE), or (b) have the Step 1d preflight PARSE its output instead of trusting the exit code (defensive, CA). Surfaced in the E-256 Step 1d live-run. Domain: SE or CA | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-06, skill-side --profile web preflight) |
| [IDEA-123](IDEA-123-step1d-generate-before-reconcile-baseline-ordering.md) | Step 1d runs `bb report generate` (a WRITER — can ingest net-new plays) BEFORE `reconcile-scoreboard`, so a legitimate self-caused ingestion delta can trip the one-way ratchet vs a pre-generate baseline (false epic-FAIL). Fix: re-snapshot the baseline post-generate, or ensure the smoke target is already fully in the committed baseline. Surfaced in the E-256 Step 1d live-run; sibling of IDEA-122. Domain: CA (implement/SKILL.md Step 1d) + E-257 baseline-ownership. ~~Do NOT assume the fresh-target case is rare — needs a grounded read.~~ **⛔ FAILURE MODE MOOT 2026-07-26**: the one-way ratchet gate was retired (D2, `877413e`); the scoreboard is a pure diagnostic with no verdict to trip, so the false epic-FAIL described here cannot occur and both Open Questions dissolve with it. **But "moot" ≠ "revert"**: the fix already SHIPPED in E-262-06 (terminal static `.smoke-fixture`, order kept, one operator bootstrap re-snapshot) — this was closed work, not a pending candidate — and whether the static fixture retains value now that its ratchet-specific justification is gone is an OPEN question for CA, not settled by the retirement. The measurement discipline survives the gate. Status left PROMOTED because it is accurate; see [[IDEA-195]] for the vestigial gate code the retirement left standing. | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-06); failure mode moot 2026-07-26 |
| [IDEA-124](IDEA-124-per-game-completeness-rank-dedup-simplification.md) | Retire `player_dedup.py`'s per-game `_COMPLETENESS_RANK` (`:473`) + the unreachable dup-wins branch in `_delete_or_update_game_stats` — vestigial-by-convention (no `src/` writer sets `full`/`supplemented` on `player_game_*`; column is `NOT NULL DEFAULT 'boxscore_only'`), but the CHECK still permits those values and `test_player_dedup.py:~588` defends the schema-permitted state. Independent of the E-259 cutover; DEFERRED out of E-259-02 (Q3, verified by PM). Domain: DE | CANDIDATE | 2026-10-10 |
| [IDEA-125](IDEA-125-eviction-grep-target-doc-trees-and-src-docstrings.md) | Extend `context-layer-assessment.md`'s Deletion-Side-Eviction grep-target enumeration from `.claude/**`+CLAUDE.md+agent-memory to ALSO name all doc trees (`docs/api/`, `docs/ROADMAP.md`, `docs/coaching/` — not just `docs/admin/`) + `src/` docstrings/comments. The E-259 eviction split (05=`.claude`+CLAUDE.md, 06=`docs/admin`) structurally omitted those trees; closure Codex F3 found 3 missed refs. Distinct from IDEA-115 (discipline-within-scope) + AC-6 (memory dirs) — this is the file-tree axis. NOT for the E-259 closure (would re-trip trigger-7). Domain: CA | CANDIDATE | 2026-10-10 |
| [IDEA-126](IDEA-126-league-detection-age-group-range-form.md) | `detect_league_level` misses GC's free-text `age_group` range form (`"Between 13 - 18"`) — only `\d+U` matches, so summer HS-age travel opponents with no NGB fall through to `unknown` -> suppress, killing Most Likely Arms + Tier-2 Scouting Analysis, instead of the intended `youth_travel` -> `PITCH_SMART_15_18` labeled estimate (E-243-02). Detection fix, not an API gap; +companion doc note for the range form. Related IDEA-066/E-218. Domain: SE | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-03 code + E-262-09 doc note) |
| [IDEA-127](IDEA-127-report-name-only-badge-false-positive-sequencing.md) | Report generator stamps `report_generation_runs.identity_match_method='name_only'` in `_ensure_team_row` (`generator.py:1821-1830`, cascade `teams.py:119-176`) BEFORE it back-fills the team's `public_id` (`:1844-1853`) and `gc_uuid` (`:2168-2179`) anchors within the SAME run, so the FIRST direct report for any pre-scouted opponent stub (game-loader-created, `game_loader.py:1617-1623`, NULL public_id at cascade) always shows the operator "name-only match" wrong-team-risk badge (`admin/reports.html:117-120`) even though the team is fully resolvable and self-heals. Sequencing bug, no stat misattribution; erodes badge credibility for the real wrong-team case. Confirmed live (team id=42). Domain: SE | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-03) |
| [IDEA-128](IDEA-128-perspective-provenance-public-scorebook-caveats.md) | Two api-scout-flagged corrections to `.claude/rules/perspective-provenance.md`'s field table (surfaced in E-261 Codex review): add a public-scorebook caveat to the "scores stable across perspectives" row (two independent public scorebooks CAN disagree by a run — E-261's 12-4 vs 12-5), and promote the "Uncertain: public games `id`" row to definitively perspective-specific (post-E-239 the public path is sole populator, `event_id` per-perspective = `game_stream_id`). Context-layer edit, claude-architect domain; out of scope for E-261. Domain: CA | PROMOTED | 2026-07-12 -- folded into E-262 (E-262-05) |
| [IDEA-129](IDEA-129-write-only-raw-api-archive.md) | Write-only raw API response archive — the in-memory crawl-to-load pipeline stores no raw payloads, leaving nothing to inspect/replay when a report looks wrong. Cheap append-only archive keyed by endpoint+time for debugging/replay/fixtures; must stay write-only (not a hot-path cache) + gitignored/redacted. Reconciles the stale CLAUDE.md "store raw before transforming" claim. Source: PLATFORM-AUDIT residual #10. Domain: SE/DE | CANDIDATE | 2026-10-10 |
| [IDEA-130](IDEA-130-persist-rotated-refresh-token-to-data.md) | Persist rotated GC refresh token beyond container-local `.env` — a rotated refresh token lands only in the container-local `.env`, so a container rebuild loses a live token and forces interactive re-auth. Also persist to host-mounted `./data` (same durable location as `app.db`); cheap resilience, same secret discipline as `.env`. Overlaps IDEA-023. Source: PLATFORM-AUDIT residual #11 (WATCH). Domain: api-scout/SE | CANDIDATE | 2026-10-10 |
| [IDEA-131](IDEA-131-scouting-signal-catalog-and-discovery-pass.md) | Scouting Signal Catalog + Discovery Pass — the durable, growable inventory of opponent-intelligence signals computable from data already in the DB (`.project/research/scouting-signal-catalog.md`, 21 `SIG-NNN` entries) plus the discovery-pass hypothesis-sweep METHOD that grows it. The catalog+method meta-artifact E-263 (Deep Scout v1) consumes; holds the SHOULD-tier signals + automated agent-run discovery pass as the v2+ backlog. Companion: `.project/research/deep-scout-design-2026-07-12.md`. Domain: baseball-coach (content) / claude-architect (structure) | CANDIDATE | 2026-10-11 |
| [IDEA-132](IDEA-132-vs-date-matchup-paradigm.md) | The `--vs`/`--date` matchup paradigm — deliberately design how a scouting report pairs an opponent with OUR team + game date (`deep-scout <their_public_id> --vs <our_team> --date <game_date>`, degrading to opponent-only). Unlocks the pairing sections deferred from E-263 v1: self-scout first-inning callout, steal-light-vs-our-runners, matchup-conditioned eligibility; morning-run passes the pairing for free. Operator-requested 2026-07-13 (opponent-only locked for v1). Companion: deep-scout-design §5. Domain: PM/SE | CANDIDATE | 2026-10-11 |
| [IDEA-133](IDEA-133-deep-scout-llm-two-pass-model-eval-gated.md) | Deep Scout v2 LLM two-pass synthesis (model-eval-gated) — narrate the deterministic fact sheet into a coach memo (pass 1) then player-materials under ethics rails (pass 2). E-263 v1 ships the Game Plan deterministically; this is the LLM upgrade. HARD operator gate (2026-07-13): a model-eval step MUST precede any prose ("test models with evals before the prose"). Companion: deep-scout-design §6; overlaps IDEA-079/085. Domain: SE | CANDIDATE | 2026-10-11 |
| [IDEA-134](IDEA-134-play-level-cross-perspective-duplication.md) | Play-level cross-perspective duplication — 17/404 dev-DB games have `plays`/`play_events` double-loaded under two `perspective_team_id`s (game `035d97e2`: 67+67 identical rows). Same cross-perspective family as E-261 (which dedups game ROWS, not play rows). NOT a correctness bug for perspective-scoped aggregates (E-263 TN-3 neutralizes it) but redundant storage + a standing double-count trap. Clarify vs E-261 scope; decide if cleanup warranted. Source: 2026-07-13 Fable-scout Note A. Domain: DE | CANDIDATE | 2026-10-11 |
| [IDEA-135](IDEA-135-shf-column-is-sacrifice-flies-data-dictionary.md) | Data-dictionary correction — `player_game_batting.shf` is sacrifice FLIES, not sac bunts (verified: per-team shf sums match SF play counts exactly — Bayport 23, Griffs 12, Five Star 10). Anyone computing bunt tendencies from `shf` silently gets flies. One-line fix to `docs/gamechanger-stat-glossary.md` / `.claude/rules/data-model.md`. E-263 unaffected (uses plays.outcome + spray bunt). Source: 2026-07-13 Fable-scout Note B. Domain: docs-writer/CA | CANDIDATE | 2026-10-11 |
| [IDEA-136](IDEA-136-step1d-preflight-scope-web-profile-only.md) | Step 1d closure-smoke preflight should scope the `bb creds check` to the `web` profile only — it audits BOTH web + mobile, and the permanently-dead-by-design mobile profile (no programmatic refresh, unused by the reports path) produced a FALSE env-FAIL that held E-261 closure while web was live/auto-refreshing. Fix: assert on web only / ignore the mobile block (skill text = CA; optional `bb creds check --profile web` affordance = SE). api-scout confirmed reports path is web-exclusive. Sibling of IDEA-122/123. Source: E-261 closure 2026-07-13, api-scout diagnosis | CANDIDATE | 2026-10-11 |
| [IDEA-137](IDEA-137-docs-api-corpus-pii-certification-sweep.md) | Corpus-wide `docs/api/` doc-PII certification sweep — real GC identifiers committed in 6 files story E-262-09 never touched: team name "Nighthawks Navy"/"...AAA 14U" (avatar-image, players, public-team-profile-id, opponents, search-opponent-import, teams-public-public_id-id), full UUID `14fd6cb6-...` (progenitor_team_id), public_id slug `smgRExWHuBJJ`. A name-only grep misses the bare UUID/public_id — operator runs `scripts/check_doc_pii.sh docs/api` with the real (gitignored) denylist to certify + scrub. Surfaced by E-262-09 PII gate. Domain: api-scout + operator | CANDIDATE | 2026-10-11 |
| [IDEA-138](IDEA-138-plays-endpoint-path-variable-naming.md) | Normalize `/game-stream-processing/{game_stream_id}/plays` see_also/prose refs to `{event_id}/plays` (the plays endpoint takes `event_id`; its file is already `-event_id-plays.md`). IDEA-107 class, for /plays — a **7-file** surface (a subset fix is a half-fix). E-262-09 boxscore rename made it more conspicuous by contrast; Codex corroborated it byte-for-byte pre-existing at E-262 closure. Fix: `{game_stream_id}/plays`→`{event_id}/plays`, token-scoped. Domain: api-scout | CANDIDATE | 2026-10-11 |
| [IDEA-139](IDEA-139-context-fundamentals-worked-example-figure-refresh.md) | Refresh the stale figures in `context-fundamentals/SKILL.md`'s worked-example block (`:160-190`: ~697 ambient subtotal, ~1,715 total) that E-260-04 + E-262-05 deliberately left — now mildly inconsistent with E-262-05's refreshed `:28`/`:193` ambient figures. Multi-figure coordinated refresh (not one-line); inconsistency pre-existed E-262-05. Consider replacing hardcoded totals with the regenerating command. Surfaced by CA in E-262-05. Domain: CA | CANDIDATE | 2026-10-11 |
| [IDEA-140](IDEA-140-reconcile-loaded-games-against-fresh-schedule.md) | Reconcile loaded games against the freshly-fetched schedule — retire removed games and redirect/merge rescheduled ones. The load pipeline is accumulate-only: a game that MOVED to a new date is re-inserted (double-counting the opponent's query-time season aggregates), and a REMOVED game persists as stale stat rows; `merge-duplicate-games` only covers same-date cross-perspective twins. Corroborate a reschedule by team-pair + score/play-count and merge/retire via the canonical `merge_duplicate_game` seam. Surfaced 2026-07-14 from re-scouting the same team over time. Domain: SE/DE | PROMOTED | 2026-10-12 -- promoted to E-267 (game grain) |
| [IDEA-141](IDEA-141-pitcher-outings-epic-owns-k-rate-basis.md) | The upcoming pitcher-outings epic must explicitly own the K-rate stat/basis decision (K/9 vs K/G vs K/BF vs K/BB) so it isn't dropped in the gap after E-264. E-264 fixed ERA-basis only and left our invented 9-inning K/9 as-is (coaches benchmark it externally); GC shows K/G (game-length), not K/9. Fold into that epic's scope, don't build standalone; K/G would reuse E-264's `teams.innings_per_game`. Raised by baseball-coach during E-264 formation. **RESOLVED + folded into E-265 (OQ1 ruling 2026-07-15): set = K/BF + BB/INN + K/BB — drop K/9 AND K/G on the new section; K/BF sidesteps the innings-per-game basis machinery entirely.** Domain: PM/coach | PROMOTED | 2026-10-13 -- folded into E-265 |
| [IDEA-142](IDEA-142-member-membership-type-residue-cleanup.md) | Remove the dead `'member'` `membership_type` residue left by E-239 (member-creation path removed; live DB = tracked\|235, member\|0). Dead branch `generator.py:1988` + relic READ checks (teams.py cascade, game_merge.py, auth.py, reports_admin.py); consider dropping the whole column. Surfaced by SE during E-264 formation after a false "member-team" design premise. Fold into a future housekeeping epic. Domain: DE/SE | CANDIDATE | 2026-10-13 |
| [IDEA-143](IDEA-143-richer-groupc-whiff-battedball-outings-profile.md) | Richer Group-C whiff/batted-ball stats for the Outings Breakdown (swinging-strike%, K-looking vs swinging, GO/AO) that E-265 v1 deliberately deferred. swing%/K-looking are CLEAN (`play_events.pitch_result` enum); GO/AO needs a batted-ball outcome-string classifier; true GB/FB% is unreliable (hit trajectory absent from `plays.outcome`) — likely drop permanently. Inherits the ~90-95% pitcher-attribution caveat. Raised by coach + DE during E-265 refinement. Domain: SE/DE | CANDIDATE | 2026-10-13 |
| [IDEA-144](IDEA-144-deliberate-scouting-report-template-split.md) | Deliberate size-driven split of the ~900+-line `scouting_report.html` into Jinja2 partials, as its OWN refactor with real section boundaries — NOT smuggled in via a speculative feature epic. SE flagged during E-265 refinement (which chose inline over an E-263-style `{% include %}` framework). Promote on real merge/navigation pain or a concrete shared-include consumer. Domain: SE/UXD | CANDIDATE | 2026-10-13 |
| [IDEA-145](IDEA-145-outings-print-tiering-by-role.md) | Tier the Outings Breakdown PRINT artifact by pitcher role — full per-outing log only for probable/likely starters, season-line-only for relief arms — instead of E-266's uniform force-open-all. Keeps the pre-game printed page scannable. baseball-coach flagged during E-266 planning as a content-scope follow-on (E-266 Non-Goals keep content off the table). Promote if the printed artifact runs too long in practice. Domain: coach/SE | CANDIDATE | 2026-10-15 |
| [IDEA-146](IDEA-146-staleness-aware-plays-spray-refresh.md) | Staleness-aware plays & spray refresh (unfreeze the first-scout whole-game gate) — once a game is charted, GC post-game chart edits / corrected pitch counts / partial-first-pass spray never refresh, so plays-derived FPS%/QAB/P-PA drift from the boxscore that DOES upsert. NARROWED: uncharted→charted self-heals; freeze bites edit-after-charted + partial spray. Keep the cross-perspective dup protection while adding a change-detection refresh. Hazard H3 (two-channel CONFIRMED). Source: 2026-07-19 accumulate-only re-run audit. Domain: SE | CANDIDATE | 2026-10-17 |
| [IDEA-147](IDEA-147-rederive-batting-team-id-on-orientation-change.md) | Re-derive `plays.batting_team_id` on a home/away orientation correction — `_upsert_game` rewrites orientation unconditionally but frozen plays keep run-1 `batting_team_id` → silently swapped batting splits after a real orientation flip (most often `home_away=None`→resolved). Overlaps CC-2/E-268 root. Hazard H4 (Codex CONFIRMED/high, subagent CONFIRMED/med). Source: 2026-07-19 re-run audit. Domain: SE | CANDIDATE | 2026-10-17 |
| [IDEA-148](IDEA-148-player-name-correction-ratchet-not-longer.md) | Player-name corrections that are NOT strictly longer are permanently blocked by the longer-name-wins ratchet (`ensure_player_row`) — "Jon"→"Jim", wrong-long→correct-short never propagate; feeds the report roster grid + LLM narrative. Allow a genuine correction without regressing stub-does-not-clobber-full-name. Corner case CC-4. Source: 2026-07-19 re-run audit. Domain: SE | CANDIDATE | 2026-10-17 |
| [IDEA-149](IDEA-149-e261-failclosed-twin-double-count-query-surface.md) | E-261 fail-closed twin double-counts in non-perspective-scoped game reads — an ambiguous-date twin the E-261 pass refused to merge inflates `_query_record` (W-L), recent form, and runs-avg (game-level reads count `games` rows directly; season aggregates are safe). New query-surface consequence of the intentional bias-to-refuse. Corner case CC-8; relate to E-261/E-267. Source: 2026-07-19 re-run audit. Domain: SE | CANDIDATE | 2026-10-17 |
| [IDEA-150](IDEA-150-outings-vs-headline-fps-scope-coherence.md) | Outings card vs. headline FPS% read different plays scopes (this-run canonical game ids vs. whole-season) → same report, two different FPS% for one pitcher when the DB carries a game the fresh crawl no longer returns. Flag-gated (`FEATURE_PITCHER_OUTINGS`). Make both surfaces read one scope before the flag goes on. Corner case CC-9. Source: 2026-07-19 re-run audit. Domain: SE | CANDIDATE | 2026-10-17 |
| [IDEA-151](IDEA-151-morning-run-opponent-links-resolve-once-permanence.md) | Morning-run `opponent_links` resolve-once permanence — a wrong auto single-hit match, or a link that later goes stale, is reused forever → the wrong-team scouting report is delivered every game morning until manual `bb report map-opponent`. Add stale/wrong-link re-validation or expiry. Corner case CC-10; morning-run family (relates to IDEA-080). Source: 2026-07-19 re-run audit. Domain: SE | CANDIDATE | 2026-10-17 |
| [IDEA-152](IDEA-152-accumulate-only-hygiene-cluster.md) | Accumulate-only hygiene cluster — seven no-coach-impact re-run residues grouped for batch clean-up: H5 (`reconciliation_discrepancies` unbounded per-regen bloat), H6 (renamed-opponent stranded team rows, corruption REFUTED), H7 (`season_id` drift, spray-only), CC-3 (jersey NULL-clobber — coach-minor), CC-5 (ERA-basis write-once), CC-6 (orphan failed-generation HTML files), CC-7 (reports-row accumulation degrades team-deletion cascade). Shares the soft-vs-hard-retire question with E-267; cross-links IDEA-134. Source: 2026-07-19 re-run audit. Domain: DE/SE | CANDIDATE | 2026-10-17 |
| [IDEA-153](IDEA-153-cc2-cross-perspective-redirect-score-misattribution.md) | CC-2 — cross-perspective redirect flips home/away while FREEZING scores (`_upsert_game` COALESCE asymmetry) → runs silently re-credited to the wrong team; corrupts W-L/recent-form/runs-avg on BOTH reports + poisons `batting_team_id`. Two-channel CONFIRMED/high 2026-07-19 (each with an executable repro through the real ScoutingLoader). Promoted to E-268 (own targeted fix, SEPARATE from E-267 — distinct mechanism). Source: 2026-07-19 re-run audit. Domain: SE | PROMOTED | 2026-10-17 -- promoted to E-268 |
| [IDEA-154](IDEA-154-per-perspective-game-retire.md) | Per-perspective game retire — E-267-02's `_other_perspectives` guard (correctly) refuses to hard-delete a `games` row a second perspective owns, so a genuinely removed game BOTH perspectives loaded is retired by no grain. Priority order is fixed: recent form (highest) > season lines > W-L/freshness (cosmetic) > pitcher-rest safety (SHOULD-HAVE, downgraded). Rationale is ERROR DIRECTION (a stale game can only ADD an appearance → false positives only, never masking) — NOT "perspective-scoping insulates," which is FALSE. Gap is contingent on a case-insensitive team-name match, frequency unmeasured (one operator query settles it). DE costing warning: a refused game has NO grain positioned to retire it — new work, not a grain extension. Source: E-267-02 AC verification. Domain: DE/SE | CANDIDATE | 2026-10-17 |
| [IDEA-155](IDEA-155-team-game-number-doubleheader-rank-collision.md) | `team_game_number` DENSE_RANK (`src/api/db.py:298-300`) has NO `game_id` tiebreak → two same-date games with equal/NULL `start_time` share a rank, so a real doubleheader collapses to one game number. Feeds rotation-cycle detection; NOT rest or pitch-count math (`rest_days` uses a separate LAG window). Pre-existing defect in main, NOT introduced by E-267; doubleheaders are normal in HS ball so it is live, not theoretical. Verified verbatim 2026-07-19. Source: found incidentally during the E-267-02 doubleheader check. Domain: SE | CANDIDATE | 2026-10-17 |
| [IDEA-156](IDEA-156-completed-games-with-data-missing-team-id-predicate.md) | `_completed_games_with_data` (`src/reports/generator.py:597-616`) scopes its two EXISTS subqueries by `(game_id, perspective_team_id)` with NO `team_id` predicate — the SOLE `player_game_*` reader lacking one (CR-verified across all 12 modules). A stale "uncovered" row (E-267-03 AC-1b) keeps its live `perspective_team_id`, so it inflates the coverage count N, can hold the coach-facing "Through {date}" at a game with no live data, and — worst — keeps `N > 0`, SUPPRESSING the E-235 Phase 4b HIGH-1 silent-empty-report gate. Presence/count/date only; NO stat aggregate affected (all four are `team_id`-scoped). PRE-EXISTING, not an E-267 regression. Fix the predicate, NOT the retire scope. Source: E-267-03 round-2 AC verification. Domain: SE | CANDIDATE | 2026-10-17 |
| [IDEA-157](IDEA-157-dedup-fork-refusal-is-not-durable.md) | `plan_player_dedup` fork refusal is NOT durable — it is recomputed from the current population every run, so a genuinely ambiguous stub + `{John Smith, Janet Smith}` fork is refused today and silently MERGED tomorrow once Janet leaves and the fork drops to a pair. The planner's own "this is ambiguous" judgment is discarded by an unrelated population change, potentially conflating two people with no record doubt ever existed. Moderate: natural roster evolution reaches the same state without any E-267 retire, and the path predates E-267-04 round 3. Filed against the PLANNER (durability of refusal), not against the roster retire. Pairs with IDEA-089. Source: E-267-04 round-3 review. Domain: DE/SE | CANDIDATE | 2026-10-18 |
| [IDEA-158](IDEA-158-zero-completed-games-blocks-game-grain-retire.md) | A zero-completed-games payload blocks the game-grain retire — 3 interacting blocks, none wrong alone: crawler discards the full array (`scouting.py:163-166`), loader returns before the reconcile (`scouting_loader.py:186-191`), and `boxscores_complete` is VACUOUSLY TRUE (`∅ ⊆ anything`) so the obvious fix is unguarded exactly where it creates risk. Vanished games keep counting in W-L/recent form/freshness. Codex P1+P2, triaged VALID but deliberately FILED: reachability needs ~30 simultaneous reversions, and refusing on a zero-completed payload IS the epic's degraded-re-scout posture (E-267-04 AC-1c). P2 (no test for the production zero-boxscore shape) is a PREREQUISITE. Likely fix = offline operator command, not a load-path change. api-scout probe pending. Source: Codex Phase 4 epic review. Domain: SE | CANDIDATE | 2026-10-18 |
| [IDEA-159](IDEA-159-stripped-perspective-unprotects-game-grain-retire.md) | A stripped perspective un-protects the game-grain retire — **LIVE MECHANISM, currently ZERO instances**. `_other_perspectives()` refuses only while a game carries ≥2 perspectives; `cascade_delete_team` (`lifecycle.py:451`) drops the deleted team's `game_perspectives` rows while `:353-356` correctly KEEPS the `games` row, leaving one perspective. If the survivor's next re-scout also misses the redirect (doubleheader date, or GC's routine ~30-min per-perspective `start_time` disagreement), a real game is hard-deleted with its child surface. api-scout verified 2026-07-20: 0 unprotected instances across all 561 games, all 17 perspective teams probed, all 22 perspective-artifact games carry 2 perspectives. All teams are `tracked`, so an ordinary report deletion is the trigger. Do NOT fix by widening `_other_perspectives()`. Source: E-267 closure. Domain: SE | CANDIDATE | 2026-10-18 |
| [IDEA-160](IDEA-160-max-game-retirements-absolute-cap.md) | **READY TO IMPLEMENT / BUNDLE-READY** (not research — one story, fully specified, unblocked; E-262 Housekeeping is the container precedent). `MAX_GAME_RETIREMENTS` — give the game grain the absolute cap the roster grain already has. **The mitigation severity ordering is INVERTED**: the roster grain got `MAX_ROSTER_DEPARTURES=2` and fails as "grid clutter, never a corrupted stat" by the code's own words, while the game grain has only the flat 0.5 floor and hard-deletes a `games` row plus its FULL child surface (`plays`/`play_events`/spray/recon). Worked case: 12 of 20 games returned → `12 >= 10` passes the floor → 8 live games destroyed. HARDENING, not a defect fix (floor is weaker, not absent). Shape: one constant + one guard via the existing `extra_guard` seam, mirroring `roster_departure_guard`. **Trap: count the right population** — the roster cap initially counted backfill churn and deadlocked permanently. Tests must prove the cap fires where the floor would not, and keep both refusal causes distinguishable in the WARN. Source: independent Fable review at E-267 closure. Domain: SE | CANDIDATE | 2026-10-18 |
| [IDEA-161](IDEA-161-main-session-durable-record-surface.md) | A durable, repo-visible record surface for the main session's own process behavior. P-10's second half: the orchestrator's only write target is its own memory, so its decisions/escalations/failures leave no epic-visible trace (the E-267 over-escalation incident exists only in the operator account, invisible to the audit). E-271's Autonomy Gate closes the decision-rule half; this is the record-surface half, explicitly out of E-271 scope. Mechanism unscoped (History log vs. structured trail) + brushes the ratchet/meta-freeze boundary → idea, not epic. Revisit after E-271 lands + one dispatch runs under the new gate. Source: CR spec-audit S-2 on E-271. Domain: CA | CANDIDATE | 2026-10-19 |
| [IDEA-162](IDEA-162-step1a-step1d-diff-read-tension.md) | Reconcile the Step-1a vs Step-1d diff-read tension in `implement/SKILL.md`: Step 1a (~:408) treats a NOT NULL/FK migration diff read as main-session-evaluable, Step 1d (~:470) bars diff-stat reads through main as domain work — they contradict. `dispatch-pattern.md:27` (diff/grep = routed domain work) and E-271's Autonomy Gate side with Step 1d; Step 1a's permissive reading should likely be reworded. Pre-existing, NOT P-cited (out of E-271's meta-freeze) → idea. Surfaced by CA during E-271 Codex triage. Domain: CA | CANDIDATE | 2026-10-19 |
| [IDEA-163](IDEA-163-post-cascade-probe-operator-blindness.md) | Report-delete operator blindness — `_delete_report` swallows cascade failure with a `logger.warning` and flashes `"Report deleted."` unconditionally, so full delete / partial retention / cascade exception are indistinguishable (the 46 E-273 retentions were invisible). The `.claude/rules/admin-ui.md` "Post-Cascade Probe for Retention UI" convention is written for exactly this helper but NOT implemented. Deferred at E-273 planning: reclamation REDUCES the urgency (a retained team is swept by the end of the same delete request, so the flash becomes accurate again), leaving mainly the cascade-EXCEPTION path. Source: orphaned-reference-data-handoff §12. Domain: SE | DEFERRED | 2026-10-19 |
| [IDEA-164](IDEA-164-generate-report-second-destructive-pass-guarding.md) | `generate_report()` now carries a SECOND terminally-wired destructive pass: E-267's reconcile-at-load hard-deletes `games` + full child surface, and E-273's `reclaim_orphan_reference_data` (fired from `cleanup_expired_reports` at generation START, `generator.py:1517-1519` → `lifecycle.py:300`) hard-deletes unreachable `teams`/`team_rosters`/`players`. Both are opportunistic/terminal rather than behind an explicit destructive command, so a command operators read as read-only destroys data on two axes — including under `bb report morning-run` cron. Structurally the same shape E-270 was chartered to harden on the reconcile axis. NOT a defect claim: E-273 shipped reachability + three preserved roots + reap-then-gate + a rollback-on-non-fixed-point self-assert, and passed a full review round. Open: does a cap/confirmation even have an analogue when the population is reachability-derived rather than crawl-diffed? Raised by PM at the 2026-07-24 E-270 dispatch premise re-check; held OUT of E-270 scope by team-lead decision (the narrow in-scope piece became E-270-03 AC-6, which pins the attribution boundary in test). Source: E-270 dispatch premise re-check. Domain: SE | CANDIDATE | 2026-10-22 |
| [IDEA-165](IDEA-165-relative-db-path-backup-purge-divergence-test.md) | Exercise the relative-`--db-path` divergence in the purge backup test. `test_backup_runs_before_the_purge_on_the_resolved_path` pins that `bb db purge-scouting` passes the RESOLVED path to `backup_database`, and it does discriminate (the weaker form fails it, mutation-confirmed) — but only because the raw value is `None` in that invocation. It never exercises the scenario the requirement exists for: a RELATIVE `--db-path`, where `backup_database` (argument as-is) and `purge_scouting_data` (via `resolve_db_path`) genuinely resolve to two different files, i.e. a backup of the WRONG database on the most destructive command in the system. Open alternative: pass the resolved path to `purge_scouting_data` too, making the divergence unreachable — judged a provable no-op at review (same function, same input, same process) and deliberately not filed, but it is the structural fix. Explicitly optional; AC-5 requires only that a test pin the argument, which it does. Source: E-270-02 AC-5 re-verification. Domain: SE | CANDIDATE | 2026-10-22 |
| [IDEA-166](IDEA-166-e2e-fixture-clone-shallow-copy-fragility.md) | `TestGenerateReportDestructiveReconcile._schedule` clones fixture games with `dict(clone_src)` — a SHALLOW copy, so clones share nested objects (notably `score`) with their source by reference. Correct today for two conditions nobody has written down: nothing mutates a nested payload object, and `_load` re-reads from disk per call. A plausible future edit (per-game scores, any in-place tweak) turns that into silent cross-contamination between games meant to be independent — in the one test that proves report generation's destructive path fires end-to-end, whose whole design principle is that no assertion may pass for the wrong reason. Fix is trivial (`deepcopy`, or explicit per-game fields). Open: does the same pattern appear in the other `tests/fixtures/e2e/`-driven E2E tests? Reviewed and ruled no-action for E-270-03 by cr; captured so the REASONING survives the review thread, not just the conclusion. Source: E-270-03 review. Domain: SE | CANDIDATE | 2026-10-22 |
| [IDEA-167](IDEA-167-pii-fixture-comment-names-wrong-shape.md) | The PII-fixture comment in `tests/test_report_generator.py` (E-270, commit `4b0d17d`) says the shape that tripped the `us_phone` scanner was "two hex letters"; the old segments were one hex letter + a digit (`…0000000000c1`), and what actually matched is ten zeros bounded by `-` and any NON-DIGIT. The operative instruction (keep the segment all-numeric) is correct, so a reader who follows it is safe — but one who reasons from the shape could conclude `…0000000000a2` is fine, which is precisely the tripping shape. Captured for what it instances, not its size: the SEVENTH occurrence of E-270's prose-vs-reality class, introduced IN THE FIX TO THE SIXTH (whose own first attempt quoted the offending literal in a comment and re-blocked the scanner). Fold into the next touch of that file — explicitly not worth a dedicated commit. Open: is a shape description worth keeping at all vs. the bare instruction? Source: E-270 commit sequence (cr). Domain: SE | CANDIDATE | 2026-10-23 |
| [IDEA-168](IDEA-168-season-vocabulary-drift-is-silent.md) | E-272's season axis keys on the exact token `"summer"` (strip+lower), and vocabulary drift fails SILENTLY. The gate (`_is_summer_season`) is a 2-way split (everything not-`"summer"` takes the NSAA default); the log (`_log_bracket_season_disagreement`) is a 3-way split firing only on membership in `{spring,fall,winter}` AND only when a mapped `\d+U` bracket is present. So `"Summer 2026"` or `"summer_ball"` routes an "Anytown Reserve" opponent to spring NSAA rules with NO log anywhere. Fully spec-compliant (TN-4 designed the default; AC-7 scoped the log to the bracket case). **Failure direction is safe ONLY for sub-varsity words** (NSAA sub-varsity 90/1-2-3-4 ≥ NRBL at every count, so drift over-rests) — **NOT for Varsity**: drift routes a summer Varsity team to `nsaa_varsity`, which CROSSES OVER with Legion (50 pitches = 1 rest day NSAA vs 2 Legion; also looser at 61-70 and 81-90), so it UNDER-rests. Corrected 2026-07-25 after CA found the same false generalization in E-272 TN-4. Captured because the vocabulary is OBSERVED, not specified — we match a token GC never contracted to keep stable. **Evidence updated 2026-07-25 (api-scout live probe n=18): `"summer"` 17, `"spring"` 1** — supersedes OQ-1's "only token ever observed"; a sibling exists and matches the same bare-lowercase shape, so drift risk is DOWN (likely a plain enum) but NOT eliminated (api-scout: open vocabulary on n=18; fall/winter unsampled). Upside: `_KNOWN_NON_SUMMER_SEASONS` now has a confirmed live member, so the disagreement log fires on real data. **SECOND TRIGGER folded in 2026-07-25: season-field ABSENCE** (same landing spot, same varsity exposure, different route — SE measured `Norfolk Varsity` season=None→`nsaa_varsity` vs true summer→`legion`, 110 vs 105). Currently UNREACHABLE (season present 18/18; `_fetch_public_team_info` is fail-safe by structure — all 5 signals set in ONE `if 200:` block, so failure nulls them together → `unknown` → card SUPPRESSED, never an isolated `season=None`). **Do NOT "fix" that swallow-everything handler on this idea's account — granular error handling would CREATE the exposure.** One fix serves both triggers: make the Varsity season-absent fallback safe rather than assuming NSAA. **DO NOT close as "superseded by E-263-02c's operator pick"** — per E-272 TN-6 that pick structurally cannot reach `bb report morning-run` (unattended, always infers), which is the strategic forward surface; the operator pick closes this on the interactive path and leaves it fully open on the unattended one. Strong prior: widen the LOG (observability-only), not the GATE (can mis-route). Do NOT re-litigate the spring/NSAA default — correct on base rates (spring is modal), but it is *spring-is-likelier*, NOT *spring-is-safer*. Source: E-272-02 review (cr surfaced as idea-capture, not a blocker; PM concurred). Domain: SE | CANDIDATE | 2026-10-23 |
| [IDEA-169](IDEA-169-public-team-profile-header-divergence.md) | Two hand-maintained `Accept` contracts for ONE endpoint: `generator.py::_fetch_public_team_info` sends browser-generic `Accept` (via `BROWSER_HEADERS`) while `team_resolver.py` pins GC's vendor Accept + `gc-app-name: web` for the same `GET /public/teams/{public_id}`. **Provably a no-op** — api-scout live probe (n=18) found `team_season.season` present/non-null 18/18 WITHOUT the vendor Accept, paired bare-vs-vendor requests identical (zero field diffs; the one apparent diff was `avatar_url` CloudFront re-signing, isolated by a bare-vs-bare control), and `Vary` lists `Origin,Accept-Encoding` NOT `Accept`. Case for aligning is http-discipline fidelity + killing a duplicated contract (SE: promote `_ACCEPT_HEADER` to a shared constant), NOT correctness. **api-scout caveat: do NOT sell the vendor Accept as robustness** — pinning `version=0.1.0` would fail SILENTLY if GC ever enforced then retired it, given current error handling. Raised as Codex P1 in E-272 Phase 4 and DISMISSED on live evidence; recorded so the refutation survives and it is not re-raised from static reading. api-scout explicitly recommended NOT doing it as E-272 work. Source: E-272 Phase 4 Codex + api-scout probe. Domain: SE | CANDIDATE | 2026-10-23 |
| [IDEA-170](IDEA-170-doc-pii-gate-cannot-see-src.md) | A real team `public_id` sits in two docstrings in `src/gamechanger/team_resolver.py` (~:15, ~:61), verified against a real dev-DB team row — not a placeholder. **The coverage gap is the finding, not the instance:** `scripts/check_doc_pii.sh` is invoked against docs trees only, so the ONLY tool that can detect a real `public_id` (literal denylist) is never pointed at `src/`/`tests/`/`scripts/`, while `pii_scanner.py` structurally cannot catch it (opaque slug matches no pattern). So this class is undetectable in source by any automated gate — it surfaced only because an agent happened to read the file. Low severity for THIS instance (a `public_id` is public-by-construction, and these are docstrings) but that is a property of what landed there, not of the gap — a real athlete name would be the same hole with a far worse instance. Two separable pieces: the docstrings fold into the next touch of that file (IDEA-167 precedent); the coverage extension is the real item, gated on a noise dry-run. Unrelated to E-272. Source: api-scout during E-272 Phase 4. Domain: SE | CANDIDATE | 2026-10-23 |
| [IDEA-171](IDEA-171-unmatched-age-group-forms-are-unread.md) | `detect_league_level` recognizes only two `age_group` shapes (`\d+U` bracket, free-text range); any third form is not merely unmapped but **UNREAD** — it falls through to name keywords, and with no level word in the name reaches `unknown` and SUPPRESSES the card. api-scout observed a live third form, `"high_freshman"` (n=18). Real coaching exposure: an LSB-adjacent freshman opponent carries a legible level signal ("freshman" is right there in the field) and the engine discards it. Failure direction is SAFE (suppression, not a wrong table) but it looks identical to "no data" rather than "could not parse". **THE TRAP, capture or lose a round to it:** the obvious fix fails — `_` is a word character, so `\bfreshman\b` does NOT match `high_freshman` (but DOES match `high freshman` / `high-freshman`). Any fix must normalize separators BEFORE matching, or the patterns silently keep missing the exact form the fix targets — the failure mode preserved through its own fix. SE-verified pre-existing, NOT an E-272 regression (reconstruction of pre/post paths identical in all 4 cases). **DIAGNOSIS UPGRADED 2026-07-25 (operator + api-scout, verified vs GC's own web-bundle enum): `age_group` is NOT an age bracket — it is a polymorphic three-family LEVEL field** (school `high_varsity`/`high_junior_varsity`/`high_freshman`/`elementary`/`middle_12U`/`middle_13O`/`college`; travel `NNU`; recreational `"Between N - M"`), already on `GET /public/teams/{public_id}`, populated 18/18, discriminating varsity/JV/freshman live. So this is a STRUCTURED level signal we already receive and ignore, not an odd unparsed string — reframed from a small fix to the seed of suggested follow-up **E-274** (read `age_group` as a level signal ahead of the ngb check). Two hard constraints: **GC has no "reserve" level** (3 HS values vs LSB's 4 classifications — our two `high_freshman` teams are almost certainly Freshman+Reserve collapsed; harmless for rest rules, both →`nsaa_subvarsity`, but the field CANNOT fully replace inference), and the value is **self-reported by the opposing coach** so it is a strong signal, not authority. Cheap gap to close first: school-family values observed only on OUR teams via `/me/teams`, never yet on an opponent's public profile. **PROMOTED to E-274 (2026-07-25).** Evidence gap CLOSED POSITIVE (25 non-managed opponent public profiles, unauthenticated: `high_junior_varsity` ×19, `high_varsity` ×6, `high_freshman` confirmed; 0 auth-vs-public mismatches; populated 91/91 swept; school is the ONLY family on LSB's HS schedule). **Two of this idea's own claims were corrected by the epic:** its recommended separator-normalization fix is REJECTED (normalizing `_`→space makes `middle_12U` match `\b(\d+)U\b` → routes a middle-school arm to the Pitch Smart 15-18 curve, which UNDER-rests it — the fix would cause the safety regression the epic must prevent), and "closed enum" is NOT supported (api-scout could not certify exhaustiveness; GC's own mapper has a `default:` branch and the creation picker offers 3 of 7 — treat the set as OPEN and use an allowlist that fails visibly). Source: api-scout probe + SE, E-272 Phase 4; upgraded at E-272 closure; promoted at E-274 discovery. Domain: SE | PROMOTED | 2026-10-23 |
| [IDEA-172](IDEA-172-varsity-pattern-outranks-legion-patterns.md) | `\bvarsity\b` sits AHEAD of all four Legion patterns in `_LEVEL_WORD_PATTERNS`, so first-match-wins makes `"Norfolk Legion Varsity"` resolve `nsaa_varsity` with no season — an explicit "Legion" in the name LOSES to "Varsity". Pre-existing ordering (pre-E-272 table had it too), but **E-272's season signal is now MASKING it**: with `season="summer"` the Varsity branch maps to `legion` anyway, so it resolves correctly by a different route and the weakness is invisible. That masking is why it is worth capturing now — it will look healthy in every test and live run until a summer Legion team arrives without a season signal, then silently takes NSAA Varsity (110, 0/1/2/3) instead of Legion (105, 0/1/2/3/4). **Compounds with IDEA-168**: that idea's two triggers (drift, absence) are exactly the conditions that remove the mask. Check whether "Legion Varsity" is a real naming convention before acting. Source: SE, E-272 Phase 4 follow-up. Domain: SE | CANDIDATE | 2026-10-23 |
| [IDEA-173](IDEA-173-send-cap-reset-destroys-its-own-measurement.md) | The dispatch send cap's two unblock paths are NOT equivalent: **raising the threshold preserves the measurement, clearing the counter destroys it.** During E-272 the operator cleared the counter mid-story twice to unblock the main session, so 2 of the epic's 7 staging-boundary log rows are corrupted LOW (read 2 and 4 against higher true figures). Self-defeating instrument: the log exists to tell us whether 25 is set right, but the fastest unblock erases the row's evidentiary value — and it is taken under time pressure on the LONGEST, most send-heavy dispatches, i.e. the log systematically under-samples its own strongest evidence, silently (a cleared row looks low, not missing). Concrete risk: someone later reads rows of 2 and 4 and concludes 25 is generous; the opposite is true for those rows. Likely whole fix = make a reset NON-destructive (record the pre-reset value or flag the row "cleared at N" — still unblocks, still usable) and/or document threshold-raise as the default unblock. First settle whether the log is an evidence base at all or just an operational counter — if the latter, close this. **The reset was the right operational call each time; the gap is that the tooling makes the right operational call and the right measurement call mutually exclusive.** Related IDEA-161 (orchestrator behavior leaves no durable trace) + E-260 dispatch-cost-accounting is the natural home. Source: main session, E-272 closure. Domain: CA | CANDIDATE | 2026-10-23 |
| [IDEA-174](IDEA-174-season-absent-docstring-overclaims-structure.md) | `_fetch_public_team_info`'s docstring (`src/reports/generator.py` ~:1668-1670) asserts *"an isolated `season=None` alongside a usable `team_name` is not a shape this function can currently produce"* — **strictly false**. The five signals are NOT co-located in the payload: `team_name`/`ngb`/`age_group` are top-level but `season`/`season_year` sit behind a defensive `pub_data.get("team_season") or {}`, so a 200 omitting or nulling `team_season` yields exactly that shape (the `or {}` is the code's own acknowledgment). What the coarse handler DOES guarantee is that a **fetch failure** is all-or-nothing — a real, load-bearing property and the reason not to split it into per-field handling — but payload-shape completeness is a **different and weaker claim**. It is an API claim wearing the clothes of a structural one; what makes it true in practice is api-scout's live evidence (`team_season` present and flat on every sample), which belongs IN the sentence rather than being silently substituted. Risk is not today's code but a future reader citing it as a guarantee-by-construction. Fix direction: separate the structural half from the empirical half, cite the sample, and keep the "do not split into per-field error handling" instruction at full force. **OUT of E-274 scope on purpose** — the wording guards against a granular-error-handling refactor and weakening it casually is worse than imprecision. Low severity: E-274 established it adds no new risk KIND (season-absent + "Varsity" in the name already resolves `nsaa_varsity` today by the same spring default; only population changes). Open: [[IDEA-168]]'s "Second trigger" section makes the same over-claim and drives that idea's priority rating — likely needs the same correction. Surfaced by SE correcting a reconciliation PM had proposed in the stronger form. Source: E-274 discovery (SE). Domain: SE | CANDIDATE | 2026-10-23 |
| [IDEA-175](IDEA-175-consultation-prompts-should-name-the-team-roster.md) | Consultation prompts should NAME the epic's team roster. During E-274 planning PM sent three domain consultations to `scout`/`coach`/`SE` — the retired E-272 instances — instead of `scout2`/`coach2`/`SE2`. Two were resumed and answered anyway; one never replied. Cost: a stalled discovery round and **two competing baseball-coach rulings** team-lead had to reconcile by hand. **Structural, not inattention:** agent-type names are reused across back-to-back epics, a resumed agent cannot self-detect the mismatch (SE reported the request "read as normal" — full prior context, question squarely in-domain, every local signal says respond), and the sender has no roster either since the spawn happens in the main session. The sharpest cost is not delay but two instances of ONE agent issuing independent domain rulings with equal standing and no principled basis for preferring either — here they agreed and the retired instance even surfaced a safety item the current one missed, but that is luck, not a property of the mechanism. Open: where the roster belongs (spawn prompt vs `## Dispatch Team`, which lists agent TYPES not instance names — that distinction is the crux); should an off-team agent refuse or answer-and-flag; is never reusing a bare agent-type name a cheaper structural fix. Guard proposed by SE, endorsed by team-lead who called the collision foreseeable. Worth preserving alongside: PM escalated under Anti-Fabrication Pattern 3 rather than answering rest-rule questions itself — this guard reduces how often that is needed, it does not replace it. Related [[IDEA-161]], [[IDEA-173]]. Source: E-274 discovery (SE + team-lead). Domain: CA | CANDIDATE | 2026-10-23 |
| [IDEA-176](IDEA-176-sophomore-pattern-missing-plural.md) | `\bsophomore\b` (`starter_prediction.py:313`) has **no `s?`** while its neighbour `\breserves?\b` does. SE-verified: `"Anytown High Sophomore"` → `nsaa_subvarsity`, `"Anytown High Sophomores"` → `unknown` (suppressed), `"Anytown High Reserves"` → `nsaa_subvarsity`. **Safe-direction** (a miss suppresses the card, never a wrong rest number) — which is why it is an idea, not a defect. The finding is the **inconsistency**, not the miss: a reader sees a table where some level words tolerate a plural and one does not, with nothing marking the difference as deliberate, so the next editor adding a word has to guess. Team names here ARE routinely pluralised, so the input is not contrived. Open: is the divergence deliberate elsewhere (the other 7 entries are unchecked)? per-entry `s?` vs a normalisation step (the latter is tidier but E-274 TN-5 records why broad normalisation of this matcher is treated with suspicion — it recruits inputs into branches by accident)? **Do NOT promote alone** — [[IDEA-172]] reorders the same list and is the natural carrier; E-274 deliberately does not touch the list at all. Found by SE while demonstrating why a `high_*` prefix match would be a trap; SE explicitly called it idea-not-story. Source: E-274 discovery (SE). Domain: SE | CANDIDATE | 2026-10-23 |
| [IDEA-177](IDEA-177-surface-the-competition-level-to-the-coach.md) | **The report never tells a coach which league's pitch-count table produced its availability calls.** Trace-verified 2026-07-25: `StarterPrediction` (`starter_prediction.py:72-88`) has NO league field (`league` is an argument, never an output), `generator.py:2432-2447` computes-passes-drops it, `renderer.py:812-813` never puts it on the context, and `llm_analysis.py:63` BANS the vocabulary in the system prompt. The only coach-visible "level" text is static template prose with nothing interpolated (`scouting_report.html:660`,`:664`). Two separable arguments: (1) **honest Freshman/Reserve label** — GC has no Reserve tier so Reserve opponents are tagged `high_freshman`; rest math is indifferent (both → `nsaa_subvarsity`) but scouting value is NOT (Freshman = first-year 9th graders; Reserve = sophomore-age, more physical, advanced pitch mix), and api-scout measured **17 of 73 opponents Reserve-named, 15 tagged `high_freshman`** — roughly a fifth of the schedule, not hypothetical; (2) **trust surface** — a coach cannot sanity-check a rest number without knowing whether it came from NSAA Varsity / Sub-Varsity / Legion / NRBL / a Pitch Smart estimate. (2) subsumes (1) and is arguably stronger. **Was E-274-03; REMOVED when OQ-4's trace falsified its premise** — scoped as "make an existing label honest," which required the label to exist. Became additive UX on a dense bench artifact (a decision with an operator in it), picked up a `browser-render-testing.md` headless-Chromium obligation, and was only ever SHOULD HAVE (baseball-coach revised its own MUST down after checking where the label surfaces). **Do NOT promote as a "small copy fix"** — that framing is what got it removed. Impl shape (3 parts, no LLM, all deterministically assertable): the tier distinction is lost TWICE today (`_LEVEL_WORD_PATTERNS:309-312` and the DB branch `:436`) so preserve it through detection; add a coach-facing label field to `StarterPrediction` (none exists — the additive part); thread through `renderer.py`; render. Precedent for wording: E-272 TN-9 Senior/Junior Legion collapse. Source: E-274 OQ-4 trace + both baseball-coach instances. Domain: ux-designer + SE | CANDIDATE | 2026-10-23 |
| [IDEA-178](IDEA-178-ngb-american-legion-shadows-nrbl.md) | **LIVE DEFECT in E-272 (shipped 2026-07-25): `ngb=american_legion` shadows NRBL, so the league E-272 added never fires for the teams it was built for.** NRBL follows ALB regs, so coaches tag NRBL teams `ngb=["american_legion"]` — accurate about the governing body, not an error. E-272 made recognized `ngb` authoritative at Priority 2, so `legion` returns immediately (`starter_prediction.py:445-463`) and the bracket ladder at `:476` (ngb-EMPTY region) never runs. Observed (team names are fictional sentinels — see the idea file): *Example Clinic Reserve* (summer, `16U`, american_legion) → **legion**, should be nrbl; *Example Athletics Reserve* (summer, `Between 13 - 18`, american_legion) → **legion**. **The precedence defeats E-272's own bracket floor** — the 16U team would have resolved nrbl via the 15U-16U rule. TN-2 justified ngb-wins as "a genuine usssa team is a different rule SYSTEM"; sound for USSSA/PG (innings/outs) but **Legion and NRBL are the SAME system**, so the premise that made it safe is the one that fails. **Not urgent, and WHY is load-bearing:** LEGION and NRBL are byte-identical today, which is why nothing looked broken and the closure smoke passed — but the feature is inert, and it becomes a live wrong-table bug the moment the curves diverge, i.e. the exact scenario the distinct-constant design exists for. **Two corrections to the first report:** "NRBL never fires" is NOT measured (n=2; the precise claim is "any NRBL team whose coach tags the body accurately"), and **fixing ngb precedence alone would NOT fix the second team** — its range-form `age_group` short-circuits to `youth_travel` at `:477` before the name path, so a ngb-only fix looks successful on one team and silently misses the other on the quieter estimate path. **Do NOT fold into E-274** (that reads `age_group` for SCHOOL-family teams; this is a summer-path precedence defect — same function, different decision). **PM TRIAGE 2026-07-25 — build the detection as DISPLAY-ONLY; the precedence change becomes CONTINGENT.** PM verified first-hand that `LEGION` (`starter_prediction.py:188-197`) and `NRBL` (`:208-217`) are byte-identical (105 max, same five tiers), so the precedence change buys **zero difference in any pitch count or rest day** — the affected teams already get correct numbers. Per ux-designer's Revision 2026-07-25b, reuse the SAME reserve-age detection as a **display-only overlay** (`rules_label` = the table that computed the numbers, `level_label` = the scouting tier, two lines when they diverge: *"Rest rules: American Legion / Level: NRBL (Summer Reserve)"*). A display computation structurally cannot regress which rules apply; a precedence change to a rest-rule chain can — same logic, far smaller blast radius, and it delivers this idea's whole stated concern (the mislabel), which was never about the numbers. **The precedence change is NOT retired, and the contingency is the codebase's own design intent rather than PM caution:** `.claude/rules/pitch-rules.md` records the identical "identical curves today, deliberately separate constants" rationale for **two pairs** — `:134` (`NRBL` vs `LEGION`, "a future Legion-only change must not silently move NRBL") and `:180` (`PITCH_SMART_15_18` vs Legion). Same-numbers-today, keep-them-separately-addressable **is** display-only-now / precedence-contingent-on-divergence. So the operator's "classify NRBL as legion for pitching rules" must NOT be read as retiring the distinct constant, and baseball-coach's ladder is preserved intact, activating as written on divergence. **Build the trigger WITH the overlay — "activates on divergence" is NOT self-executing:** nobody monitors nrbl.net against ALB, display-only removes the last incentive to, and if the curves diverge unnoticed the affected teams silently take the wrong table with nothing in the suite to catch it (the exact risk `:134` names). **A test asserting `LEGION == NRBL` is a few lines, fails loudly on divergence, and belongs in the story — without it the contingency has no trigger and this idea quietly becomes unreachable a second time.** **Scope discipline (ux-designer, deliberate):** do NOT extend the two-field split to [[IDEA-177]]'s HS Freshman/Reserve case — GC's `age_group` enum has no Reserve value, so there is no discarded STRUCTURED signal there, only a name signal that **E-274 TN-2/TN-4 already ruled against** (0-of-17). **Sizing UNRECONCILED (2026-07-25 audit) — cite the mechanism, not a count.** The relayed "40 of 752" failed independent verification (no saved script reproduces it; an audit full-corpus recompute got 54 nrbl-resolvers / 26+6 rung split under a possibly-different predicate). What IS verified: a 5,049-combination brute force shows zero `american_legion`-tagged inputs ever reach `nrbl` — structurally impossible, since `ngb` returns at Priority 2 before the bracket ladder is reachable. Re-measure the count with a saved predicate in the fixing/display story; until then say "dozens of the 752, count unverified." Original gate, retained: should `american_legion` stay dispositive, carved out to american_legion only, never usssa/perfect_game? Durable lesson: **a green suite and a passing smoke cannot detect an unreachable branch** — only ground truth (teams whose right answer a human already knew) found it. Source: team-lead running the classifier against operator-labelled real teams. Domain: baseball-coach (ruling) + SE (fix) | CANDIDATE | 2026-10-23 |
| [IDEA-179](IDEA-179-unparsed-age-group-forms-reach-adult-tables.md) | **LIVE under-rest hazard.** GC's create-team **rec** family offers exactly `Under 13` / `Between 13 - 18` / `Over 18` and **only the middle one is parsed** (`_AGE_RANGE_RE` needs digit-dash-digit); the travel `NNO` form (`18O`) is likewise unmatched (`_AGE_BRACKET_RE` needs a `U`). All fall to name-keyword matching: **`Under 13` + a "Reserve" name + summer → `nrbl`, 105 max.** A team whose coach declared "Under 13" on a Legion-equivalent table. PM traced it in code, not relayed. **Same mechanism as the `middle_12U`/`elementary` suppression coach ruled for E-274, on a family coach was NEVER SHOWN** — the ruling should almost certainly extend but has NOT been, and assuming it does on a pitch-count gate is the inference not to make. **LIVE, not prospective** — unlike the school-family bracket case (blocked by the `_` word-boundary quirk), nothing blocks this. Severity honestly both ways: per coach's stakes correction this gate governs opponent-scouting PREDICTIONS, not our athletes (LSB routes through `teams.classification` at Priority 1), so the harm is a wrong availability prediction — real de-escalation, but the direction is the unacceptable one and this one already fires. Open: `Under 13` and `Over 18` are opposite ends and may not want the same answer (`Over 18` is adult rec, arguably closer to Legion); `18O` is travel not rec; is the fix per-form or a broader "non-empty `age_group` matching NO family must not fall through to an adult table"? **Gated on a coach ruling (routed 2026-07-25); do NOT fold into E-274** (school family only — neither creates nor fixes this); natural to fix alongside [[IDEA-178]]. Found by the operator supplying GC's exhaustive create-team vocabulary as UI screenshots and running EVERY value through the classifier — **second defect in two days found only by exhaustive enumeration against ground truth.** Durable lesson: enumerate the vendor's actual vocabulary and run all of it, rather than testing the values we happen to have seen. Source: operator + team-lead, post-E-272. Domain: baseball-coach (ruling) + SE (fix) | CANDIDATE | 2026-10-23 |
| [IDEA-180](IDEA-180-doc-pii-gate-tree-scope-and-history-gap.md) | **The doc-PII byte-gate certifies two trees and no history.** Filed under the premise "the gate runs on staged content only" — **that premise is false and the correction is the finding.** `.githooks/pre-commit` snapshots the **entire index** (`git checkout-index -a`) and `check_doc_pii.sh` greps a whole directory, so committed-and-still-present content inside a gated tree IS examined. What is narrow is **which trees**: `GATE_TREES` is exactly `epics` + `.project`, and only when a staged path falls under one; `docs/api` is a separate manual/CI invocation. So the gate is **never** aimed at `.claude/**`, `docs/admin/`, `docs/coaching/`, `docs/VISION.md`, `tests/`, `migrations/`, `scripts/`, `src/`, or `CLAUDE.md`, staged or not. **That tree scope — not staging — explains the motivating evidence**: LSB's own (denylisted) program name sits in **24 tracked files** across exactly those directories (incl. 8 `tests/test_*.py`) with **zero** occurrences under `epics/`/`.project/`. Temporal half that IS real: the gate reads the **index**, so **no gate has ever examined a past commit** — an identifier committed then scrubbed survives in reachable objects, and no tree-scope widening can reach it. **Unresolved and needs the operator** (agents cannot read `secrets/**`): a real opponent school name from the E-274 block list sits in 2 committed `.project/ideas/` files inside a GATED tree, so if it were denylisted every recent `.project/` commit would have blocked — either it is not on the denylist or the relayed block output was partial. Also noted: `.claude/rules/pii-safety.md`'s "Coverage footgun" section is now stale in the *other* direction (still says the gate is `docs/api`-only and planning artifacts are UNGATED). Distinct from [[IDEA-137]] (identifier CONTENT in `docs/api`) and [[IDEA-170]] (gate not covering `src/` — the natural carrier for this idea's tree-scope half, sharing its noise dry-run); the history half can fold into neither. Cheapest independently-shippable piece: make the success line state its scope, since `[doc-pii: REAL, 0 matches]` reads as a whole-repo pass. Source: E-274 planning-commit block (2026-07-25), mechanism read from the hook rather than relayed. Domain: CA | CANDIDATE | 2026-10-23 |
| [IDEA-181](IDEA-181-schedule-placeholders-become-opponent-team-rows.md) | **TBD/tournament schedule placeholders are stored as opponent `teams` rows.** Of 156 entries probed in `resolve156.json`, **20 are a `NON_TEAM` bucket** — not classification failures but placeholder text a coach typed into GC's manual opponent flow (`TBD May 16, 2026`, `South Tournament`, `TBD- 05/30/26, 3:00 PM`). An **INGESTION** concern, independent of the classifier: `unknown` is the *correct* answer for a non-team, and E-274 neither creates nor fixes it. Two separable consequences: (1) it **pads any denominator drawn from "teams that fail to classify"** — the buckets account for 64+46+26=136, so the honest suppressed-card figure is **46 of 136 (34%), not 46 of 156 (29%)**; the rate is *worse* than was reported, same population-vs-number family as E-274's five logged instances; (2) orphan-reclamation, dedup, and the `bb report map-opponent` unresolved-opponent surface all carry rows with no `public_id`, no roster, and **no path to resolution** — permanent un-actionable reference-tier inventory. Low severity, safe direction (card suppresses; no wrong rest number or stat), but **silent and monotonic** — every season adds more, none resolve, and the only symptom is a quietly degrading denominator. **Check FIRST whether E-273's `reclaim_orphan_reference_data` already sweeps them** — if a placeholder with no games qualifies as an orphan, the population is self-limiting and only the denominator half survives. Open: reject-at-ingestion vs. `is_placeholder` marker (very different blast radii); is a predicate even safely writable, given `Tournament` is a legitimate token in real team names (bias-to-refuse); how many exist live and do any have games attached. Captured not scoped, per instruction. Cites the `CORRECTION (2026-07-25, next session)` note in `.project/research/2026-07-25-session-handoff.md`. Source: main session probing `resolve156.json` during E-274 planning. Domain: SE | CANDIDATE | 2026-10-23 |
| [IDEA-182](IDEA-182-rules-engine-innings-and-outs-forms.md) | **USSSA and Perfect Game are UNBUILT, not unresolvable — and the difference has been mis-stated.** `PitchCountRules` expresses exactly one shape (pitch count → rest days); USSSA is **innings**-based and PG is **outs+pitches**, so `get_rules_for_league()` returns `None` and the card suppresses. But the **published regulations are already written down in `.claude/rules/pitch-rules.md`** — PM verified the table plus the `## USSSA (Youth Travel, 7U-18U)` section (3 innings max to pitch next day; 1-day max 6 innings 7U-12U / 7 innings 13U-14U; mandatory rest if >3 in a day) and `## Perfect Game (7U-14U)`, both marked *"Reference data only -- not yet implemented in engine"*, with the file itself naming the blocker as *"structural engine extension (a code change, not just new thresholds)"*. So only the ENGINE is missing, not a defensible table. Sharpens the E-274/IDEA-178 framing: `usssa`/`perfect_game` are *recognized* `ngb` values that classify fine and then hit a `None` rule set — [[IDEA-178]]'s ruling keeping them **fully dispositive** stands and is independent, but "so nothing can be done for those teams" does not follow. **Additive capability, NOT a defect** — suppression is correct behavior for an inexpressible rule form; no wrong number today, which is why it sat. Blockers: measure the population first (~8 teams among those probed is a session anecdote); PG additionally needs a schema decision (no `program_type` value for PG tournaments) so it is strictly more work and should not be bundled. Open: additive sibling rule types vs. a unified workload abstraction (**prefer siblings — the 3 shipped pitch-count sets are a frozen rest-safety surface**); are PG's *tournament*-scoped rules even the same kind of object (IDEA-066 asked in 2026-04, never answered); does innings-based rest math have appearance-level innings for a scouted opponent (`ip_outs` gives outs, but availability unchecked); should the suppression copy distinguish "we could not tell" from "we know the rules and cannot compute them" (cheap, overlaps [[IDEA-177]]). **Why it was not already filed — the durable bit:** [[IDEA-066]] noted the innings extension in its Notes in 2026-04, was PROMOTED to E-218 which shipped only the *detection* half, and left no live candidate behind. **A promoted idea's Notes are not a backlog** — anything the epic does not take must be re-filed at promotion. Source: baseball-coach Ruling 3, 2026-07-25 session; verified by PM against the rule file. Domain: SE (+ baseball-coach for rule fidelity) | CANDIDATE | 2026-10-23 |
| [IDEA-183](IDEA-183-resolution-rung-c-ignores-season-year.md) | **Resolution ladder rung (c) accepts a single search hit without checking its season year.** Read in source: `_resolve_via_search` (`src/gamechanger/opponent_ladder.py`) calls `search_teams_by_name`, returns `None` on `len(hits) != 1`, and on one hit returns `result["public_id"]` — **nothing inspects `season`**, and `search.py` contains no `season`/`year` at all. So a name whose only indexed GC presence is an OLD season resolves to that old profile, silently and confidently. **A gap in an existing intent, not a missing one** — the function's own docstring promises zero/2+ hits fall to rung (d), "NEVER a wrong-team auto-ingest," and a stale-season hit is precisely that. **MEASURED 2026-07-25 over all 752 distinct DB team names: 2 teams reproduce, both exactly one season back (→2025)** — low severity, correctness cleanup, NOT a coach-facing risk. A first probe found 14 wrong-season resolutions, but it used a more permissive matcher; re-checked against rung (c)'s real single-hit condition, **11 of the 14 are refused by the existing `len(hits) != 1` guard**, 3 reproduce, and 1 of those 3 resolves FORWARD (DB is the stale side — a different problem, excluded). **The mechanism is now CONFIRMED, not hypothesised:** `result.name` typically embeds the year, so an active team is indexed once per season and returns 2+ hits → already refused; only a team whose SOLE indexed presence is an old season slips through. So the population is bounded by **GameChanger's naming convention, not our code** — if GC stops embedding the year, active teams collapse to single hits and this grows. That is the durable reason to fix rather than close. **Unattended path** (`resolve_opponent` ← `src/reports/morning_run.py:463`, the cron driver) so the failure is silent and confident, but realistic harm is scouting **last season's roster for one opponent**. **Fix needs no extra HTTP call**: `hits[].result.season.year` is already on every hit (`docs/api/endpoints/post-search.md`, re-verified 2026-07-25 in the COMPLETE/closed `result` key set); rung (c) holds the year and discards it. **Open, NOT traced — do not assume:** does downstream reference-date filtering make such a report empty rather than wrong? Less load-bearing at n=2, but kept — it separates a wasted run from a populated report on last season's roster. Also open: refuse vs. prefer-newest; does `POST /search` accept a year constraint in the request; which reference year (DB season / operating year / game date — they diverge exactly at season boundaries). **Impl trap:** `/search`'s `season` is an OBJECT `{name, year}` while the public profile's `team_season` is FLAT — that doc says do not carry a parser between them, and anyone adding this gate will just have been working in the flat shape. Related [[IDEA-181]] (same family — resolution accepting what it should refuse), [[IDEA-151]] (resolve-once permanence makes a self-committed stale resolution harder to undo). Source: 2026-07-25 live probe of every DB team name. Domain: SE | CANDIDATE | 2026-10-23 |
| [IDEA-184](IDEA-184-estimate-banner-false-for-8u-14u.md) | **The estimate banner is factually FALSE for 8U-14U.** `scouting_report.html:664` (verified verbatim) tells the coach *"This level doesn't publish pitch-count rules, so rest and availability use a standard youth pitch-count guide."* It renders on `is_estimate` → the `youth_travel` league → per `_league_from_age_bracket`'s own docstring, **"14U and below"** — the whole 8U-14U range. **Self-refuting on the repo's own evidence:** `.claude/rules/pitch-rules.md:180` says `youth_travel` routes to *"the USA Baseball Pitch Smart 15-18 curve (`PITCH_SMART_15_18`: max 105, tiers 30/45/60/80/105)"* — the guide the banner invokes as a substitute for missing rules is itself **age-banded**, and the constant is named for the band we apply. We are not lacking a rule; we are applying the wrong band of the guide we cite. **Worse than baseball-coach's "insufficient" ruling — it is inaccurate in the REASSURING direction on the youngest arms:** a coach reads it and concludes no better guidance exists, when a stricter published rule does. A wrong number with an honest caveat invites scrutiny; a wrong number with a caveat that explains away a better answer suppresses it. **Direction only, no figures** — E-274 OQ-3(b) bars printing the Pitch Smart breakpoints as cited fact (coach recall, no web access). *Attribution correction:* the relayed cite of `pitch-rules.md:163` ("50 (7U-8U) to 95 (13U-14U)") is real but sits under **Perfect Game → Rules (Outs + Pitches)**, so it is PG's schedule, not a generic youth guide; the self-refutation argument does not need it. **SCOPE FOR E-275 STORY 01, operator-approved; do NOT promote independently** — filed only because E-275 does not exist yet. **Trap — do not treat it as auto-resolved by the suppression fix:** the banner survives for the other `youth_travel` route, the free-text range form ([[IDEA-126]] estimate path, e.g. `"Between 13 - 18"`), where the sentence is arguably still false since 13-18 straddles bands that publish rules. Story 01 must decide the surviving copy deliberately. Sibling defect in the same block: `scouting_report.html:660`'s `unsupported_level` branch asserts we know the level when genuinely-unknown teams get the same sentence (ux-designer, `.project/research/2026-07-25-uxd-competition-level-disclosure-design.md`) — two copy defects, same small region, two agents, never audited as a set. Ruling: baseball-coach URGENT CORRECTION in `.claude/agent-memory/baseball-coach/e275-classifier-hardening-rulings.md`. Evaluate with [[IDEA-177]] at E-275 planning (same card; that one is additive, this one subtractive). Source: 2026-07-25 session; verified in template + docstring + rule file. Domain: SE (+ ux-designer for copy) | CANDIDATE | 2026-10-23 |
| [IDEA-185](IDEA-185-partial-id-churn-still-retires.md) | **Partial `player_id` churn still hard-deletes live stat lines, even after E-276 fixes the health gate.** E-276 restores the gate's prior population so a TOTAL churn (zero overlap) refuses outright — the intersection is empty and the payload proves nothing. **Partial churn is the harder shape and survives**: 9 prior lines against a fresh boxscore of 5 survivors + 4 brand-new ids gives an honest `comparable 5 >= 4.5`, so the gate permits and the 4 churned lines are retired. A re-issued id for the same human is exactly what `bb data dedup-players` exists to merge — the player has not left, GC just renamed them — so this destroys data a merge would have preserved. **Set arithmetic structurally cannot fix it**: "this player is gone" and "this player has a new id" are byte-identical as ids. Closing it needs a same-game name-prefix dedup-candidate signal, and the ordering constrains the design — the retire runs BEFORE `dedup_team_players` by explicit design (dedup merges `player_id`s, so a reconcile running after it would diff canonical prior ids against raw payload ids), so a fix must respect that ordering rather than reverse it. **Explicitly NOT the fix: an absolute per-game player-line cap** — E-276's Non-Goals record why (with the gate honest a cap adds nothing to the total-churn case, and adding one would mask E-276's own regression tests exactly as the roster cap currently masks the roster gate; a cap bounds the damage of a wrong decision, it does not make the decision right). **"No urgency" here means UNMEASURED, not benign** — nobody has measured partial-churn frequency against the live DB, and that measurement should precede any design. Blocked on E-276 landing (the residual is only well-defined once the gate is honest). Promote on: a reported disappearing stat line, a frequency measurement showing it is not rare, or promotion of [[IDEA-089]] (Tier 2 co-occurrence fork disambiguation), whose same-game co-occurrence signal is the natural instrument for both — doing them together is much cheaper than twice. Source: E-276 discovery, surfaced independently by data-engineer and software-engineer, routed out deliberately to hold that epic's scope tight per operator instruction. Domain: SE (+ data-engineer) | CANDIDATE | 2026-10-23 |
| [IDEA-186](IDEA-186-roster-retire-lock-after-truncated-crawl.md) | **One truncated roster crawl permanently locks the roster-grain retire. ⛔ The "E-276 does not worsen it" half is RETRACTED (2026-07-25) — the fix CREATES a second entry route**, needing neither a truncated crawl nor churn: `DB {a,b,c}`, cap 2, run 1 `fresh {a,n1}` → legacy PERMITs, cap PERMITs, corrected REFUSEs, so today retires `b,c` and converges clean while the fix strands them; two runs later `absent ∩ previously = 3 > 2` and **the cap refuses forever**. Does NOT touch deletion-neutrality (only ever about never *permitting* what today refuses — this is a refusal that compounds); it kills the separate fix-neutrality claim. **Why it was missed: ruled out via the CANDIDATE population, never re-checked through the GATE** — the mechanism was already in E-276's TN-3 and nobody re-ran it against the conjunction. The five-run trace below remains valid **for the input it ran**; what was wrong was the universal quantifier silently attached to one executed trace. **⚠️ ANSWERED — and the answer changed with E-276's design (2026-07-25).** The conjunction was dropped; the roster grain ships with **no floor ratio at all**, so the second entry route above — a floor refusal stranding rows that then trip the cap — **has no first step and is CLOSED**. E-276 disposes of the lock in **three buckets** and they must not be collapsed to a verdict: **(a)** that floor route CLOSED; **(b)** the 3-or-more-genuine-departures lock **pre-existing and unchanged**, which is this idea's own truncated-crawl route by a longer path; **(c)** a candidate whose reachability is **NOT established empty** — no route found, which is weaker than a negative result. ⛔ **Do not read this as "the lock is gone."** Reproduced through the real `ScoutingLoader`, side-by-side with today's code and E-276's then-current **conjunction** gate, **byte-identical including which gate refuses at each step** on the truncated-crawl route (a trace against a design that no longer ships — kept because the route it demonstrates is bucket (b), which does): run 2 truncated → gate refuses, churn survives; run 3 crawl recovers → **both gates PERMIT and the CAP refuses**; run 5 a genuine departure is NOT retired. Once locked it does not self-heal. Silently returns the affected team-season to pre-E-267 behaviour (H2 — departed players persist on the coach-facing roster grid as false lineup options), and the failure is quiet in the way that matters: **it looks exactly like the guard working.** **Mechanism EXECUTED at loader tier — and it is the CAP, not the gate**: churn rows the jersey backfill creates survive a refused run, enter the NEXT run's pre-load snapshot, and are then counted as genuine departures by the cap's `absent & previously` narrowing (3 against a cap of 2) — which is why it only triggers after a refusal, since churn never enters the snapshot in normal operation. DE first established this by gate-and-cap simulation, **flagged that tier itself** rather than letting it inherit the credibility of the executed results beside it, then re-ran it through the real `ScoutingLoader` — **flag discharged**. A superseded account is recorded in the file so nobody re-derives it: the earlier ratio-is-self-reinforcing story (boundary `|snapshot ∩ fresh| < 0.5·|snapshot|`, "the transient path does not lock") is refuted on both counts at loader tier — the transient path IS the entry condition. Entangled with `MAX_ROSTER_DEPARTURES` and its cap scoping, so any future work touching either should read this first. Surfaced during E-276 planning and deliberately kept OUT of that epic — filing it there as an "accepted consequence" would have wrongly implied E-276 caused it. Related [[IDEA-185]]. Source: DE self-audit, 2026-07-25. Domain: SE (+ data-engineer) | CANDIDATE | 2026-10-23 |
| [IDEA-187](IDEA-187-de-memory-states-health-gate-invariant-in-superseded-form.md) | **⛔ BOTH REPORTED DEFECTS DEFLATED, in two separate passes — the residual is ONE CROSS-REFERENCE, and the honest disposition is DISCARD-or-one-line-fix (DE's call).** Filed claiming `.claude/agent-memory/data-engineer/health_gate_prior_set_must_be_temporal.md` states E-276's health-gate invariant in a superseded form. **Checked against the committed file (full read + second-channel grep) and the claim does not hold.** (1) **The internal-inconsistency claim quotes a paragraph THAT DOES NOT EXIST** — there is no *"Prefer a gate whose safety…"* paragraph in the file or anywhere in `.claude/agent-memory`; DE already replaced it with a correct **"Where E-276 actually landed (corrected…)"** paragraph stating the per-grain shape (corrected gate on game + player-line, **roster gets NO floor ratio at all**) and recording the conjunction as dropped. That was the headline claim and its whole basis for "a rewrite". (2) **⛔ THE SUFFICIENCY CLAIM IS INVERTED** — E-276 **TN-10**, which this idea itself cites as authoritative, puts "necessary but NOT sufficient" on **same-population** and calls **the temporal clause the load-bearing half**; the idea relocated the insufficiency onto the temporal clause, and the file's opening paragraph already states TN-10's version near-verbatim. **Acting on the idea as written would have INTRODUCED the inversion into the memory.** (3) The per-grain claim does not hold of the body (see 1) and is vacuous rather than wrong for roster, which under V1 runs no ratio gate to require wording for. **WHAT SURVIVES: one cross-reference** — the per-grain shape (**the paragraph opening *"Where E-276 actually landed (corrected…)"***) sits **below the midpoint** while the rule, "Required wording" and recall-deciding `description:` sit at the top with no pointer down, so a recall that stops at the description learns the rule and not its scope. *(**Position corrected TWICE. 2026-07-25**: this row said "at the bottom". **2026-07-26**: it said "line 21 of 35" — correct when written, rotted when DE appended to its own file (now 41 lines; the paragraph is still line 21). **That is the FOURTH time this index/file pair has produced `doc-sweep.md`'s named index-row shape**, so the claim is now stated by the paragraph's **opening words** rather than by coordinates — a form no append can falsify. The idea file and story 05 carry identical wording, changed in the same pass so the three cannot drift apart again.)* **Durable lesson, which inverts the idea's own filed note**: it credited *"found by reading the file, not by grepping"* as the careful method — **reading is where it failed**, because near-homograph forms defeat readers and greps in *different* ways and neither instrument covers the other; a grep for the quoted paragraph would have returned zero and settled it in one command. Quote the literal text and resolve the quote against the artifact (`.claude/rules/tool-output-integrity.md`). **The earlier Defect-2 deflation (pass 1) did not check the section directly above the one it was deflating.** Prior Defect-2 record, retained: its "Quantified" bullet records the SE-20/DE-222 divergence dispute as one population over four sweep bounds (`15 / 26 / 100 / 222`, "a colleague's sweep collapsed to the identical 3 shapes") — **contested at E-276 final triage, and the state is PARTLY OPEN rather than refuted.** The three-populations *framing* SURVIVES and is the load-bearing part — always name **which** divergence a count measures (corrected-gate refusal; gate-VALUE difference; observable-outcome difference). One row assignment is now source-verified (the colleague's sweep measures the **observable-outcome** population, not corrected-gate refusal) and **what the 222 measured remains OPEN**. ⛔ **Its "decisive check" is REFUTED as a UNIT ERROR and must not be cited as established**: `a<b` ∧ `b≤2` ⟹ `p≤3` bounds the **pre-load roster size**, not the number of swept parameter *combinations* — **222 counts combinations, 3 counts shapes** — and the colleague's own sweep carries the cap and still scales 20 → 26 → 44 across three spaces with a byte-identical shape set. Best-supported account is **artifact-of-bounds**, on a four-point exact fit `c(n) = (3n−2)(n−1)/2`. The bullet's *lesson* (a count carries its bounds) is right, **and so is most of its worked example**; the one narrow claim that does hold is that *"a colleague's independently-bounded sweep collapsed to the identical 3 shapes"* **overstates independence** (that sweep pre-filters on DE's own boundary, so it explores within the derivation — accurate wording: *an analytic derivation confirmed by execution over an added axis*). **Routed out of E-276 on OWNERSHIP, not substance**: agent-memory content is edited only by the owning agent (`.claude/rules/agent-routing.md` own-memory carve-out), and E-276's Dispatch Team is SE + CA, so story 05's residue sweep can flag it and nothing more — **and ownership is now the ONLY reason this is an idea rather than a one-line edit.** Fires by construction at E-276 closure via the Deletion-Side Eviction sweep; cheapest resolution is a short message to DE next time it is spawned. ⛔ **The filed claim "Grep will NOT find this" is RETIRED and was backwards for the reported defect** — a grep for the quoted paragraph returns zero and settles it immediately; near-homographs defeat *readers* here, which is the instrument this idea used. ~~Blocked on E-276 landing (the corrected wording to copy is its TN-10).~~ **WEAKENED — there is no wording to copy**; the surviving edit is a pointer to a paragraph the file already contains, so it need not wait. Open, and now the file's highest-value question because it is the only unchecked one: **three sibling memories it links** (`[[games_row_vs_stat_rows_coupling]]`, `[[schema_drop_test_blast_radius]]`, `[[scouting_query_role_vs_dedup_filters]]`) have never been checked against E-276's settled shape — **do not infer they are clean from this row's two deflations**, which found real-but-small residuals rather than nothing. Related [[IDEA-185]], [[IDEA-186]]. Source: E-276 final triage (PM), 2026-07-25; deflated twice, second pass at the E-276 pre-dispatch amendment. Domain: data-engineer | CANDIDATE | 2026-10-23 |
| [IDEA-188](IDEA-188-roster-delete-converts-refused-fork-to-executed-merge.md) | **A delete in the roster grain silently converts a REFUSED player-dedup fork into an EXECUTED merge, same run, no signal.** `plan_player_dedup` REFUSES a fork (a stub prefix-matching ≥2 distinct fuller names) because prefix matching cannot tell one human from two. The roster retire runs BEFORE the dedup sweep and **fork members are not exempt** — so deleting one maximal member **destroys the ambiguity that caused the refusal**, the planner then sees an unambiguous pair, and merges. Executed end-to-end: retire deletes Janet → fork collapses → same-run sweep merges → **the next healthy crawl restores the roster row but NOT the merged-away stat row, and does not un-merge the identity.** `team_rosters` is re-derivable; a merged `player_game_*` row is not. **Damage splits by branch** (`merge_player_pair` is delete-or-update): fork refused = split line, team sums correct; fork broken with a **colliding game** = stat rows DELETED, **team sums WRONG, low**; fork broken with distinct games = canonical line inflated, other human's season vanishes. In a real id re-issue **both broken branches occur across a season**, so "team sums correct either way" is true of the refused case ONLY. **PRE-EXISTING (today's code fires it identically — that leg is what keeps this an idea, not a blocker) but ⛔ V1 DOES WIDEN IT.** A "not widened" claim was filed and **retracted**: it rested on a sweep and a derivation that were both sound *only at zero churn*, and both fixed churn at zero. Executed on a 13-row roster containing the fork trio, crawl dropping one player: at churn ≥12 **today's floor REFUSES** (its live-population denominator is inflated by the churn) **while V1's cap sees one genuine departure and PERMITS** — threshold `c > 2·|fresh| − |S|`. So V1 deletes a fork maximal member in a region today does not. **Same family as [[IDEA-186]] and not by coincidence**: the churn-inflated denominator is the ONE place today's floor is stricter than V1, hence the only place removing the floor can widen anything — the two ideas describe one region. **Retraction lesson, bigger than the fact**: the sweep was run to VERIFY another agent's result and **reproduced that agent's blind spot**, both having held churn constant — **independent verification that shares a missing axis is not independent.** **The coupling that hid it**: E-276's artifact listed the three `DELETE FROM team_rosters` paths as *independent* when the first can **trigger** the second in one run — enumerating delete paths was not sufficient without asking which can cause each other. Open: exempt fork members from the retire (mirrors `exempt_player_ids` but gives dedup a veto over the roster grain), or re-check ambiguity against the **pre-retire** component; at minimum make the conversion LOUD, since it is currently indistinguishable from an ordinary merge; frequency unmeasured and should precede any design. Natural carrier is [[IDEA-089]] (co-occurrence fork disambiguation — a real signal would let refusal rest on evidence rather than on ambiguity a delete can erase). **Durable lesson**: the finding was first reported as "a cost of V1" and is "a pre-existing defect the wording mis-stated" — **a finding's DISPOSITION needs its own control run, not just its mechanism.** Related [[IDEA-185]], [[IDEA-186]]. Source: CR derived + named the experiment, SE executed, DE found the control. E-276 planning, 2026-07-25. Domain: SE (+ data-engineer) | CANDIDATE | 2026-10-23 |
| [IDEA-189](IDEA-189-collapse-failure-invisible-to-loadresult-errors.md) | **A failing player-dedup collapse is invisible through `LoadResult.errors` — the channel this codebase treats as authoritative.** `dedup_team_players`' except block logs an ERROR with a traceback and continues, but does **not** increment `result.errors`. That is a **swallow-WITHOUT-count**, diverging from the swallow-**and**-count pattern documented as deliberate in the same loader (`game_loader.py` catches broadly, logs ERROR, and returns 1 into `result.errors` so a failed cleanup never loses a good load) — and `.claude/rules/testing.md` builds directly on that pattern: *the result object is the evidence, a spy is not.* Here the result object is not evidence. **Consequence: a split identity that does not self-heal** — a component the planner retries and never merges leaves two `player_id`s for one human, each carrying part of a season, while an operator or test checking the result object sees a clean load. **It SHARPENS rather than changes E-276's residual ruling**: that epic classified a merge that FAILS (rather than refuses) as a compound residual and reasoned the productive closure was at the merge-failure end **because such a failure would be recurring and visible**. Recurring yes; visible **through the channel we actually check, no** — the ruling stands, its supporting assumption is narrower than stated. **"No urgency" means UNMEASURED, and the defect's whole shape is that the check everyone runs would not show it** — a log grep settles whether collapses are failing today and should precede any design. Open: should it increment `errors` (which gates callers' notion of a clean load, so this could turn a cosmetic identity problem into a hard failure of report generation) or need a separate counter; is there a third channel (E-276's reconcile result objects) that should carry it; **does the same swallow-without-count shape appear elsewhere in the load path** — found by reading one except block, nobody has swept for siblings. Promote on: non-zero ERROR occurrences in production logs, a split identity surfacing on a coach-facing report, or any work already touching that error handling (the fix is plausibly a one-line increment plus a test). **Captured because it is the kind of item that dies for procedural reasons rather than on its merits** — raised while closing something else, the least likely moment for a finding to be written down; the second such item from that session. Related [[IDEA-188]], [[IDEA-089]] — all three sit on the `team_rosters` ↔ dedup coupling. Source: CR-2 raised the question at E-276's roster-lock close, SE verified in source; routed out deliberately, neither proposed it ride along. Domain: SE (+ data-engineer) | CANDIDATE | 2026-10-23 |
| [IDEA-190](IDEA-190-executable-pin-for-the-vacuous-permit-caller-set.md) | **Replace `crawl_is_authoritative`'s `permit_empty_prior` calibration paragraph with an executable caller-set pin.** That paragraph has been **wrong twice, in OPPOSITE directions, about the same question, inside a single epic**: it first overstated that no grain reaches `prior_count == 0` ("applying it unconditionally would not widen anything"), then E-276 falsified it from the other side (story 02 made the **game** grain reach that input; story 03 removed the roster floor entirely). SE proposed pinning it executably instead — an AST walk over `src/db/reconcile_at_load.py` asserting the caller set and that every caller passes the keyword, **~25 lines, test-only, no production change** — and reports it **would have failed on both previous versions of the paragraph**. **Not cosmetic**: the paragraph is a SAFETY calibration, and a future editor acting on the stale version removes story 01's opt-in guard, which is why CR rated the live instance MUST FIX. The failure mode is the one this project has the most evidence about — true when written, falsified by a change elsewhere, unmodified, so no sweep finds it and re-reading confirms it. **Open: AST vs. a simpler grep assertion** (nobody has compared; an AST walk is itself code that can rot); **assert the caller COUNT or only universal keyword-passing?** — prefer the universally-quantified form with NO count, since a hardcoded number over a growing population is **the exact defect E-276 hit four times**; and where the pin belongs (precedent for both locations). Promote when anything next touches that caller set or paragraph — that is when the pin pays and when someone already has the context — or on a third rot. No urgency otherwise: the paragraph is correct as of 2026-07-26 and the pin guards a future edit, not a present defect. **Deliberately NOT taken into E-276 as a scope call, not an oversight** (03 mid-review at the circuit breaker; 04 a clean single-purpose slice; 05 context-layer prose routed to CA; a sixth story for ~25 lines is thin). **⚠️ And the strongest argument FOR taking it in-epic does not survive checking**: it was proposed as serving E-276's fifth goal, but that goal's subject is the gate's INVARIANT while this pin guards a caller-configuration calibration — adjacent, not an instance, so **E-276 must NOT be recorded as leaving goal 5 undischarged for want of it.** Credit: SE proposed it during E-276-03 round 2, which correctly did not add it. Related [[IDEA-187]], [[IDEA-189]]. | CANDIDATE | 2026-10-24 |
| [IDEA-191](IDEA-191-retired-roster-benign-claim-survives-in-other-agents-memory.md) | **The retired "roster failures are benign" claim survives in TWO other agents' memory files that E-276 could not edit.** E-276 retired *"grid clutter, never a corrupted stat"* / *"a wrong delete self-heals"* and scoped every copy it could reach; two live copies sit with agents who were not on its dispatch team — **(1)** `.claude/agent-memory/baseball-coach/e267_reconcile_at_load_review.md` Verdict 3 (*"the failure mode is benign (stale grid clutter, never wrong STATS…)"*), the coach-side twin of the sentence TN-9 required scoped, **in entirely different words**; **(2)** data-engineer's "DE FLAG 2", which [[IDEA-187]] does **not** cover (that idea's surviving scope after two deflations is structural). **Why it bites: these are the sentences that made bias-to-refuse feel SAFE on the roster grain — the reasoning E-276 overturned on the operator's ruling — and agent-memory is loaded as context, so it reaches decisions directly rather than waiting to be looked up.** **⚠️ The transferable part is the RETRIEVAL property: copy (1) shares NO TOKENS with the retired sentence and was found only by the judgement-expansion step of a retirement sweep. A token grep for the retired wording finds NEITHER copy** — `doc-sweep.md`'s "retired claims survive in forms carrying none of their tokens", landing exactly as that rule describes. **Ownership is the blocker and is not a technicality**: only baseball-coach may fix (1), only data-engineer may fix (2); E-276 correctly reported rather than edited. **Open: SCOPE-IT, DO-NOT-DELETE per the E-276 precedent** (the benign claim is true *outside* the band régime and is what the operator's prefer-delete ruling rests on, so a reader must not be left believing it holds unconditionally); **is there a third copy?** — two were found by two different sweeps with different methods and **nobody has swept the remaining agent-memory dirs for the JUDGEMENT rather than the wording**; and whether this recurs structurally, since every epic retiring a claim can strand copies with agents not on its team and **the retiring epic is exactly the party that cannot fix them**. Promote when either agent is next dispatched or consulted on reconcile/roster/data-loss questions — when the stale premise would be acted on and its owner is available. Filed because both flags were category (iii), no idea file and no durable record; **an item raised while closing something else is the least likely to be written down.** Credit: CA found copy (1) and reported both rather than editing another agent's directory. Related [[IDEA-187]], [[IDEA-188]], [[IDEA-186]]. | CANDIDATE | 2026-10-24 |
| [IDEA-192](IDEA-192-proxy-session-endpoint-normalization-and-data-first-capture.md) | **Two reusable fragments salvaged from E-072 at its abandonment: the proxy-path normalization ruleset, and the data-first insight.** The ruleset (UUID→named placeholder, `/public/teams/` slug→`{public_id}`, normalize ALL parameter positions, `{id}` fallback marked "placeholder names uncertain", normalize-THEN-deduplicate, exclude `status: NOT_API` docs) took four E-072 refinement passes to work out and would be expensive to re-derive. **The data-first insight is the more valuable half**: proxy sessions from 2026-03-12 on carry `response_body` in the endpoint-log, so most endpoints need NO curl recapture -- which removes the time-sensitivity (`gc-signature` expires in minutes) and most of the credential exposure that made E-072's workflow expensive. Detect the format by checking for a `response_body` key in the first JSONL entry; among entries matching one template prefer the largest non-empty 2xx body, ties by earliest timestamp. **Security constraint travels with it: save ONLY `response_body`, never `request_headers` (live `gc-token`).** Also records that `bb proxy endpoints` output is raw/unnormalized and must not be mistaken for the normalized set. **Supersedes E-072 as the unblock pointer for [[IDEA-014]].** The ~45% `response_body` population figure is a 2026-03 period observation, NOT a current fact -- re-measure before relying on it. Open: whether this is a skill at all, or a short procedure inside `ingest-endpoint` -- the whole-new-skill assumption is what carried the context-ratchet cost that helped kill E-072. No pull in ~4.5 months; promote when a real session is actually painful to process by hand. Domain: api-scout (+ claude-architect if it becomes a skill) | CANDIDATE | 2026-10-24 |
| [IDEA-193](IDEA-193-creds-import-warning-sends-operators-in-a-circle.md) | **`bb creds import`'s access-token warning routes operators into a dead end and back.** When a pasted curl carries an access token, `_resolve_web_token_key` (`credential_parser.py:282`, the E-175 spec's "line ~270" -- cite the symbol, it drifts) tells the operator to "copy a curl command from a POST /auth request" -- but `import_creds` never executes the curl, so `POST /auth` response tokens are unreachable AND a `POST /auth` request's client token is discarded by this same function a few lines below. **Both halves of the suggested workflow fail**, so a faithful operator loops until they give up or read the source. Live today; a handful of lines; independent of everything else in the abandoned E-175. Fix names the three paths that DO work: paste the `POST /auth` JSON response body (the `gc_auth` shape is already handled), copy a GET curl carrying a refresh token, or run `bb creds setup web`. **⛔ Do NOT carry over E-175 TN-8's wording verbatim** -- it promised "copy a POST /auth curl (tokens extracted automatically)" on the assumption the rest of E-175 would ship, which it did not; that clause would restate the same false promise. Natural carrier: the next epic touching `credential_parser.py`, folded in as small additional scope rather than planned around. Domain: SE | CANDIDATE | 2026-10-24 |
| [IDEA-194](IDEA-194-post-auth-curl-execution-for-creds-import.md) | **The full `POST /auth` curl-execution design from E-175, preserved at abandonment -- the design was sound, the cost/benefit was not.** All three defects verified still live 2026-07-26: client tokens discarded (`_resolve_web_token_key` returns `None` for any non-null type that is not `"user"`); tokens live in the response body but `import_creds` never issues the request; the misleading warning ([[IDEA-193]], the cheap independent half -- do not make it wait on this). **Explicit promotion trigger, deliberately high: the operator hits this AND all three workarounds fail.** Do not promote on the defect being real -- it has been real for months with nobody blocked, because `bb creds setup web` is the primary path and two other import shapes work. **Open question to settle BEFORE promoting: should this just DETECT a `POST /auth` curl and say "paste the response body instead"?** -- a fraction of the work that closes most of the confusion; the execution design may be over-built. Three constraints must survive any future attempt: never execute a `{"type":"logout"}` curl (it kills the session server-side -- detect pre-execution and refuse); do not route through `create_session()` (its default headers can invalidate the captured `gc-signature`); the body may contain a password (request content only -- never logged, stored, or printed). **Also corrects inherited reason-rot**: E-175's 2026-07-08 demotion cited E-254's auth hardening, which is app-internal auth in `src/api/` -- a different subsystem that does not touch `credential_parser.py`. Verdict right, reason false. Domain: SE | CANDIDATE | 2026-10-24 |
| [IDEA-195](IDEA-195-delete-vestigial-reconciliation-gate-code.md) | **~180 lines of retired reconciliation-ratchet gate code still import and still run in `src/reports/recon_scoreboard.py`.** The gate was retired 2026-07-26 (D2, `877413e`) and the scoreboard is now a pure diagnostic, but the machinery is a **named vestige, not a suspected one** — it can be invoked, will produce a plausible verdict against a baseline nobody maintains, and reports nothing wrong while doing it. `doc-sweep.md` names the shape: a prose sweep structurally cannot see code that still parses, so retiring the CLAIM left the EXECUTABLE standing. **Delete** (all verified present 2026-07-26): `evaluate_gate`/`load_baseline`/`write_baseline`/`default_baseline_path`, `GateResult`/`GateViolation`/`BaselineError`, `RATCHETED_AXIS_COUNTERS` + its 2 use sites, the `--update-baseline` flag + CLI branch + `0/1/3/4` exit-code contract in `src/cli/report.py`, the gate tests, and `.project/baselines/reconciliation-scoreboard.json`. **Keep** `compute_scoreboard`, `to_json_dict`, and the stat-definition constants — `canonical-seams.md` records those constants as CODE-CANONICAL (the E-245 doc mirrors them by hand). **⚠️ FOOTGUN: there are TWO ratchets sharing this vocabulary and only one is retired.** The *context-layer* ratchet (trigger 7, `.claude/hooks/context-ratchet.sh`, `.project/baselines/context-layer-ratchet.json`, its own `--update-baseline`) is **LIVE and gates every epic closure** — and the two baseline JSONs sit in the SAME directory with adjacent names. Scope this by FILE, never by grepping "baseline"/"ratchet"/`--update-baseline`, or a sweep deletes a live closure gate. Open: does `docs/admin/operations.md` still document the gate AS a gate (doc reconciliation may be the more valuable half — a runbook for a retired gate is worse than unused code); and `scripts/reclaim_orphan_reference_data.py` + its test matched the baseline vocabulary but are almost certainly E-273's separate convention — **not confirmed, check before touching**. Retiring the gate did NOT retire the North Star measurement discipline. Natural carrier: the next epic touching `recon_scoreboard.py`. Related [[IDEA-123]]. Domain: SE | CANDIDATE | 2026-10-24 |
| [IDEA-196](IDEA-196-prod-roster-residue-survives-report-generation.md) | **A live prod share link renders 19 roster players where GameChanger has 18 — and a full report generation this morning did NOT heal it.** The extra is a phantom entry at jersey **#4** carrying the literal `"Unknown"` sentinel (the nameless-stub marker from the canonical upsert in `src/db/players.py`), present in the roster list ONLY, in no stat table; separately **#18**'s surname is a **two-letter transposition** on prod. Dev matches GC exactly on both; **adjudicated against GameChanger by api-scout across 4 GETs**, so GC is the arbiter, not our two environments disagreeing. **Neither residue self-heals, for two DIFFERENT reasons** — the stub is reachable via a `team_rosters` row so orphan reclamation **correctly** declines it; and **the transposition is permanently sticky BY DESIGN**, a code-level finding rather than an observation: `ensure_player_row` overwrites only when the incoming name is **strictly longer** (`players.py:29`, with the `Unknown`-is-length-0 carve-out at 48-60), and a transposition is **exactly the same length**, so no re-crawl or regeneration can ever correct it. That is why "just regenerate" is not a fix. **Two halves. (a) Immediate prod cleanup** — open question what tool does it SAFELY: `bb data dedup-players` merges same-name duplicates and **will not touch an `Unknown` stub**, and hand-deleting `team_rosters` + `players` rows on production is not a procedure we have. **(b) The mechanism, the part worth understanding**: what path creates a player row with a jersey but no name (unidentified — this decides whether it recurs); and why the stub never received the real name — relayed as "the upsert never received it", but the code suggests a second reading that must not be assumed away, that the rule applies **per row**, so a real player upserting into a DIFFERENT row leaves the stub as never a candidate. **Those two readings imply different fixes; establish which first.** Whether generation should reconcile roster names against the crawl every run is raised-not-answered, and brushes against reconcile-at-load's roster grain which E-276 deliberately left with **no floor gate** — read that design before reinventing a gate removed on purpose. **⛔ Fix design explicitly OUT OF SCOPE** for this capture. Promote when the operator wants the prod page fixed, or **a second `Unknown` stub appears on any report** (the more important trigger — one is cleanup, two is an active creating path). **Calc-eval side facts recorded so they are not re-investigated: ~314 facts verified clean per environment, IDENTICAL computation behavior across prod and dev, and 3 P/BF cells consistent with charted-PA gating = verified-NOT-defect, no action.** The clean-computation result is what localizes this to how roster state was WRITTEN, not how it is read. Source: four-agent report evaluation 2026-07-26. Related [[IDEA-185]], [[IDEA-188]], [[IDEA-189]]. Domain: SE (+ data-engineer) | CANDIDATE | 2026-10-24 |
| [IDEA-197](IDEA-197-415-mis-signals-as-transient-5xx.md) | **A 415 (stale/wrong vendor `Accept` pin) is correctly never retried but raises `GameChangerAPIError`, whose documented meaning everywhere else is "5xx after retries" — so a DETERMINISTIC failure is reported to the operator as TRANSIENT.** `exceptions.py:46` and `.claude/rules/auth-module.md`'s hierarchy both say 5xx-after-retries; **there is no literal `415` anywhere in `src/`** (searched), so a 415 falls to `client.py`'s generic tail ("Unexpected non-success status -- treat as a non-retryable API error") — the classification is **by omission, not by decision**. Downstream: `morning_run.py` logs *"Transient error (5xx/connection)"* per team and its preflight says *"...not an auth failure"*, while `crawlers/scouting.py` warns-and-skips per game — so **a stale boxscore pin yields a team crawling ZERO games with only warnings, summarized as transient**, pointing the operator away from the cause. **The asymmetry is the argument: the 403 path already carries an operator hint naming the Accept pins** (auth-module.md's whole "False-403 Misdiagnosis Trap"), and 415 is the SAME root cause with wording that points the other way. **Low urgency and honestly so** — fires only when a pin goes stale, and the inventory was **nine pinned call sites, zero mismatched** (2026-07-26; inventory in `docs/api/error-handling.md`) — but a stale pin is exactly the unexpected-day scenario. Open: distinct subclass vs. a 415 branch; whether the morning-run SUMMARY wording changes too (that is what an operator actually reads on a scheduled run); and **is 415 alone?** — the generic tail catches ANY unexpected status, so other deterministic 4xx may inherit the same mislabel; enumerate rather than assume. **⛔ Fix design OUT OF SCOPE.** **Three relay corrections from checking the sites: `morning_run.py` line numbers had drifted by exactly 9** (relayed `:693`/`:258`, actually **`:702`**/**`:266`** — cite by message text, they will rot again); **four** misleading client docstrings, not three; and **one docstring IS already correct — the one on the method that CONTAINS the raise** (*"or on any unexpected status"*). So the truth is documented AT the raise site and lost in every summary propagating outward from it, including the class docstring and the rule file — **the information was never missing, it failed to travel**, which is a sharper statement of the defect than "the docstrings are wrong". Cross-ref for a **client/creds hygiene** epic: [[IDEA-193]] + [[IDEA-194]] (different files, same family of "the error path tells the operator something false") — see all three together. Source: api-scout Accept-header pass 2026-07-26. Domain: SE (+ api-scout) | CANDIDATE | 2026-10-24 |
| [IDEA-198](IDEA-198-two-unsynced-team-pin-enumerations.md) | **`_delete_team_scoped_data` and `_TEAM_PIN_TABLES` are two hand-maintained lists of team-scoped pin tables, and nothing enforces that a new `teams(id)` pin lands in both.** The canonical-seams second-path shape, in a hard-deleting path. **The drift is demonstrated, not theoretical: E-273's Codex F1 was `game_perspectives` missed by one of three otherwise-agreeing enumerations.** Failure is quiet and asymmetric — a table missing from the sweep's list leaves rows after an INFERRED deletion, missing from the cascade leaves them after a DELIBERATE one; neither raises. **⚠️ Do NOT "fix" this by unifying the lists** — the divergence is deliberate (E-273 TN-4 omits the two TN-7 root tables), and unifying would drag `opponent_links`/`user_team_access` back into the sweep's delete path, undoing TN-7. Open: docstring cross-reference vs. a test asserting the relationship (a test must encode which tables legitimately differ, risking a THIRD copy of the same knowledge); DE and PM both landed on cross-references, naming the RULE not just pointing. Promote when a new `teams(id)` FK table is added — that is the moment drift can occur. **Operator explicitly DECLINED building it in E-277** (recorded in that epic's TN-6); surfaced by data-engineer as a finding beyond the audit handoff's scope. Domain: SE (+ data-engineer) | CANDIDATE | 2026-10-24 |
| [IDEA-199](IDEA-199-append-only-migration-rule-invisible-to-non-de-agents.md) | **The append-only migration rule is invisible to every agent except data-engineer.** `.claude/rules/migrations.md` — the path-scoped rule that loads for anyone touching `migrations/**` — contains NO append-only rule; the prohibition lives only in `.claude/agents/data-engineer.md`. So an SE editing a migration never sees it. **The gap is self-concealing: `apply_migrations.py` tracks by FILENAME with no checksum, so an edited applied migration is simply never re-applied — no error, no mismatch, nothing to notice** — and the damage surfaces later when file and live schema disagree. **⚠️ This is a gap that ALWAYS EXISTED, not a regression — and the archive records the opposite.** `.project/archive/E-100-team-model-overhaul/epic.md` asserts its fresh-start override targeted "the append-only migration rule in `/.claude/rules/migrations.md`"; **that sentence is false about where the rule lived.** DE scanned all 10 revisions of that file with a positive control ("Idempotency" fires on all 10, bare "append" on none) and the E-100-06 diff touches only numbering and an ALTER TABLE sentence. **So promote it as "codify a rule that only ever lived in one agent's definition", NOT as "restore deleted text"** — the restoration framing sends someone hunting git history for text that does not exist. Provenance of the false claim: entered at E-100's Codex triage 2026-03-14 as finding F2, written into that epic unchecked, survived four months, and PM compounded it into "the canonical file silently lost the rule" before DE contested and settled it. Open: does the restored rule need a scope qualifier — its rationale is entirely about DDL divergence and **does not reach a comment**, which is mechanically inert; E-277 left an applied migration's stale comment alone anyway but for a DIFFERENT reason (it is a record of what was applied, and correcting it rewrites that record). Surfaced when DE stated the bar as a project rule, then checked and corrected it to an agent-definition convention — the correction is what exposed the gap; PM verified CLAUDE.md and `docs/` state no such rule. Promote when a context-layer epic is already in `.claude/rules/`. Domain: claude-architect | CANDIDATE | 2026-10-24 |
| [IDEA-200](IDEA-200-operations-runbook-migration-table-misses-012.md) | **`docs/admin/operations.md` says the migration set is `001`-`011` and its per-migration table ends at 011; `migrations/012_teams_innings_per_game.sql` exists.** Small, but it is the operator-facing runbook, in the very section telling the operator to inspect applied migrations against the live DB — **a count disagreeing with `ls migrations/*.sql` invites exactly the wrong incident conclusion** (a migration failed to apply, or the DB is ahead of the code) at the worst moment. Self-repeating rather than one-off: the section carries a hand-maintained COUNT and a hand-maintained ROW per migration, so every future migration needs two edits here plus the file itself. Open: keep the hardcoded range at all? `.claude/rules/migrations.md` already tells readers to `ls migrations/*.sql` rather than trust a written number **on the explicit grounds that such lists rot — and this is that rot.** Raised by data-engineer as a drive-by during E-277 discovery and independently verified by PM (twelve files present, no `012` row). Out of E-277 scope — that epic touches no documentation. Fold into the next docs pass on that file. Domain: docs-writer | CANDIDATE | 2026-10-24 |
| [IDEA-201](IDEA-201-matcher-divergence-substring-vs-word-boundary.md) | **Two matchers over the SAME level-word vocabulary disagree, and which one runs depends on the opponent's data rather than on anything we control.** `_nsaa_level_from_name` (the `ngb=nsaa`/`nfhs` path) does bare `if keyword in name_lower`; `_LEVEL_WORD_PATTERNS` (the empty-`ngb` path) uses `\b`-anchored regexes — both in `src/reports/starter_prediction.py`, **both re-read against the source during E-275 planning**, not relayed. So two teams with identical names can resolve differently, and the substring side has no notion of a word: its behaviour on any untried name is genuinely unpredictable rather than merely undocumented. **The general case of [[IDEA-176]]** (the `sophomore`/`Sophomores` plural gap) — fixing this subsumes that; fixing 176 alone leaves the matchers disagreeing on everything else. The real cost is second-order: *"which matcher ran?"* becomes a question for every name-related finding. A clean canonical-seams instance, but **unification is not just deleting one** — they return different things (a league id vs. a level class). Which behaviour is correct is **unruled**; a 563-name corpus now exists at `proxy/data/sessions/` to rule it on. One of four E-275 MINORs captured rather than built; E-275 reorders within `_LEVEL_WORD_PATTERNS` and never touches the other matcher, so it neither fixes nor worsens this. Fold into the next epic touching either matcher. Domain: SE | CANDIDATE | 2026-10-25 |
| [IDEA-202](IDEA-202-ngb-over-bracket-guard-tests-only-the-harmless-bracket.md) | **`test_legion_ngb_beats_14u_bracket` may be asserting ngb-over-bracket precedence on 14U — the one bracket where the assertion cannot distinguish a working rule from a broken one**, because 14U resolves `youth_travel` rather than to a competing league. 15U/16U (`nrbl`) and 17U/18U (`legion`) are where the precedence has something to beat, and per the seed they are untested in this role. **⚠️ STATUS IS THE POINT: this is the ONE of four E-275 MINORs that was NOT re-verified against the source.** The test name and location are confirmed to exist; the characterization of its weakness is carried on the E-275 spec seed's word, and that seed was found during planning to carry five claims that did not survive checking. **Read the test body first, and do NOT cite this idea as evidence the test is weak — cite it as the reason to look.** Why it matters if it holds: a guard that passes for a reason unrelated to the property it names occupies the slot where a real guard would go and reports health — the *"a check that RAN is not a check that WORKED"* shape. Also check the sibling `test_usssa_ngb_beats_15u_bracket`/`..._18u_bracket` guards: if the whole family shares the shape this is a small sweep, not a one-test fix. E-275's fixture pack does NOT close it (ground truth over input shapes ≠ a targeted precedence guard). Related [[IDEA-178]]. Domain: SE | CANDIDATE | 2026-10-25 |
| [IDEA-203](IDEA-203-api-docs-prescribes-sentinels-the-pii-gate-blocks.md) | **`.claude/rules/api-docs.md` instructs authors to write `Anytown`/`Springfield`/`Example`-family placeholders, and per the E-275 spec seed the doc-PII byte-gate has already blocked a round of sentinels of exactly that class.** If it holds, the context layer prescribes values the commit gate then rejects — and the author cannot see why, because the real denylist is the uncommitted, gitignored `secrets/pii-denylist.txt`. **A contradiction between two halves of the safety system, and it outlives any one epic**: an author who cannot reconcile a rule with a gate is being trained to route around one of them, and it will be the gate. It also degrades the gate's signal — a block on a prescribed placeholder is indistinguishable, from the author's side, from a block that caught a real identifier, so the response to both becomes *"change the string until it passes"* rather than *"find out what I leaked."* **Premise UNCONFIRMED** (same five-bad-claims seed as [[IDEA-202]]); nobody has reproduced the block against the prescribed taxonomy specifically. **⛔ The right diagnostic is the EXIT CODE, never the token** — reading the denylist to learn which string collided pulls real identifiers into context to avoid writing them, the wrong trade. Likely resolution is changing the taxonomy (`Anytown`/`Springfield`/`Eagles` are real English place and mascot words with a genuine collision surface) but that is a CA call. **Evaluate with [[IDEA-204]] — almost certainly one piece of work**; together they are the two halves of "the gate does not do what a reader of the rules would expect." Related [[IDEA-170]], [[IDEA-180]]. Domain: claude-architect | CANDIDATE | 2026-10-25 |
| [IDEA-204](IDEA-204-agent-memory-holds-real-identifiers-outside-gate-coverage.md) | **Committed agent-memory carries real denylisted team identifiers, in a tree the doc-PII gate has never been pointed at. MEASURED, not estimated: 15 files, 33 matching lines, across SIX agents' memory directories** (baseball-coach 4, api-scout 4, claude-architect 3, SE 2, PM 1, DE 1), by running `scripts/check_doc_pii.sh` over `.claude/agent-memory/` on 2026-07-27. **Not one agent's lapse — a systemic property of how agent memory has been written, including the directories of the agents who would fix it.** api-scout found it against its OWN directory unprompted and reported ~6 names in one file; **it understated by an order of magnitude, and that is evidence of the METHOD (one agent looking honestly at itself cannot see the other five), not an error to correct away.** The gap is not a gate failure: the byte-gate is invoked against `docs/api` only, and the pre-commit `GATE_TREES` covers `epics`/`.project` — agent-memory, the highest-traffic prose in the repo and loaded into context on nearly every interaction, sits outside both. **The half that is easy to miss: the remediation has NO verifying check** — whoever redacts has nothing that confirms they finished, so widening the gate's path argument is arguably the more important half; a redaction with no gate is a one-time cleanup that rots invisibly. **⛔ NO REDACTION AUTHORIZED — nobody edits those files; the operator decides** (api-scout confirmed it left its own untouched rather than racing the ruling), and **⚠️ the gate prints matching LINES, so re-running it pulls real identifiers into whoever's context — cite the numbers, do not re-run casually.** No identifier is reproduced in the file itself, deliberately. Sequencing is operator's: widening `GATE_TREES` first blocks commits until existing content is clean. **Evaluate with [[IDEA-203]].** Related [[IDEA-170]], [[IDEA-180]] (check whether this is that finding rediscovered). Domain: claude-architect (gate config) + the owning agent (content) | CANDIDATE | 2026-10-25 |
| [IDEA-205](IDEA-205-seniors-is-an-unreliable-legion-signal.md) | **`\bseniors\b` resolves `legion` on teams that are not Legion — observed live on a school-family team carrying "Seniors" in the ordinary graduating-class sense.** ⚠️ **SEVERITY WAS RE-CLASSIFIED 2026-07-27 AND THE MILD READING MUST NOT BE CITED.** The file originally called this data-accuracy rather than safety on the strength of its one observed instance; **SE measured the tables by execution and the direction SPLITS BY VALUE**: `high_varsity` → `legion` over-rests (conservative), but `high_junior_varsity`/`high_freshman` → **LESS rest at 1-45, 51-60, 71-80 — a sub-varsity arm that threw ONE pitch would be called available immediately.** SE's framing is the sentence to design any restatement against: *an idea filed as "bare `seniors` over-rests a varsity team" understates it by exactly the two values where it under-rests.* **The urgency rating was RE-DERIVED rather than edited, because it shared none of the retired claim's words and no sweep would have found it** — re-derived verdict: not urgent on the observed population, and **not safe to describe as mild**. Two further corrections held inside: *"two independent corpora"* is **RETRACTED** (both observations are one operator's network; also 1 confirmed live misfire, not 4) and this was **NOT** the entire basis for coach's RULING 4 narrowing — the load-bearing leg is the **plural-only pattern-form argument**, which needs no sample size. **⛔ E-275 FIRED THIS IDEA'S OWN TRIGGER ON 2026-07-27 AND PROMOTION WAS DECLINED — the trigger is NOT spent.** SE established by execution that **no reordering can fix it**: only `\bseniors\b` matches, and a single-match name is order-independent (the most aggressive possible hoist still resolves `legion`). **The fix belongs to E-274's school-family branch and the answers are already RULED and merely unimplemented** (coach RULING 1 / E-274 TN-3) — so **this is one of TWO coach-ruled defects now hanging on E-274's build/shrink/shelve decision** (see [[IDEA-208]]); if E-274 shelves, this loses a home it only appears to have. Ruling of record: RULING 4 AMENDMENT. Related [[IDEA-206]], [[IDEA-213]], [[IDEA-172]]. Domain: SE (+ baseball-coach) | CANDIDATE | 2026-10-25 |
| [IDEA-206](IDEA-206-juniors-pattern-may-target-the-wrong-word-form.md) | **`\bjuniors\b` may be matching a word form nobody uses: 0 of 563 names and 0 of 2,518 raw bodies. Entirely unattested.** Coach's hypothesis is that Legion names its divisions adjectivally ("Junior"), which a coach would render **singular** — and the corpus is consistent: 4 singular "Junior" occurrences, **all four "Junior Varsity"**, zero "Junior Legion". So the pattern may be dead code occupying a precedence slot and appearing in guard tests as though it were live. **The sharper consequence is the INVERSE** — if real Legion teams do use singular "Junior", the classifier misses them entirely and nothing would surface it. **Both readings are speculative; the only established fact is the attestation count. ⛔ BLOCKED on a citation-grade check of actual Legion naming convention — coach was explicit: do not change the regex on recall.** The design trap if it is retargeted: **singular "Junior" collides with "Junior Varsity", 4 of 4 observed, which means the OPPOSITE** — a naive singular pattern classifies every JV team as Legion, far worse than the defect it fixes (`junior varsity|jv` already precedes the Legion patterns, so ordering may already do the work — check before designing around it). Also unsettled whether n=563 supports an absence claim at all: only 22 names carry a Legion token, and **absence within 22 is much weaker than absence within 563** — the exact denominator error already corrected once in this family. **Removal may be the honest answer** rather than retargeting. `seniors` breaks the hypothesis (8 attestations) for a reason that is itself the argument of [[IDEA-205]] — evaluate the two together. Hypothesis of record: RULING 4 AMENDMENT. Related [[IDEA-176]]. Domain: baseball-coach (+ SE) | CANDIDATE | 2026-10-25 |
| [IDEA-207](IDEA-207-little-league-dead-ends-to-generic-unknown.md) | **A team whose only `ngb` is `little_league` falls out of `detect_league_level`'s Priority 2 block to `return "unknown"` BEFORE the age-bracket and name-word ladder ever run** — so a real, well-known governing body with published age-tiered pitch-count regulations gets the same bare *"League not detected"* copy as a garbage or typo'd value. **baseball-coach has RULED (RULING 2): recognize it, do not leave it dead-ending, do NOT build its rule table yet** — a `_NGB_MAP` entry to its own league id (which `get_rules_for_league()` returns `None` for, as with `usssa`/`perfect_game`) plus its own `_LEAGUE_WARNINGS` string. **The general principle is the durable half**: "recognized value, deliberately no table" and "genuinely unrecognized value" **must remain distinguishable to a coach** — the first is a coaching-informative fact they can act on, the second says nothing about the opponent, and a third tier already exists in the code to carry it. Collapsing them is a real loss, and it is worse in the direction that discourages a coach from looking further. **Coach explicitly did NOT rule on the rule table** — separate, lower-priority (n=2 observed), and must cite Little League International before any number is printed as fact. **The higher-value move may be the open question rather than this fix: the `ngb` vocabulary has never been enumerated exhaustively the way `age_group` now has, so other values may dead-end the same way.** Small — map entry, warning string, test; recorded as a Tier 2 row in E-275's fixture pack rather than built (an executed row asserting the ruled label would have been red against the closure gate). Natural companion [[IDEA-179]]; related [[IDEA-178]], [[IDEA-182]]. Domain: SE | CANDIDATE | 2026-10-25 |
| [IDEA-208](IDEA-208-8u-14u-travel-bracket-binds-wrong-curve.md) | **LIVE, SHIPPED: the entire `8U`-`14U` travel bracket maps to `youth_travel` and thence to `PITCH_SMART_15_18` — a 105-pitch curve whose own constant name says which age band it is calibrated for — rendered with an amber "directional read" badge.** baseball-coach RULED 2026-07-25: **reclassify the whole below-15U bracket from BINDING GUIDELINE ESTIMATE to SUPPRESS, terminal**, identical to `Under 13`/`middle_12U`/`middle_13O`/`elementary`/`college`. **The equal-treatment argument is the sharp form: the same real child gets a different answer depending only on which picker their coach happened to use** — `middle_13O`, the school family's name for the same 13-14 population, already suppresses. Coach ruled the labeled estimate **insufficient mitigation**, and the usual "never suppress, always contextualize" principle does not transfer: that governs stat rows and sample-size uncertainty, while the starter card's suppress state is an honest ABSENCE of a projection. There is no uncertainty about this team's age — it is *"we know exactly what level this is and are knowingly applying a curve we have already ruled does not fit it."* **Audience makes it worse: USSSA 8U-14U youth coaches are now a named core audience in `docs/VISION.md`.** ⚠️ **NO EPIC HOME — that is why this is a capture rather than a story.** Ruled into E-274's lane 2026-07-26 but E-274's scope is the `age_group` SCHOOL family while this sits in the TRAVEL-bracket branch, so it is **already orphaned there**, and E-274 is under a build/shrink/shelve call at 4% measured value. **⚠️ Read with [[IDEA-184]] ALWAYS and do not merge them**: this is an ENGINE defect (wrong table selected → suppress), 184 is a COPY defect (the banner asserts this level publishes no pitch-count rules, false for a population that has them → rewrite); **neither fix closes the other**, and fixing one alone moves 8U-14U from a false number to a different false sentence. A correct youth table is separate, later work and **NOT a prerequisite**. Coach's falsifier recorded. Related [[IDEA-205]] (the other E-274-dependent ruled defect), [[IDEA-179]], [[IDEA-177]]. Domain: SE (+ ux-designer for the paired copy) | CANDIDATE | 2026-10-25 |
| [IDEA-209](IDEA-209-parse-ngb-case-asymmetry.md) | **`_parse_ngb` accepts a JSON string or an already-parsed list and lowercases only ONE of them** — `[str(v).lower() for v in parsed]` on the JSON path, bare `return ngb` on the list path (confirmed by reading the function, not relayed). So `'["AMERICAN_LEGION"]'` resolves `legion` while `["AMERICAN_LEGION"]` resolves `unknown`: the same value in the two shapes the function exists to accept gives different answers. **A quieter third path is the one to watch** — a bare non-JSON string raises `JSONDecodeError`, is swallowed, and returns `[]`, **indistinguishable downstream from "this team declared no governing body."** `unknown` is a suppress-or-warn state, so the failure direction is *less information* rather than a wrong number (the milder direction) but for a reason nobody would predict from the call site. **Trigger is real but narrow**: the JSON-string shape is what the GC public API returns, so the list path is exercised mainly by internal callers and tests — which is why it has not bitten. Few lines in one function; fold into the next epic touching `detect_league_level` or `_parse_ngb`, do not plan around it. **⚠️ Do NOT bundle with [[IDEA-210]] on the strength of "both are case normalization"** — see that row. One of four E-275 MINORs, mechanism re-verified against the source during planning. Related [[IDEA-201]], [[IDEA-202]], [[IDEA-207]]. Domain: SE | CANDIDATE | 2026-10-25 |
| [IDEA-210](IDEA-210-classification-compare-is-case-sensitive.md) | **`detect_league_level`'s Priority 1 branch tests `classification in ("jv", "freshman", "reserve")` against lowercase literals with no normalization, and the DEFAULT is the LESS conservative table.** Confirmed by reading the source: a stored `"JV"`, `"Freshman"` or `"Reserve"` misses every literal and falls through to `nsaa_varsity`, which requires **one less rest day at every breakpoint** than `NSAA_SUBVARSITY` (0/1/2/3 vs 1/2/3/4 at 30/50/70/90) — the under-rest direction this project treats as unacceptable. **Same class as the defect E-275 exists to fix, on a different signal and with a shorter path: no season ambiguity is needed to unmask it, only a capitalized string.** Severity is bounded by audience — Priority 1 is DB-sourced and applies to TRACKED teams, so the population is our own program: *more* worth fixing on one reading (these are the arms we are directly responsible for), less on another (the values are ours to control). **Neither reading has been checked — nobody has looked at what `teams.classification` actually holds, and that is a one-query `SELECT DISTINCT` that should probably just be run.** Promote on either trigger: anything touching Priority 1, or a single observed non-lowercase value. Open: is the value constrained on WRITE (canonical upsert, CHECK, enum) or free text — that decides whether normalizing the read is sufficient; and `program_type` is compared in the same block in the same literal style. **⚠️ Do NOT fold into [[IDEA-209]] without thinking**: both are case-normalization defects in the same function, which makes them look like one change, but they sit on different priority tiers, different data sources (DB column vs. API field), and **fail in OPPOSITE directions** — 209 toward less information, this toward a looser table. Related [[IDEA-201]], [[IDEA-202]]. Domain: SE | CANDIDATE | 2026-10-25 |
| [IDEA-211](IDEA-211-pii-safety-rule-misstates-current-gate-coverage.md) | **`.claude/rules/pii-safety.md` says `epics/**` and `.project/**` are UNGATED and the byte-gate is "scoped to `docs/api/` only". `.githooks/pre-commit` gates both trees** — it builds `GATE_TREES` by prefix-match, snapshots the index, runs `check_doc_pii.sh` per staged tree, and blocks on non-zero, with a counter check refusing to pass if a gate was staged but never executed. **It has already produced a defect on its first contact with a reviewer**: during the E-275 spec audit, code-reviewer cited the rule (doing exactly the right thing) and filed a MUST FIX proposing the epic adopt *"a real identifier here is caught by nobody"* — caught only because PM read the hook. Note the shape: **the false claim was the more ALARMING one, in a SAFETY note.** **⚠️ FIX THE SPECIFIC SENTENCES; DO NOT REWRITE THE SECTION** — the file carries a clause-by-clause criterion/evidence table: the `SKIP_PATHS`/cannot-regex-detect-NAMES half is **still true and must be preserved**, the IDEA-096 incident is **EVIDENCE** (editing destroys the record), and the correction must land in the **two-sided form** (an identifier already on the denylist DOES block; a NOVEL name is caught by nobody, and example mode passes non-blocking). **SECOND DEFECT, same family: on exit `3` the hook prints `INCONCLUSIVE — example mode`, does not set BLOCKED, and falls through to the identical `[pii-hook] PII scan passed.` terminating line CLAUDE.md tells the operator to verify** — so the line the operator checks is printed whether the gate certified the tree or certified nothing. **Root cause of the mode split, stated at the level that misleads in NEITHER direction: the script resolves its denylist relative to the CURRENT WORKING DIRECTORY, so ANY CWD lacking `secrets/` is example mode** — established by execution (an absolute script path from inside a worktree still returned 3; the person hunting the trap hit it four times in one night while looking for it). The worktree case is the common instance, **not the rule** — and the reverse error is worse: a worktree PATH certifies fine from the right CWD, which is how every E-275 certification of record was made. **⛔ Do NOT "fix" the fail-open** (deliberate, and the hook explains why); the defect is the reporting. Operator diagnostic: read which `[doc-pii: …]` line appears. Open: **is [[IDEA-102]] closeable?** Related [[IDEA-203]], [[IDEA-204]], [[IDEA-180]]. Domain: claude-architect | CANDIDATE | 2026-10-25 |
| [IDEA-212](IDEA-212-malformed-tier-table-under-rests-silently.md) | **`_is_excluded` picks a rest tier by looping the table with a `for/else` fall-through that guards only `total_pitches > max_tier.max_pitches` — the TOP tier. A pitch count landing in a MID-TABLE gap satisfies neither, so `required_rest` keeps its initialiser of 0.** SE executed it (narrowing LEGION's third tier to 46-55): counts 56-60 return **`[0,0,0,0,0]` days against a well-formed `[2,2,2,2,2]`** — a tier-boundary typo yields a zero-rest recommendation on a real arm, with a green suite and no exception. **LATENT, and the epistemic status is the reason this is a capture rather than an incident: coach BELIEVED no shipped table has the gap; SE VERIFIED it by execution across all six constants** (first tier starts at 1, every `max_pitches + 1` equals the next `min_pitches`). **If a future check finds a live gap this becomes urgent.** Coach's severity argument: a gap producing zero rest **looks identical to a confident, correct "no rest needed"** — no badge, no warning — which is the exact failure the suppression discipline running through every ruling exists to make structurally impossible. Benign contrast so the fix does not over-reach: `p=0` and negative counts also match no tier and **correctly** yield 0. **Two candidate shapes, not exclusive**: coach's contiguity assertion over shipped tables (cheaper, obvious to a future contributor) vs. SE's **refusing gate** (stronger — defends against malformed tables from sources not yet written). **Timing is the strongest part of the case: the table count is about to grow, so the defense earns its cost NOW rather than after the next table lands.** Also worth carrying: E-275 story 02's "add literal pins for two constants" **undersells itself** — on `NRBL` and `PITCH_SMART_15_18` those pins are the only thing between a typo and a zero-rest recommendation. **Two adjacent captures recorded inside rather than lost in a message**: `_is_excluded` takes `apps[-1]` as most-recent by LIST order not DATE order (live-or-latent turns on whether `build_pitcher_profiles` guarantees sort — unchecked), and whether Legion's published regulations actually contain the 2-in-3 rule the engine applies uniformly (**coach's domain, coach has not been asked**). Not a spec-audit finding — found beside the question, must not be counted in that tally. Related [[IDEA-178]], [[IDEA-211]]. Domain: SE (+ baseball-coach) | CANDIDATE | 2026-10-25 |
| [IDEA-213](IDEA-213-ambiguous-name-observability-flags.md) | **Two observability-only flags over `detect_league_level`, fully specified and cut from E-275 by operator ruling — a complete design parked for want of a consumer, NOT an open question.** ⚠️ **They are DISJOINT and not variants of one another; promoting one does not give you the other.** FLAG A fires on a Legion-family signal beside a generic-tier signal; FLAG B fires when `legion` rests on `seniors`/`juniors` **alone**. SE measured across all 45 Legion×tier shapes: **both 0 · A only 40 · B only 2 · neither 3** — and **B's population contains the only confirmed live defect while A's does not.** An earlier framing called B a "widening" of A, which **hid an option the operator was never offered: cut A, keep B.** **A's day-one output is five records on a settled question** (all five Legion + `reserve(s)`, adjudicated in full by E-275 TN-6 — *"no ruling change and no code fix"*), so E-275 TN-5's claims that it had *"the widest reach in this epic"* and was *"the only part producing new information"* were **overclaims, the second close to backwards — do NOT promote A on their strength.** A also fires on bracket- and `ngb`-decided names where **resolution is never in doubt** (SE drove 1,440 combinations with a positive control), diluting its only surviving justification, which is prospective arrival-detection. **B was cut NOT for lack of value but under the governing rule** — *"a confirmed live defect gets a fix or an explicitly-homed follow-up, never just a log line watching for its shape"* — with [[IDEA-205]] as that homed follow-up; **reviving B needs an affirmative reversal, not a silent one.** **Predicates are SETTLED by execution; only the record's CONTENT is open** — and **B needs FIVE conditions, not three**: the naive form false-positives on `ngb`-sourced and bracket-sourced `legion`. **Logging default is a SHAPE SUMMARY, not raw names**, and the rejected argument is recorded so it is not re-made as novel: *"names already appear in reports"* understates the difference — reports are access-gated, **logs get pasted into issues**; treating an existing exposure as licensing a new one is the move that needs naming. Two reusable things beyond the flags: **Flag A's trigger is a DERIVED RULE, not a word list** (a hand-transcribed copy already dropped `frosh`), and **the guard ACs A-2/A-3 CANNOT be demonstrated fail-first** — absence-of-feature makes an EMISSION assertion discriminate and an ABSENCE assertion vacuous, so a DoD demanding one either stalls or gets waved through. Related [[IDEA-206]], [[IDEA-172]], [[IDEA-201]]. Domain: SE (+ baseball-coach for the trigger ruling) | CANDIDATE | 2026-10-25 |
| [IDEA-214](IDEA-214-codify-e275-instrument-failures-into-tool-output-integrity.md) | **E-275's ten catalogued instrument failures sit in `.project/research/E-275-planning-record.md` §2, under a CLASSIFIER epic where nobody reasoning about tool output will find them — and E-275 is parked at DRAFT, so if it is ever archived the catalogue archives with it.** That is the failure mode this idea exists to prevent. `.claude/rules/tool-output-integrity.md` already owns the material; most instances are worked examples of rules it states, **plus three shapes it does not name**: (1) **a figure needs its DEFINING SETS attached, not just its value** — E-275 produced two 14s and two 22s, each pair genuinely different quantities colliding in size by arithmetic accident, so **a matching count is NOT evidence that two figures are the same quantity** (adjacent to the producibility check but distinct: producibility asks *could this number have been made*, defining-sets asks *of what population*); (2) **the excision that loses things is the one attached to a STRUCTURE rather than to CONTENT** — a protected analysis nearly dropped because it sat in a section HEADER above the ACs being cut, and sweeps that enumerate notes, ACs and figures do not enumerate headers, preambles and transitions; (3) **the TWO-ARTIFACT CONTRADICTION, the one the rule file most lacks** — an idea file called a defect mild while its own epic's Technical Note already stated the relation proving it harmful, **both files internally consistent and individually correct**, missed by six review passes, two audits and three agents who read both, and found only by EXECUTING the claim across its input domain. **Single-file review cannot catch that by construction.** And the strongest finding for wherever the rule file states its own limits: **awareness of the class conferred no immunity — two of the ten were committed by the authors of the rule against them, hours after writing it, one by a reviewer applying the very discipline in the audit commissioned to prevent that error. Every corrected figure was caught by re-deriving; not one by careful re-reading.** **PM cannot write `.claude/rules/**`, which is why this is an idea and not an edit.** A fold-in, not a project — promote when CA is next in the context layer for any reason, and **do not let it ride on E-275's closure**. Open (CA's call): how much inlines vs. stays a citation, and whether defining-sets merges with producibility or sits beside it. Related [[IDEA-211]], [[IDEA-203]], [[IDEA-204]]. Domain: claude-architect | CANDIDATE | 2026-10-25 |
| [IDEA-215](IDEA-215-reclaimresult-docstring-counts-intent-not-outcome.md) | **`ReclaimResult`'s docstring says its three count fields "count the rows removed." All three are computed BEFORE the deletes and describe the INTENDED target, not the outcome.** Three fields, **TWO** failure modes — not three: `teams_deleted`/`players_deleted` are **set sizes** (`len(team_ids)`, `len(player_ids)`), which **cannot go red on an under-delete** (the same vacuity E-277 story 03 surfaced against AC-1 one layer over); `roster_rows_deleted` is a **real `COUNT(*)`** taken pre-delete. **So the tempting one-line fix — "these are set sizes" — is FALSE about `roster_rows_deleted`.** The accurate unifying sentence is about **timing and intent**, not mechanism. Prose defect on a **hard-deleting** surface: a reader auditing whether a sweep did what it claimed reaches for these fields, and the docstring says they answer a question they do not. **⚠️ TWO HAZARDS. (1) THE PHRASE WRAPS — `grep "count the rows removed"` returns EMPTY** (`count the` ends one line, `rows removed` begins the next), so a searcher concludes it is already fixed; **discriminate by the `ReclaimResult` symbol.** (2) The **"three fields wrong in three different ways"** framing that circulated at E-277 closure is an **over-count, corrected at filing** — it would send someone hunting a third distinction that does not exist. **⛔ NOT authority for changing the fields to real rowcounts** — that is a behavioural change to a shipped result object and needs its own justification. Fold-in, no epic of its own; next story touching `lifecycle.py` docstrings takes it. Deliberately NOT folded into E-277 story 03: no AC covered it and `cr4` had already approved that story, so widening would have voided the approval's scope. Related [[IDEA-198]] (different problem, same module — kept separate so neither is buried under the other's title). Domain: SE | CANDIDATE | 2026-10-25 |
| [IDEA-216](IDEA-216-sweep-acs-must-forbid-correcting-an-accurate-citation.md) | **A sweep-style AC that says "correct **or** verify accurate" PERMITS the right outcome without FORBIDDING the wrong one.** `ca-2`'s sentence is the payload: ***an AC that permits the right outcome does not thereby forbid the wrong one.*** **The comparison that makes it actionable — both halves shipped in the SAME epic:** E-277 story 04's **AC-6a** gave a *disposition* with no ⛔ naming what must not change, while **story 05's AC-8 next door NAMED THE FILE THAT MUST NOT BE TOUCHED** (a byte-identical twin a literal instruction would have edited). **AC-6a still PASSES reachable-red** — *"record each citation with its verdict"* is checkable — **so falsifiable and preventive are DIFFERENT PROPERTIES, and this AC has only the first.** That is the reusable insight and the honest limit on reachable-red, this project's most productive AC instrument. **The evidence bounds any future review of this class: a clean diff CANNOT distinguish "the AC prevented the wrong edit" from "the implementer happened to check" — the two are byte-identical**, and it was settled only because `ca-2` reported the **mechanism** (it resolved each symbol to its `def` before editing) from the one position that could. Fix shape: a **⛔ naming the non-member that must survive**, with **resolve-each-symbol-to-its-`def`** as the named operation — the operation generalizes, the non-member does not. **⛔ NOT a finding against E-277 story 04, which SATISFIED AC-6a and delivered a correct sweep**; the defect is in the AC's text and its audience is the next author who copies the shape. Deliberately not retrofitted — tightening an AC after implementation and approval is the move refused for [[IDEA-215]]. Likely target `.claude/agents/product-manager.md` (binds only when writing/auditing an AC; an always-loaded rule taxes every agent for a narrow audience). Credit is `ca-2`'s; `cr5` explicitly declined it. Promote when the next sweep-style AC is written. Domain: claude-architect (+ PM) | CANDIDATE | 2026-10-25 |
| [IDEA-217](IDEA-217-query-record-counts-games-no-other-surface-counts.md) | **The coach-facing win-loss header counts a strictly wider set of games than every other surface on the report.** `_query_record` (`src/reports/generator.py:396-422`) filters on season + team participation + non-null scores and nothing else; `_query_freshness` (`:589-633`) and the game logs additionally require a perspective-scoped data-bearing stat child (`EXISTS player_game_batting/pitching WHERE perspective_team_id = ?`). **Both read directly, not relayed.** Adjudicated against GameChanger by api-scout: **GC 25-15-0 over 40 games · live header 25-16 · dev header 25-17** — prod carries one phantom row, dev both. **The probe replayed the perspective-scoped EXISTS into `_query_record` on dev and got the GC-correct record with ZERO data cleanup**, which is the whole argument for treating the header as independently fixable: it is correct after this change whether or not the bad rows ([[IDEA-218]], [[IDEA-219]]) are ever repaired. Structurally it is a **data-bearing-coverage violation of the shape `.claude/rules/data-model.md` already names** — a scored `games` row with no stat rows is the MODAL scouting case, so the header is not merely wrong about two rows, it is counting a population the rest of the report has agreed to exclude, and future rows of that shape land in it silently. Both phantoms are losses, so it currently understates the team — **nothing about the mechanism guarantees that direction.** ⚠️ **The fix is justified by an outcome, not an argument, and that gap should close before it ships**: ask baseball-coach whether a record should reflect games PLAYED or games we have DATA for — a real game whose boxscore was never scored is dropped by this change. Adjacent, from reading the query rather than from the audit: **a TIE counts as neither win nor loss and is invisible** (strict `>`/`<`), inert today at zero ties. ⛔ Row cleanup out of scope. Related [[IDEA-220]], [[IDEA-221]], [[IDEA-196]]. Domain: SE | CANDIDATE | 2026-10-25 |
| [IDEA-218](IDEA-218-dedup-natural-key-misses-twins-split-across-dates.md) | **`_find_duplicate_game` gates candidate selection on `game_date` EQUALITY (`game_loader.py:1183`, read directly), so a cross-perspective twin whose two rows carry different dates can never be collapsed** — the `start_time` and score-total doubleheader tiebreakers run only on rows the date gate already admitted. One real 2026 game is persisted twice under both perspectives' event ids and counts twice toward the record header. **⚠️ THE EFFECT IS ESTABLISHED; THE RELAYED MECHANISM DOES NOT MATCH THE CODE.** The audit said *"the two perspectives' start instants land on different UTC calendar dates"* — but `_derive_game_date` (`game_loader.py:146-165`) derives from **`summary.last_scoring_update`**, not a start instant, and converts through the canonical venue-local seam `derive_local_date` (`src/util/timezone.py:78`), with a raw UTC slice only as the unparseable fallback. **Three candidate causes nobody has separated** — different `last_scoring_update` instants per scorebook, different-or-absent per-perspective `timezone` values, or a fallback path — **and they imply different fixes. Establish which first; a fix aimed at the relayed phrasing hits the wrong line.** Why it matters beyond the header: `.claude/rules/canonical-seams.md` makes **prevention over cleanup** the stated design, and this defeats it silently (nothing errors, nothing warns); a twin is two child surfaces for one game, safe only while every consumer perspective-scopes — a per-query discipline, not a structural guarantee. ⚠️ **Widening the key to a ±1-day window is a MERGE RISK, not an obvious fix** — it pools genuine consecutive-day games against the same opponent, and a wrong merge is destructive; fixing the derivation is narrower if the derivation is what is wrong. Open: how many twins already carry this shape (nobody swept), and **is `last_scoring_update` the right basis for a calendar date at all** — it is when a scorekeeper last touched the book, possibly the next morning. ⛔ Cleanup goes through `merge_duplicate_game` / `bb data merge-duplicate-games`, NEVER a hand-deletion. Related [[IDEA-217]], [[IDEA-219]], [[IDEA-220]] (opposite dedup outcome — confirm they are different games). Domain: SE (+ data-engineer) | CANDIDATE | 2026-10-25 |
| [IDEA-219](IDEA-219-free-text-opponent-name-match-attributes-a-third-teams-game.md) | **A third team's home game is recorded as a loss for us, because during THAT team's ingest its free-text opponent string matched our team's name.** **Disproven rather than doubted**: api-scout adjudicated against GameChanger and our team verifiably played a different game at the **identical instant** — two games at one moment is impossible, so this is not a judgment call about which record is better. **The row is the small half; the UNIDENTIFIED CREATING PATH is the finding** — a phantom that exists is a minute's deletion, a path that manufactures them produces one per matching opponent string forever, and the rows look structurally identical to real games. It is also **cross-team contamination**, a worse class than a duplicate: another program's game asserted as ours, inherited by any future surface that trusts `games` participation. Note the neighbouring precedent — the canonical team-creation cascade **deliberately refuses to attach `gc_uuid`/`public_id` on a name-only match** because a wrong name-only identification is irreversible; if a free-text string can nonetheless bind a GAME to a team by name, that caution is enforced on one axis and not the other. **⛔ Identify the path BEFORE deleting — the row is currently the only known instance of whatever produced it.** Hypothesis to falsify (from the documented cascade, NOT a diagnosis): the name+`season_year` match rung. Strong discriminator available: **does the third team's row carry a `progenitor_team_id`?** — presence means the coach linked via team lookup, absence means hand-typed, separating "our matching was too loose" from "the source was ambiguous." Open: **which** of the two phantoms prod carries (25-16 vs dev 25-17 — the handoff does not record it, and it decides merge-vs-delete); whether **impossible simultaneity** is worth building as a standing detector (it needs no external adjudication); and how many more exist — nobody swept. Promotion trigger: **a second phantom anywhere means an active creating path** (same trigger as [[IDEA-196]]). Related [[IDEA-217]], [[IDEA-218]], [[IDEA-088]], [[IDEA-043]]. Domain: SE (+ data-engineer) | CANDIDATE | 2026-10-25 |
| [IDEA-220](IDEA-220-double-loaded-plays-probably-by-design-verify-the-consumers.md) | **⚠ FILED WITH ITS OWN PREMISE CONTESTED — the audit flagged one game's 142 `plays` rows (71+71 under two `perspective_team_id`s, vs a 47-77 norm) as an anomaly, and `.claude/rules/perspective-provenance.md` says it is the DESIGN**: whole-game idempotency is keyed `(game_id, perspective_team_id)` and *"different perspectives of the same game each get their own plays rows."* The `games` row storing the OPPONENT's event id is likewise just which perspective the dedup collapsed first — load order, not a correctness property. **So the honest reading is that this game was correctly deduped and correctly loaded twice — the OPPOSITE case from [[IDEA-218]], where a twin was NOT collapsed; confirm they are different games before any fix touches both.** **The one thing that would make it real: doubled rows are safe only while EVERY consumer filters on `perspective_team_id`** — a per-query discipline, MUST-constraint 3, and the #1 hazard of the E-259 query-time cutover, because omitting it doubles a line with nothing crashing. **Nobody has checked whether any displayed stat doubles for this game**, and that is the entire question; it is answerable by comparing this game's contribution to FPS%/P-PA/P-BF/QAB% against a single-perspective game, or by looking for it as an outlier in `bb report reconcile-scoreboard`. Bounded, not proven, by the same audit's calc lane: ~314 facts verified correct per environment, identical behavior prod vs dev, no doubled stat observed. **⛔ Do NOT delete either perspective's rows to tidy the count** — both are real data, and E-244's redirect-map plus whole-game idempotency mean a hand-deletion will not re-load cleanly. **The durable point: a row count that looks wrong against a per-game norm is not evidence of a defect when the schema deliberately stores one row set per perspective — check the consumers, not the count.** Related [[IDEA-217]], [[IDEA-196]], [[IDEA-106]], [[IDEA-124]]. Domain: SE (+ data-engineer) | CANDIDATE | 2026-10-25 |
| [IDEA-221](IDEA-221-report-number-formatting-paths-disagree-across-sections.md) | **Four cross-section display inconsistencies, present IDENTICALLY in live and dev — which rules out data and localizes all four to render paths.** Three share one shape: **one value, several formatting paths, no shared seam.** (1) the **Top Bat callout** shows a SLG one digit off from the batting table and spray card, which agree with each other (`.537` vs `.538`) — the callout is the outlier; (2) a **Most Likely Arms mini-log row shows an ILLEGAL `5.3` innings pitched** where the main pitching table shows `5.1` for the same outing; (3) a batter at **exactly `.2625`** renders `.263` in the batting table and `.262` on his spray card; (4) the bullpen annotation's `N/29` numerators disagree with the pitching table's GR counts and the `/29` denominator is unexplained — **lowest confidence, flagged for investigation, not asserted.** **Symptom 2 is qualitatively different and is the one to promote alone if anything is: `5.3` is not a rounding disagreement, it is a value that CANNOT EXIST** — innings are integer outs rendered in thirds, so the fractional digit is only ever `.0`/`.1`/`.2` — and it sits on the card that informs pitcher-availability decisions. The others are individually cosmetic; **their cost is trust, not accuracy** — two numbers for one player on one page invites exactly the hand-checking against GameChanger the tool exists to replace, under pre-game pressure. Structurally this is the **canonical-seams defect in the PRESENTATION layer**, so fixing four symptoms individually leaves the fifth to be found by a coach. Hypotheses to falsify (reasoned from the values, **not** read from the render paths): symptom 2 may be a decimal division (`16/3 → 5.33`) where a thirds conversion belongs — a wrong UNIT CONVERSION, meaning that path may be wrong for every outing with only non-multiples of three visibly illegal; symptoms 1 and 3 look like a **round-half-even vs round-half-up** split, `.2625` being the classic discriminator. **The convention should be RULED against GameChanger's own, not defaulted.** Companion result that localizes it: the same evaluation's calc lane verified ~314 facts per environment as computationally correct — **the computation is sound and the divergence is in presentation.** No player is identified in the file, deliberately. Related [[IDEA-217]] (the query-layer instance of the same two-surfaces-two-answers shape), [[IDEA-196]], [[IDEA-083]], [[IDEA-076]]. Domain: SE (+ ux-designer) | CANDIDATE | 2026-10-25 |

## Status Definitions

| Status | Meaning |
|--------|---------|
| `CANDIDATE` | Active idea, worth revisiting |
| `PROMOTED` | Became an epic -- see Notes in the idea file for the epic ID |
| `DEFERRED` | Set aside deliberately -- includes reason and re-review date |
| `DISCARDED` | Decided against -- includes reason |

## Adding a New Idea

1. Copy `/.project/templates/idea-template.md`
2. Name it `IDEA-NNN-short-slug.md` (next number in sequence)
3. Fill in all sections
4. Add a row to the index table above
