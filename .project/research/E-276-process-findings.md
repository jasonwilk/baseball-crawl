# E-276 — Process Findings from the Planning Session

**Date**: 2026-07-25. **Source**: extracted verbatim from the History section of `epics/E-276-reconcile-health-gate-prior-capture/epic.md`.
**Status**: reference record. Nothing here is needed at the point of implementing E-276.

> ## Extraction status: COMPLETE. This file is now the authoritative copy of the process findings.
>
> `epic.md`'s History has been trimmed to the one-line index in the **Index** section below plus its chronological epic-status entries. The full text lives here.
>
> **⚠️ It was halted once, and the reason is worth keeping** because it is the epic's own subject arriving in the epic's own extraction. On the first pass the destination was written and the source deliberately NOT trimmed — then `epic.md` was observed to have **gained a History entry from a concurrent author after the copy was taken** (*"THE CORRECTION OF A DEFECT IS NOT A SAFER PLACE THAN THE DEFECT WAS"*). Deleting ~160 lines from a section under active authorship is where content is silently lost, and it would have produced the worst available outcome: an index in `epic.md` pointing at a research file missing everything added after the snapshot.
>
> **A file-state verification is a claim with a timestamp** — *"copied verbatim before reporting"* bounds what was copied, not what is there now. The resumption re-read the History end to end, diffed it against this file, found exactly one added entry, copied it across, and only then trimmed. **The safe ordering is destination-first, and the safe check is a full re-read rather than a resumed offset.**

---

## What this file is, and why it exists separately

E-276's `epic.md` grew to ~1,300 lines, and its History carried the session's process findings with their instances and derivations. Those findings are the epic's most transferable output **and an implementer never needs them**, so they are extracted here and the History keeps a one-line index plus this pointer.

**The selector used**, and it is the general test for anything else in that file: *is this needed at the point of implementing, or is it a record of how we arrived at the decision?* The first belongs in a story or a Technical Note; the second belongs here.

**What deliberately did NOT move, and must not be swept here later**: the design, the preconditions, the ACs' basis, the forbidden shapes, the five required inputs, the operator-facing scope section, the régime tables (though not their derivations), and the load-bearing prose corrections. Anything an implementer or reviewer must not have to fetch stayed inlined in `epic.md`.

**Extraction method**: entries are preserved **verbatim and in their original order**, not re-cut thematically. Several of these findings turn on single words (`bound` vs `rate`, `safety` vs `availability`, `self-healing` vs `permanent-while-broken`), and a re-authored summary of a finding about summaries carrying truth-values past their qualification would be the fifth host applied to the extraction itself. The thematic grouping lives in the index below, not in the text.

**Chronological epic-status entries stayed in `epic.md`** — creation, the gate-semantics settlement, the numbering decision, and the round that retracted the roster-lock fix-neutrality claim. Those are epic history proper, not process mechanisms.

## How to read this record

> Every failure below was **caught**, inside the session, by a named check — and that is why it is here. The point is not that people were wrong; it is which instrument caught what, so a future planner can tell which checks are load-bearing. DE's framing, and it governs the whole record including the orchestration items: *a claim that was flagged, executed, and overturned within the session is evidence FOR the rule that caught it. An unflagged version of the same claim would have shipped* — this epic would have named a mechanism that is wrong at loader tier. **The rules earned their place by catching something, not by anyone having been wrong.**

---

## Index

**The host / mechanism series** (a claim's carrier, numbered as they were found):

| # | Name | One line |
|---|---|---|
| 5th host | The SUMMARY of the claim | A summary inherits the truth-value of the moment it was written and carries it forward silently |
| 6th host | A true claim with false stated SUPPORT | Nearly discarded because checking the citation would have discarded a sound conclusion |
| 7th host | A quantity over a space the author CHOSE | Reported as though the space were given; every count must carry its space |
| 8th mechanism | **PROVENANCE AS AUTHORITY** | A claim about X from the owner of X is the least-checked kind; the one with predictive power |
| — | A printed conclusion inside an execution artifact | Inherits the artifact's credibility without inheriting its verification |
| 13th mechanism | **THE DANGEROUS FIX PASSES EVERY CHECK** | Removing a false claim about a relation makes the document consistent and leaves the relation broken. **(b) closes every CHECK while closing NOTHING** — the fix a consistency sweep rewards, and the one that discards the finding. See Addendum 2 |
| 11th mechanism | **A ONE-WAY CITATION IS NOT AN EDGE** | The target exists and carries what the source says, and the target never routes back. **Under a "read only the notes you cite" protocol the note is unreachable** however clearly it names the story. **A both-ends check that walks one end produces a ✅ that forecloses the re-check** — an unverified edge is safer than a wrongly-verified one. Instance: TN-19, whose promotion's stated purpose was unmet for the life of the note. See Addendum 2 |
| 12th mechanism | **AGREEMENT WITH A DERIVATIVE IS NOT CORROBORATION** | Two artifacts agreeing on a detail *neither needed* is the signature of copying, not of observation. Third instance this epic. **Open the primary; never consult a summary of it.** See Addendum 2 |
| 9th mechanism | **A COUNT FALSIFIED BY AN EDIT THAT NEVER TOUCHES IT** | Correct when written, invalidated **remotely** when its space's boundary moves elsewhere. **Not the 7th host** — that one is wrong at authoring time and fixed by stating the space; this one *stated* its space and the space moved. **No sweep finds it**: the text is unmodified and self-consistent. See Addendum 2 |
| 10th mechanism | **A VERIFIED REPAIR VERIFIES WHAT IT WAS POINTED AT** | Enumerations drift **downward-only** — the site that ADDS a member is rarely the site that LISTS them, so a list leaks past a failed review, a repair, *and* a verification of that repair. See Addendum 2 |

**The two-pattern taxonomy** (do not collapse these — a reviewer drilled on the first probe passes the second):

| Pattern | Probe |
|---|---|
| Correct **verdict** on an **unsupporting premise** | *does the premise support the conclusion?* |
| Correct **fact** on the **wrong axis** | *does the fact bear on the claim at all?* |

**Everything else, by theme:**

- **The signature defect, in checkable form** — *what region did I sample, and what region am I claiming?*
- **Asymmetric framing (the top codification candidate)** — neither the alarming nor the reassuring qualifier is the defect; the asymmetry is. Check: *are BOTH sides of the trade stated?*
- **Verification that inherits what it was meant to catch** — a confirmation is only as independent as its most-constrained axis.
- **A correct narrowing licenses stopping** — more dangerous than an under-covered input set, because it is real work.
- **Grep and sweep method failures** — a grep cannot distinguish an assertion from its retraction; a correct pattern silently narrows when markup moves; a hit can be the correct form that looks like the error. **Further members catalogued in Addendum 1 and 2: line breaks, and word INFLECTION (`permit` vs `permits`, missed against text the searcher wrote minutes earlier). The class's member COUNT is disputed across three enumerations and is deliberately left unstated.**
- **The tool label says "occurrences" and the value is LINES** — `output_mode=count` returns matching lines, `-o` returns matches; 19 vs 33 on one pattern. **Two agents misled in one session, hours apart.** Addendum 2.
- **The four Codex passes bound differently than they read** — 7 findings / 0 dismissed, but two were found only by the sweep that followed the pointer, and one overturned the previous pass's fix. Addendum 2.
- **A review run CONCURRENTLY with fixes to its own subject cites stale lines** — a **gate-sequencing** defect, not a reviewer defect; filing it as the latter tunes the instrument to compensate for a scheduling choice. The verify-by-content rule absorbed the cost. Addendum 2.
- **🔧 TWO GREPS AND A READ — the technique, split by which of three failures it catches.** **Discovery**: grep the *target population* for the source's identifier (all ideas for `E-276`). **Verification**: grep each *named consumer* for the citing note's identifier (story 03 for `TN-19`). **Content**: **open the target and count** — no grep reaches a false claim about what the target says (F4). **Enumerating an edge backward is not verifying it backward**, and **opening a file is necessary but not sufficient when the claim is a count.** Addendum 2.
- **⛔ AN EXCLUSION ADDED FOR PRECISION CAUSED A FALSE CLEAN** — `[^|]{0,200}` to keep matches in a table cell, against prose that uses `|` freely. **The other narrowing members bite BY ACCIDENT; this one bit BY CONSTRUCTION**, so the tighten-the-pattern instinct is the cause and careful work is the more exposed. **Remedy inverts it: simple unanchored patterns, one term per invocation, no exclusions — accept noise, read hits.** Both parties then undercounted the same claim from opposite directions (3 and 4), which is the demonstrated case for sweeping **forms, not sites**. Addendum 2.
- **🔧 REPAIR SHAPES DIFFER: a citation defect is ONE EDIT AT ONE END; a content-claim defect is a SWEEP** — it propagates into every derived artifact (index rows, summaries, the entry stating the actionable edit). **F4 did not close because it was repaired as a citation defect.** A finding tells you where it *looked*, never where the claim *lives*. Addendum 2.
- **⛔ THE LIMIT OF ARTIFACT REVIEW — three levels, and the third has no reviewer.** A single-file sweep misses cross-artifact gaps (G2); an **edge-walk** finds those and misses defects in the relation between an artifact and **the process that produced it**; **nothing finds the third — the mechanism appears in no file.** Sole detector: **the actor volunteering it.** Commissioning another pass cannot close this class. Addendum 2.
- **A rule earning its keep on a case its author never considered** — verify-by-content was written for line-number rot and absorbed *concurrent-edit* rot. **The strongest argument for keeping a rule, and the one that goes missing, because the cost it prevented never became an incident.** Addendum 2.
- **Editing a retraction into agreement with the text it retracts** — a new failure shape, one instruction away from happening.
- **The divergence-count gap** — four successive wrong reconciliations, each accepted because it corrected its predecessor.
- **Single-run blindness** — fifteen failed attacks, all single-run, against a multi-run effect.
- **Review design** — the two-pass self-audit, and the three techniques re-reading cannot replace.
- **The orchestration layer's own failures** — seven instances, one with a structural fix.
- **Attribution and credit** — four participants corrected a credit downward; why an accurate tally is what makes the record usable.
- **What worked** — persisting a throwaway probe was the cheapest high-value act of the session.
- **Retros in participants' own words** — DE's, SE's.

---

## The findings, verbatim

- **THE SIGNATURE DEFECT FINALLY HAS A CHECKABLE FORM** [SE, offered as mechanical rather than as a call for care]:

  > **What region did I sample, and what region am I claiming?**

  Every instance today was *a correct computation carried past its case*, and that framing diagnoses but does not prevent — you cannot instruct someone to notice a boundary they have not seen. **This question has a natural checkpoint even though the inferential step does not**: it is answerable at the moment of writing the claim, from the claim and the run alone, without re-deriving anything.

  The instance that produced it: a strand run sampled `R=13, c=14` — **the first row of the LOCK régime** — and reported the trade for the *whole* widening region from that single point. **Confirming a claim quantified over a region requires sampling the region, not a point inside it.** It is the operational half of CR's *"a confirmation is only as independent as its most-constrained axis"*, and the two belong together: one asks which axes you varied, this asks over what range.

- **A THIRD INSTANCE OF A CORRECT VERDICT RESTING ON A PREMISE THAT DOES NOT SUPPORT IT — and the three together make it this epic's most reliable failure.** §4 used **rows**-re-derivability to license a claim about **deletes**; §6 used re-derivability to license adequacy in the one input where re-derivability is false; §3 offered **change-control on a constant** (the cap cannot move silently — true, and verified) as evidence for **adequacy of the guard at its current value** — which is false at the shipped value by the epic's own executed evidence, since a degrading crawl erodes 13 rows to 1 **at cap 2, with nobody tuning anything.**

  **All three verdicts were RIGHT. All three reasons were wrong.** `.claude/rules/tool-output-integrity.md` already names why they survive — *a verdict's stated reason rots independently of the verdict, and a correct conclusion immunizes its false premise* — so **any check asking "was the call right?" passes all three.** The only check that catches them is reopening the premise, which is what the adversarial reads did.

  **⚠️ A FOURTH INSTANCE, and it is a DISTINCT SUB-SHAPE with a different detection question** [CR-2]. The first three are *correct verdict on an unsupporting premise*. The fourth is **correct FACT on the WRONG AXIS**: the §3 grading cited *"the cap is pinned, it cannot move silently"* — **true, verifiable, about the same constant** — as evidence of the cap's **adequacy at its current value**. The two are **orthogonal**: change control triggers only on mutation, the adequacy failure requires none. They never touch.

  **The detection question differs, which is why it needs its own name.** For the first three you ask *does the premise support the conclusion?* For this one you ask **does the fact bear on the claim at all?** A premise that is merely insufficient still points at the right subject; a fact on the wrong axis does not, and **checking it harder confirms it harder.**

  **Orthogonal is WORSE than opposed, because tension is visible and orthogonality is not** — and it *manufactures* confidence rather than merely failing to remove it.

  **In each case the correct reason was already on disk, one or two sections away.** For §3 it is the accepted-residual argument: the operator ruled V1 knowing the trade, and the rate residual is recorded as *"ACCEPTED, not closed"* because the cap cannot distinguish a genuine two-a-week departure sequence from slow degradation — they are byte-identical, and no gate could separate them on the evidence a crawl carries. **The finding is non-blocking because the residual was priced and taken, not because a test pins a constant.** Each author reached for the nearer fact rather than the right one.

- **THE CORRECTION OF A DEFECT IS NOT A SAFER PLACE THAN THE DEFECT WAS.** Recorded as its own finding rather than a footnote, because it is the strongest argument in this record for adversarial re-execution over review-by-reading.

  > **PM over-claimed, then over-retracted, and neither was caught by its author.**

  The over-claim was a confounded number inflating a methodology finding about PM's own method failure. The over-correction was an honest retraction about *one run* that sat **adjacent to the hazard description**, where it read as deflating *the hazard* — **the identical position, one step later.** And it was committed *while writing up the correction of a different defect*.

  **A self-review of a correction is exactly where confidence peaks**, which is why the correction inherits the original's blind spot rather than escaping it. The tell is the same one this epic records elsewhere from the other side: **the two defects the reviewing layer caught in PM were both cases where PM was most confident it had just been careful.**

  **Consequence for the record**: a defect and its fix are **two claims, not one claim and its resolution** — and the fix gets less scrutiny precisely because the surrounding attention has just been spent. Neither this instance nor its predecessor yields to "check it again"; both required a reader who had not written either.

- **A DISTINCT SPECIES: VERIFICATION THAT INHERITS THE THING IT WAS MEANT TO CATCH.** An exhaustive sweep was run *to verify another agent's claim* and **reproduced that agent's blind spot** — both had held the churn axis constant, independently. SE's own words: ***"I did not verify your claim. I reproduced your blind spot with different arithmetic."*** Two agents, one fixed axis, and **the agreement made the claim more credible than the original rather than testing it.** Found by a third varying the axis the other two had fixed.

  **Not "an unchecked claim" and not "a correct computation carried past its case"** — it is a *check* that failed in the same way as the thing it checked, which is why it made matters worse rather than merely failing to help. **Operational consequence, and it is what makes this recordable rather than merely cautionary: a confirmation is only as independent as its MOST-CONSTRAINED AXIS — so a verification must state which axes it VARIED, not just its result.** "Confirmed independently" is not a claim anyone can evaluate; "swept over A, B, C with D fixed" is.

- **FOUR PARTICIPANTS CORRECTED A CREDIT DOWNWARD TODAY, and this is recorded for a structural reason rather than as courtesy.** A reviewer declined attribution for a residue finding because its own corollary had asserted a property of `merge_player_pair` **it had not read** — *"the same error as yours in the same paragraph, and I said it with more confidence."* Another credited a colleague's independent run that its own report had absorbed. **An accurate attribution record is what makes the failure tally usable**: a tally inflated by generous crediting would mislead exactly the reader it is written for, because the interesting quantity is not who erred but *which check caught what*, and a misattributed catch teaches the wrong check.

- **THE COMPLETED TALLY, which supersedes every count of "findings" in this record** [DE's final synthesis]:

  > Between all four participants — the reviewer's four, DE's five, SE's three, the relay layer's five — **every one was a correct computation carried past its case. Not one was an arithmetic error.**

  **Seventeen defects, zero of them arithmetic.** That is the epic's subject measured on itself, and it is why "check the maths" would have caught none of them.

  **And it explains why BOTH review passes were needed rather than one** [DE]:

  > An author checking their own computation **re-derives the computation, which is correct, and never re-examines the scope claim, which is the part that was wrong.**

  **Record this as COMPLEMENTARY to the earlier entry finding the self-audit the higher-yield pass — not as a correction of it.** Both are true and they cover different failure modes: the self-audit catches what only its author can see (a frame they know they chose), and cross-review catches what its author structurally cannot (a scope claim invisible from inside the case that produced it). Letting the later entry read as overturning the earlier would lose one of the two.

- **THE SHARPEST PROCESS FINDING OF THE SESSION, and it is about a WORD CLASS: three findings needed a qualifier corrected in the direction of sounding SAFER — *"self-healing"*, *"≤2 as a total"*, and *"bounded"* versus *"rate"*.** Each was caught by a different agent, each about someone else's phrasing, and **none by its author.**

  **The mechanism is what makes it actionable**: each was written by whoever had just done the work that made the claim true *in the case they tested*, and in each case the softer word was the one that made the result feel finished. **The author is the person least able to see it, because the qualifier is invisible from inside the case that produced it.** That is why all three needed a different agent, and why the count is three rather than zero.

  Directly consistent with `.claude/rules/tool-output-integrity.md`'s safety-comment sub-class — alarming prose is self-protecting — but running the *other* way: here the **reassuring** word is the self-protecting one, because it signals the work is done.

  ### ⚠️ AND THE MIRROR, DEMONSTRATED RATHER THAN HYPOTHESISED — read this entry and the one above as ONE finding

  **The author of the entry above then committed its inverse, twice, in the same document.** PM's two errors this session both ran toward sounding **more alarming**: an inverted polarity attaching *"safety"* to a fail-CLOSED asymmetry, and an operator-facing summary that stated the régime where the shipped design corrupts a stat while **omitting the régime where today's code permanently locks the roster and the design fixes it.** Both were committed while consciously guarding against the reassuring-qualifier failure.

  **The consequence is what makes the pair usable: a rule against soft qualifiers that produces hard ones has not fixed anything — it has MOVED the bias.**

  **So neither direction is the defect. The ASYMMETRY is.** That resolves what would otherwise read as a contradiction in the record — `tool-output-integrity.md` saying alarming prose is self-protecting, the entry above saying reassuring prose is too. Both are true, because the failure is **asymmetric framing in either direction**, and the two rules are one rule seen from opposite ends.

  **The check is therefore NOT "does the wording sound appropriately grave."** It is: **are BOTH sides of the trade stated?** A widening reported without its alternative is not a trade, it is a defect — and the same sentence with the alternative attached is a decision the reader can actually make.

  **Both instances were caught from outside**, and the second landed inside *"the section whose whole purpose is to let an operator judge fairly."* Same structure as everything else in this record: the defect lands in the artifact designed to prevent it, is invisible to its author, and is caught by a reader who did not write it. **That is also the argument for why this pair, and not the mechanism generalisation, is the codification candidate — it names a check that a non-author can run in one pass.**

- **THE MOST TRANSFERABLE SENTENCE THE DESIGN ROUND PRODUCED, in DE's words at DE's request:**

  > **An under-covered input set announces itself as incomplete — you can always ask "did I try enough?" A correct narrowing answers that question falsely, because it tells you the space is small and you have already characterised it.** The failure is not skipping work; it is having done real work that retires the wrong question.

  The instance: DE derived the exact region where the losing design's floor is active — correct work — and then treated that region as homogeneous. It is not; it splits into transient and sustained truncation, and the two designs differ decisively across that split. **Neither author caught it**, and the counterexample and its execution landed within one exchange of each other. Distinct from every other host catalogued here: not a claim propagating unverified, not a summary outliving its qualification, but **rigour producing a stopping condition.**

- **A NEW FAILURE SHAPE, and it was one instruction away from happening: EDITING A RETRACTION INTO AGREEMENT WITH THE TEXT IT RETRACTS.** PM was directed to repair a defective sentence "in `epic.md`, to match the authors' wording". The epic's only occurrence of that sentence is **inside PM's own correction record, quoted as the thing being reported false.** Complying would have rewritten a correction until it no longer corrected — leaving a paragraph that announces a defect and then states it approvingly.

  **Why it is its own shape rather than a variant of the bad instruction that produced it**: the instruction was wrong about *where* the text was, but the damage would have come from a **class of edit that is always available and always wrong** — a retraction is the one kind of prose whose quoted material must NOT be brought into line with the current text, because the quotation *is* the evidence. Any "make these two copies consistent" sweep will reach for it. **A consistency pass that does not distinguish an assertion from its retraction will silently destroy the retraction**, and the result reads as more consistent, not less.

- **AND THE GREP CANNOT TELL THEM APART — prohibition 3 from an angle the rule does not currently state.** `.claude/rules/tool-output-integrity.md` warns that a grep hit does not tell you *which alternative of an OR-pattern* matched. This session produced the sharper case twice: **a grep hit cannot distinguish an assertion from its retraction.** Searching for a defective sentence returns the paragraph retracting it, the paragraph quoting it as false, and the paragraph asserting it, identically. **Only a read separates them** — and the reader most likely to skip the read is the one running a consistency sweep, which is precisely the operation that would destroy the retraction. Worth codifying in those words, since the rule's current phrasing does not reach it.

- **A grep hit can be the CORRECT form that LOOKS like the error — the doc-sweep shape running in reverse.** Resolving whether an acceptance criterion still carried a superseded mandate, a grep for the offending phrase returned two hits and **both were the corrected text** (one a quotation of the superseded version inside the block correcting it; one a heading reading *"NOT by short-circuiting"*). `.claude/rules/doc-sweep.md` records an error hiding behind a legitimate neighbouring use of the same token; this is its inverse, and it is why the prohibition-banner sweep is specified as **grep-then-read** with that reason attached rather than as a bare step. **Two reviewers and a predecessor had ruled the AC defective from the match.**

- **THE COORDINATION PROTOCOL CAUGHT REAL DEFECTS EVERY ROUND AND PRODUCED TWO FAILURE MODES OF ITS OWN**, recorded together because reporting only one would misrepresent it. It caught: the roster lock, the population mis-description, the precondition-(e) direction, four wrong count claims, and two prohibited shapes in implementation guidance. It also produced: **re-raising items that had already landed** (three message crossings on one artifact, and four repeated asks on a receipt that had already been confirmed twice — the asker reading a stale state and escalating on it), and **routing a review request without its object** — a reviewer was asked for an adversarial read of a document it had never been sent, refused on exactly the right grounds, and asked for the text. In an epic whose subject is unverified claims, **being asked to review an artifact you have not seen is the failure mode arriving through the process rather than the prose.**

- **THE FINDING THAT SUBSUMES FIFTEEN FAILED ATTACKS: every sweep anyone ran was SINGLE-RUN, and the lock is a MULTI-RUN effect.** Twelve executions across three agents, every one a single load. **No single-run sweep of any parameterization could have observed F1** — so the three reviewers did not miss it through insufficient coverage on some axis. **We were not under-parameterized; we were measuring the wrong KIND of object.** That sits one level below the earlier "every attack aimed at what the gate permits" diagnosis and subsumes it. Consequence, made binding pre-dispatch: the game and player-line grains must be checked **multi-run** before any dispatch, because they share the denominator structure and nothing had examined them that way. **Game grain: CHECKED CLEAN**, byte-identical across both multi-run shapes, with a mechanism worth keeping — newly-completed games enter `fresh` and so raise numerator and denominator together, meaning the strand cannot outgrow the recurring set. Recorded as a **checked clearance with its mechanism**, not as an absence of findings. **Player-line: DIVERGES**, verdict held pending an executed check of whether the in-run `dedup_team_players` sweep closes the doubled-row window.

- **A NEW HOST, and the most insidious of the set: a printed conclusion inside an execution artifact inherits the artifact's credibility without inheriting its verification.** SE's own sweep printed a conclusion line contradicted by its own data one line above — the rule right, the labelling off by one. Not a relay and not a summary of someone else's claim: **a summary generated in the same breath as the data it misreads, sitting inside the evidence file where it looks like output.** It matters here more than anywhere because this session's entire discipline has been *prefer executed artifacts* — and this is a false claim wearing an executed artifact's clothes. SE caught it by re-deriving the algebra rather than re-reading its own summary.

  **Its exact companion, from the reviewer that ruled on the convergence: "re-running a script only confirms the script."** That reviewer settled the 20/26/44 question by re-deriving the figures **by hand** from the three shapes and cross-checking the shape set against a different artifact — not by re-executing SE's code. **Two ends of one insight: an artifact's authority extends neither to the prose written beside it nor to a second run of it.**

- **RECORDED BECAUSE IT WORKED, and a process record that catalogues only failures teaches anxiety rather than effectiveness.** The convergence question was unanswerable for one reason: SE had run its sweeps as inline heredocs and never saved a file, so a reviewer searching both shared scratchpads **found no artifact producing 20, 26, 44 or 222** and could only recommend withdrawal. **SE then persisted them to `scratchpad/t_divergence_sweep.py` — about ten minutes of work — and an unverifiable claim became a verified one.** The file states the discriminating yes/no answer, its population consequence, and its own single-run blind spot up front. That is what made a *ruling* possible instead of a withdrawal, and it is why the sweep is now on TN-16's port list rather than lost. **Persisting a throwaway probe is the cheapest high-value act in this whole session.**

- **THE DIVERGENCE-COUNT GAP PRODUCED THREE WRONG RECONCILIATIONS, AND PM SHIPPED THE THIRD BEFORE CATCHING IT.** (1) cap-based: the gap is the departure cap. (2) cap-based, inverting (1): *"DE's sweep already applied the cap"*, difference relocated to a churn axis and a bound. (3) populations: both are wrong because they are cap-based, since *"a capped population cannot reach 222 — `a < b` with `b ≤ 2` forces `p ≤ 3` at any range."*

  **(3) reached PM as a settled correction from the orchestration layer, and PM rewrote the Background on it before checking.** It is a **UNIT ERROR**: `p ≤ 3` bounds the pre-load *roster size*, not the number of swept parameter *combinations* realising those shapes, since new-id count, churn count and live size still range freely. **222 counts combinations; 3 counts shapes.** The artifact-of-bounds account — the ORIGINAL one — survives, independently re-derived by a code-reviewer as `c(n) = (3n−2)(n−1)/2` and **re-checked by PM against all four of DE's figures: 15/26/100/222 at n = 4/5/9/13, four for four.** Reverted; the surviving discipline from (3) is only *name which divergence you count*.

  **Three findings, in ascending order of transferability.** First: **arriving as the correction of a wrong claim is not evidence** — each account was accepted because its predecessor was known-bad, and `.claude/rules/tool-output-integrity.md` already records that the relief of catching an error is what stops the replacement being checked. Second: **this particular gap is unusually good at generating confident wrong explanations**, because both numbers are real, both authors competent, and any story linking them sounds like insight — three iterations makes that a property of the material, not a run of bad luck. Third and cheapest: **the check that beat all three was fitting the reported figures to a curve** — four numbers, one formula, one minute, and it sat undone for three rounds while three method-level arguments were constructed instead. **Reconciling two measurements by reasoning about their METHODS is far more error-prone than testing whether one arithmetic relation reproduces the reported values.**

  **PM's own exposure, recorded rather than elided**: PM had written *"a reconciliation is a claim, and arriving as the correction of a wrong claim is not evidence for it"* into this record about someone else's error, and then implemented one — in the same session, hours apart. The sentence was true and did not fire, because the incoming claim came from the orchestration layer as settled. That is the **eighth mechanism** (provenance as authority) firing at the maximum-deference position again, with the trigger already written down.

  **PROCESS FINDING, and it is the reason several of the above were caught late: the successor's brief was generated from a handoff artifact that kept growing after the brief was sent.** `.project/research/E-276-triage-handoff.md` gained a second reviewer's entire audit (7 MUST / 7 SHOULD, including the roster-lock counterexample) plus a set of falsifier verdicts, *after* the brief was built from its header. The brief was internally consistent, confidently framed, and materially incomplete — and PM only found the newer sections while running an unrelated consistency grep. Exactly the case `.claude/rules/tool-output-integrity.md` records: **a handoff is a claim with a timestamp, and a prompt generated from it does not update when it does.** The mechanical fix is cheap and belongs to whoever generates such a brief: **re-issue BOTH the artifact and any prompt built from it**, and, on the receiving side, **check the artifact's mtime against your own start before treating it as the state.**

  **A count shipped INSIDE the correction of a count, and it is the sharpest single item that round produced.** TN-13 exists to record that "72 existing reconcile tests" was inherited and never measured. Its correction then asserted *"19 tests with 13 direct `crawl_is_authoritative` calls"*. Re-measured at final triage: **the 13 is wrong — there are 7 call sites across 6 test functions**, and no reading recovers 13 (executions counting the parametrize expansion are 9). The other three counts in that note — the 9 helper call sites with their per-file split, the 72 as 34 + 20 + 18, and the 19 — **all verified correct**. So the note is now right in four places and was wrong in the one it added while fixing itself. **The same defect, one paragraph below its own diagnosis, committed by the party writing the diagnosis**, sitting beside a `[PM-VERIFIED by grep]` tag. Two carry-forwards: `def test_` counts and COLLECTION counts diverge wherever `parametrize` appears (30 vs 34, 17 vs 19 here) — **a reviewer counting `def test_` would have "refuted" two correct claims and still missed the wrong one**, so a count's UNIT is as load-bearing as its space; and **correcting a count is precisely when a fresh one gets asserted unchecked**, because attention is on the claim being retired.

  **The which-gate-refused AC was drafted on a false premise and reading the source inverted it — the more useful outcome.** PM wrote it as an *addition*, assuming the refusal messages were undifferentiated. They are not: all three grains already emit a comparable/prior/floor triple, and `retire_absent_games` carries an explicit comment stating its three causes are *"named apart"* because *"the remedies differ."* **The requirement is therefore PRESERVATION of an existing deliberate property that the change degrades silently** — one unlabelled triple can no longer say which of two mechanisms it reports, and the comment asserting exhaustiveness stays on the page still reading as though it holds. Two concrete losses: the player-line grain's `18 of 18` is the exact healthy-looking message that hid this bug (the corrected gate reads `0 of 9` on the same input), and roster's `roster_db_count` is **the tell the original audit used**. Two prose sites were added to TN-9 off the back of it.

  **The transferable form, and the reason this is recorded rather than just fixed**: adding a mechanism to a guarded path silently degrades every message and comment that enumerated the old mechanisms. Nothing fails; the enumeration merely stops being exhaustive. It is this epic's own class — an accurate claim falsified by a change elsewhere — arriving inside the remediation for it. And **"we are adding a refuser" is a searchable trigger** in a way "some prose may have gone stale" is not. Found only because a PM-authored AC was checked against the source before shipping, which is the author-owned-claims discipline pointed at PM's own output.

  **One reclassification worth its own line, because it changes what story 05 hands to claude-architect.** The three-instance pattern is now **two** instances plus one prior case: `crawl_is_authoritative`'s docstring is a **stale contract**, and `.claude/rules/python-style.md` already carries that class *and* the sweep it prescribes. Counting it as a third instance of a mechanism we have no rule for would have invited a second rule for a class already written down — the context layer paying twice. Stated at the weaker strength this leaves ("two, plus one already covered") rather than preserved at three, and this is the **fifth** bounding of a blanket claim on this surface, consistent with the standing prior that unbounded rules here need boundaries more often than retractions.

- **The orchestration layer's own failures, recorded at the main session's explicit instruction and in the same register as the agents'.** An epic whose thesis is *a claim propagates unverified and survives review* cannot omit the layer through which claims propagated unverified — that would be a document true in every sentence and false in its shape, which is the same species as the invariant that was true-as-written and false-in-effect. Attributed to the **role**, not a personality, because the finding is structural:
  1. **Four positions relayed to PM as settled that were not** — the intersection as fail-closed insurance; the intersection withdrawn entirely; SE's joint recommendation as authoritative; and the pre-conjunction neutrality scoping. Each was retracted within the hour.
  2. **A false claim authored by the relay layer** and written into TN-9 as this epic's worked example of a rotten citation — that the handoff carried a stale symbol name. Restated confidently three times, never resolved against the repo, and caught by DE rather than by its author.
  3. **Stale story-numbering shorthand carried the whole session** (02/03 for what are 03/05), which would have misrouted dispatch had PM not caught it.
  4. **The generalization**, DE's and applying to the relay layer more than to DE: *verification that does not include the claim's baseline or scope is not verification.* Four positions were relayed without checking what any was measured against. DE's sharper form: **the checker was less rigorous than the checked.**

  **A relay layer is a claim-propagation surface with no verification step** — structurally the same defect as a gate that measures the wrong population: it passes things through while appearing to check them.

  **⚠️ A SEVENTH INSTANCE, the worst of them, and the ONLY ONE WITH A STRUCTURAL FIX.** The relay layer asserted the *contents of a file it has never read* — twice in one message, with emphasis, and built an instruction on top of it: *"the defective §6 prose is in `epic.md`; repair it there first."* It was not. The inference was *"PM inlined the reconciliation"* → *"§6's prose is in the epic"*, promoted to a fact and delivered as the headline. **PM inlined §6's CONCLUSIONS, never its prose**, and a grep settled it in one call.

  **The predicted failure had it been followed**: go looking for text that is not there, then either doubt the grep or **paste the defective sentence in so it can be repaired — manufacturing the defect in the act of fixing it.**

  **The actionable form is not "check more carefully", and that distinction is the whole value of this instance** [relay layer, on itself]: *"I have no channel to check file contents in this role at all. Therefore I must not assert them. Route it or attribute it, every time."* **A prohibition on asserting what you structurally cannot verify is enforceable; an instruction to be careful about it is not.** Every other entry in this record asks someone to check something they could have checked. This one identifies a party that could not.

  **And the shape it arrived in is the epic's own, at its sharpest**: the message's *verdict* was correct — the "last place" sentence WAS in the epic and WAS false — **riding a fabricated premise about where the defect lived.** A right answer is not evidence the reason was right, which is precisely why checking the premise rather than acting on the conclusion was the correct response. **It landed inside the epic about unverified claims, in a message directing someone else to fix an unverified claim.**

  **And the actionable half, which belongs beside it:** the mechanism that caught all of this was the standing instruction to **treat every settled item as provisional until READY**. That is what kept the gate sections stubbed through two rounds of "this is settled" that were not. The relay failures were real and **none of them hardened**.

- **Review design changed as a result, not merely a lesson recorded.** Across this session three checks were applied to claims — the relay layer's, DE re-deriving SE's numbers, and each agent re-measuring its own work. **Only the self-check caught anything**; both failures were an agent checking *someone else's* claim. DE's mechanism: *reviewing another agent's claim is where the frame goes unexamined, precisely because the claim arrives already framed.* Since CR-spec-audit-plus-holistic-team is exactly that configuration, internal review now runs in **two passes**: a **self-audit** (each contributor re-checks its own claims, fixtures and comparisons for **frame and baseline**, not internal consistency), then the cross-review, with reviewers instructed to **attack the frame before the arithmetic** — *computation right, frame wrong, frame was the claim.* The self-audit is expected to be the higher-yield pass, which is itself the finding.

- **The strongest claim for the self-audit: the rule catches errors its own authors still commit.** Two instances, one from each agent, both *after* the rule was written down. DE asserted a boundary's sufficiency it had not tested. **SE executed DE's boundary arithmetic, confirmed it, then wrote "the scope ruling stands" — importing a scope conclusion it never tested — while holding the falsifying mechanism in the same message**, filed as a fixture-design note rather than as a limit on the scope argument. Verified the internals, inherited the frame, counterexample already written down and misfiled: one message after confirming, two after naming the rule. That is a stronger argument for the check than "it caught someone being careless."

- **PM was exposed to the same over-claim and escaped by sequence, not by judgment.** The scope justification needed a reachability boundary, so PM would have written DE's version — the only thing in between was DE executing its own weakest claim first. **That is the self-audit working upstream of the consumer**, not luck, and it is the argument for running self-audit before cross-review rather than after.

- **The self-audit pass justified itself empirically, and the proof came from the agent who proposed it, about its own wrong claim.** DE, verbatim: *"It would not have been found by re-reading — I had re-read that sentence three times while writing it."* That is the evidence for the **re-execution** form of self-audit over the re-reading form. Two further findings from the same pass: **both claims DE flagged as its own weakest were wrong**; and the one that mattered — a safety property about a failure path, the fifth instance of this defect class in a single session — survived four exchanges **because it sat beside executed results and read as reasonable.** Proximity to evidence is not evidence.

- **A third review technique that re-reading cannot replace: TRY TO SATISFY THE CRITERION, then check the target exists.** That is how the review's most consequential finding (TN-17 — seven ACs mandating an unreachable assertion target) was found. DE's account: *"It did not come from reading the ACs, which are well-written and internally coherent. It came from asking what would happen if I tried to satisfy this one, then checking whether the named target exists."* The ACs were not wrong about **what** to assert; they were wrong about what is **reachable**, and no amount of re-reading surfaces that. Sits alongside the self-audit's re-execution rule and the frame-before-arithmetic rule as the third technique this session that found something reading could not.

- **EIGHTH MECHANISM — PROVENANCE AS AUTHORITY** [joint DE/SE: **SE's** account of why it skipped a check, **DE** naming the mechanism and its inverse-pairing, **SE** supplying the trigger]. **The one with predictive power, and it subsumes six earlier entries.**

  > **A claim about X from the owner of X is the least-checked kind, precisely because ownership reads as having already done the checking.**

  The instance: SE relayed DE's phrasing about a number as an epic defect **without opening the epic** — its own account being that the check was one command, it had grepped that file twice that hour, and it skipped it *because the claim came from the party who owned the number.* (The epic never had the defect.)

  **Distinct from the seven already catalogued, and the distinction is worth preserving.** The fifth host is a summary carrying a truth-value past its qualification; the relay failures were frames arriving pre-framed. This is neither — the claim was correctly framed and correctly summarised. It was simply **not checked, because its source was the party with the best reason to have checked it already.**

  **It is the exact inverse of the rule recorded above.** We wrote *"only its author reliably catches it."* The other half is *"and everyone else defers to the author, which is why nobody catches it sooner."* Together:

  > **Author-owned claims are simultaneously the most reliable and the least checked — so a single error in one propagates further than an error anywhere else.**

  **The TRIGGER — a tripwire, not a policy** [SE]:

  > **Check the claims where ownership is doing the work** — not every claim from an author about their own artefact, specifically the ones you are accepting *because* they own it. **If the reason you did not verify is "they would know," that is the verification trigger, not a substitute for it.**

  **A first draft of this said "the owner's claim about their own artefact still gets the one-command check." That form was withdrawn by its own author** and is recorded here only so it is not revived: it degenerates into check-everything, and since deference to whoever did the work is *normally correct*, a rule that fires constantly gets ignored inside a day. The trigger above fires rarely, which is the point — this needs a tell, not a rule.

  **All three instances carry the tell exactly** (the two catalogued here, plus this entry's own drafting below): SE skipped a one-command grep on a file it had already grepped twice, with nothing behind the claim but DE's ownership of the number; DE wrote "your 222" because attribution felt like bookkeeping beside the arithmetic it had just finished; and the relay of this very mechanism went unchecked because it came from the party who had just proposed it.

  **Why the trigger matters more here than for the other seven** [DE]: an entry naming a mechanism with an unusable trigger **is the shape that gets quoted later minus the part that does anything** — the fifth host applied to our own record of the fifth host. And this is the only one of the eight that *predicts* rather than explains, so its trigger carries more weight than the others'.

  **It reframes the orchestration entries.** Every position the relay layer passed on as settled came from its owner — each agent about the thing it had just built. Ownership was read as verification. **The relay layer is the maximum-deference position in this team structure, which is why the mechanism fired hardest there: not six separate lapses of care, but one mechanism firing six times at the point in the workflow where it fires hardest.** More accurate and more useful than six confessions.

  **The sixth instance is this entry's own drafting**, and it belongs here rather than as a separate item: the message directing that this mechanism be recorded **itself relayed a withdrawn trigger and a wrong owner**, unchecked, because both came from the party who had just proposed them. A demonstration of the trigger's necessity produced while transmitting the trigger.

  **And it predicts where the next one lands**, which none of the other seven do: not in a relay or a summary, but in whatever any of us says about our own work that a colleague then repeats. **No instance was carelessness** — every party was being appropriately deferential to whoever had done the work, which is normally correct behaviour and exactly what made it invisible to the usual checks.

- **SEVENTH HOST, and the one most likely to recur here: a quantity asserted over a space the author CHOSE, then reported as though the space were given.** DE's naming of its own error: *"The count was correct; the frame was mine and went unstated."* Same family as computation-right / frame-wrong, but the frame is a **sweep bound** rather than a baseline. Both competing divergence counts had this shape — DE's 222 over `0..12`, SE's ≈20 over `0..7`, both enumerating the same three shapes. **(A final-triage round briefly "corrected" this clause to say they measured different populations; that correction was itself wrong and has been reverted — see the divergence-count record above, where the artifact-of-bounds account is confirmed by a four-point exact formula fit.)** **This matters disproportionately in this codebase because parameter sweeps are how nearly everything here was established today** (2197, 6560, 862, 179, 4207). **Standing consequence: every count in this epic carries its space, or it is not a count.**

- **An executed figure travels faster than its provenance — and only its author reliably catches it.** Two instances, with the locus corrected after both authors re-checked: **DE's number travelled one hop and acquired a wrong owner; SE's report of DE's number travelled one hop and acquired a wrong location.** (Only the first was ever a real epic defect — F1. The second was a flag raised against the epic that turned out not to apply; the attribution in the text was already correct.) In a multi-agent thread a number acquires the authority of "executed" while its origin drops away. Cheap to catch at authoring time, expensive at review — and see the eighth mechanism above for *why* nobody else catches it.

  **A second wrong explanation was caught in the same exchange and is worth its own line**, because it is the kind that survives: SE diagnosed the 222-vs-20 gap as the departure cap, having computed gate-verdict versus deletion-outcome divergence **inside its own parameter space** and presented that as confirmation of a hypothesis about **DE's** method. It confirmed only that SE's two metrics differ. **Inferring another agent's method from a comparison run entirely in your own space** is the seventh host and the eighth mechanism compounding — a frame of one's own choosing, applied to someone else's work, deferred to because it came from a party who had executed something.

- **Blanket rules on this surface have needed BOUNDING roughly as often as correcting — four times.** The deletion-neutrality absolute, the precondition-completeness claim, PM's three-instance pattern generalization, and the anti-WARN assertion steer (which genuinely inverts at the e2e level). In every case the fix was a **stated scope**, not a retraction. Worth carrying as a prior: on this material, an unbounded rule is more likely to need a boundary than to be wrong. **Amended at final triage — now FIVE**: the reachability boundary needed a direction scope (over-deletion only), and the three-instance pattern needed one instance reclassified out. The prior held both times, which is the point of recording it.

- **A true claim was nearly discarded because its stated support was false** — the sixth host, and it landed in the sentence nominating itself as this epic's strongest evidence. The two-independent-derivations claim is sound, but earlier drafts credited SE-2 with a combination sweep it never ran (the 862-of-6560 figure is SE's; SE-2's route was purely mechanistic). A reader checking the citation would have found the attribution false and could reasonably have discarded the convergence as inflated. **SE caught it only by recognising the numbers as its own.** Fixed by re-attributing SE-2's half to its actual evidence and stating the two *methods* — reproducing a failure versus breaking a premise — since the differing method is what makes the convergence load-bearing at all.

- **The defect class found a fifth host: not the claim, but the SUMMARY of the claim.** SE's "the only surviving hole" was accurate when written, became false when DE's contest arrived, and was allowed to stand in a later summary without re-checking. A summary inherits the truth-value of the moment it was written and carries it forward silently.

- **The fifth host recurred within the hour, under active guard — which makes it COMMON rather than exotic.** A relay of SE's boundary execution reached DE framed as *"confirmed — the scope ruling stands on executed evidence."* SE had tested only whether the boundary predicts corrected-gate **refusal** (the necessary half, never retracted); what DE retracted was **sufficiency for divergence**, which additionally requires the legacy gate and the cap to permit, and SE's sweep varies neither. Applied to the epic, that summary would have **reverted an already-corrected paragraph** to the over-claimed form.

  Three things make this worth more than the correction. **The packaging is the mechanism**: it arrived *already framed as a confirmation*, which is what stops a frame being checked. **It recurred within an hour of the host being named, in a relay of the work of the agent who named it** — a failure mode that repeats under active guard is a different finding from one observed once. And **the epic was never exposed only by luck**: the relay went to DE and SE, not to PM, who had received the corrected two-condition form. Recorded as luck rather than containment-by-design, because implying otherwise would teach the wrong lesson.

  That is now **six relay failures from the orchestration layer, all the same shape** — see the eighth mechanism, which explains why they concentrate there rather than being six independent lapses. The sixth was committed while transmitting the mechanism itself.

- **Two independently-derived lists agreeing is verification; one re-read was not.** SE flagged its own IN/OUT completeness pass as weakly verified — a re-read of its own artefact against its own memory — and PM had used that list as authority to delete rather than reconcile. DE then rebuilt the list independently from its own record without consulting SE's: **16 withdrawn items (11 DE's, 4 SE's, 1 from the relay layer), every one appearing only in the prohibition banner, an explicitly-refuted account, or a History retraction. Zero live uses, no accepted-but-absent findings.** The question is closed properly rather than plausibly.

- **DE's retro, in its own words, at its request:** *"The health-gate safety sentence was wrong three times before it was right, and every failure had the same shape: a true-sounding absolute asserted uniformly across the three grains without checking the one grain whose writers violate its premise. I made that error twice — a neutrality proof resting on `W ⊆ fresh`, which the roster jersey backfill contradicts by design, and an earlier 'strictly narrower' claim resting on the same premise — and SE made it once. In each case the claim was retired only when someone built the counterexample rather than re-reading the argument. This is the epic's own defect class, committed inside the acceptance criterion written to prevent it. The binding conclusion: any AC phrased as an absolute about deletions gets a counterexample attempt before it ships."*

- **The transferable result of this planning session, in SE's words: two of DE's errors and one of SE's own were caught by EXECUTING the roster grain specifically; none were caught by reasoning.** That is worth more than any single acceptance criterion here, and it is the evidentiary case for the scope ruling that put the roster grain in — a grain added because the operator asked us to check it, which then produced every correction that mattered.

- **Retro worth keeping — execution beat derivation three times on this surface, and twice it was the deriver's own claim that fell.** (1) SE reproduced the roster deadlock that a uniform narrowing would have caused; chasing it led DE to read the redirect site and discover its *own* motivating premise was empty. (2) SE-2 executed 862 of 6560 violating combinations against a deletion-neutrality claim three of us had accepted, including as a first-class AC. (3) DE retracted its "unreachable" assessment of the TN-15 residual after PM invoked the annotation-is-a-hypothesis rule, having read only the tolerant guard and not the branch beneath it. Both of the second and third findings were reachable **only because the roster grain was in scope** — the evidentiary case for that scope ruling, and for preferring execution over derivation here.

- **A false claim reached the epic document and was corrected in place** (TN-9). PM inherited a claim that the originating handoff carried a stale symbol name, restated it without resolving it against the repo, and wrote it into an epic whose subject is exactly that failure. All three roster symbols are real and distinct; PM verified by grep and read. Recorded rather than quietly fixed, because it is the best specimen the epic produced and it is why every load-bearing claim there is tagged EXECUTED / DERIVED / PM-VERIFIED.

---

## Addendum — findings produced during assembly, after the extraction was specified

- **A fresh instance of the markup/whitespace grep-narrowing class, found in one command during the story-03 rewrite.** A literal grep for `grid clutter` against `src/` returned **zero matches** for a phrase that is present at `src/db/reconcile_at_load.py:1217-1219` — the phrase wraps across a line break (`grid\n    clutter`). That is the third catalogued member of the class (line breaks), arriving live during assembly rather than during review. It is a *characterized* member, so it does **not** close the OPEN unexplained thread (a literal grep missing a plain unemphasized string, cause never isolated); it is independent evidence that the *"an unexpected count is a cross-check trigger, never a finding"* reflex is load-bearing in ordinary work, not only under adversarial review. The unexpected zero was treated as a cross-check trigger and the pattern re-run partially, which found it.

  **What it cost had it not been cross-checked**: the sentence is `retire_departed_roster_players`' docstring claim that this grain's failure mode is *"grid clutter, never a corrupted stat"* — the sentence the operator's prefer-delete ruling rests on, and one the shipped roster design falsifies in the band régime. A false-clean would have shipped an epic that misses its own most load-bearing prose site.

---

## Addendum 2 — findings from the post-READY amendment and the four Codex passes (2026-07-25)

These post-date the extraction and the first addendum. Two are new mechanisms; two are method notes on the sweep instruments themselves.

- **🔧 THE TECHNIQUE THAT FINDS BOTH CLASSES: enumerate outward from the CITED artifact and grep consumers for ITS OWN IDENTIFIER. [CR-3's method lesson — a checkable procedure, not a disposition.]**

  **Forward enumeration — walk each consumer, collect what it cites — structurally cannot surface *"TN-19 claims story 03 cites it, and story 03 does not."*** The consumer has no pointer to follow, so there is nothing to walk. **Only walking backward from TN-19 and grepping consumers for the string `TN-19` finds it.** This is the mechanism behind the 47-vs-78 enumeration gap, and it is **also why an author cannot find this class in their own work**: you enumerate the links you built, and the defect is a link you believed you built.

  **⚠️ PRECISION THE SINGLE FRAMING LOSES — the gap has TWO shapes needing two different moves, and *"enumerate backward"* only covers one:**

  | | **IDEA-189 shape — the edge is ABSENT** | **TN-19 shape — the edge is PRESENT and false** |
  |---|---|---|
  | State | nothing in the epic points to IDEA-189 at all | TN-19 *does* name story 03 AC-6/AC-7 |
  | Why forward enumeration misses it | there is no link to walk | — it does **not** miss it; the edge was **on the list** |
  | What actually failed | **discovery** | **verification** |
  | The move | enumerate the **target population** (all ideas) and grep each for the source's identifier (`E-276`) | grep the **named consumer** for the source's identifier (`TN-19`), rather than checking the named target's *content* |

  **The second row is the one worth carrying, because it is the less obvious failure.** The fifth-pass walk **did** enumerate `TN-19 → 03 AC-6, AC-7` as a backward edge. It then verified it by confirming *the named ACs exist and carry their requirements* — which they do — and marked ✅. **Enumerating an edge in the backward direction is not the same as verifying it in the backward direction.** The verification test is not *"does the target say something sensible?"* but **"does the target contain the source's name?"** — a one-line grep that no amount of reading the target's content substitutes for.

  **⛔ AND THERE IS A THIRD CLASS NO GREP REACHES [CR-3, and it is why F4 did not close on the first repair].** The two above are about whether the *link* is real. The third is about whether the *claim* is:

  | | edge **ABSENT** | edge **PRESENT, citation false** | edge **PRESENT, name resolves, claim about CONTENT false** |
  |---|---|---|---|
  | instance | IDEA-189 | TN-19 | **F4 — the DE file's paragraph position** |
  | what failed | **discovery** | **verification** | **neither grep** |
  | the move | grep the target population for the source's id | grep the named consumer for the source's id | **open the target and count** |

  **In F4 every name resolves.** Story 05 AC-9 names the DE memory file, IDEA-187 names it, the README row names it — and all three said its per-grain paragraph sat *"at the bottom"* / *"in the last third"* when it is at **line 21 of 35**. **No grep of any kind settles that.** So the standing procedure is **two greps AND a read**: grep the target population for the source's identifier (discovery); grep each named consumer for the citing note's identifier (verification); **open the target for any claim about its content.** All three are mechanical, all three are cheap, and **none is what a careful read of the citing document produces.**

  **⚠️ THE REPAIR SHAPES DIFFER, AND THAT IS THE OPERATIVE HALF:**

  > **A citation defect is fixed by ONE EDIT AT ONE END. A content-claim defect is fixed by a SWEEP** — because the claim propagates into every artifact derived from the first: index rows, summaries, and the entry stating the actionable edit.

  **F4 did not close because it was repaired as if it were a citation defect** — the two artifacts named in the finding were corrected and three derived copies were left standing, including the ideas **index row** and the Open-Questions entry **stating the edit the owning agent would actually make**. A subsequent sweep of the claim's *forms* found a **fourth** the re-verify had not named: story 05 AC-9's own correction note restated the retired position **one paragraph below its own correction**. **A finding tells you where it LOOKED, never where the claim LIVES.**

  **And "open the target" is necessary, not sufficient — demonstrated on this very finding.** CR-3 reported the file as **36 lines** when it is **35**: it *did* open the target, and took the length off a rendered view instead of counting. **The claim was about a count, and only counting settles a count.** By its own diagnosis, an instance of the third class committed inside the correction of the third class.

  **⚖️ THE STRONGEST CASE FOR KEEPING THREE CLASSES RATHER THAN FOLDING THIS INTO THE OTHER TWO** [CR-3's closing observation, and it comes from the only class that resisted three attempts to close]:

  > **F4 is the only one of the six findings that needed a DIFFERENT INSTRUMENT at re-verify than at discovery.** The other five were found by grep and confirmed by grep. **F4 was found by opening a file and counting; its first repair was verified by checking the named sites; and it closed only when the check became *"read every artifact derived from the claim."*** **Discovery instrument, repair shape, and verification instrument all differ for this class.**

  **That is why it is a third class and not a variant of the second.** A taxonomy that splits on *what went wrong* would fold F4 in with TN-19 — both are "a false statement in a citing document." **Splitting on *what finds it, what fixes it, and what confirms it* keeps them apart**, and those are the three questions a reviewer actually has to answer.

  **Companion log — the units trap arrived inside the count used to confirm this fix.** PM's **12** (occurrences, `-o`) and CR-3's **6** (matching lines, `count`) are **both correct for their unit**. It was disposed by **measuring both ways rather than reconciling by argument**, which is the disposal this epic already prescribes for an unexpected count. Second instance of the label trap in one session.

- **⛔ THE DANGEROUS FIX IS THE ONE THAT PASSES EVERY CHECK. [Route (b), pre-registered by CR-3 and sharpened in triage.]** A one-way citation admits two repairs: **(a)** add the citation to the consumer — closes the routing gap; **(b)** delete the source's claim that the consumer cites it — **makes the document self-consistent and leaves the relation exactly as broken as it was.**

  **CR-3 first framed (b) as "closes nothing"; the accurate form is stronger and it adopted it: (b) closes every CHECK while closing NOTHING.** That is what makes it the *dangerous* route rather than merely the weak one — **it is the fix a consistency sweep rewards**, and the one that discards the finding while producing a green result.

  **Generalizes past citations**: whenever a document asserts a relation that does not hold, **removing the assertion and repairing the relation are both "fixes" and only one of them is.** The tell is that the cheaper repair improves no reader's situation — under E-276's reading protocol, deleting TN-19's claim would have left the note exactly as unreachable from the only story touching `MAX_ROSTER_DEPARTURES`, with its promotion's stated purpose still unmet. **Same shape on F3: correcting the residual count closes it; re-scoping the sentence to make it true leaves a retired figure unmarked in a directory outside every sweep's scope.**

- **⛔ A ONE-WAY CITATION IS NOT AN EDGE — AND A "BOTH-ENDS" CHECK THAT WALKS ONE END IS INDISTINGUISHABLE FROM A PASSING ONE. [NEW MECHANISM — found by the independent enumeration, against an edge the first walk marked ✅.]**

  TN-19 stated *"Cited by story 03 AC-6 and AC-7."* **No story file contained the string `TN-19` at all** — five occurrences in `epic.md`, zero across all five stories. **Both ends were individually correct**: TN-19 named the ACs, and the ACs stated their requirements soundly. **The edge did not exist**, because citation is *directional* and only the outbound half was built.

  **Why the miss was structural rather than careless**: the fifth-pass edge-walk verified the SOURCE end — *does TN-19 name real ACs, and do they carry what it says?* — and marked ✅. It never asked the return question. **The evidence was already in that same walk's own output**: its Section 1.B listed story 03's citations as *TN-9, 11, 12, 13, 14, 16, 17*, with no TN-19. **Two sections of one report contradicted each other and the ✅ stood**, because a check that produces a green mark forecloses the re-check that would catch it. **An unverified edge is safer than a wrongly-verified one.**

  **The operational form**: for a citation edge, *"the target exists and carries what the source says"* is **half** the check. The other half is **does the target route back** — and it is the half that matters under a reading protocol, which this epic has: the guide tells an implementer to read their story and then *only the TNs it cites by number*. **Under that protocol an uncited TN is unreachable however clearly it names the story**, so a one-way citation silently converts a mandatory note into an invisible one. TN-19's own banner says it was promoted *"so a story and a future cap-tuner can cite it"* — **its stated purpose was unmet for the life of the note**, and it exists to stop exactly one error a cap-tuner makes.

  **Generalizes to any navigable reference under a "read only what you cite" protocol** — TN→story, doc→doc, rule→agent-definition. Three further instances of the same shape were found in the same pass (TN-1's roster-exemption box, TN-5's roster-premise subsection and its precondition-(e) box, all naming story 03's requirements from a note story 03 did not cite). **The sharpest: story 03 AC-2 restates TN-5's executed table inline without citing it** — the implementer had the numbers and no route to the derivation.

- **⛔ AGREEMENT BETWEEN AN ARTIFACT AND SOMETHING WRITTEN FROM IT IS NOT CORROBORATION. [Recurring — third instance.]** Story 05 AC-9 and IDEA-187 both placed a paragraph in the DE memory file's *"last third" / "bottom."* It is at **line 21 of 35**, with four substantive sections below. **They agreed because one was derived from the other**, and the reviewer who caught it simply opened the file and counted. Prior instances: the ideas index agreeing with its own idea file (both wrong), and the epic index agreeing with the extracted findings file. **The check is to open the primary, never to consult a summary of it** — and the tell is that two sources agree on a *detail neither needed*, which is the signature of copying rather than of independent observation.

- **⛔ A COUNT CAN BE FALSIFIED BY AN EDIT THAT NEVER TOUCHES IT. [NEW MECHANISM — the hardest shape in this epic.]** TN-13 asserted *"exactly one existing assertion inverts"*, which was **true for its space** (the fourth test file, the vacuous-permit inversion). The roster design's reversal then required a **second** existing-assertion change — in a **different story**, in a file TN-13 does not mention. **TN-13's sentence is now false and not one character of it changed.**

  **This is what makes it the hardest shape: no sweep finds it.** A term sweep finds modified or contradictory text; this text is unmodified and internally self-consistent. A structural-field sweep finds scope words in summaries; this is a numeral in a body paragraph that was correct when written. **The only detector is re-deriving the count against the current tree** — which nothing prompts you to do, because nothing looks wrong.

  **Distinguish it from the 7th host, which it resembles and is not.** The 7th host is *a quantity over a space the author chose, reported as though the space were given* — an error at the moment of writing. **This one is correct at the moment of writing and is invalidated remotely, by a change to the space's boundary made elsewhere, later, by someone else.** The 7th host's fix is "state your space"; that fix does **not** help here, because TN-13 *did* state its space and the space itself moved. **The fix is a standing re-derivation, not a better sentence.**

  **Generalizes past counts**: any claim whose truth depends on a *boundary defined elsewhere* — a count, an exhaustive enumeration, a "the only X", a file-list completeness claim — is exposed. Found by Codex across two independent passes; the epic-wide total is now tabulated in Success Criterion 2 rather than inferred from any single note.

- **⛔ A VERIFIED REPAIR VERIFIES WHAT IT WAS POINTED AT. Enumerations drift DOWNWARD-ONLY. [NEW MECHANISM.]** The `refused_by` enum was failed by CR-2 for non-exhaustiveness, repaired by SE (adding `boxscores_incomplete`), and the repair was **verified by CR-2**. It was **correct and incomplete**: a second member, `skipped_no_exemption_plan`, was introduced in a *later subsection of the same Technical Note* and never propagated up to the list. Story 03 then named a third, different set. **Three sources, three sets, on the surface an implementer builds from — surviving a failed review, a repair, and a verification of that repair.**

  **The mechanism: the site that ADDS a member is rarely the site that LISTS them.** New members enter at the point of *use* — a requirement, a wrapper instruction, a story AC — and lists live upstream. So enumerations leak in one direction, and **a review that verifies a repair is scoped to the defect it was shown**, not to the class. "Was this fixed?" gets yes and stops.

  **The mechanical fix that also produced a real finding**: replacing the flat list with a **per-grain membership table** surfaced two facts nobody had stated — **`refused_by == "gate"` is unreachable on roster** (no floor gate under V1) and **`"cap"` is unreachable on player-line** (no cap). A flat list had been silently inviting tests that assert states the code cannot produce. **The table was adopted to fix a consistency defect and paid for itself as a correctness check.**

- **METHOD — the emphasis hazard INVERTS onto the WRITE side: markup interpolation defeating an exact-match EDIT, not a search.** Every prior instance of this class was a *read* failing to find. At the fifth-pass edge-walk an `Edit` was **rejected outright** because the `old_string` reproduced a phrase as `**bolded**` where the file carries it plain. **Same hazard, opposite surface, and the failure mode is better**: a search fails *silently* toward false-clean, while a write fails *loudly* and refuses to land. **The write side is the safe direction of this bug** — which is worth knowing precisely because it means the read side is the one carrying the risk. Mitigation is unchanged and already standing: **read back the literal line after any failed edit, and copy from that read rather than from memory.**

- **⛔ METHOD — A DELIBERATE EXCLUSION ADDED FOR PRECISION PRODUCED A FALSE CLEAN. The worst member of the grep-narrowing class, and it arrived inside the check written to close that class.** [Self-reported by CR-3, cause and all.]

  A sweep for surviving positional claims reported **ZERO surviving** and was wrong. Its pattern carried `[^|]{0,200}` between the two halves of the claim — **an exclusion added to keep matches inside a table cell.** This epic's prose uses `|` freely, so the pattern **silently stopped matching across any sentence containing a pipe** — which is exactly where the missed copy (`E-276-05:91`) sat. **The clean was a property of the pattern, not of the tree.**

  **Why this is categorically worse than the other members, and the distinction is the whole entry**: markup interpolation, case, hyphenation, line breaks and inflection all narrow a pattern **BY ACCIDENT** — the author did not know the text contained them. **This one narrowed BY CONSTRUCTION. The exclusion was added to make the pattern MORE PRECISE, and the precision is what produced the false clean.** So the usual instinct — *tighten the pattern to cut noise* — is the instinct that causes it, and a reviewer doing careful work is more exposed than a careless one.

  **⚠️ It also had the missed line in its own earlier tool output, under a different grep, and did not open it.** Two failures, both volunteered.

  **The remedy is the operational half and it inverts the instinct: simple UNANCHORED patterns, ONE TERM PER INVOCATION, NO EXCLUSIONS.** Accept the noise and read the hits. Run that way, the same sweep returned zero live copies against nine preservation copies — **each classified by reading the surrounding prose rather than by the match** — four out-of-scope exclusions checked and stated, and nine further phrasings never previously tried.

  *(Catalogued as a member without an ordinal, per the standing decision three bullets down: three enumerations of this class disagree on its size, and asserting a count for a member of a class about unverified counts is the seventh host in miniature.)*

  **AND THE SECOND HALF, in its author's words, because it lands on the same finding:**

  > **"I reported a number as a property of the claim when it was a property of my sweep"** — the seventh host, **inside the finding raised about unverified claims.**

  **That both parties undercounted from OPPOSITE directions — the reviewer's three, PM's four — is the argument for sweeping FORMS rather than SITES, and it is now demonstrated twice rather than asserted.** Neither count was a fact about the tree; each was a fact about a search. **Two independent undercounts of one claim is stronger evidence for the method than either party's correction of the other.**

- **METHOD — word INFLECTION is a further member of the grep-narrowing class, and the ordinal is deliberately not stated.** A verification grep for ``permit iff `P >= X + g` `` returned **zero** against live text reading ``permit**s** iff``. Re-searching on the formula alone returned 8 hits across two files. **The miss was against text the searcher had written minutes earlier**, which is the sharpest available demonstration that authorship confers no protection.

  **On the ordinal**: the banner calls this class *markup/whitespace/hyphenation*, Addendum 1 calls line breaks *"the third catalogued member"*, and the amendment relay called inflection *"a sixth"*. **Those enumerations disagree, and this record declines to pick one** — asserting an unverified ordinal for a member of a class about unverified counts is the 7th host in miniature. **Catalogue the member; the count of members is itself a quantity over a space nobody has fixed.**

- **METHOD — the tool label says "occurrences" and the value is LINES. Two agents misled by it in one session.** `output_mode=count` returns **matching lines**; `-o` returns **matches**. On one identical pattern this session the same sweep read **19** and **33**. PM4 hit it on the pre-amendment sweep and PM5 hit it again on the post-amendment one. **Two independent agents, same tool, same misreading, hours apart — that is a property of the label, not of either agent.**

  **The operational consequence is not the discrepancy, it is the false movement it manufactures**: a sweep re-run in the other mode looks like the tree changed when nothing did — and this epic has already recorded (twice) that *an unexpected count is a cross-check trigger, never a finding*. **Compare like with like, or the instrument reports a delta the artifact does not have.**

- **⚙️ PROCESS — a review run CONCURRENTLY with fixes to its own subject reports citations that are already stale. [Gate-sequencing defect; disclosed by the party who caused it.]** Audit 4 was launched in the background while audit 3's findings were being routed and applied, so it read the epic **mid-edit**. Its line citations rotted before it reported — one cited site resolved to a *different* Success Criterion in the tree by the time the findings were triaged.

  **The attribution matters more than the incident, which cost nothing here.** The obvious filing is "the reviewer cited stale lines", and that is **wrong in the most expensive direction**: it tunes the instrument to compensate for a scheduling choice, and the next such report gets discounted for a fault it does not have. **This is a sequencing property of how the gate was run, not a property of the review.**

  **Remedy is sequencing, not review design**: freeze the artifact for the duration of a gate, or run the gate serially against a known revision. **Corollary that saved this one**: the standing *verify-by-content-not-by-line-number* rule absorbed the whole cost — every citation was confirmed by reading, so a stale pointer degraded to a minor inconvenience rather than a wrong disposition. **The rule was written for line-number rot from ordinary editing; it turns out to cover concurrent-edit rot too, which nobody designed it for.**

  **And it is the reason-rots-independently-of-the-verdict shape once more.** PM's first account — *"audit 4 ran against the pre-amendment file"* — was **true, and named no mechanism**. The real reason was recoverable only from the party who ran the gate; **no amount of checking the verdict would have produced it.** A verdict that is right for an unstated reason is not self-correcting, because every check of it passes.

  **⛔ AND THIS BOUNDS WHAT ARTIFACT REVIEW CAN DO AT ALL — record it beside G2's limit, because the two complete each other.** This epic now has a three-level hierarchy of what a review reaches:

  | Review shape | Finds | Blind to |
  |---|---|---|
  | **Single-file sweep** (term, structural) | defects *within* an artifact — stale text, contradictions, scope words | a gap annotated in one file and depended on in another |
  | **Edge-walk** (the fifth pass) | defects in the *relations between* artifacts — G2's class, *"unreachable to any single-file sweep"* | defects in the relation between an artifact and the **process that produced it** |
  | **— none —** | | **the gate-sequencing defect above: the mechanism lived in HOW THE GATE WAS RUN, which appears in no file** |

  **Every reviewer with access to every artifact would have read *"audit 4 ran against the pre-amendment file"*, found it true, and stopped.** The record was complete, self-consistent, and silent about its own cause.

  **The only channel that recovers this class is THE ACTOR VOLUNTEERING IT.** That is not a review technique and cannot be made into one — which is why the disclosure norm this epic keeps recording as an *outcome* (SE's §12, the four downward credit corrections, this one) is **load-bearing infrastructure rather than good manners.** It is the sole detector for a class of defect no amount of reviewing can reach.

  **Corollary for scoping future passes**: commissioning another artifact review can never close this class, so *"we ran one more pass"* is not evidence against it. **Ask the actors what they did, or it stays invisible.**

- **STATED AS A LIMIT, because the four passes bound differently than they read.** Four Codex passes returned **7 findings, 0 dismissed** — a clean record that is easy to over-read. **Two of the seven were found only because a human-directed sweep went looking beyond the cited site** (the Success Criterion, and TN-9's third dependent), and **one overturned a PM fix from the previous pass**. So the passes are not independent of the triage that followed them: **the pass supplies the pointer, the sweep supplies the extent, and reporting the pair as one number would credit the pointer with the extent.**
