# Opus 5 + Sonnet 5 self-read findings (condensed from session transcript)

Companion to model-behavior-reference-v2.md. The two Claude reviews' passage-level
inventories, preserved for the CA layer pass. Reviewer models verified via
transcript message.model (opus review: claude-opus-5, 29 turns).

## Opus 5 self-read — passages it would OVER-COMPLY with (prune candidates, ranked)

1. `doc-sweep.md` "The Discipline" (4 mandatory passes) + unconditional gate
   ("ran only step 1 = report as a gap"): would run the full sweep on a one-line
   typo fix. NOTE the mid-section split: the retired-claims TAXONOMY is truth
   (keep); the mandatory PROCEDURE is behavior (dose per model).
2. `testing.md` __pycache__ mutation protocol: verification-of-verification,
   would run all of it unrequested. The underlying FACT ((mtime,size) pyc
   invalidation) is non-derivable — keep the fact, dose the protocol.
3. `tool-output-integrity.md` response-protocol steps 2-3: the GATE is
   well-scoped (keep); the enumerated COMMAND MENU reads as a procedure —
   would cross-check clean reads defensively. Trim the menu, not the gate.
4. `CLAUDE.md` purge-scouting paragraph (~1,400 words single-paragraph
   rationale): density reads as a mandate to trace every clause before acting
   near purge/reconcile. "Legacy scaffolding in a different costume."
5. `workflow-discipline.md` gate stack duplications (intentional
   defense-in-depth): produces re-verification of already-checked gates. Low
   cost; flagged, not urged.
- NOT scaffolding despite appearance: `dispatch-pattern.md` concurrency
  advisory = a delegation cap, exactly what the Opus 5 vendor page prescribes.

## Opus 5 self-read — passages it GENUINELY NEEDS (never prune)

1. "A claim you RELAY is a claim you AUTHOR" — self-assessed accurate:
   "verification triggers on authorship, and relayed text doesn't feel
   authored"; inherited text enters as established context and self-
   verification runs downstream of it. Most load-bearing passage in the layer.
2. The two-cause read-disagreement differential — would misfile as "tool
   glitched"; MORE needed on Opus 5 (delegates more -> more concurrent writers).
   Keep the harness-note-is-primary-evidence sentence.
3. ugrep silent-empty quirk + Prohibition 3 (read-and-quote before ruling) —
   would reliably do it for defect reports, NOT for prose-staleness rulings
   (the E-270 bite).
4. `testing.md` piped-pytest exit codes + db-fixture deadlock (non-derivable).
5. `worktree-isolation.md` git checkout -- prohibition (counterintuitive
   staging semantics; encodes a fact — keep mechanism with the prohibition).
6. `agent-team-compliance.md` Patterns 1-3 (counterweight to documented scope
   expansion: answering for a named agent is the cheapest expansion).
7. doc-sweep retired-claims taxonomy CONTENT ("a rating derived from the claim
   sharing none of its words — no grep will find this") — genuine blind spot.

## Sonnet 5 self-read — key items

- Literalism bite: unprompted-vigilance duties (vision-signal capture,
  flag-to-PM) thin out silently on Sonnet consultations — "does not infer
  requests you didn't make." Consider explicit prompting in consult spawns.
- tool-output-integrity's worked examples risk UNDER-generalization on Sonnet
  (exact-pattern read as the trigger condition rather than one instance).
- Visibility warning: consultation Sonnet agents load ONLY layer + own
  definition — a shared-layer prune of relay discipline would remove it from
  their view entirely unless re-added per definition. Run the who-loads-this
  check on tool-output-integrity.md clause by clause before moving anything.
- Effort/frontmatter audit (Opus review G2): sonnet agents (baseball-coach,
  docs-writer, ux-designer) have NO effort field; five opus agents at high;
  api-scout medium. All aliases float (G1) — record dated alias→model
  resolution; re-tune trigger = vendor releases, not repo diffs.
- code-reviewer severity-floor already vendor-compliant; its "do not flag
  pre-existing code" scope line is in tension with the operator's no-
  pre-existing-excuse feedback — separate look, not part of the prune.
