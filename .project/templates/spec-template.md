<!-- NO REAL NAMES OR IDENTIFIERS — no person's name, ever; for teams, orgs, ids and UUIDs use the PII-Safe Placeholder Taxonomy in `.claude/rules/api-docs.md`. -->

# <Title>

**Date**: YYYY-MM-DD · **Status**: `COMPLETE (this commit)` · `PARKED + why` · `STUB` · `OPEN + what decision is owed` — or say it belongs to a chunk in flight.
**Source**: what asked for this — `.project/specs/README.md` NOW, a defect, an operator ask.

## Goal

One paragraph: what is true after this chunk that is not true now.

## Files

What gets created, deleted, moved, edited — by path, one line each.

## The work

The steps, in order. Verify every LOAD-BEARING claim you cite against the repo: an inherited claim is unverified, and unverified premises are where stubs come from.

## Out of scope

What a reader would expect here and will not find, and where it lives instead.

## Verification

Numbered commands, in order, each with its expected result. Redirect pytest to a file and capture `$?` separately. Prove any clean result with a positive control.

## Progress log

- **YYYY-MM-DD** — what happened.
