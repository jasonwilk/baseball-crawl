# E-230: Fix the 56 Post-RTK Test-Suite Failures

## Status
`COMPLETED`
<!-- Lifecycle: DRAFT → READY → ACTIVE → COMPLETED (or BLOCKED / ABANDONED) -->

## Overview
E-229 removed RTK (Rust Token Killer), whose output compression had been silently hiding pytest failures. With RTK gone and plain pytest output restored, the full suite now reports **56 failed / 4397 passed**. This epic does two things: **make the suite green** (stories 01-03 — investigate each failure, decide the correct behavior first, then fix whichever side is wrong; explicitly **not** a blind "make tests green" pass) and **keep it green** (story 04 — a lightweight context-layer convention so failures can't silently accumulate again).

**Net finding (stated plainly):** first-hand SE + DE investigation found **zero confirmed product bugs**. All 56 failures are **test-maintenance** — stale fixtures and stale assertions — and for each, production behavior was **proven correct** by applying the decision framework (reproduce → `git`-trace the source line → verify the production path). The user's "fix code OR fix test" question resolves to **"fix test" for all three buckets**, but only after evidence. The documented per-bucket evidence trail is the deliverable, satisfying the "evaluate what the behavior SHOULD be" requirement.

## Background & Context
RTK compressed pytest output so that pass/fail counts and suite truncation were invisible (the documented ~67 silent failures across E-173/E-179 that triggered the now-removed E-224 pytest guardrails). E-229 removed RTK and dropped that compensating tooling as a full drop; plain pytest output is honest again. E-229 touched zero `src/` runtime code, so these 56 failures are **pre-existing and were masked**, not caused by E-229.

A full-suite run on clean `main` (HEAD `46edaaa`, plain `python -m pytest tests/`, ~91s) produced 56 failures that resolve to **three buckets**, characterized first-hand by software-engineer (code trace + `git log`/`git show`) and data-engineer (data-layer review). All three are test-maintenance:

- **Cluster 1 — `gs` fixtures omit a key production always supplies (52 failures)**: the template guard is **correct and load-bearing**; the failing test fixtures omit the `gs` key. Fix = the fixtures, plus user-confirmed hardening of all four `gs` templates as defense-in-depth (Story 01).
- **Cluster 2 — spray-chart geometry tests are stale (2 failures)**: a deliberate source redesign (commit `a8d6ac6`) the tests never tracked. Fix = the tests (Story 02); design intent confirmed square by the user via the live production report.
- **Cluster 3 — game-coverage assertions are stale (2 failures)**: a later-added fixture game the assertions never accounted for. Fix = the assertions (Story 03).

A fourth story (Story 04, claude-architect) adds lightweight regression prevention so failures cannot silently accumulate again. This epic is the **forward fix** of the now-visible failures (plus prevention). It is distinct from **IDEA-072 (RTK Compression Retrospective Audit)** — the backward-looking investigation of what *else* RTK's compression may have hidden across `git diff`/`git log`/reviews. E-230 references IDEA-072 but does **not** absorb it; the two remain separate per the ideas-vs-epics rule.

## Goals
- Reduce the full suite from 56 failed / 4397 passed to **0 failed** under plain `python -m pytest tests/`.
- For every failure, document the proof that production behavior is correct and the fix is on the test side (per the Decision Framework in Technical Notes) — not a blind green pass.
- Bring the three stale pitcher-dict fixture helpers into line with the real production query contract (`gs` always present: NULL, `0`, or positive int).
- **Prevent recurrence**: establish a full-suite-green closure gate and a code-reviewer full-suite trigger so test failures cannot silently accumulate again (story 04).

## Non-Goals
- **No schema or migration changes.** DE confirmed the data layer is already correct: `gs` is `INTEGER NULL` (`migrations/001_initial_schema.sql:335`) and every production path supplies it. No loader or migration touch.
- **No removal or weakening of the `gs` template guard.** The guard is correct and load-bearing for real NULL `gs` (a normal production state). It is NOT the bug. See Cluster 1.
- **No reintroduction of suppressed-failure tooling.** E-229 deliberately dropped the RTK-era pytest `-v` rule and `-x`/exitfirst hooks. This epic adds no replacement rule, hook, or `addopts` default.
- **No `-x`/`--exitfirst` masking.** Verification runs the full suite; partial runs that stop at the first failure are not acceptance evidence.
- **No IDEA-072 work.** The backward-looking RTK-distortion audit is out of scope and stays a separate idea.
- **No behavior change to `get_game_coverage()`** (Cluster 3) or to `src/charts/spray.py` (Cluster 2): the production behavior is intended; only the stale tests are wrong.

## Success Criteria
- `python -m pytest tests/` reports **0 failed** (from 56 failed / 4397 passed), with no test selection flags, no `-x`, and no RTK/suppression tooling.
- Each of the three clusters has a documented fix-side decision with the supporting evidence (production-correct proof) captured in its story.
- The three pitcher-dict fixture helpers supply `gs`, with coverage of NULL (→ em-dash), `0` (→ `0/N`, a known pure reliever — real data, never em-dash), and positive int (→ `{gs}/{g-gs}`).

## Stories
| ID | Title | Status | Dependencies | Assignee |
|----|-------|--------|-------------|----------|
| E-230-01 | Populate `gs` in pitcher fixtures + harden `gs` template guard (all 4 templates) | DONE | None | software-engineer |
| E-230-02 | Realign spray-chart geometry tests to square (3,3) cropped output | DONE | None | software-engineer |
| E-230-03 | Fix game-coverage indicator assertions to "(4 games)" | DONE | None | software-engineer |
| E-230-04 | Executable full-suite-green closure gate + CR closure pass (regression prevention) | DONE | None | claude-architect |

## Dispatch Team
- software-engineer (01, 02, 03)
- claude-architect (04)

## Technical Notes

### Per-Failure Decision Framework (the heart of this epic)
For each failure, the implementer applies the framework software-engineer used during discovery — and records the verdict + evidence in the story:

1. **Reproduce in isolation** — run the single failing test, confirm the error.
2. **Trace the source history** — use `git log -L`/`git show` on the relevant source line to determine whether the **source changed under a stale test** (→ fix the test) or a genuine **code/contract gap** exists (→ fix the code).
3. **Confirm real behavior** — run the production query/render path (or read the authoritative contract: `key-metrics.md`, `migrations/*.sql`, function docstrings) to confirm what the behavior *should* be.

The investigation result here is that **all three buckets resolve to "fix the test," with proof that production is correct in each case**. A fix MUST NOT mask a real regression — but because production was proven correct first-hand for every bucket, fixing the tests is the evidenced-correct action, not a shortcut to green. The evidence trail per bucket is the deliverable.

### Cluster 1 — `gs` fixtures omit a key production always supplies (Story 01, 52 failures)
- **Failure**: `jinja2.exceptions.UndefinedError: 'dict object' has no attribute 'gs'`. Spread: 40 `tests/test_report_renderer.py` + 9 `tests/test_report_workload.py` + 3 `tests/test_dashboard_workload.py` = 52.
- **Why it raises**: the guard `{% if pitcher.gs is none %}&mdash;{% else %}{{ pitcher.gs }}/{{ pitcher.games - pitcher.gs }}{% endif %}` protects *present-but-None*. When the key is **absent**, Jinja treats it as `Undefined` (not `None`), so `is none` is False, the `{% else %}` branch runs, and `pitcher.games - pitcher.gs` raises. The crash is triggered by the **fixtures**, which omit the key — not by a production data path.
- **The guard is CORRECT and load-bearing — do NOT remove or weaken it.** DE + SE traced EVERY production pitcher-dict path and confirmed all supply `gs`: `_query_pitching` (`src/reports/generator.py:203`/`:222`), `get_team_pitching_stats` (`src/api/db.py:188`), the opponent-scouting query (`src/api/db.py:888`), and the dashboard mutators (`src/api/routes/dashboard.py:997`) all SELECT/preserve `gs`. NULL `gs` IS a normal production state (manual-entry opponents with no `appearance_order`; pre-backfill rows; member teams when the GC season-stats payload lacks "GS" — `src/gamechanger/loaders/season_stats_loader.py:503`), and the `is none` guard exists precisely to render those as em-dash. Removing it would break real rendering.
- **The actual gap**: the failing tests' pitcher-dict builders omit the `gs` key — they predate the column and were never updated. RTK hid the resulting render exception.
- **Fix part A — fixtures (clears the 52 failures)**: populate `gs` in the three fixture helpers — `_make_pitcher` in `tests/test_report_renderer.py:30`, the pitcher builder in `tests/test_report_workload.py`, and `_make_pitcher` in `tests/test_dashboard_workload.py:223` — to match the real query contract. `gs` semantics (DE): **NULL = unknown** (→ em-dash); **`0` = known pure reliever** (real data → `0/5`); **int > 0 = starter** (→ `{gs}/{g-gs}`). Fixtures must exercise at least one None case and one int case.
- **Fix part B — template-guard hardening (user-confirmed in-scope, all 4 templates)**: harden the guard so a *missing* key degrades to em-dash identically to present-None (e.g. `pitcher.gs is none` → `pitcher.get('gs') is none`, or tolerate `undefined`), so a future caller that forgets `gs` degrades to em-dash instead of a 500. The same pattern appears in four templates: `src/api/templates/reports/scouting_report.html:711` and `src/api/templates/dashboard/opponent_print.html:398` (crash today via the fixtures) plus `src/api/templates/dashboard/opponent_detail.html:392` and `src/api/templates/dashboard/team_pitching.html:84` (no failing test, same pattern). Hardening must preserve present-None → em-dash, `0 → 0/N`, and the positive-int split. The user chose to harden all four (Open Question 2, RESOLVED).
- **Display contract (already settled — no coach needed)**: `key-metrics.md` → GS/GR shows `{gs}/{g-gs}`; NULL `gs` (unknown) = em-dash; `0` is data (pure reliever), never em-dash. `display-philosophy.md` → never suppress.

### Cluster 2 — spray-chart geometry tests are stale (Story 02, 2 failures)
- **Failures**: `tests/test_charts/test_spray.py::test_render_uses_3x4_figsize` (`:198-204`) and `::test_render_png_dimensions_approx_3x4_at_150dpi` (`:362-375`; the `height > width` portrait check at `:375` is the actual failing line). Deterministic (3/3 reproductions), not flaky.
- **Root cause** (SE git-traced): commit `a8d6ac6` ("redesign scouting report print layout") **deliberately** changed `src/charts/spray.py` to `figsize=(3,3)` (line 451) and cropped the axes (`set_ylim(365, 8)` / `set_xlim(-4, 324)`, lines 479-480, `dpi=150` + `bbox_inches="tight"` at line 494) to remove dead whitespace. That commit changed the source but never updated these two tests. Current near-square output **is** the intended redesigned output. (Note: the "3x4" in the commit message and the test names refers to the **page card grid** — 12 cards per page — NOT the figure aspect ratio. The figure itself is intentionally square.)
- **Verdict (RESOLVED — fix the tests)**: design intent is settled by **ground truth** — the user confirmed the desired output is the live production report `https://bbstats.ai/reports/zoVS7ElENReB4Kkr`, which renders near-square spray charts (exactly what the current code produces). Combined with the deliberate redesign commit, this confirms **square is intended**. Story 02 is a **test-fix only** (no code-fix branch): update the figsize assertion to `(3,3)`, realign **all three orientation-encoding assertions** (`:373` `width>=200`, `:374` `height>=300`, `:375` `height>width` — the last two/three encode the old portrait expectation; `:375` is the actual failing line, `:373-374` pass only incidentally) to the implementer's **measured** near-square output, rename both tests + the `:199` docstring away from "3x4", and pin to measured dimensions. `src/charts/spray.py` is not modified.

### Cluster 3 — game-coverage assertions are stale (Story 03, 2 failures)
- **Failures**: `tests/test_dashboard.py::TestGameCoverageIndicator::test_opponent_detail_shows_coverage_indicator` and `::test_opponent_print_shows_coverage_indicator` (assertions at `tests/test_dashboard.py:3248` and `:3266`). Both assert `"Through Mar 1 (3 games)"`.
- **Root cause** (SE reproduced): the fixture seeds a **4th** completed opponent game (`game-opp-2025`, `tests/test_dashboard.py:277`) added later for a different test. `get_game_coverage()` correctly returns `game_count=4`. Production and the format string are correct.
- **Verdict**: fix the **test** assertions to `"(4 games)"`. Behavior is intended all-time coverage; do NOT change `get_game_coverage()`.

### Constraints (all stories)
- Tests must pass with **plain** `python -m pytest tests/` (no RTK, no `-x`/exitfirst masking, no `-k`/selection narrowing as acceptance evidence).
- No new suppressed-failure tooling (rule, hook, or `addopts` default).
- Per the testing rule's Test Scope Discovery: after changing a fixture, template, or source line, find and run **all** test files that exercise the changed render/source path, not only the story-named files.
- Fixes must not mask a real regression — but the investigation already proved production correct per bucket, so the evidenced action is to fix the tests.

### Prevention (Story 04, claude-architect)
Story 04 is the "keep it green" half: an **executable** closure gate (not inert policy text) closing the **accumulation** gap that RTK's compression exploited, across 4 context-layer files (~15-18 lines). The load-bearing edit is a new **Phase 5 Step 1b "Full-suite green gate"** in `.claude/skills/implement/SKILL.md` — unconditional on every epic closure, cloning the existing Step 1a closure-CR pattern: code-reviewer runs `python -m pytest tests/` in the **main checkout**, reds route into the Phase 4a remediation loop, and the PM MUST NOT mark the epic **COMPLETED** until it reports 0 failed. The same edit reconciles the adjacent Phase 5 prose Step 1b makes false (the `:419` "no review logic in Phase 5" caveat and the `:608` workflow-summary diagram). Supporting edits: scope the per-story "no pytest" ban (implement/SKILL.md:254 + code-reviewer.md:280) so it applies to per-story worktree review and names the closure pass as the exception (worktree pytest tests main's code via the editable-install meta-path-finder, so it is misleading per-story; at closure the changes are in main where pytest is authoritative — a reconciliation, not a loophole); document the gate in a new `## Full-Suite-Green Closure Gate` section of `.claude/rules/workflow-discipline.md` (alongside the sibling Documentation/Context-Layer Assessment gates); and append the inverse direction to the existing Test-Validates-Spec line in `.claude/rules/testing.md`. No new hook, CI, rule file, or `addopts`; no pytest-verbose/exitfirst reintroduction — it is a verification PASS at closure, mirroring Step 1a. The gate is **unconditional** (decided): silent accumulation is exactly the masked failure mode; gating only "invariant" epics reopens the door (~90s/closure, cheap). This is a standing change to how every future epic closes; the main session surfaces that to the user in the READY summary. claude-architect owns the exact wording/placement (domain-expert-designs); the story ACs specify outcomes.

### Independence / sequencing
The four stories touch disjoint files (Story 01 = the 3 pitcher-fixture helpers + the 4 `gs` templates + a new `tests/test_template_gs_guard.py`; Story 02 = `tests/test_charts/test_spray.py` only; Story 03 = `tests/test_dashboard.py` assertions only; Story 04 = `.claude/skills/implement/SKILL.md` + `.claude/agents/code-reviewer.md` + `.claude/rules/workflow-discipline.md` + `.claude/rules/testing.md`). No shared file, no ordering dependency. **Note on the gs-template coverage**: Story 01's missing-key unit test deliberately lives in a NEW file (`tests/test_template_gs_guard.py`), NOT in `tests/test_dashboard.py` — keeping Story 01 and Story 03 file-disjoint. (Route-level tests in test_dashboard.py structurally cannot exercise the missing-key path anyway: they seed pitching via DB INSERT without `gs`, SQLite NULLs it, the query SELECTs it, so the dict always carries `gs=None` — present-None, never missing.) They execute serially during dispatch regardless, but carry no `Blocked by`.

## Open Questions
1. **Spray-chart print-output geometry (design intent) — RESOLVED.** The user confirmed the desired output is the live production report `https://bbstats.ai/reports/zoVS7ElENReB4Kkr`, which renders **near-square** spray charts — exactly what the current code (`src/charts/spray.py:451,479-480,494`) produces. Combined with the deliberate redesign commit `a8d6ac6`, this confirms **square `(3,3)` cropped is intended → fix the tests**. Story 02 is a test-fix only; no precondition and no code-fix branch remain.
2. **`gs` template-guard hardening — RESOLVED.** The user chose to **harden all four `gs` templates** (in addition to the required fixture fix). Story 01 now carries the hardening as required ACs (AC-3/AC-4), not opt-in.

## History
- 2026-05-31: **Closure assessments (Phase 5 Step 3 / 3a).**
  - **Documentation assessment: No documentation impact.** Reviewed against the doc update triggers (`.claude/rules/documentation.md`): no new feature/endpoint, no architecture/deployment change, no agent created/materially modified in the user-facing sense, no DB schema/migration change, and nothing that changes how coaching staff or operators interact with the system. The work is test-maintenance plus an internal dispatch-process gate. No `docs/admin/` or `docs/coaching/` update needed; docs-writer not dispatched.
  - **Context-layer assessment (6 triggers, per-trigger verdict):**
    1. New convention/pattern/constraint — **YES**: the testing.md "Inverse direction: when you change a production contract, stale tests are MUST-FIX" rule (a new testing convention). **Codified in-epic by E-230-04** (claude-architect as the story implementer).
    2. Architectural decision with ongoing implications — **NO**: no technology choice or structural decision (test-maintenance + a process gate, not architecture).
    3. Footgun/failure mode/boundary discovered — **YES**: the editable-install meta-path-finder making worktree/pre-merge pytest test main's pre-epic code (why the closure gate must run in main post-`git apply`). **Codified in-epic by E-230-04** (SKILL.md sub-step 5 + code-reviewer.md Test Execution Constraint scoping + workflow-discipline.md gate section).
    4. Change to agent behavior/routing/coordination/closure — **YES**: the new unconditional Phase 5 Step 1b full-suite-green closure gate + the COMPLETED-flip deferral to Step 8 sub-step 5 — a standing change to how every future epic closes. **Codified in-epic by E-230-04.**
    5. Domain knowledge for future agent decisions — **NO**: no baseball/API/data-model domain insight surfaced (the `gs` display contract was already settled in `key-metrics.md`/`display-philosophy.md`).
    6. New CLI/workflow/operational procedure — **NO**: no new `bb` subcommand, script, or skill; the closure-gate change is internal to the existing implement skill, not a new operator workflow.
  - **Result**: triggers 1, 3, 4 fired, but all three were codified IN-EPIC by claude-architect as the E-230-04 implementer — no separate claude-architect codification pass required. Both assessment gates satisfied; epic ready to archive after the Step 8 green gate + COMPLETED flip.
- 2026-05-31: **Dispatch complete — all 4 stories DONE; Phase 4 review done; closure bookkeeping (COMPLETED flip deferred to the Step 8 green gate per E-230-04 option-(a)).** Outcome: full suite green at 4472 passed / 0 failed (from 56 failed / 4397 passed at discovery). All fixes were test-maintenance; zero product bugs, consistent with the discovery net finding. Story summary: 01 = populated `gs` in 3 pitcher-fixture helpers + hardened the `gs` guard (`pitcher.gs is undefined or pitcher.gs is none`) across all 4 templates + new `tests/test_template_gs_guard.py` rendering each real template's gs cell through Jinja; 02 = realigned the 2 spray-geometry tests to the confirmed-square `(3,3)` cropped output (near-square aspect band, `src/charts/spray.py` untouched); 03 = corrected the 2 game-coverage assertions + docstring to "(4 games)" (`get_game_coverage()` untouched); 04 = added the executable Phase 5 Step 1b full-suite-green closure gate + scoped the per-story pytest ban + new `## Full-Suite-Green Closure Gate` section in workflow-discipline.md + inverse-direction append to testing.md's Test-Validates-Spec.
  - **Baseline restated 56 → 58**: the AC-7 micro-cluster — 2 stale last-outing assertions in `tests/test_report_workload.py` surfaced during E-230-01 dispatch inside AC-5's named-command surface; folded into E-230-01 as AC-7 (see the scope-decision entry below). All 58 were test-maintenance, zero product bugs.
  - **Story-01 em-dash-assertion honesty fix (during per-story review)**: the initial guard test's em-dash assertion was dead under autoescape; corrected to an exact `&mdash;` match (autoescape=False mirroring the real `render_report` env) with exact-match `0/5`/`3/5` cell values — strictly strengthened the test.
  - **Story-03 "(6 games) leak" = FALSE ALARM, investigated and disproven**: a reported full-suite-order test-isolation leak (detail test rendering "(6 games)") was checked first-hand and disproven — the test DB is per-test `tmp_path`-isolated, `get_game_coverage()` deterministically returns 4, two consecutive full-suite runs are identically green. The only real finding was the stale 3→4 assertion + docstring, exactly as discovery scoped. Recorded so the record shows it was checked, not skipped.
  - **E-230-04 footprint variance (~30 lines vs the ~15-18 DoD estimate)**: superseded by the AC-1 design ruling, NOT scope creep. Codex iter-1's P1 + the dispatch-time AC-1 internal-contradiction resolution required real Step-8 enforcement mechanics (the gate runs authoritatively in main post-`git apply`, before the closure commit, because the editable-install meta-path-finder makes worktree/pre-merge pytest test main's pre-epic code). The honest hybrid → option-(a) deferral (COMPLETED flip moved from Step 2 to Step 8 sub-step 5, set only on a green suite) is the enforcement half the ruling required. DoD line reads superseded-by-ruling, not failed.
  - **Phase 4b Codex caught 4 valid findings that per-story + Phase-4a integration review missed** (4/4 accepted, 0 dismissed, all remediated + PM-re-verified by literal read): F1 (SKILL.md sub-step 5 red-suite rollback pointed at the archive-undo `git mv` that hasn't run yet → fixed to a minimal `git reset HEAD && git checkout -- .`); F2 (COMPLETED-timing dishonesty: prose claimed reject-path resets revert COMPLETED but they don't → fixed via option-(a) deferral, consistent across SKILL.md :441/:451/:549/:550 + workflow-discipline.md:92-94); F3 (2 AC-7 last-outing assertions were bare substrings satisfied by the inline JS selector token → element-pinned `<th class="last-outing-header mob-hide-extra">` / `<td class="last-outing-cell mob-hide-extra" data-date=...>`, teeth-proven); F4 (the render_report None test asserted `&mdash;` document-wide, vacuous because the report header carries `&mdash;` → scoped to the GS cell via `_GSGR_CELL_RE`, teeth-proven). F3/F4 were the same masked-pass class the epic exists to kill — caught and fixed, not waved.
  - **`test_js_snippet_present` :242-243 — partial-teeth note (left as-is)**: during the F3 remediation-completeness sweep, sibling bare-substring asserts (`assert "last-outing-cell" in html` / `"last-outing-header"`) were examined. Settled by the objective teeth test as a `<script>`-presence SMOKE check (the test also asserts `"<script>" in html`; it makes no element-class contract claim), so it is NOT the AC-7 false-confidence inversion and NOT a MUST-FIX instance of the new testing.md inverse-rule. Partial teeth = hygiene, not a defect; left as-is. Hardening (assert the `querySelectorAll('.last-outing-*')` call inside the extracted `<script>` block) capturable as a small follow-up if desired. `:280` (compound two-class string) also left — already has teeth.
- 2026-05-31: **Dispatch scope decision (PM, story-scope domain) — 4th micro-cluster folded into E-230-01.** During E-230-01 dispatch, the AC-5 named command surfaced 2 additional failures in `tests/test_report_workload.py` (`TestReportHTMLOutput::test_last_outing_header_class`, `::test_last_outing_cell_class_and_data_date`) NOT in the documented 56-failure baseline. SE verified first-hand (revert `gs` edit + re-run on pristine HEAD) they pre-exist this story and are not `gs`-related — discovery's count missed them (they fail identically on clean HEAD; the same RTK-masking the epic exists to fix explains why they were uncounted). Same decision-framework verdict as the rest of the epic: **template correct, assertion stale → fix the test** (template now emits `class="last-outing-header mob-hide-extra"`; assertions stale-match the whole `class="..."` attribute; fix = assert the class token). PM authorized SE to fix them within E-230-01 (added as AC-7) rather than deferring to a new story, because: (a) they live in `tests/test_report_workload.py`, already inside AC-5's 0-failed surface; (b) the epic Goal + Success Criteria + Story-04 unconditional closure gate all require full-suite 0-failed, so they must clear for the epic to close regardless; (c) same file, same theme, two-token fix — routing elsewhere is pure churn; (d) "no pre-existing excuse" favors fixing. Baseline restated: 58 total stale-test failures (56 documented + this 2-failure micro-cluster), all test-maintenance, zero product bugs.
- 2026-05-31: Created (DRAFT). Discovery completed via first-hand SE (code trace + `git log`/`git show`) and DE (data-layer review) consultations relayed through the main session.
- 2026-05-31: Reframed Cluster 1 per refined SE+DE follow-up. Corrected from "fix both layers (template + fixtures)" to **"fix the fixtures (required); the template guard is correct and load-bearing — keep it; template hardening is optional opt-in hygiene."** DE+SE traced every production pitcher-dict path and confirmed all supply `gs`; NULL `gs` is a normal production state the guard rightly renders as em-dash. Net finding recorded: **zero confirmed product bugs — all 56 failures are test-maintenance**, each proven via the decision framework. Added Open Question 2 (template-hardening opt-in) and restructured Story 01 to make the fixture fix the required core and the hardening a conditional sub-section. baseball-coach not needed (the GS-unknown display contract is already settled by `key-metrics.md` + `display-philosophy.md`).
- 2026-05-31: Both Open Questions resolved by the user; one prevention story added. **(1) Spray geometry → square confirmed**: user pointed to the live production report `https://bbstats.ai/reports/zoVS7ElENReB4Kkr` (near-square = current code output); Story 02 collapsed to test-fix only (portrait code-fix branch retired; tests renamed off "3x4"; dimension assertion to be measured at dispatch). **(2) `gs` hardening → KEEP all 4 templates**: Story 01 reverted from opt-in to required hardening (AC-3/AC-4). **(3) Prevention → added as Story E-230-04** (claude-architect): widen CR Step 1 to full-suite on cross-cutting-contract changes or last-story-of-epic; full-suite-green closure gate in workflow-discipline.md; inverse-direction append to the existing Test-Validates-Spec line in testing.md — < 12 lines, no new hook/CI/rule, no pytest-verbose/exitfirst reintroduction. Epic now 4 stories / 2 domains (software-engineer 01-03, claude-architect 04). The rejected-as-too-heavy GitHub Actions CI gate captured as CANDIDATE IDEA-073.
- 2026-05-31: Internal review iteration 1 (CR spec audit APPROVED + SE/DE/CA holistic, all "sound/dispatchable"; SE empirically rendered the gs hardening through Jinja — all 4 cases pass). 6 polish findings, all ACCEPTED (0 dismissed): F1 (AC-3 template line numbers now "approximate — match by `pitcher.gs is none` pattern"), F2 (AC-2 makes the `0 → 0/N`, not-em-dash assertion explicit), F3 (AC-4 accepts any equivalent guard form, not just `.get('gs')`), F4 (AC-2 broadened — realign ALL three orientation-encoding asserts `:373/:374/:375` to the measured square output, not just `:375`), F5 (AC-1 also updates the `:199` docstring `(3,4)→(3,3)`), F6 (E-230-04 AC-2 fixes the closure-gate home to workflow-discipline.md as sole home, dropping the CLAUDE.md option — CR+CA converged; CA owns prose). Two informational non-findings noted (DE's read-only corroborating pytest run; SE's confirmation Story 03 is gs-independent).
- 2026-05-31: Codex spec review iteration 1 — 3 findings, all ACCEPTED (0 dismissed; Codex caught real gaps the internal pass missed). **P1 (crux — propagation completeness)**: the original E-230-04 was inert policy text (implement/SKILL.md:244 skips CR for context-layer stories, :254 bans CR pytest, Phase 5 has no full-suite step). CA designed the executable fix: a NEW unconditional **Phase 5 Step 1b** in `.claude/skills/implement/SKILL.md` (cloning the Step 1a closure-CR precedent) where code-reviewer runs `python -m pytest tests/` in the MAIN CHECKOUT before the PM marks the epic COMPLETED; reds → Phase 4a remediation loop. Per-story "no pytest" ban scoped to worktree review with the closure pass named as the exception (worktree pytest tests main's code via the editable-install meta-path-finder — misleading per-story; authoritative at closure). E-230-04 now 4 files (added implement/SKILL.md, the executable lever), ~12-15 lines, restructured to 6 ACs with AC-1 as the executable Step 1b. **P2 (overlap)**: SE found route-level tests structurally cannot exercise the missing-key path (DB INSERT → SQLite NULLs gs → query SELECTs it → dict always present-None, never missing). Resolution = harden all 4 templates + ONE template-unit test rendering each gs-less directly through Jinja, in a NEW `tests/test_template_gs_guard.py` (Story-01-owned, NOT test_dashboard.py) → added as Story 01 AC-6; **the disjoint-files claim STAYS TRUE** (no correction needed). **P2-status (trivial)**: epic state is COMPLETED, not DONE (DONE is story-level); propagated DONE→COMPLETED across the gate descriptions. Decision: closure gate is UNCONDITIONAL (every epic) — a standing change to how every future epic closes; main session surfaces it to the user at the READY gate.
- 2026-05-31: Codex spec review iteration 2 (re-run after the P1 restructure) — 3 findings, all ACCEPTED (0 dismissed); all "propagation completeness" tail (the exact drift class this epic fixes); Codex found nothing in the other 11 categories. **G1 (Phase 5 prose reconciliation)**: Step 1b makes adjacent implement/SKILL.md prose false — the `:419` "No review logic belongs in Phase 5" caveat and the `:608` workflow-summary diagram (Validate→PM completes, no gate). Added E-230-04 AC-1b requiring CA to reconcile both in the same edit; footprint bumped ~12-15 → ~15-18 lines. **G2 (acceptance command coverage)**: E-230-01 AC-5's scoped command ran only the 3 fixture files, not the new AC-6 test — added `tests/test_template_gs_guard.py` to the AC-5 command so acceptance evidence runs the new behavior. **G3 (stale docstring)**: E-230-03 docstring at `tests/test_dashboard.py:3242` still said "(3 games)" — added to AC-1 + Files scope (same class as F5). This was iteration 2 = the circuit-breaker threshold; all findings minor propagation polish with diminishing returns.
- 2026-05-31: Set to **READY** (user-authorized after the iteration-2 circuit-breaker decision: fix + READY). Planning stops here — the user said "plan an epic," not "plan and dispatch," so dispatch is a separate authorization (`implement E-230`). The one user-facing standing change surfaced at the gate: E-230-04's UNCONDITIONAL full-suite-green closure gate means every future epic closure runs `python -m pytest tests/` in the main checkout before the epic is marked COMPLETED (~90s/closure).

## Review Scorecard

### Planning reviews (pre-dispatch)
| Review Pass | Findings | Accepted | Dismissed |
|---|---|---|---|
| Internal iteration 1 — CR spec audit (F4/F5/F6; F6 CR+CA converged) | 3 | 3 | 0 |
| Internal iteration 1 — Holistic team SE/DE/CA (F1/F2/F3) | 3 | 3 | 0 |
| Codex iteration 1 (P1 propagation / P2 overlap / P2 status-model) | 3 | 3 | 0 |
| Codex iteration 2 (G1 Phase-5 prose / G2 command coverage / G3 docstring) | 3 | 3 | 0 |
| **Planning subtotal** | **12** | **12** | **0** |

### Dispatch reviews
| Review Pass | Findings | Accepted | Dismissed |
|---|---|---|---|
| Per-story CR — E-230-01 (round-1 SHOULD FIX: em-dash assertion dead under autoescape) | 1 | 1 | 0 |
| Per-story CR — E-230-02 | 0 | 0 | 0 |
| Per-story CR — E-230-03 | 0 | 0 | 0 |
| E-230-04 (context-layer-only — PM-only AC verification, no per-story CR) | 0 | 0 | 0 |
| Phase 4a — CR integration review | 0 | 0 | 0 |
| Phase 4b — Codex code review (F1/F2 SKILL.md gate-logic; F3/F4 test-teeth) | 4 | 4 | 0 |
| **Dispatch subtotal** | **5** | **5** | **0** |

| **Grand total (planning + dispatch)** | **17** | **17** | **0** |
