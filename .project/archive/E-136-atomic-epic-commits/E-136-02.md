# E-136-02: Update Dispatch Pattern Overview

## Epic
[E-136: Atomic Epic Commits](epic.md)

## Status
`DONE`

## Description
After this story is complete, the dispatch pattern overview will reflect the atomic commit workflow: file-copy/patch-apply merge-back instead of git merge, and atomic commit at closure instead of per-story commits.

## Context
The dispatch pattern file (`dispatch-pattern.md`) is a brief overview that references the implement skill as the authoritative source. It describes team roles, permitted orchestration, and domain work boundaries. References to `git merge` for merge-back and `git commit` as permitted orchestration need to be updated to reflect the patch-apply approach and atomic commit at closure.

## Acceptance Criteria
- [ ] **AC-1**: The header paragraph no longer references `git merge` for merge-back. It describes the main session as managing patch-apply merge-back and atomic commit at closure.
- [ ] **AC-2**: The "Main session (spawner + router)" entry in Team Roles lists `git apply` (patch-apply merge-back) instead of `git merge` as a permitted git operation, and lists `git commit` only in the context of the closure atomic commit.
- [ ] **AC-3**: The "Permitted orchestration" list under Domain Work During Dispatch reflects patch-apply merge-back (`git apply`, `git worktree`, `git branch -D`) and atomic commit at closure (`git add -A`, `git commit`), replacing `git merge` and `git branch -d`.
- [ ] **AC-4**: All references in the file are internally consistent -- no surviving references to `git merge --no-ff` or per-story commits.

## Technical Approach
The dispatch pattern file at `/.claude/rules/dispatch-pattern.md` is a short overview (~47 lines). The changes are localized to the header paragraph, Team Roles section, and Permitted Orchestration list. Read the full file and update all references to merge-back mechanics and commit operations.

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Files to Create or Modify
- `/.claude/rules/dispatch-pattern.md` (modified)

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] No regressions in existing rule structure
- [ ] Code follows project style (see CLAUDE.md)

## Notes
This file has a `paths: "**"` frontmatter, meaning it is loaded for every agent interaction. Changes here affect how all agents understand dispatch mechanics.
