# E-228-04: CUT (was: Matchup data layer -- query helpers in `src/api/db.py`)

## Status
`CUT`

## Cut Reason
Removed during E-228 v1 refinement (2026-04-28). Folded into **E-228-12** (combined matchup engine + input builder). v1 ships 4 new helpers in `src/api/db.py` (`get_top_hitters`, `get_hitter_pitch_tendencies`, `get_sb_tendency`, `get_first_inning_pattern`) added as part of the same story that builds the engine, because v1's signal selection is tightly coupled to helper return shapes -- splitting helpers from engine forces excess interface churn for a small story.

See `epic.md` History entry "2026-04-28" for the full v1 refinement context.
