# E-228-07: CUT (was: LLM Tier 2 wrapper -- `enrich_matchup()` with hallucination guardrails)

## Status
`CUT`

## Cut Reason
Removed during E-228 v1 refinement (2026-04-28). Renumbered to **E-228-13** with a tightened scope. The hallucination guardrail (player-name round-trip) and the OpenRouter integration mirror remain non-negotiable. v1 ships a multi-field JSON output schema (`game_plan_intro` + per-hitter `hitter_cues` + per-sub-section prose: `sb_profile_prose`, `first_inning_prose`, `loss_recipe_prose`) per Codex pass 7; pull-tendency notes are deterministic engine output formatted by the renderer (no `pull_tendency_prose` field, per Codex pass 5). The LLM wrapper signature was clarified during refinement to take `(MatchupAnalysis, MatchupInputs)` together -- this resolves the original Codex-flagged spec contradiction where AC-1 took only the analysis but AC-3 required the prompt to render inputs tables.

See `epic.md` History entry "2026-04-28" (lines 128 + 129, Codex passes 5 and 7) for the canonical schema state and the full v1 refinement context.
