# E-228-06: CUT (was: Matchup input builder -- `build_matchup_inputs()` + perspective-aware queries)

## Status
`CUT`

## Cut Reason
Removed during E-228 v1 refinement (2026-04-28). Folded into **E-228-12** (combined matchup engine + input builder). The pre-flight gate on opponent-perspective coverage (`_MIN_OPPONENT_PERSPECTIVE_COVERAGE`) and the `data_completeness` block on `MatchupAnalysis` are simplified in v1 -- the engine emits `confidence="suppress"` for severely thin data and per-component `data_notes` entries for partially thin components, but the multi-field `DataCompleteness` dataclass and the `plays_loaded_per_game` gating are deferred to E-230.

See `epic.md` History entry "2026-04-28" for the full v1 refinement context.
