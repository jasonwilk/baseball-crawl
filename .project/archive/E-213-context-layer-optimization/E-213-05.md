# E-213-05: Recalibrate Context-Fundamentals Budget and Clean Up Memory

## Epic
[E-213: Context Layer Optimization](epic.md)

## Status
`DONE`

## Description
After this story is complete, the context-fundamentals skill's budget numbers will reflect post-optimization actuals (CLAUDE.md line count, universal rule count/lines, total ambient range), a superseded user memory entry will be removed, and the 5 largest agent memory topic files will have `Last updated:` date headers for staleness assessment.

## Context
The context-fundamentals skill (`/.claude/skills/context-fundamentals/SKILL.md`) claims CLAUDE.md is ~152 lines and universal rules are ~258 lines / 6 files. These numbers were measured post-E-112 (2026-03-16). After 100+ epics of growth and the optimizations in E-213-01 through E-213-04, the numbers will have changed significantly. Agents using stale budget numbers make wrong load/defer decisions.

Additionally, user memory contains a struck-through superseded entry (`feedback_spawn_pm_opus.md`) that still loads into context, and large agent memory topic files lack dates for staleness assessment.

## Acceptance Criteria
- [ ] **AC-1**: Every occurrence of stale budget numbers in the context-fundamentals skill is updated to post-E-213 actuals. This includes: the budget table (lines 74-85), the prose summary at line ~28 (~560-870), universal rule count at line ~79, agent MEMORY.md range at line ~82, total ambient at line ~83, and the comparison prose at line ~184. A grep for the old values (e.g., `560-870`, `~152 lines`, `~258 lines`, `6 files`) should return zero hits after the update.
- [ ] **AC-2**: The "measured post-E-112" note in the skill is updated to "measured post-E-213 (2026-04-05)"
- [ ] **AC-3**: The struck-through `feedback_spawn_pm_opus.md` entry is removed from `/home/vscode/.claude/projects/-workspaces-baseball-crawl/memory/MEMORY.md` and the linked file `feedback_spawn_pm_opus.md` is deleted (direct write outside worktree per TN-6)
- [ ] **AC-4**: At least 5 of the largest agent memory topic files (by line count) have a `Last updated: YYYY-MM-DD` line near the top, with dates derived from `git log` (actual last-modified date, not today's date)
- [ ] **AC-5**: The context-fundamentals skill's worked example (if present) is updated to use post-optimization numbers

## Technical Approach
This story must execute after all 4 prior stories (E-213-01 through E-213-04) to get accurate post-optimization line counts. Use `wc -l` on the modified files and `ls` on the rules directory to measure actuals. Use `git log -1 --format=%ci` on each topic file to determine last-modified dates. Target topic files for date annotation: identify the 5 largest by line count across all agent memory directories.

## Dependencies
- **Blocked by**: E-213-01, E-213-02, E-213-03, E-213-04
- **Blocks**: None

## Files to Create or Modify
- `/workspaces/baseball-crawl/.claude/skills/context-fundamentals/SKILL.md` (modify -- update budget numbers)
- `/home/vscode/.claude/projects/-workspaces-baseball-crawl/memory/MEMORY.md` (modify -- remove superseded entry)
- `/home/vscode/.claude/projects/-workspaces-baseball-crawl/memory/feedback_spawn_pm_opus.md` (delete)
- 5 agent memory topic files TBD (modify -- add `Last updated:` dates)

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] No regressions in existing context layer behavior
- [ ] Code follows project style (see CLAUDE.md)

## Notes
The exact post-optimization numbers cannot be known until E-213-01 through E-213-04 are complete. The implementer will measure actuals at execution time. The "5 largest topic files" should be identified by running `wc -l` across all `.claude/agent-memory/*/` directories.
