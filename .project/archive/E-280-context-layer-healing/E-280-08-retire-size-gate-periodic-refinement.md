# E-280-08: Retire the size gate, state the periodic refinement cadence, state the batched audit cadence

## Epic
[E-280: Context-Layer Healing](./epic.md)

## Status
`DONE`
<!-- DONE 2026-08-02 against frozen tree `7d0e35782410931b7f2980a4147e49cbc71aaaf3`. PM AC verdict PASS on all criteria. THE LAST STORY OF E-280. -->
<!-- ⚖️ TN-7 RULING ON THE +1,629 BYTES: SUBTRACTION, NOT A SWAP. Applied on the per-fact restatement ledger, never the byte count. BEFORE: four paragraphs each discharged their duty by POINTING AT THE GATE in four words — ONE fact restated FOUR times, and FALSE after the retirement. AFTER: each paragraph names its own live discipline — FOUR DISTINCT facts, each stated ONCE, each true. Gate-pointer restatement goes 4 -> 0; no fact now lives in more places than before; restatement strictly DECREASED. -->
<!-- ⚠️ CONSISTENCY NOTE, because this ruling cuts the opposite way to E-280-02's: there PM REFUSED a byte argument offered in the epic's favour (load-on-demand vs always-on), on the ground that TN-7 IS NOT A BYTE TEST. The same rule with the sign reversed is what governs here — bytes rising is no more a TN-7 failure than bytes falling was a TN-7 pass. A pointer to a retired mechanism was never a baseline worth preserving: it is cheap and FALSE, and naming the live discipline costs bytes and is TRUE. -->
<!-- BASIS AND ITS LIMIT, stated rather than implied: PM ruled on the LEDGER LOGIC. PM did NOT independently verify the factual premise that the pre-state discharged those four paragraphs by a four-word gate pointer — that rests on the implementer's account, with code-reviewer running a second read precisely because it has twice caught ledgers narrower than claimed. IF THAT PREMISE IS FALSE, THE RULING DOES NOT FOLLOW. -->
<!-- Verified by others and confirmed as reported: AC-10 (orchestrator-verified independently — `context-ratchet.sh` and its baseline JSON byte-unchanged and ABSENT from the epic diff; retiring the gate did not touch the instrument); AC-5's control firing in 5 files, so the zero is absence of the GATE not of the script; AC-13 run against the FINISHED epic diff with a control showing `implement/SKILL.md` at 177 insertions / 41 deletions, so the zero is absence rather than an empty diff. -->
<!-- ⭐ AC-11 is the seed-independence rule's first real test and it WORKED: seeds written from the class BEFORE any regex, and the adjacency pattern the implementer would naturally have written is BLIND to 2 of 4. Bare cardinal used instead, every hit resolved by reading. Cardinal `eight` -> 0, five ordinals preserved. THAT IS THE EXACT FAILURE PM MADE ON E-280-07's AC-1c AND COULD NOT SEE, caught in advance by the Status-block intervention. Item 45's mechanism, fifth firing. -->
<!-- AC-9a PASSES on PM's own checkable form (Status-block ruling above): two of three cadences vanish outright; the surviving batched audit is a clause of the SAME section, in the SAME unit (epic closures), archive-checkable the SAME way, with no second tracking surface. One obligation with two multiples. -->
<!-- ⚠️ PM DRAINED after this verdict. Closure package handed to a fresh PM instance against epic TN-21, which is the authoritative handoff. Prefer TN-21 and TN-20 over any message. -->
<!-- ⚖️ PM RULINGS 2026-08-02, on the implementer's flags. BOTH READINGS CONFIRMED; implement as flagged. -->
<!-- FLAG 2 — RENAMING `### Cadence and the ratchet` to `### Cadence` is NOT RED. AC-2's RED is "a new `##` or `###` heading, or a new file"; a rename creates NEITHER, and the heading count is unchanged. AC-2 forbids NET-NEW structure and a rename is not that. Further: leaving the old title would strand a heading pointing at a RETIRED GATE — the retired-claim residue this epic has filed repeatedly, and the exact defect that cost E-280-07 a round when a heading mispointed after its content moved. So the rename is not merely permitted, it is required by the epic's own retired-claim discipline. -->
<!-- FLAG 3 — AC-9a is NOT RED on the implementer's reading, and the decisive reason is SATISFIABILITY, not preference. AC-1a mandates THREE closures and AC-8 mandates FIVE; both are operator-ruled with no latitude. So a reading of AC-9a's "one pass subsumes three cadences" that forbids two intervals would forbid what the operator ORDERED — unsatisfiable by any correct artifact, and PM has twice ruled (E-280-06 AC-3, E-280-07 AC-1c) that a reading no correct artifact can satisfy is not the reading the AC meant. "Three cadences" therefore refers to the three cadences REPLACED, not to the resulting schedule count: two vanish entirely (the per-epic size gate, the per-epic audit expectation) and one survives as an operator-mandated multiple. -->
<!-- FLAG 3, THE CHECKABLE FORM, so this is not PM's say-so: the surviving audit clause is ONE obligation with two multiples, not two obligations, IF AND ONLY IF it needs no separate tracking machinery — same UNIT (epic closures), same SECTION, same ARCHIVE-CHECKABLE method, no second place anyone must look. **RED if the two multiples require different units, different trigger artifacts, or a second tracking surface.** That is the property AC-9a protects; the interval count never was. -->
<!-- FLAG 1 — factual, accepted: AC-13 says "all FIVE `ratchet` occurrences"; there are five LINES and SIX OCCURRENCES (line 644 carries two). PM's earlier "still five" was right about SITES and is corrected here. No verdict changes — byte-identity requires both figures unchanged either way — but it is a wrong cardinal inside the story that retires wrong cardinals. Verify six-on-five and record both. -->
<!-- ⚠️ THREE THINGS BEFORE YOUR GLOBAL VERIFICATIONS (AC-11, AC-13). This intervention is placed here because it demonstrably worked on E-280-09. -->
<!-- 1. A POSITIVE control proves the pattern RAN. A NEGATIVE control proves it can SEE — but ONLY IF THE SEED WAS WRITTEN INDEPENDENTLY OF THE PATTERN. A seed drawn from the pattern's own alternatives tests nothing: E-280-07's first negative control was circular for exactly that reason, and the "class-capable" pattern then scored 0 — BLIND against an independently written seed. For AC-11 this binds directly: DO NOT seed from the numerals your own pattern lists. Seed from the CLASS — a trigger count expressed in a form you did not think of. -->
<!-- 2. AC-11's coverage question: can your pattern reach a trigger count expressed WITHOUT a numeral it enumerates? AC-13's: is byte-identity tested ACROSS THE FINISHED EPIC DIFF, or against a remembered state? Those are the two ways each check returns clean while missing its class. PM verified both premises current before you started — five `ratchet` sites still five after edits by 02, 06, 09 and 07; AC-11's fourth-site premise intact, no fifth appeared. -->
<!-- 3. YOU INHERIT AN OPEN OBLIGATION, not a caveat. E-280-07's AC-1c passed with a NAMED RESIDUAL: layer-wide coverage rests on doc-sweep steps 1+2 over five literals; step 3 (semantic read) covers only 07's three owned files. A consequence-form instance sharing none of those literals, in a file nobody read, survives both — and no wider regex closes it. Treat layer-wide SEMANTIC coverage as NOT established. See epic TN-20 item 63. -->
<!-- 4. If either AC reads broader than the defect it targets, FLAG rather than complying conservatively (epic TN-20 item 45). Four criteria in this epic needed that calibration and all four were caught only because an implementer flagged. -->
<!-- 5. A floor names the token-reachable sites and misses the ones sharing no token, which are the sites the sweep exists for (item 44). An all-clear floor is a SUCCESSFUL sweep, not a wasted one. -->
<!-- 6. State negative results AS RESULTS (item 61): "I looked and found nothing" and "I did not look" are indistinguishable in a record unless one is stated. A count that did not move is EVIDENCE, not a non-event. -->
<!-- 7. A correction that lives only in your completion report HAS NOT LANDED (item 62). Your artifacts are read by the closure review, which is not a person. -->
<!-- 8. This is the LAST story. After it: Codex, the closure integration review over the whole epic diff, the full-suite gate, the merge. Closure obligations sit in the Status block of epic.md and in TN-4(c), TN-15 and TN-20. -->

## Description
After this story, no file in the context layer asserts that the layer's size is gated, bounded by a committed baseline, or subject to an operator-signed-exception ritual. In its place the layer states two cadences in one file: a **scheduled periodic context-layer refinement pass** (replacing continuous gating) and the **batched audit cadence** (TRIM 2). `.claude/hooks/context-ratchet.sh` survives untouched as an on-demand diagnostic.

## Context
**Operator ruling, 2026-08-02, verbatim: *"I don't think gating and ratchets are working. I think we just need periodic refinement."*** This closes epic OQ-1 and OQ-B. The measurement is kept and only the one-way gate goes — the same disposition the operator gave the reconcile-scoreboard ratchet on 2026-07-26, where the scoreboard survived as a diagnostic and the gate against a committed baseline was retired.

**What made the gate indefensible was structural, not anybody's conduct.** `context-ratchet.sh` is an unregistered manual diagnostic that accrues continuously and is charged to whichever epic closes next. E-279 accrued +286 lines in 22h33m of elapsed planning time and contributed +64 at dispatch, failing at +350; the live tree measured +418 on 2026-08-01, of which **89% was `agent-memory`** — a subtree that grows as a function of doing work, against a fixed total. An epic closing more than about a day after a snapshot fails by construction, whatever its own discipline. Epic TN-9 carries that reading in full; preserve it there as evidence and do not update it.

**TRIM 2 arrived here from E-280-04 on 2026-08-02**, because the gate seams and the audit-cadence line land in the same file and a single story is the file-disjoint home for both. Its quality risk is the highest of the four trims in the primary analysis's own words — *"these audits found a CRITICAL; reduce frequency, don't delete"* — which is why AC-4 exists.

**Two hazards specific to this story, both from the repo's own record:**

**The likeliest false positive is FIVE hits away.** `.claude/skills/implement/SKILL.md` says "ratchet" at roughly lines 511, 519, 529, **532** and 537, and **none of them means this instrument** — all five refer to the reconcile-scoreboard ratchet, already retired 2026-07-26. That file belongs to E-280-02 and E-280-06 and is out of scope here. Read each hit before ruling on it; a grep hit is a candidate, never a finding. **Expect five and not four** — this note said four until 2026-08-02, and an unexplained fifth hit is exactly the kind of unexpected count this repo treats as a cross-check trigger. The `532` hit reads *"not a ratchet"*, which is the most misreadable of the five and still refers to the other instrument.

**A retired claim survives in the judgement that rested on it, sharing none of its tokens** (`.claude/rules/doc-sweep.md`). Both sites in `context-layer-guard.md` use the gate to justify the *absence* of any other size discipline — "there is no separate unenforced line target" — so deleting the gate clause alone leaves each paragraph asserting that nothing bounds the thing it is about. AC-6 exists for exactly that shape.

## Acceptance Criteria

**TRIM 2 — batched audit cadence (carried from E-280-04, criteria unchanged in substance):**

- [ ] **AC-1**: The batched audit cadence is stated in `.claude/rules/context-layer-assessment.md` and **nowhere else under `CLAUDE.md`, `.claude/rules/`, `.claude/skills/` or `.claude/agents/`**. **RED**: the cadence stated in two or more locations.
- [ ] **AC-1a**: The cadence **names which audits are batched** and states the operator-ruled interval: **the batched Fable audit runs once per THREE epic closures.** **RED**: an audit class left unnamed; a cadence satisfiable by the bare sentence "audits are batched"; or any interval other than three closures. (Operator ruling, 2026-08-02, interactive gate. **Work-proportional and checkable from the archive** — count closures since the last run — rather than wall-clock, so a quiet fortnight does not consume a cycle and a busy one does not skip.) (⚠️ **This is the AC set most in need of that bar and it was the one missing it.** TRIM 2 carries the epic's only MEDIUM quality risk, and the primary analysis's own bound is *"these audits found a CRITICAL; reduce frequency, don't delete"* — a frequency you cannot name is not a reduced frequency, it is an unbounded one. AC-4 forbids eliminating audits; without this AC, AC-4 is the only thing standing between "batched" and "never".)
- [ ] **AC-2**: No new file and no new heading is created for it. **RED**: a new `##` or `###` heading, or a new file.
- [ ] **AC-3**: The file contains no pair of sentences requiring both a batched audit cadence and a per-epic audit fleet; where the existing per-epic hygiene cadence could be read as conflicting, it is reconciled in the same edit. **RED**: two sentences a reader could satisfy only by doing both.
- [ ] **AC-4**: The line reduces audit frequency rather than eliminating audits. **RED**: text a reader could satisfy by never running an audit.

**Gate retirement:**

- [ ] **AC-5**: **Verify by absence.** Across **`CLAUDE.md`**, `.claude/rules/`, `.claude/skills/` and `.claude/agents/`, zero sentences remain asserting that context-layer growth past a baseline must be offset, must carry an operator-signed exception, or is otherwise hard-bounded. **RED**: any such sentence. (`CLAUDE.md` added 2026-08-02 to match Success Criterion 4's four trees — it carries no such sentence today, verified, so this is future-proofing rather than a live gap, but an AC narrower than the criterion it discharges leaves a hole a later edit can walk through.) (Grep for the false forms and require zero; confirming the replacement arrived is strictly weaker.)
- [ ] **AC-6**: **Every site is enumerated by a regenerated search and carries a written verdict, `no change needed` included.** The list is a **superset** of epic TN-15's floor. **RED**: a TN-15 site absent from the regenerated list, or any listed site with no written verdict. (A search returning exactly TN-15 did not search. A per-site written verdict is required because a surfaced site left untouched is this repo's recorded failure mode — surfacing is not ruling.)
- [ ] **AC-7**: For each of the two `context-layer-guard.md` sites, the surviving paragraph makes a **true and complete** statement about what disciplines the thing it is about — CLAUDE.md's size in one, MEMORY.md's length in the other. **RED**: a paragraph that, after the edit, either still cites the gate or asserts that no discipline applies while naming none. (Both sites justified the absence of another control by pointing at the gate; removing the pointer without repairing the justification leaves a false sentence, and the falsehood shares no token with the deleted clause.)
- [ ] **AC-8**: A **scheduled periodic context-layer refinement pass** is stated, running **once per FIVE epic closures**. **RED**: no cadence stated; a cadence expressed only as a disposition ("refine regularly", "keep the layer lean"); or any interval other than five closures. (Operator ruling, 2026-08-02, interactive gate. Same work-proportional, archive-checkable basis as AC-1a, and deliberately a longer cycle than the audit's three — refinement is the heavier pass.)
- [ ] **AC-9**: The periodic refinement pass names **what it produces** — an observable output someone can point at afterwards. **RED**: a pass defined only by an activity, with no artifact, record, or checkable end state. (An action-shaped cadence is satisfiable by performing it badly; a produced artifact is not.)
- [ ] **AC-9a**: **The one pass SUBSUMES the three cadences it replaces, and the file states one where it previously stated three.** Those three are the per-epic size gate (trigger 7), the per-epic audit expectation, and TRIM 2's batched audit. **RED**: any two of the three surviving as separately-scheduled obligations a reader would have to satisfy independently. (Design owner's recommendation, adopted. This is what makes the change a net subtraction rather than a swap — and it is why TRIM 2 stops being a standalone item and becomes one clause of the cadence: AC-1 through AC-4 are satisfied *by* this pass, not alongside it.)
- [ ] **AC-10**: `.claude/hooks/context-ratchet.sh` and `.project/baselines/context-layer-ratchet.json` are **byte-unchanged**, and the surviving prose describes the script as an on-demand diagnostic. **RED**: either file appearing in this story's diff, or prose still describing the script as a gate. (Retiring a gate is not deleting an instrument.)
- [ ] **AC-10a**: **Trigger 7 survives as a COHERENT trigger, not a dangling diagnostic.** It states a yes/no question, and its "yes" has a stated consequence that routes somewhere live — the periodic refinement pass of AC-8. **RED**: trigger 7 reduced to "run the script and record the reading" with no yes/no; or a yes/no whose "yes" obliges nothing. (Design owner's Charge-2 finding, and it is a defect in the file's own internal contract rather than a stylistic preference: the header requires *"an explicit yes or no verdict"* per trigger and Assessment Procedure step 3 says *"If any trigger is 'yes': spawn `claude-architect`"*. A reporting-only trigger 7 would be the one trigger in eight whose verdict routes nowhere. **The fix is better than either a gate or a bare diagnostic, because the measurement now has somewhere to go:** trigger 7 measures the growth, trigger 8 contributes to it, and the periodic pass is where both land. Note this AC is what my AC-11 does NOT cover — AC-11 is about the count, and the dangling form would satisfy it.)
- [ ] **AC-11**: **No file under `CLAUDE.md`, `.claude/rules/`, `.claude/skills/` or `.claude/agents/` states a numeric count of the assessment's triggers.** The number lives only in the numbered list itself. **RED**: any **cardinal count of the set** — a numeral or number-word saying how many triggers there are — anywhere in those trees. **NOT RED: ordinal references naming a specific trigger**, which must survive: `context-layer-assessment.md` legitimately carries at least three (Assessment Procedure step 3's *"Triggers 7 and 8 additionally invoke the Learning-Loop Lifecycle"*, the Learning-Loop opener, and trigger 8's cross-reference to trigger 7), and stripping them would destroy the file's navigability. (An implementer sweeping numerals near "trigger" surfaces all of them; this is `doc-sweep.md`'s tombstone shape, where the hit looks accounted for whichever way it is ruled. Distinction added 2026-08-02 on the design owner's finding.) (Four sites carry it today. Three are edited by their owning stories — `implement/SKILL.md` Step 3a by E-280-02 AC-14, `product-manager.md` by E-280-04 AC-14, `workflow-discipline.md` by E-280-07 AC-8 — and this AC is the **global verification** that no fourth survives and no owner missed theirs. A count restated in four places goes wrong in four places at once the moment a trigger changes; removing the restatement removes a permanent class of future defect, which is this epic's design principle applied to its own instrument.)
- [ ] **AC-12**: **Trigger 8's promotions are RE-PRICED, not merely un-gated.** Condition (b) no longer references the ratchet baseline; condition (a) — the promotion must cite the specific defect it demonstrably caught — survives; **and a live pricing mechanism replaces (b)**, namely that promotions are reviewed at the periodic refinement pass of AC-8. **RED**: (a) removed; **or (b) removed with nothing in its place**, leaving a promotion subject to no cost at all. (⭐ **The highest-value finding of the design round, and my earlier version of this AC was too weak — it removed (b) and preserved (a) and stopped there.** The file states the consequence in its own words: an ungated "promote NOW" *"is what turned trigger 8 into a growth pump."* Retiring trigger 7 un-prices trigger 8 as a side effect, so retiring the gate without re-pricing promotions re-opens the exact failure the gate was built to close. A reader must be able to point at what prices a promotion now.)
- [ ] **AC-13**: **All FIVE** `ratchet` occurrences in `.claude/skills/implement/SKILL.md` (~511, ~519, ~529, ~532, ~537 as of 2026-08-02 — **re-derive by grep, they will move**) are **byte-identical across the whole epic diff**. **RED**: any of the five appearing in any story's diff. (All five refer to the reconcile-scoreboard instrument, already retired 2026-07-26 — a different thing from the gate this story retires. A token sweep for "ratchet" hits them and a careless pass edits history that is correct. This story does not own that file; this AC is a verification.)
  - ⚠️ **Rewritten from phrase-quoting to a count on 2026-08-02, because the quoting form was broken three ways and protected 1 of 5.** It quoted two sentences and said "the two further hits" when TN-15 now names five (so the arithmetic was wrong); the two quoted forms appear nowhere in lines 529, 532 or 537, so a byte-identical check on them said nothing about those; and **the first quoted phrase did not match the file at all** — it read *"retired 2026-07-26 with the ratchet"* while line 511 has `RETIRED` in caps, so a literal grep returns zero and an implementer could read that as either "absent" or "passed". The unprotected 532 is the *"not a ratchet"* line, the most misreadable of the five. A count is both complete and mechanically checkable; a quotation is neither.

## Technical Approach
Enumerate the sites by search **before** editing anything, and report the enumeration — AC-6 is checkable only against a pre-registered list, and reconstructing one from the diff afterwards is the failure mode this repo's 8-of-8 author-detection record predicts.

Epic TN-15 carries PM's own search results as a floor and, as importantly, the sites PM checked and ruled OUT with the reason. Read it before searching so the **five** `implement/SKILL.md` false positives do not consume a round. (⚠️ This sentence said "four" until 2026-08-02 — **the count-propagation defect this epic exists to fix, living inside the story that fixes it, and the third instance of it in this planning round.** The Context section and AC-13 were corrected and this line was not. Codex caught it.)

`.claude/rules/doc-sweep.md` governs the sweep, and its retired-claim section is directly on point rather than generically applicable: enumerate the **judgements that depended on the gate** — the ratings, the "there is no separate target" concessions, the cadence-versus-size framing — not merely the rephrasings of it.

The two cadences are different things and should read as different things: the batched audit cadence governs how often audits run; the periodic refinement pass governs how often the layer is deliberately pruned. Placing them in one file is a single-sourcing decision, not a claim that they are one mechanism.

## Dependencies
- **Blocked by**: **E-280-02, E-280-04, E-280-06, E-280-07.** This story runs **LAST**.
- **Blocks**: None.

⚰ **RETIRED 2026-08-02, do not restore:** ~~Blocked by: None. Both files are touched by nothing else in the epic. Position in the serial order is arbitrary — it may be moved anywhere in the order, or run first, without consequence.~~ **That was FALSE as written**, and Codex caught it. The claim was true of this story's *edits* — it owns two files nobody else touches — and false of its **verifications**, which are global and read files other stories change:
- **AC-11** requires zero numeric trigger counts across four trees. Three of the four sites are removed by **02 AC-14** (`implement/SKILL.md`), **04 AC-14** (`product-manager.md`) and **07 AC-8** (`workflow-discipline.md`). Run first, this AC fails on work not yet done.
- **AC-13** requires all five `ratchet` occurrences in `implement/SKILL.md` to be byte-identical **across the whole epic diff** — a claim about the finished diff, which does not exist until **02** and **06** have edited that file.

**The lesson, and it is the same one MF-1 taught at the other end of the epic:** a story's dependencies follow from what its ACs *read*, not only from what its edits *write*. A verification story looks independent right up until you ask what has to be true for its checks to mean anything. Encoded in structure here rather than asserted in prose, because prose is what got this wrong.

## Files to Create or Modify
- `.claude/rules/context-layer-assessment.md` (modify — TRIM 2 cadence line, trigger 7 gate retirement, trigger 8's baseline condition, the "Cadence and the ratchet" subsection, the periodic refinement pass)
- `.claude/rules/context-layer-guard.md` (modify — two sites, both in the justification position: the closing sentence of `## CLAUDE.md Scope` and the `## MEMORY.md Scope` paragraph)

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] The AC-6 regenerated site list with per-site written verdicts is committed as an artifact
- [ ] `.claude/rules/doc-sweep.md` three-step discipline applied to the retired gate
- [ ] No regressions in existing tests

## Notes
Agent-memory files (`claude-architect/epic-codifications.md`, `code-reviewer/`, `product-manager/operator-followups.md` and `MEMORY.md`) carry stale ratchet references. **They are not this story's work.** Each is reconciled at closure by its owning agent under the deletion-side eviction discipline; a hit in another agent's directory is flagged, never edited. Recorded as a closure obligation in epic TN-15.
