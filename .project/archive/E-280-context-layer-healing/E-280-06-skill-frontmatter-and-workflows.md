# E-280-06: Skill frontmatter ×10 and CLAUDE.md Workflows compression (DOCTOR c)

## Epic
[E-280: Context-Layer Healing](./epic.md)

## Status
`DONE`
<!-- DONE — flip performed 2026-08-02 AT CLOSURE by the successor PM, NOT at the time the work completed. Deliberately not backdated: this file read `TODO` for the whole of its dispatch and through the PM handoff, while the epic Stories table read `DONE`. That is the SECOND instance of epic TN-20 item 63 (an atomic status update performed at one of its two sites), on a second story, surviving four subsequent stories and a PM generation change. Full disclosure in epic TN-20 item 65 and in the Dispatch Scorecard. -->
<!-- CODE-REVIEWER VERDICT: APPROVED against frozen tree `fb1527279bc291a6170197534c079bd4fae0d5d6`. No MUST FIX, no SHOULD FIX — the ONLY findings-free per-story review of this dispatch. RECEIVED DIRECTLY FROM `cr-e280`, not through the orchestrator, at PM's insistence and CR's agreement (`"PM holding was correct and I would rather it held"`). CR sourced it from its own assignment and verdict lines — both written before E-280-09 existed — and DECLINED UNPROMPTED to infer the SHA from `fb15272` being 09's review base. Substance: AC-3 tested mechanically over all 34 quoted `## Workflows` phrases, seven of which appear in more than one file and every one resolving to exactly ONE `description:` declaration; AC-7's four `workflow-help` phrases resolve to its own frontmatter; AC-9's two bare-`main` diff bases fixed with all ten skills swept for a third and none found; AC-4's `compound_dispatch` and `handoff_from_plan` surviving with every cross-reference resolving. -->
<!-- CR corroborated by REPRODUCTION against the immutable tree, re-derived AT THE MOMENT OF SENDING (2026-08-02T06:08:36Z) rather than quoted from its earlier report: `.claude/skills` 3,341 lines · `CLAUDE.md` 20,205 B · 10 frontmatter blocks at line 1 · 10 `description:` lines · 0 surviving `Activation Trigger` strings · always-on load 84,388 at that tree. SIX for six. Re-derivable by anyone from the same tree object at any future point, which is what makes it recordable rather than believable. ⚠️ CR's own bound, recorded because it is the honest one and it cannot be closed by anything: it can establish that it SENT the verdict, NOT that it was delivered or recorded. PM's empty record and CR's held record are both true and not in conflict. -->
<!-- ⚠️ A NON-FINDING recorded so it is not later misread as one: the epic Dispatch Scorecard's row for this story read `UNRECORDED IN THE ARTIFACT` for a period, and that is NOT evidence CR's verdict failed to arrive. CR established the ordering from file metadata — `epic.md` mtime 06:08:10Z, CR's send 06:08:36Z — so the row was written 26 seconds BEFORE the verdict was sent. An absence that PREDATES the thing it appears to be an absence of. -->
<!-- CR's sixth figure independently settles a question elsewhere in this epic: `84,388` was a GENUINE measurement of the always-on load at `fb15272`, not a bad derivation. It went STALE when stories 07 and 08 ran. See epic TN-20 item 46a, where two earlier and wrong accounts of that figure's provenance are retired. -->
<!-- PM AC VERDICT: PASS on AC-1 through AC-9. ⚠️ ISSUED FIRST-HAND BY THE SUCCESSOR PM against the closure tree, NOT relayed and NOT inherited from the drained predecessor — a verdict is the one thing in this system that cannot be handed over. COVERAGE STATED RATHER THAN PAPERED OVER, per the practice this epic used on story 04. VERIFIED FIRST-HAND: **AC-1** — exactly 10 `SKILL.md` files, each carrying `name:` and `description:` in a frontmatter block opening at line 1. **AC-8** — countable at 10 and 0: 10 non-empty descriptions, and **0 surviving `Activation Trigger` strings anywhere under `.claude/`, established by ABSENCE with a positive control that fired 11 times in the same invocation**, so the zero is verified absence and not an unexplained empty. **AC-9** — both owned files read literally: `plan/SKILL.md`'s retired phrase is gone and its remaining staging references are about the PLANNING COMMIT, not a review surface; all four surviving `unstaged` uses in `codex-review/SKILL.md` belong to the standalone (non-WORKDIR) prompt-assembly path and **none equates unstaged content with the current story's changes**, which is what AC-9's RED actually tests. **AC-2** — form confirmed on a SAMPLE, not on all ten. NOT INDEPENDENTLY VERIFIED BY PM, resting on the implementer's artifact plus code-reviewer's independent read: AC-3's 34-phrase one-declaration mapping, AC-4's per-file written verdicts, AC-7's phrase reachability, and the two byte/line measurements (PM has no Bash, by design). -->
<!-- ⚠️ SCOPE OF PM'S CHECK, stated because it is not the same measurement CR made: PM verified against the CLOSURE tree, after stories 09, 07 and 08 ran; CR verified at `fb15272`. For AC-1, AC-8 and AC-9 the closure-tree result is the stronger one for the epic's purposes — the properties hold on the tree that ships — but it is a different measurement from CR's and is not offered as a reproduction of it. -->
<!-- An unexpected count arose during AC-1 and was treated as a cross-check trigger rather than a finding: `agent-standards/SKILL.md` returns TWO `name:`/`description:` pairs. Read literally, the second pair at `:35-36` is a TEMPLATE EXAMPLE (`name: agent-name`) inside the skill that documents the agent file format — an example, not a second declaration, the same discriminator AC-3's own ruling turns on. Not a defect. -->

## Description
After this story, all ten `SKILL.md` files carry `name` and `description` frontmatter and are harness-discoverable, the trigger phrases that currently live in three places live in one, and CLAUDE.md's Workflows section is a pointer rather than a restatement. This is net-subtractive despite adding frontmatter.

## Context
**Zero of ten SKILL.md files have YAML frontmatter** — confirmed independently by PM and by the design owner. Every one opens with `# Skill: <name>` / `**Category**` / `**Adapted for**`. No skill is harness-discoverable and all loading routes through CLAUDE.md prose.

The two halves are one restatement, not two chores. CLAUDE.md's `## Workflows` section is **seven bullets covering six skills** — *Curate the vision* is a bullet that invokes the product-manager in curate mode and names no skill — and each skill bullet restates the trigger phrases that belong in that skill's `description` frontmatter. The in-body `## Activation Triggers` sections restate them a third time — **133 body lines plus 10 headings** today. Adding the frontmatter creates the single source; collapsing the other two is what makes it single. Under the epic's design test in TN-7, doing only one half fails.

Arithmetic worth stating so the closure reading is not misread as growth: frontmatter adds 4 lines × 10 = 40 to `.claude/skills`, and the collapsed Activation Triggers sections remove roughly 143, for a net near **−100 lines**. CLAUDE.md is not in the four counted subtrees at all, so its compression scores zero on the line-count diagnostic while removing roughly 700 tokens from every agent's always-on load — which is the actual point, and an instance of the instrument mispricing the change (epic TN-4b). Note the framing: the context-layer size **gate was retired** by operator ruling on 2026-08-02 (epic OQ-1), so these numbers are reported and never judged. No subtree total can fail this story.

The reference format lives in the installed marketplace skills at `~/.claude/plugins/marketplaces/context-engineering-marketplace/skills/*/SKILL.md`: a `name` plus a `description` written in the form "This skill should be used when…".

## Acceptance Criteria

- [x] **AC-1**: All ten `SKILL.md` files carry a YAML frontmatter block beginning at line 1 with both `name` and `description`. **RED**: fewer than ten, or a block not starting at line 1.
- [x] **AC-2**: Each `description` is written in the harness-discoverable form and carries that skill's trigger conditions. **RED**: a description that states what the skill *is* without stating when it should be used.
- [x] **AC-3**: Every trigger phrase present in CLAUDE.md's `## Workflows` section before the change appears in **exactly one** location after it, and a worklist names the location per phrase. **RED**: a phrase in zero locations, or a phrase in two. (This goes red on a lost phrase and on a surviving duplicate — both failure directions.)
  - ⚠️ **OPERATIVE READING, ruled 2026-08-02: "location" means DECLARATION — exactly one `description:` declaration per phrase. The `workflow-help` cheat sheet is NOT a second location, and neither is a body mention, FOR THE SAME REASON: neither declares. AC-3 is not RED on either.**
  - **The reason needs no carve-out, and that is why it is the operative one (code-reviewer's, adopted over PM's first form).** Under a strict *occurrence* reading **AC-3 is UNSATISFIABLE by any correct artifact**: `plan/SKILL.md`'s body must say *"implement E-NNN"* — it tells the user what to type to dispatch, at three sites (`:547`, `:565`, `:708`) — and removing those would break the plan skill's own user-facing output and the very discriminator AC-3's resolution preserves. **Seven of the 34 phrases occur in more than one file; all seven resolve to exactly one declaration.** A reading no correct artifact can satisfy is not the reading the AC meant. **Verified first-hand by PM, not relayed: the three `plan/SKILL.md` sites were read.**
  - **PM's original behavioral discriminator is KEPT, for a different job**: *a location is a place consulted to determine behavior; a rendering that only displays, and declares its source, is not one.* It needs two conditions to stay honest and it **generalizes to renderings this epic has not seen**; the satisfiability argument needs no conditions but only proves what *this* AC must have meant. **Use the declaration reading to judge AC-3; use the behavioral rule for the next rendering that appears.**
  - **Why, in the AC's own terms rather than by exception.** AC-3 exists to stop a phrase being **declared** twice, because two authoritative declarations can disagree and the failure is a silent misroute. A cheat sheet is not consulted to route anything; it is read by a human, and a stale one fails **visibly** the moment someone types the phrase and nothing happens. **Different failure mode, different severity, and not the one this AC was written against.**
  - **The strict reading has a real cost and it decided nothing, but it confirms the direction**: counting a rendering as a location forces the cheat sheet to print phrase *categories* instead of literals — **a cheat sheet that will not tell you what to type**, which destroys the artifact's only function to satisfy a criterion aimed at something else.
  - **Two conditions, so this is a discriminator and not a loophole**: the rendering must **declare its source** (the Maintenance section does, and was updated to say so explicitly), and it must **not be consulted for routing**. A rendering that became authoritative *would* count.
  - **Same calibration as E-280-04's AC-9b**: forbid the harmful thing, not the category containing it. There, the defect was a false member inside a true group, not grouping itself; here it is a second *declaration*, not a second *appearance of the string*.
- [x] **AC-4**: Every SKILL.md gets a **written verdict** on its in-body `## Activation Triggers` section — removed, retained with a stated reason, or absent to begin with. **RED**: a SKILL.md with no verdict, or a file carrying both a frontmatter `description` and an in-body triggers section with no reason recorded for the duplication.
- [x] **AC-5**: The `.claude/skills` subtree line count is strictly lower after the change than before, both figures recorded. **RED**: not lower. (This is a property of this story's own edit, not a baseline check — the size gate is retired, nothing is offset, no exception exists or is needed. Its RED is the one that matters: if the count is not lower, the Activation Triggers collapse did not happen and only the additive half landed.)
- [x] **AC-6**: CLAUDE.md's byte size is strictly lower after the change, and both figures are recorded. **RED**: after ≥ before.
- [x] **AC-7**: No skill becomes unreachable. Every trigger phrase present before the change resolves, after it, to **either** a frontmatter `description` on the skill it triggers **or** the CLAUDE.md Workflows pointer. **RED**: a phrase whose post-change location is neither. (Reworded 2026-08-02: the previous RED was *"a file the harness does not consult for that phrase"*, which **no reviewer in this repo can evaluate** — harness skill-discovery behavior is documented nowhere here, so the criterion rested on knowledge nobody has. The two named locations are checkable by reading.)
- [x] **AC-8**: **Countable at 10 and 0**: all ten skills have a non-empty frontmatter `description`, and zero skills retain an in-body `## Activation Triggers` section. **RED**: fewer than ten descriptions, or any surviving triggers section. (This exists because AC-3 is **silently vacuous for four of ten skills** — agent-standards, context-fundamentals, filesystem-context and multi-agent-patterns are not user-triggered and have no CLAUDE.md bullet to enumerate against. Their descriptions derive from their in-body triggers instead, and without this AC an implementer could satisfy AC-3 completely while leaving those four untouched.)
- [x] **AC-9**: **Review-surface invariant, in the two skills this story owns that carry it.** `.claude/skills/plan/SKILL.md` and `.claude/skills/codex-review/SKILL.md` no longer define the review surface as unstaged working-directory state. Every site is enumerated with a **written verdict**, `no change needed` included. **RED**: any surviving sentence equating unstaged content with the current story's changes, or an enumerated site with no verdict. Known sites, a **floor not the list**: `plan/SKILL.md`'s *"Review the current story's changes via … (unstaged changes = current story)"* — a verbatim instance of the exact retired phrase — and `codex-review/SKILL.md`'s staged/unstaged diff-assembly template plus its *"standalone (non-WORKDIR) staged/unstaged path"* reference in the large-refactor guidance. (⚠️ **Same reason as E-280-04 AC-15**: E-280-07 AC-1c verifies this layer-wide and cannot edit these files. Note this is a **body** edit, unlike the rest of this story — it does not conflict with the line-1 frontmatter work, and both files are already in the Files list.)
⚰ **AC-10 and AC-11 MOVED OUT 2026-08-02 to [E-280-09](./E-280-09-seam-depth-and-freeze-scope.md), where they are its AC-1 and AC-2. Nothing was dropped and no criterion weakened.** Both were added to this story during dispatch — AC-10 from E-280-04's unsatisfiable AC-8 limb, AC-11 from the epic TN-20 item 22 mechanism gap — and this story became four distinct bodies of work. **They are narrow, deep, `implement/SKILL.md` seam work; this story is wide, shallow and mechanical, and squeezing a mechanism design in behind ten frontmatter edits is how it gets designed badly.** E-280-09 runs immediately after this story and before E-280-07. Split reasoning: epic TN-20 item 35.

<!-- Superseded AC-10/AC-11 text removed rather than left in place: their full content, with every rationale bullet, lives in E-280-09. Two copies of a criterion is the restatement defect this epic exists to remove, and a tombstone carrying the criterion is still a second copy. -->


## Technical Approach
Enumerate the trigger phrases from CLAUDE.md's Workflows section before editing anything — AC-3 is checkable only against a pre-change enumeration, and reconstructing it from the diff afterwards is the failure mode the repo's 8-of-8 author-detection record predicts.

**FOUR skills are not user-triggered and have no CLAUDE.md Workflows bullet** — `agent-standards`, `context-fundamentals`, `filesystem-context`, `multi-agent-patterns`. They still need frontmatter under AC-1; their `description` states the load condition their consuming agent uses rather than a user phrase, and AC-8 is what keeps them from being silently skipped.

**Do not read "seven bullets" as "seven skills."** CLAUDE.md's Workflows section has seven bullets, but one of them — *Curate the vision* — invokes the product-manager in curate mode and names no skill. So the section covers **six** of the ten skills, not seven. (An earlier draft of this story said three-and-seven; it was wrong in both halves, and the arithmetic is worth re-checking against the file rather than trusting either version.)

Verify the marketplace reference format by reading one of those files rather than taking the shape from this story.

## Dependencies
- **Blocked by**: E-280-02 (both modify `.claude/skills/implement/SKILL.md`; the line-1 frontmatter insert is the safest edit to land last, and the trigger-body removal must see E-280-02's final text)
- **Blocks**: **E-280-07** — its AC-1c verifies layer-wide that the "unstaged = current story" invariant is gone, and AC-9 here removes this story's instances in `plan/SKILL.md` and `codex-review/SKILL.md`. **E-280-08** — its AC-13 requires the five `ratchet` sites in `implement/SKILL.md` to be byte-identical across the finished epic diff, and this story edits that file.

## Files to Create or Modify
- `CLAUDE.md` (modify — the `## Workflows` section)
- `.claude/skills/plan/SKILL.md` (modify)
- `.claude/skills/implement/SKILL.md` (modify)
- `.claude/skills/ingest-endpoint/SKILL.md` (modify)
- `.claude/skills/codex-review/SKILL.md` (modify)
- `.claude/skills/codex-spec-review/SKILL.md` (modify)
- `.claude/skills/workflow-help/SKILL.md` (modify)
- `.claude/skills/agent-standards/SKILL.md` (modify)
- `.claude/skills/filesystem-context/SKILL.md` (modify)
- `.claude/skills/context-fundamentals/SKILL.md` (modify)
- `.claude/skills/multi-agent-patterns/SKILL.md` (modify)

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] The AC-3 trigger-phrase worklist is committed as an artifact
- [ ] `/workflow-help` cheat sheet checked for consistency with the new single source
- [ ] No regressions in existing tests
