# E-280: Context-Layer Healing — Shrink the Error-Generating Surface

## Status
`ACTIVE`
<!-- READY set 2026-08-02; ACTIVE set 2026-08-02 on operator dispatch authorization. The 60-day READY freshness gate no longer applies (ACTIVE epics are exempt). -->
<!-- Dispatch requires separate operator authorization (Dispatch Authorization Gate). -->
<!-- PRE-DISPATCH PREREQ: delete the two tombstones E-280-03 and E-280-05 with plain `rm`, NOT `git rm` — this epic directory is untracked, so `git rm` errors and the skipped command then lets `git add -A` commit them. Main owns this at staging. -->
<!-- CLOSURE OBLIGATIONS: (1) TN-4(a) per-subtree diagnostic reading, reported not gated — SEE THE MANDATORY CAVEAT IN TN-4(c) BELOW, which must be stated in the reading itself; (2) TN-15's four memory-reconciliation items, two of which (code-reviewer, software-engineer x2 files) need ideas with resolution triggers because those agents are not on the dispatch team; (3) the standard documentation + eight-trigger context-layer assessments; (4) `.claude/agent-memory/claude-architect/epic-codifications.md:112` describes the E-267 case as "cited as a CONCRETE CASE" in `tool-output-integrity.md`; after E-280-01 it is a casebook pointer. CA's own directory, so it reconciles at the closure authoring window (surfaced by the E-280-01 implementer, correctly classified as not story work). -->
<!-- NOTHING OPEN AT READY. Codex P1-2 (report-ceiling values) was the last item and is CLOSED: the design owner measured the reports directly, found no report-length regression, and the ceiling landed as a 6,000-char tail guard labelled an ESTIMATE with code-reviewer excluded. TN-19 carries the measured tables inline. -->

## Overview
The fleet's wall-clock cost per shipped story roughly tripled between early July and late July 2026, and two independent transcript analyses agree the cause is the process layer, not the model. This epic implements the four operator-ruled trims, two structural cleanups, and — added 2026-08-02 — the retirement of the context-layer size gate in favour of a scheduled periodic refinement pass, under one binding design principle: **every change must SHRINK the error-generating surface** — fewer restatements, structure in place of prose, verification at authoring time — rather than merely cutting review cost. Restatement is this repo's dominant defect generator, so each fact this epic touches ends up stated once with pointers to it.

## Background & Context

### What was measured
Two independent analyses of the same transcript corpus, with different scripts and different metric definitions, compared only after both were complete. **Both source files are ON DISK, UNTRACKED, and EPHEMERAL by operator ruling — they will be deleted.** Every number this epic relies on is therefore quoted inline in Technical Notes TN-1 and TN-2 rather than cited by path. Their verdict ordering: **process ≫ pathology > model ≈ 0**.

The E-279 dispatch was then decomposed directly (TN-3) and showed the same shape at story grain: 66% of active time was review and verification, and one story spent 14 minutes on implementation against a 94-minute verification tail.

### Why this is a context-layer epic, not a policy memo
The cost lives in files. `.claude/rules/` grew from 165KB to 307KB between 2026-06-27 and 2026-07-29 (TN-2), and median end-of-session main-agent context grew 391k → 554k tokens across the same window. The trims are edits to specific rules, skills, and agent definitions; the design owner (claude-architect) locates the seams and picks the mechanisms, and PM frames the acceptance criteria and owns scope and sequencing.

### What this epic is NOT reacting to
The model. Reverting the fleet to Opus 4.8 has a measured upside of **at most 1.4x on dispatch cost, and that figure is a ceiling confounded with epic mix** (TN-1). Corrected against a constant-model bridge control, Opus 5 is at parity or slightly faster (TN-2). Opus 5 stays.

## Goals
- Each of the four trims is expressed as a **property of the workflow files** that a reader can check against the files alone, without knowing who edited them or in what order.
- The delivery-confirmation economy is single-sourced **one-of-each**: exactly one completion report, one review verdict from code-reviewer, and one AC verdict from PM per story, each issued once against a frozen story state and not re-askable. PM's AC authority is preserved (OQ-5, operator-ruled).
- Per-story review effort is tiered by **file-path class** — a mechanical test — never by an agent's judgment call about risk.
- The heaviest always-loaded rule file is materially smaller in bytes with **every rule it states still present**, verified by a per-rule inventory rather than by an impression of the diff.
- All ten `SKILL.md` files carry `name` and `description` frontmatter, and CLAUDE.md's Workflows section is compressed without losing a trigger phrase.
- The context-layer size **gate** is retired and replaced by a scheduled periodic refinement pass; the measurement survives as an on-demand diagnostic, and no file in the layer still describes a gate, a baseline, or an exception ritual that no longer exists.

## Non-Goals
- **Reverting the model.** Opus 5 stays; see Background.
- **Deleting audits.** TRIM 2 changes audit *cadence* only. The primary analysis rates this trim's quality risk as MEDIUM in its own words — *"these audits found a CRITICAL; reduce frequency, don't delete."*
- **Modifying any file under `epics/E-271-workflow-process-redesign/` from an E-280 STORY.** The narrow exception, executed by PM at planning time under fresh operator authorization and outside the story set, is E-271's own status demotion to DRAFT plus its dated History entry — ordered by the operator in the same OQ-A ruling. No story touches that directory. See TN-6.
- **Distilling any rule file other than `.claude/rules/tool-output-integrity.md`.** Distillation authority is file-scoped and operator-granted for that one file. A second file is a second authorization.
- **Removing any RULE.** The distillation compresses incident narratives to pointer/evidence form; the rules themselves all survive.
- **Re-deriving the wall-clock analysis.** The evidence is settled; this epic verifies citations, it does not re-measure.
- **Re-snapshotting the ratchet baseline, or offsetting against it.** The operator retired the GATE outright (OQ-1/OQ-B), so there is nothing to re-snapshot and nothing to offset. No story runs `--update-baseline`, manufactures deletions to buy instrument credit, or seeks a signed exception. What E-280-08 does is retire the gate's own prose; what the remaining stories do is *report* their subtree deltas as diagnostics.
- **Deleting `context-ratchet.sh` or its baseline file.** Retiring the gate is not deleting the instrument. The script survives as an on-demand diagnostic, mirroring the 2026-07-26 reconcile-scoreboard precedent where the measurement was kept and only the one-way gate retired. Whether the now-unread baseline JSON is eventually removed is a later decision, not this epic's.
- **Weakening the per-claim verification discipline.** The trims cut the NUMBER of verdicts, never the rigor of one. See TN-3, "The residual floor."
- **Resolving E-279's own ratchet figure or its exception record.** See OQ-1; that is an E-279 follow-up.
- **Registering either manual diagnostic as a hook.** `context-ratchet.sh` and `dispatch-telemetry.py` both say so in their own headers.

## Success Criteria
1. Every trim's landed state is checkable by reading the named files, with no reference to this epic's process.
2. `.claude/rules/tool-output-integrity.md` is smaller in bytes than its pre-epic size, and a per-rule inventory shows a surviving location for every rule present before the change.
3. The epic's own context-layer size change is **reported with per-subtree numbers and their causes** at closure, as a diagnostic reading. It is a report, not a gate: no per-subtree number can fail this epic, and none is offset or excepted.
4. **No file under `CLAUDE.md`, `.claude/rules/`, `.claude/skills/` or `.claude/agents/` still asserts a context-layer size gate, a committed baseline that bounds growth, or an operator-signed-exception ritual.** Checkable by reading the named files; the seam inventory is TN-15. (The trees are named literally because "the counted surface" was the retired instrument's own term for itself, and a criterion phrased in a retired vocabulary is one nobody can check.)
5. No file under `epics/E-271-workflow-process-redesign/` is modified by any E-280 **story**. (PM's operator-ordered demotion of that epic's status is a planning action outside the story set — see Non-Goals and TN-6.)
6. **The number of context-layer assessment triggers is stated in exactly one place — the numbered list itself.** Four sites state it today; each is edited by the story owning its file, and E-280-08 AC-11 is the global verification. This criterion spans four stories, which is why it is recorded at epic level rather than left to any one of them.

## Stories

| ID | Title | Status | Dependencies | Assignee |
|----|-------|--------|-------------|----------|
| E-280-01 | Distill `tool-output-integrity.md` — every rule survives, narratives become pointers (DISTILLATION) | DONE | None | claude-architect |
| E-280-02 | One-of-each verdicts, frozen review surface, tier routing table — `implement/SKILL.md` (TRIM 1 skill-side + TRIM 3 routing) | DONE | None | claude-architect |
| E-280-04 | Report schemas with ceilings and code-reviewer tier keying (TRIM 4 + TRIM 3 keying) | DONE | E-280-02 | claude-architect |
| E-280-06 | Skill frontmatter ×10 and CLAUDE.md Workflows compression (DOCTOR c) | DONE | E-280-02 | claude-architect |
| E-280-09 | Strip review depth from the routing seam; scope the frozen-state check (`implement/SKILL.md` seam work) | DONE | E-280-06 | claude-architect |
| E-280-07 | TRIM 1 rule-side — bound verdict re-issuance, reduce the insurance prose, verify the invariant is gone layer-wide | DONE | E-280-02, E-280-04, E-280-06, **E-280-09** | claude-architect |
| E-280-08 | Retire the size gate, state the periodic refinement cadence, state the batched audit cadence (TRIM 2 + OQ-1/OQ-B) + terminal global verifications | DONE | E-280-02, E-280-04, E-280-06, **E-280-09**, E-280-07 | claude-architect |

**ALL SEVEN STORIES ARE `DONE` (2026-08-02).** Remaining: Codex over the whole epic diff → code-reviewer's closure integration review → the full-suite green gate → closure merge with operator approval. **The outgoing PM drained after E-280-08's verdict; the closure package is handed to a fresh PM instance against TN-21, which is the authoritative handoff.**

**Retired story numbers — never reused.** `E-280-03` and `E-280-05` were written from the design owner's first decomposition and were superseded before dispatch by its restructured, file-disjoint one. Both files are tombstones in place and **must be deleted before the planning commit**; each names the successor AC for every criterion it carried, and nothing was dropped.

⚠️ **Delete them with plain `rm`, NOT `git rm`.** This epic directory is **untracked** — it has never been committed — so `git rm` errors with "did not match any files". The failure mode is silent and gets the tombstones committed: the erroring command looks like a no-op, gets skipped, and the subsequent `git add -A` then **adds** both files instead of removing them. (Corrected 2026-08-02; the instruction had read `git rm` since the tombstones were created.)

**Both of E-280-05's successor pointers are stale, and neither can be fixed in a file that is being deleted, so they are corrected here.** It names TRIM 2's successor as *E-280-04 AC-10 through AC-13*. Wrong on both axes now: the story is **E-280-08**, and the criteria are its **AC-1 through AC-4** — E-280-04's AC-10 to AC-13 no longer exist, since TRIM 2 moved out when the operator's gate-retirement ruling landed on the same file (`.claude/rules/context-layer-assessment.md`) and made a dedicated cadence story the file-disjoint home for both.

**Sequencing.** Stories run serially: `01 → 02 → 04 → 06 → 09 → 07 → 08`. **E-280-09 was created 2026-08-02 by splitting E-280-06 mid-dispatch** — 06 had accumulated two seam/mechanism criteria (from E-280-04's unsatisfiable AC-8 limb and from TN-20 item 22) on top of frontmatter ×10 and the CLAUDE.md compression, making it four distinct bodies of work. **09 runs after 06 because both edit `implement/SKILL.md`** (06's is line-1 frontmatter, 09's is body seam work) **and before 07, whose AC-1c reads the finished state.** Reciprocal edges are stated in the Stories table, in both story files, and here — three places, deliberately, because this epic has already produced one phantom edge and one false independence claim, and both came from an edge recorded in only one place. Full reasoning: TN-20 item 35. **The last two are terminal verification stories and their order is load-bearing, not incidental** — 07 verifies the review-surface invariant is gone layer-wide, then 08 verifies the trigger count and the untouched `ratchet` sites across the finished diff. The decomposition is deliberately file-disjoint — each story owns its files exclusively, and **only one file is touched twice**:
- `implement/SKILL.md` by 02 (body) and 06 (line-1 frontmatter plus trigger-body removal). 02 precedes 06 because the frontmatter insert is the safest edit to land last and 06's trigger-body removal must see 02's final text.
- 04 after 02 because its rubric keying keys off the tier table 02 defines.
- **07 moved from second to LAST on 2026-08-02, and the reason is a defect the spec audit caught rather than a preference.** Its AC-1 demanded zero instances of the retired "unstaged = current story" invariant across three trees, but seven instances live in files owned by 04 (`code-reviewer.md`) and 06 (`plan/SKILL.md`, `codex-review/SKILL.md`) — both of which ran *after* it. The AC was unsatisfiable by the decomposition, and no AC anywhere removed those seven sites. Fixed three ways at once: 04 gains AC-15 and 06 gains AC-9 to remove their own, 07's AC-1 narrows to the files it owns, and 07's new AC-1c is the layer-wide verification, now reachable because 07 runs last. 07 still follows 02 for the original reason — the rule-side statement must match the skill-side mechanism.
- **08 runs LAST and is blocked by 02, 04, 06 and 07** — not because of its edits, which touch two files nobody else does, but because of its **verifications**. AC-11 checks that no numeric trigger count survives anywhere, and three of the four sites are removed by other stories; AC-13 checks the five `ratchet` sites in `implement/SKILL.md` are byte-identical **across the finished epic diff**, which does not exist until 02 and 06 have edited it. ⚰ **This bullet previously said 08 "has no dependency on anything and its position is arbitrary… may be moved anywhere in the order, or run first, without consequence." That was FALSE** — Codex caught it, and it is MF-1's lesson at the other end of the epic: **a story's dependencies follow from what its ACs READ, not only from what its edits WRITE.** A verification story looks independent until you ask what must be true for its checks to mean anything.

01 goes first: largest single win, and zero dependencies.

**A stale dependency edge is struck here, in the direction opposite to the one it first appears to need.** E-280-01 recorded `Blocks: E-280-02 (which amends this file's two-cause differential once the freeze-tree mechanism exists)`, and its Handoff Context promised 02 that section. That is residue from the FIRST decomposition. The restructured 02 says the opposite in as many words — *"Do not touch `.claude/rules/tool-output-integrity.md`… If the freeze makes something genuinely dead, that is a later epic's finding"* — and 02's Files list contains one file, which is not that one. The two stories share no file and pass no artifact, so **01 and 02 are independent**; the reciprocity defect was a phantom edge, not a missing one. Both sides are corrected in the story files.

## Dispatch Team
- claude-architect

<!-- Every scoped file is a context-layer path (.claude/rules, .claude/skills,
     .claude/agents, CLAUDE.md), so the Routing Precedence rule in
     .claude/rules/agent-routing.md routes all of it to claude-architect
     regardless of any other signal. -->

## Technical Notes

### TN-1: Primary evidence, quoted inline (source file is ephemeral)
From `2026-07-29-wallclock-model-analysis.md` (first analyst, written 13:38Z 2026-07-29; parsed all 68 session transcripts plus every subagent transcript, nothing sampled). **Quoted here because the file will be deleted.**

| Measurement | Value |
|---|---|
| Throughput, Jul 06–12 (Opus 4.8) | 50.4 active hours, 75 stories closed → **1.49 stories/active-hour** |
| Throughput, Jul 24 20:22 – Jul 29 (Opus 5 + Fable) | 48.9 active hours, 24 stories closed → **0.49 stories/active-hour** |
| Per-turn pace, role-matched sec/turn | PM 21.2 → 18.7 · SE 21.1 → 22.0 · CR 27.7 → 28.2 (**flat**) |
| Output per turn, role-matched | PM 157 → 305 · SE 149 → 309 · CR 73 → 189 tok/turn (**~2x**) |
| Model-effect ceiling | E-270 (first Opus 5 dispatch, pre-amendment process): 0.58 act-h/story, 190 turns/story vs the 4.8 median 0.40 / 108 → **~1.4x, a ceiling and not a point estimate** |
| Process effect, model held constant | sm/story within the Opus 5 era: 25 (E-270, 7/24) → 56 (E-278, 7/28) → 174–203 (E-276/E-277) |
| Audit/meta share of active hours | **~35% recent (17.1 of 48.9h) vs ~3% in the peak 4.8 week (~1.4 of 50.4h)** |
| Pathology (resend + chase traffic) | 6.4–9.9% of sends in recent dispatches, 18.9% in the mixed hub session; estimated **5–15% of dispatch active time**; 27–96 pathology messages per Opus 5 dispatch vs 1–20 per 4.8 dispatch |

Its own trim estimates, which are this epic's expected-value baseline:
- **Trim 1** (dedupe the delivery-confirmation economy): 0.2–0.5 act-h per dispatch plus most of the 5–15% pathology tax. Quality risk **minimal** — it trims traffic the repo's own rules already call unverifiable.
- **Trim 2** (cap the audit/meta share): 5–10 active hours/week at current cadence. Quality risk **medium**.
- **Trim 3** (risk-tier the per-story review loop): 0.1–0.3 act-h/story on the lighter half of stories. Quality risk **medium-low if the tiering is by file-path class rather than judgment** — this conditional is why TRIM 3's AC must key on paths.
- **Trim 4**: keep Opus 5; cap report length for long-lived agents instead.

### TN-2: Replication evidence, quoted inline (source file is ephemeral)
From `2026-07-29-wallclock-model-analysis-replication.md` (second analyst, independent scripts, different metric definitions, compared only after both were complete).

| Measurement | Value |
|---|---|
| Raw per-call wall | Opus 4.8 14.9s (n=34,210 calls) vs Opus 5 15.6s (n=16,575) — **+5%** |
| Sonnet-5 bridge control | A **constant** model got **+2.2 s/call slower (+21%)** across the era boundary at identical output per call, while Opus moved only +1.1s — ambient drift. Corrected against the bridge, Opus 5 is at parity or slightly better. |
| The doubling predates the model | calls/story 132 (early-4.8) → 221 (late-4.8) → 491 (opus5); h/story 0.53 → 1.01 → 1.33. **The per-story cost doubled entirely within the Opus 4.8 era.** |
| E-276 is the outlier | Excluding it, the opus5 cohort is 1.00 h/story — parity with late-4.8's 1.01 |
| Machinery growth, git-dated | `.claude/rules/` 165KB (6/27) → 203 (7/12) → 224 (7/24) → **307KB (7/29)**; combined rules+CLAUDE.md 190 → 274 → 329KB; median end-of-session main-agent context 391k → 445k → **554k tokens** |

**One divergence between the two analyses, and it is load-bearing for TRIM 4.** The primary reports ~2x output per turn role-matched; the replication's per-call means across *all* calls show only +11% (935 → 1,041 tok/call). Not a contradiction — different populations — but the replication's own conclusion is that *"the 'Opus 5 is 2x more verbose' claim should be scoped to subagent report-writing turns, not all traffic."* **So TRIM 4 is correctly aimed at long-lived agents' REPORTS and would be mis-scoped as a cap on output generally.**

### TN-3: E-279 dispatch decomposition (RELAYED, dated evidence)
Measured 2026-08-01 from session `fd615ae6-f076-4675-8b8c-a2bf6321b47e`. **Provenance matters: this is relayed to PM, not re-derivable from the repo, and is dated evidence rather than a standing property.**
- 5.0 active hours for 5 stories — **1.00 h/story** — of which **66% was review and verification**.
- Story 01: **14 minutes implementation vs a 94-minute verification tail**, with **8 CR verdicts** and **7+ re-verifications of a single AC**.
- **106 sends/story**, of which **15–20% of all sends are delivery or state insurance**.
- **891k output tokens** across the one 5-story dispatch — the TRIM 4 figure.
- All five stories were **shell, markdown, or memory** files; on a file-path tier, **three of the five would have been single-pass** — the TRIM 3 figure, and note it is a counterfactual estimate rather than a measurement.
- The trims are estimated to remove **30–50%** of that overhead.

**The residual floor, stated so no story tries to cut it.** What remains after the trims is the **per-claim verification discipline** — checking a claim against the artifact that would produce it. That is deliberately untouched by this epic. TRIM 1 bounds how many TIMES a verdict is issued and re-issued; it does not weaken what a single verdict must establish. A story that reduces the rigor of one verification, rather than the number of verifications, has misread the scope.

TN-3 is the sharpest single justification for TRIM 1: eight verdicts and seven re-verifications of one AC is not thoroughness, it is an unbounded confirmation loop, and the fix is a bound (**one verdict of each kind — see OQ-5, which settled this as one-of-each rather than one-in-total**) plus a frozen target (verification against a fixed story state so re-verification has nothing to chase). Note what the measured harm was and was not: eight verdicts from **one** reviewer. The count, not the number of reviewers, is what this trim attacks.

### TN-4: How the retired gate was priced, and what survives of this note
⚠️ **The GATE this note describes was retired by operator ruling on 2026-08-02 (OQ-1/OQ-B). Read everything below as a description of an instrument, not of a constraint.** Nothing in E-280 must clear a baseline, offset a subtree, or obtain an exception. **What survives and is still load-bearing:** the per-subtree delta *forecasts* in (a), which the stories still record as diagnostics and which feed Success Criterion 3; and the whole of (b), the byte-versus-line divergence, which is one of the two reasons the gate was worth retiring and which still governs how E-280-01 measures its own win. **What is dead:** every "clears / cannot clear" verdict in the table below, the declaration-and-exception framing, and the closure reconciliation against OQ-1/OQ-2.

`.claude/hooks/context-ratchet.sh` — note the path is `.claude/hooks/`, not `scripts/` — counts **lines** of `*.md` + `*.sh` across four subtrees and, while it was a gate, failed if **any single subtree OR the total** exceeded baseline (its `check()` sets `status=1` on `cur > base`, and is called once per subtree and once for the total). Committed baseline at `.project/baselines/context-layer-ratchet.json`, generated `2026-07-28T15:09:40Z`:

| Subtree | Baseline lines |
|---|---|
| `.claude/rules` | 2640 |
| `.claude/agents` | 1869 |
| `.claude/skills` | 3269 |
| `.claude/agent-memory` | 10442 |
| **total** | **18220** |

**(a) Expected per-subtree deltas — still recorded, no longer judged.** PM's first forecast was WRONG in one direction, in the epic's favour: PM predicted `.claude/skills` would grow on the frontmatter, and the design owner measured it ending **net-negative**, because the frontmatter `description` replaces the in-body `## Activation Triggers` sections it duplicates. The forecast, with the retired verdict column struck:

| Subtree | Expected delta | ~~Gate verdict (RETIRED)~~ |
|---|---|---|
| `rules` | distillation −40 to −55, E-280-07 deletions ≈ −25, E-280-08 cadence lines ≈ +1 → **≈ −70** | ~~Clears comfortably~~ |
| `skills` | frontmatter **+40**, Activation-Triggers collapse **−143**, E-280-02 deletions ≈ −20 → **≈ −120** | ~~Clears~~ — but the substance survives the gate: the frontmatter is not a growth item once the triggers it replaces are removed, which is the one-restatement argument paying off |
| `agents` | E-280-04 schemas + tier keying ≈ **+40** | ~~Cannot clear~~ — and this is now simply a number to report. Note what the retirement bought: the "no offset available" problem is gone, and with it the temptation to manufacture deletions |
| `agent-memory` | already **+374** before E-280 writes a line; this epic's own planning and closure writes add more | ~~Structurally cannot clear~~ — the asymmetry that made this subtree unfixable by any amount of care is precisely what the operator ruled the gate was not worth |

**CLAUDE.md is not in the counted surface at all**, so the Workflows compression scores zero on this instrument — a fact about the instrument, and never a reason to skip the compression.

**Closure obligation arising from this note, owned by PM and not by any single story.** Stories 01, 04, 06 and 08 each record their own subtree delta; nobody aggregates them. At closure PM states the per-subtree reading once — the delta per subtree with its cause — as a **diagnostic**. Success Criterion 3 is satisfied by that statement. There is nothing to reconcile it against: OQ-1, OQ-2 and OQ-B are closed and no per-subtree number can fail this epic.

**(b) The instrument under-prices exactly what we are distilling — and this half is UNAFFECTED by the retirement.** `.claude/rules/tool-output-integrity.md` is **98 lines** (counted) at a relayed ~29KB — roughly 300 bytes per line, because it is long-paragraph prose. Line-priced, the heaviest always-loaded file in the repo is 3.7% of the rules subtree. A distillation that halves its bytes moves the line count by a few dozen and looks trivial while removing the real context cost. **The distillation story therefore measures bytes directly and treats the line count as a secondary diagnostic;** a line count is the wrong instrument for prose whether or not anything gates on it. Retiring the gate removes the pressure to optimize for the wrong number — it does not make the wrong number right.

**(c) MANDATORY CAVEAT for the closure diagnostic: the instrument prints the word `FAIL`, and it is not about this epic.** Added 2026-08-02 during E-280-01's review, before the reading is written, because this is precisely the retired-claim residue shape `.claude/rules/doc-sweep.md` describes — a later reader finds `FAIL` in the closure record and reconstructs a gate that no longer exists.

`.claude/hooks/context-ratchet.sh` prints `.claude/rules 2640 → 2642 +2 << GROWTH` under a banner reading **`FAIL -- the context layer grew past baseline`**. That comparison is against the **committed baseline of 2,640** (snapshotted 2026-07-28) — *not* against any story's pre-change state. E-280-01's AC-6 criterion is "strictly lower after the change than before it", i.e. **2,649 → 2,642**, and nothing in AC-6 is measured against the baseline. **The gate is retired (OQ-1/OQ-B); nothing is offset, no exception is owed, and no per-subtree number can fail this epic.** The closure reading MUST state that the word `FAIL` in that output is a fact about a stale baseline, not a verdict on this story or this epic.

**One corroboration to keep in the same reading, because it strengthens TN-9 rather than merely explaining an artifact.** The script now puts `.claude/rules` at baseline **+2**, which places the pre-change state at baseline **+9** — exactly the `rules +9` component of TN-9's gate-ending `9 + 0 + 35 + 374 = 418`. That figure is now producible from a second, independent direction, which is the producibility check this repo's record asks for rather than the agreement-with-the-conclusion check it warns about.

**Which figures this note pins, and which it deliberately does not.** Everything above is a LINE count, and line counts are stable across E-280-01's remediation rounds (AC-6 sat at 2,642 through round 1 and round 2 alike), so this note does not go stale per round. **BYTE figures are deliberately absent from it.** They moved once already — the rule file read 22,889 B at round 1 and 23,172 B after remediation, and there is no reason to assume the last round is the last. The closure per-subtree diagnostic takes its byte figures from **code-reviewer's final measurement against the closed story**, never from a round's completion report; recording an interim byte value here would create exactly the stale-absolute problem the criterion-vs-evidence cut says to avoid by removal rather than by update.

### TN-20: Dispatch findings, recorded as they land (for the closure record and the context-layer assessment)
Findings that emerge DURING dispatch, written here as each story closes rather than reconstructed at closure from message threads. A finding reconstructed from a thread is a summary of a summary; this section is the durable form. **Item 1 is the standing codification candidate for this epic** — CA, CR and PM nominated it independently — and it should be handed to claude-architect at the eight-trigger assessment.

**From E-280-01 (DONE 2026-08-02):**

1. **An enumeration checked against its own generating definition cannot detect a member that definition excludes.** AC-1 defined a rule as "any numbered rule or bolded rule-sentence" and its RED-b was "a rule present in the pre-change blob with no row." **RED-a was mechanically checkable and was checked; RED-b is unverifiable BY CONSTRUCTION under any check that reuses the definition that built the list.** The missed member — an unbolded, unnumbered directive riding the tail of a narrative sentence — was found by code-reviewer with a *different instrument* (a directive-sentence sweep), which is the only way it could have been found. **Owner named, because the finding is worthless if it reads as an implementer's excuse: AC-1 is PM's, and its definition under-matched.** The distilled file's own words are the diagnosis — *under-match is silent, over-match is visible*, and prefer an over-matching enumeration with a resolution step *only while unresolvable sites must be REPORTED*. AC-1 was an under-matching enumeration with no resolution step, governing the distillation of the rule that says not to write one. Disposition: no AC went RED (the clause is out of scope by AC-1's letter, so PM ruled AC-1 PASS), and the gap is real against AC-1's intent, so the row was added post-hoc and labelled.

2. **Three instruments ran in this story and none of them worked on the first attempt; all three were caught by checking that the instrument had OPERATED, separately from its result.** (i) CA's AC-1 checker silently widened when a numbered table was added to the inventory, and began checking remediation-era rows against the pre-change blob. (ii) CA's first fix for that made its own negative control **vacuous and green** — guarded on non-empty cells, so a control built from the blank-verdict round-1 inventory skipped every row and reported `0 rows parsed, 0 failures`, shape-identical to a real pass. (iii) CR's span splitter broke on an escaped pipe with no positive control. **This is the distilled file's own remedy — "require an instrument to emit evidence that it OPERATED, separate from its result" — arrived at three times from three directions by two agents, inside the story distilling it.** Strong evidence the rule survives the distillation in usable form, and the sharpest available answer to "did compressing it cost anything."

3. **Deferral would have shipped a defect, and the remedy's own author would have shipped it too.** CR proposed taking the `8 of 8` disposition clause at closure rather than in-story. CA's first draft of that clause named **two sites of five**; CR's re-enumeration then found **four of five**, the fifth being the guard clause the remediation had just created. **CR volunteered that a clause it had written would have carried the same gap**, because it would have enumerated the sites it had in front of it. What caught it: CA grepping its own clause, then a non-author re-enumerating **from the file rather than from the clause**. A concrete argument for the no-deferral rule, and worth more in the record than the rule stated abstractly.

**From E-280-02 (verdict issued against frozen tree `19f7878657afb896d85b39da13aa9e4a40fd689a`):**

4. **⭐ THE EPIC'S SHARPEST AC FINDING, and it is the same defect as item 1 at a different layer. A RED specified as an ENUMERATION OF VIOLATION FORMS is only as complete as the enumeration — and being mechanically checkable is exactly what gets it checked and trusted.** The implementer found, while verifying an unrelated claim, that its own shipped mechanism defined the per-story review surface as `git diff <merge-base> <tree-sha>`. Because the freeze stages everything, that diff contains **every story frozen so far** — so routing it would have handed the reviewer a story already DONE and verdicted, for a second verdict. **The mechanism built to bound re-verdicts would have manufactured one on every story after the first.**

   **Neither guarding AC's RED reaches it.** AC-1's RED is *"any surviving **sentence** that asks for a verdict to be repeated… ; any **loop** with no stated terminating verdict"* — but no sentence asks for repetition and no loop lacks a terminator; each story gets exactly one verdict, and the defect is that the verdict's **scope** silently includes an already-verdicted story. AC-5a's RED — *"no two verdicts may cite the same tree SHA"* — misses it for a different reason: the SHAs genuinely differ, it is the **content** that overlaps. AC-1's BODY does reach it (*"no **path** through this file directs any agent to re-issue"*), which is the trap: **body and RED disagreed, and the RED is the operative test.**

   **What would have caught it — the remedy shape, and it generalizes past this epic:** prefer a RED phrased as a **PROPERTY OF THE OUTCOME** over a list of textual forms. Here: *"no verdict is issued against a diff containing changes already covered by a prior verdict."* Coverage-based, and it catches the procedural form and the textual form alike. **Detection is consistent with the file E-280-01 distilled: found by EXECUTION (reasoning the mechanism through against a real diff), not by reading — and not by its author's ACs, which are PM's.**

5. **The same failure surfaced independently at the ORCHESTRATOR end, which is why it is one finding and not two.** The main session ran `git add -A` for story 01 without `git write-tree`, so no tree SHA exists for 01 to serve as 02's base; it was worked around by path-scoping, exact only because 01 and 02 are file-disjoint. **That workaround does not survive E-280-06, which shares `implement/SKILL.md` with E-280-02.** From E-280-04 onward each story's tree SHA is recorded at its staging boundary, and 06's review base is 04's tree. **Assessment of whether the landed text prevents a recurrence, stated at its true strength and no higher:** it largely does — `git write-tree` is now part of the freeze command itself (Step 5 "Freeze the review surface FIRST"), and the retention instruction is present and bolded (*"Retain each story's tree SHA when it passes — it is the next story's review base"*). The residual is **placement, not absence**: Step 5a's three numbered steps close a story without restating it, so the instruction sits at the point of explanation rather than the point of action. Recommended, not required by any AC: a carry-forward clause in Step 5a.

6. **The TN-7 ruling on E-280-02, with PM's own first ledger corrected.** E-280-02 grew `implement/SKILL.md` by ~+6.8 KB / +72 lines against a ≈ −20-line forecast. **Verdict: TN-7 PASSES.** Two things about how it passes, because the reason is what a later reader will reuse.

   **First, the argument that does NOT carry it.** The implementer's reason was that this trades bytes in a *load-on-demand* skill against an always-on file. That is a **cost** argument, and TN-7 is not a byte test — accepting it would answer a restatement question with a byte answer, which is the precise substitution TN-7 exists to prevent. Rejected as the basis, though true on its own terms.

   **Second, PM's own supporting ledger was too generous and is corrected here.** The first ruling claimed *"three facts down, one up."* Re-counted against the landed text, the honest ledger is:
   - *Who issues which verdict* — **DOWN**, to one canonical sentence with AC-13 requiring every site to match. A real reduction.
   - *What is under review* — **FLAT in site count** (~6 before, ~6 after), **not down as first claimed**. What changed is kind, not count: from implicit, differently-worded and undetectable, to addressable, consistently-worded and **self-detecting** (a drifted freeze surfaces as a SHA mismatch).
   - *How deep does review go* — **NEUTRAL to slightly down**; one table replaces one ad-hoc branch.
   - *How to freeze* — **UP, and then partly back DOWN.** Seven sites carried the `git add -A && git write-tree` operation at first verdict; the review-base fix left that at seven (it restated the *base*, not the command, adding a second restated fact across ~5 sites). **Final count on the closed story: SIX** (`:242`, `:266`, `:312`, `:395`, `:571`, `:894`) — Step 5a stopped carrying its own copy and now references the canonical block by name. The *verification* half is single-sourced; only the freeze command, which is executed in place, remains inline.

   **⬆ IMPROVED by the round-2 remediation, and this is the epic's design principle paying off inside a bug fix.** The MUST FIX remediation added `#### THE FROZEN-STATE CHECK` — *"Defined once, here. Every other site that needs it references it by this name rather than restating the commands"* — after **three independently-worded copies of the verification drifted from each other and from the story's own Technical Approach inside a single story.** So the *verification* half is now single-sourced while the freeze *command* stays inline where it is executed. That converts part of the "how to freeze" UP column into a genuine reduction, and it was discovered by the defect rather than by the design. **The restatement this epic exists to remove is what produced the epic's own worst bug.**

   **So the pass rests on a narrower basis than first stated**: one genuine reduction, plus the conversion of the largest restatement surface from a silently-drifting one to a self-detecting one. **A restatement that cannot silently drift is not the hazard TN-7 targets** — that is the load-bearing sentence, and it is what carries the verdict once the arithmetic is honest. Recorded per this repo's rule that **a verdict's stated REASON rots independently of the verdict**: the verdict never moved, the first reason was partly wrong, and a closure record carrying the generous ledger would be worse than one carrying this.

7. **⭐ THE INOPERABLE INSTRUMENT, and the observer who reported what it was supposed to say. ONE finding with two ends — recorded together because neither end is detectable alone.**

   **End A — the instrument.** E-280-02 shipped its write-during-review check as *"`git status --porcelain` must be empty."* **It is never empty in a dispatch worktree**: `--porcelain` reports the index column, the freeze is `git add -A`, and `HEAD` is the merge base, so every accumulated story shows staged forever. Applied literally, **every verdict is discarded forever and the re-freeze reproduces the same tree — an unbounded loop with no terminating verdict, which is AC-1's own second RED, shipped inside the story that defines it.** It is also blind in the direction that matters: a real write landed during review and porcelain read 8 lines before and 8 lines after, *identical*. **Neither check is total on its own** — the tree-identity check (`git write-tree` vs the cited SHA) reads the INDEX, so it reports clean while unstaged writes exist; porcelain-as-written misses an *added* write. Working tree == index == frozen tree is the property; one instrument does not establish it. The story's own Technical Approach was right (*"at verdict the **unstaged diff** must be empty"*) and the landed file diverged from it.

   **End B — the observer.** The orchestrator twice reported *"`git status --porcelain` was empty at freeze time."* **It was 8 lines both times.** No unstaged entries were seen, and "empty" was reported — the expected conclusion substituted for the observation actually made, then relayed as a measurement.

   **Why the two ends are one finding:** *it passed unchallenged because the expected reading and the reported reading agreed.* A broken instrument plus an observer reporting what the instrument was supposed to say is a failure mode **neither party can detect alone** — the instrument cannot report that it is measuring the wrong thing, and the observer has no disagreement to notice. Splitting this into "a tooling bug" and "a reporting slip" loses exactly the part that makes it instructive. Companion to item 4's *"a check that RAN is not a check that WORKED"*: here the check ran, returned a plausible reading, and was reported as the reading it was designed to return.

8. **The pattern named in item 4 has now fired THREE times in one epic, and the third instance is the cleanest.** AC-4 reads *"names an observation that goes red when a file is written during review"* with RED *"no observation named, or an observation with no stated consequence."* **An inoperable observation is a form the RED does not enumerate** — an observation is named and a consequence is stated, so both limbs pass while the instrument does not work. Three for three: story 01's AC-1 under-matched on what counts as *a rule*; story 02's AC-1 under-matched on what counts as *a re-ask*; story 02's AC-4 under-matched on what counts as *a failed observation*. **All three are PM's, all three were mechanically checkable, and being mechanically checkable is what got them checked and trusted.** ⚠ **The normative resolution, which matters because "body vs RED" recurs: the BODY defines the requirement and the RED is a possibly-incomplete TEST for it. An under-enumerating RED does NOT shrink the requirement.** So a defect that violates the body fails the AC whether or not the RED enumerated that form. (This is why PM ruled AC-4 FAILS here while ruling story 02's AC-1 PASS: AC-1's body was violated only by text the implementer had already fixed before the verdict, so nothing live ever violated it.)

   ⚠️ **READ THIS BEFORE INFERRING ANYTHING FROM "AC-4 WAS AMENDED MID-STORY AND THE STORY THEN FAILED."** The two new RED limbs on AC-4 **add no requirement.** The body already demanded a *discriminating* observation; the original RED enumerated only presence/absence forms, so an inoperable observation passed both limbs while satisfying nothing. The amendment made an **already-violated body checkable** — it did not manufacture the failure, and the failure did not depend on it. **That distinction is the whole guardrail on mid-story AC edits:** amending a RED to make an existing body testable is legitimate; amending a body to create a breach that was not there is changing the answer after seeing the work, and is not. Anyone auditing this epic will land on this pair, so the reasoning is recorded rather than left to be reconstructed.

9. **FOUR instrument failures across two stories, all of which RAN and none of which initially WORKED — and the fourth is a distinct sub-shape.** The first three are item 2 (a checker that silently widened; its fix's own control vacuous-and-green; a span splitter with no positive control). The fourth, **self-reported by the implementer**: its final regression search was aimed at **AC-1's wording** (`"exactly once"`, `"no path through this file"`) rather than at **the file's** text. Both returned 0; neither was a defect — the property is present, worded as *"Each verdict is issued once against a given frozen tree and is not re-askable"* and *"No two verdicts from the same agent may cite the same tree SHA."* **A control drawn from the SPEC instead of the TARGET reads exactly like a real absence**, which is `tool-output-integrity.md`'s line-wrapped-control case: a bad control's empty is indistinguishable from a tool failure's empty. **The transferable line, and it is the mirror of item 8's body-vs-RED doctrine one level down — at the level of CHECKING rather than of JUDGING: checking the spec's phrasing is not checking the requirement.** Item 8 says an under-enumerating RED does not shrink the requirement; item 9 says a search for the RED's *words* does not test the requirement either. Same error, once in the judge and once in the instrument.

10. **The porcelain scoping enumeration, RESOLVED by PM at the operator's request — two parties enumerated the same class and got different sets.** The implementer named three sites (Prerequisites item 3, Step 7a preflight, Step 11 sweep); code-reviewer named two (the closure preflight and Prerequisites item 3). **PM enumerated the file directly: there are exactly THREE distinct sites, four textual uses** — `:54` and `:55` (Prerequisites item 3, one site), `:703` (Step 7a preflight), `:853` (Step 11 sweep). **The implementer's set is complete; code-reviewer's is short by one — the Step 11 sweep.** All three literally begin `cd /workspaces/baseball-crawl`, so all three are correct as written. **No live risk arises from the short set**, because the *shipped* prohibition (`:303`) names all three AND states the discriminator, so the omission lived in the analysis and never in the artifact. Recorded because "an under-enumerated scoping fails silently, by skipping a step rather than blocking one" is the dangerous direction, and this one resolved clean — **which is itself worth knowing, since three of this epic's other enumeration findings did not.**

11. **⭐ THE ONE THING IN THIS EPIC THAT ANSWERS THE ENUMERATION-GAP PATTERN RATHER THAN INSTANCING IT — accepted, with a bound that must travel with it.** E-280-02's final edit replaced a two-row blindness table (an enumeration of two *observed* cases) with a structural argument: **three states — working tree, index, frozen tree — admit exactly two equalities, so exactly two checks are necessary and sufficient**, and `working tree == index == frozen tree` follows by transitivity. **PM ruled the claim HOLDS.** Checked for a fourth state and for write shapes outside the three: `HEAD` is not relevant (the review surface is the frozen tree, not HEAD); untracked files are inside "working tree" and are covered by half (1)'s second command; `.gitignore`d files are correctly out of scope, since they enter neither the frozen tree nor the closure patch; mode changes and symlinks are caught by `git diff`. **No third equality and no uncovered write shape exists.**

    **It is the mechanism-level twin of the AC-level remedy in item 4** (*"a RED phrased as a property of the outcome"*): both replace a list of forms with a property. **Their shared residual, stated once for both, because an unbounded version of either is the elegant-over-claim sub-class this epic's own source file warns about:** the implementer's generalization — *"an enumeration justified by a structural property does not under-enumerate the way a list of observed forms does"* — is **TRUE BUT BOUNDED, and "does not under-enumerate" is too strong.** A structural argument is only as complete as its **decomposition**: pick the state list wrong and it yields a confidently-complete-but-wrong enumeration, with more authority than a list of forms would have carried. It **relocates** the risk from *"did I list every observed form?"* to *"did I decompose the structure correctly?"* — a real improvement, because the second question is far more tractable and can be checked by a non-author, but **not immunity.**

    **This very check demonstrates the bound.** The structural argument says *two checks*; it does **not** say that half (1) needs **two commands** — `git diff --quiet` alone leaves `working tree == index` unestablished for untracked files, which is why `git ls-files --others --exclude-standard` is there. **So the structure determined the COUNT correctly and under-determined the IMPLEMENTATION.** Carry the remedy; carry the bound with it.

12. **A cost of the freeze mechanism, recorded because it looks like a defect and is not.** Across E-280-02's six freezes, PM's verdict was **three times right in content and stale in citation** — the reads consistently ran ahead of the SHA quoted. The cause is mechanical, not carelessness on either side: each re-freeze invalidates a citation while leaving the content reads valid, and the orchestrator and the verifier are writing to each other across that boundary. **The freeze did not create this staleness — it made it VISIBLE.** Before the freeze, a verdict against a moved tree was simply undetectable; now it is caught every time, which is the mechanism working. Expect the same shape on every story and do not read it as a process failure. (Companion to item 7: there, the expected reading and the reported reading agreed and nobody could see it; here they disagree loudly, which is the improvement.)

13. **The six-freeze churn on E-280-02 is an ORCHESTRATOR finding, recorded here so it does not read as implementer indiscipline — which would be the wrong lesson landing on the wrong party.** Code-reviewer correctly declined to assign cause (it cannot see message ordering from the worktree); the orchestrator could and did: **the story was routed in five separate messages, each inviting another write, and the stop-writing instruction was sent after them.** The implementer's writes were responses to routing already in flight. **Corrected order, which CR's own finding implies: consolidate all findings into ONE pass, then freeze.** Six frozen states on a story whose circuit breaker bounds a story to two is a routing pattern, not a discipline problem.

    ⚠️ **AND IT IS NOT EVIDENCE ABOUT THE FREEZE DESIGN — a reader of this record could easily conclude "the freeze required six attempts, so it is fragile," and that inference is wrong.** The churn measures the ROUTING, which was the orchestrator's. **The design's own evidence is the LAST round, not the first four: given a single consolidated pass, the freeze held for an entire review with zero lines under `.claude/` moving.** State the two separately wherever this is retold, because the causes are unrelated and the aggregate reads against the mechanism.

14. **A SEVENTH narrowing instance, this one on the code-reviewer, and it is the best evidence this epic has.** CR grepped for the stop-writing clause with a lowercase pattern, got zero against the file's `REPORT IT AND STOP`, and had drafted it as possibly-missing before a wider read found it **four lines away**. **Grep entry 2 — letter case — firing on the reviewer, inside the review, in the epic that distilled the rule stating it.** It stayed a footnote rather than becoming a fabricated MUST FIX **only because CR re-read instead of ruling on the empty**, which is the response protocol working exactly as written.

    ⚠️ **COUNT CORRECTED 2026-08-02, and the correction is the point. This entry first read "eight instances, four parties, nobody exempt" and PM recorded it with a ⭐. That figure was never measured — it SUMMED REPORTS.** Code-reviewer declined to certify it and was right to: it observed exactly **TWO** first-hand — its own lowercase-grep zero against `REPORT IT AND STOP`, and the orchestrator's `--porcelain` "empty", which CR measured three times. The other six it was only told about. **This is "a repeated assertion is not a repeated measurement" — counting STATEMENTS and reporting them as OBSERVATIONS — committed in the closure record of the epic that exists because figures ride artifacts unchecked, about the tally most likely to be quoted later.**

    **Standing count: at least TWO verified first-hand** (code-reviewer's own, and the orchestrator's, the latter measured three times by CR); **the remainder reported by their authors and not independently confirmed.** A real count is derivable — every instance has an artifact — but **someone must REGENERATE it from the artifacts, never sum the reports.** Not done here, and deliberately so.

    ⭐ **The substance survives intact and is stronger without the number, which was the only part a skeptic could puncture: the compressed rule is demonstrably OPERATIVE.** The distillation did not turn a live discipline into inert prose — every party who tripped the rule caught it **by applying the rule**, and CR's lowercase zero stayed a footnote instead of a fabricated MUST FIX only because it re-read rather than ruling on the empty. That claim never needed a tally. Pair with code-reviewer's line for the closure narrative: **the blindness table is not reasoning about a hazard; it is a transcript of one.**

15. **⭐ THE OPERATIONAL FORM OF THIS EPIC'S ENTIRE DETECTION RECORD, and it is code-reviewer's, not PM's.** On reproducing the item 11 structural claim in a fresh `git init` repo, CR's own lesson was **not the invariant**: *"the claim was elegant enough that I would have been reluctant to challenge it on prose, and the reproduction is what made challenging it cheap. That is the transferable part — not the invariant, the fact that it was testable."*

    **Execution beats reading, and the reason is COST, not virtue.** Every party in this epic re-read carefully and still missed things; every catch came when checking got cheap enough to actually do. That reframes the 8-of-8 authorship record and the 4-of-4 execution record from a statement about *diligence* into a statement about *price* — which is actionable in a way "read more carefully" never is. **Design the check to be cheap and the challenge happens; leave it to prose and elegance suppresses it.** Note the direction of the risk: elegance does not make a claim wrong, it makes it **expensive to question**, and this epic's own record shows the closing generalization is exactly where that lands.

    **CR's reproduction also strengthened item 11's bound rather than merely confirming it**: five shapes tested, each caught by exactly one half, **including two the file does not enumerate** (untracked new file; new file added). Its ruling matches PM's, including the bound — *an observed-forms enumeration would not have predicted the untracked-file rows.* **Two parties, two methods, same conclusion and same bound.** Item 11 is as well-evidenced as anything in this epic.

16. **A premature `DONE` flip: TWO failures, not one, and the record names the originating end first because a record showing only the second would put the lesson on the party who CAUGHT it rather than the party who CAUSED it.**

    **End A — the originating relay error (orchestrator).** The message routed to PM read *"CR APPROVED with no MUST FIX and no SHOULD FIX. Both gates clear."* That was an accurate characterization of code-reviewer's verdict on **`805c0a4`** — and by the time it was sent, CR's **current** verdict on `cd1c4cd` read *"No MUST FIX. **Two SHOULD FIX.**"* **A characterization of a superseded verdict was carried forward and stated as the current one.** It is the same stale-citation-with-valid-reads pattern that ran through this entire story, except that this one **inverted the SUBSTANCE rather than the address** — the class this repo's rules call worse than a weakened claim, because it reads as confident and complete. Orchestrator's remedy: **quote the verdict's own findings line verbatim rather than summarizing it.**

    **End B — PM acted on the characterization rather than the verdict.** PM flipped E-280-02 to `DONE` on that summary and reverted it to `IN_PROGRESS` on learning otherwise. **Neither end excuses the other**, and **End B is the one with the durable remedy: it protects against the next relay error too**, which is why it is adopted for stories 04, 06, 07 and 08 rather than treated as a one-off. **The status field is the artifact PM owns and it is the one PM did not verify.**

    **End C — the REASON under the revert was then found ambiguous, by code-reviewer, applying PM's own doctrine to PM's record unprompted.** The revert is right under either available reading, **so no check asking "was the call correct?" could ever have surfaced a wrong reason sitting underneath it** — only reopening the reason does. That is *a verdict's stated REASON rots independently of the verdict*, caught in the live case rather than quoted. The disambiguation is in E-280-02's Status block: the operative reason is **open valid findings**, sufficient alone; the reading that a `DONE` is false because a *later* story will edit the same file is **disowned as false**, since story DONE means this story's ACs are met and not that the file is final. **Worth carrying past this epic: three parties in one story checked a right answer's reason and two of them found something wrong with it.** The concrete harm was not bookkeeping: `E-280-06` also edits `implement/SKILL.md` and depends on 02 being finished, so a `DONE` marker on a file under active edit is a false signal to a downstream story. **Rule for the rest of this epic: PM reads the verdict's own findings list before any DONE flip, and "no findings" is a claim to check, not a summary to accept.**

    **It then fired a SECOND time, on me, in the next turn — which is the useful part.** Having filed this item against myself for acting on a characterization, I recorded item 14's *"eight instances, four parties"* with a ⭐ from a relayed sum I had not measured. **Same rule, different artifact: once in the status field, once in the closure record.** So the remedy above is scoped too narrowly if read as being about DONE flips. **The general form: PM does not record a FIGURE it did not derive, any more than it records a STATUS it did not verify** — and the closure record is exactly where an unverified figure does the most damage, because it is what gets quoted.

17. **Reason-checking is a WINDOW, not a discipline — the bound belongs with the claim, and code-reviewer supplied it about its own work.** After CR audited a stated reason twice in one story (items 11 and 16-C), the tempting characterization was *"the reviewer audits reasons."* CR narrowed it first: **both times the reason was sitting in a relay it was already reading for something else, and in both cases the wrong reason was a SENTENCE — the only form it can audit cheaply.** It has no general practice of auditing reasons and went looking for neither.

    **Stated narrowly, which is the only form that is true:** *a relay in flight is the one place a stated reason is cheap to check, because you are reading it anyway and the artifact is still reachable. Once it lands in a closure record, the same check costs a deliberate re-opening that nobody schedules.* **This explains a fact the epic had otherwise only observed — why wrong reasons concentrate in durable records.** Not because records are written carelessly, but because the check that would catch them is affordable only while the claim is still in transit. It is item 15's COST framing applied to reason-checking: not a discipline anyone can be asked to exercise on demand, but a window that is briefly cheap and then closes. **Record the bound with the claim; an unbounded version is a capability nobody can deliver on request.**

18. **⚠️ A closing generalization about over-claim control, and it is FALSE — refuted by its own example list. Recorded because it is the epic's own failure mode arriving in the sentence that summarizes the epic's failure modes.** The proposed claim: *"self-bounding is the only form of over-claim control that has worked here — no reviewer caught an over-claim in someone else's capability statement this dispatch; every one was withdrawn or narrowed by its author."*

    **Two counter-instances, one of them inside the claim's own cited evidence.** (a) **The structural-completeness generalization was bounded by PM, not by its author** — the implementer offered *"an enumeration justified by a structural property does not under-enumerate the way a list of observed forms does"*, and PM ruled it *true but bounded, "does not under-enumerate" is too strong*, at the orchestrator's explicit request. Code-reviewer independently supported the same weaker form. **That is a non-author bound, and it is item 11 — cited by the claim as an instance of self-bounding.** (b) **Code-reviewer declined to certify the orchestrator's "eight instances" tally**, an over-claim in someone else's statement caught by a reviewer, which is precisely what the claim says did not happen (item 14).

    **What survives, and it is still worth having:** self-bounding was **frequent and unprompted** here, usually one message after the over-claim, and that is a real and encouraging pattern. **What does not survive is "only" and "no reviewer" and "every one".** The true version is the weaker one: *over-claims were controlled BOTH by authors narrowing their own and by non-authors bounding others' — and the author-narrowing cases were faster.* **Note the shape: a correct observation with a universal quantifier welded on, in a closing position, in the last paragraph of a message.** That is exactly where this epic's record says the defect lands, and it is the *mirror* of the 8-of-8 authorship record rather than an instance of it — which is what made it attractive.

19. **MF-1's shape recurred a THIRD time, and the epic now has a settled remedy for it — an AC that reads on a file its story cannot edit.** E-280-04's AC-8 first RED limb (*"the routing seam restating rubric content"*) read on `implement/SKILL.md`, absent from 04's Files list. **Precedent applied rather than re-derived**: narrow the AC to the story's owned files, relocate the cross-file criterion to the story that owns the seam. Landed as **E-280-06 AC-10**, with the live instance named (tier B's *"skips rubric priorities 2, 4 and 5"*).

    **Two rulings that keep this from becoming a loophole.** (a) **It is NOT a deferral of a review finding.** The no-deferral rule governs *findings*, which reach FIXED or DISMISSED inside their story; this is an **AC scoping defect**, a different class with an established remedy. Conflating them would either paralyse the decomposition or license real deferrals. (b) **E-280-04's DONE is contingent on E-280-06 AC-10 EXISTING IN THE SPEC, never on 06 having executed.** The requirement must have an owner before the story that shed it can close; requiring execution would block 04 behind 06 for no gain, since 06 runs next regardless. **Direction-of-travel check, per item 8's test:** narrowing an AC makes a story *easier*, so it needs a stronger justification than a RED clarification does — here it is a genuine unsatisfiability, and **the requirement moves rather than dies**. If AC-10 is ever removed from 06, 04's narrowing retroactively becomes a silent weakening; that dependency is stated in both story files.

20. **The first FALSE PREMISE in this epic to originate in a STORY rather than an agent — and it is PM's.** E-280-04's Context asserted the vendor written-document line sits in *"the six Opus 5 adapters"*; it is in **two** (`claude-architect`, `product-manager`). The implementer had already written *"the Model Adapter's written-document guidance below is a separate surface"* into **four files where it was false** before catching it — **because a grep returned 2 where it expected 6.** An unexpected count treated as a cross-check trigger, not a re-read: the distilled rule firing again, on a premise the spec handed down. AC-4 is unaffected and stays GREEN (its *"every existing"* tolerates absence; no diff line touches vendor-line text). **The lesson is aimed at PM: every party has been checking agent-authored claims, and the story files were the one channel nobody was auditing — they arrive with the authority of the spec and none of the suspicion a brief attracts**, which is `tool-output-integrity.md`'s inherited-claim rule pointed at the artifact PM writes.

21. **FILE-WINS exercised AGAINST THE ORCHESTRATOR — the first time in this dispatch the rule ran in that direction, and it prevented a shipped defect.** The orchestrator read `git diff --stat` against `.claude/skills/`, saw byte-unchanged, and instructed the implementer to land story 02's two fixes. **The reading was accurate when taken and stale by roughly one minute** — the implementer had written at 04:19:04Z. **The implementer refused to re-apply on the orchestrator's say-so, checked the file, and reported the contradiction.** Had it complied, duplicated clauses would have shipped.

    **Two lessons, and the second is the transferable one.** (a) `git diff --stat <tree>` is a **live read against the working tree with no timestamp attached** — a status asserted without attaching its measurement time, in the dispatch that produced that rule. (b) ⭐ **Report-don't-fix is not only for defects an implementer FINDS; it is for INSTRUCTIONS IT RECEIVES that the artifact contradicts.** `dispatch-pattern.md`'s *"the artifact wins and the brief is wrong"* has been exercised all dispatch on briefs flowing *down*; this is the case where the brief was an **instruction to act**, and the same rule applied. Credit is the implementer's, not the orchestrator's.

22. **A GAP IN THE FREEZE MECHANISM, found by PM tripping it: the design assumes only implementers write, and PM writes during dispatch AS ITS ROLE.** PM's story-file edits — status flips, AC rulings, RED clarifications — land in `epics/`, which is outside every reviewed surface, but they **move the worktree**, so `git write-tree` stops matching the cited SHA and AC-4's check fires. **This will recur on every story**, because AC verification and status ownership are exactly what PM is spawned to do.

    **The orchestrator's working practice is path-scoped confirmation** (freeze-check the reviewed subtree rather than the whole worktree), and it is correct — but **`implement/SKILL.md` states the check unscoped**, so the practice is not in the artifact. **Do not "fix" this by telling PM to stop writing**: status transitions during dispatch are PM's job and deferring them would put the epic's own status record behind its work. **The honest options are to scope the check to the reviewed paths in the file, or to state PM's planning-artifact writes as a named exception with the reason.** Flagged for E-280-06 or E-280-07 to decide; not adjudicated here, since choosing the mechanism is claude-architect's and PM is the party that trips it.

23. **Item 20's remedy shape, for the CLOSURE CODIFICATION rather than as an E-280 incident.** The gap is standing: **every relay in this dispatch was checked against its artifact — and the artifact itself was never checked against anything.** Story files arrive with the authority of a spec and none of the suspicion a brief attracts. **Remedy: a spec claim is a claim, and the party who can falsify it cheapest is the implementer about to act on it** — which is precisely how the "six adapters" premise died, via a grep returning 2 where 6 was expected. That is item 15's cost framing again: the check is affordable at the moment of use and nowhere else.

24. **⭐ THE MECHANISM behind items 4, 8, 18 and now 9a — stated once, because four instances is a pattern and not a series.** **A RED written as an enumeration of failure FORMS is narrower than the body it tests BY DEFAULT, not by accident.** The body states the *requirement*; the RED states *the failures the author happened to think of*. Those are never the same set, and the gap is invisible precisely because the RED is the mechanically checkable half — so it is the half that gets run, and running it feels like verification. **All four instances in this epic are PM's**, which is what makes it a mechanism rather than carelessness.

    **Remedy, unchanged from item 4 and now load-bearing across five ACs: phrase the RED as a PROPERTY OF THE OUTCOME, not as a list of forms.** Its mechanism-level twin is item 11 (a structural warrant beats an observed-forms enumeration), with the same bound: the property must be correctly chosen, so the risk **relocates** to the decomposition rather than disappearing.

    ⭐ **SHARPENED, and the remedy CHANGES — this is where the epic's two lines of finding converge.** The design owner established that the three narrowings of the AC-18 requirement (TN-19 blanket → the AC's RED findings-only → the file's opening clause findings-only) were **not copied from each other**: each was arrived at independently, from the same requirement, by the same mechanism. It then **withdrew its own framing in favour of PM's**, and named why the difference is practical rather than verbal. **Its version — *"an enumeration checked against its own generating definition cannot detect a member that definition excludes"* — is a claim about CHECKING, and implies a better check. PM's is a claim about PROPAGATION — *a requirement restated at N layers narrows at each one independently, so the narrowings correlate without any of them causing the others* — and implies FEWER LAYERS.**

    **So the fix for a requirement that narrows on restatement is not a better RED. It is one fewer restatement** — which is **TN-7 arriving at the same answer from the opposite direction.** Until this point the enumeration-gap findings (items 4, 8, 18, 19, 24) and the restatement principle (TN-7, item 6) read as two separate concerns of this epic; **this is the first time they converge on a single remedy**, and that convergence is the strongest available argument that the epic's design principle was aimed at the right thing. A better RED helps one criterion; removing a layer removes the whole class at that layer.

25. **AC-18's ruling AMENDED: it now rests on the MEASUREMENT ground alone, because both of PM's other grounds were falsified — by code-reviewer, arguing against its own interest.** PM declined option (b) (prose-capped, findings exempt) on two grounds: that *"prose versus findings"* needs judgment, and that under a cap *"the evidence goes before the finding does."* **CR falsified the first** — its schema's `### MUST FIX` / `### SHOULD FIX` / `### AC VERIFICATION` are enumerated headings, so a cap on *"all text outside the three enumerated finding sections"* is mechanical. **And that falsification retires the second too**, which PM should state rather than let stand: if finding sections are exempt, evidence living inside them is not cut, so the evidence-goes-first harm does not arise under (b) as scoped.

    **The conclusion is unchanged and the surviving ground is sufficient for BOTH alternatives: TN-19 measured that no report-length regression exists**, so (b) and (c) alike are machinery for an unmeasured problem, on the one agent with a documented literalism failure mode, in an epic whose test asks for one *fewer* place a fact is restated. **Recorded per PM's own doctrine that a verdict's stated REASON rots independently of the verdict** — the verdict never moved, two of three reasons were wrong, and only reopening them caught it. **CR volunteering a falsification that strengthens the option it preferred is the cleanest instance of non-author bounding in the epic** (and a further counter-instance to item 18's retired "self-bounding only" claim).

26. **AC-18 PASSES on the landed text — and the narrower-than-body defect has now appeared at all THREE layers of the same requirement.** Reading the whole paragraph rather than its first clause settles it: `code-reviewer.md` says *"**You are deliberately excluded**"* from the ceiling *"every other agent definition in this repo carries"*, with the vendor literalism reason stated. Blanket, so the body is met and no ceiling is applied in any form. **But its opening clause reads *"No length ceiling applies to your findings output"* — narrow, bolded, and in the most-read position in the paragraph.** SHOULD FIX: `your findings output` → `your output`, so the sentence agrees with its own paragraph's conclusion. **The same narrowing ran through TN-19's ruling (blanket), the AC's RED (findings-only, widened by PM), and the file's opening clause (findings-only)** — one requirement, three layers, the same defect at two of them. **Method note: CR quoted the first clause accurately and PM reached a different verdict by reading the enclosing paragraph — which is `tool-output-integrity.md`'s prohibition 3 working exactly as written, on a quotation rather than a grep hit.**

27. **⭐ THE SHARPEST ONE-LINE STATEMENT OF CLASS A THIS EPIC PRODUCED — and it came from the party that committed it.** The tier-B rationale was *"a single rationale covering three members, true of two."* The author's own diagnosis: **the sentence read cleanly BECAUSE two-thirds of it was right, which is exactly why no re-read caught it.** That is Class A — a false premise under a correct-looking whole — compressed into one sentence, and it explains the detection record rather than merely restating it: **a grouped justification is checked as a group, so the true members carry the false one past every reader.** Companion criterion: E-280-04 AC-9b, which forbids grouped rationales for this reason.

28. **A COUNTER-INSTANCE to this epic's authorship record, recorded BECAUSE it cuts against the pattern.** The record has been that authors never catch their own factual errors (8 of 8, and item 18's over-claim about self-bounding was itself an over-claim). **Here an author did**: the implementer's first draft of the Priority-4 rationale asserted *"tier B is the only tier that reviews `docs/`"* — **a path-to-tier mapping inside the rubric, which is AC-8's second RED** — and it caught and reworded it **before review**, to cite the seam rather than redefine it. In AC-8's own territory. **Recording it rather than omitting it for tidiness**, because a pattern kept clean by dropping its counter-examples is the exact defect this epic has been filing all dispatch. **The honest form of the record: authors rarely catch their own, not never — and the catch happened where a criterion the author was holding in mind named the failure shape in advance**, which is a mechanism, not luck.

29. **AC-9b earned itself inside the remediation that motivated it — a criterion catching a defect the review round that produced it did not.** Code-reviewer's MUST FIX said tier B wrongly skipped Priority 4. The implementer fixed that, and its corrected rationale was **still grouped**: *"Priorities 2 and 5 are skipped (bugs/regressions, schema drift): **those** target executable behavior."* One reason, two members. **Its own diagnosis is the entry: *"I fixed the false member and left the grouping that hid it. AC-9b catches the mechanism; my fix had only caught the instance."*** Now split with distinct reasons per priority.

    **Two things this settles.** (a) **The correction still carried the defect in smaller form** — the strongest argument available for the mid-story tightening, and it arrived *after* the justification for it rather than before, which is the order that makes it evidence rather than rationalization. (b) **AC-9b is correctly calibrated in both directions**: it REDs on a grouping *untrue of any member*, so it caught this one while PASSING the legitimate 2-and-5 grouping that replaced it. A blanket ban on grouped rationales would have failed a correct rationale and taught implementers to pad.

30. **A SECOND counter-instance to the authorship pattern, and the only one where the author caught it on itself, unprompted, against its own prior position.** PM retired **two of its own three grounds** for the AC-18 ruling while the conclusion never moved: code-reviewer falsified the judgment-boundary ground, and **PM then retired the evidence-goes-first ground itself, unprompted, because it followed from CR's point.** The design owner rates this the cleanest instance in the epic of *a verdict's stated REASON rotting independently of the verdict*. **Every other instance this dispatch was caught by a second party** (items 16-C, 18, 25 in part). Record the asymmetry rather than the event: the pattern is that authors rarely catch their own **facts**, and the two counter-instances (this and item 28) are both cases where **a criterion the author already held named the failure shape in advance** — 28's implementer held AC-8, and here PM held its own reason-rot doctrine. **That is the mechanism, and it is the only reliable route to self-catching found in this dispatch.**

31. **⭐ AN INSTRUMENT WHOSE EVERY FAILURE IS A FALSE POSITIVE IS MEASURING THE WRONG SCOPE, NOT THE WRONG THING.** Two rounds running, 100% of the frozen-state check's half-(1) failures were PM's `epics/` writes with zero lines under `.claude/`. **The obvious response — loosen the check — would have been wrong**, because the unscoped check also catches a real true positive: an implementer writing outside its Files list. **The check was correct; its DOMAIN was wrong.** Repair is scoping plus a report (E-280-06 AC-11), never relaxation. Generalizes past this instrument: **when every alarm is spurious, interrogate what the instrument is pointed AT before you weaken what it fires on** — a wrong scope and a wrong threshold present identically as noise, and only one of them is fixed by turning the sensitivity down.

32. **A SUCCESS RECEIPT CANNOT SEE STRUCTURE — "the edit applied cleanly" tells you nothing about where it landed.** PM inserted E-280-06's AC-11 **inside AC-10's sub-bullets**, orphaning AC-10's provenance note under AC-11. The tool reported success, truthfully: the bytes were written. **What it could not report is that the note now belonged to the wrong criterion** — which would have destroyed the AC-8→AC-10 chain and with it the *"if AC-10 is removed, E-280-04's narrowing becomes a silent weakening"* safeguard, the single thing standing between that narrowing and being silent.

    **Caught by re-reading the region rather than trusting the tool's response** — `dispatch-pattern.md`'s effect-in-the-artifact check applied to one's **own write**, which is the direction nobody applies it: that rule is written for verifying *others'* delivery. **The repair is stronger than the fix**: the note now **names AC-10 explicitly** instead of relying on position, so a future mis-anchored edit cannot orphan it the same way. **Structural correctness is not in the class of things any tool response reports, so a structural edit needs a structural read-back — and "applied cleanly" is the phrase most likely to stop you doing one.**

33. **⚠️ CODE-REVIEWER'S PRE-REGISTERED FALSIFICATION CONDITION *FIRED*. It was deferred to PM and RULED — it did not fail to fire, and the record must never say otherwise.** CR committed, **before reading AC-18**: *"I would call the exclusion wrong if it exempts my REPORT rather than my FINDINGS."* At `347f9d9` the clause read *"your findings output"* and the condition correctly had not fired; at `351ce8c` it reads *"your output"*, and with the Scope line defining scope as *"your SendMessage reports"*, **that is the report-level exemption named in advance.** CR's own reason for insisting this be recorded as FIRED: ***if the record says it did not fire, the pre-registration was theater.*** It fired **in CR's own favour**, on a change it had already conceded against its interest was mechanically avoidable.

    **CR's diagnosis is sharper than "over-scoped", and it is right: *"The reason did not widen; only the claim did."*** Every sentence justifying the exemption is about findings — *structurally driven by finding count*, *a finding omitted to fit a budget is a defect shipped*, *report every finding you have* — while the exempting sentence now covers everything. The one-word widening fixed a misread by **making the text match the broader claim rather than making the claim match its reason.**

    **PM's ruling takes a FOURTH disposition the three offered did not contain: the blanket claim STANDS and the REASON is widened to match it.** Narrowing the body (disposition 2) would cap code-reviewer's prose — **machinery for a problem TN-19 measured does not exist**, on the one agent with a documented literalism failure mode, which is the identical ground on which PM declined options (b) and (c). Leaving it bare (disposition 1) preserves a claim broader than its stated reason, which is the defect this epic has filed all dispatch. **So: `code-reviewer.md` owes one clause stating why the exclusion is BLANKET rather than findings-only** — that a cap on its prose is an instrument for an unmeasured problem, on the role where instrumentation is riskiest, and that it would add a boundary someone must apply. **Fix the reason, not the claim.** This is the same move as item 25 applied to a file instead of to a verdict: when a conclusion is right and its stated reason does not reach it, repair the reason.

34. **Item 29 gains a THIRD payoff for AC-9b, and it is the best of the three because the criterion did not do the catching.** Code-reviewer measured the split-out tier-B claim *"a tier B diff carries no migration and no column reference"* and **falsified it** — **118 files under `docs/` reference DB column names** (⚠️ **PM first recorded 117 here, the RELAYED figure. 118 is what the party that re-ran the measurement observed** — the router-must-not-be-a-source rule firing inside the item written about producibility. The divergence is immaterial to the conclusion and was correctly not chased; **the figure of record is the one someone measured last, not the one that travelled**), `docs/admin/architecture.md` references `migrations/`, `docs/ROADMAP.md:161` names `season_id`. Conclusion right, premise false, remedied to *"a tier B diff performs no database writes… a mention is not a write."*

    **The surface reading is backwards and the real one is the finding**: this is a false premise under a correct conclusion **in the sentence written one round earlier to fix a false premise under a correct conclusion** — which looks like a regression and is a success. CR's framing is why: ***"grouped, the claim was unfalsifiable because no single member owned it; split, I could test one member in one grep."*** **AC-9b's payoff here was not catching something itself but making someone else's check CHEAP** — and per item 15, cost is the whole mechanism. **A criterion whose value shows up as other people's checks becoming affordable is a better result than one that catches things itself**, because it scales to checks nobody anticipated.

35. **E-280-06 SPLIT into 06 + E-280-09, mid-dispatch, on PM's recommendation and the orchestrator's approval.** 06 had accumulated two seam/mechanism criteria during dispatch — AC-10 from E-280-04's unsatisfiable AC-8 limb, AC-11 from item 22's mechanism gap — on top of frontmatter ×10 and the CLAUDE.md compression, becoming **four distinct bodies of work: three wide-and-shallow, one narrow-and-deep.** 09 takes the deep half. **The deciding argument was the PINCH POINT, not the sizing rule**: E-280-07's layer-wide check and E-280-08's terminal verifications both read what these stories write, so **a partial landing at 06 is the most expensive failure available in this epic** — and AC-11 fixes a mechanism that fired spuriously on every story of the dispatch, which is not something to squeeze in behind ten frontmatter edits. **Superseded AC text was DELETED from 06, not tombstoned: a tombstone carrying the full criterion is still a second copy**, which is the defect this epic exists to remove.

36. **⭐ THE CONSTRUCTIVE HALF OF THE REASON-ROT DOCTRINE, which this epic has needed five or six times and never stated: *when a conclusion is right and its stated reason does not reach it, REPAIR THE REASON.*** Every prior use of reason-rot here has been **detective** — noticing that a verdict's stated reason had decayed (items 16-C, 25, 30). **None said what to do about it, and the disposition was ad hoc every time.** AC-18 forced the question: the claim was blanket, every justifying sentence was about findings, and the three offered dispositions all either capped code-reviewer's prose (machinery for a problem TN-19 measured does not exist) or left a claim broader than its reason standing. **The fourth disposition — widen the reason to match the claim — is the general answer**, and it is item 25 aimed at a FILE instead of at a verdict.

37. **CR's SUPPLIER-SIDE producibility rule, and it is new rather than a restatement: *a measurement relayed without its method is not a measurement anyone else can use.*** Every prior statement of producibility in this repo — including the `18 of 18` casebook entry — aims at **whoever QUOTES a figure**. This one aims at **whoever SUPPLIES one**, and it is the half that would have resolved the 117/118 divergence in one step: had the grep travelled with the number, a re-run would have reproduced it or shown exactly which token set differed. Instead **two correct-looking figures sat unreconcilable because neither method was recorded anywhere.** CR named its own contribution unprompted, which is what makes the rule land on the supplier rather than read as blame.

38. **REPORTING a measurement and SHIPPING one are different acts with different bars — a boundary nothing in this repo's rules currently names.** The implementer's own framing, and the sharpest item of its round: its handling of the 118 measurement was correct throughout — it re-ran rather than accepting the relay, reported what it observed, declined to reconcile to the relayed figure, correctly did not chase an immaterial difference. **The defect was entirely in the transition from reporting to shipping**, where a figure that was fine in a message became a figure in a durable file that no reader could reproduce. **Companion: the orchestrator's *"nothing turns on the divergence"* was correct about a transient state and wrong once the figure shipped** — a DISPOSITION that decayed, the third instance of the epic's status-decay class and the first where the decayed claim was a disposition rather than a status or a verdict summary.

39. **The `6,000` count is the CONCRETE instance of `doc-sweep.md`'s tombstone rule, which this epic has otherwise only stated abstractly.** Grepping `6,000` across `.claude/agents/` returns **NINE** files, which naively falsifies the true claim *"8 of 9 carry the ceiling."* The ninth is `code-reviewer.md`, whose hit sits inside the sentence that **retires** the ceiling for it: *"Every other agent definition in this repo carries a 6,000-character report ceiling. **You are deliberately excluded.**"* **A string that must remain findable, sitting inside the sentence that exempts it, indistinguishable from a live instance by count alone.** Only reading the line distinguishes an applied ceiling from a reference to one — Prohibition 3 earning itself on the last verification of the story.

40. **AC-9b's calibration divergence, recorded because a conservative reader will over-split.** The implementer judged its own grouped form (*"Priorities 2 and 5 … those target executable behavior"*) **defective** and split it; **PM ruled that same form COMPLIANT**, since AC-9b REDs on *a grouping untrue of any member* and that justification is true of both. **PM's reading governs and the split shipped anyway, so nothing turned on it** — but the divergence belongs in AC-9b's own text: **the defect was never the grouping; it was the false member hiding inside a true group.** A rule banning all grouped rationales would have failed a correct rationale here and taught implementers to pad.

41. **⭐ TOMBSTONE A RETIRED CLAIM; DELETE A RELOCATED CRITERION. The two cases look identical and the rule points OPPOSITE ways.** This repo endorses tombstoning — a retired claim kept findable so a later reader meets the correction rather than the ghost. **That practice does not transfer to a criterion that MOVED**: a tombstone carrying the full criterion is **a second copy**, which is the restatement defect this epic exists to remove, and both copies then drift independently. **The discriminator: is the text still asserting something (tombstone it, struck, with the correction attached), or is it now asserted somewhere else (delete it, leave a pointer)?** E-280-06's AC-10/AC-11 were deleted and replaced by a pointer to E-280-09 for exactly this reason. **Worth stating because a reader applying the repo's tombstone habit to a relocation produces a duplicate while believing they are preserving a record.**

42. **A BRIDGE LEFT IN AN ARTIFACT IS A CLAIM ABOUT A FUTURE EDIT, AND IT DECAYS LIKE ANY OTHER — with a bonus finding about how the bridge was removed.** PM needed a contiguous delete it could not construct without re-reading, and bridged with a commented block carrying its own expiry condition (*"retained only until the next edit confirms E-280-09 exists on disk"*). **Self-diagnosis, and it is the right one: cleaner would have been to read the range first; context was optimized for and untidiness was bought.** The expiry condition is what made it **collected rather than discovered** — an unconditioned bridge is indistinguishable from content.

    **And the removal itself failed on the first attempt, in the exact shape E-280-09's AC-3 exists to catch.** PM's delete anchored **mid-block**, removing the second half and leaving the opener, the table and two rationale bullets standing — **and the tool reported success, truthfully.** Caught by grepping for the block's own strings **with a positive control**, which returned only the two intended pointer references. **Confirming the edit applied says nothing about whether the thing you were removing is gone: verify a removal by ABSENCE of the old form, never by the success of the operation.** That is item 32's success-receipt lesson and `tool-output-integrity.md`'s verify-by-absence rule arriving together, on the same author, two rounds apart.

43. **⭐ TWO ACs SILENTLY COUPLED, NEITHER SAYING SO — and the arithmetic was RIGHT FOR A REASON NOBODY HAD STATED, which is why no check would ever have surfaced it.** E-280-04's **AC-16** requires the 6,000 ceiling to sit above every **covered** role's measured p50; it holds because the highest covered p50 is claude-architect's **3,291**. **Code-reviewer's p50 is 4,013 — HIGHER — and clears the bar only because AC-18 excludes it.** So **AC-16's lower bound is a consequence of AC-18's exclusion, and neither AC mentions the other.** Narrow or remove the exclusion and AC-16's justification breaks **silently**.

    **The detection route is the finding.** PM had already verified AC-16's both-sidedness once, correctly, as `3,291 < 6,000 < 13,882`. It surfaced only on **re-deriving the operand from TN-19's table instead of from its own prior arithmetic** — at which point the question *"3,291 is the highest covered p50, but is it the highest p50?"* had a different answer. **Every check asking "is the arithmetic right?" passes; only asking "why is this operand the right one?" finds it.** That is the producibility check aimed at an operand rather than at a figure, and it is the same shape as items 4 and 25: **a correct conclusion resting on an unstated premise is invisible to any verification of the conclusion.** Warning recorded in AC-18's own text, where an editor changing the exclusion will meet it — **not in AC-16, which is the one that breaks**, because the breaking site is not where the action is taken.

44. **⭐ A FLOOR IS WRITTEN FROM WHAT ITS AUTHOR COULD FIND BY TOKEN, SO IT SYSTEMATICALLY NAMES THE TOKEN-REACHABLE SITES AND MISSES THE ONES SHARING NO TOKEN — which are the sites the sweep exists for.** Two consecutive stories, same shape: **E-280-04's AC-15** floor named a site whose text was correct, while the real defect sat a paragraph away; **E-280-06's AC-9** floor named `codex-review`'s staged/unstaged template, which is the **standalone (no-worktree)** path and correct, while the actual defect was `git -C <epic-worktree-path> diff main` — a bare `main` base inside the epic worktree, sharing **no token** with the phrase the floor was built from. `plan/SKILL.md` carried the same.

    **This is a property of FLOORS, not a defect in these two ACs**, and it changes how they should be written. **A floor is evidence that the author searched; it is never evidence of where the defects are** — and its named sites are drawn from exactly the population a token search reaches, which is the complement of the population that needs a sweep. **Consequences: (a) always label a floor "a floor, not the list" (both did, and both were still read as leads); (b) the sweep's value is in the sites the floor does NOT name, so an implementer reporting only floor-site verdicts has not swept; (c) a floor whose every site resolves to `no change needed` is a SUCCESSFUL sweep, not a wasted one** — that is precisely the case where the token-reachable sites were fine and the real work was elsewhere.

45. **PM's criteria keep needing a HARMFUL-THING-versus-CATEGORY calibration at the point of FIRST APPLICATION — three times now, and it is not discoverable earlier.** AC-9b (a grouping *untrue of any member*, not grouping itself), AC-18 (a second *authoritative declaration*, after the RED/body split), and now AC-3 (a second **declaration**, not a second *appearance of the string* — the `workflow-help` cheat sheet is a rendering, not a location). **In all three the criterion was written correctly against the defect its author had seen, and the category it names is broader than the defect it targets.** The over-broad reading is only visible when the criterion meets a case its author did not imagine — which is the first real application, never the drafting. **Practical consequence: the calibration question ("does this forbid the harm, or the category containing it?") should be asked AT the first application by the party applying it, and routed to the AC's owner rather than resolved by the implementer.** All three times the implementer flagged rather than resolved, which is why all three were caught; **an implementer that reads a criterion conservatively and complies silently produces the over-broad outcome and nobody ever learns.**

46. **⭐ THE ALWAYS-ON LOAD RECONCILES, MEASURED — a chain with no relayed number in it, established rather than asserted.** ⚠️ **HEADING CORRECTED 2026-08-02 AT CLOSURE, AND THE CORRECTION IS ITSELF A FINDING — see 46a below. This entry read "CLOSES END-TO-END… the epic's headline claim" and that was FALSE: this is a POST-STORY-06 measurement, and stories 07 and 08 ran after it.** The epic's true end-state is **86,024** (TN-4a). **The measurement below is preserved verbatim as EVIDENCE of what was observed at that point; only the end-to-end framing is retired**, per this repo's criterion-vs-evidence cut. Code-reviewer rebuilt the `paths: "**"` set from the post-06 tree and ran TN-9's own one-liner: **measured `84,388`**, against **derived `92,419 − 6,079 (story 01) − 1,952 (story 06) = 84,388`**. **Agreement to the byte, across two stories and two independent edits.** Its own caveat is what makes it worth recording: **the agreement counts only because it RAN THE INSTRUMENT rather than the arithmetic.** With items 34 and 37 on the record — a relayed figure surviving in PM's own producibility entry, and a measurement supplied without its method — **a chain with no relayed number anywhere in it is the property this epic most needed to be able to claim.** The pre-change `22,157` for `CLAUDE.md` also reproduces TN-9 exactly, so the chain is checkable from story 01 through story 06 with no borrowed figure.

46a. **⭐ AN HONESTLY-SCOPED BODY UNDER AN OVERREACHING HEADING — and the HEADING is what travels. Item 46's own defect, found at closure, and it had already cost something.** Every scoping clause in item 46's body is correct and was correct when written: *"the post-06 tree"*, *"across two stories"*, *"from story 01 through story 06"*. **A reader checking the body finds nothing wrong.** The heading said **"CLOSES END-TO-END — the epic's headline claim"**, which no post-06 measurement can support in a seven-story epic whose 07 and 08 were still to run — and which they falsified, adding **+1,047** to `dispatch-pattern.md` and **+565** to `worktree-isolation.md`. True end-state **86,024**, not 84,388: a **1,636-byte** gap under a banner reading *headline claim*.

    **It had already propagated, which is the evidence that this is a mechanism and not an aesthetic complaint.** The orchestrator circulated **84,388 (−8,031)** as the epic's result. ⚠️ **The provenance of that was itself misdiagnosed TWICE before it settled, and the correct account is the third one.** It was first explained as a bad DERIVATION — *"it counted the two wins and assumed the other six files unchanged"*, filed as E-276's `18 of 18` producibility shape. **That account is FALSE and is retired here: `84,388` was code-reviewer's genuine MEASUREMENT against tree `fb15272`, where `dispatch-pattern.md` and `worktree-isolation.md` really were still at 11,433 and 6,727 because stories 07 and 08 had not yet run.** It never failed producibility. **It went STALE, and the orchestrator's actual error was quoting item 46's heading** — *"I did not invent the figure or its framing; I quoted the epic's own headline."*

    **That correction STRENGTHENS this item rather than weakening it, which is why it is recorded rather than dropped.** A bad derivation would have been one person's arithmetic slip. **What actually happened is the mechanism this item names, demonstrated end to end: a true measurement, correctly scoped in a body nobody disputed, was carried into circulation by an overreaching HEADING — by a reader who did exactly what readers do with headings.** Caught only because code-reviewer re-ran the instrument at closure instead of re-running the arithmetic, which is item 46's own stated lesson firing on item 46.

    ⚠️ **DISPOSITION FOLLOWS, and it splits by ROLE rather than by figure — the earlier account led to the opposite instruction and it was wrong.** **`84,388` as the epic's headline or result = CRITERION → superseded by 86,024.** **`84,388` inside code-reviewer's story-06 verdict, or anywhere recording what was observed at `fb15272` = EVIDENCE → PRESERVE IT; it is a true reading of that tree and striking it would falsify the record.** The same number, both roles, one file apart — the case `tool-output-integrity.md` says exists and this is a live instance of it.

    **The transferable form, and it is NOT covered by any existing rule here:** the rules on this page govern the truth of CLAIMS. **A heading is not a claim a reader checks — it is the INDEX ENTRY they quote**, so a body's qualifiers do not travel with it and cannot repair it. **Scope the heading, or the qualifiers protect only the reader who was already going to read carefully.** Companion to item 49 (*rationale broader than the criterion*) at the opposite end of the entry: **49 is the argument overreaching the rule below it; this is the title overreaching the evidence beneath it, and both are invisible to any check that reads the operative text.**

    **Disposition, and the two halves go opposite ways** (item 41's discriminator): the **measurement** is EVIDENCE of the post-06 state and is preserved verbatim — editing it would falsify the record of what was observed; the **"closes end-to-end / headline claim"** framing is CRITERION-shaped, since a reader is meant to take it as the epic's result, so it is **retired rather than updated.**

47. **AC-3's reading settled by SATISFIABILITY rather than by intent — a cleaner instrument than the one PM reached for, and worth preferring where it is available.** PM ruled the `workflow-help` cheat sheet not a location via a **behavioral** discriminator (consulted to determine behavior vs. merely displaying), which needs two conditions to stay honest. **Code-reviewer reached the same outcome with none: under a strict *occurrence* reading AC-3 is UNSATISFIABLE by any correct artifact** — `plan/SKILL.md`'s body must say *"implement E-NNN"* to tell a user what to type, at three sites, and removing it would break the discriminator AC-3's own resolution preserves. Seven of 34 phrases occur in multiple files; **all seven resolve to exactly one declaration.**

    **Both are kept, doing different jobs, and the distinction generalizes**: **a satisfiability argument proves what THIS criterion must have meant and needs no conditions; a behavioral discriminator generalizes to cases the criterion has not met and needs conditions to stay honest.** Prefer satisfiability where available — *"no correct artifact could satisfy the other reading"* is checkable by anyone and appeals to nothing. PM's rule is retained for the next rendering that appears, which satisfiability cannot reach.

48. **A THIRD kind of instrument failure, self-caught: excluding a file by NAME rather than by WHICH PART OF IT matched.** Code-reviewer's first AC-3 script reported **four phrases in ZERO locations** — which would have been **AC-7 RED, an unreachable skill, a fabricated MUST FIX.** Cause: it excluded `workflow-help/SKILL.md` **by filename** when discounting cheat-sheet hits, discarding that file's own frontmatter **declaration** along with its rendering. All four were declared there. **Caught by RESOLVING the hits instead of reporting the count** — the unexpected-count reflex, and the fifth instrument in this epic that ran and did not work on its first attempt. **Transferable: a filter aimed at a REGION of a file must not be expressed as a filter on the FILE**, because the file also contains the thing the check exists to find.

49. **⭐ A NEW VARIANT: THE RATIONALE BROADER THAN THE CRITERION — and it is worse than a RED narrower than its body, because the rationale is what PERSUADES.** E-280-09's AC-1 said *"no **tier row** states review depth"* in **both** body and RED, while its five rationale bullets argued about depth claims in the seam generally. **Seven sites stated depth; three were tier rows. Literal compliance would have left four standing — including the bare absolute that argument (ii) of that very AC is about.** Items 4/8/18/24 are REDs narrower than their bodies, caught by asking what the body requires; **this one survives that check, because body and RED agree.** Only comparing the criterion against **its own justification** finds it. **A reader who finds the rationale convincing concludes the criterion covers what the rationale argues — and nothing in the criterion corrects them.** Ask of every AC: *does the argument I just wrote justify something wider than the RED I just wrote?*

50. **⭐ THE ROUTING-FACT / RUBRIC-CONTENT DISCRIMINATOR, and it bounds the whole "single-source it" principle: A FACT MUST LIVE WHERE ITS CONSUMER READS IT, AND "DON'T RESTATE" CANNOT OVERRIDE THAT.** Whether a per-story reviewer pass happens is decided by the **orchestrator at routing time**, from `implement/SKILL.md`; what a reviewer does once engaged is consumed by the **reviewer**, from `code-reviewer.md`. **Pointing the seam at `code-reviewer.md` for the routing fact would make the seam UNUSABLE — the orchestrator does not read that file at that moment.** So the seam keeps *"skips the code-reviewer for the per-story verdict"* and deletes the depth column, and **this is not restatement, because they are different facts with different consumers.**

    **The test is a mechanical join, not a judgment: which party consumes this, and when?** ⚠️ **This is the first real BOUND on TN-7 that this epic has found.** Every prior application drove toward fewer statements; this one says **a single-sourcing that relocates a fact away from its consumer has not removed a restatement, it has broken a mechanism** — and the failure is silent, because the seam still reads coherently to anyone not routing.

51. **Item 45's mechanism WORKED, on the first story after it was written — and that is a different claim from the pattern recurring.** The prior two calibrations (AC-9b, AC-3) came from an implementer's unprompted judgment. **This one was produced by PM's own written warning in E-280-09's Status block** — *flag a criterion that reads broader than its target rather than complying conservatively.* **A warning placed where the implementer meets it converted a would-be silent conservative compliance into a flag.** That is the item-15 cost principle in its constructive form: **the flag happened because flagging was made cheap and expected, not because anyone tried harder.**

52. **⭐ A BROKEN INSTRUMENT CAUGHT BY TWO PARTIES MEASURING SEPARATELY — a remedy no earlier item in this epic contains.** E-280-09's implementer built a fixture table whose scoped-command column was garbage: `${1:+-- $1}` passed `-- src/` as a **single argument**, so every scoped command errored and fell through to DIRTY. **Its own words: *"I ran 'half 1' as one command instead of two, which is precisely the defect under repair, in the verification of that repair."*** It withdrew the columns rather than present numbers it knew were broken; the unfiltered columns take no path argument, were unaffected, and are what the finding rests on.

    **The detection route: it caught the broken fixture ONLY because its row 3 disagreed with code-reviewer's.** ***"The disagreement was the detector. Had we agreed, I would have shipped the broken table as corroboration."***

    ⚠️ **PM'S FIRST FRAMING HERE — *"independent measurement caught what self-inspection did not"* — OVERSTATED THIS INTO A METHOD, and code-reviewer's narrowed version is the one that survives. Recorded in place of it, not beside it.**
    - **The discriminating property is what an instrument does when it FAILS.** *Fails loudly* (errors, or returns outside the domain of valid answers) → one careful run suffices. ***Fails INTO the domain of valid answers*** → no amount of care by one party detects it, **because there is nothing to notice.** **All five instrument failures in this epic are the second class**: `${1:+-- $1}` fell through to DIRTY, a span splitter to zero spans, a filename filter to zero locations, `git status --porcelain` to non-empty. **Every one produced a plausible answer.**
    - **The rule is NOT "use a second party": put a KNOWN-ANSWER CASE in every measurement, and escalate to a second party only when you cannot construct one.**
    - ***"A detection route that depends on someone else having done the same work by coincidence is not a control; it is luck that paid off."*** Two parties measured the same thing here only because code-reviewer had already published a table. **Had it not, nothing would have caught it.**
    - ***"Disagreement tells you one of you is wrong, not which."*** Resolving it required reading both instruments. **So a disagreement is a cross-check TRIGGER, not a finding — this repo's existing rule for an unexpected count, applied to a second party instead of a second reading. Not a new rule; the same one, one level out.**

54. **The named-repeat remedy CHANGES, and the new one is actionable where the diagnosis was not.** On *ruled on an instance and never asked where else the same claim applied* — twice in three stories (item 44's floors; row C) — **both times the instance ruled on sat in a file the author OWNED and the twin sat in a file it did NOT.** Code-reviewer's reading: ***"That is not carelessness about scope; it is that the natural unit of attention is the file in front of you, and nothing in the procedure asks 'where else did I write this?' at the moment of writing."*** **So the remedy is not more diligence — it is THE QUESTION, ASKED AT AUTHORING TIME, in the story's own Definition of Done.**

53. **TWO STALE PREMISES found in E-280-07's ACs before its implementer met them, both created by the layer moving after they were written.** (a) AC-3 pinned its referent to `implement/SKILL.md` *"in its post-E-280-02 shape"* — **that file has since been edited by E-280-06 and E-280-09.** (b) AC-1c said this story is *"sequenced LAST"* (**it is now second-to-last**; E-280-09 was inserted before it and E-280-08 runs after) and its owning-story enumeration **omitted E-280-09**. **The (b) omission is item 44's lesson applied to a dependency list rather than to a sweep: an enumeration goes stale by the ADDITION of a member, not only by the token-reachability of its search.**

    **Both were found because the orchestrator asked for a stale-premise check on a story whose ACs read a layer that had changed four times since drafting.** ⚠️ **The generalizable form: an AC that pins its referent to another story's OUTPUT ("the post-E-280-NN shape") acquires a dependency on the sequence never changing — and this epic changed its sequence mid-dispatch.** Prefer *"the file as it stands when this story runs"*, which cannot go stale. **A version-pinned referent is a restatement of the sequence, and it drifts exactly like any other restatement.**

55. **⭐ ITEM 53'S DEFECT SURVIVED PM'S OWN REPAIR OF IT, TWICE OVER — and it is item 54's mechanism demonstrated on the party that wrote item 54.**

    **First survival — the untouched twin.** PM corrected E-280-07's **AC-3** stale referent and wrote *"the referent is the file as it stands when this story runs"* into the correction. **The same stale claim was asserted a second time, ten lines below, in the story's Technical Approach** (*"Take `implement/SKILL.md` in its post-E-280-02 shape"*) and was left standing. Found by the orchestrator, not by PM. **This is exactly item 54: the instance was ruled on and nobody asked where else the same claim was asserted** — and unlike the two prior cases, **both sites were in a file PM owns**, so the "attention follows file ownership" explanation does not cover it. **The cheaper version of the defect, and still not caught by its author.** The reader most exposed is an implementer skimming Technical Approach for marching orders, who may treat the AC section above as already settled.

    **Second survival — the repair repeated the defect.** PM's fix repinned AC-3 to *"post-E-280-09"*. **That is another version pin**, acquiring a dependency on 09 being the last edit — **in the epic that inserted a story into its own sequence once already, on PM's own recommendation.** Both sites now read ***"as it stands when this story runs"***, which cannot go stale.

    **Two things to carry.** (1) **A correction that restates the general rule in its own text is not thereby immune to the specific defect** — PM wrote the generalization *into the fix* and then violated it in the same fix. (2) **Item 54's remedy is confirmed by its own violation: the question must be asked at authoring time, mechanically, in the Definition of Done.** Knowing the rule, having just written the rule, and being the file's owner were jointly insufficient — which is the 8-of-8 authorship record arriving one more time, on the epic's own record-keeper.

56. **An asymmetry worth stating once, from the two enumeration-staleness classes this epic found.** **A SWEEP's enumeration going stale produces a FALSE CLEAN. A DEPENDENCY LIST's enumeration going stale produces an UNREACHABLE AC.** Same defect, opposite failure modes — **and only the second announces itself.** (Item 44 is the sweep case; item 53(b)'s omission of E-280-09 from AC-1c's owning-story list is the dependency case, and MF-1 was the same class caught in planning.) **The practical consequence: a stale dependency list is self-limiting and a stale sweep list is not, so the two do not warrant equal vigilance — the sweep is the one that needs a positive control.**

57. **⭐ THE POSITIVE-CONTROL RED PRODUCED ITS OWN NAMED FAILURE, TWICE IN ONE FREEZE — and that is the amendment WORKING, not three people being careless.** AC-1c's added RED forbids *"a zero reported with a control that also returned zero."* The implementer's first combined invocation used the control string `POSITIVECONTROL_frozen tree`, **which matches nothing** — the exact named failure. Its note: ***"the AC names that exact failure and I hit it anyway… the control has to be known-present, and 'I wrote a control' is not the same claim as 'the control fired.'"*** In the same freeze the orchestrator passed four paths as one unquoted string — **the `${1:+-- $1}` defect from story 09, in the freeze for the story about sweeping** — and noticed *only because story 09's retraction had made it look.*

    **Final reading: retired forms → 0, control `frozen tree` → 34. Verified absence.** **The point is that the AC did not PREVENT the defect; it made the defect VISIBLE, which is the entire claim of a positive control.** Without it, the first invocation's zero and the correct zero are the same character on the screen. **Corollary, and it is the transferable sentence: writing a control is not the same claim as the control firing — so the control's own hit count must be REPORTED, not merely obtained.** That is why the AC requires the number alongside the zero rather than requiring that a control exist.

58. **⭐ A POPULATION NO TOKEN SWEEP CAN REACH, found in the story built to sweep — the strongest concrete case this epic has for `doc-sweep.md` step 2.** `dispatch-pattern.md`'s main-session role paragraph and Team Roles item 1 both stated **`git add -A` after each story passes review** — the **retired ORDERING**, carrying neither `unstaged` nor `current story`. **A token sweep for the invariant returns ZERO on both.** They encode a **consequence** of the invariant rather than the invariant itself, which is precisely the class `doc-sweep.md` names and which **is invisible to step 1 by construction**. Both fixed; `after each story passes review` now returns 0 across `.claude/rules/`. **Pair with item 44: a floor names the token-reachable sites; this names a population that has no token at all — so a token sweep is a STARTING POINT and never a check, and the sites that matter most are systematically the ones it cannot see.**

59. **⚠️ PM'S AC-1c PASS WAS WRONG AND IS RETRACTED — and the bound it exposes is on PM's own amendment.** AC-1c is **RED**: two occurrences of the retired **ordering** survive in trees the AC explicitly covers — `.claude/agents/product-manager.md:123` and `.claude/skills/multi-agent-patterns/SKILL.md:50`, both stating *"`git add -A` after each story passes review"*, **false on both clauses** (the freeze stages on the completion report, and the frozen tree is what isolates).

    **⭐ THE BOUND, and it is the honest limit of the positive-control RED PM added: A POSITIVE CONTROL PROVES THE INSTRUMENT EXECUTED. IT DOES NOT PROVE THE PATTERN IS WIDE ENOUGH.** The control `frozen tree` fired 34 times — pattern form, path set and tool all resolved — **and it would have fired identically with a pattern that could not reach the token-free population.** PM accepted `0 + control 34` as verified absence **without asking whether the patterns could reach the class the story had just discovered.** Execution and coverage are different properties and the control speaks only to the first.

    **The diagnosis is the epic's central pattern at its sharpest, and it is not PM's alone: the story identified the token-free class (item 58), fixed both members in the file it owned, and then verified layer-wide with an instrument that cannot see the class it had just discovered.** Items 44 and 58 colliding. **The sentence PM wrote one message earlier — *a token sweep is a starting point and never a check* — was falsified by the sweep PM then passed.**

    **Two facts worth keeping.** (a) **One of the two surviving sites is PM's own agent definition**, which told PM the wrong ordering while PM verified the sweep that missed it. (b) **Code-reviewer's own first attempt under-matched** (30-character window against a 45-character gap, returned zero) **and it did not report that empty as absence** — it widened until a known-present control fired. **That is the correct handling of the same failure PM mishandled**, and the difference was not care but *treating an unexpected zero as a trigger rather than a result*.

    **Bookkeeping, ruled by PM:** E-280-07 carries both fixes. **AC-1c is 07's verification and it is 07's RED — the failure belongs to the criterion that owns layer-wide reach, not to the closed stories whose files happen to hold the text.** File-disjointness is a dispatch-parallelism discipline, not a constraint on a serial epic's remediation. **E-280-04 and E-280-06 need no annotation: neither story's ACs were wrong**, and recording a defect against a story whose criteria did not reach it would misattribute it. This item is the cross-story record.

60. **A SILENT failure in the scoped freeze check, and the class difference is what sets its severity.** **zsh does not word-split unquoted `$VAR`**, so a multi-path routed set passed by variable reaches git as one argument matching nothing — **and `git diff --quiet` on a non-matching path exits 0.** Every scoped half reads clean, silently. Reproduced: `git ls-files -- $P` → 0 files, expected 3, **no error**. **A multi-path routed set is the normal case from here on.** Remedy: a **path-set control** — confirm `git ls-files -- <routed-paths>` returns the expected count before trusting any scoped reading. Owner `implement/SKILL.md`. **Contrast with the orchestrator's earlier `${1:+-- $1}` defect, which ERRORED LOUDLY: same root cause, opposite detectability, and only the silent one needs a control.**

61. **⭐ A REPORTING RULE, not just an instance: *"I looked and found nothing" and "I did not look" are INDISTINGUISHABLE IN A RECORD unless one of them is stated.*** This epic produced several checks whose entire value was a **clean result** — E-280-08's two premises re-checked before its implementer met them (five `ratchet` sites still five; AC-11's fourth-site premise intact, no fifth appeared); the porcelain enumeration that resolved clean (item 10); a floor whose every site resolves to `no change needed` (item 44(c)). **Only the ones someone bothered to state are legible afterwards, and an unstated clean check is worth exactly as much as an unperformed one to every later reader.**

    **Two consequences.** (a) **State negative results as results**, with what was checked and what came back — a closure record containing only findings misrepresents the method as having found problems everywhere it looked. (b) **A count that did not move is EVIDENCE, not a non-event**: five `ratchet` sites surviving four stories' edits to that file is precisely what AC-13 exists to establish, so *"unchanged"* is the measurement rather than the absence of one.

62. **The E-280-07 artifact defect, and its lesson is about WHO READS ARTIFACTS in this system.** The rebuilt AC-1c instrument existed in the completion report and **nowhere in the tree**; the committed artifact still carried the refuted token invocation, `control = 34`, and an unqualified *"AC-1c PASSES"*. **The successor that reads that artifact is E-280-08 — a STORY rather than a person, which will do what the artifact tells it.** Corrected to a struck retirement block naming both survivors, the rebuilt instrument at 0/0/36, the negative-control table, and a conclusion qualified to *"PASSES on the rebuilt instrument"*. **Transferable: a correction that lives only in a completion report has not landed. The report is read once by one party; the artifact is read by whoever comes next, and in this system that is often not a person at all.**

65. **⭐ A CLEAN APPROVAL CAUSES NO ARTIFACT EFFECT, SO IT IS THE ONE VERDICT THE SYSTEM CANNOT VERIFY WAS DELIVERED — and E-280-06 is the worked instance of a bound `dispatch-pattern.md` had already STATED but never OBSERVED FIRING.** Code-reviewer's finding, and it explains a fact that otherwise reads as random carelessness.

    **The question is not "why was 06's verdict lost" but "why 06 and none of the other six."** Every other story's verdict carried MUST FIX or SHOULD FIX items that were routed, fixed, and **left changes in the tree**; their delivery is therefore inferable from the effect they caused. **E-280-06 is the only story where code-reviewer filed nothing.** A findings-free APPROVED changes no file, so **there is no effect to look for and no way to confirm it arrived.** Six of seven verdicts are recorded; the seventh is precisely the clean one.

    **This is `dispatch-pattern.md`'s own ⚠ bound, firing.** That rule already says the effect-in-the-artifact check *"reaches ACTIONABLE findings only"*, and names as unverifiable exactly this class — a question, a ruling, a hold, a status report. **A findings-free approval belongs in that list and was not in it.** The rule was correct in advance and had never been seen to fire; **it has now been observed, which is worth more than the rule restated**, and the observation was made by the party whose verdict went missing rather than by the party who noticed the gap.

    **Remedy, and it is small: PM records the verdict AND its tree SHA in the story file ON RECEIPT, regardless of findings.** This epic made the tree an address; that makes the verdict one. **It converts a verdict from something whose arrival is inferred from its side effects into something with its own artifact** — which is the same move, one layer up, that the freeze made for the review surface.

    **Two things this does NOT establish, stated because the temptation is to over-read a tidy explanation.** (a) It does not excuse the item-63 bookkeeping failure: the `TODO` status and the missing verdict are **two separate defects** that happened to land on the same story, and the status flip had nothing to do with findings. (b) Code-reviewer's own bound stands and was volunteered: **it can establish that it SENT the verdict, not that it was delivered or recorded.** Its reproduction against the immutable tree — five figures, five for five — establishes that the review HAPPENED and its content was sound; **it does not and cannot establish delivery, and nothing in this system can.**

64. **⭐ PM RULING ON CODEX P1 #1's SECOND HALF: THE AC CHECKBOX IS A DRAFTING WORKSHEET; THE AUTHORITATIVE PER-AC RECORD IS PM'S VERDICT COMMENT UNDER THE STATUS LINE.** Recorded here rather than in a message because *"contradictory truth sources"* is a fair description of the artifact **until someone states which source is authoritative**, and a ruling that lives only in a thread has not landed (item 62).

    **The finding is ACCEPTED, not dismissed — and the remedy is the stated convention, NOT ticking boxes at closure.** Codex observed `E-280-08` and `E-280-09` carrying `DONE` over unchecked ACs and DoD. The observation is true. Ticking them now would **manufacture tidiness**: it would imply a worksheet was maintained through dispatch when it was not, and would backdate a record at the exact moment this epic is insisting records not be backdated.

    **The measured basis, because "it's the convention" is a claim and I checked it.** ⚠️ **RELABELLED after code-reviewer checked this ruling rather than inheriting it. PM's figures count EVERY top-level checkbox in each file — acceptance criteria PLUS Definition-of-Done items — and were read as AC counts because the surrounding ruling is about AC checkboxes.** Both instruments are given, because both support the conclusion and only one of them is true of what it says:

    | Story | AC checkboxes only (CR) | **All checkboxes, ACs + DoD (PM)** |
    |---|---|---|
    | 01 | 10/10 | 14/14 |
    | 02 | 17/17 | 20/20 |
    | 04 | 0/16 | 0/19 |
    | 06 | 9/9 | 9/13 |
    | 07 | 0/10 | 0/13 |
    | 08 | 0/16 | 0/20 |
    | 09 | 1/3 | 1/6 |

    **There was no convention to violate under EITHER instrument** — 10/10, 17/17, 0/16, 9/9, 0/10, 0/16, 1/3 is exactly as scattered as the all-checkbox column. The practice is inconsistent across seven stories written and executed by the same implementer, in both directions. **The ruling is unaffected; only its evidence was mislabelled.**

    ⭐ **RELABELLED RATHER THAN RECOUNTED, DELIBERATELY, and the reason is a finding.** Re-taking the figures AC-only would have made the label true and **hidden what the discrepancy revealed**: the +3/+4 gap in every row **is** the Definition-of-Done block, unchecked in six of seven stories. **So the DoD checkboxes are a SECOND instance of this ruling's own residual** — an unmaintained field nobody reads — sitting in the same files as the first. Recounting would have erased the evidence of it; relabelling surfaces it.

    ⚠️ **THIS CHANGES THE SCOPE OF THE CODIFICATION, and that is the actionable part: the story-template repair routed to claude-architect covers TWO unmaintained fields, not one** — the AC checkboxes and the Definition-of-Done block. A repair scoped to the ACs alone would leave the second standing, in the same files, with the same failure mode.

    ⭐ **The general practice this yields, which is worth more than the instance: WHEN TWO INSTRUMENTS DISAGREE, THE DISCREPANCY IS DATA — normalizing to one instrument destroys it.** The reflex on finding two counts of the same thing is to pick the correct one and discard the other. Here the *difference between them* carried the finding, and neither count alone contained it. **Reconcile before you normalize, and ask what the gap is made of.**

    ⚠️ **This is the FIFTH carrier of this epic's signature shape — a correct conclusion resting on a figure that does not measure what it is labelled as measuring — and it landed INSIDE the ruling that closes a Codex finding ABOUT checkbox state.** Caught by a non-author who tested a hypothesis about the discrepancy instead of guessing at it, which is the detection route the whole 8-of-8 authorship record predicts.

    **The inversion is the diagnostic and it settles which field to trust** (the orchestrator's observation, and it is the sharp one): **06 has CHECKED ACs under a `TODO` status while 08 and 09 have a `DONE` status over UNCHECKED ACs.** Two fields disagreeing in **opposite directions** on different files cannot both be tracking the verdict — it says the status line and the checkboxes are maintained by different passes, and that **neither is the record.**

    **Why the verdict comment wins, on grounds independent of convenience:** (a) PM's atomic status-update protocol names exactly two sites — the story file `## Status` and the epic Stories-table row — and **has never mentioned checkboxes**; (b) the verdict comment carries **author, date, frozen tree SHA and the enumerated ACs** (E-280-09's names AC-1, AC-2, AC-3 explicitly), while a checkbox carries **none of those four**; a record with provenance beats one without. (c) A checkbox cannot express the thing this epic most needed to express — *which* ACs passed against *which* tree.

    ⚠️ **The residual, stated rather than buried: this ruling makes the artifact CONSISTENT but leaves the checkboxes as a field nobody maintains and nobody reads — which is item 63's class exactly, one field over.** The honest options are to delete the checkboxes from the story template or to make them a stated worksheet; **both are context-layer changes owned by claude-architect, not PM, and neither is E-280 work.** Routed to the closure codification as a candidate, so the ruling does not read as having disposed of a problem it has only named.

63. **A STATUS FIELD NOBODY READS CANNOT FAIL LOUDLY — the vacuous-check class, in the RECORD rather than in an instrument.** E-280-07's story file read `TODO` for its entire dispatch while the Stories table read `IN_PROGRESS`: **an atomic status update performed at one of its two sites, by the party that owns the protocol.** It survived undetected because **every gate in this dispatch keyed off the Stories table or the orchestrator's own tracking — nothing reads the story file's status.** Surfaced only when a `DONE` edit failed on stale content, and **disclosed rather than silently corrected**, which is what makes it usable: a quiet fix would have left the record showing a clean dispatch. **Same class as the four vacuous guards (item 2 and `tool-output-integrity.md`'s anti-vacuity paragraph): a check with no reader is not a check, and a duplicated field where only one copy is consulted is a divergence waiting with nothing to announce it.**

---

## TN-21: CLOSURE HANDOFF — written for a successor PM who cannot ask questions

**Status when written: E-280-08 is the last story and is `IN_PROGRESS`. Stories 01, 02, 04, 06, 09, 07 are `DONE`.** After 08: **Codex over the whole epic diff → code-reviewer's closure integration review → the full-suite green gate → closure merge with operator approval before any commit.**

⚠️ **Written BEFORE the outgoing PM's context was exhausted, deliberately — flushing to the artifact before the pressure peaks rather than after, per `dispatch-pattern.md`. If E-280-08's AC verdict is not recorded in its story file, it was not issued and the successor owns it.**

### 1. Closure deliverables, each with where its inputs live
- **TN-4(a) per-subtree diagnostic** — stories 01, 04, 06, 08 each record their own subtree delta; **nobody aggregates them.** PM states the per-subtree reading once at closure, as a **diagnostic**, satisfying Success Criterion 3. **⚠️ TN-4(c) is MANDATORY and must be stated IN the reading**: `context-ratchet.sh` prints `FAIL -- the context layer grew past baseline` against the **retired** 2,640 baseline; that word is a fact about a stale baseline, **not a verdict on this epic**. Byte figures come from **code-reviewer's final measurement**, never from a completion report.
- **Review scorecard** — the existing table in History covers planning. **Dispatch rounds are not in it**; add them or state that it is planning-only, per the E-279 composition-note precedent.
- **Documentation assessment** (`.claude/rules/documentation.md`) and the **eight-trigger context-layer assessment** (`.claude/rules/context-layer-assessment.md`) — **per-trigger yes/no verdicts recorded in History; a blanket "no impact" is not sufficient.** ⚠️ **TN-20 item 1 is the epic's standing codification candidate** and should be handed to claude-architect at that assessment; **items 24, 36, 44, 45, 50, 52, 54, 57, 61 are the other strong candidates.**
- **TN-15's four memory-reconciliation obligations.** ⚠️ **Two need IDEAS with resolution triggers, because their agents are NOT on this dispatch team**: code-reviewer's file, and software-engineer's `dispatch-git-gotchas.md` **plus** its `MEMORY.md` index row. **A third**: `.claude/agent-memory/claude-architect/epic-codifications.md:112` describes the E-267 case as *"cited as a CONCRETE CASE"* in `tool-output-integrity.md`; **after E-280-01 it is a casebook pointer.** CA's own directory, reconciled at the closure authoring window.
- **PM's own memory**, authored in the **worktree** copy at Step 8 sub-step 3 so it rides the closure patch: `MEMORY.md` Active→Archived flip, `archived-epics.md`, and **the numbering line — E-280 and E-280-09 are consumed; 03 and 05 are RETIRED-NOT-REUSED.**

### 2. Things NOT in the artifacts — the only irreplaceable content
- **E-280-07's AC-1c passed with a NAMED UNCLOSED RESIDUAL, carried as an OBLIGATION not a caveat.** Layer-wide semantic coverage is **NOT established**. Code-reviewer is **on record intending to run its own semantic pass** over the epic diff rather than treat that zero as class-level clean. **If it does not, the residual is still open and the successor must say so at closure.**
- **PM deliberately did NOT order a 199-file semantic read**, priced against a residual four independent checks failed to surface any instance of. **That was a judgment, not an oversight** — do not re-litigate it silently, and do not report it as coverage.
- **The 6-freeze churn on story 02 is an ORCHESTRATOR routing finding (item 13), NOT implementer indiscipline, and NOT evidence about the freeze design** — the design's evidence is the last round, where the freeze held for an entire review with zero lines moving.
- **The `805c0a4` fold-forward is precedent; `cd1c4cd` is NOT an instance of it** — that tree carried a direct verdict on its own merits.

### 3. The outgoing PM's own open self-findings, so they are not re-derived
- **The AC-1c PASS on `cb3a24a` was RETRACTED** (item 59) and the bound it exposed is load-bearing: **a positive control proves the instrument EXECUTED; it does not prove the pattern is WIDE ENOUGH.**
- **Item 34 carried the RELAYED `117`; the measured figure is `118`** — router-must-not-be-a-source, inside the entry about producibility.
- **Item 55: the repair repeated the defect it was repairing** — a version pin replaced by another version pin, and a twin left standing ten lines below.
- **Item 63: the one-site status flip**, above.
- **Four AC calibrations were needed at first application** (AC-9b, AC-18, AC-3, E-280-09 AC-1) and **all four were caught only because an implementer FLAGGED rather than complying conservatively** — item 45. **The Status-block intervention that produces those flags is the single most effective thing tried in this dispatch; keep using it.**

### 4. Where everything else is
`epic.md` Status block (closure obligations, 4 items) · **TN-4(c)** (the mandatory `FAIL` caveat) · **TN-15** (seam inventory + memory obligations) · **TN-19** (measured report tables) · **TN-20 items 1-63** (every dispatch finding) · each story's Status block (gates, trees, per-story findings). **Prefer the artifact over any message, including this one.**

---

### TN-5: Distillation — what "every RULE survives" has to mean
"Compress carefully" is an action, not a property, and is satisfiable by compressing badly. The checkable form is a **per-rule inventory**: enumerate the rules stated in `.claude/rules/tool-output-integrity.md` before the change, and after the change record a per-rule verdict naming where each one now lives. A rule with no surviving location is a failure; a rule that moved to a pointer is a pass with its target named. The inventory is the artifact the verdict is read from, and its row count against the pre-change enumeration is what makes an omission visible. Incident narratives compress to pointer or evidence form — and per `.claude/rules/tool-output-integrity.md`'s own criterion-vs-evidence cut, a figure a reader must *see what was observed* is evidence and is preserved verbatim, while a figure a reader must *meet* is a criterion and may be restated.

### TN-6: E-271 boundary — RESOLVED by the OQ-A ruling; E-280 goes first and E-271 is DRAFT
**Operator ruling, 2026-08-02: E-280 dispatches first, and E-271 is demoted to DRAFT now.** PM executed the demotion the same day (status flip plus a dated History entry in `epics/E-271-workflow-process-redesign/epic.md`, citing the operator ruling and the grounds already on record). **The consequence for this epic is the one that matters: the semantic collision is DISSOLVED, and E-280-02 and E-280-04 are written to FIXED TEXT against the layer as it stands today.** No story defers to E-271's post-state, and no story reads an E-271 file.

The ruling picks none of OQ-A's three offered options. It takes a fourth the options did not contain — demote the frozen epic rather than sequence around it, unfreeze it, or cut E-280's mechanism — and it is strictly better than all three: E-271's freeze was what made the conflict irreconcilable, and the freeze had already been overtaken by events (its P-6 half-discharged and half-prohibited, its anchors stale, one AC tombstoned by E-279).

**What the four conflicts become.** All four were pairs of the form "E-271's frozen AC forbids E-280's edit." With E-271 in DRAFT and owing re-refinement against the post-E-280 layer, each collapses from a contradiction into an input to that re-refinement:
- **E-271-01 AC-5** (staging boundary byte-identical) — E-280-02 rewrites the staging boundary. E-271's re-refinement writes its AC against the rewritten one.
- **E-271-02 AC-6** (code-reviewer rubric structure unchanged) — E-280-04's tier keying changes rubric application. Same disposition.
- **E-271-01 AC-1** (task-state completion polling) — this one is NOT merely deferred and must not be lost. It answers the same question as E-280-02's freeze by a different mechanism, so shipping both would leave two mechanisms for one fact — the restatement defect this epic's own design principle targets. E-280 ships the freeze; **E-271's re-refinement must decide whether AC-1 still has a job, and the honest answer may be no.** Recorded so the question gets asked rather than inherited.
- **E-271-01 AC-2** (Step 5 item 3 rewrite, adjacent to the item 2 / item 4 branch E-280-02 deletes) and its "the ONLY edits to `implement/SKILL.md` for this epic" declaration — both re-derived at re-refinement.
- **E-271-03 owns `.claude/skills/plan/SKILL.md`**, which E-280-06 also touches (line-1 frontmatter). Genuinely disjoint from body edits, and now moot in either order.

**Anchors still move, and this ruling does not change that.** E-271's line anchors were already stale on 2026-07-28 (`SKILL.md` cited at ~:462/:529/:630, actually at 477/547/655-656) before E-280 touches anything. **Re-derive every anchor by phrase** — that obligation now sits wholly on E-271's re-refinement, and E-280's own stories cite by phrase and heading rather than by line for the same reason.

**The residual E-280 owes.** Nothing to E-271's dispatch. One thing to its re-refinement: the post-E-280 shape of `implement/SKILL.md`, `code-reviewer.md`, `dispatch-pattern.md` and `worktree-isolation.md` is what E-271 must be re-refined against, and that shape does not exist until this epic closes. That is a fact about scheduling, not an obligation with an owner in E-280.

### TN-7: Design principle (operator-ratified) and its test
Every change must shrink the error-generating surface, not merely cut review cost. The test to apply per change: *after this edit, is there one fewer place where a fact is restated?* A change that adds a new rule to enforce an old one fails this test even if it saves review time. Prefer structure over prose, single-sourcing over cross-references, and verification at authoring time over verification at review time.

### TN-9: The always-on load, measured (design owner, 2026-08-01)
**Every agent pays ~92 KB / ~23,000 tokens of always-on context per session.** That is `CLAUDE.md` (22,157 B) plus the seven rules whose frontmatter is `paths: "**"`:

| File | Bytes |
|---|---|
| `tool-output-integrity.md` | **29,251** |
| `workflow-discipline.md` | 12,035 |
| `dispatch-pattern.md` | 11,433 |
| `worktree-isolation.md` | 6,727 |
| `agent-routing.md` | 6,095 |
| `agent-team-compliance.md` | 3,586 |
| `vision-signals.md` | 1,135 |

Total **92,419 bytes ≈ 23,000 tokens**, and `tool-output-integrity.md` alone is **32% of it** — 98 lines, 4,850 words, 21 paragraphs over 600 characters. That is the ordering argument for E-280-01 going first.

The figure is reproducible in one command, which is why the design owner proposes it as a **second reported metric attached to OQ-1** rather than a replacement gate — simple-first, no new infrastructure:

```
tot=0; for f in .claude/rules/*.md; do head -8 "$f" | grep -q '"\*\*"' && tot=$((tot+$(wc -c <"$f"))); done; echo $((tot+$(wc -c <CLAUDE.md)))
```

This is the quantity that actually matters and the one the line-priced gate cannot see (TN-4b).

**The reading that ended the gate.** At `2026-08-01T22:40:25Z`, on a **clean working tree at HEAD `31ae5b1`**, the instrument returned **exit code 1 at +418 lines** over the 18220 baseline: rules +9, agents +0, skills +35, agent-memory +374 (9 + 0 + 35 + 374 = 418). Measured first-hand, not inflated by uncommitted planning writes, and reproducible on demand. The archive's +350 is an earlier reading; both are true of their own moments. **The agent-memory component is E-279's closure writes and is 89% of the overhang** — a subtree that grows as a function of doing work, against a fixed total, which guarantees perpetual FAIL and a perpetual exception ritual. That is the reading the operator ruled on: *"I don't think gating and ratchets are working. I think we just need periodic refinement."* **This figure is now EVIDENCE of why the gate went, not a debt E-280 inherits.** Preserve its value; do not update it.

One consequence the ACs still enforce, and it survives the retirement on its own merits: **moving narrative into `.claude/agent-memory/` does not remove it from anybody's context** — it relocates it inside the same layer, deferred to one agent instead of resident for all. Narrative displaced by the distillation belongs in `.project/research/`, outside the layer entirely, which is why E-280-01 AC-7 names that destination and forbids the other. The argument was originally made in ratchet-credit terms; it does not depend on them.

### TN-10: Checklist-4 telemetry — run at planning time, deliberately not adjudicated
The design owner ran `.claude/hooks/dispatch-telemetry.py` against session `fd615ae6-…` (E-279's dispatch, closed — last write 20:43, roughly two hours before the read, so the "never write a rate from a live dispatch" bar is clear). Over 1,331 agent turns, **pre-adjudication**: STEER? 79 · UPHELD 83 · SELFCORR? 180 · GATE 82 (excluded by design) · OP-STEER 0.

**Those 342 rows are deliberately NOT adjudicated, and that is a finding rather than an omission.** `dispatch-telemetry-design.md` already establishes the band cannot discriminate: STEER spans 4.3–7.6 pre-prune and 2.9–7.8 post, overlapping, with both the highest and lowest readings post-prune; SELFCORR's rise is confounded by the vendor's own note that Opus 5 narrates corrections more, so correction-count is not error-count. Paying hours of reading for a number the design pre-declares non-decisive is precisely the cost inflation this epic exists to reverse.

The decisive test is the four targeted recurrence checks, which are bounded booleans against E-279's landed artifacts. **Check 4 (software-engineer's respx lines) is already out of E-279's reach** — its only story wrote no code — so it stays open regardless. Run checks 1–3; record the candidate counts with the pre-adjudication caveat; leave check 4 open. E-280's own dispatch yields a second same-class reading at no extra cost. Sonnet 5 and Fable 5 remain unfalsified either way.

### TN-14: The telemetry was adjudicated, and the finding is a taxonomy rather than a rate
Supersedes TN-10's "do not adjudicate" framing on one point: the design owner adjudicated the session anyway, and the result is the strongest single piece of design evidence for TRIM 1 in this epic.

**First, the baselines are RAW.** Re-running the instrument against two recorded baseline sessions reproduced their recorded rates exactly — `32e9948e` 21/483 = 4.35 against a recorded 4.3, `15538a3f` 20/690 = 2.90 against a recorded 2.9. No adjudication was ever performed on them. Two consequences: the comparable E-279 figure is the **raw** 79/1331 = **5.94 per 100 turns**, which sits inside *both* the pre-prune band (4.3–7.6) and the post-prune band (2.9–7.8) — the guardrail reads **no signal**, as the design predicted. And an *adjudicated* E-279 number would be incomparable to every baseline, so the earlier recommendation against full adjudication was right on correctness grounds as well as cost.

**Second, the adjudication of all 79 candidates, disjoint and priority-ordered:**

| Class | n |
|---|---|
| Wrong direction (agent → main) | 8 |
| Assignment / spawn traffic (design excludes) | 7 |
| Gate relay — Codex, CR findings (design excludes) | 7 |
| **Freeze / release / hold / confirm choreography** | **32** |
| Praise, main's own self-corrections, questions, refinements | 16 |
| **Genuine steering** | **9** (8 firm, 1 weak) |

**89% false-positive rate, and the largest real category — 32 of 79, 41% — is protocol choreography, which is TRIM 1's exact target.** Not steering, not error: pure coordination overhead. Representative rows: *"FREEZE — stop editing, confirm done"*, *"RE-FROZEN — all four files"*, *"FINAL BATCHED RELEASE — 4 prose items"*, *"ONE WORD release, then we stage"*, *"Did CR's abandonment refinement land?"*. **This is measured, on one session, and is usable in an AC as measured — unlike TRIM 3's three-of-five, which stays a counterfactual.**

**Honest bound, and it is the design owner's own:** one session was adjudicated. The three largest false-positive classes are structural — they come from the instrument's cue matching and from the dispatch protocol, not from anything E-279-specific — so a comparable rate in the baselines is *expected*. That is an inference, not a measurement, and must not be written as one.

**Instrument verdict, which belongs with OQ-1:** the guardrail was never measuring steering, and the noise is dominated by a category this epic is about to remove. **A post-E-280 STEER reading will move for reasons that have nothing to do with steering, and the operator should be told not to read any such delta as evidence about steering.** That is the same class of problem as the byte-versus-line divergence in TN-4(b): two independent respects in which our instruments price something other than what we care about.

**Recurrence checks:** (1) command menu — no counter-evidence; response-protocol vocabulary appears across all agents, which is a presence signal, not proof of correct handling. (2) CA's codification bullet — **PASS**; CA worked directly from `epic-codifications.md` throughout, the designed post-collapse behavior. (3) CR's five Mandatory Review Checks — **SPLIT, do not score as pass**: the pointer half verifies, but the behavioral half is uncovered because E-279's stories were shell, markdown and memory. (4) SE respx — **OPEN**, as the design already declared, and unaffected by anything run here.

**Two free readings:** Sonnet 5 participated (`dw-e279`, 7 turns, 0 self-correction candidates) — record as *participated, sample insufficient*, not as a discharge for the Sonnet class. And the alias register re-verified: all five `opus[1m]` agents resolved to `claude-opus-5`, `sonnet` to `claude-sonnet-5`, no drift.

### TN-11: The verdict-authority site floor (E-280-02 AC-2, E-280-07 AC-2)
⚠️ **This list is a floor for FINDING sites. It is NOT a list of sites to change, and after the OQ-5 ruling most of it should resolve to `no change needed`.** It was compiled on 2026-08-01 under a design that struck PM's AC authority; the operator ruled the opposite on 2026-08-02. **An implementer who treats these rows as a deletion worklist will implement the design the operator declined.** A sweep returning exactly these sites and nothing else did not search; a sweep that deletes most of them did not read the ruling.

- `.claude/skills/implement/SKILL.md` — Step 5 item 1 (the existing single-verdict branch), item 2 ("send the work to both in parallel"), item 4 (the dual-route branch), the `### Gate Interaction` section, the `PM-Reviewer AC Disagreement` paragraph. **Live territory**: these are where re-issuance and choreography live, and they are E-280-02's real target. The `PM-Reviewer AC Disagreement` paragraph in particular is *load-bearing under one-of-each* — with two issuers preserved, a disagreement path is still needed.
- `.claude/rules/dispatch-pattern.md` — "Both PM and code-reviewer must approve before the staging boundary advances"; "PM is authoritative on ACs"; Team Roles items 2 and 4. **Both quoted sentences SURVIVE (OQ-5).** Verdict here is expected to be `no change needed` on the authority statements, and edits only where the file describes the review SURFACE by staging state.
- `.claude/rules/workflow-discipline.md` — any dependent statement of PM's dispatch-time AC role. **Expected `no change needed`.**
- Both spawn templates, for the "unstaged = current story" invariant. **Live territory** — this is a mechanism statement the freeze makes false, not an authority statement.

The design owner's original estimate of roughly 25 deleted lines was made under the superseded design and should not be treated as a target; the surviving deletions are the choreography and the staging-state definitions, and the honest expected magnitude is smaller.

### TN-13: Planning discipline for this epic (it runs under its own trims)
By operator order this planning round runs under the trims it implements: single verdicts against frozen files, no confirmation-chase or insurance messaging, **one internal review iteration planned rather than three**, and review stopping when a pass finds nothing new. If a reader later compares this epic's review-pass count against E-271's four rounds or E-276's six, the difference is deliberate and is the epic's own thesis applied to itself.

**Why TRIM 1 is split by FILE (02 skill-side, 07 rule-side) but not by PART.** The three parts — one-of-each verdicts, frozen state, no insurance messaging — share one seam, and splitting them by part would leave contradicting sentences standing between stories, which is the named cause of the harm this epic treats. Splitting by file does not: 02 defines the mechanism and 07 states it, so 07 simply matches 02's landed shape. A partial landing across the part boundary is worse than either endpoint; a partial landing across the file boundary is just an unfinished sweep.

⚠️ **Correction, 2026-08-02.** This paragraph previously closed *"…which AC-1 of 07 catches by absence"* — **and that was false as written**, which the spec audit caught. AC-1's layer-wide form was unsatisfiable: seven instances of the invariant sat in files 07 does not own, in stories that ran after it, with no AC anywhere removing them. The safety property is now real rather than asserted, but it is carried by **three** things and not one: 04 AC-15 and 06 AC-9 remove their own files' instances, 07 AC-1 checks the files 07 owns, and **07 AC-1c is the layer-wide catch, reachable only because 07 now runs last.** A claim that a split is safe *because* an AC catches the residue is worth exactly as much as that AC's reachability — this one had none until the audit forced the check.

### TN-15: Gate retirement — the seam inventory
The operator retired the context-layer size **gate** on 2026-08-02 and replaced it with **periodic refinement**. This note records where that ruling lands in the files, because a ruling nobody can locate in the layer is a ruling that gets re-litigated.

**Seams PM found by search, all inside E-280-08's two files.** CA is the design owner and locates the seams authoritatively; this is a floor for that search, not the answer, and a search returning exactly these did not search.
- `.claude/rules/context-layer-assessment.md` — **trigger 7** (the gate itself: "MUST either offset the growth… or record an operator-signed exception"); **trigger 8**'s condition (b), which prices promotions against the same baseline; the **"Cadence and the ratchet"** subsection under the Learning-Loop Lifecycle, whose closing sentence is the strongest surviving statement of the gate ("The layer's SIZE… IS hard-bounded").
- `.claude/rules/context-layer-assessment.md`, **a retired-claim site sharing no token with the gate** and therefore reachable by no token sweep: the Learning-Loop Lifecycle opener, *"Triggers 7 and 8 make the context layer prune and re-home knowledge, not only accrete it."* After AC-10a, trigger 7 observes and routes rather than forcing an offset, so the pruning claim is carried by trigger 8 and the periodic pass. It contains neither "baseline" nor "exception" nor "ratchet". AC-6's regenerated enumeration should reach it, but it is named here because it is exactly the kind of site a floor exists to stop being rediscovered. (Design owner, 2026-08-02.)
- `.claude/rules/context-layer-guard.md` — **two** sites, both asserting the gate as the thing that bounds the layer: the closing sentence of `## CLAUDE.md Scope` ("the four context-layer subtrees are bounded by the ratchet"), and `## MEMORY.md Scope` ("their line counts are priced by the ratchet baseline — there is no separate unenforced line target"). **Both sentences use the gate to justify the ABSENCE of any other size discipline**, so deleting the gate clause alone leaves each paragraph asserting that nothing bounds the thing it is about. This is `doc-sweep.md`'s retired-claim shape: the judgement that depended on the claim survives it.

**Seams PM checked and ruled OUT of scope, with the reason, so nobody re-finds them and re-opens this:**
- `.claude/skills/implement/SKILL.md` has **FIVE** hits saying "ratchet" at ~511, ~519, ~529, **~532**, ~537, and **none of them means this one** — all five refer to the *reconcile-scoreboard* ratchet, a different instrument retired 2026-07-26. Leave them alone. This is the single likeliest false positive in the sweep. **The count was stated as four here until 2026-08-02 and that was my own undercount, caught by the spec audit** — my originating grep did return `:532` and I dropped it in transcription. It matters because this list exists to make the search checkable: E-280-08 AC-6 requires a regenerated search, an implementer gets five against a stated floor of four, and by this repo's own rule an unexpected count is a cross-check trigger — so the unaccounted hit would consume exactly the round this note exists to prevent. **`~532` is also the most misreadable of the five**, because its wording is *"That is a standing invariant in its own right, **not a ratchet**"* — a sentence about `self_games == 0`, referring to the reconcile-scoreboard instrument.
- `.claude/rules/workflow-discipline.md` and `.claude/agents/product-manager.md` state the assessment's **trigger count** but say nothing about the ratchet, so they are out of scope *for the gate sweep specifically*. ⚠️ **They are NOT out of scope for the epic** — both carry the count restatement TN-16 retires, and both have a definite edit owned by another story (E-280-07 AC-8 and E-280-04 AC-14 respectively). They are two of the **four** count sites; the others are `implement/SKILL.md` Step 3a (E-280-02 AC-14) and `context-layer-assessment.md`'s own list (this story). Do not read this bullet as "no edit owed."
- Agent-memory files (`claude-architect/epic-codifications.md`, `code-reviewer/`, `product-manager/operator-followups.md` and `MEMORY.md`) carry ratchet references. **Not story work.** Each is reconciled at closure by its owning agent under the deletion-side eviction discipline; a hit in an agent's dir is flagged, never edited, by anyone else. Recorded as a closure obligation, below.

⚰ **RETIRED 2026-08-02, superseded by TN-16 — do not implement:** ~~Scope constraint PM set: the assessment keeps EIGHT numbered triggers, because three files restate the count and a renumber would spray a cosmetic cascade across a file-disjoint decomposition.~~ **The design owner produced a better answer to the same problem: stop stating the count.** My constraint froze a number to avoid a cascade; de-restatement removes the cascade's cause. See TN-16.

**Closure obligations arising from this note — four memory-reconciliation items, kept together so none is rediscovered late.** (1) PM retires the ratchet items in its own memory: `MEMORY.md`'s Open Operator Obligations ratchet block, and `operator-followups.md` §3, whose "do not act on this" framing is now doubly stale. (2) CA reconciles `epic-codifications.md`'s ratchet entries at its closure codification. (3) The stale references in `code-reviewer/`'s memory are **flagged to code-reviewer, not edited**. (4) **TWO software-engineer files, and the idea filed for this obligation must name both.** `dispatch-git-gotchas.md` rests on the retired "unstaged = current story" invariant end to end — five occurrences, confirmed, and not incidental: the premise, the procedure built on it, **and the file's own frontmatter `description`, which is the text surfaced during recall**, so the stale claim reaches a reader who never opens the body. **And `software-engineer/MEMORY.md`'s index row for it, which independently restates the invariant** rather than merely pointing at it. Naming only the topic file would have SE reconcile it and leave the index asserting the retired claim — the exact failure `context-layer-assessment.md`'s own eviction rule warns about (*"the `MEMORY.md` index AND its topic files, not just the index"*), here in mirror image to E-259's recorded instance, which ran index-first. Same treatment for both: flagged, never edited by another agent. **For (3) and (4) both, the owning agent is NOT on this dispatch team**, so PM files each as an idea with a resolution trigger; a flag that lives only in a completion message is a handoff to nobody.

### TN-16: Design-round incorporation (claude-architect, 2026-08-02) — what was adopted, what was declined
The design owner delivered four deltas against the operator rulings. Recorded here with dispositions, because two of PM's own calls were superseded and one of CA's had a gap.

**Adopted in full.**
1. **"One verdict" means one verdict per FROZEN STATE.** The operator's ruling admits two readings — a failed story escalates immediately with no second look, or no verdict is re-issued against an already-verdicted artifact. **The second is adopted** (E-280-02 AC-5a). A remediation produces a new tree SHA, so reviewing it is the first verdict on a different artifact. This satisfies TN-3 exactly — it bounds re-issuance without touching what one verdict must establish — and yields an observable violation: two verdicts citing the same tree SHA. **It is also what makes the freeze load-bearing rather than decorative.** The alternative is recorded rather than discarded: if the operator meant the harsher reading, only the circuit-breaker text changes and the freeze mechanism is unaffected either way. PM adopted this under decide-and-disclose rather than escalating — it interprets a ruling in the design owner's own domain, overrides no standing rule, and is reversible in one AC.
2. **⭐ Trigger 8 must be RE-PRICED, not merely un-gated** (E-280-08 AC-12). Trigger 8's condition (b) prices promotions against the ratchet baseline, so retiring trigger 7 un-prices trigger 8 *as a side effect nobody named*. The file states its own consequence: an ungated "promote NOW" *"is what turned trigger 8 into a growth pump."* **PM's first version of that AC was too weak** — it removed (b), preserved (a), and left nothing in (b)'s place, which is precisely the hole. Promotions are now priced at the periodic refinement pass.
3. **Stop stating the trigger count** (TN-15's constraint retired; E-280-08 AC-11 verifies globally). The count appears in four places, so retiring or renumbering any trigger makes four sites wrong at once. Removing the restatement removes a permanent class of future defect — the epic's own design principle applied to its own instrument — and is cheaper than keeping four copies synchronized. **File-ownership routing solves the cascade problem that motivated PM's constraint:** `implement/SKILL.md` → E-280-02 AC-14, `product-manager.md` → E-280-04 AC-14, `workflow-discipline.md` → E-280-07 AC-8, `context-layer-assessment.md` → E-280-08. No new file enters the epic, so this is a within-scope refinement rather than a scope change needing operator sign-off.
4. **One periodic pass subsumes three cadences** (E-280-08 AC-9a), and the byte-measurement command replaces the retired ratchet as the distillation's instrument (E-280-01 AC-5a).
5. **The precise re-verdict seams** now sit in E-280-02's Technical Approach, cited by phrase. They are the real TRIM 1 skill-side target.

**Design owner's own retractions, recorded so they are not re-inherited.** CA withdrew its proposal to re-address the AC×surface enumeration obligation to code-reviewer alone — it was reasoned from the superseded collapse design, and under "pm owns acceptance" that obligation stays addressed to PM. CA also withdrew its counted-surface split recommendation as moot.

**Declined, with reasons.**
- **PM keeps E-280-02's AC-6** (the enumeration obligation survives with a named owner), against CA's view that the concern "evaporates by construction" under one-of-each. The AC costs nothing, has a reachable RED, and *evaporates by construction* is exactly the reasoning that leaves an obligation unprotected when a large block around it is deleted. The obligation sits near text this epic is rewriting.
- **PM declines the 6→5 story merge** (CA's S-03 folding the rule-side story into the ratchet story). Two reasons. **Sizing:** the merged story would span four files and six distinct concerns, past the one-session bar. **Dependency:** E-280-08 has no dependency at all while E-280-07 must follow E-280-02, so merging them couples independent work behind 02 for no benefit. CA's substantive content is adopted; only the packaging is not.
- ⚠️ **A missing member in CA's decomposition, flagged rather than silently repaired: its five stories drop `.claude/rules/worktree-isolation.md` entirely.** No story in that table owns it. That file carries the "unstaged = current story" invariant the freeze retires and the `git checkout --` prohibition whose *reasoning* must be updated rather than deleted (E-280-07 AC-5 and AC-6). Dropping it would strand both. This is the enumeration-omission class this repo's record says is caught by reading but never by the author — E-280-07 retains the file.

### TN-17: Design-owner review round (2026-08-02) — and a stale-copy correction that changes one disposition
The design owner reviewed the frozen set and returned three charges plus three findings. **Charge 1 (the OQ-5 inversion) CONFIRMED with no refutation.** The rest sorted as follows, and the sort matters because **three items quote text that had already been replaced** — the review was run against the epic as of the first incorporation pass, before the design owner's own deltas were folded in. Per `.claude/rules/dispatch-pattern.md`, the durable artifact wins over a relay, so those three are adjudicated against the current file rather than the reviewed one.

**ADOPTED — Finding A, material, and it refutes the design owner's own tier table.** Verified first-hand on disk before acting, since a reviewer working from a stale copy is not a source:
- **No top-level `templates/` exists.** The Jinja templates are the 17 files under `src/api/templates/`, already tier A via `src/`. The only tracked `templates/` is `.project/templates/` — epic and idea markdown — so a literal match would have pulled **prose into the full rubric**, inverting the "because Jinja renders" rationale attached to it. Entry removed.
- **`.githooks/` was omitted though it holds `pre-commit`, the executing PII gate that E-279-04 modified** — while `.claude/hooks/` sat in tier A *"because those execute."* Added.
- **Nineteen tracked top-level paths matched no tier**, so AC-8 would have gone RED on day one. Fixed by a **default rule (unmatched ⇒ tier A)**, which makes AC-8 satisfiable by construction and fails safe. AC-8 now goes RED on the *absence of a default*, so it cannot be satisfied by an enumeration that rots.
- Knock-on recorded in AC-12 rather than left to surprise the implementer: E-279's story 04 becomes tier A, so the classification will likely land **below** the relayed three-of-five. That is the divergence AC-12 exists to surface, not a fault in the table.

**ADOPTED — Charge 2's substance, on a premise that no longer holds.** The charge endorsed a PM scope constraint that had already been retired, and quoted the superseded AC-11. **But the coherence defect inside it is real and independent of all that, and nothing in the current AC set covered it:** a trigger 7 reduced to "run the script and record the reading" dangles off Assessment Procedure step 3 (*"If any trigger is 'yes': spawn `claude-architect`"*) — the one trigger in eight whose verdict routes nowhere. Now **E-280-08 AC-10a**: trigger 7 keeps a yes/no whose "yes" routes growth to the periodic pass. This is a better structure than either a gate or a bare diagnostic, because the measurement gains a destination.

**ALREADY SATISFIED — Charge 3.** The charge asks that the promotion pipeline name where size pressure is reviewed. The current AC-12 already requires exactly that (*"a live pricing mechanism replaces (b), namely that promotions are reviewed at the periodic refinement pass of AC-8"*, with a RED on *"(b) removed with nothing in its place"*). The charge was written against the earlier, weaker AC-12. No change.

**DECLINED — Finding B, and this one needs stating plainly because it reverses the design owner's own prior recommendation.** Finding B records the four-way trigger-count restatement as surviving this epic and recommends capturing it as an idea. **It does not survive: the de-restatement is in scope and landed** — Success Criterion 6, with edits routed to E-280-02 AC-14, E-280-04 AC-14, E-280-07 AC-8, and verified globally by E-280-08 AC-11. Finding B's stated reason is that the fix *"touches three files owned by three different stories, which is exactly the cascade PM declined"*, and Charge 2 puts it as *"the two extra files stay out."* **There are no extra files.** All four sites live in files the epic already owns, which is precisely the routing the design owner itself proposed one message earlier (*"assign the `:573` edit to S-02… Same pattern for `product-manager.md:160`: it is an agents file, so S-04 owns that edit"*). Three-different-owners is the file-disjoint design working, not a cascade.
- **Why PM is not conceding here, given that the design owner argued both sides.** `.claude/rules/dispatch-pattern.md` warns that when each party argues the other's case the record gets set by whoever spoke last, which is uncorrelated with truth, and that yielding because yielding looks rigorous is its own failure. The account that explains all the evidence: the earlier position was right on the merits (it removes a permanent defect class at the cost of four in-scope edits), the later one rests on a factual error about file count, and that error is the same one the earlier message had already corrected. **The de-restatement stands.**

**Finding C — conclusion holds, premise updated.** No file collision, correct. But `workflow-discipline.md` is no longer *conditionally* listed in E-280-07: it carries a definite edit (AC-8). Disjointness is unaffected — E-280-08 touches only `context-layer-assessment.md` and `context-layer-guard.md`.

### TN-18: Spec audit disposition (code-reviewer, 2026-08-02) — 2 MUST FIX + 9 SHOULD FIX, all ACCEPTED
**Verdict NOT APPROVED. Every one of the eleven findings was accepted; none dismissed.** Two are worth reading in full because each falsified a claim the epic made about itself.

**MF-1 — E-280-07's AC-1 was unsatisfiable by the decomposition, and it falsified TN-13's safety argument.** AC-1 demanded zero instances of the retired "unstaged = current story" invariant across three trees, but **seven instances live in files E-280-07 does not own** — `code-reviewer.md` (three), `plan/SKILL.md` (one, a verbatim instance of the exact retired phrase), `codex-review/SKILL.md` (three) — owned by E-280-04 and E-280-06, **both of which ran after 07**, and neither carrying an AC to remove them. So no AC anywhere in the epic produced the state 07's AC-1 required. Fixed three ways: **04 gains AC-15**, **06 gains AC-9**, **07's AC-1 narrows to its own files**, and **07's new AC-1c is the layer-wide verification, reachable because 07 moved to LAST.** TN-13's sentence *"which AC-1 of 07 catches by absence"* is corrected in place rather than deleted — a claim that a split is safe *because* an AC catches the residue is worth exactly the reachability of that AC, and this one had none.
- **My own enumeration found more than the audit's seven**, recorded so the worklists are right: `codex-review/SKILL.md` has a **fourth** site (the *"standalone (non-WORKDIR) staged/unstaged path"* reference in the large-refactor guidance), and several `.claude/agent-memory/` files rest on the invariant — software-engineer's `dispatch-git-gotchas.md` is built on it end to end. **Agent-memory is deliberately outside every story's scope**: each agent reconciles its own directory at closure under deletion-side eviction. Closure obligation, added to the list in TN-15: flag `dispatch-git-gotchas.md` to software-engineer, which is not on this dispatch team, so it needs an idea with a resolution trigger rather than a line in a completion message.
- **Two sites confirmed `no change needed` and recorded so nobody strikes them:** `implement/SKILL.md`'s Step 8 clean-tree preflight (about the **main checkout at closure** — a correct, unrelated use of "unstaged") and `worktree-isolation.md`'s `git checkout --` prohibition, which 07 AC-6 already preserves.

**MF-2 — TN-15 said four `ratchet` hits in `implement/SKILL.md`; there are five.** The missing one is `~532`, and **this was my own transcription error, not a search failure**: my originating grep returned `:532` and I dropped it when writing the note. The audit's substance ruling matches mine for all five — every one refers to the reconcile-scoreboard instrument — so no history was at risk. **The count was the defect, and it mattered:** E-280-08 AC-6 requires a regenerated search, an implementer would get five against a stated floor of four, and this repo treats an unexpected count as a cross-check trigger, so the unaccounted hit would have consumed exactly the round TN-15 exists to prevent. `~532`'s wording — *"That is a standing invariant in its own right, **not a ratchet**"* — is also the most misreadable of the five.

**The nine SHOULD FIX, all accepted, grouped by what they were.** *Mechanically wrong:* the tombstones cannot be removed with `git rm` because this epic directory is untracked, and the failure is silent — the erroring command gets skipped and `git add -A` then **adds** them (SF-1); E-280-05's successor pointer was stale on **both** axes, story and AC numbers (SF-2). *Internal contradictions:* story 06 said "seven bullets covering the seven user-triggered skills" in Context while its own Technical Approach said six — **I fixed one half in an earlier pass and left the other**, an instruction pair in tension inside the epic that exists to remove instruction pairs in tension (SF-3). *Citations:* two line anchors survived in an epic that commits to phrase citation, both into files it edits (SF-4); AC-11 described its target as a "one-line instruction" when the site defends **two paragraphs**, so an implementer would hunt for something that does not exist (SF-5). *Missing bars:* the batched-audit ACs lacked the checkable-interval bar their sibling AC-8 has, on the epic's only MEDIUM-risk trim (SF-6); two ACs scoped three trees where the Success Criterion they discharge names four including `CLAUDE.md` (SF-7); 02's AC-3 read layer-wide from a story owning one file — the same shape as MF-1 (SF-8). *Unreachable RED:* 06's AC-7 turned on whether "the harness consults" a file for a phrase, and **harness skill-discovery behavior is documented nowhere in this repo**, so no reviewer could evaluate it; now grounded in two locations a reader can check (SF-9).

**The E-271 demotion was verified clean on every substantive axis** — status `DRAFT`, one dated History entry, all three story files untouched, no E-280 story reaching that directory — **with one prose correction I owed and have made.** My entry asserted in bold *"Nothing else in this file was edited"* while three Status-block comments had in fact been amended, one materially (dissolving the ratchet half of a closure obligation). The following sentence was true; the bolded absolute was not. Corrected, with the closure-obligation amendment now named explicitly, because dissolving a signed-exception obligation is exactly what such an absolute hides.

⚠️ **Coverage bound on the audit's FIRST report, and it is not a criticism of the auditor.** Internal evidence shows that report was run against the epic as of the **first** incorporation pass: it describes E-280-02 as having 13 ACs (it has 17), `workflow-discipline.md` as *conditionally* listed in 07 (it is unconditional), and E-280-08's AC-11 as *"pins the count at eight and keeps trigger 7's written verdict"* (that text was replaced; AC-11 is now the zero-numeric-count property). All three observations are **correct about that report**. **The team lead disclosed the cause — the review was launched while incorporation was still landing.**

**Coverage as it actually stands, corrected 2026-08-02 (SF-vi).** The audit's second and third passes ran against the then-current tree and covered the pass-2 and pass-3 additions — TN-16 and TN-17 in full, AC-5a/5b/5c, the corrected tier table (with `templates/` and `.githooks/` independently verified on disk), all four count-de-restatement ACs, SC-6 against the auditor's own four-site grep, 08's AC-1a/AC-9a/AC-10a/AC-12/AC-13, 04's AC-15, 06's AC-7 and AC-9, 07's AC-1/AC-1c, the resequencing, both tombstone headers and the OQ-1 fix. A targeted fourth pass covered the final `E-280-04`, this note in full, and the fourth-pass History entry. **Open at last check: story 07's AC label ordering, since fixed.**

⚠️ **This paragraph previously concluded "the pass-2, pass-3 and pass-4 additions are UNAUDITED."** That was true when written — of the first report — and was falsified by the second and third passes without being updated. **Same propagation class as MF-A, running in the opposite direction: a claim accurate of one artifact generation, left standing after later generations overtook it.** It errs safe, and the cost is still real: a reader seeing "unaudited" in the durable record either re-runs an audit already done or discounts verification that happened, **which is exactly the redundant-audit cost this epic exists to remove.**

**Verified-clean findings worth keeping** (recorded so no later pass re-derives them): TN-9's eight byte figures and the 92,419 total are exact, as are the 98-line count and the seven-file `paths: "**"` set; TN-4's baseline table matches the committed JSON exactly; TN-15's gate-seam inventory is **complete** at six sites, all inside E-280-08's two files, so AC-5 is satisfiable; TN-11's five cited sites all resolve and AC-6's quoted phrase is verbatim; story 01's nine-epic narrative census and its 98-line/29,251-byte figures are exact; story 06's arithmetic (10 skills, zero with frontmatter, all 10 carrying Activation Triggers, exactly 133 body lines) is exact; story 04's 9-agent split into 6 adapter / 3 Sonnet-pinned is exact; dependency reciprocity is complete in all three places with no further phantom edges; and no story edits any agent's memory.

### TN-19: Report ceilings — there is no measured report-length inflation, and the cap is a tail guard
Codex P1-2 was right that `E-280-04` required ceilings and stated no value, so **any numeric cap satisfied its ACs**. Resolving it turned up something larger, through a three-relay chain in which **two relays decayed and a measurement settled it.**

**Relay 1 (wrong).** PM was told the values were derivable from *"per-role report-length distributions… quoted inline in TN-1/TN-2."* PM checked: they are not there. TN-1 quotes per-**turn** output role-matched (PM 157 → 305, SE 149 → 309, CR 73 → 189 tok/turn), TN-2 per-**call** means across all traffic (935 → 1,041), TN-3 one aggregate. **Tokens-per-turn is not tokens-per-report**, and TN-2's own scoping note draws exactly that line.

**Relay 2 (also wrong, and it was PM's).** ⚰ **RETIRED: ~~"there is no fallback: both source analyses are ephemeral and slated for deletion… unrecoverable from this repo."~~** **Both files are on disk right now** — `.project/research/2026-07-29-wallclock-model-analysis.md` and `-replication.md`, verified. *Slated for deletion* had decayed into *deleted* in a single hop, in PM's own writing. The design owner read both and found they contain **no per-report data either** — everything is tok/turn or tok/call, and the primary analyst proposes the TRIM 4 lever while never proposing a value. **So the conclusion survived and its reason did not**, which is this repo's named defect: a correct verdict shielding a false premise. *"The files are here and do not contain it"* is a far stronger position than *"the files are gone"*, and it is the one that survives someone running `ls`.

**The measurement, and it changes what TRIM 4 is for.** The design owner extracted every `SendMessage` payload — **the reports themselves** — from E-279 and from three peak-4.8-week sessions. Quoted inline here because the source analyses may yet be deleted and **these tables are the first direct measurement of the quantity TRIM 4 concerns**:

| E-279 (Opus 5), payload chars | n | p50 | p90 | max |
|---|---|---|---|---|
| main | 267 | 2,990 | 3,823 | 5,114 |
| product-manager | 104 | 3,063 | 4,075 | 6,676 |
| claude-architect | 100 | 3,291 | 5,021 | 11,911 |
| code-reviewer | 52 | 4,013 | 7,214 | 13,882 |
| **all** | **532** | **3,080** | **4,388** | **13,882** |

| Peak 4.8 week, all roles | n | p50 | p90 | max | mean |
|---|---|---|---|---|---|
| `3e4dc7ab` | 101 | 1,620 | 5,599 | 11,492 | 2,309 |
| `e109d59d` | 280 | **4,941** | **10,333** | **32,363** | **5,439** |
| `9e899c68` | 270 | 1,917 | 4,308 | 11,295 | 2,301 |

**E-279 sits inside the 4.8-era range and below that era's heaviest session on every statistic**; within-era spread (p50 1,620–4,941) dwarfs any era gap, and 4.8-era SE and CR ran p50 7,760 and 8,191, roughly double E-279's. **So the 2x figure does not transfer to reports. There is no report-length regression to repair.** Bounds, stated not buried: three sessions per era, high variance, and payload characters are a different population from tok/turn — which is the whole trap. This does not refute TN-1's per-turn finding; it says that finding **cannot be spent on reports.**

**What E-280-04 therefore lands (AC-16, AC-17, AC-18):** a **6,000-character (~1,500-token) ceiling for long-lived agents, labelled an ESTIMATE**, sitting above every measured role p50 in both eras and biting roughly the top decile — it would have caught claude-architect's 11,911 and code-reviewer's 13,882 outliers without touching normal traffic. **The story must say plainly that this is a guardrail against future drift, not a repair of observed inflation.** Coherent with the primary analyst rating TRIM 4's expected saving **"small"** — alone among the four trims — which fits a generous tail-biting cap and contradicts an aggressive one.

**Code-reviewer is EXCLUDED, and the reason is a documented model failure mode rather than politeness.** CR pins `claude-opus-5`, and the vendor documents that this model may follow a conservatism instruction literally and report less — *"ask it to report everything and filter in a separate pass instead."* **A length ceiling is a conservatism instruction wearing different clothes.** CR is also the role whose length is structurally driven by finding count, so it is simultaneously the most tempting target and the one where truncation costs most.

**Ruled by the team lead under decide-and-disclose**, no operator round: TRIM 4 already said cap reports, and a measured, labelled, excluded-where-hazardous cap is that.

### TN-12: Consultation completeness (per-domain verdict)
- **claude-architect — CONSULTED.** Design owner for the entire epic; its input is recorded in the Stories decomposition and in the per-story Technical Approach sections.
- **baseball-coach — WAIVED.** No coaching data, statistic, report surface, or domain judgment is touched.
- **api-scout — WAIVED.** No GameChanger payload, endpoint, data-availability, or credential surface is touched; no AC asserts anything about API behavior.
- **data-engineer — WAIVED.** No schema, migration, SQL, or ETL path is touched.

## Open Questions
**All five operator gates were ruled on 2026-08-02 and are CLOSED. None blocks READY.** They are kept here in resolved form because each ruling contains a *reason* a later reader will otherwise re-derive. Do not re-open one without a fresh operator instruction.

- **OQ-1 — CLOSED. The context-layer size GATE is RETIRED.** The question asked whether the per-epic charging model still fits; the operator answered a level up: *"I don't think gating and ratchets are working. I think we just need periodic refinement."* **No subtree gating, no baselines, no exceptions, no re-snapshot.** `context-ratchet.sh` survives as an **on-demand diagnostic** — the measurement is kept and only the one-way gate goes, exactly mirroring the reconcile-scoreboard precedent of 2026-07-26. **Periodic refinement replaces continuous gating:** a scheduled context-layer refinement pass, stated alongside the batched audit cadence in E-280-08. Implementation seams are in **TN-15**; the trigger-count decision that supersedes PM's original scope constraint is in **TN-16**.
  - **CA's split-the-counted-surface recommendation (gate `rules + agents + skills` at +44, report agent-memory) is SUPERSEDED, not adopted and not rejected on its merits.** It was a better gate; the operator declined to keep a gate. Recorded so nobody revives it as "the recommendation that was never answered."
  - **What made the question decidable was ownership-independent and survives the ruling as the reason:** an unregistered diagnostic that accrues continuously and is charged to whichever epic closes next means **an epic closing more than about a day after a snapshot fails by construction, whatever its own discipline** (E-279 accrued +286 in 22h33m of planning and contributed +64 at dispatch, failing at +350). That is a property of the charging model, not of any epic's conduct.
  - **The E-279 figure provenance is now moot for E-280 and is preserved in TN-9 as evidence.** For the record: the archive records Reading 1 = +350 with Reading 2 owed; a relay supplied +418 with a signed exception "recorded in three places"; PM found no `418` anywhere in the repo; CA then measured the live tree and reproduced +418 exactly. So **the figure is producible and the signature is still not in the repo** — an E-279 follow-up, not E-280 work, and with the gate retired there is nothing left for the signature to authorize.
- **OQ-2 — CLOSED, dissolved by OQ-1.** It asked whether to offset within subtrees, take a signed exception, or defer to OQ-1. With no gate there is nothing to offset and nothing to except. The stories record their deltas; nobody judges them.
- **OQ-3 (design owner) — CLOSED as to shape and as to outcome.** The telemetry was run AND adjudicated; findings are in TN-10 and TN-14.
- **OQ-4 — CLOSED** 2026-08-01; the decomposition is in the Stories table.
- **OQ-5 — CLOSED. PM RETAINS AC AUTHORITY.** Operator verbatim: **"pm owns acceptance"**. The ruling adopts the literal reading of the original words that PM flagged and the design owner had read the other way: *"one completion report / one review verdict / one AC verdict per story"* is **three artifacts, one OF EACH** — a bound on how many times each verdict is issued, not a collapse to a single issuer.
  - **The settled shape: PM issues the single AC verdict, code-reviewer the single review verdict, both against the frozen story state, neither re-askable.** This removes the measured overhead (E-279's 8 CR verdicts and 7+ re-verifications of one AC) without striking any standing rule.
  - **"PM is authoritative on ACs" STAYS in `dispatch-pattern.md`.** No sentence assigning PM its dispatch-time AC role is removed by any story. E-280-07's AC-1 was written to verify the *absence* of that sentence and is inverted accordingly — see that story.
  - **A consequence worth stating because it inverts an AC:** "Both PM and code-reviewer must approve before the staging boundary advances" remains **TRUE** under one-of-each. Both still approve; each approves once. TRIM 1 does not retire dual approval — it retires **re-issuance**.
- **OQ-A — CLOSED. E-280 dispatches FIRST; E-271 is demoted to DRAFT.** PM executed the demotion 2026-08-02. E-280-02 and E-280-04 are written to **fixed text against the current layer**. Full disposition of all four collisions is in **TN-6**; the ruling takes a fourth option none of the three offered, and the reason it is better than all three is recorded there.

## History

### CLOSURE RECORD (PM, 2026-08-02)

**TN-4(a) — the per-subtree diagnostic. REPORTED, NOT GATED.**

⚠️ **MANDATORY CAVEAT, per TN-4(c), stated IN the reading and not beneath it: `.claude/hooks/context-ratchet.sh` prints a banner reading `FAIL -- the context layer grew past baseline`. That word is a fact about a STALE COMMITTED BASELINE (snapshotted 2026-07-28), NOT a verdict on this epic or any story in it.** The gate was retired by operator ruling (OQ-1/OQ-B). **Nothing is offset, no exception is owed, and no per-subtree number can fail this epic.** A later reader finding `FAIL` in this record must not reconstruct a gate that no longer exists.

**The BYTE reading — the always-on load, which is the instrument TN-4(b) says is the right one for prose.** Code-reviewer's measurement, re-running TN-9's own one-liner against a rebuilt `paths: "**"` set.

⚠️ **THIS TABLE IS `86,024 @ a5b65eb` AND IS NOT THE CLOSURE FIGURE. EVERY FIGURE BELOW IS LABELLED WITH THE TREE IT WAS TAKEN ON, DELIBERATELY.** The closure codification is still landing in these files — `tool-output-integrity.md` has already moved 23,172 → 24,106 → 24,900 as CA's edits went in, and the eight-file total was last reported at **87,752**. **Code-reviewer re-takes this immediately before the closure commit and the FINAL figure is that one, not this one.** Labelling by tree is the direct lesson of `84,388`: that figure was a true reading of `fb15272` and became false only when quoted without its tree. **A figure with its tree attached cannot go stale — it can only become historical.**

| File | TN-9 base | At closure | Delta | Cause |
|---|---:|---:|---:|---|
| `tool-output-integrity.md` | 29,251 | 23,172 | **−6,079** | E-280-01 distillation; narratives → pointer/evidence form |
| `CLAUDE.md` | 22,157 | 20,205 | **−1,952** | E-280-06 Workflows compression; triggers single-sourced to skill frontmatter |
| `dispatch-pattern.md` | 11,433 | 12,480 | +1,047 | E-280-07 rule-side TRIM 1 — the freeze mechanism stated where its consumer reads it |
| `worktree-isolation.md` | 6,727 | 7,292 | +565 | E-280-07 — retired-invariant removal plus the `git checkout --` reasoning rebuilt rather than deleted |
| `workflow-discipline.md` | 12,035 | 12,059 | +24 | E-280-07 AC-8, trigger-count de-restatement |
| `agent-routing.md` | 6,095 | 6,095 | 0 | untouched |
| `agent-team-compliance.md` | 3,586 | 3,586 | 0 | untouched |
| `vision-signals.md` | 1,135 | 1,135 | 0 | untouched |
| **TOTAL** | **92,419** | **86,024 @ `a5b65eb`** | **−6,395 (−6.9%)** | ≈1,600 tokens off every agent's every session |

Per-file deltas sum to −6,395 independently, so it reconciles from both directions. **Two bounds from code-reviewer, both material:** it is a measurement of the **index tree** (CR checked each of the eight files individually against disk — none carries unstaged movement, so it is true of disk too), and **it moves if anything further lands in those eight files**, which the closure codification may do. **CR's instruction is to RE-TAKE this at the closure commit rather than quoting this table if `.claude/rules/` or `CLAUDE.md` change again.**

⚰ **A superseded figure, corrected rather than quietly dropped: `84,388` / `−8,031` must NOT be quoted as this epic's result. **`87,752 @ 1c425fa` is the closure figure** — not `84,388`, and not `86,024`, which was a TRUE reading of `a5b65eb` SUPERSEDED by the settled-tree re-take when the closure-phase edits landed, not an error corrected.** ⚠️ **But `84,388` is NOT a wrong number and must not be struck where it records an observation.** It was code-reviewer's genuine measurement against tree `fb15272`, where `dispatch-pattern.md` and `worktree-isolation.md` really were still at their TN-9 values because stories 07 and 08 had not yet run. **It did not fail producibility; it went STALE**, and it travelled because it sat under TN-20 item 46's *"closes end-to-end / headline claim"* banner. **Disposition splits by ROLE: as a headline or result it is a CRITERION and is superseded; inside CR's story-06 verdict or anywhere recording what was seen at `fb15272` it is EVIDENCE and is preserved.** See **item 46a**, which carries the full correction, including the two earlier and wrong accounts of the figure's own provenance.

**⚠️ THE PER-SUBTREE LINE READING IS NOT DELIVERED, AND THE REASON IS A DEFECT IN TN-4(a)'s OWN DESIGN — stated rather than papered over.** TN-4(a) says *"Stories 01, 04, 06 and 08 each record their own subtree delta"* and asks PM to aggregate them at closure. **Two things are false about that.** (a) **Only story 01 actually recorded one** (`.claude/rules` 2,649 → 2,642, −7 lines); 04, 06, 07 and 08 recorded none in their status blocks. (b) **More importantly, the fragments are NOT COMPOSABLE even where they exist.** The stories are serial and several touch the same subtree — 02, 06 and 09 all edit `implement/SKILL.md` (in `.claude/skills`), and 01, 07 and 08 all edit `.claude/rules`. **A per-story figure is a SNAPSHOT at one point in the sequence, not an addend**, so chaining or summing them would produce a confident number whose derivation assumes facts nobody checked — **the identical error that produced `84,388`, one level up, in the closure obligation designed to prevent it.** **The only correct instrument is ONE run at the settled closure tree**, which PM cannot execute (no Bash, by design). **It was run, and the readings are below — so the obligation is DISCHARGED, not owed.** The non-composability reasoning above stands unchanged: it is the reason a single settled-tree run was the right instrument rather than a chained estimate.

**PROVENANCE, stated rather than softened: both readings were taken by the MAIN SESSION, not by code-reviewer, which terminated before it could re-take them.** They are deterministic mechanical measurements — `os.path.getsize` for the byte reading, the committed `.claude/hooks/context-ratchet.sh` for the line reading — repository state rather than content judgement, and re-runnable by anyone. Arithmetic was done in `python3`, deliberately not `bc`: code-reviewer established this container has no `bc`, and `$((new-old))` silently treats the empty operand as `0`, printing a clean `+0.0%` with nothing in the output looking wrong.

**READING 1 — the always-on byte load. `87,752 @ 1c425fa`. THIS IS THE CLOSURE FIGURE.** It reproduces code-reviewer's provisional figure exactly from an independent run.

| File | TN-9 base | `1c425fa` | Delta |
|---|---:|---:|---:|
| `CLAUDE.md` | 22,157 | 20,205 | **−1,952** |
| `tool-output-integrity.md` | 29,251 | 24,900 | **−4,351** |
| `dispatch-pattern.md` | 11,433 | 12,480 | +1,047 |
| `worktree-isolation.md` | 6,727 | 7,292 | +565 |
| `workflow-discipline.md` | 12,035 | 12,059 | +24 |
| `agent-routing.md` | 6,095 | 6,095 | 0 |
| `agent-team-compliance.md` | 3,586 | 3,586 | 0 |
| `vision-signals.md` | 1,135 | 1,135 | 0 |
| **TOTAL** | **92,419** | **87,752 @ `1c425fa`** | **−4,667 (−5.05%)** |

**BOTH anchored comparisons are recorded, and NEITHER MAY TRAVEL ALONE** — the epic's own convention is `<delta> @ <base> → <tree>`, and a bare comparison is a position rather than a reading:

- **−4,667 @ TN-9 base → `1c425fa` (−5.05%)** — the epic's result. **The headline moved −6.9% → −5.05%.**
- **+1,728 @ `a5b65eb` → `1c425fa`** — the closure-phase give-back, **all of it `tool-output-integrity.md`**, which is nonetheless still **−4,351 (−14.9%)** against where it started.

**READING 2 — the ratchet line reading. `+1,005 lines @ 1c425fa`.**

⚠️ **CAVEAT FIRST, per TN-4(c), because the banner is the thing a later reader will fixate on: the script printed its `FAIL` banner demanding an operator-signed exception and offsetting. THAT TEXT IS RETIRED. The script is byte-unchanged BY DESIGN — E-280-08's AC-10 required exactly that, since retiring a gate is not deleting an instrument.** Nothing is offset, no exception is owed, and no number below can fail this epic. `--update-baseline` was **NOT** run; re-baselining is the operator's alone.

```
subtree                 baseline   current     delta
.claude/rules               2640      2652       +12
.claude/agents              1869      2002      +133
.claude/skills              3269      3371      +102
.claude/agent-memory       10442     11200      +758
total                      18220     19225     +1005
```

**`.claude/agent-memory` is 758 of 1,005 — 75% of the growth.** That is precisely the subtree claude-architect ruled the refinement pass did **not** cover and which the next closure owes. **This reading was taken after that ruling and by a different party, and it corroborates it independently** — the pass's largest target is the part E-280 did not examine.

**THE TWO INSTRUMENTS DISAGREE IN SIGN AND BOTH ARE RIGHT — record the reconciliation, because a reader meeting either number alone draws the wrong conclusion.** Always-on bytes: **−4,667**. Lines across the four `.claude` subtrees: **+1,005**. The scopes differ: the ratchet counts `.claude/agent-memory`, which **nothing loads always-on**. **E-280 reduced what every session pays and grew what is loaded on demand.** Neither figure contradicts the other, and neither is the whole result.

**Documentation assessment — TWO TRIGGERS FIRE.** T1 new feature/endpoint: **no.** T2 architecture/deployment: **no.** T3 existing agent materially modified: **YES** — E-280-04 edited all nine `.claude/agents/*.md`. T4 schema: **no.** T5 epic changes how the system works: **YES** — the dispatch, review and closure workflow changed. Affected doc: **`docs/admin/agent-guide.md`**, measured stale on five counts (7 agents listed against 9 on disk, `ux-designer` and `code-reviewer` absent; api-scout, data-engineer and software-engineer listed Sonnet when all three are Opus; line 84 asserts *"The PM creates an Agent Team, spawns implementing agents"*, which `dispatch-pattern.md` contradicts outright; last updated 2026-03-04, so the 90-day staleness clause fires independently). **docs-writer dispatched.** *Verified clean and stated as a result: every `ratchet` reference in `docs/` — `operations.md` ×4, `production-deployment.md` ×2 — is the reconcile-scoreboard instrument retired 2026-07-26, read literally, not ruled from grep. No docs edit is owed on the gate retirement.*

**Context-layer assessment — explicit per-trigger verdicts.** **1** new convention/pattern/constraint: **YES** (verdict economy, freeze-by-tree-SHA, file-path tiers, the report ceiling, frontmatter as single source). **2** architectural decision: **YES** (size gate retired for periodic refinement; trigger 8 re-priced; measurement survives as diagnostic). **3** footgun/failure mode: **YES** (TN-20's findings). **4** agent behavior/routing/coordination: **YES**. **5** domain knowledge: **NO** — no baseball, GameChanger or schema surface touched, consistent with TN-12's three WAIVED verdicts. **6** new workflow/operational procedure: **YES** (both cadences; sub-clause checked — no skill added, renamed or retired, and the `/workflow-help` cheat sheet stays consistent under AC-3's declaration ruling). **7** net growth: **the reading above, which gates nothing**; carried into the periodic refinement pass. **8** reusable behavioral lesson: **YES**. → **claude-architect codifies before archival.** Candidates: TN-20 item **1** (standing, nominated independently by CA, CR and PM), plus **24, 36, 44, 45, 50, 52, 54, 57, 61**, and from after the predecessor PM drained: CR's seed-independence signature, the verb-versus-mechanism distinction, the TN-7 premise correction, **item 46a** (a heading is an index entry, not a claim) and **item 64**'s residual (the AC checkbox is a field nobody maintains).

**Cadence records — E-280 DISCHARGES NEITHER CADENCE. It closes owing BOTH, and there is NO EPOCH LINE.** Ruled by claude-architect, the design owner, under the self-starting clause it landed in `context-layer-assessment.md` (*"Where a cadence has no run on record, the next closure owes one"*). ⚠️ **This record initially read "refinement pass — RUN (bounded)" on a report that the question was settled; it was not, CA had not ruled, and the ruling went the other way. Corrected here rather than quietly rewritten.**

- **Periodic context-layer refinement pass — NOT RUN.** Three independent grounds. (1) **AC-9's artifact does not exist**: the pass is specified to produce *"a dated refinement record… naming what was retired, what was re-homed, what was kept and why, and the readings it acted on."* E-280's History has none, and **CA's instruction is that one MUST NOT be written retroactively.** (2) **Coverage** — the pass is defined over four subtrees and E-280 deliberately pruned three (`.claude/rules`, `.claude/skills`, `.claude/agents`, plus `CLAUDE.md`); `.claude/agent-memory/` received closure bookkeeping only, **five files touched and seven of nine agent directories never opened**, append-only where a refinement pass deletes. Per TN-9 that subtree is **89% of the measured growth**, so the pass's largest target is exactly the part not examined. (3) **The decisive one: E-280 FOUND refinement work there and DEFERRED it** — TN-15 recorded `software-engineer/dispatch-git-gotchas.md` resting on the retired invariant end to end, plus its index row and code-reviewer's stale references, and those became **[[IDEA-234]] and flags, not fixes.** In CA's words: *an epic that identifies stale content in the subtree the pass targets and routes it to an idea has not performed the pass over that subtree.* Two components never ran at all: the **trigger-8 promotion re-examination against condition (a)**, and **acting on accumulated trigger-7 readings** (E-280 retired the gate rather than acting on its readings). **CA's summary is the sentence for the record: *E-280 was a deep refinement of part of the layer. That is not the pass as specified, and saying so costs nothing.***
- **Batched adversarial audit — NOT RUN. The next closure OWES one.** No Fable-tier audit ran; every review was `cr-e280` (Opus, same tier) plus one Codex pass. **Codex is NOT a substitute** — the codex-review skill is a separate per-epic instrument, while the cadence names the **Fable** independent context-layer audit specifically. **Recorded as not-run deliberately: booking an audit that did not happen to tidy a counter would be the vacuous check this epic just removed, one layer up** — and that audit class is the one that found a CRITICAL two same-tier passes had missed, which is `agent-routing.md`'s stated basis for escalation. CA stated its own falsifiability: *if anyone can point to a Fable audit in E-280, this ruling flips.* Nobody has.

**⚠️ HOW THIS DIVERGENCE SHOULD BE READ, because "PM got it wrong" is the wrong account and the right one is more useful.** The Cadence text says *"re-read the four subtrees and deliberately prune"* and **never states what a PARTIAL pass counts as.** Two careful readings diverged because **the criterion had a gap** — the same class as the epoch gap, found the same way, on first application. The rule's owner ruled, and is closing the gap with a clause stating that coverage of all four subtrees is what makes it a run. **Underspecified criterion → divergence → owner rules → gap closed** is the accurate sequence.

**The routing, stated because it is what makes this settle cleanly rather than by seniority:** the cadence lives in `.claude/rules/`, and under `agent-routing.md`'s Decision Routing table **context-layer architecture is claude-architect's domain with PM advisory.** **PM owns the History record; CA owns the rule** — so what the cadence *means*, and whether an epic discharges it, is CA's call to make and PM's to record.

**CA led with the case AGAINST its own ruling, which is why it is worth trusting.** Its own assessment: **ground (1) is the WEAKEST** — this closure entry substantially *is* AC-9's artifact, dated and naming what was retired and re-homed. **What it lacks is *"the readings it acted on"*, and the ratchet reading is still open**, so that component cannot be complete regardless. **What actually decides it is coverage plus ASYMMETRY**, and the asymmetry is the transferable part:
- **Recording NOT RUN when it ran** costs one extra pass at the next closure — **with its worklist already assembled** in [[IDEA-234]] and code-reviewer's flags.
- **Recording RUN when it did not** leaves `.claude/agent-memory/` unexamined for roughly five closures — 89% of measured growth, containing memory that **actively asserts a retired invariant in its recall-surfaced frontmatter**.

**Those are not symmetric errors, and the cheap one is the one to make.** **PM's bound is not refuted by the ruling — it is the ruling's evidence.** In CA's words: *an honest statement of what was not done describes an incomplete pass.* The bound survives as **the next pass's worklist** rather than as a qualifier on a discharge.

**Why both are recorded as explicit negatives even though the rule makes silence equivalent.** Under the clause as written, a negative record and no record are the same thing — neither is *a run on record*, so the obligation persists either way. They are recorded anyway, per this epic's own item 61: **a negative stated as a result is distinguishable from a question never asked.** A future reader must be able to tell the cadences were evaluated and found undischarged rather than overlooked. **The next closure inherits a refinement pass over `.claude/agent-memory/` with its worklist already assembled** in [[IDEA-234]] and code-reviewer's flags. **This is the rule working correctly on first application, not an awkward outcome.**

**TN-7 on story 08 — the ledger in its checkable form, corrected.** *Gate-pointer restatement **4 → 0**, verified against the merge base; **two** sites swapped a false pointer for a true statement (`context-layer-assessment.md`'s `### Cadence and the ratchet` and trigger 8's condition (b)); **two** sites retained a discipline already present and elaborated it (`context-layer-guard.md` ×2, which already named the closure diff and the line-200 truncation respectively).* **The 4 → 0 count is what TN-7 tests and it holds independently of the premise.** The earlier characterization — *"each names its own live discipline"* — read as four additions where two are retentions, **and PM's own SC-4 report repeated the same overreach on the same two sites, calling them "rebuilt".** No verdict changes; story 08's APPROVED stands. **Symmetry note, which is what makes this a rule rather than a rationalization: a byte argument was refused in the epic's FAVOUR on story 02, and refused AGAINST it here. Bytes rising is no more a TN-7 failure than bytes falling was a TN-7 pass.**

⚠️ **OPEN AT CLOSURE — the AC-1c layer-wide SEMANTIC residual.** Layer-wide **semantic** coverage of the retired "unstaged = current story" invariant is **NOT established.** It rests on doc-sweep steps 1+2 over five literals, with step 3 covering three files. **Code-reviewer has committed in writing to a semantic pass at the closure integration review; if that pass does not run, this residual is OPEN and this record says so.** It must not be reported as coverage either way. The governing bound is TN-20 item 59: **a positive control proves the instrument EXECUTED; it does not prove the pattern is WIDE ENOUGH** — and PM found a **second** instance of that same weakness at closure on a different terminal verification (a phrase pattern that could not match `**eight**-trigger` across inline emphasis), which is why the residual is held open rather than narrowed. PM deliberately did **not** order a 199-file semantic read, priced against a residual four independent checks failed to surface any instance of; **that was a judgment, not an oversight.**

**Known coverage boundary, recorded rather than assumed:** CA's cadence-epoch clause landed **while Codex was already running**, so Codex reviewed the story-set tree and **the epoch clause is covered by the closure integration review only.**

- 2026-08-02: **Status → ACTIVE.** Dispatch opened in worktree `/tmp/.worktrees/baseball-crawl-E-280` (branch `epic/E-280`). All six stories `TODO`; serial order `01 → 02 → 04 → 06 → 07 → 08`. The two tombstones (`E-280-03`, `E-280-05`) are **already absent from the worktree** — the pre-dispatch `rm` prereq is discharged, verified by listing the epic directory, which holds exactly the six live story files plus `epic.md`.
- 2026-08-02: **Status → READY.** Final batch landed: the operator's cadence ruling (**batched Fable audit per THREE epic closures, periodic context-layer refinement per FIVE** — work-proportional and checkable from the archive, closing Codex P1-1) into E-280-08 AC-1a and AC-8; the code-reviewer's two scoped-check floor gaps (`code-reviewer.md`'s untracked-file warning added to E-280-04 AC-15 as the token-free retired-claim member, **cured** by the freeze rather than falsified by it; and `software-engineer/MEMORY.md` added alongside the topic file in TN-15 item 4, since naming only the topic file would leave the index asserting the retired claim); and **Codex P1-2 resolved by measurement rather than by estimate** — see below. Dispatch requires separate operator authorization.

**P1-2's resolution is the most substantive thing in this batch and it changed what TRIM 4 is for.** Three relays, two decays, one measurement. The routed premise (*"per-role report-length distributions quoted inline in TN-1/TN-2"*) was false — those are per-turn and per-call figures. **PM's own follow-on was also false**: I wrote that the source analyses were *"unrecoverable from this repo"* when both are on disk, verified — *slated for deletion* decayed into *deleted* in one hop, in my own writing, and the correct conclusion shielded the bad premise exactly as this repo's record predicts. The design owner then **read both files** (confirming they hold no per-report data either) and **measured the reports directly** from `SendMessage` payloads. Result: **E-279's report lengths sit inside the peak-4.8-era range and below that era's heaviest session on every statistic — there is no report-length regression to repair.** The 2x verbosity finding is per-turn and does not transfer. So the ceiling landed as a **6,000-character tail guard, labelled an ESTIMATE**, with `code-reviewer` **excluded** on a documented model failure mode (a length cap is a conservatism instruction, and that model may follow one literally and report less). Ruled by the team lead under decide-and-disclose. **PM's earlier AC-16 required the opposite of what the measurement supports and was rewritten, not supplemented**; AC-1 was also amended, since requiring a ceiling on *every* agent would have made AC-18's exclusion a RED — an instruction pair in tension inside the epic that treats them. TN-19 carries both measured tables inline, because the source analyses may still be deleted and these are the first direct measurement of the quantity TRIM 4 concerns.

### Review Scorecard
| Review Pass | Findings | Accepted | Dismissed |
|---|---|---|---|
| Design-owner deltas (claude-architect, round 1) | 5 | 5 | 0 |
| Design-owner review (claude-architect, round 2) | 6 | 4 | 2 |
| Design-owner residuals (claude-architect, round 3) | 2 | 2 | 0 |
| CR spec audit — pass 1 | 11 | 11 | 0 |
| CR spec audit — pass 2 (re-audit) | 3 | 1 | 2 |
| CR spec audit — pass 3 (final verdict) | 1 | 1 | 0 |
| CR scoped depth check — pass 4 | 2 | 2 | 0 |
| Codex spec review — iteration 1 | 4 | 4 | 0 |
| Design-owner P1-2 measurement (claude-architect, round 4) | 3 | 3 | 0 |
| **Total** | **37** | **33** | **4** |

**Composition note, per the E-279 precedent.** This scorecard is authored by the successor PM generation, which was present for every pass above. The predecessor generation's work — epic creation, the first decomposition, TN-1 through TN-14 — precedes this table and is recorded in the 2026-08-01 History entries; cite those rather than this scorecard for it.

**The four dismissals were all STALE-STATE, not disagreements on merit**, and the pattern is the epic's own thesis: reviewers repeatedly read a tree that had moved. CA round 2 contributed two (Charge 3 already satisfied by the then-current AC-12; Finding B reporting the count de-restatement as out of scope when it had landed as SC-6 — CA accepted the correction and diagnosed the mechanism as its own inherited-claim restatement). CR pass 2 contributed two (MF-B and MF-C, both closed by edits landing after its snapshot). **No finding was dismissed as wrong on substance.**

**Cross-review yield worth recording.** Three defects were structural and each was found by a different party: **MF-1** (E-280-07's AC-1 unsatisfiable — seven invariant sites in files owned by later stories) by CR; **the tier table's 19 unmatched paths and inverted `templates/` entry** by CA, refuting its own draft; **P1-3** (E-280-08's false independence claim) by Codex. All three are the same shape — *an AC whose reachability depends on something the decomposition did not encode* — and no single reviewer found more than one of them.

### Dispatch Scorecard (per story)

**Kept SEPARATE from the planning table above, deliberately, per the E-279/E-278 precedent: the two count against different artifacts** — the planning scorecard scores the SPEC, this one scores the IMPLEMENTATION. Merging them would produce a total that means nothing.

| Story | Frozen tree | code-reviewer verdict | PM AC verdict |
|---|---|---|---|
| 01 | — | APPROVED (round 2) | PASS — AC-1–9 incl. AC-5a |
| 02 | `347f9d9` | APPROVED `cd1c4cd` w/ **2 SHOULD FIX** → CONFIRMED CLOSED on `347f9d9` | PASS — all 14 |
| 04 | `a4aac1b` | APPROVED — adversarial re-verification of AC-8 limb 2 | PASS |
| 06 | `fb15272` | APPROVED — **no MUST FIX, no SHOULD FIX; the only findings-free review of the dispatch** | PASS — AC-1–9, issued first-hand at closure |
| 09 | `3d2274c` | APPROVED — no MUST FIX, no SHOULD FIX | PASS — AC-1 (as widened), AC-2, AC-3 |
| 07 | `eba9a2e` | APPROVED — no MUST FIX, no SHOULD FIX; labelling-only confirmed MECHANICALLY | PASS — all 10 |
| 08 | `7d0e357` | APPROVED | PASS — all criteria |

**Totals across the dispatch: 2 SHOULD FIX, 0 MUST FIX, 0 dismissals** — every finding reached FIXED within its story; **no finding was deferred at any point**, and one proposed deferral was declined by the orchestrator (TN-20 item 3, where taking it at closure would have shipped a defect).

⚠️ **Story 06's row was EMPTY IN THE ARTIFACT until closure, and the reason is the best finding of this closure — see TN-20 item 65.** Its story file read `TODO` through the whole dispatch and a PM handoff while the Stories table read `DONE`, and neither gate's verdict was recorded anywhere. **The successor PM declined to flip it or fill this row from an orchestrator's characterization** — TN-20 item 16-B applied by a later PM to the party that wrote item 16-B — and the row was filled only when `cr-e280` supplied its verdict directly, with the AC verdict issued first-hand rather than inherited. **`fb15272` appearing as E-280-09's review base establishes only that 06 was FROZEN, which the completion report triggers; a freeze is not a verdict**, and that gap is exactly what was not allowed to close itself.

**A finding that belongs with this table rather than only with its fix** (TN-20 item 58's shape, landing inside the sweep built to catch it): the stale-invariant hit found in `code-reviewer/MEMORY.md:71-74` — *"When using `git diff HEAD`, files listed in the review assignment but not appearing in the diff were not changed in this epic"* — **shares no token with `unstaged` or `ratchet`** and was found by reading, not by any sweep.

- 2026-08-02 (sixth pass): **Codex iteration 1 (4 findings) plus the two queued items incorporated.** **P1-3 is the significant one and it is MF-1's lesson at the opposite end of the epic: E-280-08 declared itself independent — `Blocked by: None`, "position is arbitrary… may run first without consequence" — and that was FALSE.** Its *edits* touch two files nobody else does, but its *verifications* are global: AC-11 checks a trigger count whose three other sites are removed by 02/04/07, and AC-13 checks byte-identity "across the whole epic diff", which does not exist until 02 and 06 have edited `implement/SKILL.md`. **A story's dependencies follow from what its ACs READ, not only from what its edits WRITE.** Encoded in structure — 08 blocked by 02/04/06/07, reciprocal `Blocks` in all four, Stories table and Sequencing note updated — rather than asserted in prose, since prose is what got it wrong. **P2 was the count-propagation defect living inside the story that fixes it**: 08's Technical Approach still said "four" `ratchet` hits after Context and AC-13 were corrected to five — **the third instance of that class this round.** **P1-1 (cadence intervals) is HELD pending the operator ruling** the lead is seeking; **P1-2 (report ceiling values) is routed to CA**, with a caveat on the derivation basis. Also landed: story 07's AC label ordering, and **SF-vi** — the "pass-2/3/4 unaudited" bound in TN-18 and the fourth-pass History was true of the audit's first report and falsified by its later passes, the same propagation class as MF-A running in reverse; the accurate stale-copy examples and the launch-timing disclosure are kept, only the overtaken conclusion replaced.
- 2026-08-02 (fifth pass): **Spec re-audit and design-owner residuals incorporated.** CR's re-audit against current state resolved 7 of its 9 first-pass findings and raised 3 MUST FIX; **two of the three were already closed by edits that landed after its snapshot** (the `Blocks: None` reciprocity in 04/06, and TN-18's existence), verified before triage rather than assumed. **MF-A was real and is the sharpest kind of miss: the five-hit correction landed in TN-15 and never propagated to AC-13, the only enforceable expression of it.** AC-13 quoted two sentences, said "two further hits" when five are named, did not cover 529/532/537 at all, and **its first quoted phrase did not match the file** — line 511 reads `RETIRED` in caps, so a literal grep returns zero. It protected 1 of 5, leaving the *"not a ratchet"* line unguarded. Rewritten as a count. Also fixed: AC-1c's TN-18 pointer re-aimed at TN-15's closure-obligations block, now carrying **four** memory items including software-engineer's `dispatch-git-gotchas.md`; OQ-1's pointer to the retired scope constraint re-aimed at TN-16; story 06's AC-8/AC-9 ordering; and story 04 gained a clause explaining its 9→14 AC gap, which the soon-deleted tombstone used to carry. **CR's ruling to leave the first-pass History entry alone is honored** — it records what was decided then, evidence rather than criterion, and editing it would falsify the record. **Design owner's two residuals adopted:** AC-11's RED now distinguishes cardinal counts (RED) from ordinal references naming a specific trigger (must survive — three legitimate ones exist), and TN-15's floor gained the Learning-Loop opener, a retired-claim site sharing no token with the gate.
- 2026-08-02 (fourth pass): **Spec audit incorporated — TN-18 added. 2 MUST FIX + 9 SHOULD FIX, all eleven ACCEPTED, none dismissed.** **MF-1 is the significant one: E-280-07's AC-1 was unsatisfiable by the decomposition** — it demanded the retired "unstaged = current story" invariant be gone across three trees while seven instances sat in files owned by 04 and 06, both running after it, with no AC anywhere removing them. **07 moved from second to LAST**, 04 gained AC-15, 06 gained AC-9, 07's AC-1 narrowed to its own files and its new AC-1c is the layer-wide check. TN-13's "AC-1 of 07 catches it by absence" was false and is corrected in place. My own enumeration found a fourth `codex-review/SKILL.md` site the audit missed, plus agent-memory files resting on the invariant — the latter are a closure obligation, not story work. **MF-2: TN-15 said four `ratchet` hits in `implement/SKILL.md`; there are five — my own transcription error, since the originating grep did return `:532`.** The nine SHOULD FIX covered a `git rm` that cannot execute on an untracked directory (with a silent failure mode that would commit the tombstones), a story-06 self-contradiction I created by fixing one half of a count in an earlier pass, two surviving line anchors, an AC describing two paragraphs as a one-line instruction, a missing interval bar on the epic's only MEDIUM-risk trim, two ACs narrower than the Success Criterion they discharge, a layer-wide AC in a one-file story, and an AC whose RED turned on undocumented harness behavior. **The E-271 demotion verified clean substantively; one prose overstatement of mine corrected** — "Nothing else in this file was edited" was false of three Status-block comments, one materially. ⚠️ **Coverage bound recorded: the audit's FIRST report ran against the first-pass epic** (the lead disclosed launching it while incorporation was landing). ⚰ **This entry originally concluded "so the pass-2, pass-3 and pass-4 additions are unaudited" — true when written, falsified by the audit's later passes, and corrected 2026-08-02 under SF-vi.** Current coverage is in TN-18.
- 2026-08-02 (third pass): **Design-owner review round incorporated — TN-17 added.** Charge 1 (OQ-5 inversion) CONFIRMED clean. **Finding A is material and refutes the design owner's own tier table**: no top-level `templates/` exists (a literal match would have pulled `.project/templates/` prose into the full rubric), `.githooks/` was omitted though its `pre-commit` executes, and 19 tracked top-level paths matched no tier — all fixed in E-280-02, with a **default-to-tier-A rule** that makes AC-8 satisfiable by construction and fails safe. All three claims verified first-hand on disk before acting. **New E-280-08 AC-10a** from Charge 2's substance: trigger 7 keeps a routable yes/no rather than dangling off the assessment's step 3. **Three review items were written against the pre-second-pass epic** and are adjudicated against the current file: Charge 3 is already satisfied by the current AC-12, Finding C's premise is updated, and **Finding B is DECLINED** — it reports the trigger-count de-restatement as out of scope when it has landed as Success Criterion 6, on the stated reason that it pulls in extra files, when all four sites are already epic-owned. The design owner argued both sides of that one across two messages; TN-17 records why the earlier position is the one the evidence supports rather than the later one.
- 2026-08-02 (second pass): **Design-owner deltas incorporated — TN-16 added with per-item dispositions.** Five adopted, three declined. The two that change the epic most: **trigger 8 is re-priced rather than merely un-gated** (retiring trigger 7 un-prices trigger 8 as an unnamed side effect, and the file itself calls an ungated trigger 8 a growth pump — PM's first AC left that hole open), and **the trigger count stops being stated at all**, which retires PM's own keep-it-at-eight constraint in favour of removing the restatement that made a cascade possible. Also adopted: one-verdict-per-frozen-state with a tree-SHA RED, the precise re-verdict seams in `implement/SKILL.md`, one periodic pass subsuming three cadences, and the byte measurement replacing the retired ratchet as the distillation instrument. **Declined:** the 6→5 story merge (sizing and a needless dependency coupling), and CA's view that E-280-02 AC-6 is unnecessary. **Flagged:** CA's five-story table drops `worktree-isolation.md` entirely — an enumeration omission that would strand both the staging-state invariant and the `git checkout --` prohibition; E-280-07 retains the file.
- 2026-08-02: **All five operator gates ruled and incorporated; every story is `TODO` and nothing blocks READY-for-review.** Three rulings, and each changed the epic in a way a reader would otherwise mis-infer:
  - **OQ-A: E-280 dispatches first, E-271 demoted to DRAFT.** PM executed the demotion (status flip + dated History entry in E-271's epic file) under fresh operator authorization, which is the narrow exception to this epic's own no-touch Non-Goal; no E-280 *story* reads or writes that directory. The semantic collision is dissolved and **E-280-02 and E-280-04 are now written to fixed text**. TN-6 rewritten; Success Criterion 5 narrowed from "this epic" to "any E-280 story" so it stays true of the demotion.
  - **OQ-5: PM retains AC authority; verdict economy is one-OF-EACH.** ⚠️ **This ruling INVERTED an acceptance criterion rather than merely unblocking one.** E-280-02's AC-1 read *"No path through this file requires two agents to approve the same code story"* and E-280-07's AC-1 required verifying the **absence** of dual-approval and PM-AC-authority sentences. Under one-of-each those sentences are TRUE and must SURVIVE — both stories now target **re-issuance** instead, which is what the measured overhead actually was. An unblock that left those ACs standing would have shipped the design the operator declined. Also corrected: the epic claimed "E-280-02 AC-8 is written to hold whichever way this is ruled"; that property is E-280-**07** AC-3, and 02's AC-8 is the tier-partition criterion.
  - **OQ-1 + OQ-B: the size GATE is retired for periodic refinement.** Operator verbatim: *"I don't think gating and ratchets are working. I think we just need periodic refinement."* The script survives as an on-demand diagnostic. **New story E-280-08** carries the retirement plus the periodic-refinement cadence plus TRIM 2's batched audit line — TRIM 2 moved out of E-280-04 because the gate seams land in the same file (`context-layer-assessment.md`), so a dedicated cadence story is the file-disjoint home for both. TN-15 added with the seam inventory, the four false-positive `ratchet` hits in `implement/SKILL.md` that mean the *other* instrument, and PM's scope constraint keeping the trigger count at eight. OQ-2 and OQ-B dissolved with the gate. TN-4 kept but its verdict column struck; TN-9's +418 reframed from inherited debt to the evidence that ended the gate.
  - **One structural defect found during incorporation and fixed in the direction opposite to the obvious one:** E-280-01 recorded `Blocks: E-280-02` with a Handoff Context promising 02 the distilled two-cause differential — residue from the first decomposition. The restructured 02 forbids touching that file in as many words and lists one file that is not it. The edge was a **phantom**, not a missing reciprocal; both sides struck, and 01 and 02 are independent.
  - **Planning ran under the lean charter** (TN-13): single verdicts against frozen files, no confirmation-chase traffic, review stopping when a pass finds nothing new. This incorporation round produced no consultation traffic; the rulings arrived settled.
- 2026-08-01: Created (DRAFT). Scope operator-ruled the same day via interactive gates; not open for re-litigation.
- 2026-08-01: OQ-1 re-framed to be independent of E-279's ratchet figure after PM found the relayed `+418` signed exception is not recorded anywhere in the repo, while the committed archive records `+350` with Reading 2 owed. TN-3 extended with the relayed 891k-output-token and three-of-five-single-pass figures and with the residual-floor statement; two Non-Goals added from it.
- 2026-08-01: **Restructured to the design owner's second, file-disjoint decomposition** — five live stories (01, 02, 07, 04, 06), only one file touched twice. `E-280-03` and `E-280-05` tombstoned in place with per-AC successors named; both need `git rm`. **OQ-A opened and is now the highest-priority gate**: E-271 collides SEMANTICALLY, with two of its frozen ACs forbidding two E-280 stories' edits, so the "record it as a dispatch-time item" boundary treatment PM was given is insufficient for those two. **OQ-B opened** (absorb +418 or re-snapshot), deliberately kept separate from OQ-1. **PM's `.claude/skills` growth forecast was wrong and is corrected**: it ends net-negative (≈ −120) because the frontmatter replaces the Activation-Triggers bodies. **TN-14 added** — the telemetry was adjudicated after all: 89% false positives, and 41% of candidates are freeze/release/hold/confirm choreography, which is measured evidence for TRIM 1. **OQ-5 sharpened** with the literal reading of the operator's own words (three artifacts, one each) under which no standing rule needs overriding.
- 2026-08-01: Design owner (claude-architect) delivered the first decomposition and two recommendations. Six stories written. **PM's `+418`-is-unverified finding was corrected in half**: CA measured the live tree and 9 + 35 + 374 = 418 is producible, so the FIGURE stands as a current measurement; only the signature remains unrecorded. TN-9 (always-on load, ratchet state), TN-10 (telemetry, run but deliberately not adjudicated) and TN-11 (dual-approval site floor) added. **OQ-5 opened** — CA's TRIM 1 design requires striking PM's AC authority, which overrides a standing operator ruling, so PM escalated rather than deciding. E-280-02 set `BLOCKED` on it.
