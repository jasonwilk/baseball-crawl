# E-085-04: Context-Layer Documentation Update

## Epic
[E-085: Credential Resilience and Diagnostic Improvements](epic.md)

## Status
`DONE`

## Description
After this story is complete, CLAUDE.md and `docs/api/auth.md` will reflect the new login fallback capability in `TokenManager`, the enhanced `bb creds check` output, and the proxy section in `bb status`. Agents will know that the system can auto-recover from expired refresh tokens and how to guide operators through credential diagnostics.

## Context
E-085-01 adds auto-recovery via the full login flow. E-085-02 and E-085-03 enhance the CLI diagnostic output. The context layer needs to reflect these changes so that agents (and the main session) give operators accurate guidance. Currently, CLAUDE.md says the system requires "no manual token capture beyond the initial refresh token" -- after this epic, it should say the system requires no manual intervention at all for web profile, given email/password are in `.env`.

## Acceptance Criteria
- [ ] **AC-1**: CLAUDE.md "Three-token architecture" paragraph updated to mention that `TokenManager` can auto-recover from expired refresh tokens via the full login flow (web profile only). Note: `GAMECHANGER_USER_EMAIL` and `GAMECHANGER_USER_PASSWORD` are already listed in CLAUDE.md -- this AC covers the behavioral change (auto-recovery), not re-adding the variables.
- [ ] **AC-2**: CLAUDE.md `bb creds check` command entry updated to describe the enhanced multi-section output (credential presence, token expiry, API health, proxy status)
- [ ] **AC-3**: CLAUDE.md `bb status` command entry updated to mention proxy connectivity as one of the sections
- [ ] **AC-4**: `docs/api/auth.md` "Token Refresh Flow" section updated to note that `TokenManager` now implements the login fallback automatically when refresh fails
- [ ] **AC-5**: No speculative or future-state language -- all updates describe implemented, tested reality
- [ ] **AC-6**: CLAUDE.md describes current implemented reality only (per project convention)

## Technical Approach
Read the completed E-085-01 story to understand the final `TokenManager` API. Read CLAUDE.md sections that reference credentials, token management, and the `bb` commands. Update in place.

Reference files:
- `/workspaces/baseball-crawl/CLAUDE.md` -- primary target (GameChanger API section, Commands section)
- `/workspaces/baseball-crawl/docs/api/auth.md` -- Token Refresh Flow section

## Dependencies
- **Blocked by**: E-085-01, E-085-02, E-085-03 (must document implemented reality, not planned behavior -- AC-2 depends on 02's `bb creds check` output, AC-3 depends on 03's `bb status` proxy section, AC-5/AC-6 require all implementation to be complete)
- **Blocks**: None

## Files to Create or Modify
- `CLAUDE.md` -- update GameChanger API section, Commands section
- `docs/api/auth.md` -- update Token Refresh Flow section

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Updated docs accurately describe implemented behavior (verified by reading the completed stories 01, 02, 03)
- [ ] No speculative or future-state language in any updated file

## Notes
- Keep the CLAUDE.md changes minimal and precise. The detailed login flow documentation lives in `docs/api/auth.md` -- CLAUDE.md should reference it, not duplicate it.
- The mobile profile caveat remains: "Mobile profile note: the mobile client key has NOT been extracted, so programmatic mobile token refresh is not yet possible."
- CA consultation confirmed: update the "Three-token architecture" paragraph and `.env` variables list. No changes needed to `bb creds refresh` command entry. No new rule file needed -- CLAUDE.md update is sufficient (credential resilience is a behavioral change to an existing module, not a cross-cutting concern).
