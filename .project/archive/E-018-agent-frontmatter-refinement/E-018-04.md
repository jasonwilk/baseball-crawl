# E-018-04: Refine data-engineer Frontmatter

## Epic
[E-018: Agent Frontmatter Refinement](./epic.md)

## Status
`DONE`

## Description
After this story is complete, the data-engineer agent configuration file will have complete, well-structured YAML frontmatter that follows the project's frontmatter standard. The bloated multi-paragraph description with embedded routing examples will be replaced with a concise routing trigger, an explicit `tools` allowlist will be added, and all required frontmatter fields will be present in the canonical order.

## Context
The data-engineer agent designs database schemas, writes SQL migrations, architects ETL pipelines, and optimizes query patterns. It is an implementing agent (routes through PM) that needs full file editing and Bash capabilities for writing migrations and testing SQL. It does NOT delegate to other agents (no Task/Agent needed -- it receives work, not dispatches it).

The frontmatter standard is documented at `/.project/research/E-018-frontmatter-standard.md`.

## Acceptance Criteria
- [ ] **AC-1**: The YAML frontmatter in `/Users/jason/Documents/code/baseball-crawl/.claude/agents/data-engineer.md` contains exactly these fields in this order: `name`, `description`, `model`, `color`, `memory`, `tools`
- [ ] **AC-2**: The `description` field is: `"Database schema design, SQL migration management, ETL pipeline architecture, and query optimization specialist. Designs and implements the data layer against coaching analytics requirements. Requires a story reference before beginning any work."`
- [ ] **AC-3**: The `tools` field lists exactly: `Read`, `Write`, `Edit`, `Bash`, `Glob`, `Grep`, `WebFetch`
- [ ] **AC-4**: The `name` field is `data-engineer`, `model` is `sonnet`, `color` is `blue`, `memory` is `project` (all preserved from current values)
- [ ] **AC-5**: The multi-paragraph routing examples that were in the old description are removed entirely -- they do NOT appear in the frontmatter or the system prompt body
- [ ] **AC-6**: The system prompt body (everything below the `---` closing delimiter) is unchanged except for the removal of routing examples. All sections (Identity, Core Responsibilities, Work Authorization, Database Standards, etc.) are preserved verbatim
- [ ] **AC-7**: The file parses as valid YAML frontmatter + Markdown body with no syntax errors

## Technical Approach
1. Read the frontmatter standard at `/Users/jason/Documents/code/baseball-crawl/.project/research/E-018-frontmatter-standard.md`
2. Read the current agent file at `/Users/jason/Documents/code/baseball-crawl/.claude/agents/data-engineer.md`
3. Replace the entire YAML frontmatter block (between `---` delimiters) with the new frontmatter
4. Verify the system prompt body below the frontmatter is intact
5. Verify the file parses correctly

## Dependencies
- **Blocked by**: None (E-018-R-01 is DONE)
- **Blocks**: None

## Files to Create or Modify
- **Modify**: `/Users/jason/Documents/code/baseball-crawl/.claude/agents/data-engineer.md`

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] File parses as valid YAML frontmatter + Markdown
- [ ] No regressions -- agent identity, responsibilities, and system prompt content preserved

## Notes
- The frontmatter standard is at `/Users/jason/Documents/code/baseball-crawl/.project/research/E-018-frontmatter-standard.md` -- read it for full rationale behind each decision.
- Do NOT change the agent's system prompt body content (responsibilities, database standards, work authorization, skill references, memory instructions, etc.).
