# E-018-02: Refine baseball-coach Frontmatter

## Epic
[E-018: Agent Frontmatter Refinement](./epic.md)

## Status
`DONE`

## Description
After this story is complete, the baseball-coach agent configuration file will have complete, well-structured YAML frontmatter that follows the project's frontmatter standard. The bloated multi-paragraph description with embedded routing examples will be replaced with a concise routing trigger, an explicit `tools` allowlist will be added, and all required frontmatter fields will be present in the canonical order.

## Context
The baseball-coach agent is a domain expert that translates coaching needs into technical requirements. It is a consultative agent (direct-routing exception) that primarily reads files and produces requirements documents. It does NOT execute code (no Bash needed), does NOT delegate to other agents (no Task/Agent needed), and does NOT need web access (domain knowledge is internal).

The frontmatter standard is documented at `/.project/research/E-018-frontmatter-standard.md`.

## Acceptance Criteria
- [x] **AC-1**: The YAML frontmatter in `/Users/jason/Documents/code/baseball-crawl/.claude/agents/baseball-coach.md` contains exactly these fields in this order: `name`, `description`, `model`, `color`, `memory`, `tools`
- [x] **AC-2**: The `description` field is: `"Baseball analytics domain expert and coaching requirements translator. Defines what statistics and data matter for coaching decisions, validates schemas and features against real coaching needs, and designs scouting report formats."`
- [x] **AC-3**: The `tools` field lists exactly: `Read`, `Write`, `Edit`, `Glob`, `Grep`
- [x] **AC-4**: The `name` field is `baseball-coach`, `model` is `sonnet`, `color` is `red`, `memory` is `project` (all preserved from current values)
- [x] **AC-5**: The multi-paragraph routing examples that were in the old description are removed entirely -- they do NOT appear in the frontmatter or the system prompt body
- [x] **AC-6**: The system prompt body (everything below the `---` closing delimiter) is unchanged except for the removal of routing examples. All sections (Identity, Your Context, Core Responsibilities, Key Baseball Analytics Knowledge, etc.) are preserved verbatim
- [x] **AC-7**: The file parses as valid YAML frontmatter + Markdown body with no syntax errors

## Technical Approach
1. Read the frontmatter standard at `/Users/jason/Documents/code/baseball-crawl/.project/research/E-018-frontmatter-standard.md`
2. Read the current agent file at `/Users/jason/Documents/code/baseball-crawl/.claude/agents/baseball-coach.md`
3. Replace the entire YAML frontmatter block (between `---` delimiters) with the new frontmatter
4. Verify the system prompt body below the frontmatter is intact
5. Verify the file parses correctly

## Dependencies
- **Blocked by**: None (E-018-R-01 is DONE)
- **Blocks**: None

## Files to Create or Modify
- **Modify**: `/Users/jason/Documents/code/baseball-crawl/.claude/agents/baseball-coach.md`

## Definition of Done
- [x] All acceptance criteria pass
- [x] File parses as valid YAML frontmatter + Markdown
- [x] No regressions -- agent identity, responsibilities, and system prompt content preserved

## Notes
- The frontmatter standard is at `/Users/jason/Documents/code/baseball-crawl/.project/research/E-018-frontmatter-standard.md` -- read it for full rationale behind each decision.
- The baseball-coach is the most restricted agent in terms of tools -- it is purely consultative and advisory. No Bash, no WebFetch.
- Do NOT change the agent's system prompt body content (responsibilities, domain knowledge, output standards, skill references, memory instructions, etc.).
