# E-221: Test Fixture Schema Parity Audit + Post-E-220 Perspective Residuals

## Status
`COMPLETED`

## Overview
Land the three residual perspective-awareness fixes that Codex round 8 surfaced on E-220's dispatched work, then eliminate the test-infrastructure drift that allowed the whole round-by-round discovery cascade to happen in the first place. E-221 is the deliberate landing spot for "ship E-220 with known residuals" — it catches what we chose not to grind further on, and it hardens the test safety net so the next cross-cutting invariant epic does not repeat this pattern.

## Background & Context
E-220 made `perspective_team_id` a first-class NOT NULL concept on four stat tables. Eight rounds of post-dispatch Codex review uncovered cross-cutting integration gaps — helpers that took `game_id`/`team_id` without knowing the new perspective dimension. Rounds 1-7 fixed them. Round 8 surfaced three more residuals (cluster 3 Phase 1a incompleteness, Phase 1b missing reconciliation cleanup, `bb data reconcile --game-id` not plumbing perspective) plus a deeper finding: the test safety net itself is lying.

The user chose to ship E-220 with residuals captured as E-221 rather than grind an additional round. This decision was right: diminishing returns were clear by round 7, round 8 regression (round 6's own cluster 2 fix introduced the P1-1 FK violation) showed that continuing to patch was risking new bugs, and the fixture drift explains why so many rounds were needed — per-story CR reviewed diffs against fixtures that did not enforce the very invariants E-220 was establishing.

**Round 8 P1 findings** (all accepted for E-221 scope):
- **R8-P1-1** — `src/api/routes/admin.py::_delete_team_cascade` Phase 1a cascades by `game_id IN (...)` without scoping to the deleted team's perspective. When cross-perspective games exist, the admin delete wipes other participants' rows. Round 6 cluster 3 added the informed-consent UI but never scoped Phase 1a; round 7 added Phase 1b scoped by `perspective_team_id = T` on 5 tables but did not backfill Phase 1a.
- **R8-P1-2** — `_delete_team_cascade` Phase 1b is incomplete. It cleans 5 tables scoped by `perspective_team_id = T` but omits the newly-NOT-NULL `reconciliation_discrepancies.perspective_team_id` from round 7. Team delete fails with a FK violation when the deleted team owns reconciliation rows. Same silent-catch pattern as round 7 P1-1 pre-fix.
- **R8-P1-3** — `src/cli/data.py::bb data reconcile --game-id X` calls `reconcile_game(conn, game_id, dry_run=...)` with no `perspective_team_id` kwarg. Round 6 cluster 4 made `reconcile_all` iterate perspectives but left the single-game CLI path on the default. Cross-perspective games silently miss half the data. The "helper knows, caller doesn't" pattern.

**Round 7 fixture evidence** (expanded here as formal scope):
- 7 test files currently define `games` tables inline with columns that do not enforce production FK constraints: `test_report_generator.py`, `test_e211_report_generator.py`, `test_game_ordering.py`, `test_gc_uuid_resolver.py`, `test_pitching_workload.py`, `test_player_dedup.py`, `test_reconciliation.py`
- 22+ other test files already load `migrations/001_initial_schema.sql` via `executescript` — the correct pattern exists but there is no shared helper and no CI guardrail
- `test_report_generator.py`'s inline `reconciliation_discrepancies` fixture is severely drifted (missing `boxscore_value`, `plays_value`, `delta`, `correction_detail`, UNIQUE, and FKs) — round 7 applied a minimum-viable single-column add to unblock the bonus bugfix but left the rest for this epic
- `test_admin_delete_cascade.py::test_cascade_delete_team_preserves_games_row_when_other_perspective_remains` only asserts own-team rows disappear; it does not assert that the other participant's perspective-tagged rows survive. Had it, R8-P1-1 would have been caught in round 6.

E-220's History records the full 7-round remediation arc and the structural miss that drove this epic.

## Goals
- R8-P1-1, R8-P1-2, R8-P1-3 fixed with RED tests demonstrating the bug before the fix
- All 7 inline-fixture test files migrated to a shared real-schema helper with FK enforcement
- A lint/CI guardrail prevents new inline `CREATE TABLE` statements in `tests/`
- `test_admin_delete_cascade.py` gains cross-perspective preservation assertions (other participant survives)
- Full test suite baseline matches or improves on E-220 close state (72F/4254P/16E after E-220 round 8)

## Non-Goals
- Adding new E-220-style invariants or perspective dimensions — this epic consolidates what E-220 started, it does not extend it
- Re-opening E-220's bonus bugfix in `generator.py` — that landed in round 7
- Building a generic test-fixture generator or ORM layer — the shared helper is a thin `executescript` wrapper, nothing more
- Fixing the 72 pre-existing test failures or 16 pre-existing errors — those predate E-220 and are out of scope
- Migrating the inline `reconciliation_discrepancies` fixture in `test_report_generator.py` to real schema via the shared helper in a single sweep — the shared helper migration (Story 05) handles the whole file atomically, not column-by-column

## Success Criteria
- R8-P1-1: admin delete of a team that shares games with other perspectives leaves other participants' rows intact (plays, spray_charts, player_game_batting, player_game_pitching, reconciliation_discrepancies). RED test added that exercises the cross-perspective case and fails before the fix.
- R8-P1-2: admin delete of a team with reconciliation_discrepancies rows succeeds without FK violation. RED test added.
- R8-P1-3: `bb data reconcile --game-id X` processes all loaded perspectives for the game, not just one. RED test added that loads two perspectives and asserts discrepancies from both land in the database.
- Shared helper `tests/conftest.py::load_real_schema(conn)` exists, is used by all 7 former-inline files, and enables `PRAGMA foreign_keys=ON` via prepended PRAGMA per `.claude/rules/migrations.md`.
- Grep audit: zero `CREATE TABLE` statements in files under `tests/` except in `tests/conftest.py` itself (if applicable) or explicit fixture helpers marked with a pragma comment.
- CI guardrail: a test or lint step fails when a PR introduces an inline `CREATE TABLE` in `tests/`.
- `test_admin_delete_cascade.py` has at least one test that asserts cross-perspective rows survive a single-team delete under the informed-consent path.
- Full suite: 72F/4254P/16E matched or improved (net zero regression, new tests pass).

## Stories
| ID | Title | Status | Dependencies | Assignee |
|----|-------|--------|-------------|----------|
| E-221-01 | Shared real-schema fixture helper | DONE | None | software-engineer |
| E-221-02 | Migrate 7 inline-fixture test files to helper | DONE | E-221-01 | software-engineer |
| E-221-03 | CI guardrail: forbid inline CREATE TABLE in tests/ | DONE | E-221-02 | software-engineer |
| E-221-04 | Cross-perspective preservation test coverage | DONE | E-221-02 | software-engineer |
| E-221-05 | R8-P1-1 Phase 1a perspective scoping + Phase 2 conditional games DELETE | DONE | E-221-04 | software-engineer |
| E-221-06 | R8-P1-2 Standalone regression test for reconciliation_discrepancies cascade | DONE | E-221-05 | software-engineer |
| E-221-07 | R8-P1-3 `bb data reconcile --game-id` perspective plumb | DONE | E-221-02 | software-engineer |

## Dispatch Team
- software-engineer
- data-engineer

## Technical Notes

### TN-1: Shared fixture helper (Story 01)
Create `tests/conftest.py` (or extend existing) with a `load_real_schema(conn: sqlite3.Connection) -> None` helper. It reads `migrations/001_initial_schema.sql` from the repo root (use `Path(__file__).resolve().parents[1] / "migrations" / "001_initial_schema.sql"` — repo-root-relative resolution per CLAUDE.md architecture rules), prepends `PRAGMA foreign_keys=ON;\n` to the SQL string, and calls `conn.executescript(...)`.

Per `.claude/rules/migrations.md`: "A `PRAGMA foreign_keys=ON` set on the connection before `executescript()` has no effect on the SQL it runs. If a script needs FK enforcement, prepend `PRAGMA foreign_keys=ON;\n` to the SQL string passed to `executescript()`." This is the load-bearing detail — setting the pragma on the connection alone does not work.

Keep the helper under 30 lines. No pytest fixtures, no parameterization, no environment flags. If a test wants a different schema state, it applies its own modifications after calling the helper.

### TN-2: Fixture migration strategy (Story 02)
The 7 files each have inline schema definitions for subsets of tables. For each file:
1. Replace the inline `CREATE TABLE` block with `load_real_schema(db)`.
2. Walk every INSERT in the file and verify columns referenced exist in real schema. If the old fixture omitted columns the real schema requires as NOT NULL, the INSERT must supply a value.
3. Expected breakage class: hardcoded sentinel values (like `team_id=999`) that were never seeded into `teams`. Fix by seeding a real opponent team with `_seed_team()` helpers if they exist in the file, or create minimal per-file seed helpers.
4. Run each file's tests in isolation before moving on. Do NOT batch all 7 then run the suite — the failure attribution becomes impossible.

**`test_report_generator.py` is the largest** (41 inline CREATE TABLE statements) and the most likely to surface cascading assertion failures. Land it last in the migration sequence, after the other 6 are green, so any issues it surfaces can be isolated.

### TN-3: CI guardrail (Story 03)
Add a pytest test in `tests/test_no_inline_schemas.py` (or equivalent) that:
1. Walks every file under `tests/` matching `test_*.py`
2. Greps each for `CREATE TABLE` outside of allowed locations (the shared helper itself, or files marked with a pragma comment like `# noqa: fixture-schema` for legitimate exceptions)
3. Fails with a clear message listing any violations

Alternative implementation: a pre-commit hook or a CI shell script. Pytest-based is preferred because it runs in the same test harness and is harder to skip.

### TN-4: Cross-perspective preservation test coverage (Story 04)
Before fixing R8-P1-1 (Story 05), Story 04 adds the test that catches it. The test:
1. Loads a game from perspective A and perspective B via GameLoader with different `perspective_team_id` values
2. Calls `_delete_team_cascade` on team A with the informed-consent confirmation
3. Asserts that perspective A's stat rows are deleted
4. Asserts that perspective B's stat rows for the SAME game_id are still present
5. Asserts the `games` row itself is still present (via round 6 cluster 2's preservation logic)

This test will FAIL before Story 05 lands and PASS after. Story 05 cannot be marked DONE without this test passing.

### TN-5: R8-P1-1 Phase 1a scoping (Story 05)
`src/api/routes/admin.py::_delete_team_cascade` Phase 1a currently cascades scoped rows by `game_id IN (...)`. The fix: scope by `perspective_team_id = T` (or the equivalent filter) on every DELETE in Phase 1a that touches perspective-carrying tables (`plays`, `spray_charts`, `player_game_batting`, `player_game_pitching`, `reconciliation_discrepancies`).

The game-level cleanup (delete `game_perspectives` rows for T's perspective, then conditionally delete `games` row if no perspectives remain) was already correct from round 6 cluster 2 — do NOT regress that. The fix is at the stat-table layer only.

Consult DE if the SQL shape is unclear — the existing `_delete_game_scoped_data_for_perspectives` helper at `src/reports/generator.py:1306-1389` may be directly reusable. Investigate whether Phase 1a should call the same helper rather than duplicating the logic.

### TN-5b: E-221-05 refactor-delegate direction (amendment 2026-04-13)

**Preserves TN-5 as the historical record.** During the 2026-04-13 session restart of E-221-05 dispatch, SE empirically discovered that the admin cascade's in-place Phase 1a + Phase 2 fix from 2026-04-12 was insufficient: three tests fail in `tests/test_admin_delete_cascade.py`, not one. CR independently verified the empirical state, and the user authorized **Option 2 — refactor-delegate**.

Under Option 2:
- `_delete_team_cascade` in `admin.py` no longer contains in-place Phase 1a/1b/2/3/4 DELETE blocks.
- Cleanup is delegated to `src/reports/generator.py::cascade_delete_team` (the canonical helper already used by the reports flow).
- `admin.py` retains only the admin-specific wrapping: the `confirm_cross_perspective` probe, the informed-consent exception raise, and the audit log entry. The wrapping ends with a single delegation call.
- `src/reports/generator.py::cascade_delete_team` may be extended as needed to support the admin caller's scope; SE determines the minimum extension during implementation; CR verifies no divergent code paths are introduced.

**IDEA-069 absorption**: The cascade consolidation refactor captured as IDEA-069 on 2026-04-12 (CANDIDATE) is absorbed into E-221-05's scope on 2026-04-13 and promoted to PROMOTED at story closure. Future cascade-delete invariants land in exactly one place rather than two — the duplication risk class that caused R8-P1-1 in the first place is eliminated.

**Why the prior diagnosis was wrong**: The 2026-04-13 checkpoint handoff doc claimed the RED test's fixture set `team_id = team_a_id` on team B's perspective rows (a latent semantic bug). SE verified on disk that the fixture is already semantically correct (`team_id = team_b_id, perspective_team_id = team_b_id` for team B's scouting records). The real root cause is missing production logic in `admin.py::_delete_team_cascade` — specifically, missing teams-row retention handling and missing cross-perspective orphan cleanup. The delegation path inherits both from the canonical helper, so no fixture amendments are needed for the RED test or `test_delete_team_proceeds_with_cross_perspective_confirmation`; only `test_cascade_deletes_plays_and_children` may need an assertion update to reflect games-row survival under `NOT EXISTS` semantics.

### TN-6: R8-P1-2 Phase 1b reconciliation cleanup (Story 06)
`_delete_team_cascade` Phase 1b cleans 5 tables by `perspective_team_id = T`. Add `reconciliation_discrepancies` as the 6th. One line of SQL: `DELETE FROM reconciliation_discrepancies WHERE perspective_team_id = ?`.

RED test: insert reconciliation rows tagged with team T's perspective, then call `_delete_team_cascade` on T, assert the rows are gone (and the cascade succeeded — no FK violation). Must be tested under FK enforcement (the shared helper from Story 01 provides that).

### TN-7: R8-P1-3 CLI perspective plumbing (Story 07)
`src/cli/data.py::bb data reconcile --game-id X` calls `reconcile_game(conn, game_id, dry_run=...)`. The fix has two options, and **SE must consult DE** before implementing:

**Option A**: Inside the CLI handler, query `game_perspectives` for the given game_id and iterate all perspectives, calling `reconcile_game` once per `(game_id, perspective_team_id)` pair. Mirrors `reconcile_all`'s per-pair iteration from round 6 cluster 4.

**Option B**: Add a `--perspective-team-id N` flag to `bb data reconcile --game-id X`. When omitted, use Option A's iteration. When supplied, reconcile only that one perspective.

Option A alone is sufficient for correctness. Option B is a UX improvement for operator debugging. DE calls which (or both) given the reconcile CLI's operator context.

RED test: load a game from two perspectives, call the CLI path for that game_id, assert both perspectives' discrepancies land in `reconciliation_discrepancies` (distinguishable by the 6-column UNIQUE from E-220 round 7).

### TN-8: Dispatch order and serialization
Stories execute serially during dispatch (one story at a time in the epic worktree). The dependency chain is:
- E-221-01 first (shared helper must exist before migrations)
- E-221-02 next (migrations depend on the helper)
- E-221-03 after 02 (the guardrail check runs against the post-migration state)
- E-221-04 after 02 (cross-perspective test requires real schema via the helper)
- E-221-05 after 04 (fix lands after the RED test is in place)
- E-221-06 and E-221-07 parallel-safe logically but still serial in dispatch; they depend on 02 for FK-enforced tests
- E-221-06 and E-221-07 may run in either order after 05

### TN-9: RED-test discipline per E-220 round 7 precedent
Every bug-fix story (05, 06, 07) MUST add its RED test BEFORE the fix, run the suite to see it fail, then land the fix and see it pass. This is the round 7 pattern that proved the fix addresses the bug and not a symptom. Code-reviewer will verify the commit graph shows this sequence.

### TN-10: Test baseline expectations
Pre-close E-220 baseline was 72F/4254P/16E. This epic's final run should be 72F/(4254+N)P/16E where N is the number of new tests added (at minimum 4 — one per bug, one cross-perspective assertion expansion). Any REGRESSION (new failures beyond the 72, or previously-passing tests now failing) is a blocker.

## Open Questions
- None — E-220 round 8 findings are fully specified, and the fixture audit scope is enumerated. If Story 02 migration surfaces additional drift that was not in the round 7 audit, capture as round-8 follow-up on the epic, do not block.

## History
- 2026-04-09: Created. Scope absorbed from E-220 round 8 Codex findings (3 P1s) plus the test fixture drift problem that enabled the round-by-round discovery cascade. User chose to ship E-220 with residuals rather than grind a round 9; E-221 is the deliberate landing.
- Expert consultations: none required for planning — all technical decisions already made during E-220 round 7-8 (DE on schema, SE on fixture strategy, Codex on bug surface). Any new decisions during dispatch (Option A vs B in TN-7) route to DE.
- 2026-04-09: E-221-08 added per user direction at E-220 close. Scope: claude-architect codifies the 8 E-220 lessons-learned into durable context-layer artifacts. User explicitly required CA use `.claude/skills/context-fundamentals/SKILL.md` as the procedural guide. Story is independent of E-221-01 through E-221-07 (no implementation dependency) and may run in parallel or last. Dispatch Team updated to include claude-architect.
- 2026-04-12: E-221-08 DROPPED from epic scope. The E-220 lessons-learned codification is being done as a direct claude-architect dispatch outside the epic — a single ~35-line atomic commit covering the four highest-leverage fixes (perspective-provenance.md frontmatter gap, PM echo-back requirement, CR Invariant Audit Mode, plan-skill fidelity-recovery runbook) plus two smaller fixes (implement-skill expert-recs-are-provisional step, dispatch-pattern main-session-default-relay note). The direct-dispatch path honors the project's "Simple first" principle and avoids the story-ceremony overhead that had no proportional benefit for context-layer-only work. Dispatch Team reverted to software-engineer + data-engineer (claude-architect removed — Phase 1 direct dispatch runs outside this epic). Story count: 7. Related: E-222 (meta-fidelity-fix) was ABANDONED the same day after scope spiral to 14 stories; its planning artifacts and the full recovery context are preserved in `.project/archive/E-222-meta-fidelity-fix-ABANDONED/`.
- 2026-04-12: ACTIVE -- dispatch begun via implement skill with "and review" modifier. Team: software-engineer, data-engineer (implementers), product-manager (status/AC), code-reviewer (quality gate). Main session to route stories serially starting with E-221-01.
- 2026-04-12: E-221-03 scope expanded during dispatch after PM enumeration discovered 15 pre-existing drift files in `tests/` beyond the 7 covered by E-221-02. AC-4 relaxed to allow documented exceptions with rationale; AC-6 added for per-file classification; Technical Approach updated. Expansion authorized by user directly during dispatch (Option A: inspect, classify, migrate-or-pragma per file). Scope growth kept inside E-221-03 rather than creating a follow-up epic.
- 2026-04-12: E-221-05 scope expanded during dispatch after E-221-04's RED test exercised both Phase 1a and Phase 2 bugs in `src/api/routes/admin.py::_delete_team_cascade`. Original TN-5 conflated the admin cascade with `src/reports/generator.py::cascade_delete_team` (which has the correct `NOT EXISTS (game_perspectives)` guard) — the admin cascade lacks the guard and unconditionally deletes the shared games row. User chose Option C: apply the minimal fix in place (scope Phase 1a DELETEs + add NOT EXISTS guard to Phase 2 games DELETE), and capture the admin/generator cascade consolidation refactor as a follow-up idea (IDEA-069; IDEA-068 is the parallel main-session-behaviors capture) for a future dedicated cleanup. Scope growth kept inside E-221-05 rather than creating a follow-up epic.
- 2026-04-13: E-221-05 scope expanded for the SECOND time during dispatch, this time to absorb IDEA-069 (consolidate cascade delete logic). After the session restart, SE re-ran the test suite against the checkpointed state and empirically discovered that three tests fail in `tests/test_admin_delete_cascade.py`, not one: (a) the RED test `test_delete_team_cascade_preserves_other_perspective_rows`, (b) `test_cascade_deletes_plays_and_children`, and (c) `test_delete_team_proceeds_with_cross_perspective_confirmation`. SE's diagnosis traced all three to missing production logic in `admin.py::_delete_team_cascade` — specifically, missing teams-row retention and missing cross-perspective orphan cleanup — rather than the fixture-anchoring bug the prior session's checkpoint handoff doc had posited. CR independently verified all four empirical claims (fixture state on disk, pytest output, admin.py code state, generator.py helper contract); the handoff doc's fixture-anchoring diagnosis was wrong — the fixture is already semantically correct on disk (`team_id = team_b_id, perspective_team_id = team_b_id` for team B's scouting records). Main session escalated to user; user chose **Option 2 — refactor-delegate**: replace `_delete_team_cascade`'s in-place cleanup phases (1a/1b/2/3/4) with a delegation call to `src/reports/generator.py::cascade_delete_team`, preserving only the admin-specific wrapping (`confirm_cross_perspective` probe, informed-consent exception, audit log). IDEA-069 (CANDIDATE on 2026-04-12) is absorbed into E-221-05's scope on 2026-04-13 and promoted to PROMOTED at story closure. TN-5 preserved as historical record; TN-5b appended to document the refactor-delegate direction. AC-5 rewritten to reflect delegation contract (the "do NOT modify Phase 1b" constraint is retired); AC-7 added to require all three failing tests green. Scope growth kept inside E-221-05 rather than creating a follow-up epic, consistent with the 2026-04-12 scope-growth pattern.
- 2026-04-13: E-221-05 DONE and staged after 3-way verification (SE + CR + PM all APPROVED). Full suite at 73F/4257P/16E matches TN-10 target. CR round 1 flagged two SHOULD FIX findings: #1 (admin-UI delete path does not clean `reports.team_id`) DISMISSED as pre-existing and out of scope, captured as IDEA-070 (CANDIDATE); #2 (AC-4 citation polish) applied in place by PM. Handoff doc open question #3 resolved — the cited `test_cascade_delete_team_preserves_games_row_when_other_perspective_remains` test exists at `tests/test_report_generator.py:1723` inside `TestCrossPerspectiveScopedDelete`, not in `tests/test_admin_delete_cascade.py`.
- 2026-04-13: E-221-06 scope REFRAMED from "add DELETE to `_delete_team_cascade` Phase 1b" to "add standalone regression test for R8-P1-2." PM's pre-dispatch scope assessment (verdict b, RED-test-only reframe) confirmed that the functional fix for R8-P1-2 was already absorbed by E-221-05's canonical helper consolidation — `src/reports/generator.py:1445-1448` (perspective pass) and `:1476-1478` (anchor pass) both clean `reconciliation_discrepancies`, and the 21/21 admin cascade test suite proves it empirically. The original charter's AC-5 ("fix is a single new DELETE in Phase 1b") is superseded because Phase 1b no longer exists in `admin.py` after E-221-05's refactor. What remains is the TN-9 obligation to maintain explicit standalone regression coverage for R8-P1-2 rather than relying on transitive coverage via the E-221-04 cross-perspective RED test. Story reframed as testing-only: add new test function(s) in `tests/test_admin_delete_cascade.py` that exercise both the perspective pass (AC-1) and the anchor pass (AC-3), with FK enforcement via `load_real_schema`. No production code changes permitted — any `src/` edits in the diff are a scope violation. The RED-vs-regression framing: because the bug is already fixed, this lands as a regression test, not a literal red-then-green RED test; the red-then-green transition is historically captured in E-221-05's dispatch (where SE watched the E-221-04 RED test go from fail to pass). Original charter preserved in the story file's Historical Record section for provenance. Dispatch Team unchanged. E-221-07 (CLI perspective plumb) confirmed NOT absorbed by E-221-05 — `src/cli/data.py:1160-1161` is untouched; that story's original scope stands.
- 2026-04-13: Phase 4b Codex remediation. After all 7 stories landed DONE and staged, the main session entered the "and review" Phase 4 integration review. **Phase 4a (CR integration review)** returned APPROVED round 1 with zero cross-story findings; CR verified all 8 focus areas explicitly: cross-story helper usage, report-deletion path regression, test class naming, import collisions, story-file hygiene, `_delete_team_scoped_data` extension regression, TN-9 discipline adherence, and CLI-vs-library contract parity. No remediation was required from Phase 4a. **Phase 4b (Codex headless review)** was attempted next and timed out at 10 minutes mid-analysis; Codex was fetching `CLAUDE.md` and the reviewer checklist from the GitHub API when the timeout fired. Main session fell back to prompt-generation per `.claude/skills/codex-review/SKILL.md` Step 4. The user ran the lean pointer-style prompt locally against the worktree and Codex returned three findings: **Finding 1 (MUST FIX) — informed-consent gate drift**: `_get_delete_confirmation_data()` at `admin.py:835` queried only 4 stat tables (plays, player_game_batting, player_game_pitching, spray_charts) and omitted `reconciliation_discrepancies`, but E-221-05's canonical helper Pass 2 at `generator.py:1476-1478` unconditionally deletes `reconciliation_discrepancies WHERE team_id = ?`. A team whose only cross-perspective footprint was reconciliation rows would bypass the informed-consent gate and silently wipe foreign-owned data on delete — a regression of E-220 round 6 cluster 3's informed-consent protection, re-introduced as a side effect of the Option 2 refactor's broader cleanup surface. **Finding 2 (SHOULD FIX) — flash message misrepresents retention**: when `cascade_delete_team` retains the teams row because cross-perspective games still FK-reference it (the code path introduced in E-221-05 at `generator.py:1554-1569`), the admin HTTP handler at `admin.py:2434` still flashed `Team "X" deleted.`. Operator flow: click delete, see "deleted" confirmation, refresh the admin teams list, see the team still there — contradicts the operator's mental model and is an honesty failure, not a correctness failure. **Finding 3 (SHOULD FIX) — no execute-mode coverage for E-221-07 iteration loop**: the new `TestReconcileCLIGameIDPerspectives` class exercised only the default dry-run path; AC-5 required `--dry-run` AND `--execute` semantics preserved under the iteration loop, so an execute-mode companion test was missing. **Triage**: all three findings classified VALID — no dismissals. Main session routed all three to SE in a single remediation round (authorized via the post-review remediation exception in `.claude/rules/workflow-discipline.md`). **Disposition Finding 1 — FIXED**: SE added a 5th UNION branch to the gate query covering `reconciliation_discrepancies WHERE team_id = T AND perspective_team_id != T` (the cross-perspective footprint), parallel to the existing 4-table pattern; the fix lives at `admin.py:836-877`. SE semantic-analyzed and deliberately excluded a "Shape 2" variant that would have matched `reconciliation_discrepancies WHERE perspective_team_id = T AND team_id != T` — that shape represents T's own scouting records ABOUT foreign games, which are legitimate own-perspective rows the canonical Pass 1 cleans unconditionally, so including them in the informed-consent gate would have generated operator warnings for every delete of a team that ever scouted anyone (a false-positive flood that would have trained operators to ignore the gate). The semantic analysis is preserved here as a design decision for future reference: the gate query's purpose is "warn when foreign-owned data would be affected," not "warn when any cross-perspective row exists." Tests added at `test_admin_delete_cascade.py:1558-1671` — one unit-level test against `_get_delete_confirmation_data` directly, one end-to-end test against the HTTP POST route. **Disposition Finding 2 — FIXED via Option (b)**: SE implemented a post-cascade probe at `admin.py:2432-2454` that re-queries the teams table after `_delete_team_cascade` returns and emits a retention-aware flash message — `Team "X" data removed; team row retained because cross-perspective games still reference it.` — when the row is retained, and the clean-path flash otherwise. SE explicitly rejected Option (a) — threading a "retained" boolean through `cascade_delete_team`'s return signature — for four reasons preserved here: (1) smaller blast radius (a contract change to `cascade_delete_team` would have touched the reports path, the admin path, and every existing caller site; the post-cascade probe is local to `admin.py` and the admin-layer concern); (2) cleaner layering (the admin HTTP flash message is an admin-layer concern, not a data-helper-layer concern — the data helper should not know or care what UX text the caller renders); (3) CLAUDE.md "don't add abstractions beyond what the task requires" governance (a boolean return type is an abstraction the task doesn't warrant); (4) accurate ground truth (querying the real post-cascade teams state via SELECT is strictly more trustworthy than a cached boolean the data helper could fail to update if its internals change later). Tests added at `test_admin_delete_cascade.py:1673-1818` — one for the retention flash path, one regression guard for the clean-path flash. **Disposition Finding 3 — FIXED**: SE added one new test function `test_cli_game_id_execute_mode_processes_all_perspectives` at `test_reconciliation.py:1033-1137` inside the existing `TestReconcileCLIGameIDPerspectives` class. The test invokes the CLI with the `--execute` flag, asserts both perspectives reconcile under correction mode, asserts the shared `run_id` clusters their rows, and asserts the per-perspective output labels render in execute mode the same way they do in detection mode. **Remediation scope**: 1 production file modified (`src/api/routes/admin.py` — +28/-3 across the two separate fix sites for F1 and F2), 2 test files modified (`tests/test_admin_delete_cascade.py` +290 lines for the four new F1+F2 tests, `tests/test_reconciliation.py` +105 lines for the F3 execute-mode test). No schema changes, no migration changes, no contract changes to `cascade_delete_team`. Full suite delta: 73F/4266P/16E post-remediation vs 73F/4261P/16E pre-Phase-4b baseline — +5 passing (matches the 5 new tests: 2 for F1, 2 for F2, 1 for F3), zero new failures, zero regressions. Admin cascade suite 27/27 (23 prior + 4 new in `TestE221Phase4bRemediation`). Reconcile CLI class 3/3 (2 prior + 1 new execute-mode). **Skill compliance note**: per `.claude/skills/codex-review/SKILL.md` Step 5 ("Remediation fixes are NOT re-reviewed"), the remediation fixes are not routed back to Codex or CR for a re-review round. Main session advances the staging boundary and proceeds to Phase 5 closure after this disposition record. **Scorecard prep for Phase 5**: Phase 4a (CR integration review) produced 0 findings. Phase 4b (Codex) produced 3 findings (1 MUST FIX, 2 SHOULD FIX). All 3 accepted; 0 dismissals. All 3 FIXED in a single remediation round. Net Phase 4 remediation changes: +28/-3 production, +395 test.
- 2026-04-13: **E-221 COMPLETED**. All three Round-8 P1 residuals from the E-220 post-dispatch review arc are closed: R8-P1-1 (admin cascade perspective scoping), R8-P1-2 (`reconciliation_discrepancies` cleanup coverage), and R8-P1-3 (`bb data reconcile --game-id` CLI perspective plumbing). The test fixture schema parity goal (Stories 01-04) is also closed, with 7 inline-fixture test files migrated to the `tests/conftest.py::load_real_schema` helper from Story 01, 15 additional drift files migrated or pragma-exempted during Story 03's in-dispatch scope expansion, and a CI guardrail (`tests/test_no_inline_schemas.py`) preventing new drift. **Stories by session**: the prior session (checkpointed on 2026-04-13 before a planned restart) closed E-221-01, E-221-02, E-221-03, and E-221-04, then marked E-221-05 IN_PROGRESS with the admin.py production fix applied but the RED test blocked on a misdiagnosed fixture-anchoring bug. The continuation session (this one) closed E-221-05 (via a second scope expansion absorbing IDEA-069 into an Option 2 refactor-delegate), E-221-06 (reframed as a testing-only regression bulwark after verdict (b) scope assessment), and E-221-07 (original scope stood; DE chose Option A unconditional iteration), then absorbed a Phase 4b remediation round after Codex surfaced three findings the earlier review passes had missed. Total scope shipped this session: 3 stories + 1 post-review remediation pass. **Idea promotions and captures**: IDEA-069 (consolidate cascade delete logic between admin route and reports path) PROMOTED on 2026-04-13 and absorbed into E-221-05 as load-bearing work under the Option 2 refactor-delegate path. IDEA-070 (admin-UI delete-team path does not clean `reports.team_id`) CAPTURED on 2026-04-13 from the dismissed E-221-05 round-1 CR finding #1 (pre-existing FK gap, out of scope for E-221-05's charter); remains CANDIDATE for future promotion. **Agent coordination notes**: the session-local 3-way verification policy (SE + CR + PM independent AC verification plus main-session triangulation) was active throughout the continuation session per the handoff doc's explicit directive, and it paid off — E-221-05's refactor scope correction came from SE's empirical investigation of three failing tests that the handoff doc's fixture-anchoring diagnosis had misattributed, with CR independently verifying all four empirical claims before the main session escalated to the user for the Option 2 decision. The `rtk proxy pytest` pattern was replaced mid-dispatch with `/usr/local/python/current/bin/python -m pytest ...` after the user reported rtk rewrite problems; SE adopted the pattern seamlessly and CR's empirical verification runs under the new pattern matched SE's reported numbers. The prior session's handoff doc contained one factual error (the fixture-anchoring diagnosis for the RED test's team B seeds) which was caught by SE empirically on session restart and resolved cleanly via the Option 2 path — a clean demonstration that the 3-way verification policy functions exactly as intended when one source disagrees with empirical reality. **Documentation Assessment**: per `.claude/rules/documentation.md`, the six update triggers were evaluated per-trigger with explicit yes/no verdicts: (1) New feature or endpoint ships — **YES** (the admin delete flow gained a new retention flash message variant "Team \"X\" data removed; team row retained because cross-perspective games still reference it." and the informed-consent gate query was extended to include `reconciliation_discrepancies`; both are operator-visible behavior changes on the existing `POST /admin/teams/{id}/delete` route); (2) Architecture or deployment configuration changes — NO; (3) New agent created or materially modified — NO; (4) Database schema changes — NO (no migrations, no column changes, no new FKs; the Option 2 refactor was pure code consolidation); (5) Epic completes that changes how the system works or how users interact with it — **YES** (operators running `bb data reconcile --game-id X` now see per-perspective output labels and two runs' worth of discrepancies instead of one; admin UI delete behavior now surfaces the teams-row retention case with an honest flash message). **Action**: docs-writer dispatched by main session for `/tmp/.worktrees/baseball-crawl-E-221/docs/admin/operations.md` team-deletion section update (around lines 138-148) in parallel with this closure entry. **Context-Layer Assessment**: per `.claude/rules/context-layer-assessment.md`, the six triggers were evaluated per-trigger with explicit yes/no verdicts: (1) New convention/pattern/constraint established — **YES** with three patterns: (1a) team deletion delegates to `src/reports/generator.py::cascade_delete_team` as the canonical helper, with the admin route wrapping HTTP-layer concerns (confirm_cross_perspective probe, informed-consent gate, audit log, flash message) but cleanup logic living in one place; (1b) the "cleanup-detection mirror invariant" — when extending a cleanup helper's surface (new table or broader WHERE clause), all detection/gate queries that mirror that surface must be audited and updated in lock-step; E-221's Phase 4b MUST FIX surfaced exactly this failure mode, where the cleanup grew but the gate didn't and a silent-delete path emerged; (1c) the "post-cascade probe for retention UI" pattern — when a cascade can conditionally retain a row (e.g., teams-row retention when cross-perspective games still FK-reference it), the HTTP/UX caller probes the post-cascade database state to emit accurate user-facing messages rather than threading retention state through the data-helper contract. (2) Architectural decision with ongoing implications — **YES** with two decisions: Option 2 refactor-delegate (chosen over in-place patching in E-221-05, now the established pattern for future cascade work); Option (b) post-cascade probe (chosen over contract change in Phase 4b Finding 2, now the established pattern for retention UI). (3) Footgun/failure mode/boundary discovered — **YES**: the gate-cleanup drift bug pattern (Phase 4b Finding 1). Codifying it as a reviewer checklist item will prevent recurrence in future epics that extend cleanup surfaces. (4) Change to agent behavior, routing, or coordination — NO (session-local policies — 3-way verification and rtk-bypass pytest pattern — were explicitly session-scoped per the handoff doc and the prior main-session directive; not for permanent codification). (5) Domain knowledge discovered that should influence agent decisions — NO (teams-row retention semantics are code-level and captured in generator.py comments; no new baseball or API domain insight). (6) New CLI command/workflow/operational procedure introduced — NO (`bb data reconcile --game-id` change is an extension of an existing command, not a new command or workflow). **Action**: claude-architect dispatched by main session for context-layer codification of triggers 1, 2, 3 in parallel with this closure entry. **Review Scorecard**:

| Review Pass | Findings | Accepted | Dismissed |
|---|---|---|---|
| Per-story CR — E-221-01 | prior session — records not recovered | — | — |
| Per-story CR — E-221-02 | prior session — records not recovered | — | — |
| Per-story CR — E-221-03 | prior session — records not recovered | — | — |
| Per-story CR — E-221-04 | prior session — records not recovered | — | — |
| Per-story CR — E-221-05 | 2 (both SHOULD FIX) | 1 (AC-4 citation polish applied in story file) | 1 (reports.team_id gap, captured as IDEA-070) |
| Per-story CR — E-221-06 | 1 (SHOULD FIX docstring equivalence clarity) | 1 (class-level docstring comment added by SE) | 0 |
| Per-story CR — E-221-07 | 0 | 0 | 0 |
| Phase 4a — CR integration review | 0 | 0 | 0 |
| Phase 4b — Codex code review | 3 (1 MUST FIX + 2 SHOULD FIX) | 3 (all FIXED in single remediation round) | 0 |
| **Total this session** | **6** | **5** | **1** |

Prior-session CR findings for E-221-01..04 were recorded in agent memory that did not persist across the session restart; the continuation session rows (05-07 + Phase 4a + Phase 4b) are authoritative. If prior-session records are needed for retrospective analysis, the git history of this worktree branch preserves the per-story commits and diffs.
