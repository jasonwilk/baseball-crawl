# E-055-04: Proxy Commands (`bb proxy`)

## Epic
[E-055: Unified Operator CLI](epic.md)

## Status
`DONE`

## Description
After this story is complete, the operator can view proxy analysis reports and manage proxy data through `bb proxy report|endpoints|refresh-headers|review`. Bash scripts (report, endpoints, review) are invoked via `subprocess.run()`; the Python script (refresh-headers) is imported directly. Proxy lifecycle commands (start/stop/status/logs) are excluded because the proxy runs on the Mac host, not inside the devcontainer where the CLI runs.

## Context
The proxy analysis scripts are split across two directories (`scripts/proxy-report.sh`, `scripts/proxy-endpoints.sh`, `scripts/proxy-refresh-headers.py`, `scripts/proxy-review.sh`). The CLI wraps them under a unified `bb proxy` group so the operator does not need to remember which script lives where. These commands work inside the devcontainer because they read files from the mounted `proxy/data/` volume or modify source files -- they do not need Docker access.

After E-052 (Proxy Data Lifecycle), the report and endpoints scripts gain `--session`, `--all`, and `--unreviewed` flags. After E-054 (Header Parity Refresh), `scripts/proxy-refresh-headers.py` exists. This story wraps the post-E-052/E-054 interfaces.

## Acceptance Criteria
- [ ] **AC-1**: `src/cli/proxy.py` exists and defines a Typer sub-app mounted on the main app as `bb proxy`.
- [ ] **AC-2**: `bb proxy report` shells out to `scripts/proxy-report.sh`. Supports `--session SESSION_ID` and `--all` (shows the latest closed session's header report, per E-052-04 semantics) flags. Passes exit code through. (No `--unreviewed` flag -- header reports are point-in-time snapshots, not aggregatable logs.)
- [ ] **AC-3**: `bb proxy endpoints` shells out to `scripts/proxy-endpoints.sh`. Supports `--session SESSION_ID`, `--all`, and `--unreviewed` flags. Passes exit code through.
- [ ] **AC-4**: `bb proxy refresh-headers` loads `scripts/proxy-refresh-headers.py` and calls its `run(apply=...)` function. Supports `--apply` flag (dry-run by default). Exit code matches the return value of `run()`. See Notes on hyphenated filename import.
- [ ] **AC-5**: `bb proxy review` shells out to `scripts/proxy-review.sh`. Passes exit code through.
- [ ] **AC-6**: `bb proxy --help` lists all four commands with descriptions.
- [ ] **AC-7**: Tests verify that the correct scripts are invoked with the correct arguments. For bash-wrapped commands (report, endpoints, review): mock `subprocess.run` and verify the command list and `cwd`. For refresh-headers: mock the loaded module's `run` function and verify the `apply` kwarg. See Notes on hyphenated filename import for mock target guidance.
- [ ] **AC-8**: All existing tests continue to pass.

## Technical Approach
1. Create `src/cli/proxy.py` with a `typer.Typer(help="Proxy analysis commands.")` sub-app.
2. `report` command forwards `--session` and `--all` flags (no `--unreviewed`). `endpoints` command forwards `--session`, `--all`, and `--unreviewed` flags.
3. `refresh-headers` loads `scripts/proxy-refresh-headers.py` and calls its `run(apply=...)` function directly. This is a Python script with an importable `run(*, apply: bool) -> int` function -- no need to shell out. **Do NOT use subprocess for this command.** See Notes on hyphenated filename import for loading approach.
4. `review` invokes `scripts/proxy-review.sh` via `subprocess.run()`.
5. Script paths should be resolved relative to the project root (use `pathlib.Path(__file__).resolve().parent.parent.parent` or similar to find the project root). **All `subprocess.run()` calls MUST pass `cwd=PROJECT_ROOT`** because the bash scripts use relative paths internally (e.g., `proxy-report.sh` reads `proxy/data/header-report.json`). Without `cwd`, these scripts break when the operator runs `bb` from any directory other than the project root.
6. The sub-app mount in `src/cli/__init__.py` is pre-wired by E-055-01 -- do not modify `__init__.py`.

Reference files:
- `/workspaces/baseball-crawl/scripts/proxy-report.sh` (post-E-052: `--session`, `--all`)
- `/workspaces/baseball-crawl/scripts/proxy-endpoints.sh` (post-E-052: `--session`, `--all`, `--unreviewed`)
- `/workspaces/baseball-crawl/scripts/proxy-refresh-headers.py` (created by E-054: importable `run(*, apply: bool) -> int`)
- `/workspaces/baseball-crawl/scripts/proxy-review.sh` (created by E-052: marks sessions as reviewed)

## Dependencies
- **Blocked by**: E-055-01
- **Blocks**: None

## Files to Create or Modify
- `src/cli/proxy.py` (overwrite stub from E-055-01 with full implementation)
- `tests/test_cli_proxy.py` (create)

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests

## Notes
- Proxy lifecycle commands (start/stop/status/logs) are intentionally excluded. The proxy runs on the Mac host via Docker Compose; the `bb` CLI runs inside the devcontainer. These commands cannot work across the container boundary. See CLAUDE.md "Proxy Boundary (Host vs. Container)" for details.
- `bb proxy report` defaults to showing the latest session (no flags needed). `--all` shows the latest closed session's header report (per E-052-04 semantics). `--session` targets a specific session by ID. No `--unreviewed` flag (header reports are point-in-time snapshots).
- `bb proxy endpoints` defaults to showing the latest session. `--all` aggregates across sessions. `--unreviewed` filters to unreviewed sessions only. `--session` targets a specific session by ID.
- `bb proxy refresh-headers` is dry-run by default (shows what would change). The `--apply` flag writes the changes to `src/http/headers.py`.
- `proxy-refresh-headers.py` is a Python script with `run(*, apply: bool) -> int`. Unlike the bash scripts (report, endpoints, review), this one is imported directly, not shelled out to. The test for this command should mock the imported `run` function rather than `subprocess.run`.
- **Hyphenated filename import**: `proxy-refresh-headers.py` contains a hyphen, so `import scripts.proxy_refresh_headers` will fail (Python treats hyphens as minus operators in module names). Two options: (1) **Recommended**: use `importlib.util.spec_from_file_location()` to load the module by file path -- this avoids renaming and preserves the existing standalone CLI invocation (`python scripts/proxy-refresh-headers.py`). (2) Rename the file to `proxy_refresh_headers.py` -- simpler import but changes the standalone invocation. Option 1 is lower risk. The test mock target must match whichever approach is chosen (mock the loaded module's `run`, not a dotted import path).
