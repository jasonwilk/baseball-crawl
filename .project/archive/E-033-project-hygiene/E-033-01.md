# E-033-01: Update CLAUDE.md stack description to match implemented reality

## Epic
[E-033: Project Hygiene -- Align Docs and Tests with Current Reality](epic.md)

## Status
`DONE`

## Description
After this story is complete, the `CLAUDE.md` file accurately describes the implemented technology stack (FastAPI + Jinja2 + SQLite + Docker Compose + Cloudflare Tunnel/Access). The four sections that still reference the old Cloudflare D1 / Workers / TypeScript plan are corrected. No other sections of CLAUDE.md are modified.

## Context
The E-009 tech stack decision (2026-02-28) chose Option B: Docker + Cloudflare Access. The implementation stories E-009-02 through E-009-06 are all DONE. The running stack is FastAPI + Jinja2 + SQLite (WAL mode, host-mounted at `./data/app.db`) + Docker Compose + Cloudflare Tunnel + Zero Trust Access, deployed on a home Linux server.

However, CLAUDE.md still describes the pre-E-009 plan in four sections. Every agent that loads CLAUDE.md (which is every session) gets a wrong mental model. This is the single highest-impact fix in E-033 because CLAUDE.md is ambient context for all agents.

The E-009-08 story (still TODO, blocked on E-009-07) covers the Commands section update and E-004 epic update. This story deliberately does NOT touch those -- it handles only the sections that can be corrected now based on the already-implemented stack.

## Acceptance Criteria
- [ ] **AC-1**: The `### Deployment Target` section (currently line 33) is updated. It no longer mentions "Cloudflare D1", "Workers (API/ETL)", "Pages", or "KV/R2". It describes: Docker Compose on a home Linux server, SQLite (host-mounted), FastAPI serving layer, Cloudflare Tunnel + Zero Trust Access for network/auth.
- [ ] **AC-2**: The `## Tech Stack` section (currently lines 46-49) is updated. The three Cloudflare-specific bullets are replaced. The section describes: Python end-to-end, FastAPI + Jinja2 for the serving layer, SQLite (WAL mode, host-mounted Docker volume) for storage, Docker Compose for local dev and production, Cloudflare Tunnel + Zero Trust Access for network ingress and authentication. The Python version bullet (line 46, referencing `.python-version`) is preserved unchanged.
- [ ] **AC-3**: The `## Architecture` section, line 139 (`Credential management: environment variables for local dev, Cloudflare secrets for production`) is updated to: `Credential management: environment variables via .env files (local dev and production; Docker Compose reads .env automatically)`.
- [ ] **AC-4**: The `## Security Rules` section, line 144 (`Use Cloudflare secrets/environment variables for production`) is updated to: `Use environment variables via .env files for production (Docker Compose reads .env; files are git-ignored)`.
- [ ] **AC-5**: No other sections of CLAUDE.md are modified. Specifically: the Commands section, App Troubleshooting section, Python Version Policy section, HTTP Request Discipline section, Testing section, Project Management section, Agent Ecosystem section, Workflow section, Git Conventions section, and Statusline section are untouched.
- [ ] **AC-6**: After the changes, `grep -c "Cloudflare D1\|Cloudflare Workers\|TypeScript/JavaScript for Workers\|Cloudflare secrets" CLAUDE.md` returns 0.
- [ ] **AC-7**: The Tech Stack section does NOT reference Traefik (Traefik is an internal implementation detail, not a stack-level technology choice the user cares about in CLAUDE.md).

## Technical Approach
Read the current CLAUDE.md. Identify the four sections listed in the ACs. Replace the stale text with corrected text. Verify no other sections were modified by diffing before and after.

Reference for the correct stack description: `/.project/decisions/E-009-decision.md` (the "Chosen Stack" table and "Why Option B Won" section).

Key facts for the replacement text:
- Database: SQLite in a Docker volume, host-mounted at `./data/app.db`, WAL mode
- Serving layer: FastAPI (Python) + Jinja2 templates, server-rendered HTML
- Deployment: Docker Compose (`docker-compose.yml` is the same for local and production)
- Production hosting: Home Linux server (no VPS, no hosting cost)
- Network: Cloudflare Tunnel (outbound-only, no exposed ports) + Zero Trust Access for auth
- Credentials: `.env` files (git-ignored), Docker Compose reads `.env` automatically
- Language: Python end-to-end (crawlers, API, dashboard, migrations, tests)

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Files to Create or Modify
- `CLAUDE.md` -- edit four sections (Deployment Target, Tech Stack, Architecture credential line, Security Rules credential line)

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] CLAUDE.md diff shows changes only in the four targeted sections
- [ ] No regressions in existing tests (run `pytest` to confirm)

## Notes
- Do NOT add new sections or expand the scope. The goal is surgical correction of four specific stale references.
- Cloudflare is still part of the stack -- but as Tunnel + Zero Trust Access (network/auth layer), NOT as D1/Workers/Pages (compute/storage layer). References to Cloudflare Tunnel and Zero Trust Access are correct and should remain.
- The `## App Troubleshooting` section (lines 87-124) already describes the correct Docker Compose workflow. It does not need changes.
- The `## Commands` section (line 85) currently only has the pre-commit hook command. E-009-08 will add Docker Compose commands after the production runbook is done. Do not touch this section.
