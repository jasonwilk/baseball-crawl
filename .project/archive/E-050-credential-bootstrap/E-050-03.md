# E-050-03: Bootstrap pipeline script

## Epic
[E-050: Credential Validation and Crawl Bootstrap](epic.md)

## Status
`DONE`

## Description
After this story is complete, the operator can run `python scripts/bootstrap.py` to execute the full pipeline in one command: validate credentials, check team configuration, crawl all data, and load into the database. The script provides clear status output at each stage and exits early with actionable messages when a pre-flight check fails.

## Context
Today, the operator must run three separate commands in sequence (`check_credentials.py`, `crawl.py`, `load.py`) with no unified error handling between them. If credentials expire mid-crawl, the error is buried in crawler log output. The bootstrap script chains these steps with validation gates, providing a single entry point for the "capture credentials, then go" workflow.

## Acceptance Criteria
- [ ] **AC-1**: `scripts/bootstrap.py` exists and is executable via `python scripts/bootstrap.py`
- [ ] **AC-2**: The script runs four stages in order, printing a clear header for each:
  1. "Checking credentials..." -- calls `check_credentials()` from `scripts/check_credentials.py`
  2. "Checking team configuration..." -- validates teams are configured
  3. "Crawling data..." -- calls `crawl.run()`
  4. "Loading data..." -- calls `load.run()`
- [ ] **AC-3**: If credential check fails (exit code != 0), the script prints the credential check message and exits immediately without proceeding to crawl
- [ ] **AC-4**: Team configuration check calls `load_config()` from `src/gamechanger/config` and verifies that at least one team has an `id` that does NOT start with `REPLACE_WITH_`. If all teams have placeholder IDs, the script prints "No teams configured -- edit config/teams.yaml (or use the admin UI once available)" and exits with code 1. If `config/teams.yaml` does not exist, the script prints "Team config file not found: config/teams.yaml" and exits with code 1
- [ ] **AC-5**: If team config is valid, the script prints the number of configured teams and their names
- [ ] **AC-6**: The crawl stage calls `crawl.run()` directly (imported function, not subprocess). If it returns non-zero, the script prints a warning but continues to the load stage (partial crawl data is still loadable)
- [ ] **AC-7**: The load stage calls `load.run()` directly. If it returns non-zero, the script reports the error
- [ ] **AC-8**: After all stages, the script prints a summary: credentials (valid/expired), teams (count), crawl (success if `crawl.run()` returned 0, warning if returned 1), load (success if `load.run()` returned 0, failed if returned 1)
- [ ] **AC-9**: The script accepts `--check-only` flag that runs only the credential check and team config check, then exits (no crawl or load)
- [ ] **AC-10**: The script accepts `--profile web|mobile` flag (default: `web`) which is passed to `crawl.run(profile=profile)`. The `crawl.run()` function is modified (see Technical Approach step 5) to accept an optional `profile: str = "web"` parameter and pass it to `GameChangerClient(profile=profile)`. If E-050-02 is not yet merged, only `"web"` works (`"mobile"` raises `ValueError` from `create_session()` since the client ignores the profile param pre-E-050-02 -- this is acceptable).
- [ ] **AC-11**: The script accepts `--dry-run` flag that passes through to both `crawl.run(dry_run=True)` and `load.run(dry_run=True)`
- [ ] **AC-12**: The script NEVER prints or logs credential values
- [ ] **AC-13**: Tests in `tests/test_bootstrap.py` cover: successful full run (all stages pass), credential failure (early exit), team config failure -- placeholder IDs (early exit), team config failure -- missing YAML file (early exit), check-only mode, crawl failure with load continuing, and dry-run passthrough. All tests mock `check_credentials()`, `crawl.run()`, `load.run()`, and `load_config()` -- no real API calls or file I/O.

## Technical Approach
1. Create `scripts/bootstrap.py` following the same structure as `scripts/crawl.py`:
   - Project root on `sys.path`
   - `logging.basicConfig` for structured output
   - `_build_arg_parser()` for CLI flags
   - `run()` function with all logic (importable for testing)
   - `main()` entry point
2. Import `check_credentials` from `scripts.check_credentials` (AC-9 in E-050-01 ensures it is importable)
3. Import `run` from `scripts.crawl` and `scripts.load` (both already have importable `run()` functions)
4. Use `load_config()` from `src/gamechanger/config` for team config validation
5. For `--profile` passthrough: the simplest approach is to modify `scripts/crawl.py` to accept an optional `profile` parameter in its `run()` function, which gets passed to `GameChangerClient(profile=profile)`. This is a minor, backward-compatible change to `crawl.py`.
6. Status output uses `print()` for human-readable stage headers (not logging) -- these are operator-facing status messages, not log entries

## Dependencies
- **Blocked by**: E-050-01 (credential health check -- provides `check_credentials()`)
- **Blocks**: E-050-04 (operator guide references this script)
- **Soft dependency**: E-050-02 (profile-aware client -- `--profile mobile` only works after E-050-02)

## Files to Create or Modify
- `scripts/bootstrap.py` (create)
- `scripts/crawl.py` (modify -- add optional `profile` parameter to `run()`)
- `tests/test_bootstrap.py` (create)

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests

## Notes
- The `crawl.run()` modification is minimal: add `profile: str = "web"` parameter, pass to `GameChangerClient(profile=profile)` on line 170. This is backward compatible -- existing callers do not pass `profile` and get the default `"web"`.
- The script does NOT manage team configuration. It validates that teams are configured and tells the operator where to go if they are not.
- Future enhancement (after E-042-06): the team config check should also check the database for active teams when YAML has no valid entries. Out of scope for this story.
- Reference files for implementation: `scripts/crawl.py` (structure and `run()` signature at `/workspaces/baseball-crawl/scripts/crawl.py`), `scripts/load.py` (structure and `run()` signature at `/workspaces/baseball-crawl/scripts/load.py`), `src/gamechanger/config.py` (`load_config()` at `/workspaces/baseball-crawl/src/gamechanger/config.py`)
