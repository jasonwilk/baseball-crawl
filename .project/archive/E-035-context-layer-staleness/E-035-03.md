# E-035-03: Fix stale references in hooks, skills, and rules

## Epic
[E-035: Context Layer Staleness Fixes](epic.md)

## Status
`DONE`

## Description
After this story is complete, four stale references across hooks, skills, and rules will be corrected: the pii-check.sh comment will reflect that the scanner exists, the filesystem-context skill will have a valid path for E-010, the dispatch-pattern agent selection table will include docs-writer, and workflow-discipline.md will have its misleading `paths: ["**"]` cleaned up. This addresses review findings P2.1, P2.2, P2.3, and P2.4.

## Context
The context-layer review (`/.project/research/context-layer-review-2026-03-03.md`) identified four P2 (stale but not harmful) references:
- pii-check.sh line 27 says "Scanner not yet installed (E-019-03 pending)" but the scanner has existed since E-019 completed
- filesystem-context SKILL.md line 196 references `epics/E-010-...` but E-010 is archived at `/.project/archive/E-010-...`
- dispatch-pattern.md agent selection table has no row for docs-writer (gap G1)
- workflow-discipline.md uses `paths: ["**"]` which is effectively unconditional but looks like it was scoped intentionally (structural issue ST1)

## Acceptance Criteria
- [ ] **AC-1**: Given `.claude/hooks/pii-check.sh` line 27, the comment reads `# Scanner not found at expected path; allow commit (fail open)` or equivalent -- it no longer references "E-019-03 pending" or "not yet installed".
- [ ] **AC-2**: Given `.claude/skills/filesystem-context/SKILL.md` line 196, the E-010 reference either points to the correct archived path (`/.project/archive/E-010-intent-context-layer-implementation/epic.md`) or is removed entirely. If updated, add a note that E-010 is archived/abandoned.
- [ ] **AC-3**: Given `.claude/rules/dispatch-pattern.md` Agent Selection table (lines 90-95), a new row exists for documentation stories: `| Documentation (docs/admin/, docs/coaching/) | docs-writer |`
- [ ] **AC-4**: Given `.claude/rules/workflow-discipline.md`, the frontmatter either (a) has no `paths` field at all (making it unconditional, which is what `["**"]` already is), or (b) has a comment explaining that the global scope is intentional. Option (a) is preferred for clarity.
- [ ] **AC-5**: No other content in any of the four files is changed beyond what these ACs require.

## Technical Approach
1. Read `.claude/hooks/pii-check.sh` (full file, 48 lines).
2. On line 27, replace the comment. Keep the code behavior (fail open when scanner not found) -- only the comment changes.
3. Read `.claude/skills/filesystem-context/SKILL.md` (full file, 199 lines).
4. On line 196, update the E-010 path. The simplest fix is to update to the archived path and add "(archived)" after it. Alternatively, remove the bullet entirely since E-010's Technical Notes are historical and the actual skill files are the authoritative source.
5. Read `.claude/rules/dispatch-pattern.md` (full file, 109 lines).
6. In the Agent Selection table (after line 95), add: `| Documentation (docs/admin/, docs/coaching/) | docs-writer |`
7. Read `.claude/rules/workflow-discipline.md` (full file).
8. Remove the `paths: ["**"]` from the YAML frontmatter. The rule is intentionally unconditional -- the Work Authorization Gate and Dispatch Failure Protocol apply to all agents regardless of file path.

Reference: `/.project/research/context-layer-review-2026-03-03.md` sections P2.1-P2.4, G1, ST1.

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Files to Create or Modify
- `.claude/hooks/pii-check.sh` -- fix stale comment on line 27
- `.claude/skills/filesystem-context/SKILL.md` -- fix E-010 path reference on line 196
- `.claude/rules/dispatch-pattern.md` -- add docs-writer row to agent selection table
- `.claude/rules/workflow-discipline.md` -- remove misleading `paths: ["**"]` frontmatter

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] pii-check.sh still functions correctly (fail open when scanner not found)
- [ ] No unrelated content changes in any of the four files

## Notes
The pii-check.sh fix is comment-only -- do not change any code behavior. The scanner fallback path (fail open) is correct by design.

For workflow-discipline.md, removing the `paths` field entirely makes it unconditional. Rules without a `paths` field in their frontmatter load on every file touch, which is the intended behavior for this rule.
