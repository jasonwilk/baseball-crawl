# E-030-03: Update skills and agent memory

## Epic
[E-030: Remove Orchestrator Agent](epic.md)

## Status
`DONE`

## Description
After this story is complete, the multi-agent-patterns and filesystem-context skill files reflect the PM-as-entry-point routing model with no orchestrator references. The claude-architect's memory file lists 6 agents in its ecosystem topology. The routing chain diagrams show `User -> PM -> implementing agents` instead of `User -> orchestrator -> PM -> implementing agents`.

## Context
The multi-agent-patterns skill was specifically designed around the orchestrator -> PM -> implementing agent chain and its telephone game risks. With the orchestrator removed, the chain shortens to user -> PM -> implementing agent, which reduces the telephone game from two relay points to one. The skill content needs updating to reflect this simpler chain while preserving the valuable guidance about context packaging and verbatim relay.

## Acceptance Criteria
- [ ] **AC-1**: In `/workspaces/baseball-crawl/.claude/skills/multi-agent-patterns/SKILL.md`:
  - (a) The routing chain diagram shows `User -> product-manager -> implementing agents` with no orchestrator node
  - (b) Direct-routing exceptions (api-scout, baseball-coach, claude-architect) are still shown as direct from user
  - (c) The Telephone Game Problem section is updated to reflect a shorter chain (one relay point: PM -> implementing agent, not two)
  - (d) The "Orchestrator -> PM" relay description is removed or replaced with "User -> PM" context
  - (e) The PM Dispatch Checklist is preserved unchanged
  - (f) The "When to Shorten the Chain" section is updated (the chain is already shorter; direct-routing exceptions are still valid)
  - (g) The "Diagnosing Failures" section question about "Did the orchestrator pass the request verbatim, or paraphrase?" is removed or replaced with "Did the story accurately reflect the user's requirement?"
- [ ] **AC-2**: In `/workspaces/baseball-crawl/.claude/skills/multi-agent-patterns/SKILL.md`, no instance of the word "orchestrator" appears.
- [ ] **AC-3**: In `/workspaces/baseball-crawl/.claude/skills/filesystem-context/SKILL.md`, the Related Skills section:
  - (a) The reference to "orchestrator -> PM -> implementing agent chain" is updated to "user -> PM -> implementing agent chain"
  - (b) No other changes are needed (verify no other orchestrator references exist in the file)
- [ ] **AC-4**: In `/workspaces/baseball-crawl/.claude/agent-memory/claude-architect/MEMORY.md`, the "## Agent Ecosystem (Current)" section:
  - (a) Lists 6 agents (not 7)
  - (b) Does not include an orchestrator entry
  - (c) All other entries are preserved unchanged
- [ ] **AC-5**: In `/workspaces/baseball-crawl/.claude/agent-memory/claude-architect/MEMORY.md`, all other references to the orchestrator are updated or removed. This includes but is not limited to: "When to Create New Agents" note, "Key Architectural Decisions" notes about orchestrator, and any topic file index references.
- [ ] **AC-6**: All existing content in modified files (other than orchestrator references) is preserved -- no unrelated deletions or reformatting. In particular, the multi-agent-patterns skill's core guidance about verbatim relay, context packaging, and the PM Dispatch Checklist must be preserved.
- [ ] **AC-7**: Running `grep -ri orchestrator .claude/skills/multi-agent-patterns/SKILL.md .claude/skills/filesystem-context/SKILL.md .claude/agent-memory/claude-architect/MEMORY.md` returns zero results.

## Technical Approach
This is a claude-architect story. Use the Edit tool for surgical changes.

Files to read before making changes:
- `/workspaces/baseball-crawl/.claude/skills/multi-agent-patterns/SKILL.md` -- Full read; most orchestrator references are here
- `/workspaces/baseball-crawl/.claude/skills/filesystem-context/SKILL.md` -- Search for "orchestrator" (expected: 1 reference in Related Skills)
- `/workspaces/baseball-crawl/.claude/agent-memory/claude-architect/MEMORY.md` -- Search for "orchestrator" (expected: multiple in ecosystem section)

For multi-agent-patterns SKILL.md:
1. Update the routing chain diagram (remove orchestrator node)
2. Rewrite the Telephone Game section to reflect one relay point instead of two
3. Update "When to Shorten the Chain" -- the chain is already shorter now
4. Update "Diagnosing Failures" -- remove orchestrator paraphrase question
5. Preserve the PM Dispatch Checklist and verbatim relay guidance

For filesystem-context SKILL.md:
1. Update the single reference in Related Skills section

For claude-architect MEMORY.md:
1. Remove orchestrator from Agent Ecosystem list, update count
2. Update any other orchestrator references

After all edits, run `grep -ri orchestrator` on the modified files to verify zero results.

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Files to Create or Modify
- **Modify**: `.claude/skills/multi-agent-patterns/SKILL.md`
- **Modify**: `.claude/skills/filesystem-context/SKILL.md`
- **Modify**: `.claude/agent-memory/claude-architect/MEMORY.md`

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] No orchestrator references remain in any modified file
- [ ] Core skill guidance (verbatim relay, context packaging, PM checklist) is preserved
- [ ] No regressions in any modified file

## Notes
The multi-agent-patterns skill has the most orchestrator references of any file in the project. The key editorial judgment is how to handle the Telephone Game section: the concept is still valid (PM can still distort user intent when relaying to implementing agents), but the chain is shorter (one relay instead of two). Preserve the concept, update the examples.
