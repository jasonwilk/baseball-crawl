# E-145-02: Update auth-module rule for corrected fallback behavior

## Epic
[E-145: Fix Docker Credential Loading for Optional Env Vars](epic.md)

## Status
`DONE`

## Description
After this story is complete, `.claude/rules/auth-module.md` will accurately describe the corrected credential loading behavior -- that `_load_credentials()` falls back to `os.environ` for **all** keys (not just required ones) for Docker container compatibility.

## Context
CA consultation identified that auth-module.md line 36 currently says "falls back to `os.environ` only for Docker container compatibility" without clarifying that the fallback applies to all keys. After E-145-01 fixes the code, this rule file needs a one-line update to match the corrected behavior. No CLAUDE.md change needed.

## Acceptance Criteria
- [ ] **AC-1**: `.claude/rules/auth-module.md` line 36 (or its equivalent after E-145-01) accurately states that the env-var fallback covers all credential keys, not just required ones.
- [ ] **AC-2**: No other changes to the file beyond this clarification.

## Technical Approach
One-line edit to `.claude/rules/auth-module.md` in the "Environment Variable Access" section. The current text says the fallback is "only for Docker container compatibility" -- update to clarify it covers all keys. See epic Technical Notes "Context-Layer Update" section.

## Dependencies
- **Blocked by**: E-145-01 (code fix must land first so the rule describes the actual behavior)
- **Blocks**: None

## Files to Create or Modify
- `.claude/rules/auth-module.md` -- update line 36 description

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Code follows project style (see CLAUDE.md)

## Notes
- This is a one-line context-layer update. Routed to claude-architect per routing precedence (context-layer path).
