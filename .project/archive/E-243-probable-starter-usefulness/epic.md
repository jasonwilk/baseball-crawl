# E-243: Make the Probable-Starter Analysis Useful on Game Morning

## Status
`COMPLETED`
<!-- Lifecycle: DRAFT → READY → ACTIVE → COMPLETED (or BLOCKED / ABANDONED) -->
<!-- PM sets READY explicitly after: expert consultation done, all stories have testable ACs, quality checklist passed. -->
<!-- Only READY and ACTIVE epics can be dispatched. -->
<!-- READY (re-set) 2026-06-28 after the full review trail: Phase-3 internal + Codex spec review iterations 1 AND 2 + the narrative bake-off. Codex iter-2 finding #3 resolved at the rule level (agent-routing.md own-memory exception generalized, user-authorized). Quality checklist passed; zero open holds. Dispatch authorization is a separate user call. -->

## Review Scorecard
| Review pass | Findings | Accepted | Dismissed | Outcome |
|-------------|----------|----------|-----------|---------|
| Internal iteration 1 — CR spec audit + holistic team (baseball-coach, data-engineer, software-engineer, ux-designer) | ~22 | ~22 | 0 | All refinements incorporated, no design-level changes. 2 items held for rulings then resolved: M1 null-pitch (→ keep IP proxy) and the Story-06 dispatch-worktree disposition (→ split out per user). |
| Codex spec review — iteration 1 | 7 | 7 | 0 | All P1/P2 spec-quality (one-candidate path, card-spec file path, 05 dependency, testable jargon rule, test-file inventory, routing carve-out, TN-3↔TN-5 drift). Incorporated. |
| Narrative bake-off — empirical validation (user-authorized, pre-READY) | 13-model bake-off + A/B tuning round | — | — | Selected `google/gemini-2.5-flash-lite` + the validated Variant A prompt (16/16, judge + baseball-coach human review); Variant B (rotation-rationale + committee-honesty) tested and REJECTED. Folded into E-243-04 + TN-6. |
| Codex spec review — iteration 2 (re-run on final five-story artifacts incl. the bake-off changes) | 6 | 6 | 0 | All accepted: Variant-A prompt persisted to a file + corrected score claim (04 AC-2); `.env` reframed as optional override, tracked code default ships the model (04 AC-7); epic body normalized to the one-candidate case (Success Criteria/Goals/03 Description vs AC-1b); banner string canonicalized to "rest and availability"; 05 AC-1 reworded to the actual stale text. Routing carve-out (#3) RESOLVED at the rule level — claude-architect generalized the own-agent-memory exception in `agent-routing.md` (user-authorized); Story 05 routing now rests on the codified rule. |

Cumulative: 35 review findings raised, 35 accepted, 0 dismissed, plus one empirical validation pass. Finding counts reconstructed from the triage summaries.

## Overview
The opponent scouting report's probable-starting-pitcher section is currently unhelpful: it opens with "this is a true committee situation" and sometimes elevates a tired arm over a fresh one. This epic reframes the section from a fragile "name one starter" prediction into an honest ranked list of the most likely arms (with rest and eligibility), corrects the ranking so fresh arms outrank tired ones, and stops travel-ball opponents from getting blank cards. The engine's ranking is already sound; the fixes are mostly presentation, one deterministic re-rank, and a league-coverage gap.

## Background & Context
The probable-starter feature shipped in E-212 (card) and E-214 (rest-days fix, behind the `FEATURE_PREDICTED_STARTER` flag). A backtest of the **actual** engine (`src/reports/starter_prediction.py`) was run against 17 real opponent seasons (357 labeled games) loaded into the dev DB. Findings (validated against real data, not hypothesized):

- **The engine's rotation-sequence ranking is sound** — it beats naive baselines 2-3x (engine top-1 20.2% / top-2 40.1%, vs "most-frequent" 7.6% and "most-rested" 15.7%). "Committee" is *structurally true* at this level: pitch-count caps make a dominant ace physically impossible (no team's top starter exceeds ~31% of starts). The model is not broken; the presentation is.
- **Defect 1 (presentation, the core fix).** The engine hides its ranked candidate list behind an all-or-nothing confidence gate. It names a single pick only 3.6% of games and is wrong 85% of those. The deterministic ranked top-2/3 is already computed (`StarterPrediction.top_candidates`) but only surfaced in the "low" tier. Surfacing it always — reframed as "most likely arms" — yields an honest ~40% top-2 and matches how a coach thinks ("it'll be one of these two").
- **Defect 2 (ranking, validated win).** The engine ranks a tired arm #1 in 22% of games and is only 8.9% right there — the user's exact complaint. A **hard** rest-discount tiebreaker (demote any candidate inside its preferred-rest window below any fully-available starter) moves aggregate top-1 20.2% → 24.9% and the tired-arm subset 8.9% → 30.4% (22 wins vs 5 breaks, ~4.4:1). Soft/graduated penalties were too timid and did nothing — the hard variant is required. This is a pure deterministic re-rank on the existing engine, not a new model.
- **Defect 3 (league/level gap).** 7 of 17 real opponents got no prediction at all — their team name resolved to youth/travel (`\d+U`), which has no rest-rules table, so the engine suppresses the whole card.
- **Accuracy lever.** The real lever for higher accuracy is scouting *coverage* (fuller opponent game history), not a fancier algorithm — backtest accuracy is deflated by incomplete game history in our DB.

Reports are the sole coaching surface (CLAUDE.md L21 / `docs/ROADMAP.md`). This epic is NOT a ROADMAP A-E slice, so no §0 Roadmap-Tracking entry is required.

**Backtest artifacts** (read-only, throwaway spikes that import the real engine): `.project/research/starter_backtest.py` (engine-vs-baselines), `.project/research/starter_backtest_rerank.py` (the hard/soft re-rank comparison). These are the planning-time validation evidence — they are NOT CI regression gates (see Technical Notes TN-2).

## Goals
- Surface the deterministic ranked "most likely arms" (up to 3 — as many genuine candidates as exist) on every report with sufficient data, each with start-share, days rest, and rest-eligibility — replacing the all-or-nothing "name one or go blank" presentation.
- Reframe the section's wording away from a single "predicted starter" toward "most likely arms / likely rotation slot," eliminating the "true committee situation" hedge as the opening line.
- Add a hard rest-discount tiebreaker so a fully-available starter always outranks a still-tired (preferred-rest-window) one.
- Stop youth/travel opponents from getting blank cards.
- Re-point the Tier-2 LLM narration at the ranked arms and their rest reasoning, and remove the prompt guideline that tells it not to predict.
- Record the corrected model (ranked top-2/3 output, hard discount) in the baseball-coach domain memory.

## Non-Goals
- Replacing or rewriting the rotation-sequence ranking model. The ranking is validated as sound; only a deterministic re-rank tiebreaker and presentation change.
- A true innings-based / outs-based USSSA or Perfect Game rules engine (structural extension per `.claude/rules/pitch-rules.md`). Youth/travel gets a labeled pitch-count *estimate*, not a new rule unit.
- Fuller opponent game-history coverage / ingestion. The scout-coverage analysis was split out of this epic (per user decision, 2026-06-28) and produced as a standalone research memo at `.project/research/scout-coverage-lever.md`; any ingestion build is separate follow-on work.
- Changing the `FEATURE_PREDICTED_STARTER` flag default or any production config (operator decision — see Open Questions).
- Any change to non-pitching report sections (batting, spray, recent form, trust footer).

## Success Criteria
- A report for an opponent with ≥4 games of pitching data renders a ranked list of the likely arms — up to 3, as many genuine candidates as exist (the one-candidate case renders that single arm plus the unavailable block; never the old single-name confidence gate, never a fabricated phantom arm, never a blank/committee-only card) — each line showing start-share, days rest, and rest-eligibility, with no "true committee situation" hedge as the leading text.
- Given a candidate set where the highest-ranked arm is inside its preferred-rest window and a lower-ranked starter is fully available, the fully-available starter is ranked first (per Technical Notes TN-1).
- A report for a youth/travel-named opponent (e.g., "GI Home Federal 18U") with sufficient data renders a prediction labeled as an estimate, instead of a suppressed/blank card.
- The Tier-2 LLM system prompt contains no instruction to withhold a prediction at low/committee confidence, and its narration describes the ranked arms.
- The baseball-coach `probable-starter-model.md` records the ranked top-2/3 output shape (not "one name at top, always") and the hard discount tiebreaker.
- Full test suite green.

## Stories
| ID | Title | Status | Dependencies | Assignee |
|----|-------|--------|-------------|----------|
| E-243-01 | Hard rest-discount re-rank tiebreaker | DONE | None | - |
| E-243-02 | Stop blank cards for youth/travel opponents | DONE | E-243-01 | - |
| E-243-03 | Surface ranked "most likely arms" + reframe wording | DONE | E-243-02 | - |
| E-243-04 | Re-point Tier-2 LLM narration; drop the no-predict guideline | DONE | E-243-03 | - |
| E-243-05 | Correct the baseball-coach probable-starter model doc | DONE | E-243-01 | - |

## Dispatch Team
- software-engineer
- ux-designer
- baseball-coach

## Technical Notes

### TN-1: The hard rest-discount tiebreaker (E-243-01)
The re-rank runs **in place** on the engine's already-ranked, already-**truncated** candidate list (the top-N the engine surfaces) — applied *before* the confidence-tier block, so the named `predicted_starter`/`alternative` and the Tier-2 LLM both consume the re-ranked order. It partitions those candidates into two groups by rest state, then orders all FULLY-AVAILABLE candidates ahead of all DISCOUNTED candidates, preserving the engine's existing relative order *within* each group (a stable partition). Partitioning **post-truncation** (not over the full pre-truncation pool) is what reproduces the validated +17-net numbers — pulling a fresh arm in from below the cut would diverge from `rerank_hard`. Each candidate dict additionally carries its attached rest-state (`days_rest`, `last_outing_pitches`, `rest_eligibility` = available/discounted) for E-243-03 to consume. Rest states (coach model, on top of the legal minimum the engine already enforces via `_is_excluded`):
- **DISCOUNTED** = cleared the hard rest gate but `days_rest < preferred`, where preferred rest by most-recent-day pitch count is: ≤30 pitches → 2 days, 31-60 → 4 days, 61+ → 5 days.
- **FULLY-AVAILABLE** = `days_rest >= preferred`.
- Pitch count unavailable on the most recent day → apply the **IP proxy** (≤2 IP → 0-30 bucket, 3-4 IP → 31-60, 5+ IP → 61+), flag as estimate, and treat as DISCOUNTED if the proxied bucket maps to a non-zero preferred-rest requirement (baseball-coach M1 ruling — conservative-when-uncertain; NOT treated as fully-available). This intentionally diverges from the throwaway backtest harness's `rest_state` (which treated null as available).
- Doubleheader aggregation (sum of pitches on the most recent day) matches the engine's existing exclusion aggregation in `_is_excluded`.

This re-rank operates only on candidates that already cleared the hard exclusion gate. It does not change which arms are excluded, only the order of those that remain. The partition semantics are exactly the `rerank_hard` function in `.project/research/starter_backtest_rerank.py`.

### TN-2: Regression verification is a unit test, not the backtest harness
The validated improvement (aggregate top-1 → ~24.9%, tired-arm subset → ~30.4%) was measured by applying `rerank_hard` *on top of* the engine's output in the throwaway harness. Once the re-rank lives inside the engine, the harness's "engine" baseline already reflects it and the harness can no longer cleanly isolate the delta. Therefore the regression gate for E-243-01 is a **deterministic unit test** asserting the partition behavior on a constructed candidate set — not a re-run of the harness. The harness remains valuable as planning-time provenance and an optional manual eyeball; it is not wired into CI and is not modified by this epic.

### TN-3: Two-tier availability — the engine currently implements only the hard gate
`starter_prediction.py` today computes only UNAVAILABLE (hard exclusion via `_is_excluded`). The "AVAILABLE BUT DISCOUNTED" tier described in the baseball-coach model is NOT yet in code — E-243-01 introduces the preferred-rest-window computation. E-243-03's per-candidate "rest-eligibility" display is **two-valued (available / discounted)** and consumes that same computation; hard-excluded (UNAVAILABLE) arms are not in `top_candidates` and surface in their own `unavailable_arms` sub-block, NOT as a per-line eligibility value (consistent with TN-5 and E-243-03 AC-2/AC-3). This is why E-243-03 depends on E-243-01.

### TN-4: Youth/travel fallback is a labeled pitch-count estimate, not a new rule unit (E-243-02)
`get_rules_for_league()` returns `None` for `youth_travel` (and `usssa` / `perfect_game` / `unknown`), which drives the `confidence="suppress"` blank-card path. The validated, simple fix is to give `youth_travel` a pitch-count rule set as a *labeled estimate* — `league-pitch-rules.md` and `.claude/rules/pitch-rules.md` both designate the USA Baseball Pitch Smart 15-18 curve as the soft prior for unknown leagues (the same tiers already encoded as `LEGION`). E-243-02 holds these tiers in a **distinct named constant** (`PITCH_SMART_15_18`), not a literal reuse of `LEGION`, so a future Legion-only change cannot silently move the youth/travel estimate; the prediction carries an `is_estimate` flag, and the card and LLM mark it as an estimate so a coach knows it is not a binding league rule. (Domain caveat: the curve is the 17-18 sub-bracket and youth teams skew younger, so it tends to under-rest them — which is exactly why the estimate label is load-bearing.) True innings-based USSSA rules are out of scope (Non-Goals). Scope was ratified in Phase 3 (coach + DE): `youth_travel` falls back to the estimate; `unknown` (no level signal at all) stays suppressed. Coded in E-243-02 AC-4.

### TN-5: Presentation reframe (E-243-03)
The engine already computes `top_candidates` (ranked, with `likelihood`, `games_started`, `reasoning`, `recent_starts`). E-243-03 (a) enriches each candidate with the fields the card needs to display — `start_share_pct` (`round(games_started / total_team_games * 100)`, displayed grounded in games per CLAUDE.md Data Philosophy, e.g. "8 of 30 starts (27%)"), days rest, two-valued `rest_eligibility` (available/discounted, from TN-1's attached rest-state), and optional handedness (`throws` passed through, shown when present); (b) adds a new additive engine output field `unavailable_arms: list[{name, reason}]` surfacing the engine's existing hard-exclusion reasons (hard-excluded arms are absent from `top_candidates`, so "unavailable" is its own sub-block, NOT a per-line eligibility value — this is the UXD-FLAG-1 fix); and (c) replaces the confidence-tier-driven card branches in `scouting_report.html` so the ranked list always renders for the non-suppressed data states, with wording reframed from "Predicted Starter" to "Most Likely Arms". The engine-surface change (a/b) is owned by E-243-03, not E-243-01.

**Estimate treatment (resolved this cycle — coach + UXD; heading per UXD Option A).** When `is_estimate` is True (youth/travel, from E-243-02): the heading stays "Most Likely Arms" (unchanged across estimate and non-estimate — UXD owns this copy decision), and the estimate signal is carried entirely by an amber "Estimated rest" badge and a one-line plain-English banner ("This level doesn't publish pitch-count rules, so rest and availability use a standard youth pitch-count guide. Treat as a directional read, not a hard rule."). The no-jargon rule is **absolute, no carve-out**: "Pitch Smart"/"Legion"/"USA Baseball"/"soft prior" NEVER appear in the rendered report (card OR LLM narrative) — the brand name is right for the code constant (`PITCH_SMART_15_18`) and the coach-model doc, wrong for the coach's eyes (UXD E-178 consequence-oriented-label principle). The badge appears ONLY on estimates (absence = full confidence); the word is "estimate", not "uncertain".

**No per-arm estimate marker (UXD ruling).** M1's IP proxy creates a finer-grained, per-arm estimate condition (a null-pitch arm that was IP-proxied), but the card does NOT add a per-line estimate marker on the rest chip. The section-level "Estimated rest" badge + banner contextually cover every arm in a youth/travel section (where the null path is live); the only residual is an IP-proxied arm in a non-estimate (e.g. NSAA-varsity) section, which is a rare edge since NSAA varsity mandates pitch-count tracking — and even then the IP proxy still discounts the arm correctly, so the gap is cosmetic (a missing visual cue), not a ranking error. "Simple first" — SE must NOT invent a per-arm estimate chip variant. Captured as a future-refinement idea (IDEA-083). The `suppress` state renders the spec's copy ("Not enough games yet to project likely arms — rest data still accumulating"). The **ux-designer card spec** (wireframe + field hierarchy + CSS in the report pt-unit idiom + suppress-state copy), persisted at `epics/E-243-probable-starter-usefulness/E-243-03-card-spec.md`, is the concrete handoff artifact the E-243-03 implementer consumes. Display follows `.claude/rules/display-philosophy.md`: contextualize with badges, never suppress a qualifying arm.

### TN-6: LLM reframe (E-243-04) — validated by the narrative bake-off
The Tier-2 system prompt in `src/reports/llm_analysis.py` (`_SYSTEM_PROMPT_TEMPLATE`) currently instructs: "At LOW/COMMITTEE confidence: explain the ambiguity honestly. Do not manufacture a prediction." That last guideline is the prompt-level cause of hedge-heavy "committee" narration and is removed. A pre-READY narrative bake-off (~12 models scored by a GPT-5.1 LLM judge + baseball-coach human review over 5 scenarios; artifacts in `.project/research/narrative-bakeoff/`) selected the concrete spec:

- **Prompt**: the validated **"Variant A"** prompt (verbatim source of truth in `.project/research/narrative-bakeoff/`, A/B evidence in `ab_report.md`) — leads with the named most-likely arm + concrete rest reason, names unavailable arms, plain English, no hedge.
- **Model**: `google/gemini-2.5-flash-lite` at **temperature 0.0** (won on quality, cost, latency; validated by judge AND human review). Wiring (SE-verified, zero blast radius — reports Tier-2 is the only production caller of `query_openrouter`/`_DEFAULT_MODEL`): make gemini the TRUE code default (`_DEFAULT_MODEL` at `openrouter.py:26`) while preserving the `OPENROUTER_MODEL` operator override (do NOT hardcode `model=` at the call site); fix the same-file stale docstring (`openrouter.py:11-12`), `.env.example:247-248`, and the stale default assertion in `tests/test_openrouter.py:~98-109` (all haiku→gemini) in the same change. Temperature is NOT env-controlled — change the hardcoded initial call `_invoke(0.3)` at `llm_analysis.py:282` to `_invoke(0.0)` (retry at `:287` already `0.0`). Lowest supported value if OpenRouter enforces a floor. **Closure context-layer item (claude-architect, NOT the E-243-04 implementer):** `.claude/rules/architecture-subsystems.md:92` records the canonical default model as `anthropic/claude-haiku-4.5` and goes stale with this change — update it to gemini at closure (flag in the context-layer assessment).
- **Data block**: **do NOT introduce a decimal IP field** — surface pitch count (the rest-relevant datum); decimal IP produced "6.3-inning" prose with no coaching payoff. SE-verified there is no decimal-IP field in the current production `_format_pitcher_table` (it lived only in the bake-off block), so this is a guard against adding it, not a removal. The existing integer "IP Outs" game-log column (`llm_analysis.py:131`/`:136`) is OUT OF SCOPE. LLM data block only — the card's rest table / game log are unchanged.

**REJECTED — do NOT re-attempt (recorded so it isn't re-tried):** an enhanced **"Variant B"** (rotation-slot rationale field + committee-honesty instruction) was tested and rejected — it regressed on 2 of 3 models; its rotation-rationale lever cannot fire because every real opponent classifies as "committee" (no differentiated rotation roles — consistent with the structural-committee finding), and its committee-honesty wording backfired, causing models to reintroduce the banned word "committee." Rotation-rationale is a future enhancement ONLY IF the engine is later changed to emit differentiated per-arm rotation roles AND the committee wording is reworked — out of scope for E-243.

Tier-2 remains optional and non-fatal (absence of `OPENROUTER_API_KEY` = silent skip); the deterministic Tier-1 card is unchanged by this story.

### TN-7: Shared file ordering
E-243-01, -02, and -03 all modify `src/reports/starter_prediction.py`. Because dispatch is serial and these stories build on each other (TN-3), they are ordered 01 → 02 → 03 via explicit dependencies. E-243-04 depends on -03's enriched output shape. E-243-05 (coach memory) touches a disjoint file but is **blocked by E-243-01** so the model doc records the IP-proxy/threshold contract as actually implemented (not a file conflict — an as-built consistency dependency; Codex finding #3). (The former scout-coverage story was split out of this epic — see Open Questions / History.)

### TN-8: Feature flag context
The entire section is gated behind `FEATURE_PREDICTED_STARTER` (`is_predicted_starter_enabled()`), a temporary E-214 verification flag (`.env.example` shows `=1`; `docs/admin/operations.md` documents default off). Whether to promote it to default-on once these fixes land is an operator decision (Open Questions) — this epic does not change the flag.

## Open Questions
- ~~**Youth/travel fallback scope**~~ — RESOLVED (Phase 3): coach + DE ratified — `youth_travel` falls back to the labeled estimate; `unknown` stays suppressed. Coded in E-243-02 AC-4.
- ~~**Estimate-label wording**~~ — RESOLVED (Phase 3): coach + UXD answered; see TN-5 "Estimate treatment". Folded into E-243-03/-04 ACs.
- ~~**M1 (null pitch count)**~~ — RESOLVED (baseball-coach): keep the IP proxy (option b). Null is NOT fully-available — apply the IP proxy (≤2 IP → 0-30, 3-4 IP → 31-60, 5+ IP → 61+), DISCOUNTED if the bucket maps to non-zero preferred rest. The model doc's L87 IP-proxy stays correct. Encoded in E-243-01 AC-4/AC-8, E-243-05 AC-6, and TN-1.
- ~~**E-243-06 disposition**~~ — RESOLVED (user, 2026-06-28): split out of the epic. The story read the dev DB / backtest harness, which the dispatch worktree lacks (no `data/`), so it is not a dispatchable story. data-engineer is producing it as a standalone read-only memo at `.project/research/scout-coverage-lever.md` against the main checkout. E-243 is now a five-story epic (01–05).
- **Feature flag** (operator, not blocking): promote `FEATURE_PREDICTED_STARTER` to default-on after these fixes verify in production?

## History
- 2026-06-27: Created (DRAFT). Design pre-validated by the team-lead against 17 real opponent seasons; this epic formalizes it into stories.
- 2026-06-27: Phase 3 internal review incorporated (CR spec audit + baseball-coach + data-engineer + software-engineer + ux-designer). No design-level changes — refinements only. Key accepted findings: re-rank in-place/post-truncation with attached per-candidate rest-state (01); `is_estimate` field + distinct `PITCH_SMART_15_18` constant + <4-games-suppress test (02); new `unavailable_arms` engine field + games-grounded start-share + handedness + resolved estimate treatment (03); estimate-narration wording (04); full output-shape section replacement + bad-example wording fix + routing carve-out note (05); metric tightened to internal proxies (06). Two Open Questions resolved (youth/travel scope; estimate wording). Held pending rulings: M1 (null-pitch) and E-243-06 run-location/disposition. Still DRAFT — not READY.
- 2026-06-27: M1 resolved (baseball-coach): keep the IP proxy — null pitch count is NOT fully-available; apply the IP proxy and treat as DISCOUNTED if the bucket maps to non-zero preferred rest. Model-doc L87 stays. Encoded in E-243-01 AC-4/AC-8, E-243-05 AC-6, TN-1. Also confirmed: distinct `PITCH_SMART_15_18` constant (Legion independently versioned) and the directional-bias estimate label is load-bearing (must not be softened to boilerplate). Sole remaining open item: E-243-06 dispatch-worktree disposition (CR-F1), which UXD is taking to the user. Still DRAFT — not READY.
- 2026-06-27: UXD copy fix accepted (their domain — Story 03 line 13). Removed the self-contradictory "Pitch Smart" from the estimate heading (Option A): heading stays "Most Likely Arms" unchanged; the estimate signal is the amber badge + plain-English banner. The no-jargon rule is now absolute (no heading carve-out) and extends to the LLM narrative (E-243-04 AC-3) — "Pitch Smart"/"Legion"/"USA Baseball"/"soft prior" never render in the report. Updated E-243-03 AC-5, E-243-04 AC-3, TN-5. Brand name retained only for the code constant + coach-model doc + internal prompt context. Still DRAFT — not READY.
- 2026-06-28: Scout-coverage analysis (former E-243-06) SPLIT OUT of the epic per user decision. It is a read-only analysis requiring the dev DB, which the dispatch worktree lacks — hence not a dispatchable story. data-engineer is producing it as a standalone research memo at `.project/research/scout-coverage-lever.md` against the main checkout; the headline finding (coverage, not algorithm, is the accuracy lever) is preserved there. E-243 is now a FIVE-story epic (01–05) with no open holds. Removed E-243-06 from the Stories table, Goals, Non-Goals, Success Criteria, TN-7, and Open Questions; the story file is tombstoned (REMOVED) for main to `git rm` at the planning commit. Still DRAFT — not READY (awaiting Codex spec review routing).
- 2026-06-28: Codex spec review (Phase 4) incorporated — 7 findings, all ACCEPTED, no design-level changes. (1) E-243-03 AC-1 reworded to "up to 3, as many genuine candidates as exist, no phantom arms" + new AC-1b for the one-candidate path (`len==1`) + AC-8 extended to test it; (2) ux-designer card spec now references the on-disk path `E-243-03-card-spec.md` (Files/Dependencies/Technical Approach/Notes) — UXD to persist it; (3) E-243-05 now `Blocked by: E-243-01` so the doc records the as-built IP-proxy contract (option a); (4) E-243-04 AC-6 added — prompt-construction unit test + REQUIRED manual no-brand verification (the jargon rule is now verifiable, not eyeball-only); (5) E-243-02 Files + AC-6/AC-7 corrected to `tests/test_league_detection.py` (where the youth_travel suppress assertions actually live); (6) E-243-05 routing carve-out made airtight (user-directed, recorded as a dispatch decision, claude-architect as fallback author); (7) TN-3 normalized to two-valued eligibility to match TN-5. Full consistency sweep incl TN-by-TN cross-check run. Still DRAFT — not READY.
- 2026-06-28: Narrative bake-off outcome incorporated into E-243-04 + TN-6 (the pre-READY experiment the user authorized — "the narrative is the one thing we didn't validate"). Final spec: model `google/gemini-2.5-flash-lite` @ temp 0.0; the validated Variant A prompt (16/16, source in `.project/research/narrative-bakeoff/`); keep the LLM data block to pitch count — guard against introducing a decimal IP field (none exists in production; SE-verified). Recorded the REJECTED Variant B (rotation-rationale + committee-honesty) as a do-not-re-attempt note in TN-6 + Story 04 Notes — it regressed and its rotation lever can't fire (all opponents = committee). Added E-243-04 AC-7 (model/temp) + AC-8 (IP-drop), reworded AC-2 to the Variant A source-of-truth, refreshed Description/Technical Approach/Notes/Files. Consistency sweep run. Still DRAFT — not READY (main confirms with user before the READY gate).
- 2026-06-28: SE wiring precision folded into E-243-04 AC-7/AC-8 (verified against live code): AC-7 names the two distinct changes — `.env` `OPENROUTER_MODEL` for the model, and the code edit `_invoke(0.3)`→`_invoke(0.0)` at `llm_analysis.py:282` for temperature (temp is not env-controlled); AC-8 reframed as a GUARD (no decimal-IP field exists in production to remove; the integer "IP Outs" game-log column stays). Reconciled through TN-6 / Description / Technical Approach / History; consistency sweep clean.
- 2026-06-28: **Set to READY.** Quality checklist passed; all five stories (01–05) have testable ACs and listed file dependencies; expert consultation complete (baseball-coach, data-engineer, software-engineer, ux-designer); zero open holds. Reviews: Phase-3 internal (CR spec audit + holistic team) + Codex spec review iteration 1 (7 findings, all accepted) + the narrative bake-off empirical validation — see the Review Scorecard above. Dispatch is NOT authorized by this transition; it is a separate user call.
- 2026-06-28: **Reverted READY → DRAFT** the same day. The user noted Codex spec review iteration 1 ran BEFORE the narrative bake-off changes were folded into Story 04 (AC-7/AC-8, the TN-6 rewrite, the Variant-B-rejected note) — so those final artifacts have not been through Codex. A Codex spec review iteration 2 is running on the final five-story artifacts; PM re-sets READY after it is clean/incorporated (a "Codex spec review — iteration 2" Scorecard row will be added).
- 2026-06-28: Codex spec review iteration 2 incorporated — 6 findings, all accepted (0 dismissed). (1) E-243-04 AC-2: Variant A prompt persisted to `E-243-04-narration-prompt.md` (referenced as source-of-truth) + corrected score claim; (2) AC-7: `.env` reframed as an OPTIONAL override (tracked code default `_DEFAULT_MODEL` + `.env.example` ship the model) — closes the "gitignored hidden dependency" finding; (3) routing carve-out (05) being CODIFIED by claude-architect as a generalized own-agent-memory exception in `agent-routing.md` — THREADED, not resolved in-story; (4) epic body normalized to the one-candidate case (Success Criteria, Goals, 03 Description) to match AC-1b; (5) banner string canonicalized to "rest and availability" across TN-5 / 03 AC-5 / card-spec; (6) 05 AC-1 reworded to the actual stale text (the one-name shape; the sample block uses "NSAA rest rule", not "Pitch Smart"). Also folded the SE default-model edit set into AC-7 (gemini as the tracked default + same-change stale doc/test/`.env.example` fixes; `architecture-subsystems.md:92` flagged as a closure context-layer item for claude-architect). ONE consistency sweep run incl. an epic-body-vs-AC cross-check (the drift class the prior sweeps missed): no stale "rest & availability" / "16/16 across the field" / "cites Pitch Smart" / haiku-as-default residue.
- 2026-06-28: Codex iter-2 #3 RESOLVED at the rule level — claude-architect edited `.claude/rules/agent-routing.md` (user-authorized) to generalize the Routing Precedence own-memory exception from PM-only to all agents ("an agent editing files within its OWN `.claude/agent-memory/<agent-name>/` directory"). Story 05's routing note now cites the codified rule instead of an ad-hoc carve-out. The `agent-routing.md` edit lives in the main checkout and rides this epic's planning commit.
- 2026-06-28: AC-2 score wording locked with SE-confirmed figures (16/16 scoped to the chosen model `gemini-2.5-flash-lite` / field mean ≈15.4 / ≈15.8 A/B mean beating-or-tying Variant B) — corrected the overstated "16/16 across models."
- 2026-06-28: Closure deferral recorded — `.claude/rules/architecture-subsystems.md:92` (LLM Package canonical-default line, currently `anthropic/claude-haiku-4.5`) goes stale from E-243-04's default-model change. DEFERRED to the epic's closure Context-Layer Assessment Gate (same own-memory/context-layer permission constraint — batch it there with the user present). Recorded so closure picks it up.
- 2026-06-28: AC-2 score clause refined to the final SE-verified figures (supersedes the ≈15.4 in the entry above): "a perfect 16/16 for the chosen model `google/gemini-2.5-flash-lite` (tied #1 of the 13-model bake-off; field mean ≈15.3), and ≈15.8 mean while beating or tying Variant B on every model in the temp-0.0 A/B round." Only 3 of 13 models scored 16/16, so the claim is scoped to the chosen model, never "across models." Re-grep confirmed no "16/16 across" / "across models" / "across the field" residue remains as a live claim.
- 2026-06-28: **Set to READY (re-set, all four user conditions met).** Codex spec review iteration 2 fully incorporated (6/6 accepted) including the routing finding resolved at the rule level; ONE consistency sweep clean (epic-body-vs-AC cross-check + banner string + prompt-file path + routing). Full review trail: Phase-3 internal + Codex iterations 1 & 2 + the narrative bake-off (Scorecard cumulative 35/35/0 + the empirical pass). Five stories (01–05) have testable ACs and file dependencies; expert consultation complete; zero open holds. Carried to closure: the `architecture-subsystems.md:92` context-layer deferral. Dispatch is NOT authorized by this transition — separate user call.

### Dispatch — code-review dispositions (for the closure Review Scorecard)
- 2026-06-28: **E-243-04 post-dev code review — 3 SHOULD FIX, dispositions recorded.** (1) ACCEPTED — `enrich_prediction` call site did not pass `team_name`, so the Variant A header degraded to "OPPONENT: this opponent" in every shipped report; SE fixing by wiring the real opponent name at the generator call site (re-review routed to CR). (2) DISMISSED (conscious-accept) — `_pitch_display` null/IP-proxy path renders "an estimated recent workload (pitch count not on file)" instead of the SOT "estimated {N}+ pitches"; AC-8-compliant (no IP-derived field), jargon-free, rare youth/travel null path; numeric form needs innings threaded through E-243-01/03 engine output (scope expansion vs Simple-first). (3) DISMISSED (conscious-accept) — UNAVAILABLE TODAY rows emit the engine raw `reason` string instead of the SOT structured "threw N pitches X days ago — needs M more day(s)"; functionally equivalent, jargon-free, consistent with the card's rendering of the same reason, and the LLM rewords it into prose; structured form not reproducible without richer `unavailable_arms` output ({name, reason} only). Findings #2 and #3 captured as a combined future-refinement idea **IDEA-085** (richer LLM data-block field-translations to match the Variant A SOT exactly). AC verification (PM): all 8 ACs PASS; the #2/#3 substance was the non-blocking observation PM flagged at AC-verify time.
- 2026-06-28: **Phase 4b Codex code review — 1 finding, ACCEPTED (0 dismissed).** Priority-5 planning/impl mismatch in `probable-starter-model.md:153-155` (E-243-05): the new "UNAVAILABLE TODAY" sample block listed two arms (Johnson 67p/3d, Torres 45p/1d) as unavailable that actually PASS the NSAA Varsity hard gate per the doc's own table (67p→2-day gate, 45p→1-day gate) and eligibility def — they are DISCOUNTED, not unavailable, re-teaching the discounted-vs-unavailable conflation the two-tier model exists to prevent (violated AC-4/AC-6). baseball-coach remediating the sample numbers to genuinely hard-gate-excluded values; no re-review per the codex-remediation rule (E-243-05 stays DONE — in-doc correctness fix to the same deliverable). PM to re-verify the corrected sample block against the doc's own rest tables before closure.
- 2026-06-28: **Codex sample-block fix re-verified (PM) — PASS.** Corrected `probable-starter-model.md` sample recomputed independently vs the doc's NSAA Varsity table + two-tier def + preferred thresholds: UNAVAILABLE now genuinely hard-gate-excluded (Johnson 88p/1d → 71-90→3-day gate, 1<3 fails; Torres 67p/1d → 51-70→2-day gate, 1<2 fails); Ramirez 52p/2d DISCOUNTED (passes 2-day gate, <4-day preferred); Martinez 58p/7d + Williams 38p/4d FULLY AVAILABLE. All entries internally consistent; AC-4/AC-6 hold; Codex finding fully remediated.

### Closure assessments
- 2026-06-28: **Documentation assessment (PM, per `.claude/rules/documentation.md`) — TRIGGER FIRES → docs-writer needed.** Findings: (a) **PRIMARY — `docs/coaching/standalone-reports.md:46-51`** describes the report section with the OLD framing: heading "Predicted Starter", "A named starter (or committee assessment)", and "no starter is predicted" for <4 games. E-243 reframed this user-facing surface to ranked "Most Likely Arms" (no single-name/committee hedge), added the youth/travel "Estimated rest" badge + banner and the "Unavailable today" sub-block, and changed the suppress copy to "Not enough games yet to project likely arms — rest data still accumulating." This coaching-facing behavior change is a documentation trigger; docs-writer must update the section. (b) **MINOR — `docs/admin/operations.md:48`** the `FEATURE_PREDICTED_STARTER` row says "Shows the Predicted Starter section"; reword to "Most Likely Arms section" for consistency. (c) The Tier-2 default-model change (haiku→gemini) is NOT documented in `docs/` (grep of `docs/` for haiku/gemini/OPENROUTER_MODEL = no matches) — it lives in `.env.example`/code which E-243-04 already updated, so no doc edit needed there. (d) The AC-6(b) REQUIRED post-merge manual live-LLM no-jargon check is captured in code (`llm_analysis.py:35-40`) and the story; a `docs/admin` operational note is OPTIONAL/low-value (one-time post-merge gate, not a recurring runbook step) — at most a small add docs-writer can fold in while updating (b). **Verdict: dispatch docs-writer (worktree) for (a) primary + (b) minor before archiving.**
- 2026-06-28: **Context-layer assessment (six per-trigger verdicts; team-lead-evaluated, recorded by PM; claude-architect codifying the YES items in the worktree).** T1 New convention/pattern/constraint: **YES** — youth/travel→Pitch Smart soft-prior engine fallback (→ `pitch-rules.md`); "never render raw engine `data_note` to the coach; the suppress state renders softened copy keyed on `suppress_reason`" display convention. T2 Architectural decision: **YES** — Tier-2 default LLM model changed haiku→`google/gemini-2.5-flash-lite` (→ `architecture-subsystems.md:92`, the carried deferral). T3 Footgun/boundary: **YES (minor)** — discounted-vs-unavailable conflation; `data_note`-is-internal; claude-architect deciding whether it needs codification or is already covered. T4 Agent behavior/routing/coordination: **NO**. T5 Domain knowledge for future agents: **YES** — validated gemini/Variant A model selection, hard-rest tiebreaker, youth/travel estimate, 17-season backtest findings (captured in baseball-coach memory via E-243-05 + the `pitch-rules.md` note). T6 New CLI command/workflow/operational procedure: **NO** new `bb` subcommand/skill (the AC-6b manual step is a `docs/admin` item under the documentation assessment above, not context-layer). **Verdict: T1/T2/T3/T5 fire → claude-architect codifies before archiving; T4/T6 clear.**
- 2026-06-28: **Closure context-layer codification DONE (claude-architect, worktree, staged):** `architecture-subsystems.md` (LLM Package canonical-default line haiku→`google/gemini-2.5-flash-lite` — clears the carried `:92` deferral), `pitch-rules.md` (youth/travel→Pitch Smart soft-prior engine fallback), `display-philosophy.md` (the "never render raw engine `data_note` to the coach; suppress renders softened copy keyed on `suppress_reason`" convention), and `key-metrics.md`. **Closure documentation updates DONE (docs-writer, worktree, staged):** `docs/coaching/standalone-reports.md` (Predicted Starter → ranked "Most Likely Arms" + estimate badge/banner + Unavailable-today sub-block + new suppress copy) and `docs/admin/operations.md` (flag-row wording). Both closure gates satisfied — assessments above are resolved.

### Dispatch Review Scorecard (post-dev reviews)
| Review pass | Findings | Accepted | Dismissed | Outcome |
|-------------|----------|----------|-----------|---------|
| Per-story CR — E-243-01 | 1 (SHOULD: `_rest_state` negative-days guard) | 1 | 0 | Fixed with test coverage; CR APPROVED round 2. |
| Per-story CR — E-243-02 | 1 (SHOULD: dead `_LEAGUE_WARNINGS["youth_travel"]`) | 1 | 0 | Fixed; CR APPROVED round 2. |
| Per-story CR — E-243-03 | 2 (CR: stale Jinja comment; UXD+PM: suppress `data_note`→`suppress_reason` softened coach copy) | 2 | 0 | Both fixed; CR APPROVED round 2; UXD design-fidelity pass faithful (no separate findings). |
| Per-story CR — E-243-04 | 3 (ACCEPTED: `team_name` wiring; DISMISSED: null-pitch `_pitch_display` phrasing; DISMISSED: unavailable-arms raw `reason` string) | 1 | 2 | #1 fixed + CR APPROVED round 2; #2/#3 conscious-accepts → **IDEA-085**. |
| Per-story CR — E-243-05 | 0 (context-layer-only — CR skipped; PM sole AC verifier) | 0 | 0 | All 7 ACs PASS (PM). |
| CR integration review (Phase 4a) | 0 (1 informational: `format_nsaa_rest_table` orphaned-in-production, out of scope) | 0 | 0 | Clean. |
| Codex code review (Phase 4b) | 1 (discounted-vs-unavailable sample conflation, E-243-05) | 1 | 0 | Remediated + PM re-verified PASS. |

**Dispatch totals: 8 findings raised, 6 accepted, 2 dismissed (both → IDEA-085).** (Distinct from the planning-phase Review Scorecard at the top of this epic: cumulative 35/35/0 + the empirical bake-off pass.)

- 2026-06-28: **COMPLETED.** All five stories (01–05) DONE and AC-verified. Shipped: (01) a HARD rest-discount re-rank tiebreaker so a fully-available starter always outranks a still-tired one (stable post-truncation partition, in place before the confidence tier); (02) a youth/travel labeled-estimate fallback (`PITCH_SMART_15_18`) that stops blank cards while keeping `usssa`/`perfect_game`/`unknown` suppressed, with a new `is_estimate` flag; (03) the core presentation reframe — the report now always renders the ranked "Most Likely Arms" list (start-share grounded in games, days rest, two-valued eligibility, optional handedness) with an additive `unavailable_arms` sub-block, the youth/travel "Estimated rest" badge + banner, and a softened `suppress_reason`-keyed suppress copy that never leaks the raw engine `data_note`; (04) the Tier-2 LLM reframe — the validated Variant A prompt (no-predict guideline removed), `google/gemini-2.5-flash-lite` as the tracked default at temperature 0.0 with the `OPENROUTER_MODEL` override preserved, pitch-count-only data block (no decimal IP); (05) the baseball-coach probable-starter model doc corrected to the ranked top-2/3 shape + HARD tiebreaker + IP-proxy contract. Reviews: planning Scorecard 35/35/0 + empirical bake-off; dispatch Scorecard 8 raised / 6 accepted / 2 dismissed (→IDEA-085); CR APPROVED every code story; Codex Phase 4b finding remediated + PM re-verified. Closure gates satisfied: context-layer codification (claude-architect — architecture-subsystems.md clearing the `:92` deferral, pitch-rules.md, display-philosophy.md, key-metrics.md) and documentation (docs-writer — docs/coaching/standalone-reports.md, docs/admin/operations.md) both DONE/staged. Deferrals captured: IDEA-085 (richer LLM data-block field-translations). NOT a ROADMAP A–E slice (no §0 entry). One operator follow-up remains open (non-blocking): whether to promote `FEATURE_PREDICTED_STARTER` to default-on after these fixes verify in production. Note: COMPLETED is authored here in the worktree for the closure patch; it finalizes only after the Step 8 full-suite green gate passes.
