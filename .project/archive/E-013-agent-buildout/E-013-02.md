# E-013-02: Complete `general-dev.md` -- Full System Prompt + Memory Seed

## Epic
[E-013: Agent Buildout -- Complete All Stubbed Agents](../E-013-agent-buildout/epic.md)

## Status
`DONE`

## Description
After this story is complete, `general-dev.md` will be a full operational manual matching the depth of `api-scout.md` and `baseball-coach.md`. The agent will have complete frontmatter (including `description` and `memory` fields), a comprehensive system prompt covering all core responsibilities, correct project architecture references, and a seeded memory directory. An agent dispatched with a story reference can read `general-dev.md` and execute Python implementation stories confidently without missing code style conventions or project structure.

## Context
`general-dev.md` was created as a functional stub in E-007 -- just enough to enforce the work authorization gate. It is 42 lines. It is missing the `description` frontmatter field and the `memory` frontmatter field. Its system prompt covers work authorization and CLAUDE.md code style conventions but nothing else: no responsibilities, no interaction patterns with other agents, no skill references, no memory instructions.

The agent-blueprints.md in claude-architect's memory also still describes general-dev as "to be created when needed" -- this is addressed in E-013-05.

There is no `.claude/agent-memory/general-dev/` directory. The agent has no persistent memory to draw on across sessions.

The canonical agent file format is documented in the epic's Technical Notes. Code conventions are in CLAUDE.md.

## Acceptance Criteria

- [ ] **AC-1**: `general-dev.md` contains valid YAML frontmatter with all five fields: `name: general-dev`, `description` (multi-line string with 5 usage examples in the same format as `api-scout.md`), `model: sonnet`, `color: blue`, `memory: project`.

- [ ] **AC-2**: `general-dev.md` is 150 lines or longer (full operational manual, not a stub).

- [ ] **AC-3**: `general-dev.md` contains a section "## Identity" (2-3 sentences) and a "## Core Responsibilities" section with at least 4 numbered subsections covering: Python implementation (crawlers, parsers, loaders), test writing (pytest, mocking), utility and tooling scripts, and reading/consuming specs from other agents (API spec, schema docs, story files).

- [ ] **AC-4**: `general-dev.md` contains a "## Work Authorization" section that is identical in structure to the current stub's work authorization block (story reference required, refuse if missing), preserving the exact refusal message: "I need a story file reference before beginning implementation. Please provide the story ID (e.g., E-001-02) or the path to the story file."

- [ ] **AC-5**: `general-dev.md` contains a "## Code Standards" section that matches the current stub's code standards (type hints, docstrings, dataclasses/Pydantic, pathlib, logging, pytest, mock HTTP). This section must also include the HTTP request discipline rules from CLAUDE.md: use realistic User-Agent, include standard browser headers, use the session factory from `src/http/session.py` (not bare httpx/requests calls), add jitter to request timing.

- [ ] **AC-6**: `general-dev.md` contains a "## How You Interact With Other Agents" section describing: api-scout provides the API spec at `docs/gamechanger-api.md` (load this file when implementing any API client code); data-engineer provides schema documentation and migration conventions; baseball-coach's requirements inform what stats to parse and compute; project-manager dispatches stories.

- [ ] **AC-7**: `general-dev.md` contains a "## Skill References" section with named trigger conditions for at least two skills: `filesystem-context` (trigger: beginning a task that requires reading multiple context files including story file, epic Technical Notes, and referenced design docs) and `context-fundamentals` (trigger: about to load a large research artifact and context window is above 70%). See the epic Technical Notes for the canonical format.

- [ ] **AC-8**: `general-dev.md` contains a "## Memory Instructions" section and a "# Persistent Agent Memory" standard block pointing to `/Users/jason/Documents/code/baseball-crawl/.claude/agent-memory/general-dev/`.

- [ ] **AC-9**: Directory `.claude/agent-memory/general-dev/` is created and contains `MEMORY.md` with seeded content. The MEMORY.md must include entries for: project code conventions (type hints, pathlib, logging), key file paths (src/, tests/, data/, docs/, docs/gamechanger-api.md), testing rules (no real HTTP, pytest, mock at transport layer), and HTTP request discipline (session factory location, browser headers, no bare httpx calls). MEMORY.md must be under 200 lines.

- [ ] **AC-10**: Given an agent reads `general-dev.md` before executing a story that involves HTTP calls, when it writes Python code, then it imports and uses `create_session()` from `src/http/session.py` rather than creating a raw `httpx.Client()` or `requests.Session()` directly.

## Technical Approach
1. Read the current `/Users/jason/Documents/code/baseball-crawl/.claude/agents/general-dev.md` in full.
2. Read `api-scout.md` and `baseball-coach.md` as style references for `description` frontmatter format and section depth.
3. Read the CLAUDE.md sections "HTTP Request Discipline", "Code Style", "Architecture", and "Testing" -- these are the authoritative source for AC-5 content.
4. Read the epic's Technical Notes sections "Agent File Structure", "Skill Reference Format", and "Memory Seed Content (per agent)" for general-dev.
5. Rewrite `general-dev.md` in full. Preserve the work authorization refusal message verbatim.
6. Create the directory `.claude/agent-memory/general-dev/` and write `MEMORY.md` with seeded content.

## Dependencies
- **Blocked by**: None. This story can start immediately and run in parallel with E-013-01 and E-013-03.
- **Blocks**: E-013-04 (skill wiring) and E-013-05 (memory cleanup) must wait for this story to be DONE.

## Files to Create or Modify
- `/Users/jason/Documents/code/baseball-crawl/.claude/agents/general-dev.md` -- full rewrite
- `/Users/jason/Documents/code/baseball-crawl/.claude/agent-memory/general-dev/` -- new directory
- `/Users/jason/Documents/code/baseball-crawl/.claude/agent-memory/general-dev/MEMORY.md` -- new file

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] HTTP discipline rules (session factory, browser headers) are present in Code Standards section
- [ ] Memory directory and MEMORY.md exist and are under 200 lines
- [ ] Story status updated to `DONE` in this file

## Notes
- `color: blue` is the correct value. The current stub has no color field set. The agent-blueprints.md says `purple` but the stub already exists without color, and blue is the conventional color for developer agents. This choice is intentional -- document it in the MEMORY.md if desired.
- The `description` field examples should illustrate work-authorization flows: "The user wants to implement the credential parser from E-001-01" -> route to general-dev. Include examples that show the routing pattern correctly (story ID in the request).
- Do NOT make general-dev aware of cloud deployment details (Docker, Hetzner, Cloudflare Tunnel) -- that is operator-level knowledge, not implementation-level.
- Reference files for this story:
  - Current stub: `/Users/jason/Documents/code/baseball-crawl/.claude/agents/general-dev.md`
  - Style reference: `/Users/jason/Documents/code/baseball-crawl/.claude/agents/api-scout.md`
  - HTTP discipline: `/Users/jason/Documents/code/baseball-crawl/CLAUDE.md` (HTTP Request Discipline section)
  - Epic technical notes: `/Users/jason/Documents/code/baseball-crawl/epics/E-013-agent-buildout/epic.md`
