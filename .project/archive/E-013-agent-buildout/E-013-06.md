# E-013-06: Extend PM Dispatch and Quality Checklist Procedures

## Epic
[E-013: Agent Buildout -- Complete All Stubbed Agents](../E-013-agent-buildout/epic.md)

## Status
`DONE`

## Absorbs
This story absorbs the following story from E-012, which was merged into E-013 during triage on 2026-03-01:
- **E-012-03**: Extend PM dispatch and quality checklist procedures

## Description
After this story is complete, the PM's Dispatch Mode procedure in `project-manager.md` will explicitly name the filesystem-context skill as the framework for the file-reading sequence, and the PM's quality checklist will include a verifiable item requiring that story Technical Approach sections name deferred context files by absolute path. These two additions operationalize the skill references added in E-013-04: a skill referenced but not reflected in behavior is decorative; a skill reflected in concrete procedure steps is active.

## Context
E-013-04 adds skill reference sections to all agent definitions, including the PM. That story tells the PM WHEN to load the skills. This story tells the PM HOW to apply the filesystem-context skill in practice -- by revising the Dispatch Mode step 1 to name the progressive disclosure sequence explicitly, and by adding a quality checklist item that enforces the "name deferred context files by absolute path" convention in story writing.

The quality checklist gap is the more consequential of the two changes. When a story's Technical Approach says "read the relevant design document" without naming it, the implementing agent must guess which file to load. When it says "read `/.project/research/E-006-precommit-design.md`", the implementing agent can load it as deferred context in one step. This has been a recurrent pain point (noted in MEMORY.md lessons learned: "E-006 refinement pattern -- initial stories had vague technical approach sections and speculative file lists").

## Acceptance Criteria

- [ ] **AC-1**: Given `project-manager.md` after this story, when an agent reads the Dispatch Mode procedure (specifically step 1 "Read the epic directory"), then that step now includes an explicit sub-step or parenthetical referencing the filesystem-context skill and naming the two-pass reading sequence: (1) scan the Stories table for IDs, titles, and statuses; (2) open story files only for those with `Status: TODO` and satisfied dependencies.

- [ ] **AC-2**: Given the revised Dispatch Mode step 1, when an agent follows it for an epic with 8 stories where only 2 are TODO and unblocked, then the procedure directs the agent to open 2 story files (not all 8), consistent with progressive disclosure -- avoiding loading the full content of DONE or BLOCKED stories that are not actionable.

- [ ] **AC-3**: Given the quality checklist in `project-manager.md` after this story, when an agent counts the checklist items, then there is a new item: "Story `Technical Approach` sections name all referenced context files by absolute path (e.g., `/.project/research/E-NNN-slug.md`, `docs/gamechanger-api.md`) so implementing agents can load them as deferred context without guessing."

- [ ] **AC-4**: Given the revised quality checklist, when the PM applies it to a story whose Technical Approach says "consult the GameChanger API spec" (without a path), then the checklist item added in AC-3 would mark that story as failing the checklist, prompting the PM to add the absolute path `docs/gamechanger-api.md`.

- [ ] **AC-5**: Given the full `project-manager.md` after this story, when a diff is taken against the post-E-013-04 version, then the only changes are: (a) the revised Dispatch Mode step 1 (one sub-step or parenthetical added), and (b) one new item in the quality checklist. No sections are removed. No other procedure sections are modified.

## Technical Approach

Read `project-manager.md` after E-013-04 has been applied. Identify two locations:

**Location 1 -- Dispatch Mode step 1** (currently reads approximately):
> "1. **Read the epic directory.** Read `/epics/E-NNN-slug/epic.md` and all story files in the directory."

Revise to add the progressive disclosure sub-step:

> "1. **Read the epic directory.** Apply progressive disclosure (see `.claude/skills/filesystem-context/SKILL.md`): first scan the epic's Stories table to identify IDs, titles, and statuses (first pass -- titles only, no file I/O). Then open story files ONLY for stories with `Status: TODO` whose dependencies are satisfied (second pass -- full content). Do not load the full text of DONE, IN_PROGRESS, or BLOCKED story files unless a specific step requires their content."

**Location 2 -- Quality Checklist** (currently ends with approximately):
> "- [ ] Epic status set to `READY` after all stories pass this checklist"

Add one new item before or after that line:

> "- [ ] Story `Technical Approach` sections name all referenced context files by absolute path (e.g., `/.project/research/E-NNN-slug.md`, `docs/gamechanger-api.md`) rather than by vague description (e.g., 'consult the design document'). Implementing agents must be able to load these as deferred context in one step."

Reference files:
- `/Users/jason/Documents/code/baseball-crawl/.claude/agents/project-manager.md` (the file to edit)
- `/Users/jason/Documents/code/baseball-crawl/.claude/skills/filesystem-context/SKILL.md` (the skill being referenced)
- Archived E-012-03: `/.project/archive/E-012-filesystem-context-integration/E-012-03.md` (original story)

## Dependencies
- **Blocked by**: E-013-04 (skills must be wired into PM's definition before the procedures reference them)
- **Blocks**: Nothing. This is the terminal procedural story.

## Files to Create or Modify
- `/Users/jason/Documents/code/baseball-crawl/.claude/agents/project-manager.md` -- revise Dispatch Mode step 1; add one quality checklist item

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Dispatch Mode step 1 names the filesystem-context skill and the two-pass reading sequence
- [ ] Quality checklist contains the new deferred-context-path item
- [ ] No other sections of `project-manager.md` were modified
- [ ] Story status updated to `DONE` in this file

## Notes
- This story modifies `project-manager.md` which was also modified by E-013-04. It must run AFTER E-013-04 is DONE.
- The Dispatch Mode step 1 revision should add clarity without adding length. Prefer a brief in-line reference over a multi-sentence expansion.
- The quality checklist item wording should match the PM's existing checklist style: imperative phrasing, specific enough to be testable against a real story file.
- This story does NOT add a Refinement Mode procedure step for filesystem-context. The skill reference in the Skills section (added by E-013-04) covers the trigger. Adding an explicit Refinement Mode step would be useful but is a scope expansion -- capture as a follow-on idea if the need is felt after dispatch.
