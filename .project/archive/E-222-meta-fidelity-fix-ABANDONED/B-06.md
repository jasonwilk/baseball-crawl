# E-222-B-06: Cross-Workstream Integration Smoke Test (B Stories)

## Epic
[E-222: Meta-Fidelity Fix](./epic.md)

## Status
`TODO`

## Description
After this story is complete, a read-only verification pass will have confirmed internal consistency across every file modified by B-01 through B-05. A dry-run will verify that a hypothetical CR invocation in Invariant Audit Mode would have caught the E-220 `admin.py` `_delete_team_cascade` frontmatter bug (lines 939-1033). Findings are reported via completion message; no file writes.

## Context
Per epic Technical Notes, B-06 is a cross-workstream integration smoke test. It verifies that the rule file, CR agent definition, implement skill, plan skill, PM agent definition, and epic template all agree on the `## Invariants Established` section structure and Invariant Audit Mode contract. The E-220 bug is the reference scenario — if the mechanism works, a dry-run against that scenario should produce a FAIL, confirming the mechanism would have caught the original bug.

## Acceptance Criteria
- [ ] **AC-1**: Every file modified by B-01 through B-05 is read and verified for internal consistency. Files: `.claude/rules/invariant-audit.md`, `.claude/rules/perspective-provenance.md` frontmatter, `.claude/agents/code-reviewer.md`, `.claude/skills/implement/SKILL.md`, `.claude/skills/plan/SKILL.md`, `.claude/agents/product-manager.md`, `.project/templates/epic-template.md`.
- [ ] **AC-2**: Structural consistency verified: the `## Invariants Established` section structure defined in `invariant-audit.md` matches the skeleton in `epic-template.md`, the parser in `implement/SKILL.md` Phase 2 Step 0, and the READY gate verification in `plan/SKILL.md` Phase 5.
- [ ] **AC-3**: Contract consistency verified: the Invariant Audit Mode contract defined in `invariant-audit.md` matches the consumption in `code-reviewer.md` and the routing in `implement/SKILL.md` Phase 4a Step 3 + Step 4.5.
- [ ] **AC-4**: Dry-run performed: walk through a hypothetical invocation of CR in Invariant Audit Mode against `perspective-provenance.md` (with the B-01 frontmatter fix) for the E-220 `admin.py` `_delete_team_cascade` scenario (lines 939-1033). The dry-run MUST produce a FAIL finding, confirming the mechanism would have caught the original bug.
- [ ] **AC-5**: Any inconsistencies discovered are reported to team-lead via SendMessage BEFORE completion. If inconsistencies exist, story is NOT marked DONE until resolved.
- [ ] **AC-6**: No files are created or modified by this story. It is a read-only verification pass.

## Technical Approach

Read all files modified by B-01..B-05. For each cross-reference point, verify agreement:

1. `invariant-audit.md` Invariants Established section structure ⇄ `epic-template.md` skeleton.
2. `invariant-audit.md` Invariant Audit Mode contract ⇄ `code-reviewer.md` consumption.
3. `implement/SKILL.md` Phase 2 Step 0 parser ⇄ `epic-template.md` section.
4. `implement/SKILL.md` Phase 4a Step 3 + Step 4.5 routing ⇄ `code-reviewer.md` Invariant Audit Mode.
5. `plan/SKILL.md` Phase 5 READY gate ⇄ `epic-template.md` section + PM agent definition discovery checklist.
6. `perspective-provenance.md` frontmatter after B-01 ⇄ `admin.py` cascade-delete site coverage.

Then perform the dry-run: simulate a review of a hypothetical E-220 story that modifies `admin.py` `_delete_team_cascade`, with `invariant_carrying = true` and the perspective invariant declaration in the assignment. Trace through the CR Invariant Audit Mode procedure (per B-02) and verify that the per-site enumeration reaches `admin.py` lines 939-1033 and produces a FAIL (because the pre-B-01 frontmatter would not have loaded the rule at that file).

Report findings via SendMessage to team-lead. No file writes.

## Dependencies
- **Blocked by**: B-01, B-02, B-03, B-04, B-05
- **Blocks**: E-01

## Files to Create or Modify
- None (read-only verification)

## Agent Hint
claude-architect

## Handoff Context
- **Produces for E-222-E-01**: Confirmed Workstream B cross-consistency that E-01 assumes when verifying full-epic (A+B+C+D) cross-consistency.

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Completion report sent to team-lead via SendMessage
- [ ] No file writes performed

## Notes
Read-only smoke test. If any inconsistency is discovered, escalate to team-lead rather than silently fixing — cross-workstream drift is a signal that earlier stories drifted and should be re-opened.
