# E-222-B-03: Implement Skill Extensions (Phase 2 Step 0, Phase 3 Step 5, Phase 4a Step 3 + 4.5)

## Epic
[E-222: Meta-Fidelity Fix](./epic.md)

## Status
`TODO`

## Description
After this story is complete, `implement/SKILL.md` will have a new Phase 2 Step 0 that parses the epic for `## Invariants Established`, a Phase 3 Step 5 per-story CR assignment template that includes an optional `## Invariants` section, a Phase 4a Step 3 that routes CR into Invariant Audit Mode when `invariant_carrying = true`, and a new Phase 4a Step 4.5 that runs the Invariant Audit Sweep before triage.

## Context
Per epic Technical Notes, the dispatch path must propagate invariant declarations from epic.md into CR assignments and trigger the Invariant Audit Sweep automatically. This story installs that plumbing. All edits are additive — when an epic lacks the `## Invariants Established` section, behavior is unchanged from current baseline.

## Acceptance Criteria
- [ ] **AC-1**: A new Phase 2 Step 0 is added to `implement/SKILL.md` that parses the epic for `## Invariants Established`. Absent section → `invariant_carrying = false`. Present section → `invariant_carrying = true`; declarations are extracted and passed verbatim to the per-story CR assignment template in Step 5 and to the Phase 4a audit assignment template.
- [ ] **AC-2**: Phase 3 Step 5 item 2 (the per-story CR assignment template) is modified to include a new optional `## Invariants` section containing the invariant declaration verbatim when `invariant_carrying = true`.
- [ ] **AC-3**: Phase 4a Step 3 is modified: when `invariant_carrying = true`, CR is routed in Invariant Audit Mode regardless of the "and review" modifier, with the invariant declaration in the assignment.
- [ ] **AC-4**: A new Phase 4a Step 4.5 is added: Invariant Audit Sweep — before Phase 4a Step 4 triage, CR produces a per-site PASS/FAIL table for every declared invariant. Failures feed into Phase 4a remediation via the existing circuit breaker.
- [ ] **AC-5**: All edits are additive only; no behavioral change when the epic lacks an `## Invariants Established` section.
- [ ] **AC-6**: Edits do not conflict with C-02 (which modifies Phase 3 Step 5 in a later wave); this story leaves a stable anchor for C-02.

## Technical Approach

Four edits to `.claude/skills/implement/SKILL.md`:

1. Phase 2 Step 0 insertion — before current Phase 2 Step 1 — parses the epic file for the `## Invariants Established` section and sets the `invariant_carrying` flag.
2. Phase 3 Step 5 item 2 template update — adds the optional `## Invariants` section to the per-story CR assignment template.
3. Phase 4a Step 3 update — adds the invariant-carrying branch that routes CR into Invariant Audit Mode.
4. Phase 4a Step 4.5 insertion — new sub-step before Step 4 triage for the Invariant Audit Sweep.

Implementer reads the current `implement/SKILL.md` to identify Phase numbering anchors. Because C-02 depends on B-03 for the Phase 3 Step 5 area, leave a clean anchor (or explicit comment) that C-02 can extend without re-editing B-03's additions.

## Dependencies
- **Blocked by**: B-01, B-02
- **Blocks**: C-02, B-06

## Files to Create or Modify
- `.claude/skills/implement/SKILL.md` (MODIFY — Phase 2 Step 0 insertion + Phase 3 Step 5 item 2 template + Phase 4a Step 3 + Phase 4a Step 4.5 insertion)

## Agent Hint
claude-architect

## Handoff Context
- **Produces for E-222-C-02**: Phase 3 Step 5 anchor where C-02 inserts the cr-findings.log write sub-step.
- **Produces for E-222-B-06**: Wired dispatch path that B-06 dry-run-traces for consistency.

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] No behavioral change when epic lacks `## Invariants Established` (verified by code review)
- [ ] Stable anchor left for C-02

## Notes
Depends on B-01 (invariant contract source) and B-02 (CR Invariant Audit Mode delivered).
