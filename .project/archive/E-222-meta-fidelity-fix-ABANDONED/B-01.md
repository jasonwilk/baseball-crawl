# E-222-B-01: Invariant Audit Rule + perspective-provenance Frontmatter Fix

## Epic
[E-222: Meta-Fidelity Fix](./epic.md)

## Status
`TODO`

## Description
After this story is complete, a new rule file `.claude/rules/invariant-audit.md` will define the authoritative `## Invariants Established` section template and the Invariant Audit Mode contract that code-reviewer consumes. The `perspective-provenance.md` rule frontmatter will cover `src/api/routes/**` (and any other invariant-surface files currently missing), closing the E-220 cascade-delete blind spot where `admin.py` `_delete_team_cascade` was not loaded as context when the rule should have applied.

## Context
E-220's 8 rounds of Codex remediation repeatedly surfaced the same bug class because per-story code review could not see the cross-cutting invariant surface area and because `perspective-provenance.md` frontmatter did not cover cascade-delete sites. This story creates the authoritative invariant-audit rule file that downstream stories (B-02..B-05, B-06) reference, and applies the frontmatter fix as a reference implementation of the rule's own frontmatter coverage audit procedure. All other Workstream B stories build on this story.

## Acceptance Criteria
- [ ] **AC-1**: `.claude/rules/invariant-audit.md` exists with `paths:` frontmatter covering `epics/**` and `.project/archive/**`.
- [ ] **AC-2**: The rule file defines the authoritative `## Invariants Established` section template (the copy that the epic template in B-05 will reference).
- [ ] **AC-3**: The rule file defines the Invariant Audit Mode contract that code-reviewer consumes (trigger, input format, per-site enumeration procedure, structured PASS/FAIL output table format).
- [ ] **AC-4**: The rule file defines the frontmatter coverage audit procedure (how to identify invariant surface files and verify they are in the rule's `paths:` frontmatter).
- [ ] **AC-5**: The rule file cross-references `code-reviewer.md`, `implement/SKILL.md`, `context-layer-guard.md`, and `perspective-provenance.md` as the reference implementation.
- [ ] **AC-6**: `.claude/rules/perspective-provenance.md` frontmatter `paths:` field includes `src/api/routes/**`.
- [ ] **AC-7**: Implementer grepped `perspective_team_id` across `src/`, cross-referenced the current frontmatter, and documented any other missing paths as fixed in the story (or confirmed none remain).
- [ ] **AC-8**: Empirical validation recorded in the story: if the rule had been loaded when `admin.py` `_delete_team_cascade` (lines 939-1033) was edited during E-220, the rule would have been in context.

## Technical Approach

Create `.claude/rules/invariant-audit.md` as the single authoritative source for the `## Invariants Established` section template, Invariant Audit Mode contract, and frontmatter coverage audit procedure. Downstream stories reference this file rather than restating its content.

For the `perspective-provenance.md` frontmatter fix: grep the codebase for `perspective_team_id` usage and the cascade-delete sites identified in E-220 forensics, then extend the `paths:` frontmatter to cover them. Apply this as a live exercise of the rule's own frontmatter coverage audit procedure — if additional missing paths are discovered, fix them within this story.

Cross-reference `perspective-provenance.md` from the new rule as the reference implementation for how an invariant rule is structured.

## Dependencies
- **Blocked by**: None
- **Blocks**: B-02, B-03, B-04, B-05, B-06

## Files to Create or Modify
- `.claude/rules/invariant-audit.md` (NEW)
- `.claude/rules/perspective-provenance.md` (MODIFY — frontmatter only)

## Agent Hint
claude-architect

## Handoff Context
- **Produces for E-222-B-02**: Authoritative Invariant Audit Mode contract that code-reviewer agent definition consumes (trigger, input format, PASS/FAIL output table).
- **Produces for E-222-B-03**: Invariant declaration structure used by implement skill Phase 2 Step 0 parser and Phase 4a Step 4.5 Invariant Audit Sweep.
- **Produces for E-222-B-04**: Section cross-reference target for plan skill and PM agent definition invariant assessment bullets.
- **Produces for E-222-B-05**: Authoritative `## Invariants Established` section template copied into epic template.
- **Produces for E-222-B-06**: Reference implementation (`perspective-provenance.md`) for dry-run verification that the mechanism would have caught the `admin.py` cascade-delete bug.

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Rule file cross-references valid (linked files exist and section names are accurate)
- [ ] Frontmatter coverage check run against `perspective_team_id` usage with results documented
- [ ] No regressions in existing context-layer files

## Notes
This story is foundational for Workstream B. Do not defer any sub-decision from AC-3 or AC-4 to downstream stories — downstream stories reference this rule file and must not need to re-define structures it should have authoritatively established.
