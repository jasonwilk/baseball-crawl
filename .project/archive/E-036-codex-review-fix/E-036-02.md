# E-036-02: Update CLAUDE.md Commands Section if Interface Changed

## Epic
[E-036: Fix Codex Code-Review Wrapper](epic.md)

## Status
`DONE`

## Description
After this story is complete, the CLAUDE.md Commands section accurately describes the `codex-review.sh` interface as implemented by E-036-01. If E-036-01 did not change the user-facing interface (same modes, same syntax), this story verifies accuracy and is marked DONE with no file changes needed.

## Context
E-036-01 fixes the broken `codex-review.sh` wrapper. If the fix changes how users invoke the script (e.g., new flags, different syntax, changed modes), CLAUDE.md must be updated to match. The current Commands entry reads:
```
- `./scripts/codex-review.sh <mode>` -- code review of repository changes; modes: `uncommitted`, `base <branch>`, `commit <sha>`. Rubric: `.project/codex-review.md`
```

## Acceptance Criteria
- [ ] **AC-1**: The CLAUDE.md Commands entry for `codex-review.sh` accurately describes the script's current interface (modes, arguments, any new flags).
- [ ] **AC-2**: If E-036-01 did not change the user-facing interface, confirm the existing entry is accurate and record "No changes needed -- interface unchanged" in a commit message or story notes.
- [ ] **AC-3**: If E-036-01 changed the interface, the Commands entry is updated to match the new syntax exactly.
- [ ] **AC-4**: No other sections of CLAUDE.md are modified.

## Technical Approach
1. Read the updated `/workspaces/baseball-crawl/scripts/codex-review.sh` (post E-036-01).
2. Compare its `usage()` output and invocation syntax against the current CLAUDE.md Commands entry.
3. If they match: no edit needed. Story is DONE.
4. If they differ: update the Commands entry to match the implemented reality.

## Dependencies
- **Blocked by**: E-036-01 (must know the final interface before verifying/updating docs)
- **Blocks**: None

## Files to Create or Modify
- `/workspaces/baseball-crawl/CLAUDE.md` -- Commands section only (if interface changed)

## Definition of Done
- [ ] CLAUDE.md Commands entry for codex-review.sh matches the implemented script interface
- [ ] No unrelated CLAUDE.md changes

## Notes
- This story routes to `claude-architect` because it modifies CLAUDE.md (a context-layer file per dispatch-pattern.md routing rules).
- If the interface is unchanged, this is a verification-only story with no file modifications.
