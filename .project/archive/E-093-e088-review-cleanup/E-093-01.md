# E-093-01: Shrink bulk_create_opponents and Fix Lazy Imports

## Epic
[E-093: E-088 Code Review Cleanup](epic.md)

## Status
`DONE`

## Description
After this story is complete, `bulk_create_opponents` in `src/api/db.py` will be under 50 lines, and the `re` and `secrets` imports will be at the top of the file with the other standard library imports.

## Context
Code review finding #3: `bulk_create_opponents` is 56 lines (limit 50). Lines 784-785 have lazy imports (`import re`, `import secrets as _secrets`) inside the function body for standard library modules with no circular import risk. The imports belong at the top of the file.

## Acceptance Criteria
- [ ] **AC-1**: `import re` and `import secrets` appear at the top of `src/api/db.py` with the other standard library imports (not inside any function body).
- [ ] **AC-2**: `bulk_create_opponents` is at most 50 lines (including the `def` line and docstring).
- [ ] **AC-3**: All existing tests pass (`pytest`).

## Technical Approach
The function body has two extractable concerns: slug generation (lines 798-802) and the per-name insert loop body. Extract a helper (e.g., `_generate_opponent_team_id(name)`) for the slug+suffix generation. Move the two imports to the module-level import block. Count lines after extraction to confirm <= 50.

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Files to Create or Modify
- `src/api/db.py`

## Agent Hint
software-engineer

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests
