# E-093-02: Shrink resolve_opponents CLI Command

## Epic
[E-093: E-088 Code Review Cleanup](epic.md)

## Status
`DONE`

## Description
After this story is complete, the `resolve_opponents` function in `src/cli/data.py` will be under 50 lines.

## Context
Code review finding #4: `resolve_opponents` is 64 lines (limit 50). The dry-run block (lines 168-174) and the manual DB path resolution block (lines 176-181) are candidates for extraction into helpers.

## Acceptance Criteria
- [ ] **AC-1**: `resolve_opponents` in `src/cli/data.py` is at most 50 lines (including the `def` line, decorator, and docstring).
- [ ] **AC-2**: All existing tests pass (`pytest`).

## Technical Approach
Extract the dry-run echo block into a helper and/or extract the DB path resolution into a helper (the DB path resolution pattern may already exist elsewhere in the CLI -- check for reuse). The goal is to get under 50 lines with minimal new code.

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Files to Create or Modify
- `src/cli/data.py`

## Agent Hint
software-engineer

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests
