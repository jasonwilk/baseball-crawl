# E-179-05: Fix test_report_renderer.py Data Model Failures

## Epic
[E-179: Fix Pre-Existing Test Failures from E-173 UI Changes](epic.md)

## Status
`DONE`

## Description
After this story is complete, all 3 failing tests in `tests/test_report_renderer.py` will pass. The failures have mixed root causes: 1 test fails on a template assertion (footer text changed), and 2 tests fail on mock path issues (lazy import pattern changed in the renderer).

## Context
This file tests the standalone scouting report renderer. Failures span TN-1 categories 1 and 5. Specifically: `test_produces_html_with_all_sections` fails on a template assertion (footer text changed by E-173). `test_spray_charts_as_base64_data_uris` and `test_spray_chart_render_failure_is_non_fatal` fail because the renderer's lazy import pattern changed -- mocks targeting the old import path no longer intercept correctly.

## Acceptance Criteria
- [ ] **AC-1**: `TestCompleteReport::test_produces_html_with_all_sections` passes with template assertion updated to match current footer text.
- [ ] **AC-2**: `TestSprayCharts::test_spray_charts_as_base64_data_uris` passes with mock path updated to match the renderer's current lazy import pattern.
- [ ] **AC-3**: `TestEdgeCases::test_spray_chart_render_failure_is_non_fatal` passes with mock path updated to match the renderer's current lazy import pattern.
- [ ] **AC-4**: Running `pytest tests/test_report_renderer.py -v` produces 0 failures, 0 errors.

## Technical Approach
Read `/workspaces/baseball-crawl/src/reports/renderer.py` to understand the current renderer implementation. For AC-1 (footer text): read the report template to find the current footer string and update the test assertion. For AC-2 and AC-3 (mock paths): examine the renderer's lazy import pattern for the spray chart module and update the monkeypatch target path in both tests to intercept at the correct location. The renderer module is the source of truth for import paths and template content.

## Dependencies
- **Blocked by**: None
- **Blocks**: E-179-07

## Files to Create or Modify
- `tests/test_report_renderer.py`

## Agent Hint
software-engineer

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests
