# E-179-08: Fix test_opponent_resolver.py Assertion Failures

## Epic
[E-179: Fix Pre-Existing Test Failures from E-173 UI Changes](epic.md)

## Status
`DONE`

## Description
After this story is complete, all 13 failing tests in `tests/test_crawlers/test_opponent_resolver.py` will pass. The failures are caused by error count assertions being off by 1, likely due to a new error path introduced by the `finalize_opponent_resolution` integration in E-173.

## Context
This file tests the opponent resolver crawler. The failures fall under TN-1 category 7 (behavioral drift). The resolver now integrates with `finalize_opponent_resolution()` which may produce an additional error in certain scenarios, causing assertions like `assert result.errors == 0` to fail (actual: 1) and `assert result.errors == 1` to fail (actual: 2). The implementing agent should read the current resolver code and `finalize_opponent_resolution()` to understand the new error path and determine correct expected values.

## Acceptance Criteria
- [ ] **AC-1**: All 13 failing tests in `tests/test_crawlers/test_opponent_resolver.py` pass with error count assertions updated to match current resolver behavior.
- [ ] **AC-2**: Running `pytest tests/test_crawlers/test_opponent_resolver.py -v` produces 0 failures, 0 errors.

## Technical Approach
Read the current `OpponentResolver` implementation and `finalize_opponent_resolution()` in `src/api/db.py` to understand what new error paths exist. Run the failing tests to see exact assertion mismatches. Update error count expectations in test assertions to match the current behavior. If mock data needs updating (e.g., to provide fields expected by `finalize_opponent_resolution`), update fixtures accordingly.

## Dependencies
- **Blocked by**: None
- **Blocks**: E-179-07

## Files to Create or Modify
- `tests/test_crawlers/test_opponent_resolver.py`

## Agent Hint
software-engineer

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests
