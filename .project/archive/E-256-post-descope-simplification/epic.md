# E-256: Post-Descope Simplification & Foundations

## Status
`COMPLETED`

## Overview
Retire the machinery the reports-first reframe made vestigial, and lay the foundations the project never grew. The disk-based twin load flow is production-dead but actively maintained — and E-247 shipped a stat-wiping regression on the LIVE path *introduced purely to preserve parity with the dead path*. Meanwhile there is no CI, no `.dockerignore`, two known-CVE pins with no refresh mechanism, backups nothing schedules, a deploy runbook that is dead on arrival, and — most consequentially — a reports flow that is **never actually driven at closure**, which is how a physically-impossible FPS stat reached a live report and a rest-day UTC bug slipped between two epics that both closed green.

## Background & Context
Two source documents drive this epic, both uncommitted at the repo root and both **read at planning time, not summarized**:

- **`PLATFORM-AUDIT.md`** (2026-07-03) — this epic is its candidate epic **CE-6**, absorbing the audit's 6 medium and ~12 low findings in the "simplification & foundations" cluster.
- **`AGENTIC-FLOW-REVIEW.md`** (2026-07-07) — §3.7 and §6 rows 8, 10, and 12 specify the closure runtime smoke, the CI slice, and the PII gate respectively.

The audit's upheld REVISIT decision — retiring the stored `player_season_*` tables in favor of query-time derivation — was **split out of this epic** into **E-259** by user decision (2026-07-09). Different owner, different risk profile, and a strict ordering constraint (Technical Notes §1).

E-256 also **promotes IDEA-102** (`.project/ideas/IDEA-102-committed-artifact-pii-gap.md`), whose own "cheapest lever" open question — extend the byte-gate to sweep `epics/` + `.project/` against the real denylist — is precisely what story 14 implements.

**This epic is audit-derived, not roadmap-derived.** It implements no `docs/ROADMAP.md` §5 slice (A–E are all COMPLETED). Per the §0 convention it therefore carries **no `## Roadmap` section and adds no §0 tracking row.**

**Prerequisite (E-257, satisfied).** `bb report reconcile-scoreboard --json` exists (commit `8b763a1`) and emits the axis-counter keys `dropped_pitch_events` / `no_plays_units` / `self_games` that the closure smoke consumes. The strict E-257→E-256 order recorded in the prior stub is discharged.

## Goals
- Delete the production-dead disk twin load flow and the dead-code residue the descope left behind (stories 01–03).
- Extract a client-free lifecycle/deletion module from `generator.py` and correct the third orphaned UTC site (stories 04–05).
- Stand up the absent foundations: `.dockerignore`, CI, dependency refresh, ruff, scheduled off-disk backups (stories 06–10).
- Give the reports flow its **first live runtime gate at closure** (story 11).
- Close the committed-artifact PII gap for planning trees and document the suppressor scope honestly (stories 13, 14, 16).

## Non-Goals
- **The query-time season-aggregate cutover.** Split to E-259 by user decision. E-256 must not change aggregate read/write semantics.
- **Removing `epics/` or `.project/` from `SKIP_PATHS`.** See Technical Notes §6 — the two PII gates catch disjoint classes, and this would reintroduce the noise TN-2 removed.
- **A blanket `tests/**` exemption in the PII scanner.** Ruled out; Technical Notes §6.
- **Deleting `scripts/smoke_test.py`.** Ruled out; Technical Notes §5.
- **Retargeting `smoke_test.py` at reports-era endpoints.** Real finding, captured as IDEA-109. Not this epic.
- **Chasing the retired suffixed-season taxonomy to its root.** Story 02 removes one more leaf of it. The root (on-disk `data/raw/<year>-<suffix>/` trees) is a standing user ask tracked separately. **Capture, do not chase.**
- Bug-fixing the live data path (that was CE-3 / E-253). The rest-day fix (story 05) is the sole exception: it is the orphaned third site of a systemic finding whose other two sites E-252 and E-253 already fixed.
- mypy. Explicitly deferred; ruff only.

## Success Criteria
- All stories DONE and the full suite green in the main checkout at closure.
- `.github/workflows/ci.yml` runs pytest + PII sweep + lockfile-drift + `docker build` on push.
- A fresh `git clone` builds via `docker compose build` with no missing-`data/seeds/` failure.
- `implement/SKILL.md` defines Step 1d, and `code-reviewer.md` authorizes CR to run it.
- No module under `src/` reads `data/raw/`.
- `pip-audit` reports zero known vulnerabilities for jinja2 and starlette.

## Stories
| ID | Title | Status | Dependencies | Assignee |
|----|-------|--------|-------------|----------|
| E-256-01 | Delete the disk-based twin load flow | DONE | None | software-engineer |
| E-256-02 | Delete the dead backfill-appearance-order command | DONE | None | software-engineer |
| E-256-03 | Dead-code sweep: bridge, discover_opponents, ghost dirs | DONE | None | software-engineer |
| E-256-04 | Extract a client-free lifecycle module from generator.py | DONE | E-256-03 | software-engineer |
| E-256-05 | Fix the rest-day reference date at all three UTC sites | DONE | E-256-04 | software-engineer |
| E-256-06 | Add .dockerignore; delete the orphaned data/seeds surface | DONE | None | software-engineer |
| E-256-07 | Dependency refresh and vulnerable-pin remediation | DONE | None | software-engineer |
| E-256-08 | Adopt ruff (F-class) | DONE | E-256-01, E-256-02, E-256-03, E-256-04, E-256-05, E-256-07 | software-engineer |
| E-256-09 | CI workflow (the static gate) | DONE | E-256-06, E-256-07, E-256-08, E-256-14 | software-engineer |
| E-256-10 | Operator docs/admin updates (backups + smoke pointer + seeds + backfill doc eviction) | DONE | E-256-02, E-256-06, E-256-11 | docs-writer |
| E-256-11 | Closure runtime smoke (Step 1d) | DONE | E-256-05, E-256-06 | claude-architect |
| E-256-12 | data-model.md dead-by-descope note | DONE | None | claude-architect |
| E-256-13 | Document the PII suppression mechanisms + choice hierarchy (12a-i) | DONE | None | claude-architect |
| E-256-14 | Gate planning artifacts with the doc-PII byte-gate | DONE | None | claude-architect |
| E-256-15 | Evict context-layer references to deleted surfaces | DONE | E-256-01, E-256-02, E-256-04, E-256-12 | claude-architect |
| E-256-16 | Eliminate the two pre-existing PII pattern hits (12a-ii + proxy false positive) | DONE | None | software-engineer |

## Dispatch Team
- software-engineer
- claude-architect
- docs-writer

## Technical Notes

### §1. Ordering with E-259 (strict: E-256 first)
E-259 (query-time season aggregates) executes **after** this epic. Three correct reasons, and one commonly-given reason that is **wrong**:

- **Correct.** The two epics collide on `_query_batting` / `_query_pitching`. Story 04 leaves them at a clean seam; E-259 rewrites them there.
- **Correct.** E-259 is the highest-risk read-path change in the backlog, and is exactly the populated-DB DELETE+rebuild regression class that fresh-DB goldens provably cannot see (E-247 F1). Step 1d should exist before it lands.
- **Correct.** Between E-256's close and E-259's DROP the stored tables **still exist**, so cache staleness remains a real and detectable divergence class — **and E-256's own closure sits inside that window.** Step 1d's `verify-aggregates` sub-check guards that interval. Omitting it would leave the interval unguarded in order to pre-pay for a deletion that has its own story.
- **WRONG — do not write this.** "E-259 could not pass its cutover gate otherwise." E-259's operator gate depends only on `bb report verify-aggregates` **existing in the tree**, not on Step 1d naming it.

### §2. It is Step 1d, not Step 1c
`.claude/skills/implement/SKILL.md:420` already defines **Step 1c** as the Closure CR Integration Review. The runtime smoke is **Step 1d**, wired into Step 8 as sub-step **5b**. Both `AGENTIC-FLOW-REVIEW.md` and the prior E-256 stub say "Step 1c"; both are wrong. This file corrects the stub. `AGENTIC-FLOW-REVIEW.md` is an uncommitted root artifact and is **not modified by this epic** — flagged, not corrected.

### §3. Step 1d design constraints
- **Runs in the main checkout, post-patch-apply**, alongside the Step 8 full-suite gate. The epic worktree has no `bb`, no Docker, no `.env`, no `data/`.
- **Runs against the LIVE dev DB, not a fixture DB.** A fresh fixture DB is empty, so `verify-aggregates` and `reconcile-scoreboard` would both pass **vacuously** — precisely the failure class Step 1d exists to close.
- **`bb report generate` runs BEFORE `reconcile-scoreboard`**, so the ratchet measures the state the smoke just produced.
- **Honest limitation, stated in the skill text.** Whole-game plays idempotency means the scoreboard cannot see an ingestion-parser change until data is re-ingested. Do not oversell the gate.
- **The smoke fixture is a gitignored two-field `.smoke-fixture` file at the repo root — NOT `.env`** (CA Q4). It holds `generate=<public_id>` (for `bb report generate`) and `morning-run=<lsb-url-1> <lsb-url-2> …` (positional team URLs for `bb report morning-run --dry-run`, which requires them per `report.py:608`) — both LSB's own real identifiers, neither committed. `.claude/hooks/secret-read-guard.sh` denies any Bash command literally naming `.env*` or `secrets/**`, so a fixture stored there would be unreadable by the reviewer that must read it. Morning-run's asserted invariant is **exit 0 only** (resolve-and-preview ran without crashing); its honest limitation (LSB usually has no games on an arbitrary closure date, so the resolution ladder doesn't fire — it gates entry-point wiring + schedule-read API path + `operating_today()` timezone plumbing, not end-to-end resolution) is stated in the skill text.
- **env-FAIL vs epic-FAIL.** Step 1d opens with a preflight (fixture present, stack up, creds live, baseline exists). **Preflight failures escalate to the user and hold the closure; they do NOT enter the remediation loop.** Only post-preflight failures are epic-FAILs. Without this split the smoke manufactures false remediation rounds on expired credentials.
- **CR self-evaluates the trigger.** The main session assigns Step 1d unconditionally; CR runs `git diff --cached --stat main` itself and reports "not triggered" when nothing matches. Routing that read through the main session would be a `dispatch-pattern.md` domain-work violation.
- **Trigger paths:** `src/reports/`, `src/db/`, `src/api/`, `src/gamechanger/loaders/`, `src/gamechanger/parsers/`, `migrations/`.
- **Two carve-outs are required in `.claude/agents/code-reviewer.md`, or the smoke is self-contradicting:** a named second exception in the **Test-Execution-Constraint** (beside the existing closure-pytest carve-out), AND an exception to **Anti-Pattern #5's read-only Bash allowlist** (`bb report generate` mutates the DB).
- Doc pointer for the smoke procedure: `docs/admin/production-deployment.md`.

### §4. The disk-flow deletion is a re-point, not a mass delete
159 test functions enter through a disk surface. **The large majority are re-pointed**, not deleted, at `load_payload` / `load_from_data`; their subjects (boxscore parsing, team-key detection, `_find_duplicate_game` dedup, perspective tagging) are unaffected by which surface feeds them. **The gate is the criterion, not the count** — any number in this section is guidance.

**COUNT CORRECTED DURING DISPATCH (2026-07-09, PM ruling).** The "nine" below **undercounts; the real figure is ~20.** The seed list censused only tests calling the *public* disk entry points. Deleting those entry points also orphans the loaders' *private* disk helpers — `_build_opponent_name_lookup`, `_build_summaries_index`, `_parse_summary_record`, `_load_boxscore_file`, `_read_json`/`_read_json_list`, `_build_opponent_name_index`, `_build_games_index` — which carry direct test coverage and have **no in-memory counterpart to port to**. Leaving them behind as dead file-reading code would defeat the story. **The list below is a SEED, not a ceiling** (same failure mode as §15's eviction hand-list — IDEA-115).

**The deletion criterion (authoritative, mirrored in story 01 AC-3):** a test is deleted iff BOTH (a) its assertion is about filesystem behavior **or** its subject is a private disk helper this story deletes, AND (b) **the behavior it asserts does not survive on the in-memory path.**

**"Total assertion coverage must not drop" means coverage of behavior that STILL SHIPS** — it is not an invariant on the raw assertion count. Deleting a test for code that is itself deleted does not drop coverage. Clause (b) is what enforces this, and it bites in exactly one verified place: `_parse_summary_record` is a *pure* dict→`GameSummaryEntry` parser (not a disk helper), and its `_opt_int` NULL-preservation semantic (E-253-06 AC-3) is **re-implemented inline** on the surviving path at `scouting_loader.py:408-422`. The function dies; the semantic ships. Story 01 AC-6 requires that assertion to exist against `_build_games_index_from_data` before the old test is deleted. Verified-dead by contrast: `_build_opponent_name_lookup` (reads `opponents.json`/`schedule.json`, keyed by `progenitor_team_id`) has no live analogue — the in-memory path uses `ScoutingLoader._build_opponent_name_index_from_data` over a different payload shape and passes `opponent_name` straight into `load_payload` (`scouting_loader.py:107,520`); and `_build_summaries_index`'s **dual-key** (`event_id` + `game_stream_id`) resolution has no live analogue either — `_build_games_index_from_data` is single-key (`event_id == game_stream_id == game.id`).

The original nine-item seed list (filesystem-asserting, SE-verified by reading each candidate, not by regex — the regex pass produced false positives):
- `tests/test_loaders/test_game_loader.py` (note the `test_loaders/` subdir): `test_no_games_dir_returns_empty_result`, `test_missing_summaries_file_returns_error`, `test_nonexistent_boxscore_file_returns_error`
- `tests/test_plays_loader.py`: `test_load_result_no_plays_dir`, `test_empty_plays_dir`
- `tests/test_scouting_loader.py`: `test_e247_disk_path_matches_in_memory`, `test_e247_disk_malformed_roster_counts_error`, `test_e247_disk_non_array_roster_counts_error`, `test_e247_disk_missing_roster_no_error`
- `tests/test_scouting_spray_loader.py`: `test_load_all_no_dirs_returns_empty_result`

**PORTS (not a deletion):** `tests/test_scouting_spray_loader.py::test_unknown_public_id_skips_directory` — its subject is `public_id` resolution, which `load_from_data` also performs, so it re-points rather than dies (SE's lean, committed per Codex's ten-vs-borderline reconciliation). This is why the count is nine, not ten.

Note the signal: **four of the ten are E-247's own guard tests for disk/in-memory parity.** The twin is exactly what E-247 was defending, at the cost of a stat-wiping near-miss on the live path.

### §5. `scripts/smoke_test.py` STAYS — and the flow review's binary is false
`AGENTIC-FLOW-REVIEW.md` says "wire it into CI or delete it." **Both branches are wrong.** It cannot go in CI (needs a live app + credentials) and must not be deleted: `tests/test_script_entry_points.py:102` tests it, and `docs/admin/operations.md:884` prescribes it as a credential-expiry recovery step. It remains a **manual operator step** and does **not** become a Step 1d command. Story 09 records this verdict explicitly so the next reader does not reopen it.

Separately, SE found that all three endpoints it probes (`/me/teams`, `/teams/{id}/game-summaries`, `/teams/{id}/players`) are **member-team endpoints the reports product abandoned in E-239** — so it proves credential liveness against a surface nobody uses, while `bb creds check` already proves liveness via a real authenticated `/me/user` call (`credentials.py:175`). Retargeting it is real work, captured as **IDEA-109**, and is **out of scope here**.

### §6. The two PII gates catch disjoint classes
- The **pattern scanner** (`src/safety/pii_scanner.py`) matches *shapes*. A 10-digit run looks like a phone number whether or not it is one — exactly the noise that put `epics/` and `.project/` into `SKIP_PATHS` (rationale comment tagged TN-2 at `src/safety/pii_patterns.py:168`).
- The **byte-gate** (`scripts/check_doc_pii.sh`) matches *literal known identifiers* from a denylist. A literal match in `epics/` is **never noise** — it is a real identifier in a committed file.

The byte-gate therefore extends coverage to the planning trees **without a single false positive**, and `SKIP_PATHS` **keeps** `epics/` and `.project/`. Story 14 must not modify `src/safety/pii_patterns.py`.

**Suppression mechanisms (both already shipped in E-254-06) and their TRUE scope:** per-line `# pii-ok` (`pii_scanner.py:151-153`, checked *before* the pattern loop) and the whole-file `synthetic-test-data` marker (`SYNTHETIC_MARKER`, `pii_scanner.py:145-148`, evaluated before the per-line loop). Both sit OUTSIDE the `COMPILED_PATTERNS` loop, so **both suppress ALL patterns including credential patterns** (`bearer_token`, `api_key_assignment`), not just shape heuristics — despite the names. This is by design and load-bearing (a synthetic auth fixture legitimately contains `Authorization: Bearer test-token-…`, which matches `bearer_token`). But it means a real credential behind either marker is silently unscanned — so a real credential value behind either marker is a **MUST FIX**, not a sanctioned suppression.

**Choice hierarchy (story 13 records this in `pii-safety.md`):** (1) **change the data** so no pattern matches — always preferred; leaves no standing suppression; (2) **line-scoped `# pii-ok`** on the one offending line; (3) **file-level `synthetic-test-data`** only for an end-to-end synthetic fixture, and **NEVER on a file that handles, parses, or could receive real credentials**. The credential-parser test file is the worst possible place for a file-level suppressor precisely because it is the file most likely to receive a real token when a dev pastes a curl to reproduce (story 16's fix follows this: change the fake value, not add a marker).

**A blanket `tests/**` exemption is rejected on evidence** (shape heuristics fire zero times) and on the visibility-to-review axis: a marker is a **literal token in the diff** that the reviewer sees (CLAUDE.md §4g then tells CR to scrutinize it), so suppression is an explicit, reviewable authorial act. A path exemption is **invisible in the diff** — every future fixture under `tests/` is silently unscanned for credentials forever, no author intent recorded, nothing for CR to look at. Independently, `email` / `bearer_token` / `api_key_assignment` MUST keep running on `tests/**` (CLAUDE.md names "test fixtures using real credentials" a MUST FIX), so any carve-out would have to be per-pattern — strictly worse than a marker.

**§4g is a review-time control, not a structural closure (CA ruling).** The reviewability argument above is why a marker beats a path exemption — but §4g does NOT close the staged-blob hole where a `GC_REFRESH_TOKEN=<real> # pii-ok` line is staged, scanned, certified clean, and never reviewed (an ad-hoc operator commit, an unread file, or a marker added in one epic with a real token added to that file epics later). Treating §4g as a closure would be the same category error as accepting Cloudflare as CSRF mitigation. That residual is a real hole, captured as **IDEA-112** (a measurement-first suppressor-narrowing idea — NOT this epic).

### §7. Byte-gate exit-code disposition in the commit hook
The pre-commit hook does **not** invoke the byte-gate today (`.githooks/pre-commit:21` runs only `python3 -m src.safety.pii_scanner --stdin`), so exit-3 semantics are a design decision this epic makes, not a live contradiction.

| Exit | Meaning | Hook behavior |
|---|---|---|
| 0 | REAL mode, zero matches | Pass; emit `[doc-pii: REAL, 0 matches]` |
| 1 | Denylisted identifier present | **BLOCK** |
| 2 | Self-test failed / malformed denylist | **BLOCK** (fail closed) |
| 3 | Denylist absent → EXAMPLE MODE | **Announce loudly, do NOT block**; emit `[doc-pii: INCONCLUSIVE — example mode]` |

`.claude/rules/pii-safety.md` says exit 3 "MUST NOT be recorded as a pass" — it does **not** say "must block." Emitting a *different confirmation token* satisfies both: **the announcement IS the not-recorded-as-a-pass.** Exit 2 blocking follows the `pii-check.sh:37-58` precedent (a broken scanner must not let a commit through).

Blocking on exit 3 would destroy the gate. The denylist is gitignored, so **exit 3 is the default state on every fresh clone**; a hook that makes a fresh clone uncommittable is uninstalled within a day, and then nothing runs — including the exit-1 detection that is the entire point. **CI never invokes the byte-gate**, so exit 3 never arises on a runner.

### §8. Byte-gate wrapper: the index-vs-working-tree hazard
`scripts/check_doc_pii.sh` takes **exactly one** positional arg (`TARGET="${1:-}"`; extras silently ignored) and **requires a directory** (`[ ! -d "$TARGET" ]` → exit 2). All three matchers are `grep -r`. Multiple trees are handled by invoking it once per tree and aggregating exit codes — **no script change**.

The real hazard: **the byte-gate greps the WORKING TREE, but a commit hook must judge the INDEX.** These diverge under `git add -p`, or staged-then-edited. A working-tree grep in a commit hook lets a real identifier ride into the commit while the gate greps a clean working copy — a **false pass**, strictly worse than the exit-3 inconclusive we chose to tolerate.

Design: `git checkout-index --prefix="$tmp/" -a` into a `mktemp -d`, invoke `check_doc_pii.sh "$tmp/<tree>"` once per tree, aggregate exit codes, `rm -rf`. The temp path is a directory, so the `-d` guard passes. **Zero modification to `check_doc_pii.sh`** — its single-directory contract, self-test, and four exit codes all survive. The wrapper lives in `.githooks/pre-commit`, where the staged-vs-worktree concern belongs. Do **not** teach the script a `--staged` mode; that duplicates `git checkout-index` inside a script whose entire value is being a small, self-testing, PII-free harness.

### §9. `data/seeds/` — DELETE, do not commit
Git forensics (SE): commit `cf176e8`'s message claims it deleted `seed_dev.sql`, but `--diff-filter=D` shows it deleted **only `scripts/seed_dev.py`**. The SQL file was **never tracked** — the author's local `rm` left no git trace, so the file survives on every pre-E-228 machine while `git ls-files data/` is empty. `cf176e8` touched neither `Dockerfile` nor `.gitignore`; that is the orphan.

Committing it would resurrect ~21 KB of demo data E-228 removed on purpose, and contradict the documented `bb db reset` empty-DB invariant (`.claude/rules/data-model.md`).

### §10. `backup.py` — the audit's stated reason is wrong
"Invoked by nothing" is **false**: `src/db/backup.py::backup_database` is the live `bb db backup` command (`src/cli/db.py:11,43`), with `scripts/backup_db.py:40` a second wrapper, prescribed in three docs. A story author working from the audit's wording would hunt for a caller that already exists and conclude the finding was stale.

The real finding is two-part: (a) **nothing schedules it**, and (b) `_BACKUPS_DIR = _PROJECT_ROOT / "data" / "backups"` (`src/db/backup.py:20`) is **inside the same host-mounted `./data` volume as `app.db`** — one disk loss destroys the database and every backup together.

### §11. Dependency refresh moves ~11 DIRECT packages (33 total), not 2
**Scope corrected at dispatch (story-07 verification).** "~11" below counts **direct** deps. The true resolve is **30 changed + 2 added + 1 removed = 33 package moves**, including three MAJOR transitive bumps (`cryptography 46→49`, `pyopenssl 25→26`, `rich 14→15`) and a **webauthn crypto backend swap** (`pyasn1`/`pyasn1-modules` added, `asn1crypto` removed) — in the auth path. Direct deps landed exactly on the validated targets below. State the 33 figure, not 11, or a reviewer reads the lockfile diff as scope creep.

**The vulnerability count was also understated by 16×.** HEAD's `requirements.txt` carried **32 known vulnerabilities across 10 packages**, not the 2 CVEs this epic named. `starlette 0.41.3` alone carried 8 advisories (the named `PYSEC-2026-1942` Range-parser DoS plus `CVE-2026-48818` / `CVE-2026-48817`); also `python-multipart` ×6, `pillow` ×6, `cryptography` ×5, `pyopenssl` ×2, and cbor2/idna/jinja2/pygments/python-dotenv. The recompiled lockfile audits **clean**. The named CVEs were the reason to look, not the extent of the problem — a general lesson for pin-remediation stories.

SE ran the full suite against the upgraded stack in an isolated venv: **`3849 passed, RC=0`**. Target: `fastapi==0.139.0` + `starlette==1.3.1`, plus jinja2 3.1.6, matplotlib 3.11.0, numpy 2.5.1, uvicorn 0.51.0, typer 0.26.8, webauthn 2.8.0, python-multipart 0.0.32. FastAPI 0.139 dropped its starlette upper bound entirely, permanently un-sticking future starlette patches. Only `requirements.in:9` (`starlette~=0.41`) needs widening — `fastapi~=0.115` already permits 0.139.

The matplotlib 3.10.8→3.11.0 move **resolves** the existing drift (the suite already runs 3.11.0 via the unpinned devcontainer editable install). State the ~11-package scope in the story or a reviewer reads it as scope creep.

### §12. ruff scope
**17 F-class violations in `src/`** (8 F401, 5 F541, 4 F841), not the audit's 12; 22 including `scripts/`. Repo-wide 96, of which **6 are false positives** — F821 undefined-name in `tests/test_cli_creds.py`, all string return annotations paired with function-local imports, never evaluated at runtime; the clean fix is a `TYPE_CHECKING` block. Config: `[tool.ruff.lint] select = ["F"]`, scoped initially to `src/` + `scripts/`. Two F841s sit in code story 02 deletes (`backfill.py:184-185`); story 02 confirmed both via `ruff --select F841` before deleting the file, so story 08 need not address them.

**§12 CORRECTION (2026-07-09, story-02 verification).** §12 named only ONE of the two SURVIVING F841s. Both are now named, and both are PM-verified by reading the code:
- `src/api/routes/auth.py:648` — `existing_creds` assigned, never used.
- **`src/reports/starter_prediction.py:1127`** — `latest_game_date = all_dates[-1] if all_dates else ""`, assigned, never used (verified: the only other `latest_game_date` occurrence in the file is that assignment).

**Story 08 MUST NOT silently delete `starter_prediction.py:1127` — route it to story 05 first.** Its comment reads *"Latest game date for rest calculations,"* yet every rest calculation in that module anchors on the caller-supplied `reference_date` (`:456`, `:581`, `:664`, `:683`, `:995`). It is the **vestige of an intended fix for exactly the bug story 05 exists to correct** — the rest-day reference date. Deleting it as lint would erase the only in-code trace of that intent. Story 05 runs before story 08 (08 blockedBy 05), so 05 sees it first: story 05 decides whether the rest anchor should be the latest game date, and 08 then removes whatever 05 leaves dead. **mypy is explicitly skipped.**

### §13. `generator.py` split (lifecycle extraction)
`generator.py` is **3,034 lines** (not the stub's 2,792). Target: `src/reports/lifecycle.py`, client-free, ~640 lines — the contiguous deletion tail (`generator.py:2577-3034`) plus the scattered lifecycle helpers. SE verified **zero** references to any heavy import (`GameChangerClient`, crawlers, loaders, `render_report`, `reconcile_game`, `parse_team_url`, `CredentialExpiredError`) across the moved regions. `_utcnow_iso` is publicized as `utcnow_iso`, killing the cross-module underscore imports at `morning_run.py:52` and `reports_admin.py:541` in one stroke. SE asks for **two commits** — contiguous tail first — because `generate_report` sits at `:1655` *between* the two blocks.

### §14. The season-fetch seam — DE's relocation contract (E-256 relocates the fetch; E-259 substitutes the SQL)
**FINAL contract (option (c) was rejected).** SE conceded to DE after verifying in code that `_query_batting` ends `_apply_name_cascade` and `_query_pitching` ends `_apply_name_cascade` + `_compute_pitching_rates` (which writes the display strings `era`/`k9`/`whip`/`strike_pct`). SE's earlier objection rested on a false premise — E-259 does NOT supersede the relocation; it substitutes the *SQL body inside* the already-relocated pure function, so the move is a **prerequisite**, not wasted work.

- **E-256 (story 04)**: extract the pure SQL fetch into `src/api/db.py` as `get_season_batting`/`get_season_pitching(conn, team_id, season_id) -> list[dict]`. `_query_batting`/`_query_pitching` **stay in `generator.py`** as thin wrappers: fetch → `_apply_name_cascade` → (pitching) `_compute_pitching_rates`. The presentation helpers stay in `src/reports/`.
- **E-259 (its story 01)**: rewrite ONLY the SQL body inside `get_season_batting`/`get_season_pitching` against `player_game_*`, adding the `perspective_team_id` filter, in one hunk.

**Why (c) loses (record verbatim):** E-259's whole risk is one silent semantic change — the perspective filter, whose failure is a plausible-looking double-count. Under DE's contract E-259's diff is **old-SQL-vs-new-SQL side by side**. Under (c) E-259 would be a delete-from-`generator.py` + add-to-`db.py`-with-different-SQL, which git renders as a delete plus an unrelated-looking add — the reviewer **never sees the old-vs-new SQL diff at all**. Relocate where relocation is cheap (E-256); substitute where the diff must be legible (E-259).

**Acyclicity (record verbatim):** the *fetch-only* move is what keeps the import graph acyclic. `src/api/db.py` is a clean stdlib + `src.db.paths` leaf and the pure fetches add no imports. A **wholesale** move (dragging `_apply_name_cascade`/`_compute_pitching_rates` into `db.py`) WOULD create a cycle, because those are report-layer helpers and `generator.py:29-35` already imports *from* `src.api.db`. DE's split doesn't just improve layering — it is what prevents the cycle. So the fetch relocates; the presentation helpers do NOT.

**SE's two clarifications to DE's contract (both adopted):**
1. **Dict-keys scope qualifier (critical — prevents an implementer error).** "Do not change the returned dict keys" means *the wrapper's* keys. `get_season_pitching` returns rows **WITHOUT** `era`/`k9`/`whip`/`strike_pct` — those are created by `_compute_pitching_rates`, which stays in reports. Stated loosely, an implementer could conclude the fetch must return `era` and drag the formatter along (re-introducing the cycle). The fetch returns the **raw SUM columns**; the *wrapper* output is golden-stable.
2. **ORDER BY is an E-259 flag, not E-256.** Both clauses currently order over stored aggregate columns (`_query_batting`: `(ab+bb+hbp+shf) DESC, last_name ASC`; `_query_pitching`: `COALESCE(psp.ip_outs,0) DESC, last_name ASC`). In E-256 this is a **pure relocation** (unchanged). In E-259 they become expressions over per-game SUMs — E-259 must **reproduce the same ordering semantics over the new projection**. Named in E-259's story now so it is not discovered mid-cutover.

**Golden test:** zero-diff through both epics, via the **wrapper** (not the fetch). `tests/test_report_golden.py` calls `_query_batting`/`_query_pitching` and sees the composed output, which is stable across relocation (E-256) and SQL-swap (E-259). **Neither epic may touch the golden test to make itself pass.**

E-256's contract for story 04's fetch relocation:
1. Extract `get_season_batting`/`get_season_pitching(conn, team_id, season_id) -> list[dict]` into `src/api/db.py`, returning the **raw SUM columns** (no `era`/`k9`/`whip`/`strike_pct`).
2. `_query_*` stay in `generator.py` as thin wrappers: fetch → `_apply_name_cascade` → (pitching) `_compute_pitching_rates`.
3. Keep the ORDER BY clauses and dict keys **as they are** — pure relocation, no semantic change; the perspective filter and the SUM-expression ORDER BY are E-259's.
4. Do **not** move `_apply_name_cascade`/`_compute_pitching_rates` — they stay in `src/reports/` (acyclicity).
5. Do **not** delete `batting_recompute_select()`/`pitching_recompute_select()` from `src/db/season_aggregates.py` — E-259 uses them so exactly one SUM projection survives.
6. **Golden test unchanged — no import edits, no expectation edits, zero diff.**

### §15. Backfill-appearance-order eviction is grep-and-reconcile, routed by ownership (NOT a hand-list)
Deleting `bb data backfill-appearance-order` (story 02) requires evicting its references across code, docs, context layer, and agent memory. Codex round 2 proved the hand-listed set undercounts: the story's original 2 surfaces → Codex's +3 → PM's whole-repo grep found ~11. The durable fix (IDEA-115): **story 02 runs the authoritative repo-wide grep** (`backfill.appearance.order` across `src/ scripts/ tests/ docs/ .claude/ CLAUDE.md docs/ROADMAP.md`), reconciles the surfaces it owns, and routes the rest. **The list below is a SEED, not a ceiling** — the grep at implement time is authoritative; a reference not listed here that the grep finds is still in scope.

Routing table (owner ← surface):

| Owner (story) | Surfaces (seed) |
|---|---|
| **software-engineer (02)** | `src/cli/__init__.py` (command-list docstring); `src/db/backfill_game_dates.py`, `src/gamechanger/loaders/plays_reload.py` (precedent docstrings — re-point to a surviving precedent); `tests/test_cli_data.py` (command-list references); plus the deletion-target files 02 removes outright |
| **docs-writer (10)** | `docs/admin/operations.md` (backfill runbook section + precedent refs), AC-6 |
| **claude-architect (15)** | `CLAUDE.md` (Commands sentence + Footgun note); `.claude/rules/data-model.md` (mention + `verify-aggregates` footgun), AC-3 |
| **owning agent (closure)** | `.claude/agent-memory/code-reviewer/recompute_tail_noop_populated_db.md`; `.claude/agent-memory/data-engineer/season_tables_are_a_pure_cache.md`; **`.claude/agent-memory/data-engineer/season_aggregate_writers.md`** (PM-surfaced during story-01 verification: cites `load_team` at `:159` and `_compute_season_aggregates` at `:643` — story 01 shifted both. Line-number citations into `src/` go stale silently; reconcile or de-line-number them) — reconciled by each owning agent; since neither agent is on E-256's team, PM flags them for a main-session follow-up sweep at closure (reconcile-not-strike: a reference is a candidate, not an automatic strike) |
| **product-manager (closure)** | `docs/ROADMAP.md:219,376` (the "QUARANTINE, deletion-guarded" cruft verdict + "survives" note → annotate "QUARANTINE → DELETED by E-256"); **`docs/ROADMAP.md:100-103`** (§2 "What's weak" — the *present-tense* claim "**Loaders are Path-only, bridged by temp files**", naming `PlaysLoader.load_all(Path)` and `GameLoader.load_file()`. Stale TWICE: the temp-file bridges were removed by E-237, and story 01 deletes the wrappers themselves. Annotate as resolved — do not silently delete a §2 assessment row. CR-surfaced during story-01 review); PM's own `.claude/agent-memory/product-manager/MEMORY.md` (Memory Retirement); **`.claude/agent-memory/product-manager/archived-epics.md:38`** (E-204's entry names the concept as "**backfill CLI**" — no `backfill-appearance-order` token, so no token grep finds it. Surfaced by story 02's concept sweep. Reconcile-not-strike: E-204 *did* ship a backfill CLI; the entry is history, and history stays true. Annotate as removed by E-256, do not rewrite the past). ROADMAP is strategic/PM-owned — docs-writer must not edit it. |

Doc-sweep discipline (`.claude/rules/doc-sweep.md`) applies to every prose surface: token grep + synonym expansion + semantic read.

## Open Questions
- None. The season-fetch seam (formerly the sole open question) is resolved as **DE's relocation contract** — E-256 relocates the pure fetch to `src/api/db.py`, E-259 substitutes the SQL body in place (Technical Notes §14). Option (c) was rejected.

## History
- 2026-07-04: Created as a DRAFT capture stub from the platform audit (CE-6).
- 2026-07-07: agentic-flow-review items registered; E-257→E-256 sequencing made strict.
- 2026-07-07: CA design review corrections applied.
- 2026-07-09: Refined to full DRAFT. Aggregate cutover split out to E-259 (user decision). E-257 landed, discharging the upstream dependency. Expert consultation completed with software-engineer, data-engineer, and claude-architect. Flow-review item 12 pulled into scope (user decision). Promotes IDEA-102. Captures IDEA-109/110/111.
- 2026-07-09 (final rulings): **Season-fetch seam resolved as DE's relocation contract** — E-256 relocates the pure fetch to `src/api/db.py`, E-259 substitutes the SQL body in place (option (c) rejected; Technical Notes §14). **Item 12a split into two stories** by routing: 13 (claude-architect, `pii-safety.md` + choice hierarchy) and **16 (software-engineer, `tests/test_credential_parser.py:81` fixture fix)** — 16 stories total. §4g ruled a review-time control, NOT a structural closure; the staged-blob residual captured as **IDEA-112** (measurement-first suppressor narrowing, sibling of E-254, NOT this epic).
- 2026-07-09 (internal review + Codex): CR spec audit + SE/DE/CA holistics + Codex spec-review incorporated. **CA routing rule (Q1)** applied — no story carries a mandatory cross-domain edit: E-256-02's CLAUDE.md edit folded into story 15 (CA); the `docs/admin/` edits from stories 06 (architecture.md seeds) and 11 (production-deployment.md smoke procedure) consolidated into **story 10** (docs-writer), which is retitled "Operator docs/admin updates" and is now the epic's SOLE `docs/admin/` owner. Same-file collision edges added post-refactor: 11 blockedBy 06 (`.gitignore`), 15 blockedBy 12 (`data-model.md`), 10 blockedBy 06 (seeds line follows the surface removal); the 10↔11 `production-deployment.md` collision DISSOLVED (11 now only references the doc). Mechanical fixes: E-256-09 PII CI invocation pinned (bare command is a no-op), E-256-01 AC-3 reconciled to nine filesystem-asserting deletions (criterion is the gate, `test_unknown_public_id_skips_directory` ports), E-256-03 api-scout-skip rationale added (dead code). **CA Q4 (E-256-11):** `.smoke-fixture` widened to two labeled fields (`generate=` + `morning-run=`); morning-run `--dry-run` kept with an **exit-0-only** invariant + a stated honest limitation (gates entry-point/schedule-API/timezone plumbing, not the resolution ladder), order-independent after `curl /health`; preflight requires both fields non-empty (env-FAIL if missing). Story count unchanged at 16.
- 2026-07-09 (Codex round 2, 2 findings incorporated — circuit breaker, no round 3): **P1 dependency correctness** — the round-1 Q1 routing refactor made story 10 consume story 11's Step 1d smoke procedure but only added `10←06`; added `E-256-10 blockedBy E-256-11` (acyclic: 11←05,06, no back-edge). **P2 propagation completeness** — the `backfill-appearance-order` eviction was incomplete (story's 2 surfaces → Codex's +3 → PM's whole-repo grep ~11). Reframed story 02's eviction as an **authoritative repo-wide grep-and-reconcile routed by ownership** (new §15 routing table + AC-3): SE (02) owns the grep + its `src/`/`tests/` surfaces (`cli/__init__.py`, two precedent docstrings, `test_cli_data.py`); `docs/admin/operations.md`→story 10 (new AC-6, `10←02`); context-layer CLAUDE.md/data-model.md→story 15 (AC-3 tied to §15); agent-memory (code-reviewer, data-engineer) → owning-agent closure sweep (PM-flagged, off-team); `docs/ROADMAP.md:219,376` + PM's own memory → PM closure. Surfaces are a SEED not a ceiling (IDEA-115 captures the recurring hand-list-undercount lesson, routed to CA). Story count unchanged at 16.
- 2026-07-09 (**READY** — review scorecard). Set READY after two Codex iterations plus internal review (circuit breaker — no third round). Review-Scorecard:

  | Pass | Findings | Accepted | Dismissed |
  |---|---|---|---|
  | Internal iter 1 — CR spec audit | 7 (session) | 5 | **2** (CR F4 story-04 sizing; CR F6 morning-run `--dry-run` email — both refuted against code, `report.py:581`/`:666`) |
  | Internal iter 1 — holistic (SE/DE/CA) | SE 3, DE 3, CA 4 (GAP-1/GAP-2 + 2 nits) | all | 0 |
  | Codex round 1 | 8 | 8 | 0 (shaped by CA rulings Q1–Q4) |
  | Codex round 2 | 2 | 2 | 0 |
  | **Total (E-256)** | — | — | **2, both internal-CR (not Codex)** |

  Notes: the CR spec-audit and SE/DE/CA holistic passes were session-wide (both epics); the 2 dismissals are E-256 (CR false-positives). Codex rows are E-256-specific. Story count 16 unchanged across every pass. The only two Codex-round dismissals across the session were zero — every Codex finding (round 1 + round 2, both epics) was accepted or resolved via a CA ruling.
- 2026-07-09: **ACTIVE** — dispatch authorized; epic worktree created. Stories execute serially per the dependency graph.
- 2026-07-09 (dispatch, DONE flip recovered on resume): **E-256-06 DONE.** CR APPROVED with no MUST FIX; PM and CR independent AC verdicts match — **AC-1/AC-2/AC-3/AC-5/AC-6 PASS**, **AC-4 reassigned** (fresh-clone `docker compose build` is ungradeable in the worktree; discharged by story 09's CI `docker build` stage or the operator at closure — carried on the PM + main-session closure lists, an unchecked box means *reassigned*, not unfinished). `.dockerignore` created (exclude-only, env at any depth, recursive bytecode/build artifacts, `data/`+`.git/` root-anchored on purpose); `Dockerfile:41`'s `COPY data/seeds/` and its comment deleted, `:38`'s `chown` keeps its non-`/seeds` components; `.gitignore`'s dead `!data/seeds/` negations and the false `:38` "commit seed SQL files" comment deleted, `:37` repaired; `tests/test_docker_build_context.py` added (COPY targets **parsed** from the Dockerfile, both non-vacuity guards, `_excludes` zero-segment `**/` expansion, both-axes docstring, defect-pin test). **The DONE flip, this History entry, and CR's final APPROVED were lost to a dispatch interruption after PM round-3 PASS; recovered here on resume — the staged work was untouched.**

  **ACCEPTED SHOULD RESIDUE (three `.dockerignore` comment-anchoring groups — graded SHOULD, non-blocking, non-ceiling, accepted by both gates + main-session triage under the post-E-260 two-tier severity floor):** the round-3 accepted-residue inventory, now complete at three instances — (1) `:19-25` credentials-group header reads "RECURSIVE" but `secrets/`/`proxy/` beneath it are root-anchored with no anchoring note (CR's round-3-confirmation SHOULD FIX); (2) `:41-60` Python-artifacts header reads "RECURSIVE" but its tool-cache tail (`.venv/`, `.uv-cache/`, `.pytest_cache/`, `.ruff_cache/`) is root-anchored; (3) `:81-87` compose/Docker group carries no anchoring note. **None names a COPY target, none is a security pattern, none changes any effect AC-1 requires** — all three are functionally correct (single top-level dirs/files). Story 06's staged work is NOT reopened for a cosmetic comment both gates accepted; if a later story sends a round for another reason, fold in the anchoring comments then.
- 2026-07-09 (dispatch): **E-256-05 DONE.** The venue-local reference date is derived once and reaches all consumers; rest days and the 7-day `pitches_7d` window no longer inflate by a day after ~19:00 local. **AC-5 settled with a citation, not a shrug**: `latest_game_date` is the residue of a fix that ALREADY happened (`83b5458 feat(E-214)`), not the vestige of an intended one — PM's hypothesis was wrong, and the AC still earned its place by forcing the judgment. **PM ruled option (a) on the template skew SE escalated**: story 05 created a report that disagreed with itself about its own generation date (two sites venue-local, two UTC). *A report that disagrees with itself is worse than one uniformly wrong — a uniform error is invisible; a skew tells the coach the report is broken and gives no way to know which half.* Widened to AC-6; `expires_at` deliberately left a UTC slice (**"this story corrects a game-day anchor, not every UTC slice in sight"**). Six consumers, six independent falsifiers.

  **THE CLOSED-LOOP DEFECT (the epic's sharpest finding, and it indicts the spec author).** The story named three consumers; there were four — `generator.py:2336` *stored* the value into the render dict rather than *using* it on the line. SE's diagnosis: **"AC-2's grep produced the story's site list. The grep WAS the enumeration."** Guard and target therefore shared identical blind spots, and a green result was guaranteed for exactly the sites the grep could see. This is not "a method with a blind spot" — it is **a method that defined the target it later verifies**: the `x == x` tautology at spec-authoring altitude, and like that tautology it **cannot fail**. **Rule: the enumeration and the guard MUST have independent origins.** Ask not *"is this check good?"* but ***"did this check also write the list it is checking?"*** — when it did, green carries no information. CR's decision procedure makes it sort cleanly: AC-2's grep wrote its own list (**worthless**); story 04's byte-identity compared against the *index blob* (**informative**); story 03's mutation compared tests against a mutant they did not author (**informative**). *"Where did the expected value come from, and did the thing under test have a hand in producing it?"* **This leads closure item 3; "enumerate, don't aggregate" is its corollary.** Pre-committed remedies: story 08's ruff baseline must come from the index blob; **story 15's eviction target list must come from the deleted-symbol handoffs, not from a grep of the surviving prose** (now its AC-7).

  Third instance of the same shape, in test data: `_make_full_data` — docstring *"Build a complete report data dict"* — omitted `generation_date` and passed only because the footer sliced `generated_at`. SE fixed the **fixture**, not the assertion. CR's phrasing for unreachability claims, adopted: **"dead code in production, not dead code absolutely"** — name the paths you quantify over.
- 2026-07-09 (dispatch): **E-256-04 DONE** after two review rounds — the epic's highest-risk story. `generator.py` 3,031 → 2,356; new client-free `src/reports/lifecycle.py`; the pure season fetch relocated to `src/api/db.py` per §14's contract. **The golden test is zero-diff and `db.py` added zero imports**, so both brackets held. §13's `:2577-3034` range and its "3,034 lines" were both stale (authored pre-story-03); SE found the regions by symbol. **Ruling: cohesion over line range** — `_fail_report` stays in `generator.py`, because moving it would force a backward import from `lifecycle`, the edge AC-5 implies but never wrote down. The line range described where the constraint fell; the constraint is client-freeness.

  **A moved function resolves its NEW module's globals — and that defect class was two calls deep.** SE found and fixed it for `_REPO_ROOT`/`_REPORTS_DIR` (~50 patch sites; 11 real failures confirmed the seam). PM then surfaced the same class for `get_connection`, and CR traced it **one call earlier than PM did**: the sweep never opened the test DB at all, so `_REPO_ROOT` was never reached. 43 tests drove `generate_report` with zero lifecycle-side patches; the by-design exception swallow hid every occurrence. Safe only because the **live** `data/app.db` held zero qualifying rows. Fixed by connection injection (`_conn_scope`), not by documenting the hazard — **CR: "Should the call site carry a comment recording why it's safe? No, because it isn't."** PM's instinct to name the second-order guard was right; PM's prescription to write it down would have made the hazard durable and reassuring. PM then ruled `list_reports` **CLOSE, don't flag** (three lines, zero caller changes, same defect class left half-closed).

  **The transferable rule, in SE's words (CR concurs, supersedes both PM's and CR's drafts):** a method is useful exactly when its blind spot falls **outside** the space of things that can go wrong. *"The discipline isn't 'distrust your methods' — it's **know where each one stops seeing, and check whether the bug can live there.**"* SE's own failure was the case in point: it used 11 test failures as its search method, so it found exactly the defects that produce failures — and the `get_connection` defect **cannot** fail. CR's corollary: *"A falsification is only as strong as the set of things that could have falsified it."* **"Enumerate, don't aggregate" is the corollary, not the lead.**

  **CR named the two-gate design better than the skill does:** *"I was reasoning about whether the code is wrong; PM was reasoning about whether the code can be made wrong cheaply. Both are review questions, and the second is the one that keeps producing this epic's findings."*

  **The main session's own pre-stage control had the same blind spot**: `git diff --stat -- src/reports/lifecycle.py` returned empty because the 671-line new module was **untracked**, not unchanged. With PM's step-3 analysis (a consistency check cannot see consistent corruption) and CR's diffstat convention, that is three faces of one thing — **a control that cannot see its own blind spot reports the blind spot as clean.**
- 2026-07-09 (dispatch): **E-256-03 DONE** after two review rounds. `bridge.py`, `discover_opponents`, and `bb status`'s last-crawl panel removed; both `_utcnow_iso` copies consolidated into `utcnow_iso` + `UTC_ISO_FORMAT` in `src/util/timezone.py`. **PM's `status.py` rationale was FALSE**: the panel was not "vacuous" — the operator's gitignored host-mounted `data/raw/` still holds three pre-E-239 manifests, so `bb status` had been displaying a *real, months-stale* crawl date for a deleted pipeline. Actively misleading, not empty; the removal branch was right and PM's reason was wrong (reasoning from the symptom's location rather than the mechanism's — third instance this epic).

  **A live dispatch hazard was discovered, not just a defect.** SE ran mutation checks by writing mutants into `src/` **inside the shared epic worktree**. CR, building a sandbox, copied `src/util/timezone.py` mid-cycle and captured **Mutant A** — a dotted `UTC_ISO_FORMAT` — then re-read through two channels, found the live file clean with an mtime eight seconds after its `cp`, and did not report the phantom MUST FIX. Three faces of one hazard appeared in this single story: CR nearly reported a mutant as a defect; the main session relayed a timestamped "untouched" as current, making a landed fix look missing; and SE named the silent inverse — **a stale read can make a reverted fix look landed, closing a story on a fix that is not there, both gates green.** The staging boundary is the sharpest exposure: `git add -A` is a timed operation on a tree another agent may be mid-mutation on. **Standing constraint, effective immediately: mutation checks run against a copy OUTSIDE the worktree** (scratch dir + venv without the editable install). This also dissolves the read-only-Bash tension — the prohibited in-worktree write was never necessary. What prevented the fabricated defect was the clean-reread-before-defect discipline (`.claude/agent-memory/product-manager/feedback_clean_reread_before_defect.md`), which is now demonstrated to bind CR, not only PM.

  **Two review-quality refinements adopted.** (1) *"Enumerate, don't aggregate"* supersedes the seed-list-as-ceiling framing: nine times this epic a list was read as a ceiling, and once a **count** was read as a verdict (SE recorded "3 failed, 4 passed" as proof its tests were live, never asking *which four survived* — the `x == x` tautology was in the survivor list, guarding AC-4's exact property). The aggregate is never wrong; that is what makes it seductive. Rule for CA: **when a number stands in for a set, name the set before drawing a conclusion from the number.** The attached practice is *every test has a NAMED killer*. Its known limit (CR): an AST self-comparison sweep catches `assert f(x) == f(x)` structurally but not `assert f(a) == f(b)` where `a` and `b` hold equal values — a real net, not a complete one. (2) CR sharpened story 01's clause (b) into something checkable: **"a sibling covers the same behavior" and "the behavior dies with the function" are different arguments, and only the second is safe.**

  **PM overturned CR's and the main session's "tombstone" reading of `tests/test_app_import_isolation.py` by reading the file** — its docstring already declares itself a standing negative guard, and it remains falsifiable (re-introduce a `src.pipeline` import and it trips). *"Tombstone" conflates* asserts something currently true *with* cannot be made false*; only the second is a defect. Most good regression guards pass forever.* Kept unchanged, no follow-up.
- 2026-07-09 (dispatch): **E-256-07 DONE** after two review rounds. HEAD's `requirements.txt` carried **32 known vulnerabilities across 10 packages**, not the 2 the epic named; the recompiled lockfile audits clean. Three bounds tightened (`starlette>=0.49.1,<2`, `jinja2>=3.1.6,<4`, `python-multipart>=0.0.31,<0.1`), mirrored into `pyproject.toml` per `dependency-management.md:21`.

  **The durable finding is an epistemic one, and it recurred three times in one story.** (1) SE, then PM, asserted that a bare `pip install -e .` would "resolve back into the CVE" — false; unbounded `>=` picks the newest release, and `dependency-management.md:30` says so nine lines below the rule both had cited. The real protection is against **backtracking** (an env pinning `fastapi==0.115`, whose metadata caps `starlette<0.42.0`, drags starlette down). (2) CR then named `pyproject.toml` as the fix site, without asking **which file the resolver reads** — `pip-compile` reads `requirements.in`, so CR's fix would have closed the cosmetic half; SE caught it. (3) **AC-1's own premise was false**: `~=0.41` expands to `>=0.41,==0.*` and **already permitted 0.49.1**. The bound never blocked the fix floor — what it could not span was 1.x, and the lockfile sat at the vulnerable 0.41.3 because of fastapi's *transitive cap*, not the line AC-1 named. Ceiling, not floor, was always the subject.

  **AC-1 passed despite its false premise ONLY because it deferred to `pip-audit` rather than literal-version equality** (its own closing sentence). Spec-authoring rule extracted: *when an AC's rationale names a specific line as the blocker, the AC must remain satisfiable by an OUTCOME check — the named line is the part most likely to be wrong.* Root cause of all three errors: `dependency-management.md:23-30` explains what `~=` and `>=` are *for* without ever stating that **`~=` on a two-component version pins the minor, not the major.** Four agents read that section; none derived it. Routed to CA at closure as a three-part fix (name `pyproject.toml` in the checklist; put the *why* adjacent to the *what*; state the `~=` expansion rule). SE additionally refuted the false model **in the files themselves** (`pyproject.toml:8-13`, `requirements.in:10-19`), which is where it will actually be read.
- 2026-07-09 (dispatch, PM pre-dispatch spec fix on E-256-03): **AC-3 was partly unsatisfiable and was rewritten before dispatch.** It required removing "three ghost package directories… carrying only stale pre-E-239 bytecode." PM verified via two independent channels that **`src/pipeline/` does not exist in the worktree** (Glob returns nothing; a repo-wide `*.pyc` listing shows a `__pycache__` for every real `src/` subpackage and none for `pipeline`) — and a path absent from the worktree is not tracked by git. Since `__pycache__/` is **gitignored** (`.gitignore:2`), the "ghost dirs" are untracked artifacts of the operator's MAIN checkout, not repository content: deleting them yields **no diff**, cannot ride the closure patch, and **cannot be verified by code-reviewer**, who reviews via `git diff`. The committable half (the present-tense docstring at `reports_admin.py:17` naming `src.pipeline.trigger` and `src.gamechanger.bridge`) is now AC-3, gated on a grep rather than a count; the directory removal is recorded as **main-checkout housekeeping, not a story deliverable** (AC-3b), with an escalation branch if a tracked file is found. Stale bytecode also regenerates — `loaders/__pycache__/backfill.cpython-313.pyc` still exists after story 02 deleted `backfill.py` — so a one-time `rm` was never a durable fix. **Seventh seed-list defect this epic, and the first that was structurally unverifiable rather than merely incomplete.**
- 2026-07-09 (dispatch): **E-256-02 DONE** after two review rounds. The command, its module, script, and test file are gone; the deletion is pinned in the removed-commands guard so a re-add fails a test. AC-3's authoritative repo-wide grep found **three surfaces §15 did not list, two of them token-invisible**: `src/reconciliation/engine.py:462,984` (comments falsely claiming pitcher order comes "from cached boxscore JSON" — it reads the `appearance_order` DB column), `tests/test_gs_mixed_appearance_order.py:13,121` ("**a backfill** + recompute"), and PM's own `archived-epics.md:38` ("**backfill CLI**"). None shares a token with `backfill-appearance-order`. Round 2 also corrected a **chronologically inverted precedent re-point** (E-245's `plays_reload` cannot follow E-253's `backfill-game-dates`; chain restored to E-204 → E-245 → E-253). §12's F841 list was corrected (it named one of two survivors; the second, `starter_prediction.py:1127`, was routed to story 05 as AC-5 because it is evidence of story 05's own bug, not lint).

  **Two-gate design vindicated, and worth recording as a process fact.** SE reasonably declined a SHOULD FIX (renaming `pitcher_order_json`); PM and CR independently pushed, and the *reachability* framing — the name is false at **every** call site, not merely imprecise — is what moved it. The rename then surfaced a hazard **none of the three anticipated**: `signal_name="pitcher_order"` (`engine.py:650`) is a persisted reconciliation-signal string literal one token from the renamed identifier. A `replace_all` would have rewritten persisted signal data **with a green suite**, since no test asserts on that literal's value. Generalizable rule, flagged for the closure trigger-8 assessment: **a rename whose identifier also appears as a persisted string literal (signal name, column name, JSON key, status enum) must be done site-by-site, never `replace_all`** — the failure is invisible to tests because the literal's value is data, not behavior. It generalizes across agents (SE renames, CR must catch), so its load target is a `paths: "**"` rule or CLAUDE.md, not a single agent's memory.
- 2026-07-09 (dispatch): **E-256-01 DONE** after two review rounds. Both gates cleared — PM six-AC PASS, CR APPROVED. Round 1 found a real AC-3 violation (PM + CR independently): `test_e247_disk_missing_roster_no_error` was deleted though the outcome it asserts survives at `scouting_loader.py:281-283` (*missing is not malformed*); ported as `test_empty_roster_is_not_an_error` with a mutation check. Also fixed: `GameLoader.load_payload`'s undocumented season-row precondition (its narrowing routed to story 15 AC-5). Final ledger, rebuilt by name-set diff against `HEAD` rather than restated: **24 true deletions, 24 ports/renames, 5 new, net −19**, suite 3830 green. Two public-loader narrowings landed (`load_payload` no longer ensures the `seasons` row; `load_team` drops its dead `season_id` param) — only the first has a context-layer footprint. **The seed-not-ceiling failure recurred four times in this story alone** (test census, Files list, CR's convention-paragraph catch, and the deletion ledger itself), including once on a prose surface where no token grep could have found it — materially strengthening IDEA-115.
- 2026-07-09 (dispatch, story-01 CR round 1 → PM routing): Two CR findings fell outside story 01's scope and were routed, not dropped. (a) **`docs/ROADMAP.md:100-103`** added to PM's closure obligations (§15) — a second ROADMAP surface beyond the `:219,376` QUARANTINE annotation. (b) **Story 15 AC-1 widened** from "strike the symbol names" to "**rewrite the E-237 convention paragraph**" at `architecture-subsystems.md:72`: its closing sentence tells future loaders to rebuild the payload-core/file-wrapper twin this epic deletes, and **no token grep for the deleted symbols would surface that sentence** — the doc-sweep semantic-read case, made explicit rather than left to the sweep. Story 01's Handoff Context correspondingly now hands story 15 a **retired convention**, not just a symbol list. This is the seed-not-ceiling failure appearing a **fourth** time in this epic, now on a prose surface (IDEA-115).
- 2026-07-09 (dispatch, PM ruling on E-256-01 scope): SE's census found §4's nine-test enumeration **undercounts by ~2x** (real figure ~20) because it censused only the *public* disk entry points and missed the *private* disk helpers they orphan. **Ruled: criterion controls, not count.** §4 rewritten — seed-not-ceiling framing, the two-clause deletion criterion made authoritative, and "total assertion coverage must not drop" clarified to mean *coverage of behavior that still ships*. PM verified in code that `_build_opponent_name_lookup` and `_build_summaries_index`'s dual-key resolution have **no live analogue** (the in-memory path uses `_build_opponent_name_index_from_data` over a different payload shape, and `_build_games_index_from_data` is single-key), so their tests die cleanly. **One genuine coverage risk found and gated:** `_parse_summary_record` is a *pure* parser, not a disk helper, and its `_opt_int` NULL-preservation semantic (E-253-06 AC-3, the scoreless-doubleheader dedup guard) is re-implemented inline at `scouting_loader.py:408-422` — the function dies but the semantic ships, so **new story-01 AC-6** requires the assertion to exist against `_build_games_index_from_data` before the old test is deleted, and re-points the two dangling `_parse_summary_record` comments. Story 01 AC-1 rescoped to the four loaders; the epic-wide "no `src/` module reads `data/raw/`" is a **closure-verified Success Criterion** spanning stories 01/02/03, owned by no single story. **PM surfaced a third `data/raw/` reader neither SE nor the epic had found** — `src/cli/status.py:56` globs `data/raw/*/manifest.json` to populate `bb status`'s last-crawl panel — routed to story 03 as **AC-6** with a decision rule (vacuous panel → delete; live `manifest.json` writer → leave and escalate), alongside `scouting_spray.py`'s dead `_data_root` attribute. This is the §15 hand-list-undercount failure mode recurring within the same epic (IDEA-115).
- 2026-07-09 (dispatch, PM work-definition ruling on E-256-16 scope): **Story 16 WIDENED to home a second pre-existing PII hit surfaced by story 09's whole-tree CI PII scan.** SE's correct `git ls-files -z | xargs -0 python -m src.safety.pii_scanner` CI step is RED on the committed tree due to **two** pre-existing `api_key_assignment` hits, not one: `tests/test_credential_parser.py:81` (story 16's original 12a-ii target) **and** `proxy/addons/credential_extractor.py:42` (`"gc-device-id": "GAMECHANGER_DEVICE_ID"`), owned by no story. PM verified against the regex (`src/safety/pii_patterns.py:66-71`) that the pattern keys on `device[_-]?id` + `[=:]` + a 16+ value — so **:81's `gc-device-id` firing line is story 16's exact target** (the JWT at `:77` does NOT fire; `gc-token` is not a keyed token name — the main session's "fake JWT" label was a mislabel of what sits at `:81`), and the proxy `:42` hit is a **false positive** (the "value" is the 21-char env-var NAME, not a secret). **Homed here (option a), not a new micro-story:** both hits are the same acceptance concern ("make the whole-tree PII scan green the correct way") and both are SE-owned `.py` files (non-context-layer), so folding into TODO/unblocked story 16 avoids an epic-scope expansion (a new micro-story would have required user surfacing). Added **AC-5/6/7**: proxy fix is **tier-2 line-scoped `# pii-ok`** (tier-1 unavailable — the env-var name is real functional code that must stay verbatim; and §6 forbids a file-level marker on the credential-extractor file), AC-7 requires the whole-tree scan to report **zero** so the story-09 CI PII step goes GREEN. Story title + Files + Description updated; story count unchanged at 16. **Closure linkage:** the epic Success Criterion "CI passes on push" for the PII gate now depends on story 16 completing — story 09's ACs (workflow correctly authored) do NOT require a green tree, so 09 is DONE-able independently, but 16 is the gate for CI-actually-green.
- 2026-07-09 (dispatch): **E-256-09 DONE.** Both gates cleared — PM AC verdict all-PASS, CR APPROVED (round 2, no MUST FIX). Single `.github/workflows/ci.yml` job `static-gates` with fail-on-failure steps: pytest → PII **pattern scanner** (whole-tree explicit enumeration `git ls-files -z | xargs -0 python -m src.safety.pii_scanner` — not the bare no-op, not `--staged`) → ruff (F) → lockfile-drift (recompile both `.in`→`.txt` in place + `git diff --exit-code`, pip-tools pinned) → `docker build`. **AC-2** documents (in-file) why the doc-PII byte-gate is CI-excluded (gitignored denylist → always EXAMPLE MODE on runners); **AC-3** records the `smoke_test.py` keep-not-delete/not-in-CI verdict so the AGENTIC-FLOW-REVIEW false binary is not reopened. **AC-5 (docker build) is PASS-BY-INSPECTION, runtime deferred:** it cannot run in the worktree, and this step IS the mechanism to which **E-256-06 AC-4 was reassigned** — so the first-push docker-build green is a NAMED closure/operator obligation discharging both. **09's ACs concern the workflow being correctly AUTHORED, not a green committed tree** — the tree is NOT yet PII-green (two pre-existing hits homed into widened story 16), so **CI-actually-green is gated by story 16, which must land before closure.** Staging: `ci.yml` staged; the stray `.dispatch-log/` the counter hook recreated was removed first, not staged.
- 2026-07-09 (dispatch): **E-256-11 DONE.** Both gates approved, **zero valid findings.** CA shipped Phase 5 **Step 1d "closure runtime smoke"** in `implement/SKILL.md` (wired into Step 8 as sub-step 5b, plus sub-step 4b for the AC-4c dependency reinstall), the two `code-reviewer.md` carve-outs (Test-Execution-Constraint second exception + Anti-Pattern #5 exception authorizing DB-mutating `bb report generate`), and `.smoke-fixture` added to `.gitignore`. This is the reports flow's first live runtime gate at closure (runs in the main checkout post-apply against the LIVE dev DB; env-FAIL preflight escalates+holds, epic-FAIL remediates; CR self-evaluates the trigger). **AC-6 RULING — CA's merge-base adaptation ACCEPTED:** the trigger read uses `git diff --cached --stat $(git merge-base epic/E-NNN main)` vs AC-6's literal `main`; accepted because it serves AC-6's OUTCOME (trigger reflects the epic's own changes) and literal `main` would false-fire on main's post-E-260 divergence — same defer-to-outcome lesson as story 07. **Two review findings, BOTH DISMISSED:** (1) `SKILL.md:480` "same base as sub-step 3" was a FALSE POSITIVE — PM's flag was against the STALE pre-E-260 worktree copy (sub-step 3 = `main` there), but CR's `git show main:` confirmed main's post-E-260 sub-step 3 already reads merge-base and story 11 doesn't touch it, so the claim is TRUE in the shipped merged tree; (2) CR's own `:650` SHOULD (revert enumeration omits 5b) — dismissed per CR's recommendation (5b has no git footprint to reverse). **The genuine residual** (sub-step 9's `--stat main` vs sub-step 3's merge-base, both in E-260's shipped skill) was captured as **IDEA-119** (CA-owned), out of scope for story 11. **Collision renumber done during this story:** the E-256 worktree (branched pre-E-260) had assigned IDEA-116/117 to its own pytest-CVE/webauthn ideas, colliding with E-260's already-committed IDEA-116/117/118 on main; per the user's closure principle (keep main's, add E-256's on top) the worktree's two ideas were renumbered to **120/121** (content edits by PM + `git mv` by the main session), so E-256 ships 119/120/121 as a clean append with zero duplicate numbers.
- 2026-07-09 (dispatch): **E-256-12 DONE.** Context-layer-only (`.claude/rules/data-model.md` only) → PM-verified alone, CR skipped. CA appended one "**Dead-by-descope, retained by decision (E-239)**" bullet to the Key-design-decisions list (styled to the `gc_athlete_profile_id` dropped-column precedent). **AC-1 PASS:** marks `crawl_jobs` + `coaching_assignments` write-orphaned/retained-by-decision and the ~100 season split/advanced columns (home/away, vs-LHP/RHP, RISP) permanently unpopulatable post-E-239. **AC-2 PASS:** explicitly names `user_team_access` as LIVE (never mark dead), and is a schema-doc note, not an idea capture (no duplication of E-255-06 AC-4's retention idea). **PM independently grep-verified the load-bearing facts (not trust):** zero INSERT/UPDATE writers for the two dead tables (only defensive DELETEs at `lifecycle.py:518-519`, `reports_admin.py:280`); three LIVE `user_team_access` INSERT writers (`auth.py:133`, `reports_admin.py:231/:260`). Doc-only; E-260 did not touch data-model.md (clean apply).
- 2026-07-09 (dispatch): **E-256-10 DONE.** Both gates approved — PM AC-1..AC-6 all PASS, CR APPROVED (no MUST FIX). docs-writer's single `docs/admin/` pass: `production-deployment.md` (required daily-2AM-cron backup via `docker compose exec` form + off-host `rsync` copy step reconciling `backup.py:20`'s in-`./data` reality; new "Closure Runtime Smoke (Step 1d)" section documenting the two-field `.smoke-fixture` convention + ordered check sequence, paraphrased to agree with SKILL.md Step 1d); `architecture.md` (false `seeds/ # …committed to git` directory line removed + adjacent `scripts/` "seeding" comment corrected); `operations.md` (`backfill-appearance-order` runbook section struck, precedent re-pointed to `reload-annotated-pitches`, backup cross-ref added). AC-5 staleness footers on all three → `2026-07-12 | … E-256-10`. **PM independently ran the AC-6 grep-and-reconcile over `docs/admin` (not trust):** zero prescriptive `backfill-appearance-order` recipes remain; the one surviving mention (`operations.md:125`) is the reconciled "removed in E-256" precedent note (reconcile-not-strike).
  - **Accepted SHOULD residue (two-tier floor, NOT reopened):** `production-deployment.md:570`-region footer uses the label `Story:` vs the canonical `Source:` — cosmetic; AC-5's substance is met (date + producing-story present). Fold into a later `production-deployment.md` touch if one occurs.
  - **§15 reconciliation — CR's whole-repo sweep found surviving backfill-command prescriptions OUTSIDE `docs/admin`, ROUTED not dropped (per the §15 ownership table):** (a) `docs/ROADMAP.md:219,376,100-103` → **PM closure obligation** (ROADMAP is PM-owned; annotate the QUARANTINE→DELETED verdicts + the stale §2 "Loaders are Path-only" assessment); (b) `.claude/rules/data-model.md:22` + `CLAUDE.md` → **story 15** (context-layer eviction, AC-3, main session names it in 15's assignment); (c) agent-memory (data-engineer, code-reviewer, PM) → **owning-agent closure sweep** (PM-flagged, reconcile-not-strike). PM carries (a) and (c) on the closure list.
- 2026-07-09 (dispatch): **E-256-13 DONE.** Context-layer-only (`.claude/rules/pii-safety.md` only) → PM-verified alone, CR skipped. CA added a "## Suppression mechanisms and choice hierarchy" section. **AC-1 PASS:** both suppressors documented (`# pii-ok` `pii_scanner.py:151-153`, `synthetic-test-data`/`SYNTHETIC_MARKER` `:145-148`), both stated to suppress ALL patterns incl. `bearer_token`/`api_key_assignment`/`email`/`us_phone` (they sit outside the `COMPILED_PATTERNS` loop). **AC-2 PASS:** 3-tier choice hierarchy (change-the-data → line-scoped `# pii-ok` → file-level marker only for e2e synthetic fixtures, never on credential-handling files). **AC-3 PASS:** real credential behind either marker = MUST FIX; blanket `tests/**` exemption rejected on evidence (zero shape-fires) + visibility-to-review axes; §4g NOT overclaimed as a structural closure (staged-blob residual → IDEA-112). **AC-4 zero-code CONFIRMED** at the staging boundary (src/tests/scripts/migrations check returned EMPTY; contribution is `pii-safety.md` only). **PM independently verified the load-bearing citations (not trust):** read `pii_scanner.py:143-156` (both suppressors run before the `COMPILED_PATTERNS` loop at :154 — claim accurate) and `code-reviewer.md:191-196` (CA's corrected §4g "Credential Hygiene" citation is accurate — `:196` names test-fixtures-with-real-credentials a MUST FIX; the §6 citation was corrected from CLAUDE.md to code-reviewer.md §4g). E-260 clean (pii-safety.md untouched).
- 2026-07-09 (dispatch): **E-256-15 DONE.** The epic's most consequential context-layer story; context-layer-only (`architecture-subsystems.md`, `data-model.md`, `CLAUDE.md`) → PM-verified alone, CR skipped, all 7 ACs independently grep+read-verified. **AC-1:** `architecture-subsystems.md:72` GENUINELY REWRITTEN (not symbol-stripped) — `load_payload`/`load_from_data` are the sole entry points, the deleted twin named as history, and the harmful closing guidance replaced with "do NOT add a file-reading wrapper," explicitly reversing the stale E-237 split that would rebuild the deleted twin; `:74` redirect note now names `ScoutingLoader.load_team` not the deleted `_load_team_from_disk`. **AC-2:** zero live deleted-symbol references in rules/agents/CLAUDE.md (only the `:72` history annotation; the CLAUDE.md "bridge" hits are the unrelated API UUID/public_id bridges). **AC-3:** `backfill-appearance-order` struck from CLAUDE.md (Commands sentence + Footgun) AND `data-model.md` (§15(b) ref, distinct paragraph from story 12's note) — zero in-scope hits. **AC-4:** doc-sweep semantic-read half evidenced by the no-token `:72` rewrite. **AC-5:** `CLAUDE.md:127` Season_id rule NARROWED not deleted (entry-point loader + generator ensure the row; `GameLoader.load_payload` presumes it; FK-error warning; tuple-unpack note survives). **AC-6:** `CLAUDE.md:118` team-deletion re-pointed `generator.py`→`lifecycle.py`, rule unchanged; siblings need no edit. **AC-7 (closed-loop guard) vindicated LIVE:** enumeration came from the stories-01–04 handoff symbol sets + concept sweep, NOT the story's own grep — and CA's first confirmation grep SILENTLY returned nothing (ugrep 7.5.0 BRE `\|` alternation over multi-paths; `-rnE` works), so driving enumeration from it would have shipped a false-clean no-op. The two no-matching-token concept surfaces (`:72` closing guidance + the "backfill CLI"-class precedent citations) were both caught. **§15(c) agent-memory sweep targets PINNED on PM closure list** (out of story-15 scope): `data-engineer/season_aggregate_writers.md:110` (stale `GameLoader.load_file`), `code-reviewer/recompute_tail_noop_populated_db.md:23` + `data-engineer/season_tables_are_a_pure_cache.md:22` (backfill refs), PM's own `MEMORY.md:16`/`archived-epics.md:71` (reconcile-not-strike), `claude-architect/epic-codifications.md:33`. **Carried for closure Step 3a:** CA's ugrep `-rn` BRE-alternation silent-failure trap → tool-output-integrity codification. E-260 3-way-verified clean on all three files.
- 2026-07-09 (dispatch): **E-256-16 DONE — the LAST story; all 16 now DONE.** Both gates approved — PM AC-1..AC-7 all PASS, CR APPROVED (no findings). The **CI-PII-green gate closes.** SE-owned, two Python files: `tests/test_credential_parser.py:81` fixed **tier-1** (`gc-device-id: 7b615e8c…` 32-char value → `deadbeef`, 8 chars, fires no `api_key_assignment`; NO marker per the choice hierarchy — the credential-parser test file is the worst place for one), assertions `:123`/`:362` migrated to `deadbeef` in lockstep (76 passed); `proxy/addons/credential_extractor.py:43` fixed **tier-2** (line-scoped `# pii-ok` + false-positive comment; env-var NAME `GAMECHANGER_DEVICE_ID` unchanged — real code; NO file-level marker; `src/safety/` untouched). **AC-7 gate proof:** whole-tree scan `git ls-files -z | xargs -0 python -m src.safety.pii_scanner` → **453 files, 0 violations, exit 0.** PM independently re-ran the `api_key_assignment`-shape sweep (ripgrep) and reconciled every remaining hit (suppressed `# pii-ok` / SKIP_PATH `epics`+`.project` / pre-existing synthetic-marked test file) — zero unexplained; stale `7b615e8c` fully evicted worktree-wide. Staging boundary confirmed exactly the two files, no `src/safety/` change.
- 2026-07-12 (**CLOSURE — COMPLETED**): All 16 stories DONE; operator authorized closure. Closure sequence: Step 1c **Closure CR Integration Review APPROVED** (no MUST FIX — surface-removal sweep clean, both structural moves cleanly integrated); Step 1b full-suite green gate + Step 1d closure runtime smoke run post-patch-apply in the main checkout at Step 8 (a red gate aborts+reverts this COMPLETED flip). The COMPLETED status here is authored at Step 8 sub-step 3 and rides the closure patch; it is never finalized on a red suite.

  **Review Scorecard (dispatch).**

  | Story | Owner | Review path | Outcome |
  |---|---|---|---|
  | 01 | SE | CR+PM | DONE, 2 rounds; round-1 real AC-3 violation found+ported |
  | 02 | SE | CR+PM | DONE, 2 rounds; grep found 3 token-invisible surfaces |
  | 03 | SE | CR+PM | DONE, 2 rounds; mutation-outside-worktree constraint set |
  | 04 | SE | CR+PM | DONE, 2 rounds (highest-risk); golden zero-diff held |
  | 05 | SE | CR+PM | DONE; the closed-loop-defect finding (grep-was-the-enumeration) |
  | 06 | SE | CR+PM | CR APPROVED, no MUST FIX; 1 accepted SHOULD residue (3 `.dockerignore` anchoring comments); DONE recovered post-interruption |
  | 07 | SE | CR+PM | DONE, 2 rounds; 32 vulns closed (not the 2 named) |
  | 08 | SE | CR+PM | DONE; ruff F-class; `pitcher_order` persisted-literal rename hazard flagged |
  | 09 | SE | CR+PM | CR APPROVED round 2, no MUST FIX; AC-5 docker-build runtime deferred (discharges 06 AC-4) |
  | 10 | docs-writer | CR+PM | CR APPROVED, no MUST FIX; 1 accepted SHOULD residue (footer `Story:` vs `Source:`); §15 whole-repo backfill sweep ROUTED |
  | 11 | CA | CR+PM | Both gates approved, **zero valid findings**; AC-6 merge-base ACCEPTED; 2 findings DISMISSED (`:480` false-positive via CR `git show main:`; CR's own `:650`); IDEA-119 captured |
  | 12 | CA | PM-alone (context-layer) | Both ACs PASS; PM grep-verified dead-table writers |
  | 13 | CA | PM-alone (context-layer) | All 4 ACs PASS; AC-4 zero-code confirmed; §6 citation corrected |
  | 14 | CA | CR+PM | DONE (prior session); doc-PII byte-gate on planning trees (promotes IDEA-102) |
  | 15 | CA | PM-alone (context-layer) | All 7 ACs PASS; AC-1 genuine `:72` rewrite; AC-7 closed-loop guard vindicated live (ugrep silent-grep-failure) |
  | 16 | SE | CR+PM | CR APPROVED, no findings; all 7 ACs PASS; CI-PII-green gate closed (453 files/0/exit 0) |

  Closure reviews: **Step 1c APPROVED**; Step 1b + Step 1d at Step 8. **Dispatch dismissals: 2** (both story 11 — the `:480` stale-worktree false-positive + CR's own `:650` SHOULD), plus 2 accepted-SHOULD residues (06 anchoring comments, 10 footer label) — NOT dismissals.

  **Step 3 — Documentation assessment.** Triggers 1/2/5 fire (CI ships; deployment/backup config change; system-behavior change) BUT the impact was **FULLY DISCHARGED IN-EPIC by story 10** (docs/admin: backup cadence + off-host copy, Step 1d smoke procedure, seeds correction, backfill-runbook eviction, staleness convention). No `docs/api` impact — the deleted `bridge.py` is the helper module, NOT the live API UUID-bridge endpoint. **→ No additional docs dispatch.**

  **Step 3a — Context-layer assessment (eight per-trigger verdicts).**
  1. New convention/constraint — **YES** (Step 1d closure smoke + rebuild-not-start/local-reinstall closure conventions, two-field `.smoke-fixture`, E-237 convention REVERSAL, CI static gate, doc-PII byte-gate on planning trees, PII choice hierarchy) — codified in-epic (stories 11/13/14/15).
  2. Architectural decision — **YES** (season-fetch relocation to `api/db.py` setting up E-259; `lifecycle.py` extraction; starlette major dependency refresh) — codified in-epic (CLAUDE.md canonical seams, story 15).
  3. Footgun/failure mode — **YES** (the ugrep `-rn` BRE-alternation silent-failure trap) — CA codifying to `tool-output-integrity.md`.
  4. Agent behavior/routing/closure change — **YES** (Step 1d added to the closure sequence + `code-reviewer.md` carve-outs) — codified in-epic (story 11).
  5. Domain knowledge for future epics — **YES** (dead-table note story 12; PII suppressor true scope story 13) — codified in-epic.
  6. New CLI command/workflow — **YES** (CI workflow; Step 1d smoke procedure; story 02 DELETED a `bb` subcommand) — codified in-epic; no `/workflow-help` change (Step 1d is internal to the implement skill, not a user-facing workflow).
  7. Net context-layer growth ratchet — **FIRES (operator-signed exception, verbatim):** the context layer grew **+93 lines** past baseline (est. 12081→12174, `context-ratchet.sh` exit 1), driven almost entirely by the Step 1d closure runtime smoke (**+59 `.claude/skills`**) — a defect-cited, load-bearing gate (would have caught the impossible-FPS stat + the rest-day UTC bug). **No offset available without deleting the gate. The operator signed this exception at closure**; the operator (not any agent) runs `context-ratchet.sh --update-baseline` and reviews the JSON diff post-merge.
  8. Reusable behavioral lesson (gated) — **YES** (ugrep trap → `tool-output-integrity.md`, defect-cited by CA's story-15 near-miss; the closed-loop-guard / enumerate-don't-aggregate lessons already live in agent memory from prior E-256 stories — no re-promotion).
  **→ Step 3a FIRES; claude-architect codifies** (item-1 ugrep trap, §15(c) off-team data-engineer memory prune, the ratchet, and its own `epic-codifications.md`); code-reviewer reconciles its own memory.

  **Closure obligations discharged at this window (PM):** §15(a) `docs/ROADMAP.md:219,376,100-103` backfill/Path-only annotations; IDEA-119 marked LANDED/MOOT (E-260-01 AC-6 already shipped the merge-base closure-diff reconciliation the idea proposed — the sub-step-9 residual was a stale pre-E-260-worktree reading, the same landmine as story-11 finding #1); §15(c) PM own-memory reconcile (`archived-epics.md` E-204 "backfill CLI" annotated removed-by-E-256, `MEMORY.md:16` already reconciled — reconcile-not-strike); `MEMORY.md` numbering → next `IDEA-122`, E-256 Active→Archived, Memory Retirement.

  **History (documentation impact): No further documentation impact beyond story 10's in-epic discharge (recorded above).**
