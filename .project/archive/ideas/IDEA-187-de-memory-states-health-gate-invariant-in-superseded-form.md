# IDEA-187: data-engineer's Health-Gate Memory — Two Reported Defects, BOTH Deflated to a Cross-Reference

> **⚠️ THE TITLE ABOVE WAS *"…States the Invariant in E-276's Superseded Form"* AND IS RETIRED.** It encoded the claim, and the claim did not survive checking. Left renamed rather than reworded because a title asserting a retired predicate over a corrected body is `.claude/rules/doc-sweep.md`'s first named survival shape — and this idea is now a specimen of it.

## Status
`CANDIDATE` — **and the honest disposition is DISCARD-or-one-line-fix, DE's call.** Kept CANDIDATE only because the surviving residual is real and DE has not been asked.

## Summary

**⛔ BOTH REPORTED DEFECTS ARE NOW DEFLATED, in two separate passes, and what remains is a single cross-reference.** This idea was filed 2026-07-25 claiming `.claude/agent-memory/data-engineer/health_gate_prior_set_must_be_temporal.md` states the reconcile-at-load health-gate invariant in a **superseded** form. Checked against the committed file:

- **Defect 1 (the invariant)** — the internal-inconsistency claim quotes a paragraph **that does not exist in the file**; the sufficiency claim is **inverted relative to TN-10**; and the file already carries the per-grain shape. See the deflation below.
- **Defect 2 (the count reconciliation)** — already deflated in an earlier pass; its "decisive check" was refuted as a unit error and the memory's own reading is the best-supported account.

**What survives both**: the file's per-grain shape — the paragraph opening ***"Where E-276 actually landed (corrected…)"*** — sits **below the midpoint**, while its rule, "Required wording" and recall-deciding `description:` frontmatter sit at the top **with no pointer down to it**. One cross-reference. *(Stated by the paragraph's opening words rather than by coordinates; see the position note below for why.)* **E-276 still structurally cannot make even that edit**, because agent-memory content is edited only by the owning agent and data-engineer is not on E-276's dispatch team — the routing was always the reason this is an idea, and it is now the *only* reason.

**Carried at full length rather than discarded, deliberately.** An idea that shrank from "a rewrite of a load-bearing memory" to "add a cross-reference" across two independent checks is a better record of how this class of claim decays than a deleted file would be — and the two checks caught **different** error shapes (a refuted argument, then a quoted-but-absent paragraph plus a polarity inversion).

> **⚠️ DO NOT CALL IT "the pre-conjunction form."** E-276 **ships no conjunction** — that interim design was dropped 2026-07-25. Describing the memory as pre-conjunction measures it against a design that does not exist, which is the same class of error as the staleness this idea reports. **The baseline is the CURRENT TN-10: one gate per grain** — the corrected (pre-upsert snapshot) gate on game and player-line, and **no floor gate at all on roster**, whose permit is a non-empty fresh payload AND `MAX_ROSTER_DEPARTURES`. E-276's story 05 AC-9 carries this same prohibition.

**Defect 1 — the invariant. ⛔ DEFLATED 2026-07-25 (second amendment), and far further than Defect 2 was. Most of what this section claimed is either ALREADY IN THE FILE or STATED BACKWARDS. Read the deflation before citing any of it.**

> **What this section originally claimed**, preserved so the deflation can be audited against the assigned text rather than against a summary of it: that the file's opening claim and its "required wording" state the temporal clause *as though it were the whole answer*; that the temporal clause is **necessary but NOT sufficient** and the file states only the necessary half; that a required wording phrased as one rule for all three grains is wrong on the third; and — the headline — that the file is **internally inconsistent**, carrying a later paragraph beginning *"Prefer a gate whose safety does not depend on your own reasoning being right"* which states a **conjunction** and names E-276 as its source, so that both framings are now wrong and the fix is a rewrite rather than a deletion.

**Three findings, checked against the committed file** (`.claude/agent-memory/data-engineer/health_gate_prior_set_must_be_temporal.md`, read in full — **35 lines as of that check, 2026-07-25; the file is 41 lines as of 2026-07-26** — with a second-channel grep across `.claude/agent-memory` confirming the absence):

1. **The internal-inconsistency claim is FALSE. The quoted paragraph does not exist.** There is no *"Prefer a gate whose safety…"* paragraph anywhere in the file, or anywhere in `.claude/agent-memory`. DE has already replaced it: the file now carries a paragraph headed **"Where E-276 actually landed (corrected — an earlier version of this note recorded an outcome the same session overturned)"**, which states the per-grain shape correctly — corrected snapshot-population gate on **game and player-line only**, **roster gets NO floor ratio at all**, `permit = (fresh payload non-empty) AND (|absent ∩ previously| <= MAX_ROSTER_DEPARTURES)` — and records the conjunction explicitly as *adopted mid-session and later dropped*. **This was the section's headline claim and its whole basis for "rewrite rather than deletion". It is gone.**

2. **⛔ THE SUFFICIENCY CLAIM IS INVERTED — it attaches "not sufficient" to the wrong clause.** E-276 **TN-10**, which this idea itself names as the wording to copy, puts the insufficiency on **same-population**: *"**Same-population-on-both-sides is necessary but NOT sufficient** — a set read after the fresh upsert satisfies it while measuring `|fresh| >= |stale|`… **The temporal clause is the load-bearing half**."* This section relocated the insufficiency onto the **temporal** clause, which reverses TN-10's direction. And the file's opening paragraph already states TN-10's version near-verbatim — same-population is insufficient, a post-upsert read satisfies it while degrading to `|fresh| >= |stale|`, the temporal clause is what completes it. **So the file was not missing the sufficiency note; it was carrying it, and this idea reported its own inversion of it as the file's gap.** The `description:` frontmatter is correct in direction for the same reason (*"the load-bearing clause is WHEN the prior set was captured"* is TN-10's own phrasing).

3. **The per-grain claim does not hold of the body**, per finding 1 — the file states the per-grain shape, including that roster has no floor gate. It also does not hold in the form given: the required wording governs *"a retire/reconcile/parity ratio gate"*, and the roster grain under V1 **has no ratio gate**, so the rule is vacuous there rather than wrong there.

**WHAT SURVIVES, and it is one narrow thing:** the per-grain scope — the paragraph opening ***"Where E-276 actually landed (corrected…)"*** — sits **below the midpoint, with several substantive sections beneath it**, while the rule, the "Required wording", and the recall-deciding `description:` frontmatter sit at the top **with no pointer down to it**.

> **⛔ THE POSITIONAL CLAIM HAS NOW BEEN WRONG THREE TIMES, EACH TIME DIFFERENTLY, SO THE FORM WAS CHANGED RATHER THAN THE VALUE.** It said *"the bottom"*, then *"the last third"*, then **"line 21 of 35"** — and the last was correct when written and rotted on **2026-07-26** when data-engineer legitimately appended to its own file (now **41 lines**; the paragraph is still at line 21).
>
> **The first two were wrong the same way**: this idea and story 05 AC-9 agreed because **one was written from the other**, not because two people checked. **The third was wrong a NEW way, and it is the instructive one — an APPEND BELOW A CITED LINE invalidates a positional claim's DENOMINATOR while leaving its NUMERATOR correct.** So the natural check — *is the cited line still where we said?* — **passes**, and two parties ran exactly that check and both got a pass. **A position stated as a fraction has two independently rotting halves, and verifying the interesting one certifies nothing about the other.** That generalizes to any claim carrying a ratio or a total.
>
> **The claim is therefore now stated by the paragraph's OPENING WORDS and its relation to the top of the file, which no append can falsify.** Coordinates appear only as dated evidence. **The substantive finding — no pointer from the top-of-file rule, the "Required wording", or the `description:` down to the per-grain paragraph — has been unaffected by all three errors**, which is precisely why it kept surviving corrections that were themselves wrong.
>
> *(As of 2026-07-26: line 21 of 41. Dated because the number is evidence, not the claim.)* A DE instance that loads the description and stops — which is what the description is for — gets a correctly-directed temporal rule with **no indication that one of the three grains runs no ratio gate at all.** That is worth a cross-reference. It is **not** worth a rewrite, and it is DE's call whether it is worth anything.

**Why this deflation is recorded at this length rather than by editing the section into agreement with the file.** This idea's own Defect 2 was deflated on the stated principle that *"an idea that reports a defect larger than the one that exists is unusable in the same way an inflated tally is"* — and Defect 1, sitting immediately above it, went unchecked in that same pass. **The section that states the principle is not exempt from it.** Worse, finding 2 is a **polarity inversion in a safety-shaped claim**, which is the same shape E-276's own TN-5 precondition (e) recorded — *"backwards, and in the direction most likely to be correct"* — committed here while quoting the note that fixes it. And finding 1 is E-276's `.claude/rules/tool-output-integrity.md` case exactly: **a claim about another artifact's contents, restated across two surfaces (this file and the ideas index), never resolved against the artifact.**

**Defect 2 — the refuted count reconciliation.** The same file's "Quantified" failure-shape bullet records the divergence-count dispute as one population over four sweep bounds — *"the same criteria give 15 / 26 / 100 / 222 as the sweep range moves 0..3 / 0..4 / 0..8 / 0..12"* and *"a colleague's independently-bounded sweep collapsed to the identical 3 shapes."* That reconciliation was **contested during E-276's final triage — and the outcome is substantially more favourable to the memory than this idea first claimed. Defect 2 is hereby DEFLATED, not merely reworded.**

⛔ **The check this idea previously cited as decisive is itself REFUTED, as a UNIT ERROR, and must not be cited by anyone.** It read: *"had the 222 population carried the cap it could not have reached 222, since `a < b` with `b ≤ 2` forces `p ≤ 3` at any sweep range."* That inequality bounds the **pre-load roster size** — it says nothing about the number of swept parameter *combinations* realising those shapes, because the remaining swept parameters still range freely. **222 counts combinations; 3 counts shapes.** The counterexample was in everyone's hands the whole time: the colleague's own sweep **does** carry the cap and still scales 20 → 26 → 44 across three spaces with a byte-identical shape set.

**What survives, and it is a framing rather than a refutation**: always name **which** divergence a count measures — corrected-gate refusal, gate-VALUE difference, or observable-outcome difference. Of that account's three row assignments, one is now source-verified (the colleague's sweep measures the **observable-outcome** population, not corrected-gate refusal) and **what the 222 measured remains OPEN.** Meanwhile the memory's own reading — one criteria set over four sweep bounds — is the **artifact-of-bounds** account, which is the **best-supported** one on the evidence: the four figures fit a single enumeration `c(n) = (3n−2)(n−1)/2` exactly at four points.

**So Defect 2 shrinks to one narrow claim that does hold**: the bullet's *"a colleague's independently-bounded sweep collapsed to the identical 3 shapes"* **overstates independence.** That sweep pre-filters on DE's own boundary, so it explores *within* the derivation rather than arriving at it independently — accurate wording is *an analytic derivation confirmed by execution over an added axis*. **The magnitude claim ("the reconciliation was refuted") is withdrawn.**

The *lesson* the bullet teaches — a sweep count carries its bounds, say the frame or drop the count — is **correct and worth keeping**, and so now is most of its worked example. **Recorded at this strength deliberately**: E-276 spent four successive wrong reconciliations on this gap, each adopted because it corrected its predecessor, and *refuting a counter-argument supplies no argument for the original account*. An idea that reports a defect larger than the one that exists is unusable in the same way an inflated tally is.

## Why It Matters

**⛔ THIS SECTION'S ORIGINAL ARGUMENT IS RETIRED WITH DEFECT 1 AND MUST NOT BE CITED.** It read: *"A data-engineer instance consulted on any future ratio/parity/retire gate loads this file and gets the superseded invariant as the headline claim, with the corrected version buried below it and unmarked. E-276's whole thesis is that a locally-true claim propagates unverified because its truth-condition lives elsewhere — this file is that thesis pointed at the record of itself."* **The headline claim is not superseded** (it is TN-10's own direction), so there is no buried-corrected-version to be misled past. The companion sentence about Defect 2 — *"a memory that teaches 'state the frame, not the number' while carrying a worked example whose frame is wrong"* — fell with Defect 2's deflation: the memory's reading is the best-supported account.

**What still matters, at its actual size**: agent memory is recalled and acted on without the epic that produced it, and a `description:` frontmatter is what decides recall. This file's decides recall on a correctly-directed one-grain rule, over a module where **one of three grains runs no ratio gate at all**. A DE instance that loads the description and stops learns the rule and not its scope. That is a cross-reference, not a correctness problem.

**And the irony is now the file's own, which is the part worth keeping.** This idea was written to report a memory that propagates a claim past its truth-condition. It then propagated a claim about that memory's contents — across two surfaces, uncorrected, for a day — without resolving it against the memory. **The thesis was pointed at the record of itself; it just turned out to be pointing the other way.**

## Rough Timing

Not blocking, and deliberately not folded into E-276. Resolve at the **earliest** of:

- **E-276 closure.** The Learning-Loop Lifecycle's Deletion-Side Eviction sweep (`.claude/rules/context-layer-assessment.md`) fires here by construction: E-276 retires two claims that live in this file. The rule's ownership clause is the operative one — whoever runs the sweep MAY read any agent's directory to identify hits, but **only the owning agent edits its own content**, and E-276's Dispatch Team is software-engineer + claude-architect. So the sweep produces a *flag*, and this is the follow-up that flag lands in.
- The next time data-engineer is spawned for any reason — a short correction message is far cheaper than an epic, and DE is the only party who can make the edit.
- Any future work on a prior-vs-fresh ratio gate, where the file would be recalled and acted on.

## Dependencies & Blockers

- [ ] ~~E-276 lands first, because the corrected wording to copy is its TN-10 replacement paragraph.~~ **WEAKENED by the deflation**: there is no wording to copy. The surviving edit is a *pointer* to a per-grain paragraph the file already contains, so it introduces no new claim about an unshipped design and does not have to wait. **The file already describes E-276's unshipped landing** — that was DE's own call and this idea does not reopen it.
- [ ] Requires **data-engineer** specifically. Not a blocker on evidence or scope — a blocker on ownership. **This is now the ONLY reason the item exists as an idea** rather than as a one-line edit somebody makes in passing.

## Open Questions

**Both original questions are ANSWERED by the deflation, and are struck rather than silently dropped.** *"Does the `description:` frontmatter need rewriting?"* — **No.** It front-loads the temporal clause as **the load-bearing one**, which is TN-10's own direction (*"The temporal clause is the load-bearing half"*), not the superseded claim. *"Keep the temporal clause as the headline with 'necessary but not sufficient' attached?"* — **The file already carries that note, on the clause TN-10 puts it on** (same-population). Attaching it to the temporal clause instead, as this idea proposed, would have **introduced** the inversion into the file rather than removing one. **That is the sharpest reason to record the deflation: acting on this idea as written would have made the memory worse.**

What is genuinely open:

- **A SECOND INSTANCE of the same recall gap, recorded for DE to rule on and deliberately NOT decided here** *(found during the E-276 fifth-pass verification, 2026-07-25)*: **`.claude/agent-memory/data-engineer/MEMORY.md:75`**, the index line pointing at this memory file. **It is NOT a residue site** — it carries the rule correctly (*"the load-bearing clause is WHEN `prior` was captured"*) and makes no positional claim, so there is nothing in it to correct. **But it carries no per-grain scope either**, so a DE recall that stops at the index learns the rule and not that **one of the three grains runs no ratio gate at all** — the same gap as a recall stopping at the `description:` frontmatter, one level further out. **Whether the pointer request should cover the index line too is DE's call**, and it is recorded rather than actioned because this file is DE's to edit. *(Flagged by the reviewer, which explicitly declined to adjudicate it.)*

- **The one surviving edit, and whether it is worth making**: add a pointer from the top-of-file rule / "Required wording" / `description:` down to the per-grain paragraph — **the paragraph opening *"Where E-276 actually landed (corrected…)"*** — so a recall that stops at the description learns that roster runs no ratio gate. One clause. **DE's call, including "not worth it."**

  *(**Identified by its opening words, not by coordinates — this is the operative sentence, the one describing the edit DE would actually make, so a wrong position here misdirects the single reader this idea exists for. It has now been wrong three times** — "at the bottom", then "the last third", then a denominator that rotted under an append — **and the paragraph's opening words have been correct throughout.** As of 2026-07-26 it is line 21 of 41, recorded as evidence rather than as the address.)*
- Are there sibling memories with the same inheritance? The file links `[[games_row_vs_stat_rows_coupling]]`, `[[schema_drop_test_blast_radius]]` and `[[scouting_query_role_vs_dedup_filters]]`, none of which has been checked against E-276's settled shape. **This is now the highest-value question in the file** — it is the only one that has not been checked, and the two that were checked both came back smaller than reported. **Do not infer from that record that the siblings are clean**; the two checks found real-but-small residuals, not nothing, and the siblings have had no check at all.

## Notes

**⚠️ THE ORIGINAL NOTE HERE IS THE MECHANISM THAT PRODUCED THE ERROR, not the insight it was filed as — and that inversion is the most transferable thing in this file.** It read: *"Found by reading the file, not by grepping for the claim — and that is the part worth carrying. The superseded invariant shares most of its vocabulary with the corrected one, so a token grep returns the corrected passages too and reads as clean… the retired and surviving forms are near-homographs."*

**The near-homograph observation is TRUE and it cuts the other way.** When two forms share most of their vocabulary, a *reader* confuses them at least as readily as a grep does — and that is exactly what happened: the corrected passage was read as the superseded one, and TN-10's sufficiency direction was read backwards. **"I read it rather than grepping it" was offered as the reason to trust the finding, and reading is where the finding failed.** A grep for the paragraph this idea quoted would have returned zero hits and settled it in one command.

**The correction, stated as the rule this file should be remembered for:** *near-homograph forms defeat readers and greps in different ways, so neither instrument covers the other.* **Quote the literal text and resolve the quote against the artifact** — which is `.claude/rules/tool-output-integrity.md`'s standing requirement, and the check nobody ran here because the finding arrived already framed as the careful-method one. `.claude/rules/doc-sweep.md`'s retired-claims case still applies to the file; it just was not what went wrong in this idea.

Related: [[IDEA-185]] and [[IDEA-186]], the two residuals routed out of E-276 on substance. This one is routed out on **ownership** instead — the epic knows about it and is not permitted to fix it.

**Deflation history, kept because the shape of the decay is the finding.** Filed 2026-07-25 with two defects. **Pass 1** (same day) deflated Defect 2: its decisive check was a unit error, and the memory's own reading turned out best-supported. **Pass 2** (E-276 pre-dispatch amendment, 2026-07-25) deflated Defect 1 much further: a quoted paragraph that is not in the file, and a sufficiency direction stated backwards against the very Technical Note this idea cites as authoritative. **Two passes, two different error shapes, both shrinking the report** — and pass 1 did not check the section directly above the one it was deflating. **The residual after both is a cross-reference.**

---
Created: 2026-07-25
Last reviewed: 2026-07-25 (deflated twice — Defect 2 pass 1, Defect 1 pass 2)
Review by: 2026-10-23
