# E-279: Closure machinery — the write-guard wedge and the archive-rename deadlock

## Status
`COMPLETED` — set **2026-08-01** at closure. Previously `ACTIVE` — set **2026-08-01** on dispatch; `READY` — set **2026-07-28** on operator ruling.

⚠️ **COMPLETED is AUTHORED here and FINALIZES only on a green full suite.** Per the Full-Suite-Green Closure Gate, this flip is written in the epic worktree at Step 8 sub-step 3 so it rides the closure patch — it is on disk before the gate runs, and a red `python -m pytest tests/` aborts the closure commit and reverts the applied patch. **COMPLETED is therefore never *committed* on a red suite.** If you are reading this in an unmerged state, it is a claim about intent, not a certification.

⚰ **The READY freshness clock is DISCHARGED, not merely satisfied.** It ran 2026-07-28 → 2026-08-01 (4 days against a 60-day gate) and `ACTIVE` epics are exempt (`.claude/rules/workflow-discipline.md`, READY Freshness Gate). The 2026-09-26 re-confirm date no longer applies; do not reinstate it.

**⚠️ DISPATCH PREREQ — story 01 runs FIRST.** A binding design constraint (TN-1), now encoded as dependency edges 01→02 and 01→03 rather than prose alone. Dispatching this epic necessarily quotes worktree paths beside `git add`, and until story 01 lands, doing so manufactures a phantom worktree directory that wedges every agent on the machine.

<!-- Lifecycle: DRAFT → READY → ACTIVE → COMPLETED (or BLOCKED / ABANDONED) -->
<!-- Both open questions RESOLVED 2026-07-28 (see Open Questions + History). -->
<!-- Review complete: internal iteration 1, internal iteration 2, claude-architect validation, Codex spec review 1. See the Review Scorecard in History. -->
<!-- Setting READY does NOT authorize dispatch — that is a separate operator call (Dispatch Authorization Gate). -->

## Overview
Two defects in the closure machinery share one signature: both are invisible to every check the closure sequence performs, and both were found only because someone happened to look. A passive telemetry hook creates worktree directories from command *text*, and the write guard reads directories as dispatch state — so quoting a worktree path near `git add` locks every agent on the machine out of the main checkout. Separately, the archive rename at closure strands references in agent-owned files during a window in which no one is permitted to fix them. This epic closes both, and deliberately does not grow past them.

## Background & Context
Promoted from two idea files, both carrying executed evidence; start from them rather than re-deriving.

- `.project/ideas/IDEA-230-dispatch-log-hook-resurrects-worktrees-and-never-ships.md` — two coupled defects, both verified by controlled test. Defect 1 is LIVE and has a one-command repro: a bash command that merely MENTIONS a worktree path alongside `git add` — never actually staging anything — finds the directory already created, because PreToolUse fires first. A fabricated epic number sufficed. Defect 2: the dispatch-log TSV has never been committed in nineteen epics.
  - **BOTH conditions are required, and an earlier compression of this line dropped one.** It read "a bash command whose only content was *assigning* a worktree path string to a shell variable," which omits the `git add` half. `send-message-counter.sh:112` gates its Bash branch on a CONJUNCTION — the command must contain a `/tmp/.worktrees/baseball-crawl-E-` substring **AND** match a `git add` regex — and only then does `:121` run the `mkdir -p` that manufactures the phantom directory as a parent side effect. A bare assignment with no `git add` token cannot reach it. Story 01's Context states this correctly; this Background line was the outlier. Re-derived independently by `cr-e279` and PM from the hook source (see TN-12).
  - The alarming property survives the correction intact: the command need never stage anything, so *quoting* both halves in an audit, a grep, or an example is sufficient. The SendMessage branch also runs `mkdir -p`, but only after `resolve_worktree()` returns non-empty, so it cannot conjure a new epic ID from nothing — the Bash branch is the sole manufacturing path.
- `.project/ideas/IDEA-228-archive-rename-strands-outbound-refs-nobody-can-fix.md` — the original defect record plus claude-architect's 2026-07-28 refinement, which is the design this epic implements.

Both were hit at E-278's closure. Neither is anybody's mistake: at E-278 the orchestrator sequenced correctly, PM owned its files correctly, and the hook did exactly what it is for. **All three behaved correctly and the outcome was still a deadlock**, which is the signature of a procedural gap rather than an error.

### Verification status of the inherited claims
Every checkable claim in both idea files was re-verified against the live repo during planning (PM and claude-architect independently). They hold. Two premises inside claude-architect's *refinement* did NOT hold and have been corrected — see TN-7. Do not restore either.

## Goals
- Make the write guard read authoritative state (`git worktree list`) instead of a filesystem side effect, closing the whole wedge class rather than the one instance, without converting a fail-closed guard into a fail-open one.
- Restructure closure so the archive rename happens in the epic worktree before patch generation, creating the repair window whose absence is the IDEA-228 deadlock.
- Make the outbound-reference check non-optional via one script at two call sites, so the defect does not depend on somebody thinking to look.
- Delete the dispatch-log telemetry mechanism (operator ruling, OQ-1), rather than leaving a hook whose output has never once been readable — and delete every committed claim that it works.
- Leave the context layer net-smaller in prose terms: the restructure deletes four conditional rename-ordering rules and adds one script invocation.

## Non-Goals
- **No semantic reference detection.** The sweep sees literals. A reference naming an epic without spelling its path is a `.claude/rules/doc-sweep.md` step-2 problem and cannot be mechanized. Do not grow the script toward it.
- **No repo-wide dead-reference cleanup.** 25 dead epic-directory paths exist across 33 files (TN-6). Exactly one is in scope. The rest are captured as an idea.
- **No override for the pre-commit backstop** (TN-3). `git commit --no-verify` is documented as the sole escape, with its PII-scan consequence stated.
- **No re-scope of E-271** (TN-8). OQ-2 authorized ONE bounded reconciliation of `E-271-03-disjoint-file-cluster.md` — retiring criteria that events falsified — executed at planning time on 2026-07-28. E-271's epic file, its other stories, its status, its ordering and its ratchet exception are untouched. The line-anchor collision (TN-8a) remains an E-271 dispatch-time item, not an action this epic takes.
- **No new agent-memory grant.** `agent-routing.md`'s own-memory carve-out already holds the authority; this epic adds a *ceiling*, not a grant (TN-4).
- **No timeout on the guard's git call** (TN-2), and no per-line suppression marker on the sweep (TN-3). Both were considered and declined with reasons.

## Success Criteria
- A directory under `/tmp/.worktrees/baseball-crawl-E-*` with no corresponding `git worktree list` entry no longer puts the write guard into dispatch-active mode.
- A crashed dispatch (directory removed without `git worktree remove`) still enforces dispatch-active mode — the fix is strictly stronger here, not a trade.
- `scripts/check_archive_refs.sh E-NNN` exits non-zero when a literal `epics/E-NNN-` reference survives outside `.project/archive/`, and is wired at both the in-window hold and the fail-closed backstop.
- The closure sequence contains no main-checkout archive rename, no rename-ordering constraint on the rollback path, and both rollback paths collapse to `git reset HEAD` + `git apply -R --3way`.
- `src/reports/llm_analysis.py`'s source-of-truth citation resolves to a path that exists.
- The dispatch-log telemetry mechanism is deleted in all four places it lives (TN-13), and no committed file is left asserting that it works.
- **The wedge class is closed by stories 01 and 02 JOINTLY, and neither is credited alone** (claude-architect, 2026-07-28): after 02 nothing manufactures directories under `/tmp/.worktrees/`, and after 01 a directory that appears by any other means cannot wedge the guard anyway. The stories stay independent — that is the design intent — but the property is the conjunction, and an assessment crediting either story with closing the class is wrong.

## Stories
| ID | Title | Status | Dependencies | Assignee |
|----|-------|--------|-------------|----------|
| E-279-01 | Write guard reads authoritative worktree state | DONE | None | claude-architect |
| E-279-02 | Dispatch-log telemetry deletion | DONE | E-279-01 ✅ | claude-architect |
| E-279-03 | Closure restructure — rename moves into the worktree | DONE | E-279-01 ✅ | claude-architect |
| E-279-04 | Archive-reference sweep script and its two call sites | DONE | E-279-03 ✅ | claude-architect |
| E-279-05 | Reconcile software-engineer memory to the deleted hook | DONE | E-279-02 ✅ | software-engineer |

Story order is a **design constraint, not a preference** — see TN-1. Story 01 runs first because dispatching this epic is what triggers the wedge it fixes.

⚠️ **The 01→02 and 01→03 edges were ADDED 2026-07-28 (Codex P1-2) and must not be removed as redundant.** Until then, "story 01 first" existed **only in TN-1's prose** while the table and every Dependencies section recorded `None` — so under the workflow contract these stories were executable in **any** order, and the binding constraint bound nobody. **Encoding the edges was chosen over strengthening the prose deliberately**: a dispatcher reads the table, not a technical note. This is the epic's own structure-over-prose lesson applied to itself, and it is the sharpest finding in the Codex round for that reason — **the epic spent a whole iteration cataloguing claims that live in prose and are contradicted elsewhere, and its own sequencing constraint was one of them.**

**Story 01 remains `Blocked by: None`.** Only its `Blocks` line changed, so its deliberate property — it closes even if everything else slips — is untouched. The resulting order is 01 → {02, 03} → {05, 04} via the pre-existing edges.

## Dispatch Team
- claude-architect
- software-engineer

**Every story has exactly ONE owner. This was not true until 2026-07-28 and the correction is worth reading before anyone re-folds it.** An earlier form carried the software-engineer memory reconciliation and the E-271 reconciliation as ACs *inside* claude-architect's story 02, giving one story three owners.

**claude-architect raised a positive objection (2026-07-28) and PM adopted it.** `.claude/rules/context-layer-assessment.md` (Learning-Loop Lifecycle → Deletion-Side Eviction → Ownership) reserves each agent's memory directory to that agent: a sweeper "MAY read ANY dir to IDENTIFY hits and report them as the closure seed, but only the owning agent edits its own content." PM verified that clause on disk. Since **dispatch assigns stories, not acceptance criteria**, an AC owned by a second agent either double-crews one story or quietly puts the story's owner over the ownership line. The SE work is therefore its own story (E-279-05), not an AC.

**Why the objection came when it did, which is a design fact rather than a process hiccup.** Under the DELETE ruling the entire hard-send-cap subsection loses its referent while the surrounding send-receipt material stays true and valuable, so deciding what survives became a *judgement about SE's own memory* — precisely the reserved class. The earlier no-objection was recorded before the operator ruled, and is superseded rather than contradicted.

⚠️ **CORRECTED 2026-08-01 (`se-e279`, executed not reasoned) — this passage carried a FALSE PREMISE under a CORRECT conclusion, which is the exact Class A shape this epic exists to catalogue.** It previously opened: *"While OQ-1 was open, the SE-memory fix might have been near-mechanical — `DENY_AT=60` / `WARN_AT=40` are simply wrong numbers."* **Those numbers were RIGHT.** `git show c990446 -- .claude/hooks/send-message-counter.sh` returns `-WARN_AT=40` / `-DENY_AT=60`, and the retirement note that commit added reads *"previously warned at 40 sends and HARD-DENIED `SendMessage` at 60."* SE's memory file said deny 60 / warn 40. **Identical.**
- **The origin is a drift of REFERENT, not of fact.** TN-8b (below) says the retirement values match *"neither the AC nor E-271's research file"* — **that is CORRECT**, and it is about **E-271-03's AC** (`DENY_AT=25` / `WARN_AT=15`). Between TN-8b and story 05's Technical Approach, ***"the AC"* became *"this file,"*** and the *"simply wrong"* verdict attached to the wrong document, then propagated to this line and to story `:17`. **There were never three sets of numbers — there are TWO:** `{40, 60}` (the real hook, its retirement note, and SE's memory) and `{15, 25}` (E-271-03's AC and E-271's research file).
- **The conclusion never needed the premise.** The passage dies because the **MECHANISM was deleted**, not because its numbers were wrong — so no AC changed and no scope moved. That is precisely why it survived: **every reader checked whether the claim fitted the conclusion, which it did, and none checked whether it was derivable.**
- ⚠️ **THE ORIGIN, recovered 2026-08-01 and sharper than the drift account above — the hook had THREE vintages, not two, and E-271-03's AC was NOT WRONG WHEN WRITTEN.**
  - `15/25` (E-256 placeholder, live through E-260) → **operator raises to `40/60` at `dc1cc9e`** → **operator retires the deny at `c990446`** → deleted outright by E-279-02.
  - **PM verified this from two ordinary Reads rather than accepting the relay** (the epic's own standard, and the relay in question had already cost one wrong ruling that day): `.claude/agent-memory/claude-architect/epic-codifications.md:65` — *"The thresholds `WARN_AT=15` / `DENY_AT=25` were stale — the operator raised them to 40/60 at `dc1cc9e` and later retired the deny entirely at `c990446`"* — and `:175`, an independent entry recording *"current values 40/60, set by the operator on E-276's data, **replacing** the E-256 placeholder."* PM's own `archived-epics.md:79` independently records the E-260-era hook as *"warn 15/deny 25"*, corroborating the first vintage from a third file.
  - **So E-271-03's AC was written against the pre-`dc1cc9e` hook and WENT STALE UNDERNEATH ITS AUTHOR.** *"The AC was wrong"* and *"the AC went stale"* are **different findings**, and only the second explains how three careful documents agreed with each other: **none of them was careless — the ground moved.**
  - **This is E-279's own TN-3 — *an epic must re-run every measurement taken against a pre-fix file* — firing ONE EPIC UPSTREAM, on us.** The rule we wrote for this epic's benefit was already binding on E-271 and nobody applied it there. **A stale-verdict account and a wrongness account demand opposite responses**: wrongness says find who erred, staleness says re-run the measurement.
- **What the correct conclusion was shielding, concretely.** Under AC-3 **SE authors the replacement**. Taking the spec's word would have shipped a tombstone reading *"this file cited the wrong numbers"* — **a fresh false claim written into permanent memory, inside the story whose purpose is retiring a false claim.** It was caught by **execution**, not by re-reading: consistent with this repo's 8-of-8 record that no author catches their own false premise under a right conclusion.

**E-279-05 is blocked-by E-279-02** (PM, extending the objection): reconciling before the hook is deleted would have SE writing against an interim state that story 02 then removes.

The E-271-03 reconciliation was the third owner. It is **not** a story — editing a planning artifact is PM's planning function, not implementation — so PM executed it at planning time on 2026-07-28 (OQ-2). Dropping it to a follow-up idea was rejected by both claude-architect and PM: a flag that lives only in a completion report reaches nobody, the failure `context-layer-assessment.md` names in that same section.

## Technical Notes

### TN-1: Dispatch order is a design constraint
Two independent reasons, both from claude-architect's §5:

1. **The wedge fix must land first, because dispatching this epic is what triggers the wedge.** Stories here necessarily quote worktree paths next to `git add`. Any Bash command containing both creates a phantom worktree directory for whatever epic number appears in the string. During dispatch this is invisible (a real worktree already has mode 1 active); it surfaces *after* closure as a session-wide write block for the next session. IDEA-230 records this happening for real — the agent investigating the hook wedged the repository by reading about it.
2. **E-279 modifies the machinery that closes it, and the changes go live mid-closure.** `.claude/settings.json` invokes the guard as `"$CLAUDE_PROJECT_DIR"/.claude/hooks/worktree-guard.sh` — the main checkout — so the new guard becomes live when sub-step 4 applies the patch, worktree still standing, five sub-steps to go. `git config core.hooksPath` is `.githooks` (verified), so the new backstop fires on E-279's own closure commit at sub-step 10.
   - **Bounded honestly:** the invocation *path* is verified; whether the harness re-reads a hook per invocation or caches its contents at session start is NOT. If hooks are cached, the guard change lands by the next session rather than mid-closure — the *timing* claim would be wrong, the conclusion would not. TN-2's posture is safe under both readings, which is why it is stated as a rule rather than a prediction.

### TN-2: E-279 closes under the OLD sequence
**Binding, and stated explicitly because a reader who assumes otherwise will sequence the closure wrong.** E-279's own closure follows the closure sequence in effect when its closure *began* — the main-checkout archive rename at sub-step 6, not the restructured worktree-side sub-step 3 sequence this epic ships. The restructured sequence takes effect from the **next** epic.

Two consequences that are not optional:
- The new `worktree-guard.sh` goes live at sub-step 4, mid-closure. Story 01's fix must be conservative enough to be safe in that state — which it is, because the registry-unreadable path falls back to exactly today's behavior.
- The new pre-commit backstop fires on E-279's own closure commit, so **E-279 cannot commit unless its own archive-reference sweep is clean.** That is a good live test and a bad surprise; it is recorded here so it is neither.

### TN-3: The instrument — one script, two call sites, no override
`scripts/check_archive_refs.sh E-NNN` greps the literal `epics/E-NNN-` outside `.project/archive/`, non-zero exit on hits.

- **In-window hold** — step (iii) of the restructured sub-step 3, after the worktree rename and before `git add -A`. Repair is cheap here: fix in the worktree, it rides the patch.
- **Fail-closed backstop** — `.githooks/pre-commit`. This is the half that survives nobody looking, which is IDEA-228's actual complaint ("provided somebody looks").

**The two call sites cover different trees, so the backstop is NOT redundant with the in-window hold.** The in-window sweep runs in the worktree and sees the worktree. Step 7a stages *main-checkout* ancillaries — vision signals, idea captures, plan leftovers — which are not in the worktree and are invisible to it. Only the pre-commit gate sees the complete staged set. Stated here because someone will otherwise propose dropping the backstop as duplicative.

**Backstop trigger must be rename-shape-independent.** `git apply --3way` may land the move as delete+add rather than a rename when similarity drops — plausible here, since `epic.md` is rewritten during the epic. Do NOT condition on `rename from` appearing in the patch. Correct form: discriminate on ARCHIVING — a `D` under `epics/E-NNN-` AND an `A` under `.project/archive/E-NNN-` for the same epic ID, enumerated with `--no-renames` (story 04 AC-5). This is agnostic to git's representation.

⚰ **THE INERTNESS ARGUMENT BELOW IS FALSE AGAINST THE CURRENT BASELINE. Corrected 2026-08-01, measured by `ca-e279` in a scratch repo and verified by PM against the file.** It read: *"a pure-rename staged set yields ZERO entries from `git diff --cached --name-only --diff-filter=ACM`, so `.githooks/pre-commit:26-28` returns 0 before anything below it runs. A correctly-enumerating gate placed below that line is inert on exactly the hand-run case."*
  - **That measurement was TRUE of `--diff-filter=ACM` and is FALSE of the file on disk.** `.githooks/pre-commit:28` now enumerates with **`ACMR`**; a staged pure rename yields **count=1**, so the `exit 0` at `:30-31` never fires and a later gate is **NOT** inert. **The measurement is preserved above as dated evidence of the pre-`9b62395` file; the INFERENCE drawn from it does not survive the fix.**
  - **The refutation was already in the hook, written for this purpose.** The `STAGED_ARR` comment states *"R is load-bearing: without it … a rename-only staged set empties the list and exits below before any gate runs."* **It describes the exact behavior TN-3 relies on as the DEFECT that `9b62395` removed.** Checkable at any time by reading four lines.
  - ⚠️ **CITE THAT COMMENT BY PHRASE, NEVER BY LINE — this note cited it at `:14-17` and story 04's own 83-line gate insertion moved it to `:97-100` within the hour.** `:14-17` today is the NEW gate's comment, so the old citation now points at different content that reads plausibly. **The quote was accurate throughout; only the numbers moved.**
  - **THE RULE, which this epic has now paid for three times in one file in one afternoon: a line number in an actively-worked file is stale by default, and the party most likely to invalidate a citation is the implementer ACTING ON THE NOTE THAT CONTAINS IT.** `.githooks/pre-commit` carried three vintages — pre-`9b62395`, post-`9b62395`, post-insertion — and `ca-e279`'s statement of the mechanism is the one to keep: ***"I invalidated PM's citation by doing the work PM's note authorized."*** **Generalization of the mid-planning-fix rule: an epic must re-run every measurement taken against a pre-fix file, AND the implementer's own change is itself such a fix.** Cite by phrase, symbol or heading; a number survives only until someone does the work you asked for.
  - **Vintage, which is the generalizable part: TN-3 was measured BEFORE `9b62395` and AC-9 pins the state AFTER it, so two of this epic's own artifacts describe different versions of one file.** TN-14 records `9b62395` landing before dispatch. **An epic that ships a fix mid-planning must re-run every measurement taken against the pre-fix file** — the fix does not announce which notes it invalidated.
  - ⚠️ **AC-5's first-placement requirement STANDS, on SUBSTITUTED and deliberately WEAKER grounds.** It no longer rests on "otherwise the gate never runs," which is false. It rests on **ordering and cost**: the gate catches a bad closure before the index-snapshot step, and it keeps the archive diagnostic from being preempted by a PII `exit 1` that would cost the operator a second round trip. **That is preference-grade, not necessity-grade, and the AC must not imply otherwise.** `ca-e279` reported this rather than complying silently, citing PM's own ruling back: a right conclusion is exactly what buries a false premise.

**One-epic scoping is load-bearing.** The script takes a single epic ID and must reject a wildcard. A repo-wide `epics/E-[0-9]{3}-` sweep immediately collides with hard-coded epic-path literals in the test suite — the synthetic `epics/E-999-demo/` fixture in `tests/test_doc_pii_hook.py` (11 occurrences), and separately the hard-coded `epics/E-129-…` literals in `tests/test_pii_scanner.py` (~`:536`/`:539`/`:564`), which are **not** synthetic: E-129 is archived, so those are live dead paths that are nonetheless EVIDENCE — test inputs to a prefix predicate — and must never be repointed. *(An earlier form of this note attributed the `E-999-demo` fixture to both files; `E-999-demo` appears in `test_pii_scanner.py` zero times. The conclusion is unaffected and arguably strengthened.)* Story 04 AC-2 carries the full worked example.

**No override (claude-architect ruling, Q1).** The gate can only fire when the in-window hold was skipped or a reference was introduced after it — both are the "nobody looked" case the gate exists for, so an override has no legitimate user. `.githooks/pre-commit` has no per-gate override convention to be consistent with (this was verified; the earlier claim that it did is corrected in TN-7). Document `git commit --no-verify` as the sole escape **and state that it disables the PII scan too**, so it reads as an operator decision rather than an agent's tool. A per-line suppression marker was considered and **declined**: markers suppress matches without certifying safety, and a standing suppression can later hide a real dead pointer. The one narrow deadlock — a line legitimately quoting the pre-archive path as historical fact — has a cheap owner-side fix: reword so the line does not spell the dead path.

**A clean exit means "no literal path references," never "no stranded references."** State this bound in the skill so a clean exit is never over-read.

### TN-4: The repoint ceiling is a tightening, not a grant
`agent-routing.md`'s own-memory carve-out already reserves an agent's own directory to that agent and routes a story touching a *different* agent's memory to claude-architect. **claude-architect already holds the authority.** Writing a fresh grant would restate existing authority in looser words, and a looser restatement of an ownership rule is how the ownership erodes.

Add a **bound** instead, as one sentence beside the existing carve-out: in a closure archive-path repoint, **the only bytes that may change are the path literal**. Rewording, retiring a claim, updating a verdict, or adjusting a rating remains the owning agent's. **If the repoint cannot be made without touching more, it is not a repoint** — route it to the owner or capture it as an idea.

### TN-5: Who runs the sweep, and who rules on its hits
Running a deterministic script and reading an exit code is repository state, not content judgement — the same shape as sub-step 2's clean-tree preflight. **The main session may run it.** It **must not rule on the hits**: a non-zero exit is a HOLD, and each hit routes to its owner.

Owner routing (corrected — claude-architect's original two-way table would misroute): PM for `epics/` and `.project/`; claude-architect for `.claude/**`; **software-engineer for `src/`, `tests/`, `scripts/`; docs-writer for `docs/`.**

Adjudication is domain work because every hit is a criterion-versus-evidence call: an old-path citation may be a pointer to repoint (**criterion**) or a record of where something was observed (**evidence**, which editing would falsify). That cut is governed by `.claude/rules/tool-output-integrity.md`, and a sweep that "fixes every stale-looking path" destroys records.

### TN-6: The defect is already shipped 25 times; exactly one is in scope
Measured by claude-architect during planning: **27 distinct epic-directory paths referenced outside `.project/archive/`, 25 of which do not exist**, across 33 files (15 under `.project/`, 12 under `.claude/`, 2 `tests/`, 2 `docs/`, 1 `src/`, 1 `scripts/`).

**In scope — the single `src/` instance.** `src/reports/llm_analysis.py:44` states the shipped LLM prompt is "Source of truth, reproduced verbatim from `epics/E-243-probable-starter-usefulness/E-243-04-narration-prompt.md`." PM verified: that path does not exist; the file lives at `.project/archive/E-243-probable-starter-usefulness/E-243-04-narration-prompt.md`. It is shipped code making a source-of-truth claim, the fix is one line, and it is exactly what the sweep script would catch — the natural companion to the script story.

**Out of scope — the other 24, captured as [[IDEA-231]].** PM scope decision, with claude-architect's recommendation on record and agreeing. Reasoning: the backlog is 25 separate criterion-versus-evidence judgements across four owners, and a sweep that "fixes every stale-looking path" destroys records (`.claude/rules/tool-output-integrity.md`). Turning a micro-epic into a 33-file sweep is precisely the late scope growth IDEA-228 itself warns against.

⚠️ **What this reasoning does NOT rest on.** An earlier draft of this note said "much of it is legitimately evidence." **That proportion was never measured** — claude-architect flagged it as its own inference after the enumeration, having not read the 33 files to classify them, and PM restated it unchecked before catching it. The scope decision is **independent of the ratio**: 25 judgements across four owners is out of scope at any proportion. Do not reintroduce a quantifier here, and do not let a later reader treat "mostly evidence" as an established finding — measuring it is [[IDEA-231]]'s first open question.

### TN-7: Two corrected premises — do not restore either
Both sat inside claude-architect's refinement under conclusions that were and remain correct. This is the shape where a right verdict shields a wrong reason, so they are recorded rather than silently fixed.

1. **"Give the backstop an override consistent with the existing PII gate's convention."** FALSE — `.githooks/pre-commit` has no per-gate override convention. Ruling: no override (TN-3).
   - ⚠️ **The ORIGINAL correction to this item was itself false, and is corrected here.** It read: *"Its only non-blocking path is `check_doc_pii.sh` exit 3 (INCONCLUSIVE)."* There are **THREE** non-blocking exits — a missing-scanner skip (`:8-11`), an empty-staged-set exit (`:26-28`), and `check_doc_pii.sh` exit 3 for a fresh clone. **None of the three is an override**, and that is why the ruling survives: an override is a mechanism by which an author waives a gate on content they are staging, and none of these is one — they are a missing-dependency skip, a no-work skip, and a fresh-clone accommodation. The ruling turns on the absence of an override MECHANISM, never on the count of non-blocking exits.
   - This is a **third-generation instance of the same shape this TN exists to record**: a right verdict resting on a wrong premise, inside the very note written to catch right-verdict-wrong-premise defects. claude-architect authored it, `cr-e279` caught it by opening the file, and claude-architect owns the correction. Its own observation, recorded because a self-reported pattern is worth more than an observed one: **three times during this epic's planning its conclusion was right and its cited premise wrong** — this item, the F6 wording that gutted story 01 AC-1's RED, and a `:477` line-position claim read off rendered wrapping. All three were caught by someone opening the file; none by re-reading the reasoning.
2. **"Condition the backstop on the staged diff containing an `epics/` → `.project/archive/` rename."** Contradicted the same refinement's own instruction never to build a check on `rename from`. The second is correct; the trigger is rename-shape-independent (TN-3).

**Residual 2 is DISCHARGED, not open.** claude-architect checked whether the full-suite gate or runtime smoke read the real `epics/` tree. The only consumers in `src/`, `tests/`, `scripts/` are a string constant in `src/safety/pii_patterns.py:168` (prefix comparison, no filesystem read), the two PII tests which build their own temp repos, and a docstring in `scripts/codex-spec-review.sh`. **Nothing reads the real `epics/` tree at suite or smoke time.** The archived-early layout is safe; no story, no AC.

### TN-8: Boundary versus E-271, and what E-271 must re-confirm
E-271 (the workflow/process-redesign epic) is READY (2026-07-21). Its Non-Goals explicitly exclude "**No Step-8 closure machinery changes**", so IDEA-228's territory is clear of it. But there are two live interactions, both verified independently by PM and claude-architect.

**(a) File collision on `.claude/skills/implement/SKILL.md`, with line-anchor rot.** E-271-01 owns that file for Phase 3 Step 5 item 3, Steps 1a-1d and a Closure Evidence reference. The scopes are disjoint *within* the file, but this epic's restructure is not confined to Step 8 — deleting sub-step 6 forces edits at multiple cross-references.

⚠️ **Two corrections to this note, both of which a reader would otherwise act on. Treat every line number here as ILLUSTRATIVE.**

1. **The site list was incomplete.** Independently regenerated: `sub-step 6` occurs at **537, 655, 656, 658, 660, 676, 680, 683**, and `archive rename` additionally at **629**. The earlier list ("537, 655-658, 662, 666, 676, 680-688 and 692") omitted **660** (sub-step 5b, the Step 1d runtime smoke, carrying TWO references) and **629** (the Step 8 preamble, which the restructure falsifies).
2. **The `:630` sequencing premise was FALSE, and its conclusion is unaffected but STRONGER.** This note previously read *"Line 537 sits above E-271's `:630` anchor, so E-279 landing first shifts it"* — implying E-271 landing first might avoid the collision. **`SKILL.md:630` is already a blank line**, as are `:462` and `:634`; the three COMPLETED-on-disk-vs-committed restatements E-271-01 pins at `~:462/:529/:630` actually live at **477, 547, and 655-656**, and `:529`/`:530` now hold Step 1d content rather than the "Step 2 scorecard area" E-271-01 AC-3 claims. Four commits touched the file after E-271 was written on 2026-07-21. **E-271's anchors are ALREADY STALE, so dispatching E-271 first does NOT avoid the collision.**

**E-271 dispatch-time item (unchanged, and strengthened by correction 2):** re-derive *all* anchors by phrase rather than spot-checking the ones we predict moved, and convert bare line numbers to phrase anchors while there — otherwise the identical collision recurs on the next `SKILL.md` edit.

**(b) E-271-03's hook ACs are stale and partly prohibited.** The operator hand-retired the send-cap at `c990446`. E-271-03 AC-1 requires sweeping "the `DENY_AT=25` constant (~:31)" and a header reading "warn at 15, deny at 25", while **KEEPING** "`WARN_AT=15` and the advisory warn (~:89-99)". **None of those strings exist in the live hook**, whose only threshold mention is the line-9 retirement note recording the removed values as 40 and 60 — a third set of numbers matching neither the AC nor E-271's research file. So:
- The **deny half is discharged** — satisfied by hand, more completely than AC-1 asks.
- The **warn half is not merely stale, it is affirmatively prohibited.** `send-message-counter.sh:21` reads *"Do not reintroduce a threshold here without an operator order."* Executing AC-1 literally would reintroduce `WARN_AT=15` and an advisory warn against that standing order.
- **AC-6 conflicted with this epic**: it required the hook to keep logging and stay registered, which the OQ-1 delete ruling falsified outright.

**E-279 takes sole ownership of `.claude/hooks/send-message-counter.sh`** — a scope call inside this epic.

**(c) RESOLVED 2026-07-28 — the reconciliation is done, not pending.** OQ-2 authorized it and PM executed it at planning time rather than deferring it to dispatch, because E-271 is READY and dispatchable *today* while E-279 is DRAFT: deferring would have left a prohibited criterion live in a dispatchable epic for the whole interval. Executed in `E-271-03-disjoint-file-cluster.md` only: AC-1 retired under a dated tombstone (marker BEFORE the quoted text, per `.claude/rules/doc-sweep.md`), the hook struck from its Files list, AC-6's hook clauses removed, and the passages that RESTATE the retired requirement — title, Description, the "four files" counts, the Technical Approach — reconciled, since `doc-sweep.md` makes a title or derived summary carrying a retired predicate a defect in its own right. E-271's epic file received a History entry only.

**The residual set is EIGHT. This is the authoritative enumeration; two earlier statements in this very file disagreed with it and with each other.**

| # | Site in `E-271/epic.md` | What it restates |
|---|---|---|
| 1 | Stories-table row title | the send-cap warn-only item |
| 2 | Success Criteria line 33 | "The SendMessage hook no longer denies (warn-only)" |
| 3 | TN-1's "E-271-03 owns four disjoint files" + the hook in that list | the four-file count |
| 4 | TN-2 item 1a | the warn-only conversion |
| 5 | TN-10's −22L hook-deletion reference | the deletion as a ratchet input |
| 6 | TN-2 / Open-Questions runaway-ceiling residual | a judgement premised on warn-only |
| 7 | **TN-11:112** | "the only executable file touched is a bash hook (1a)" |
| 8 | **TN-12:119** | "it edits … a bash hook, skills, a rule, the template, an agent definition" |

⚠️ **Provenance, because the drift was internal and is the more instructive half.** This note previously listed **four** (items 1, 2, 4, 5); the OQ-2 sub-bullet below listed **five** (adding 3); E-271's own History:137 lists **six** (adding 6). Three enumerations of one set, **two of them in this file, eleven lines apart.** Items 7 and 8 were found by `cr-e279` by independent enumeration and appear in none of the three.

**Items 7 and 8 carry the lesson worth keeping.** Both say **"a bash hook"** and contain NOT ONE of the tokens a sweep would grep (`send-message-counter`, `DENY_AT`, `WARN_AT`, `sends.count`, `dispatch-log`). A case-insensitive token grep of E-271 returns neither. They surfaced only by reading — which is `.claude/rules/doc-sweep.md`'s central claim demonstrated on this epic's own material, and the direct argument for the written-verdict clauses in E-279-02 AC-3 and E-279-05 AC-4. In both, the VERDICT still stands (SE is rightly waived; E-271 is rightly `Destructive-Path: no`) while the stated REASON enumerates a file E-271 no longer touches: a right conclusion shielding a false premise.

**E-271's epic file remains UNEDITED, and that is a standing ruling, not an oversight.** A second authorization to touch it was requested and DECLINED (see OQ-2). This table is reachable from E-271: its History:135 already points forward to "E-279 epic TN-8(c)" **by story ID rather than by path**, so the citation survives E-279's own archiving. A future E-271 planner should treat all eight as retired with AC-1.

*(Also noted by `cr-e279`, and inside the same declined bound: one live untombstoned instruction survives at `E-271/epic.md:60` — TN-2 item 1a still says "convert to warn-only … KEEP `WARN_AT=15`", the affirmatively prohibited action. A dispatched implementer is shielded by E-271-03's Context:15, which is why this is disclosed rather than edited.)*

**The ratchet exception is undisturbed, and this was checked rather than assumed.** E-271's TN-10 already excludes the hook's −22L from the ratcheted figure as outside the four counted subtrees, so dropping the hook edit leaves the ~+35-55L estimate the operator re-confirmed on 2026-07-21 exactly as signed.

### TN-9: Consultation completeness (per-domain verdict, binding)
- **claude-architect** — CONSULTED. Design owner. The guard predicate (TN-2 story 01), the closure restructure, the script design and both call sites, the repoint ceiling, the dispatch ordering, and the TN-6 measurement are all its work. Two of its own premises were corrected in review (TN-7).
- **software-engineer** — CONSULTED-BY-ASSIGNMENT, not by advisory spawn. SE owns **E-279-05** outright (its own memory directory) and is on the dispatch team. No advisory consultation was sought because no `src/` design decision is open: the single `src/` change (TN-6) is a one-line path repoint with no design content.
- **data-engineer** — WAIVED. No schema, migration, ETL, or query. No DE domain triggered.
- **api-scout** — WAIVED. No GameChanger endpoint, payload, credential, or data-availability surface. No api-scout domain triggered.
- **baseball-coach** — WAIVED. No coaching-value, stat-definition, or report-content decision. No coach domain triggered.
- **docs-writer** — WAIVED for planning. Named in TN-5's owner-routing table as a future sweep-hit owner, which is a routing statement, not work in this epic.

### TN-10: Destructive-Path
`Destructive-Path: no`. This epic edits hooks, a skill, two rules, a new script and one `src/` comment line. No `DELETE FROM`, `DROP`, retire, or purge code. (The field is E-271's forward convention; recorded here so it is already present if E-271 lands first.)

### TN-11: Closure obligations
1. **Context-layer assessment will fire** — trigger 4 (closure-sequence and agent-coordination change) is a certain yes, and triggers 1, 3 and 6 are likely. Record all eight verdicts explicitly.
2. **Deletion-side eviction** (unconditional — OQ-1 ruled delete): sweep for `.dispatch-log`, `sends.count`, `rounds`, and the TSV's "rides the closure patch" claim across `.claude/`, `.gitignore`, and every agent-memory directory — the `MEMORY.md` indexes AND their topic files, since an index-only grep ships a false-clean.
   - ⚠️ **The "flag, do not edit" clause that stood here has NO LIVE INSTANCE in this epic, and this note previously implied it did.** It read "Hits outside an agent's own directory are flagged, never edited," which contradicted story 02 AC-3. **The story is right and this TN was stale.** `context-layer-assessment.md` reserves flagging for owners who are NOT on the dispatch team; all three agent-memory owners with hits here ARE on it — `software-engineer`'s hit is story **E-279-05**, `product-manager`'s (`archived-epics.md:79` and `e276-health-gate-triage.md:360`) are PM's own and reconciled at closure, and `claude-architect`'s (`epic-codifications.md:64` and `:171`) are its own directory and it implements story 02. **Nobody flags a hit to themselves.**
   - Every surfaced line takes a **written verdict, "no change needed" included** (story 02 AC-3) — not a removal-or-annotation binary. `archived-epics.md:85` is the worked counter-example: a true, live "rides the closure patch" statement about closure patches generally, which must be neither removed nor annotated.
3. **The `.git/info/exclude` residue is NOT repo-fixable, and leaving it is a DECISION, not an oversight.** The `.dispatch-log/` line at `.git/info/exclude:19` is what actually kept the TSV out of every commit, but the file is uncommitted and per-clone: no story can change it verifiably and no reviewer can see a change to it. Once nothing creates `.dispatch-log/`, it is inert. Recorded here at claude-architect's request precisely so a later reader does not file it as a miss. Report it to the operator at closure as a manual per-clone item rather than implying the deletion was complete.
4. **Re-scope [[IDEA-116]] (PM-owned, no story).** That idea says three hooks share an `ls -d … | head -1` glob that mis-attributes across concurrent worktrees. Verified against live code during planning: **`send-message-counter.sh` was already fixed independently** (it carries a `resolve_worktree()` with an explicit "Never `head -1`" comment) and this epic deletes it outright; **`worktree-guard.sh` is NOT fixed by E-279-01 — see the ⚠️ correction below**; **`context-ratchet.sh` remains the substantive member**. Its coupled half — the E-256 resume guard against stray `.dispatch-log/` files being swept into a commit — **goes moot once nothing creates that directory**. So at closure, re-scope IDEA-116 to `context-ratchet.sh` **plus a reduced-severity `worktree-guard.sh` entry**, and retire the resume-guard half.

   - ⚠️ **CORRECTED 2026-08-01 on a `cr-e279` finding, verified first-hand by PM against the post-story-01 file. "Fixed" was wrong, and the correction is slightly STRONGER than the finding as routed.** CR's form was *"demoted to the fallback path, not fixed."* That is right about the literal construct — `ls -d "$WT_ROOT"/baseball-crawl-E-* … | head -1` survives at `worktree-guard.sh:171`, now reachable only when the registry is unreadable. **But the arbitrary-first-match property also survives in the PRIMARY path**: the registry branch's `case` match sets `WORKTREE_DIR` and `break`s on the first hit, so with several epic worktrees registered the guard still names an arbitrary one. Demotion is therefore not the whole story — the property IDEA-116 names is present on both branches, not merely retained on the disused one.
   - **Severity, both sides, because the harmless half is what makes this easy to over- or under-state.** In THIS hook the mis-attribution is confined to the denial reason string: mode 1 denies every main-checkout write regardless of which worktree is named, so no decision changes. It is a diagnostic-quality defect here, not a correctness one. That is why the closure disposition is *reduced severity*, not *retired* — and equally why it must not be written up as though E-279 closed it.
   - **PM's Pattern-A sweep on this correction: NO other live artifact carries the "fixed" claim.** Checked `epics/`, `.claude/`, `.project/ideas/`. Every other IDEA-116 mention is dated historical record — `archived-epics.md:79`/`:81`, `epic-codifications.md:75`, and the E-256/E-260 archive files — all EVIDENCE of what was true when written, none to be edited. The single false site was this note.
   - **Three things the finding as routed did not carry, all bearing on the closure action:**
     1. **IDEA-116 already anticipated the severity point, in its own words.** Its Open Question at `:23` asks *"Should `worktree-guard.sh` change at all, or only the two counting/logging hooks?"* and answers with the same reasoning CR reached independently — the guard denies regardless of which worktree is named, "so its `head -1` is only used for the deny message — lower stakes." **So the closure action is not "add a caveat to a settled member" but "resolve an open question the idea has carried since 2026-07-12,"** now with post-E-279 evidence to resolve it on.
     2. **The re-scope must reach the README index row, not only the idea file** (`.project/ideas/README.md:137`, which states the original three-hook scope including `send-message-counter.sh` — a hook this epic deletes). This is Pattern B: an index row is written by SUMMARISING its source, so it regenerates a retired claim rather than inheriting it, and an instruction inside the source cannot reach it. This epic already paid for that once with IDEA-231's row.
     3. **Story 02 AC-1 lists `IDEA-116-*` under "evidence, leave alone" — that does NOT bar this re-scope, and the two must not be read as conflicting.** AC-1's classification is bounded to story 02's dispatch-time telemetry sweep, where IDEA-116's mention of `send-message-counter.sh` is evidence an implementer must not edit. The re-scope is PM's, at closure, after that sweep. Different actor, different time, different purpose. Recorded so nobody later reads the AC as a prohibition on the closure obligation.
   - **Method note: this is the shape TN-12 exists for.** "Fixed by E-279-01" was a planning-time prediction about a story that had not yet been written, and it read as verified because the surrounding enumeration was. It survived to dispatch because **every later reader checked whether the conclusion fitted the epic, not whether the claim was still producible against the shipped file.** Caught by CR opening the post-change file; not by re-reading the note. **No identifier grep would have found this**: the idea's scope shrank because a *judgement* rested on the retired mechanism, sharing none of its tokens (`.claude/rules/doc-sweep.md`).
5. **Ratchet (trigger 7)**: the restructure is net prose *removal* in `.claude/skills/`, offset by additions in `.claude/rules/` (the TN-4 ceiling) and `.claude/agents/` (none expected). `.githooks/` and `scripts/` are outside the four counted subtrees and are ratchet-free. Report the measured `context-ratchet.sh` figure; do not hand-derive it.

### TN-12: Premise confidence — check these, do not spot-check uniformly
Recorded because this epic has already produced three premises that were false or unverified *underneath conclusions that were correct*, which is the shape review does not catch. A reviewer's attention is better spent here than spread evenly.

**Re-opened first-hand during planning** (treat as verified): the TN-6 enumeration and its method; both hook code paths; the `settings.json` registrations; `git check-ignore -v` on the TSV resolving to `.git/info/exclude:19`; `core.hooksPath`; the `SKILL.md` cross-reference line list; E-271-03's AC text; the `src/reports/llm_analysis.py:44` citation AND that its repoint target exists at `.project/archive/E-243-probable-starter-usefulness/E-243-04-narration-prompt.md` (verified independently by claude-architect and PM).

**⚰ ITEM 1 IS DISCHARGED — do NOT spend a reviewer's attention on it.** It formerly read: *"IDEA-230's controlled repro — that assigning a worktree path to a shell variable was alone sufficient to create the directory… neither re-ran the experiment."* **It has since been re-derived TWICE and independently** — by `cr-e279` in iteration-1 review and by PM in iteration-2 triage, both from `send-message-counter.sh:112`. The mechanism HOLDS, with the correction that it requires a CONJUNCTION (worktree-path substring AND `git add` regex) before `:121` runs `mkdir -p`; see the Background & Context correction. Leaving it on a "check this" list spends attention on a discharged premise, which is the exact cost this TN exists to prevent.

**NOT re-opened — the reviewer should check these rather than the above:**
1. **Hook caching behavior** — see TN-1 item 2's bound.
2. **The evidence-versus-criterion proportion in TN-6** — never measured; the scope decision is deliberately independent of it.

**One method failure worth carrying, because it produced a silent non-check.** claude-architect's first attempt to confirm the E-243 repoint target ran `ls -d epics/E-243* .project/archive/E-243*` in zsh. The first glob matched nothing, so **zsh aborted the entire command before the archive path was ever tested.** The output read "no matches found" — true of the dead pointer, and silent about whether a repoint target existed. For several minutes "one-line fix" rested on a check that never ran. **A shell that aborts on the first failed glob turns a two-part verification into a one-part one with no visible sign**; test each path in its own command, or the negative result of the first conceals the second.

### TN-13: Deletion scope is FOUR places, not one
claude-architect's enumeration, re-verified first-hand by PM on 2026-07-28. Removing the hook file alone leaves the mechanism half-live and its claims standing.

1. **`.claude/hooks/send-message-counter.sh`** — the file itself.
2. **`.claude/settings.json`, BOTH registrations** — the hook is registered on two PreToolUse events, at lines 30 (Bash) and 41 (SendMessage). Removing one and not the other leaves the mechanism half-live. **The two removals are not symmetric, and the asymmetry is deeper on the Bash side than the first draft of this note said.** The **Bash** matcher block (opens `:8`) holds **four** hook objects — `pii-check.sh` (:12), `epic-archive-check.sh` (:18), `secret-read-guard.sh` (:24), `send-message-counter.sh` (:30) — so **only the fourth object, lines 28-33, is removed** and the block plus three siblings survive. The **SendMessage** block (`:36-46`) is sole-occupant and goes entirely. Deleting the Bash block wholesale would unregister a PII gate, a credential guard and the unarchived-epic gate with **no failing test**; worse, `epic-archive-check.sh` is the hook E-279-03's design cites as still clearing under the restructured sequence, so removing it would falsify this epic's own safety premise. Story 02 AC-2 therefore enumerates the four objects rather than naming a block.
   - *Provenance, recorded because it is this repo's recurring shape:* claude-architect caught this; PM had written "shares its matcher with `secret-read-guard.sh`" from a six-line grep window, which named one of three co-tenants and read as though it named all of them. A partial read that happens to be true is not a verified enumeration.
3. **`.gitignore:55-58`** — the whole stanza. **PM correction to the cited range, verified by reading the file:** claude-architect and the main-session relay both gave 56-58. Line 55 is `# Dispatch send-counter (transient; see .claude/hooks/send-message-counter.sh).`, which names the hook by path; line 57 carries the "stays TRACKED so it rides the closure patch" assertion; line 58 is the `sends.count` ignore rule. Deleting 56-58 would leave line 55 standing as a comment pointing at a deleted file — the exact surviving-claim defect this deletion exists to prevent.
4. **`.claude/agent-memory/software-engineer/dispatch-git-gotchas.md`** — story E-279-05, owned by software-engineer.

`.git/info/exclude:19` is deliberately NOT a fifth place; see TN-11 item 3.

### TN-14: The pre-commit rename fail-open — found here, RESOLVED ELSEWHERE, deliberately not carried
Recorded as a DECISION in the shape of TN-11 item 3, at claude-architect's request, because **a hook edit landing a few lines from a known fail-open reads as a miss unless the record says otherwise.**

**What was found.** `.githooks/pre-commit:22-24` builds `STAGED_ARR` with `--diff-filter=ACM`, which excludes `R`. Executed, not inferred: a `git mv` **plus a content edit in the same staging** classifies as `R` (measured at 57% and 73% similarity in two independent probes) and yields **zero** `ACM` entries, so `:26-28` exits 0 and **neither the PII scanner nor the doc-PII byte-gate runs**. A synthetic `GC_REFRESH_TOKEN=` value committed clean; enumerating the same path with `ACMR` produced `[PII BLOCKED]`. **The scanner would have caught it — the enumeration filter is what let it through.**

**Severity, both sides.** This is an accident-catching control, not an adversary boundary, and `--no-verify` always exists. What makes it real is that the accident is an **ordinary operation** — move a file and edit it in the same commit — and `git add -A` after a `git mv` is what the closure sequence itself does.

**Disposition: RESOLVED ELSEWHERE — landed, not pending.** Operator ruling 2026-07-28 (verbatim selection "Standalone fix now"), shipped as **main commit `9b62395`**, *"fix: PII pre-commit gate enumerates renamed files (ACM -> ACMR)"* (2026-07-28, authored by claude-architect): one filter token plus the falsified comment corrected, with rename+edit-blocks and content-neutral-rename-passes regression tests, RED confirmed against the unfixed hook before the fix, 164 tests passing across the three hook-referencing files. Reasoning for keeping it standalone: a security-control change must not reach the operator inside a closure-machinery diff, where it would be approved incidentally.

**Consequences for this epic, all of which hold:**
- **E-279 does NOT touch the filter.** Story 04 AC-9's pin stays intact; no carve-out is needed, because story 04 AC-5's placement inserts the new gate above everything rather than modifying `:22-24`.
- Story 04 AC-9 tells the implementer that **`:24` reads `ACMR` and that is correct — do not revert it to `ACM`.** The fix is already in main as of `9b62395`, so this is the baseline a dispatched implementer will find, not a contingency.
- **No idea filed for this** — a landed fix needs no parking ticket; the commit is the record.

**A SECOND, narrower fail-open in the same file is NOT resolved by that fix and is filed separately.** `:8-11` exits 0 when `src/safety/pii_scanner.py` is absent, skipping the **doc-PII byte-gate** as well — and that gate runs `scripts/check_doc_pii.sh`, a standalone harness with **no dependency on the scanner it is being skipped for**. E-279 closes this for *its own* gate via AC-5's first-placement bound; the residual is exactly that one gate. It is filed as its own idea rather than bundled with the above, deliberately: it requires a repo already in a broken state (a committed file deleted), whereas the rename bypass is reachable in ordinary operation, and bundling a weaker item with a stronger one invites a single disposition for both.

## Open Questions
**Both RESOLVED 2026-07-28. Retained with their reasoning because a successor who reads only the outcome will re-litigate them.**

- **⚰ OQ-1 — RESOLVED: DELETE.** Operator ruling, 2026-07-28, taken at an interactive gate; the option selected was verbatim *"Delete entirely."* **Story E-279-02 now carries the delete criteria as its plain AC-1 through AC-4; the former KEEP branch is retired in place there as considered-and-declined and must not be implemented.**
  - *Question as put:* delete the dispatch-log telemetry mechanism, or keep and repair it? Routed to the operator rather than decided by PM or claude-architect because E-260 built it on an explicit operator decision ("per operator decision 3 — worktree-local, no committed cross-epic aggregate"), and `c990446` shows the operator makes retirement calls on this hook.
  - *Grounds, preserved:* never readable across nineteen epics; `rounds` has no producer by construction; `sends` blanks under parallel dispatch — a repaired file would carry one usable column. And "keep" was never repo-fixable: `git check-ignore -v` resolves the TSV to `.git/info/exclude:19`, which is per-clone, uncommitted and has no history.
  - ⚠️ **The deletion cannot be complete, and the epic must not imply otherwise.** That `.git/info/exclude:19` line survives in every existing clone and no commit removes it — see TN-11 item 3 and story 02 AC-4. Report it at closure as a manual per-clone operator item.
- **⚰ OQ-2 — RESOLVED: AUTHORIZED.** Main-session ruling, 2026-07-28: the standing "do not modify E-271" instruction was the main session's own to reverse, and it was reversed on the prohibited-AC argument below. **Risk accepted by the main session and disclosed to the operator.** **The edit was EXECUTED at planning time on 2026-07-28 — it is not a story and not pending; see TN-8(c) for its exact bounds.** It was briefly scoped as an E-279-02 AC-5 and that placement was withdrawn: E-271 is READY and dispatchable today while E-279 is DRAFT, so carrying the fix inside E-279's dispatch would defer the exposure rather than discharge it, and the criterion would have had no reachable RED once the work was done.
  - **A SECOND authorization was requested and DECLINED (main session, 2026-07-28) — considered, not forgotten, and RE-CONFIRMED on 2026-07-28 during iteration-2 triage.** PM asked whether the restatements in E-271's *epic* file should also be reconciled. Declined: the executed bound was correct, the residuals are findable, E-271 already carries a dispatch-time re-scope flag for its P-6 premise, and further churn inside a READY epic nobody is currently planning buys nothing today. **A future E-271 planner should treat them as retired with AC-1** rather than reading them as live requirements.
    - ⚠️ **This bullet originally enumerated FIVE restatements. The count was wrong and the enumeration is no longer kept here — TN-8c's table of EIGHT is authoritative.** Two counts of one set lived eleven lines apart in this file (four in TN-8c, five here) against E-271's own six, and none of the three included the two sites `cr-e279` later found by independent enumeration. A single source is kept deliberately: a set enumerated in two places is a set that will disagree with itself again.
    - When the declination was re-confirmed, the reachability was CHECKED rather than assumed: E-271's History:135 cites "E-279 epic TN-8(c)" **by story ID, not by path**, so the pointer survives E-279's archiving and the bounded fix genuinely reaches a future E-271 reader.
  - *Grounds, preserved:* E-271-03's AC-1 is not merely stale. Its deny half is discharged by hand, and its warn half is **affirmatively prohibited** — executing it literally would reintroduce `WARN_AT=15` and an advisory warn against the standing order at `send-message-counter.sh:21` ("Do not reintroduce a threshold here without an operator order"). A flag in E-279 does not reach E-271's implementer, who reads E-271. The alternative was dispatching a story whose first criterion cannot be satisfied and whose remainder is forbidden.

## History

### 2026-08-01 — CLOSURE. All five stories DONE. Epic COMPLETED.

**What shipped.** The write guard reads authoritative worktree state (`git worktree list`) instead of inferring dispatch mode from directory existence; the dispatch-log telemetry mechanism is deleted entire — hook file, both `settings.json` registrations, the `.gitignore` stanza; the archive rename moves into the epic worktree so the closure sequence is an ordered (i)-(v) with no main-checkout rename and no rename-ordering constraint on rollback; `scripts/check_archive_refs.sh` plus its two call sites gate surviving pre-archive references; and software-engineer's memory no longer prescribes a remedy for a mechanism that does not exist.

**Review scorecard (dispatch).** Five stories, per-story code review on all five, PM AC-verification on all five. **Closure Integration Review: APPROVED, no MUST FIX.** Codex rounds during dispatch surfaced the `dispatch-pattern.md:42` closure-sequence contradiction (P1) and the stale `E-279-01-completion-report.md` test-count evidence (P5), both accepted and fixed.

#### Documentation assessment — ⚠️ **TRIGGER 5 FIRES. Dispatch decision OWED before archival.**

`.claude/rules/documentation.md` trigger 5 — *"an epic completes that changes how the system works or how users interact with it"* — **fires, narrowly but really.** The archive-reference gate is wired into `.githooks/pre-commit`, so **the operator can now meet a commit refused with `[archive-refs: BLOCKED]`**, and this epic's own AC-6 no-override ruling documents `--no-verify` as an operator decision rather than an agent's tool. **A gate that can refuse the operator's commit, whose only override also disables the PII scan, is operator-facing whatever its intended audience.**

- **Affected doc:** `docs/admin/operations.md` — what the block means, when it fires (epic closure, surviving `epics/E-NNN-` references outside the archive tree), and that the override is theirs and carries a PII cost.
- **Triggers 1-4 do NOT fire:** no new feature or endpoint, no deployment-configuration change, no agent created or materially modified, no schema change or migration.
- **This is recorded as FIRING, not as satisfied.** Per the rule the docs-writer dispatch happens *before* archiving; that dispatch has not occurred and is a live decision for the main session.

#### Context-layer assessment — all eight triggers, explicit verdicts

1. **New convention, pattern, or constraint** — **YES.** The closure sequence is now an ordered (i)-(v) with the rename inside the worktree; "there is no sub-step 6" is a standing constraint.
2. **Architectural decision with ongoing implications** — **YES.** Dispatch state is read from `git worktree list`, not inferred from directory existence. Fails closed on a crashed dispatch, which is strictly stronger.
3. **Footgun, failure mode, or boundary discovered** — **YES.** A passive hook manufacturing directories from command *text*; and the standing cost that a closing epic's own pre-archive path may not appear in any surviving artifact outside the archive tree.
4. **Change to agent behavior, routing, or coordination** — **YES.** The closure sequence, the write guard's mode determination, and the deletion of the send-cap mechanism all change how agents are dispatched and coordinated.
5. **Domain knowledge influencing future agent decisions** — **NO.** No baseball, API, or data-model knowledge surfaced. This epic is entirely context-layer machinery.
6. **New CLI command, workflow, or operational procedure** — **YES.** `scripts/check_archive_refs.sh` is a new script with two call sites. No `bb` subcommand and no skill added, renamed, or retired, so the `/workflow-help` cheat sheet and the CLAUDE.md Workflows section are unaffected.
7. **Net context-layer growth ratchet** — ⚠️ **FAIL, and UNRESOLVED. Neither offset nor exception has been recorded, and this verdict must not be read as either.** Reading 1 (2026-08-01, still tree): **+350 over the `18220` baseline** — `.claude/agent-memory` +306, `.claude/skills` +35, `.claude/rules` +9, `.claude/agents` +0. Reading 2 (post-authoring, pre-staging, scoped as the ratchet scopes itself, taken on an explicitly frozen tree) is owed and is the authoritative one.
   - ✅ **OWNERSHIP SETTLED 2026-08-01, and it closed AGAINST us: E-279 owns all +350** — **+286 at planning** (`25a9eea` +113, `fa3e1bf` +173) and **+64 at dispatch.** Every counted line in both planning commits is `.claude/agent-memory/`; zero in rules, agents, or skills.
   - ❌ **The E-261/E-262 re-snapshot precedent does NOT apply, and must not be cited here.** That ruling (`epic-codifications.md:94`) turned on the drift being *"prior epics' closure memory the operator has not re-snapshotted"* — **another epic's lines, identical on `main` and the worktree.** E-279 would be paying for its **own**. Same number shape, opposite ownership.
   - **How it was settled, because the method is the transferable part:** `cr-e279` read the **as-committed blobs** at `25a9eea` rather than reasoning from filenames. The two code-reviewer files carry **zero** `E-276`/`E-277` tokens and zero reconcile-at-load material; one cites **E-279-04's AC**, the other records an error CR made auditing **this** epic. **PM's hypothesis — that they were a prior epic's lessons landing in an E-279 commit by TIMING rather than attribution — is FALSIFIED, not merely unsupported.**
   - ⚠️ **Disambiguation for whoever greps next:** `regenerate_the_population_not_the_pair.md` carries a single `E-271` token — **the audit TARGET, not the recording epic.** A token count misreads it as E-271 material; only the enclosing line resolves it. **Join-not-grep.**
   - **The operator owns the disposition; net growth is not accepted on an agent's say-so.** ⭐ **And the question worth putting to them is the INSTRUMENT one, which is ownership-independent and survives this correction untouched:** `context-ratchet.sh` is **not registered** in `settings.json`, accrues continuously, and is charged per-epic to whichever epic closes next — **+286 accrued in 22h33m, so an epic closing more than about a day after a snapshot fails by construction, whatever its own discipline.**
8. **Reusable behavioral lesson (promote-to-load-target, gated)** — **YES, with promotions SCORED rather than assumed.** Several lessons recurred or generalize (see the codification entries below). Every promotion is gated on citing the specific defect it caught and fitting the ratchet baseline; **with trigger 7 failing, condition (b) is not currently satisfiable for any growth-side promotion**, so promotions are recorded as candidates pending the operator's trigger-7 disposition. Deletion-side eviction and memory-retirement hygiene fired unconditionally and are recorded with the story-05 sweep.

#### Closure obligations carried OUT of this epic

- **The off-team memory sweep.** Five agent dirs — baseball-coach, api-scout, data-engineer, docs-writer, ux-designer — have had **no identification run against them** for surviving send-cap claims. Owned by the main session per `context-layer-assessment.md:55`. **Not an E-279 blocker** (nobody on this team may edit those dirs) and **not a clean** — an unswept dir is an unknown.
- **`.git/info/exclude:19` still contains `.dispatch-log/` in every existing clone.** No commit can remove it; a manual per-clone operator item (TN-11 item 3).
- **`docs/` and `.project/` are UNSWEPT** for the retired mechanism, and the sweep that ran was identifier-based, so it cannot reach a judgement resting on the mechanism while carrying none of its tokens.

- 2026-08-01: **⭐ THE DISPOSAL PASS OVER THE ENTRIES BELOW — RUN, AND ITS OUTCOME IS A SCORED LIST, NOT A PRUNED ONE. Read this before "compressing" the codification record.**
  - **The test, adopted mid-dispatch from the main session's formulation:** ***a rule is load-bearing when you can name a case where FOLLOWING it and VIOLATING it produce DIFFERENT OUTCOMES, not merely different postures.*** A rule with no such case is a candidate for **eviction**, not for reinforcement. It is a **disposal** criterion, and this repo has many adoption criteria and almost no disposal ones.
  - ⚠️ **RULING: NOTHING BELOW IS DELETED, and the reason is not squeamishness.** Two independent grounds, either sufficient:
    1. **These entries cost the ratchet ZERO.** The measure covers `.claude/rules`, `.claude/agents`, `.claude/skills`, `.claude/agent-memory`. **`epics/` is in none of them.** Deleting entries here buys **no budget at any price** — and reporting a budget improvement from pruning this file would be a **fabricated offset**, which is the standard this dispatch applied to everyone else.
    2. **They are EVIDENCE, not criteria.** This is the archived record of what happened during the dispatch. **A reader is meant to SEE what was observed, not to MEET a standard.** The criterion-versus-evidence cut says correct the first and preserve the second — and a "consistency sweep that fixes every stale-looking figure" destroys records. **The same cut protected three figures today; it protects this file too.**
  - **So the pass produces a SCORE, and the score governs PROMOTION — which is where the lines are actually priced.** The two questions run in order, and the second is downstream of the first: **(1) must an agent recall this unprompted? (2) only then — can it live in the epic file instead?** The epic file is free **because it is inert**; memory is priced **because it is loaded**. **The ratchet prices REACH, not storage, and the failure mode of the free tier is silence, not cost.**
  - ⚠️ **TRIGGER 7 CURRENTLY BLOCKS EVERY GROWTH-SIDE PROMOTION, which I did not notice until writing triggers 7 and 8 next to each other.** Trigger 8 gates promotion on **(a)** citing the specific defect it caught and **(b)** fitting within the ratchet baseline. **With the ratchet failing, (b) is unsatisfiable for any promotion that adds lines.** So every entry below is at most a **promotion candidate**, and the operator's trigger-7 disposition is upstream of all of them. **No promotion executed at this closure.**
  - **PM-domain entries scored (the ones PM owns; each other entry is scored by its author, who owns its domain):**
    - ✅ **PASSES — the tombstone / verify-by-absence property** (*verify zero UNMARKED occurrences*): following yields *"correctly retired"*; violating yields a bare zero read as success **when the record has actually been deleted**. Different outcomes, and one branch destroys a record.
    - ✅ **PASSES — a review instrument reading unstaged state assumes a single author**: following makes the reviewer ask whose changes it is reading; violating attributes another agent's work to the story under review. Different outcomes; it happened.
    - ✅ **PASSES — an AC criterion answers only the question it was written for**: AC-2 was verified with an instrument scoped to one file when its text carried no file scope, and Codex found the site the narrow instrument could not reach. Named consequence, not a posture.
    - ✅ **PASSES — the LABEL-TO-QUANTITY class** (`M` is not a magnitude; a commit subject is not a file list; a filename is not content; a concept is not a path): **four instances in one hour, two of them PM's own**, each producing a wrong number or a wrong path that a colleague acted on. **The strongest-scoring entry of the set on this test.**
    - ⚠️ **SCORED WEAKER THAN IT READS — *"the defect fires at the moment of confident summary."*** The three instances are real, but they are drawn from one session in which we were **hunting this exact class**, and **we never counted the rules we articulated and then applied correctly.** No denominator. **The concentration claim is not established; the TRIGGER form is** — *having just articulated a rule, the next artifact you produce is a candidate for checking against it.* **Ships in the weaker form only.** This entry is the disposal test's first real catch, and it caught something all three participants found compelling.
  - **What the pass did NOT do, stated so nobody claims it did: it did not shorten this file, it did not improve the ratchet, and it did not rule on entries owned by `ca-e279` or `cr-e279`.**
- 2026-08-01: **CODIFICATION — THE TOMBSTONE / VERIFY-BY-ABSENCE CONFLICT. One merged item, one condition, one item left OPEN. Deliberately not three candidates.**
  - **THE CONFLICT, which is sharper than "the rule is silent for authors": the two rules actively contradict.** Verify-by-absence says *grep the false form, require zero*; the tombstone convention says *preserve the false form under a `⚰ RETIRED:` marker.* **Both failure directions follow from obeying both.** An author who tombstones correctly gets a non-zero check and reads it as failure. **An author who gets a clean zero may have DELETED rather than retired — destroying the record the tombstone exists to keep, and that direction is the expensive one because it looks like success.**
  - **MERGED ITEM — `cr-e279`'s actor gap is the RATIONALE, PM's property is the REMEDY, and they ship together.** CR's ruling on why: ***"a check shipped without the failure that motivates it is the thing this repo drops as ceremony two epics later; a gap shipped without a check is a diagnosis nobody can act on."*** The property: **verify ZERO UNMARKED occurrences, not zero occurrences** — the false form may appear only under a retirement marker. One pass, and it separates two states a bare zero cannot: a clean zero becomes *"deleted — was that intended?"*, a marked hit becomes *"correctly retired."*
  - ⚠️ **THE CAUTION SHIPS WITH THE CHECK, because without it the check becomes the class it was invented to close: the property detects a DELETED RECORD; it CANNOT detect that you SEARCHED FOR THE WRONG WORDS. Choosing the retired string is the unmechanized step.**
  - **Ranked first on the LUCK argument, not the check-versus-diagnosis one** (`ca-e279`'s correction, and it is the stronger ground): **this class has never once been caught by review, and the single non-self catch was accidental.** PM derived the property for its own purposes; nobody was reviewing its verification when it surfaced. **Detection record for the class: 3 self-caught, 1 by accident, 0 by review.**
  - ✅ **INDEPENDENT VALIDATION — the property caught a case its author did not have in mind, in a different file, by a different author, found by a third party** (`cr-e279`, 2026-08-01). The `epic-codifications.md` `:174`/`:175` finding — a *"no tombstone reaches this"* verdict that had enumerated the wrong tombstone — **is the missing-member class AND the property's SECOND failure direction simultaneously.** The property was derived from `ca-e279`'s wrapped-tombstone case and independently catches an enumeration failure that shares none of its surface features.
    - **Why this is worth more than any of our confidence in it: a check validated only against the case that produced it is untested.** This is the same lesson the falsified contiguity condition below teaches from the failing side — **one derived from a single example and validated on that example, one derived from a single example and validated on a second.** They land opposite ways, and that is the whole argument for testing a rule against a case it was not built for.
    - **This entry PASSES the disposal test** — following it and violating it diverge in outcome: the marked hit is read as *"correctly retired"*, the bare zero as *"deleted — was that intended?"*, and one of those states destroys a record.
  - **CONDITION on remedy (i), not a candidate — CONTIGUITY: `ca-e279`'s OBSERVATION, `cr-e279`'s SHARPENING, in that order.**
    - **`ca-e279` observed the hazard AND connected it to remedy (i) — both load-bearing moves, and it did so before CR had looked at anything:** *"a tombstone wrapped mid-claim is a tombstone a single-line grep cannot find, which would have defeated remedy (i) while appearing to satisfy it."*
    - **CR's addition is narrower and genuinely different: the FALSE-CLEAN mechanism** — a **tail-fragment** match landing on a **marker-free continuation line**, so the marker is out of view of the very line that matched. **`ca-e279`'s failure is *"hard to find"*; CR's is *"findable and reads LIVE."* The second defeats remedy (i); the first only weakens it.**
    - ⚠️ **The attribution was corrected by the party it FAVOURED. `cr-e279` argued its own credit DOWN, unprompted**, on the ground that recording it as CR's *"would be the citation defect pointed at me instead of at it."* An earlier form of this entry said *"CR's, seeded by `ca-e279`"* — that was the main session's framing and it was wrong.
    - ⚠️⚠️ **THE CONDITION AS FIRST WORDED WAS WRONG, AND `cr-e279` FALSIFIED ITS OWN AMENDMENT. This is the shipped wording; do not restore the earlier one.**
      - **How it failed: applied to a second case it flags CORRECT work as a defect.** `dispatch-git-gotchas.md:179` violates the marker-contiguity condition as originally worded — the `⚰ RETIRED` marker opens at `:171` and the values sit **eight lines below**, so a sweeper grepping `DENY_AT=60` lands on a line carrying no marker. **The hazard does not materialize**, because the line reads *"(the pre-`c990446` file literally read `WARN_AT=40` and `DENY_AT=60`); they died with the mechanism, not because they were wrong"* — **self-evidently historical on its own text.**
      - **SHIPPED WORDING (`cr-e279`, superseding):** ***Every line a grep could match must be self-evidently historical on its own text. A same-line marker guarantees that and is the cheapest way to get it; it is not the only way. A quotation whose own words carry the past tense and a dated referent satisfies the requirement without the marker.***
      - **The discriminator, which is what the first wording was missing:** the original case — `# never permissiveness.` — is a **bare fragment with no tense, no date, and no referent.** *"It reads live because there is nothing in it that does not."* **Marker-contiguity was never the requirement; it was the cheapest sufficient means, mistaken for the requirement itself.**
      - **`cr-e279`'s own diagnosis:** *"my first wording generalised from one example to a rule that flags correct work."* **Shipping the original would have had the first post-closure sweep report a FALSE DEFECT against SE's tombstone** — a rule manufacturing the very finding class it exists to prevent.
      - ⚠️ **This is the DISPOSAL TEST arriving early, against an entry every one of us was confident in.** It was caught by **applying the condition to a second case**, not by re-reading the first — consistent with this repo's record that a rule derived from one example is validated only by execution against another. **A condition that has been tested on exactly one case is an untested condition, however obviously right it looks.**
  - ⚠️ **OPEN AND UNDISPOSED — `ca-e279`'s PROPAGATION conditional.** Does the answer depend on whether the claim propagated (zero external occurrences measured — three forms, whitespace-normalized, case-insensitive, positive control in the same traversal)? A never-relayed claim may warrant describe-don't-quote; a relayed one verbatim-plus-marker.
    - **`cr-e279` confirms it has NEVER RULED on this and declines to rule from a paraphrase:** *"it has not been briefed to me in its own words at any point. I will not rule from your one-line paraphrase, because that is the exact failure we have spent the day cataloguing."*
    - **CR's PRELIMINARY CONCERN, attached as a QUESTION and not an objection:** the conditional keys on whether the claim **propagated**, while `doc-sweep.md`'s rationale keys on **future reintroduction** — *"a silently corrected claim is one a future reader reintroduces."* **Those are different risks: a never-relayed claim can still be reintroduced by a future reader who meets the corrected text and reasons back to the original.** If that holds, propagation history does not govern the risk the rule manages. **CR has not seen `ca-e279`'s argument and it may address exactly this.**
    - **Recorded OPEN rather than dropped, deliberately: a quiet drop inside a merge would credit CR with `ca-e279`'s idea while losing `ca-e279`'s actual proposal.**
  - **Orthogonality, so a later reader does not treat these as competing:** the propagation conditional answers *what the author should DO*; the zero-unmarked property answers *what the author should VERIFY*, and is correct under **both** its branches — under describe-don't-quote it reduces to plain absence; under verbatim-plus-marker it is exactly what separates retired from deleted.
  - **The generalization to lead with: this is the unlabelled-null structure's THIRD instance and the FIRST in a VERIFICATION rather than a gate** (`epic-archive-check.sh` and the documentation near-miss were gates). **A verification whose clean result is indistinguishable from a destructive one is the same defect wearing different clothes — which means the class was never about gates.** A verification is where it hides best, because a verification's whole job is to produce a clean result.
- 2026-08-01: **CODIFICATION — *"CHECK THE ARTIFACT, NOT YOUR RECOLLECTION OF IT"* IS THE GENERAL FORM. RELAY IS THE VISIBLE CARRIER, NOT THE MECHANISM.** `ca-e279` via `cr-e279`; reframes a `paths: "**"` rule rather than a file.
  - **Every logged instance of this epic's relay defects had a sender, so a sender LOOKED necessary. It is not.** `ca-e279` cited `doc-sweep.md` **by name, in its own message, and never opened it** — no relay, no sender, same defect. **The relay was always the conspicuous carrier of a failure that only ever required a recollection standing in for a read.**
  - **Why it matters for the codification: a rule scoped to relays exempts the largest category — your own memory of a document you have read before.** The E-279 record is full of both, and only the relayed half was legible as a class.
- 2026-08-01: **THE GUARD FIX'S VALUE, BOTH HALVES — the strongest argument for story 01, and it came from neither its ACs, its review, nor its Codex pass.**
  - **The epic bought its own closure a guarantee it would otherwise have had to arrange by agreement.** Four closure state-claims — clean-tree preflight, staged-diff presentation, full-suite gate, archive-rename verification — are **main-checkout** claims, and while the epic worktree exists `worktree-guard.sh` mode 1 blocks ALL agent Write/Edit there with no allowlist. **Those are enforced, not assumed and not promised.**
  - **And the second half is what makes it more than a compliment: the deadlock costs E-279 a repair window; it does NOT cost it the guard.** No benefit from its own restructure, full benefit from its own hardening — **separable only from inside this closure**, which is why it had to be written down here.
  - **The transferable rule: prefer STRUCTURAL over AGREED wherever sequencing or an existing enforcement already provides it.** An agreement is a leg walked carefully; sequencing and enforcement remove the leg. **Reach for an agreement only where neither is available** — and treat the ASSUMED column as empty, since anything in it is a belief about another agent's behaviour wearing a guard's clothes.
- 2026-08-01: **CODIFICATION CANDIDATE — ANY REVIEW INSTRUMENT READING UNSTAGED STATE ASSUMES A SINGLE AUTHOR, AND THAT ASSUMPTION IS INVISIBLE IN ITS OUTPUT.** PM's formulation, widening the main session's; recorded here rather than carried, since an item living only in an agent's context dies with it.
  - **The mechanism: a bare `git diff` over two concurrent authors does not merely show MORE — it shows an UNATTRIBUTABLE BLEND with no signal that attribution was ever available.** Whose hunks are whose is not recoverable after the fact.
  - **Two instances, one afternoon, both correctly avoided:** the main session staged story 04 by **explicit path** rather than `git add -A` while `ca-e279` had remediation in flight; and it held story 05's assignment so `cr-e279`'s bare-`git diff` review would see one author's work.
  - **What it adds to the staging-boundary protocol, which is the actionable half: the protocol says WHAT to do (`git add -A` after each story passes review) and not WHY the boundary exists — it is what makes every review instrument single-author BY CONSTRUCTION.** Without that, the boundary reads as bookkeeping and gets relaxed under time pressure by someone who does not know what it is buying.
  - ⚠️ **A DISTINCT failure shape surfaced by the same exchange, and it is harder to catch than the one this epic has been cataloguing: THE RIGHT ACTION TAKEN FOR A REASON THAT WOULD NOT TRANSFER.** The main session's stated reason the second time was *"I promised CR a clean diff"* — **TRUE, and local.** Not a false premise under a correct conclusion; **a TRUE premise that generalizes to nothing.** *"I promised"* protects nothing the next time nobody promised anything. **It is harder to catch precisely because there is nothing wrong with it** — a false-premise check passes, and only asking *"would this reason still hold with the particulars removed?"* exposes it.
    - ⚠️ **FIFTH INSTANCE, AND THE PUREST — a condition not held consistently across TWO OF THE MAIN SESSION'S OWN MESSAGES.** No elapsed time, no external change, no other agent: it restated its own ordering four minutes later and the restatement had **inverted a step**, putting PM's write window before the staging that was supposed to precede it. **PM caught it from outside; the main session had no signal.**
      - **The remedy it already had does NOT reach this: *every disposition produces an observable* cannot catch it, because BOTH versions were dispositions WITH observables — they simply contradicted each other**, and nothing in that rule requires a disposition to agree with the one issued four minutes earlier. Each observable is correct about its own disposition and silent about the relation between them. **Same "correct about what it measures, silent about what matters" family, this time between two of one author's own statements.**
      - **The mechanism is one level below "keep a consistency check": THE INVERSION ENTERED IN THE PARAPHRASE.** *"Then staging, then story 05"* compressed a four-step sequence into three and lost which side of staging the window sat on. **A paraphrase is where a step vanishes with nothing looking wrong, because every remaining step is still correct and still in order.**
      - **REMEDY: when you RESTATE an ordering, QUOTE the prior version rather than paraphrasing it** — quoting your own text makes you read it as a READER rather than as its author, which this repo already records as the one procedural catch that transfers. ⚠️ **And the asymmetry is the part that says WHICH things to quote: the main session had this remedy available and applied it twice that day to other people's prose, never to its own instructions — the one class of its output where a lost step costs an agent's WORK rather than a sentence's accuracy.**
    - ⚠️ **WHO CAN CATCH IT, which is the property that makes it actionable: this class produces NO WRONG OUTCOME AT ALL, so nothing downstream ever flags it.** The false-premise class at least leaves a false sentence in the record for someone to trip over later. **A non-transferring reason leaves a correct action AND a correct-sounding justification, and defers the entire cost to the NEXT situation — where the reason is simply absent and nobody notices it was ever load-bearing.** So it is **catchable only at the moment the reason is STATED, and only by someone who is not the actor**: from inside the decision the action is right and the reason feels sufficient. The same authorship asymmetry as the rest of this epic's record, arriving through a new door.
    - **The connection to the protocol half, which is why both belong in one entry: a protocol whose purpose is unstated is one relaxation away from gone — and the person who relaxes it will have a locally-true reason too.** That is this class doing the damage, not a separate risk.
- 2026-08-01: **⭐ THE SUMMARY FINDING OF THIS DISPATCH: FOUR DISPOSITION FAILURES, ZERO DETECTION FAILURES. Detection is not this team's weak point; what happens AFTER detection is.** Main session's formulation, independently matching `cr-e279`'s finding about its own instrument.
  - **Everything that mattered was detected.** What went wrong came after: **a verdict written and not acted on** (PM's *"AC-9's citation is stale"*, recorded and never applied); **a defect filed under the wrong phase** (PM's AC-4 mis-concession); **a claim relayed without being opened** (the main session ruling on an AC's contents against its owner, twice, without reading it); **a precondition named and not checked** (*"confirm the flip and I assign"*, then proceeding past it — the story-04 status transition that never happened).
  - **Why this reframes which process changes matter.** Every remedy this epic accumulated aims at detection — widen the pattern, add a second axis, carry a control, freeze before verifying, add an independent instrument. **They worked: the detectors found everything.** **Not one of them reaches a gap between finding a thing and doing something about it.** The disposition-side remedies are different in kind and now recorded separately: *a written verdict of "stale" is a work item, not a disposition* (only **corrected**, **no change needed**, or **deferred with an owner and a trigger** close a surfaced line); *file a defect under the phase that produced it*; *a claim you relay is a claim you author*; *check the precondition you yourself named*.
  - ⚠️ **THE REMEDY, adopted mid-dispatch by the main session and the most actionable thing this epic produced: EVERY DISPOSITION PRODUCES AN OBSERVABLE, STATED IN THE SAME MESSAGE.** Not *"I have routed X"* but the routing **plus what will be read to confirm it landed**. Not *"staging is held"* but the command whose output shows it. **A disposition that produces nothing checkable is not entitled to be reported as done** — the effect-in-the-artifact rule aimed at one's own dispositions rather than at message delivery.
    - **Its own framing is the reason it works: a disposition with no output is a GATE WITH AN UNLABELLED NULL RESULT** — structurally identical to the two inert gates this epic found, one in code (`epic-archive-check.sh`) and one nearly in documentation. **A failure whose subjective signature is identical to success is not reachable by care.**
  - ⚠️ **And the disposition failures share a tell the detection failures do not: each one FELT like the disciplined move.** Recording a verdict felt like rigour. Conceding felt like rigour. Refusing a concession felt like rigour. Naming a precondition felt like rigour. **The detectors are instrumented and their output is checkable; dispositions are self-certifying, and this dispatch produced four of them that were wrong while feeling right.**
- 2026-08-01: **E-279-04 DONE. ⚠️ A PM AC-AUTHORING GAP, RECLASSIFIED FROM PM'S OWN MIS-FILING — and the reclassification is the finding, because the two phases take OPPOSITE remedies.**
  - **What PM filed:** that Codex's `worktree-guard.sh` fail-open (P1-2) was *a third instrument-scope miss* — PM having verified story 01's AC-4 by reading the predicate rather than constructing the input.
  - **Refused by the main session, and independently by `cr-e279` — the party whose finding PM's concession would have STRENGTHENED.** CR's argument: **AC-4's literal Given is *"exits zero but emits no `^worktree ` line at all,"* and the shipped code satisfies that exactly.** A verification returning the right answer for the criterion it was given is not a scope miss.
  - ⚰ **THE AUTHORING-GAP REFRAME WAS ITSELF WRONG AND IS WITHDRAWN. PM's ORIGINAL FILING — a verification-scope miss — STANDS.** The reframe claimed AC-4 was *authored too narrowly*. **`cr-e279` then quoted AC-4 from disk: the property IS in the AC.** Its Given states the weaker test (*no `^worktree ` line at all*); **its Rationale states the stronger property on the same source line — *"a successful run ALWAYS lists the main checkout, so THAT GUARANTEED LINE is a positive control."*** One criterion, two strengths. **The AC was not narrow.**
  - **The governing rule was already on this repo's books and PM had cited it against others the same day** (`tool-output-integrity.md`): ***where an artifact states one conclusion twice at different strengths, EXECUTE the stronger statement; the weaker claim would be true either way and so certifies nothing about the stronger.*** **PM tested the Given and not the Rationale. That is a verification-scope miss, exactly as first filed.**
  - ⚠️ **`cr-e279` made the IDENTICAL miss for the identical reason and filed it against itself. Two instruments, one criterion, both testing the weaker statement — and that is the evidence the failure is at VERIFICATION rather than AUTHORING.** Had the AC been the defect, both parties would have LACKED the property; instead both HAD it, on the line they were reading, and tested the weaker clause anyway.
  - **How the wrong reframe was produced, recorded because it is the mechanism and not the apology:** the main session **relayed `cr-e279`'s first report** — *"the AC was narrower than the property the comment claims"* — restated it as its own finding, built a phase taxonomy on top of it, and **ruled on an AC's contents, against that AC's owner, without opening the AC.** *A claim you relay is a claim you author.* CR's report arrived carrying execution evidence — real repro, real controls, quoted comment — **and that surrounding authority is what carried its one unverified sentence past the check.**
  - ⚠️ **AND THERE IS NO AUTHORING DEFECT — a distinction that is NOT a courtesy, because filing one would teach the wrong lesson.** The codified rule *"execute the stronger statement"* **presupposes** that artifacts state conclusions at two strengths and **puts the burden on the READER**. So a Given weaker than its own Rationale is **the ordinary case the rule was written for**, not a defect. **Filing it as an authoring defect would tell future authors to eliminate two-strength artifacts, when this repo's actual position is that readers must execute the stronger one.** A residual authoring **IMPROVEMENT** is available and worth taking — fold the property into the Given — but improvement and defect are different things and the record must not blur them.
  - ⚠️ **The extension worth keeping: REFUSING a colleague's self-indictment feels as rigorous as making one, and neither feeling is evidence.** *Conceding is not automatically the rigorous move* was the right test applied to the wrong party — **it was the REFUSAL that needed it.** PM's mis-concession and the main session's mis-refusal are **the same failure in mirror**: both were dispositions that felt like discipline, issued without opening the one document that decides the question.
  - ⚠️ **PM's mis-filing is itself an instance of *conceding is not automatically the rigorous move*.** Both live instruments had reported; **the party who would have benefited from the concession declined it and argued against it.** When that happens the concession is very likely wrong. **Yielding to evidence is rigour; yielding because yielding looks rigorous produces a false record that is HARDER to correct than a defended error, because nobody re-opens a self-indictment.**
- 2026-08-01: **⚠️ PM RULING — E-279 CURRENTLY FAILS THE GATE STORY 04 IS BUILDING. FOUR blocking sites. Ruled a CLOSURE OBLIGATION, not a story-04 deliverable, and executed in the Step 8 sub-step 3 (i) authoring window.** Measured by `ca-e279` against the worktree, sites verified by PM.
  - **The decisive argument for closure-obligation, which is structural rather than a scope preference: THE SET OF BLOCKING SITES IS NOT CLOSED UNTIL CLOSURE.** Story 05 has not yet produced its completion report; PM's Active→Archived flip and topic-file flushes are authored at Step 8; `ca-e279`'s own closure codification is still to come. **Any of those can add a fifth site.** So story 04 cannot own this: it would be certifying a property that later work in the same epic can falsify. **Story 04 delivers the gate; the epic must then satisfy it.** TN-2 already states the requirement as a closure property, and C14 was a self-registered observable rather than an AC.
  - ⚰ **CORRECTED SAME DAY — PM's execution window was WRONG and TN-2 forbids it. Caught by the main session.** PM wrote that the repairs happen at *"sub-step 3 (i), the authoring window,"* and called it *"the first use of the restructure is the epic that shipped it."* **That is the RESTRUCTURED sequence, and TN-2 is unambiguous that E-279 closes under the OLD one** — main-checkout rename at sub-step 6, no (i)-(v), no repair window. **PM applied this story's own output to this epic's closure, which is the exact inversion `cr-e279` caught at `SKILL.md:674`, committed by the person who ruled on that finding.** The elegance was the tell: the framing was satisfying, and satisfying is what stopped it being checked.
  - **The CORRECT window, and it is tighter: the repairs must be in the WORKTREE before sub-step 3's `git add -A`, because that is what generates the patch.**
  - ⚠️ **The reason there is no later option is the sharpest fact here: E-279 CANNOT USE ITS OWN FIX, and is the LAST epic subject to the deadlock it removes.** Under the old sequence the rename lands in main at sub-step 6, after the patch is applied at 4 — but between sub-step 4 and the commit at sub-step 10 **the worktree still exists, so `worktree-guard.sh` is in mode 1 and blocks ALL main-checkout Write/Edit**; the worktree is not removed until step 9. **There is no post-rename repair window for this epic. That absence is precisely IDEA-228's deadlock**, and E-279 meets it one last time on its way out.
  - ⚠️ **ORDERING WITHIN THE PRE-STAGING WINDOW, and it is not optional — main session's catch, which PM had missed.** PM's own closure writes are authored in that same worktree window: the COMPLETED flip, the Active→Archived `MEMORY.md` flip, and the topic-file flushes (`archived-epics.md`, numbering state), plus `ca-e279`'s closure codification. **Those writes can themselves spell `epics/E-279-`.** So the order is **author ALL closure content → then the four repairs → then run the sweep → then stage.** **A sweep run before the last artifact is written certifies a tree that no longer exists** — the expired-verification class, arriving at the one moment where the gate's whole purpose is to be current.
    - **Consequence PM must apply to its own hand: write the closure memory entries WITHOUT spelling the pre-archive path in the first place** — reference the epic by ID or by its archive path. **This is the standing operator cost landing on PM immediately rather than on a future epic**, and it is cheaper to not create the hit than to sweep it.
    - **Under the RESTRUCTURED sequence this ordering is free** — (i) authors, (ii) renames, (iii) checks, (iv) stages, so the check is after all authoring by construction. **E-279 has to hold it by hand**, which is one more thing the epic buys for its successors and cannot spend on itself.
  - **Per-site dispositions, and the criterion-versus-evidence cut is load-bearing here rather than decorative — repointing the three EVIDENCE sites would destroy records, and leaving the one CRITERION ships the exact defect this epic exists to stop:**
    - `.project/research/E-279-01-completion-report.md:34` and `:838` — **EVIDENCE.** Records of what `git status` showed and of state at review time. **Reword per TN-3:114 so the line does not SPELL the dead path; do not repoint.** Adjudicated by PM (`.project/` under TN-5); **executed by `ca-e279`, which authored the report** — PM adjudicates the class, the author preserves the record.
    - `.claude/agent-memory/claude-architect/dispatch-telemetry-design.md:12` — **EVIDENCE**, same remedy, `ca-e279`'s own file.
    - `.claude/agent-memory/product-manager/e279-planning-state.md:3` — **CRITERION.** *"`epics/E-279-closure-machinery/epic.md` is canonical … prefer pointing at it"* is **a pointer a reader is meant to FOLLOW**, and it dies on archiving. **REPOINT to the archive path.** PM's own file, executed by PM at Step 8 (i).
  - **EVERY occurrence in THIS FILE self-clears and none must be touched — stated as a PROPERTY, because the enumeration rotted within the day.** `epic.md` moves into `.project/archive/` at sub-step 3 (ii), so at gate time **every** hit in it is inside the excluded tree, **wherever in the file it sits and however many there are.** The occurrence that deliberately spells the dead path **as a closure criterion** — the Success-Criteria line requiring no surviving `epics/E-279-` reference outside the archive tree — is correct as written and is the clearest case: it must spell the path to state the criterion at all.
    - ⚠️ **CORRECTED 2026-08-01 (`cr-e279`, SHOULD FIX). The earlier form was *"Two sites self-clear: `epic.md:301` and `:442`"* — WRONG IN BOTH THE COUNT AND THE NUMBERS.** CR measured **four** hits, at 355 / 361 / 386 / 532. The file was appended to all day and the citations drifted; the count was never right.
    - **The disposition was unaffected, which is exactly why it survived** — every hit self-clears wherever it sits, so the wrong numbers supported the right conclusion and no reader checking the conclusion would look. **A reader EXECUTING the repairs from those numbers would have found nothing there.**
    - **This is TN-3's phrase-anchor rule — written in this epic, today — failing on the list of sites TN-3 governs.** And the repair is not a better citation: **a line-number enumeration of a file being actively appended to cannot be kept correct, so the fix is to stop enumerating and assert the property instead.** A property has nothing to rot.
  - ⚠️ **STANDING OPERATOR COST, flagged by `ca-e279` unprompted and owed to the operator at closure rather than discovered later: from the moment this gate lands, a closing epic's own pre-archive path cannot appear in ANY surviving artifact outside the archive tree** — completion reports in `.project/research/`, agent memory, ideas. **This is a permanent consequence of AC-6's no-override ruling, not a one-off.** Cheap per instance via TN-3:114's reword, and standing. The scope is nonetheless right: excluding `.project/research/` and agent-memory would blind the sweep to exactly the agent-owned stranded references that are IDEA-228's actual complaint.
  - **What the epic got right and what it missed: the CLASS was anticipated (TN-3:114 already carries the reword remedy) and nobody ran the predicate against E-279 ITSELF.** Surfaced now rather than at the closure commit, where AC-6's no-override ruling would have left `--no-verify` as the only exit — the one escape this epic deliberately documented as an operator decision rather than an agent's tool.
- 2026-08-01: **E-279-03 DONE. A LOCATION CHECK IS A JOIN, AND ANY WINDOW-BASED PROXY FOR IT FAILS IN ONE DIRECTION OR THE OTHER — derived independently from two failures in one story, and the PAIR is the finding.** `cr-e279`'s formulation, adopted.
  - **`ca-e279`:** its negative lookahead accepted only a **TRAILING** location clause, so its own `:42` fix — which puts the location **before** the command (*"in the epic worktree: `git mv` for the archive rename…"*) — read as an unlocated residual. **An UNDER-match producing a false positive.**
  - **`cr-e279`:** its ±160-character window could not see context living in the **block header**, so `SKILL.md:656` — a failure-mode statement inside a block whose header already reads *"an ORDERED sequence, all of it in the epic worktree"* — counted as unlocated. **An OVER-match producing a false positive.**
  - **Same check, opposite scope errors, and BOTH resolved only by opening the line.** Neither party filed; each inspected first. **The generalization is this repo's documented JOIN-not-grep rule reached from a new direction: the governing context may sit anywhere relative to the command — before it, after it, or in the enclosing header — so no fixed window captures the join.** Enumerate where the command occurs, then read the enclosing structure for each.
  - **Method worth adopting, from CR: it answered "did anything else move?" by MEASUREMENT rather than trust, without having kept a snapshot.** It compared **changed-OLD-range sets across rounds** — `SKILL.md` gained exactly `28` and `404,0`, `dispatch-pattern` gained `8` and `12`, everything else identical and already certified. Its own note: *"a better answer than keeping a snapshot, because it is reproducible by anyone and does not depend on the reviewer having planned ahead."*
  - **PM bookkeeping item, recorded because it is the same class PM had been finding in others' work:** the Stories table's satisfied-dependency marker was applied to rows 02 and 03 and **not** to row 05, whose dependency (story 02) was equally satisfied. **A convention applied where attention was and not where it wasn't** — found only because the main session asked PM to confirm completeness rather than assuming its own writes were fine.
- 2026-08-01: **THREE INSTRUMENT-BLIND-SPOT ENTRIES, DELIBERATELY NOT MERGED. `cr-e279` argued against PM's plan to record them as one, and it was right — it applied PM's own do-not-merge test (the remedies differ) to PM's proposal.**
  - ⚠️ **The generalization is the LEAST useful part of the set and must not be the entry.** *"A check that is correct about what it measures and silent about what matters"* is true and tells nobody which check to add. **Each member names a DIFFERENT instrument and a DIFFERENT blind spot, and each implies a different remedy.** PM was about to collapse three actionable pairings into one unactionable abstraction — the same move this epic explicitly refused three times with do-not-merge notes, attempted by the party enforcing them.
  - **1. `--numstat` PURITY is silent about STALENESS** *(`cr-e279`, story 03)*. Instrument: a diff-purity check confirming a change touched nothing else (`1/0`). Blind to: whether the surroundings are still CORRECT after the change. **The counter-intuitive core, in its author's words: it answered "did story 03 touch anything else?" and did not answer "is the file now correct?" — so purity and correctness point in OPPOSITE directions when a change alters what its surroundings describe, and a PASSING purity check is a positive indicator that a contradiction may now exist.** Remedy: after a scoped change, re-read what the surroundings claim.
  - **2. AN ABSENCE SWEEP BOUNDED BY THE TOKEN HITS is silent about JUDGEMENTS** *(PM, story 02)*. Instrument: a token sweep plus "read the N judgement files in full," where N was drawn from the token sweep's own hits. Blind to: any judgement carrying none of the tokens — **by construction, since it was never eligible for the reading set.** Remedy: a second axis whose file set is **derived independently** of the first (subject membership, heading tree), not merely a second pass over the same set.
  - **3. A FILE-SET-SCOPED INSTRUMENT is silent about CONTENT OUTSIDE THE SET** *(PM, story 03, on its own verification)*. Instrument: PM verified AC-2 by grepping `SKILL.md`. Blind to: everything else — **AC-2's text says "no closure passage" with NO file scope, so the instrument was narrower than the criterion it was testing.** Codex found `dispatch-pattern.md:42` because nothing told it which file to look in. Remedy: **scope the instrument to the CRITERION'S text, not to the file you happen to be thinking about.**
  - *(Cross-reference only, not a merge: all three are checks whose scope was set by convenience rather than by the property under test.)*
- 2026-08-01: **THE VAGUE-FORM RESIDUAL — a `doc-sweep.md` variant no token grep can distinguish.** Three sites (`dispatch-pattern.md:8`, `:12`, `SKILL.md:28`) listed the main session's `git mv` for archival **with no location**, after `:42` was corrected to specify the worktree.
  - **The variant: `doc-sweep.md` records that retired claims survive in forms carrying NONE of their tokens. Here they survive CARRYING the token and LACKING the qualifier** — the claim was corrected where it was **specific** and survived where it was **vague**, and a token grep returns both identically.
  - **Ruled NOT an AC-2 failure and fixed anyway, and the distinction is deliberate.** AC-2 forbids a passage that *instructs a main-checkout `git mv`*; these instruct no checkout at all and each sentence remained literally true. **PM declined to stretch AC-2 to cover them, having just ruled AC-2 governs over AC-7 — a ruling is worth less if its author widens it the moment that is convenient.** Fixed on REACH instead (main-session override): `dispatch-pattern.md` is `paths: "**"`, and at `:8` the immediately preceding item names its direction explicitly while the `git mv` item does not, so a reader carrying the old model resolves the asymmetry wrongly in the most-loaded file in the repository.
  - **Disposition classes kept separate: `:42` was FALSE (must fix); these were AMBIGUOUS (fixed on reach, capture would have been defensible).**
- 2026-08-01: **⚠️ `epic-archive-check.sh` IS INERT FOR MODERN STATUS LINES — a safety gate this epic CITES has not fired for at least six epics, and E-279 is on track to be the seventh. Found by `cr-e279`, mechanism verified first-hand by PM at `.claude/hooks/epic-archive-check.sh:44`. CAPTURED as a closure item; the idea file is filed at closure, not mid-freeze.**
  - **The mechanism, read rather than relayed:** the scan is `grep -rlE '^\s*`(COMPLETED|ABANDONED)`\s*$'` — **a BARE line.** Any trailing text after the backticked status breaks the match. Measured against real archived epics, `` `COMPLETED` `` alone matches; E-276/E-277/E-278's dated forms do not.
  - **⚠️ IT IS THIS EPIC'S OWN HOUSE STYLE THAT DEFEATS IT, AND THE STYLE IS PM'S.** E-279's status line reads `` `ACTIVE` — set **2026-08-01** on dispatch… ``; flipped to COMPLETED in the same style it carries trailing text and **the gate will not see it.** The status blocks in this epic were deliberately written to carry context — which is good for a reader and fatal for the regex.
  - **NOT a bug — a deliberate design decision invalidated by a documented style drift, and the distinction governs the fix.** The hook's own comment at `:41-43` states the bare-line restriction was chosen *"to avoid false positives from prose mentions of these words."* It traded false positives for false negatives; house style then drifted into the false-negative zone. **So "just loosen the regex" is the wrong remedy**: loosening reintroduces exactly the false positives the comment names. The fix must anchor the status at line START and allow trailing text (`^\s*`(COMPLETED|ABANDONED)`` followed by end-or-punctuation), which still excludes mid-prose mentions. **Record this constraint with the idea or the next author will either under-fix or over-fix.**
  - **What it does NOT falsify, checked rather than assumed.** Story 03's *"`epic-archive-check.sh` still clears"* is TRUE and unaffected — the restructure removes the epic directory earlier, so the timing rationale holds independently of the regex. And story 02's AC-2 reasoning is sound about **REGISTRATION** — unregistering it would still be a silent disaster. **The gate is registered and does nothing for a modern status line; those are different claims and only the second is broken.**
  - **⚠️ BINDING ON E-279's OWN CLOSURE: do not rely on this hook to catch a skipped archive rename.** The backstop is the Step 12 terminal gate reading repository state, which is precisely the case it was written against. **Concretely, after the closure commit: `epics/E-279-closure-machinery/` MUST NOT exist in main and `.project/archive/E-279-closure-machinery/` MUST.** State it as a check, not as a trust.
- 2026-08-01: **E-279-02 DONE; E-279-03 IN_PROGRESS. One pattern from the final gate worth more than the findings it produced: VERIFY THE INSTRUMENT BEFORE FILING THE FINDING — twice in one round, each time by the party who would have looked RIGHT filing it.**
  - **`cr-e279`** caught its own under-scoped instrument: a first archive cut used two of the three tokens and returned 3. **It checked the instrument before reporting a discrepancy that did not exist.**
  - **`ca-e279`** held a hypothesis with **this repo's own rule file behind it** — the documented ugrep quirk where BRE alternation over multiple path arguments returns a silent empty, which would have explained CR's `0` exactly. **It TESTED the hypothesis and REFUTED it**: BRE over both paths returned 2, the same as ERE. **A plausible explanation, with a documented precedent, tested before reporting, and wrong.**
  - **Recorded as a PAIR rather than two incidents, because the pair is the finding.** Both parties held an explanation that fitted, both would have been credited for filing it, and **both spent the check that could only cost them the finding.** That is the adopt-because-it-fits discipline in its hardest form — applied not to someone else's convenient answer but to one's own, with a documented precedent making it *more* tempting rather than less.
  - **Terminal state recorded honestly: the last SHOULD FIX closed with a PUBLISHED DERIVATION rather than another corrected number** — the exact command each class row came from, so the two rows that did not reproduce on CR's instrument are settleable by whoever runs it. `ca-e279` recorded them as **unreconciled rather than settled in its own favour.** Given this epic's record of figures that agreed with everything around them, an honest "unreconciled, here is the command" is worth more than a fourth confident number.
  - **One relay rounding, caught before it reached any artifact:** the main session reported the dead test name at *"0 occurrences"*; it is **0 in the code and 2 in the repo**, the survivors being this History and a report row that *describe* the finding. **The artifact was precise; only the relay rounded.** Recorded because the epic's relay-defect record should not imply the artifact carried it.
- 2026-08-01: **E-279-02 verification — four outputs worth more than the story's own findings. All four ACs passed; these are about the METHOD.**
  - **⚠️ A PRE-REGISTERED PASS BOUNDED BY FILE SET CANNOT FIND A CLASS DEFINED BY THE ABSENCE OF ITS MARKER. The bound and the target are incompatible** (PM). Story 02's token-free pass read *"the four judgement files in full"* — and those four came from the **token sweep's hits.** But a token-free judgement is by definition **not in the token sweep's output**, so the pass could only find token-free forms in files that *also* carried tokens. **`IDEA-173` had zero of the four tokens and was never eligible for the reading set; no amount of care within the method would have surfaced it.** `ca-e279` reached the same conclusion from the other side: *bounding a pass by file set makes it checkable and simultaneously unable to discover membership.*
    - **This is STRONGER than "a bounded criterion can still carry a wrong observable," and the two must not be merged.** That one says the instrument may be miscalibrated; **this one says the instrument cannot be pointed at the target at all.** You cannot enumerate in advance the files that do not contain your tokens.
    - **AMENDMENT ADOPTED for stories 03-05: a pre-registered pass ships with a SECOND AXIS whose file set is derived independently of the first.** Pre-registration stays the default — it is what held story 02's list closed at five files with zero growth — but it now carries a declared second axis rather than a bound that silently cannot reach its own target. **`cr-e279` found IDEA-173 by reading the idea corpus BY SUBJECT, which is that second axis in practice.**
  - **`IDEA-173` VINDICATES AC-3's "enumerate the judgements, not the rephrasings" sub-bullet, and is the reason that clause was written.** Not a surviving phrasing of a retired claim but **a whole idea built on the deleted mechanism**: its thesis (*"clearing the send cap destroys the data that would justify raising it"*), its worked E-272 example, and **all four** open questions are now referent-less — there is no log, no cap, and the `25` it reasons about was raised to 40/60 and then retired. It carries **none** of the four sweep tokens. Verdict: LEFT UNTOUCHED, PM-owned under TN-5, routed to closure for a moot/close disposition.
  - **THE STRONGEST ARGUMENT THIS DISPATCH PRODUCED FOR WITHHOLDING A CANDIDATE RECONCILIATION — and it ran against PM.** PM correctly found that the class rows did not sum to the stated surface (`19+4+3+10 = 36 ≠ 34`) and proposed a cause: excluding the hook file and the SE-memory file. **That was NOT the cause — the archive row was wrong (19, actually 17) — but PM's proposed decomposition reached the correct total of 34.** The main session withheld it; `ca-e279` re-derived independently and found the real cause. **Had it adopted PM's reconciliation it would have "confirmed" a wrong decomposition arriving at exactly the number that ends the inquiry.** Adopt-because-it-fits, in its most seductive form: the fitting answer was also the terminating one.
  - **A property-defined class protects MEMBERSHIP by construction; it does NOT protect the COUNT of its members** (`ca-e279`, sharpening PM's own ruling). PM ruled story 02's bulk classes legitimate on membership grounds — correct, and what AC-3's RED concerns — and said nothing about counts, which is the part that still needed deriving. **The diagnosis underneath: the total was MEASURED (`wc -l`) and the class breakdown was EYEBALLED off the same screen, so a measured figure and an estimated one sat in one table, indistinguishable in presentation.** Presentation does not distinguish them; only re-derivation does.
- 2026-08-01: **⚠️ EXECUTION-OVER-READING HAS A BOUND, AND THIS IS IT: EXECUTION VERIFIES BEHAVIOUR AND IS BLIND TO PROSE.** Recorded as a qualification on a practice this repo rightly favours, because an unqualified form is what produced the defect.
  - **The instance:** `ca-e279`'s AC-7 restructure renamed a test. **Three instruments verified that restructure by ATTEMPTED VIOLATION — the strongest evidence available — and not one read the prose around it.** A surviving docstring at `tests/test_worktree_guard.py:234` still cites `test_ac7_blocks_creation_when_real_root_is_absent`, which exists in no file. **Execution found the semantics; nothing checked the citation**, because a renamed test is invisible to every behavioural instrument by construction — the suite passes precisely because the behaviour moved intact.
  - **The generalization: a rename is the defect class execution cannot see.** Behaviour-preserving edits are exactly the ones a behavioural check certifies while their references rot. **Where a change renames or moves a symbol, the prose referencing it needs a READING pass that no amount of green certifies.** E-270's class, arrived at from the opposite direction.
  - **Second demonstration of the uninstrumented-quiet-region pattern, by the same instrument on a different story.** Codex found **nothing** in `settings.json` or the `.gitignore` stanza — the surfaces CR had executed — and nothing on the route CR found independently. It landed on **prose around already-approved work**. The convergence candidate predicted this; it has now happened twice, and both times the findings were in what nobody had instrumented rather than in what anybody had checked badly.
- 2026-08-01: **E-279-01 DONE; E-279-02 IN_PROGRESS. Two PM rulings recorded with it.**
  - **Files-list vs AC precedence (main session's reading, PM concurs and reconciled the list).** E-279-02's AC-3 instructed an edit to `.claude/agent-memory/claude-architect/epic-codifications.md`, which its Files list did not name. **The AC binds: an AC is what DONE means, a Files list describes expected reach.** A story satisfying its Files list while failing an AC is not done; the reverse is merely an incomplete list. **But the list is not decorative and the gap was a real defect** — it is the input to the same-file collision rule, so an unlisted file is an undetected collision risk. **PM verified no collision exists** (no other E-279 story touches that file) before reconciling, rather than waving it through on the precedence argument alone.
  - **Criterion-versus-evidence applied to PM's OWN artifact, recorded because that is where the pull to tidy is strongest.** Story 01's Context cites `worktree-guard.sh:86` for the old glob; the glob now sits at `:189`. **Left unchanged.** The sentence is tensed *"Today the guard globs for a directory"* — it is a record of the defect **as found at planning time**, so the stale number is EVIDENCE of what was observed, not a citation a reader is meant to follow. Editing it would falsify the record. **The rule is easy to apply to someone else's file and hardest on one's own, where a stale-looking number reads as an oversight to fix.**
- 2026-08-01: **⚠️ LIVE INCIDENT — THE PROBE VERIFYING AC-7 COMMITTED AC-7's VIOLATION. Two real directories (`baseball-crawl-E-999`, `definitely-absent/`) were created under `/tmp/.worktrees` by a mutation probe that disabled the guard to prove the enforcement tests go RED.** Detected in the same batch and removed within the minute; **cleanup verified independently by the main session rather than taken from the report** — `/tmp/.worktrees` holds exactly `baseball-crawl-E-279`, and `git worktree list` shows only the main checkout and the epic worktree.
  - **This was a REAL session-wide wedge, not a theoretical one.** `.claude/settings.json` resolves the guard through `$CLAUDE_PROJECT_DIR` to the **main checkout**, where the OLD glob-based hook is still live until closure (TN-2). A directory under the real root therefore put **every agent on the machine** into mode 1 for as long as it existed.
  - **It is a live demonstration of the MUST ruling above, produced within minutes of it.** Under a persistence-framed reading this was a non-event — nothing leaked, teardown was clean. Under the existence-framed reading the ruling adopted, it is an incident. **The ruling and its specimen arrived in the same hour, from opposite directions.**
  - **The design flaw it exposed, and the repair is now stronger than the ruling required.** The enforcement tests aimed `mkdir` at the REAL root and relied on the guard to stop them — correct while the guard works, and **the suite itself becomes the wedge the moment it does not.** A test file whose failure mode is *"wedge every agent on the machine"* is the wrong shape for the AC that exists to prevent exactly that. Restructured into a **pure predicate** (`_is_forbidden_target`, tested against the real constant with no filesystem effect) plus behavioural tests over a **stand-in root under `tmp_path`**, so a broken guard now creates a harmless temp directory instead of a wedge.
  - **The transferable form: an instrument that must ATTEMPT the forbidden act to prove it is forbidden inherits the full blast radius of the thing it guards against.** Split the predicate from the effect, and test the predicate against the real value while testing the effect against a stand-in.
- 2026-08-01: **PM RULING — AC-7's FIRST CONJUNCT IS A HARD GATE (MUST), NOT A GUARD.** `cr-e279` filed the residual as SHOULD and explicitly deferred the severity to PM (*"if PM reads AC-7's criterion as a hard gate rather than a guard, it is MUST and I would not argue"*). Ruled MUST. **This is a ruling about the STANDARD, not a claim that anything is broken today** — CR is right that no current test trips it.
  - **The reason is in AC-7's own rationale, which locates the harm during EXISTENCE rather than at persistence:** *"a real directory puts every agent in the session into mode 1 **while it exists**."* A create-then-remove test wedges every agent on the machine for its duration. **Persistence is the AGGRAVATING case, not the defect** — so "creates" is the primary conjunct and "can leave behind" the secondary, which is the opposite of how the enforcement has been built twice.
  - **Corroborating, and it is this epic's own origin:** IDEA-230's repro wedged the repository with a directory that was transient in principle and total in effect. An epic that exists because a momentary directory blocked every agent cannot treat momentary creation as the soft half of its own test criterion.
  - **⚠️ VERIFICATION STANDARD THAT FOLLOWS, and it is the operative half of this ruling: the conjunct must be closed BY CONSTRUCTION and the guard must be shown to FIRE on a deliberate violation — in BOTH the root-present and root-ABSENT branches.** "No test violates it" is not the criterion; "a test that violates it is caught" is. **Two enforcement attempts have now failed on this same conjunct** — the single-path fixture, then the widened snapshot, which closes the root-present branch and leaves creation invisible when the root does not exist, the state the fixture's own docstring calls normal. Two failures on one conjunct is sufficient evidence that convention-based enforcement does not hold here.
  - **The seam docstring is ruled a defect either way.** It claims *"every directory this suite creates goes through here, which makes AC-7's first conjunct inexpressible"* — false as an enumeration (`:216-217` creates a `baseball-crawl-E-*` child directly, bypassing the seam, inside the test verifying the detector), and **rendered false in the opposite direction** once the `mkdir` patch makes the conjunct expressible and enforced. **A seam is a convention until something makes it an enforcement**, and prose asserting a guarantee the code does not provide is the class this repo documents.
- 2026-08-01: **CODIFICATION CANDIDATE — A PARAPHRASED CRITERION SILENTLY LOSES A CONJUNCT, AND EVERY DOWNSTREAM INSTRUMENT THEN MEASURES THE PARAPHRASE.** `cr-e279`'s diagnosis of its own miss, and a cleaner mechanism than "four instruments all missed it."
  - **What happened:** CR's original finding **restated AC-7's criterion and dropped a conjunct while quoting it** — the RED reads *"creates, **or** can leave behind"* and the paraphrase kept only the name axis. **It then widened the guard along the axis it had mis-stated and left untouched the axis it had deleted from its own quotation.** Not a wrong conclusion from a correct reading: after its first sentence about it, the second conjunct was never in view.
  - **Why this is worth more than the aggregate account:** it explains *why the question got asked the way it did* by all four parties, rather than asserting that everyone was careless. **Once a paraphrase is in circulation it becomes the criterion in practice**, and every instrument aimed at it inherits the deletion. Remedy is mechanical and cheap: **when a criterion has conjuncts, enumerate them separately at the point of use — do not paraphrase a multi-part RED into a sentence.**
  - **`cr-e279`'s statement of the convergence finding, better than the finding-count version and adopted: *"Execution held; the uninstrumented surfaces did not."*** It names **where to point the next instrument**, not merely that one was needed.
- 2026-08-01: **THE CONVERGENCE CANDIDATE IS DEMONSTRATED, NOT ARGUED — Codex returned TWO real findings, BOTH outside the region three instruments had converged on, and one of them is in code all four of us certified.** This is the candidate's own prediction tested from outside and confirmed, so it should be carried as evidence rather than as a hypothesis.
  - **P2 — the AC-7 fixture enforces only HALF its criterion.** AC-7's RED is *"any test that creates, OR CAN LEAVE BEHIND, a directory under the literal `/tmp/.worktrees/`."* The session fixture snapshots the real root before and after, so **a test that creates a real worktree directory and removes it before teardown passes the guard while wedging every agent on the machine for as long as it exists.** Codex verified the counterexample against the fixture's own logic. **Four parties handled that fixture — CR proposed it, `ca-e279` implemented it, the main session routed it, PM verified AC-7 — and all four asked whether it beat the single-path version it replaced (it does) rather than which CONJUNCT of the criterion it enforced.**
  - **P5 — the story's own AC text was the one artifact nobody swept.** `E-279-01`'s AC-6b still named the replacement remedy as `remove` / `prune` after the story itself established prune is not interchangeable. The hook, the rule file and the completion report were all reconciled; **the artifact that DEFINES done was not.** Eighth Pattern A instance of this dispatch, in the definition of the fix. Corrected by PM the same day.
  - **⚠️ The sharpest part is what Codex did NOT find: nothing on the guard logic, the registry predicate, the five bounds, or the prune semantics. It agrees with the execution work.** Both findings sit in **the test harness and a planning artifact — the two surfaces our loop never instrumented.** So the quiet regions were quiet because they were **UNINSTRUMENTED, not because they were healthy**, which is precisely what the convergence candidate predicts and what no additional round of the same three instruments could have distinguished.
- 2026-08-01: **CODIFICATION CANDIDATE — A COUNT USED TO ESTABLISH A PATTERN RECRUITS MARGINAL MEMBERS.** `cr-e279`'s generalization, raised against the main session's own error and recorded at its request.
  - **The statement:** when a count is used to establish that a PATTERN exists, **the pressure runs toward admitting a marginal member, because the count IS the evidence for the pattern.** The check is therefore not *"is the number right?"* but ***"does every member belong to the class the number is being used to establish?"***
  - *Instances, both caught by challenging MEMBERSHIP rather than arithmetic:* the main session's "fourth counting error" was three — PM's unreconciled 12-vs-14 was a missed **reconciliation**, not a wrong **count**, folded in to complete a narrative; and the earlier "three instances of shape-identical failure."
  - **Structural half, which is the actionable one: the party assembling a lineage is systematically NOT the party who made most of it**, so they count from relayed reports rather than from their own record. **The count therefore belongs to whoever holds the primary evidence, or is not asserted.**
- 2026-08-01: **CODIFICATION CANDIDATE — SATISFYING THE FORM OF A DISCIPLINE WITHOUT ITS SUBSTANCE. `ca-e279`'s generalization of its own defect, recorded because it existed only in a dispatch message.**
  - **The instance:** it reported a settled claim "as of 17:05Z" when the clock read **17:03:18Z**. The timestamp was **present, correctly formatted, and never measured** — estimated, and running the wrong way, placing its last write *after* a re-freeze that the file's mtime puts *before*. **A timestamped settled claim whose timestamp was not read is the status-decay discipline defeated while wearing its own uniform.** Its adopted remedy: read the clock, or say "after my last write" and let the mtime carry it.
  - **The generalization, which is the transferable half: this is DISTINCT from forgetting a discipline, and strictly harder to catch, because the artifact looks COMPLIANT.** A missing timestamp prompts a question; a wrong one does not. **Same family as the contiguity note certifying anchors as "literals" they were not, and as a verdict being the artifact least likely to be re-examined.** Unifying form: **an assertion carrying the marks of having been checked is the one least likely to be checked.**
  - **Companion, PM's, from the same exchange — the counter-technique when you cannot measure.** PM has no Bash and could not read the clock to establish whether its verification pass predated the final write. **It settled the question from CONTENT instead: the artifact it had read already contained the note recording the renumber→naming correction, so the read was necessarily of the post-correction generation.** Where a timestamp is unavailable, **an artifact that records its own latest correction dates itself** — and unlike an authored timestamp, that evidence cannot be estimated.
- 2026-08-01: **CAPTURED CLOSURE ITEM (not fixed) — the EXPLANATION of the `14/14` figure is itself not derivable. Third generation of the producibility defect, in the note correcting its second.** Recorded under the pre-committed capture address (a post-freeze finding goes to the epic History as a closure item) rather than triggering a sixth fix cycle. **Recording does not foreclose fixing it** — it guarantees it is not lost if the main session declines.
  - **The completion report's AC-8 note explains the wrong `14` as** *"14 strings across those 12 rows (two rows carry two anchors)."* **Re-derived from the table as it now stands: 13 anchor strings across 12 rows, and exactly ONE row carries two** — the final constraint-list row, quoting `"**No branch management**"` and `"**NEVER restore a working-tree file from the INDEX**"`. Rows 1-11 carry one anchor each. The `was "…"` quotes in three rows are historical text, not anchors into the current file.
  - **Severity LOW and stated two-sided: the headline figure is now CORRECT** (`12/12`, matching three independent counts), and no reader is misdirected about the file's contents. The defect is confined to a parenthetical explaining a retired error.
  - **Why it is worth a line anyway:** the sentence containing it says *"a figure that was never derivable from the table it described, which is this epic's own producibility rule turned on its author for the second time."* **It is now the third, inside that sentence's own repair.** The transferable point is not the recursion but the mechanism: **an explanation of a counting error is itself a count, and it inherits the whole defect class it was written to close.** Nobody re-derives the explanation, because the headline figure it corrects has already been checked.
- 2026-08-01: **CODIFICATION CANDIDATE — REVIEWERS GO CORRELATED OVER ITERATIONS, AND NO TIMESTAMP DISCIPLINE TOUCHES IT.** PM's observation about this dispatch's own record; main session asked for it as a distinct member. **DISTINCT from the expiry and finding-list candidates and must not be merged: those are about a claim going stale over TIME; this is about a set of reviewers going correlated over ITERATIONS.** Serializing, freezing and attaching timestamps — the remedies that fixed everything else here — do nothing against it.
  - **The statement:** five review rounds and thirteen findings look like thoroughness, and they are also **five rounds of three instruments training each other's assumptions.** Convergence is a reliability risk that **more rounds of the same instruments cannot detect**, and it is invisible from inside.
  - **⚠️ The trap is that productivity keeps looking like health. Every one of the thirteen findings was REAL, and none of that is evidence against the claim** — three instruments that have spent an hour aligning will keep producing real findings **inside** the region they now share, and produce nothing at all **outside** it. A rising finding count is therefore consistent with both a working review and a narrowing one, and cannot discriminate. **The only detector is an instrument that was not in the room.**
  - **Remedy, structural and in the same family as the others: introduce an instrument with NO SHARED HISTORY, rather than run the existing ones again.** This is what made the operator's Codex instruction well-timed rather than merely additive — on the sequence as it stood, this epic was heading to closure with no independent instrument at all.
  - **Refinement to an EXISTING rule, which is the part with immediate cash value.** `.claude/rules/agent-routing.md` already prescribes escalation for adversarial work, justified by **capability** ("a fresh strongest-tier context has repeatedly found what same-tier review missed"). This candidate supplies a **second and independent** justification: **independence**. The two come apart — independence also argues for a fresh instance of the *same* tier, which the capability rationale alone does not motivate, and it argues for rotating an instrument that has been in a long dispatch even when no escalation is warranted. Worth folding in as a distinct trigger rather than a restatement.
- 2026-08-01: **CODIFICATION CANDIDATE (PM-domain) — AN AC CRITERION ANSWERS ONLY THE QUESTION IT WAS WRITTEN FOR, AND WILL ANSWER ANY OTHER WITHOUT SIGNALLING THAT IT HAS.** Main session's formulation, from PM's own self-diagnosis; recorded because it existed only in a dispatch message and is the most PM-relevant lesson of this story.
  - *Instance:* PM rated the four non-greppable `(…)` anchors **minor** because they **resolve** — which is AC-8's criterion — rather than because the report's claim *"grep-verified as a single-line, single-match literal"* was **sound**, which is a different question. The AC answered the first and PM let that answer stand for the second. **Same cut that dissolved three apparent conflicts on this story** (PM-vs-CR on `:24`/`:35`, PM-vs-`ca-e279` on the in-body site, CR-vs-PM on consistency-vs-misreading), here turned on PM's own rating rather than on a disagreement.
  - **Why it is worth carrying into the PM quality checklist rather than the dispatch rules:** the existing checklist items govern whether an AC is *well-formed* (reachable RED, property-not-action, no decorative construction). **This one governs the AC's USE** — a well-formed criterion is still the wrong instrument for a question it was not written for, and it returns a confident verdict either way. The tell is a verdict that feels settled while the question has quietly changed underneath it. *(PM's own-memory copy is deferred to the Step 8 authoring window — a dispatch-time own-memory write would ride the staging boundary as if it were part of story 01, per `cr-e279`'s ruling.)*
- 2026-08-01: **FOUR CODIFICATION CANDIDATES recorded as insurance — all four existed ONLY in dispatch messages, and the completion report is frozen.** `ca-e279` carries them to Step 8 and deliberately did NOT request a freeze release, on the reasoning that they are codification material rather than story-01 correctness and the story should stage on its merits. **Disposition on all four is `ca-e279`'s as context-layer owner; PM recorded, PM did not rule.**
  1. **A FINDING LIST IS A MEASUREMENT AND EXPIRES LIKE ONE** *(main session)*. The Class-2 shape no existing member covers, and the reason is the framing: **a finding list reads as a WORK ORDER rather than as an observation with a timestamp**, so nobody brackets one. *Instance:* PM's and `cr-e279`'s finding lists both arrived largely moot, having predated the anchor conversion and described rows that no longer carried numbers. **General form, which is the version to keep: a finding list, a review verdict, a ruling, and a completion report are ALL measurements with timestamps — and this dispatch produced an instance of each going stale in flight.** Remedy is the one that has now worked three times: serialize, or attach the measurement's time. Not more care.
     - **`cr-e279`'s sharpening, which is the reason this deserves its own member rather than folding into the expiry candidate — recorded verbatim in substance: a finding list expires DIFFERENTLY from a measurement. A measurement goes stale SILENTLY; a finding list goes stale while still reading as a WORK ORDER, so the recipient ACTS on it.** Same structural defence, one layer out. The asymmetry is the whole point: a stale measurement misinforms, a stale finding list *directs*.
  2. **THE CITATION-SIDE VARIANT OF THE CONTIGUITY TRAP** *(jointly PM — the wrap warning — and `ca-e279` — the variant)*. **A phrase anchor is a claim about a grep that someone else will run later, and the only way to know it holds is to run it.** The bite: all three failures were contiguous **in the SOURCE**; the **anchor** carried an internal ellipsis, or a typographic `…` substituted for a literal `...`. **The failure arrives through the CITATION, not the text** — and the anchor is what a reader actually copies. **Without this clause, "use phrase anchors" ships a SILENT failure mode in place of a VISIBLE one**, which is a worse trade than the rot it replaces: a rotted line number is visibly wrong, a broken anchor returns a confident empty that reads as *"this text was deleted."*
     - **Companion, PM's, from the table-halves pass — the same shape one level up, and it survived the fix.** The anchor conversion protected the SITE citations and left the **ROW-NUMBER** citations, which rot on insertion identically: AC-6b's preamble carries three stale row references after rows 8 and 9 were inserted. **AC-8's table is immune because it dropped its numeric column entirely — the rot survived exactly where the number survived.** Converting one axis and leaving the other, in the very edit made to end rot. It is near-undetectable by reading because **the COUNT stayed right while the IDENTITIES went wrong**, so the summary reconciles and only the pointers are broken.
  3. **PROBE STATE — SAME PROBE, SAME FLAW, TWO INDEPENDENT AUTHORS** *(`ca-e279` and `cr-e279`)*. Both independently wrote the same repo-global `prune` probe and both carried the same defect: an earlier probe had already pruned one entry, so the run showed one prune clearing the one remaining and **tested nothing about global scope**. Both caught it by the result being *suspiciously consistent* rather than by inspection. **Remedy is structural — a fresh fixture per probe — not "be careful with state."** Two independent authors reaching the identical flaw is the argument that it is a property of the setup rather than of either author.
  4. **A NARROWED GREP CAN INDICT A COLLEAGUE, NOT JUST MISS A LINE** *(`se-e279`, 2026-08-01)*. **Recorded after PM reported its codification window closed — disclosed rather than folded in silently, because a window that reopens without saying so is not a window.** It is here rather than in a completion report because it is the only instance on this dispatch where a tool artifact was one step from a **false accusation against a named agent**, and that consequence is not represented anywhere else in this list.
     - *Instance:* verifying the main session's citation of `context-layer-assessment.md`, `se-e279` grepped the literal `MEMORY.md index` and got **EMPTY**. The file reads ``  `MEMORY.md` index  `` — **backticks interpolated between the two words**, so no contiguous literal can match. The citation was **verbatim correct**.
     - **The consequence is what makes it its own member: had that empty been reported as a finding, it would have said the main session FABRICATED a citation to PM.** Every other narrowing case in this epic costs a missed line or a false "deleted"; this one costs a colleague's credibility. **A tool artifact was one step from an integrity allegation.**
     - **It fired inside the check written to prevent it** — a verification pass, run by an agent doing exactly the right thing, defeated by `tool-output-integrity.md`'s own entry 2 (markup-interpolation narrowing). **Caught only because an unexpected empty is a cross-check trigger and never a finding.** No amount of care would have helped; the reflex did.
     - **PM's addition:** the asymmetry worth carrying is that **this class of error accuses OUTWARD by default.** A narrowed grep against your own text reads as *"I deleted that"*; against someone else's it reads as *"they made it up."* The second is the one you must be slowest to report — and it is the one that feels most urgent.
- 2026-08-01: **CODIFICATION CANDIDATE — A MANUFACTURED CLAIM WEARING A CREDIBLE NAME. Raised by `ca-e279`; recorded by PM because epic files are PM's and the candidate dies with its author otherwise. DISTINCT from every Pattern A entry below — do NOT merge.**
  - **The statement, in `ca-e279`'s framing:** *an inference you formed yourself, then attributed to another party, is more dangerous than a bad relay — because the apparent source makes it look already checked.*
  - **Why it is not Pattern A, which is the whole reason to keep it separate.** Every Pattern A instance on this dispatch was a **real claim that failed to propagate**. This is a **claim that never existed, carrying a credible name**. It defeats the one defense that has worked all dispatch — *go to the primary* — **because it sends the checker to a source that never said it.** Going to `ca-e279` would have produced "I never said that," a different failure path from any propagation case.
  - **The remedies differ, which is the operative test for splitting a candidate** (same reasoning as the verify-while-editing cut below). Pattern A's remedy is *grep for the other copies*. This one's is: **name your inferences as inferences, and treat an attributed claim as unverified until the named source confirms it.**
  - **Live instance, the main session's, recorded on its own insistence over PM's gentler write-up:** it inferred *"the rule file was untouched this round,"* attributed that to `ca-e279`, and passed it to PM as `ca-e279`'s report. `ca-e279` had in fact reported the Defect B fix landing at **both** sites. **There was no upstream claim.** Caught only because the main session had told PM to confirm rather than accept, and PM did — via the staleness tripwire recorded below.
  - **Credit where the opposite reflex showed, because this dispatch has been expensive in the other direction:** offered a freeze release for its own closure-codification material, `ca-e279` **checked what it actually held that was not already durable** — the failure taxonomy, the disposition argument, structural-over-textual, criterion-as-inherited-claim, verdict-is-least-re-checked, CR's self-assessment, and PM's "landing the fix in one location is itself the judgement" — found all of it already in the completion report, and **asked for nothing.** It verified before requesting rather than requesting to be safe.
- 2026-08-01: **CODIFICATION CANDIDATE — VERIFY-WHILE-EDITING IS A SEQUENCING DEFECT, NOT A DILIGENCE ONE. Raised by PM, granted by the main session as a declared freeze. Distinct from the expiry candidate below and must not be merged into it.**
  - **The cut.** *"A verification claim expires when the artifact changes"* names the FAILURE. This names the CONDITION under which that failure is **unavoidable**: when verification and editing run concurrently on the same files, every verification is superseded before it is delivered, **and no amount of care prevents it.** Merging the two loses the remedy, because the remedies differ — the first asks you to re-derive, the second asks you to STOP EDITING FIRST.
  - **Executed evidence, not inference: FOUR AC-8 verifications and THREE AC-6b verifications on story 01, each correct when taken and stale before it landed.** The concurrency was proven rather than assumed: two PM reads of `worktree-guard.sh` **in one turn** disagreed on a paragraph's position (`:47-49`, then `:51-53`); running the two-cause differential found a new 4-line paragraph inserted at `:36-38`, a shift matching its length exactly. **The first read was accurate and the file moved** — not a garble.
  - **Remedy, and it is structural rather than textual — the asymmetry `ca-e279` argues the whole closure codification should rest on.** Declare a freeze on the story's files, have the implementer confirm quiescence, then ONE verification pass, then ONE review; release the freeze per fix and re-freeze. Trying harder was the intervention that had already failed seven times here.
  - **⚠️ The reusable instrument, which is the most transferable thing in this entry: a STALENESS TRIPWIRE.** Carry a phrase you have **already verified present** in the target, alongside an ordinary known-stable control, in the same pattern. **Control fires + tripwire returns zero ⇒ the file changed under you** — established in ONE call, where the two-cause differential otherwise needs several steps and an mtime PM cannot read (no Bash). This is what caught the rule file: PM carried `NEVER to an agent`, verified in that file an hour earlier, the `Hook Enforcement` control fired, and the tripwire's **verified absence** exposed a whole round of edits that had been relayed as "not touched this round." A plain positive control cannot do this — it confirms the pattern ran; only a previously-verified phrase reports that the CONTENT moved.
  - **Trigger, recorded because the candidate is incomplete without it — and CORRECTED 2026-08-01 to the harsher and accurate version at the main session's own insistence.** This first read that the main session had *relayed an implementer's claim about its own diff*. **That is wrong and it understates the failure: there was no upstream claim to relay.** `ca-e279` had reported the Defect B fix landing at **both** sites explicitly; *"the rule file was untouched this round"* was the main session's **own inference**, which it then attributed to `ca-e279` and passed on as that agent's report. Its framing, kept: *"Two agents were working against a state I asserted and had not verified."* The correction matters because the original points at a weaker and rarer failure — see the manufactured-attribution candidate above, which is where this instance properly belongs. The surviving generalization: **an implementer is not a disinterested witness to its own diff, and neither is an orchestrator who has just read its report.**
- 2026-08-01: **TWO LIVE INSTANCES of the fourth codification candidate ("a verification claim expires when the artifact changes"), in ONE dispatch turn, from two different agents. Recorded as dispatch-phase evidence for a candidate this epic already carries** — the candidate is not new; the evidence is, and it is the kind that dies in a message.
  - **`ca-e279`, self-caught.** Its completion report described the story-01 diff as "two hunks, the header block and the detection block." It had run `git diff` **before** the site-12 edit and written the sentence **after**, without re-running — a stale measurement, not the hunk-merging artifact PM hypothesized. Re-run on request: changed old-line ranges `{5, 7, 14, 20-22, 85-86, 92}` against the five pinned behaviors at `29-32`, `49`, `53-56`, `61-83`, `104-116`. **No intersection** (PM checked the arithmetic independently), so the AC-6 verdict is unaffected and now rests on executed data rather than on a structural read.
  - **PM, caught by the main session.** PM asserted "my ruling did not reach `ca-e279`," citing a completion report that still listed the scope question as open. **The ruling had reached it**; the report simply predated it. PM used a report that could only speak to a PAST state as a CRITERION about the CURRENT one.
  - **The transferable half is the SUB-FORM, because the general rule was already known to both parties and did not prevent either instance.** `.claude/rules/tool-output-integrity.md` records that **a STATUS claim decays exactly like a verdict, and nobody brackets those** — PM's is that shape exactly, and PM had cited the un-actionable-delivery bound in the same session. **Awareness conferred no immunity in either case; both were caught by a second party or by a re-run, neither by re-reading.** Remedy is the one already written: attach the measurement and its time to a status assertion, or do not make one.
- 2026-08-01: **CLOSURE-CODIFICATION CANDIDATE (recorded, not ruled on) — a `paths: "**"` rule edited mid-dispatch is stale in every already-running agent's context.** Raised by `ca-e279`, which owns closure codification here (TN-11 item 1); recorded in the epic at the main session's request because **an item that lives only in an agent's context dies with that agent**, and this epic already carries one instance of an observation that nearly died in a SendMessage. **This is a CANDIDATE. Whether it belongs in the closure codification is a context-layer call and therefore `ca-e279`'s, not PM's and not the main session's.**
  - **The bounded statement, as `ca-e279` framed it:** *a `paths: "**"` rule edited mid-dispatch is stale in every already-running agent's context; disk wins, and an assertion about that file's contents made later in the same session is coming from the stale copy.*
  - **Two independent sightings, same date (2026-07-26), mechanism unestablished in both** — one in the main session's memory (a subagent's injected CLAUDE.md predating a correcting commit that was an ancestor of its own HEAD), one recorded independently by `ca-e279`. The second sighting is what lifts this above an anecdote; neither establishes the mechanism, and the candidate must not be written up as though one does.
  - **The live instance is this epic's own AC-8 edit.** `.claude/rules/worktree-isolation.md` carries `paths: "**"` (verified), so once AC-8 lands, every already-running agent on this dispatch — PM, the main session, `cr-e279` — carries the pre-edit text while the corrected text is on disk. **Anyone asserting what that file says after the edit must read the worktree copy first.** PM has adopted this for its own AC-8 verification.
- 2026-08-01: **SCOPE RULING (PM) — `.claude/rules/worktree-isolation.md` folded into story 01 as AC-8. Raised by `ca-e279` mid-story; three options offered (fold into 01, fold into 03, capture as an idea); PM ruled option 1, which was also `ca-e279`'s recommendation.**
  - **The finding.** That file states the mode-1 trigger as directory existence at `:21`, with the same framing at `:22` and `:26` — the claim story 01 falsifies. It was in **no** story's file list (PM verified across all five). PM confirmed the three sites first-hand rather than on the report.
  - **Why fold rather than defer.** The claim lives in **three** places: the hook header (story 01 AC-6b), the hook body `:177-178` (already fixed by `ca-e279` under AC-6b), and this rule file. Two were already story 01's. **Splitting one claim's correction across two stories is the exact generator of Pattern A** — "corrected where it was FOUND, surviving where it LIVES" — which this epic catalogued four instances of on 2026-07-28. Deferring to story 03 would also put a detection-mechanism claim inside the closure-restructure story, where it is off-subject; "prose goes in 03" is not a real rule here, since story 01 already edits context-layer prose. An idea capture was rejected outright: the file carries `paths: "**"` frontmatter (PM verified at `:1-4`), so it loads into **every agent's context on every interaction** — a stale detection claim there outranks the hook header in reach, and a deferral is the disposition most likely to evaporate because nothing downstream fails when it does.
  - **⚠️ The cost `ca-e279` priced was too high, and correcting it is PM's to do because PM owns statuses. It framed option 1 as "a re-open of a completed story." In STATUS terms it is not: E-279-01 has never been `DONE`.** It has been `IN_PROGRESS` since 2026-08-01 and reported complete by its implementer, but reported-complete is not accepted-complete — AC verification had not run and the diff was unreviewed. So AC-8 lands in an unreviewed hunk that the reviewer sees once, whole, rather than re-opening an accepted artifact. **This is the cheapest moment for this fold and the cost rises from here**, which is why the ruling was issued immediately rather than batched with completion.
  - **Bounds.** Reconciliation, not rewrite: the file's constraint list, index-restore hazard and own-memory rules are untouched (epic TN-4's ceiling reasoning applied to prose). AC-8 is verified **by absence** — zero surviving directory-keyed sentences — not by confirming the correction arrived, and it carries the per-site written verdict, so a verdict-free pass is a RED even where the sites happen to be right.
  - **Process note worth keeping.** `ca-e279` raised this **mid-story rather than in its completion report**, citing this epic's own line 82 ("a flag that lives only in a completion report reaches nobody"). That is the rule working as intended, and it is why the fold was cheap: had it arrived at completion, the diff would already have been reviewed. It also surfaced by the mechanical remedy this epic prescribes — grep for the claim before closing the correction — not by anyone re-reading the reasoning.
- 2026-08-01: **STATUS → ACTIVE** on dispatch. Epic worktree `/tmp/.worktrees/baseball-crawl-E-279`, branch `epic/E-279`. READY held for 4 days against the 60-day freshness gate, so no re-confirmation was required and the gate is discharged rather than waived. **Two dispatch-time properties a reader should carry forward, both stated in the epic and both easy to lose at execution time:** story 01 runs FIRST as a dependency edge (01→02, 01→03), not as prose (TN-1, and the Codex P1-2 note under the Stories table); and E-279 closes under the OLD closure sequence, not the restructured one it ships (TN-2).
- 2026-07-28: **STATUS → READY** on operator ruling, over a second Codex round. Freshness clock starts today; re-confirm or demote by **2026-09-26**. **Dispatch requires separate operator authorization.**

### Review Scorecard

| Review pass | Findings | Accepted | Dismissed | Composition |
|---|---|---|---|---|
| Internal iteration 1 | **see that entry — deliberately not restated here** | — | 0 | claude-architect holistic F1-F8 + PM sub-pass B PM-1..PM-8 |
| Internal iteration 2 (triage) | **21** | 21 | 0 | 11 CR originals + 4 NEW + 5 PM + 1 seed (F9) |
| claude-architect validation | **2** | 2 | 0 | 1 MUST, 1 SHOULD, both story 01 |
| Codex spec review 1 | **5** | 5 | 0 | 2 P1, 2 P2, 1 P3 |
| **Total (iterations 2 onward)** | **28** | **28** | **0** | |

⚠️ **UNRESOLVED EVIDENCE DISCREPANCY IN THE ITERATION-1 ROW — flagged, NOT harmonized.** That entry states *"15 findings landed, 0 dismissed"* while naming **F1-F8 plus PM-1..PM-8 (16 raised)** and recording **PM-4 and F7 as no-action**, which yields **14**. **15 is not derivable from what the entry itself states.** The PM who wrote this scorecard was not present for iteration 1 and **declines to reconstruct the figure**, because a number invented to make a table tidy is exactly the defect this epic documented twice in its own text. **Resolve it from iteration 1's own record or from whoever ran that pass — do not take a number from this row.**

**This refusal IS the scorecard applying the epic's own producibility rule to itself:** *re-derive a figure from the thing that would produce it, and treat its agreement with your conclusion as no evidence at all.* Two figures in this epic were asserted and never derived — an acceptance-criteria count stated as 38 against an actual **36**, and an iteration-2 total stated as 21 against a composition summing to 22. Both were caught by re-deriving, neither by re-reading. **Every number in the table above is stated with the composition that produces it, so a reader can check it rather than trust it.**

**Nothing was dismissed in any pass from iteration 2 onward.** Two Codex findings were accepted on their DEFECT while their stated REASONING was rejected (P1-1's would-be-circular dependency; P2-3's parallel-write model, which misreads this repo's serial dispatch) — recorded in place, because **accepting a finding on wrong grounds ratifies a bad model alongside a good catch.**

- 2026-07-28: **CODEX SPEC REVIEW (iteration 1) — 5 findings (2 P1, 2 P2, 1 P3), 5 ACCEPTED, 0 dismissed, all 5 incorporated.** Four landed directly; **P2-4 landed after a design divergence adjudicated by the main session** (PM proposed scoping, claude-architect ruled for hardening). Two were accepted on their DEFECT while their stated REASONING was rejected, recorded separately because **accepting a finding on wrong grounds ratifies a bad model alongside a good catch.**
  - **P1-2 is the sharpest, and it indicts this epic's own process.** TN-1 called "story 01 first" a binding design constraint, while the Stories table and **every** Dependencies section recorded `None` — so under the workflow contract the stories were executable in any order and the constraint bound nobody. **Fixed by encoding edges (01→02, 01→03), not by strengthening prose**, because a dispatcher reads the table and not a technical note. Story 01 stays `Blocked by: None`, preserving its deliberate "closes even if everything else slips" property. **The epic had just spent a full iteration cataloguing claims that live in prose and are contradicted elsewhere, and its own sequencing constraint was one of them.**
  - **P1-1 — defect real, diagnosis wrong.** Story 03 AC-1's step (iii) said "run the archive-reference check" while AC-8 forbade story 03 asserting the sweep is live; story 04 writes the invocation. Fixed as **wording**: step (iii) is now a RESERVED SLOT that instructs no one to run anything, plus a requirement that it stay **inert if story 04 never lands** (claude-architect — partial completion is the only state in which anyone meets that text). **Codex proposed an "unstated dependency," which would have been CIRCULAR** — the edge exists and runs 03 → 04.
  - **P2-3 — fix accepted, reasoning rejected.** Both stories left `tests/` to "the implementer selects," which Codex called a parallel-safety gap. **Dispatch here is SERIAL with a per-story staging boundary, so no concurrent-write hazard exists** and Codex's model of the workflow is wrong. Accepted on the project's same-file rule instead: `tests/test_worktree_guard.py` (01) and `tests/test_archive_refs_gate.py` (04) are now named.
  - **P3-5 — accepted narrowly.** Story 04 AC-5 restated rationale already carried in TN-3 and TN-14 — Pattern A again, in an AC. **Only the explanation became a pointer; every requirement and RED state stayed**, on the bound that *an AC which loses its RED to save prose is a worse trade than the drift it prevents.* Cited by HEADING, not line number — line anchors in this epic have rotted three times.
  - **P2-4 — ACCEPTED and LANDED as AC-1b bound 5, after a design divergence adjudicated by the main session.** AC-2 guarantees a prunable registered worktree still forces mode 1; a `BB_WORKTREE_ROOT` pointing at an existing-but-wrong directory made the root-anchored match find nothing and the guard select mode 2 — **so AC-2's guarantee was false exactly there.** It **survived** the validation MUST, because bound 2's precedence covers a *missing* root and this one **exists** — nothing is missing, so nothing fails closed.
    - **PM proposed scoping AC-2; claude-architect ruled for HARDENING and PM concurred.** The landed form: when `BB_WORKTREE_ROOT` is set, additionally match the registry for any `baseball-crawl-E-` worktree irrespective of root, and fail CLOSED on a mismatch — **one extra grep over output already in hand, no extra `git` call, inert when the variable is unset, and no test shape changes.** So **AC-2 keeps its unqualified guarantee.**
    - **The ruling's argument is this session's own evidence turned into a design rule, and it is the most transferable thing in the Codex round: _qualified safety claims lose their qualifiers when relayed._** Three instances in one session (the `.gitignore` range, the "only non-blocking path" sentence, the two-file fixture attribution). **AC-2 is exactly the sentence a future epic quotes to justify not re-checking the guard — and it would be quoted without the scope line.** A safety AC true only under a configuration the reader must remember is a worse artifact than two lines of shell.
    - **Named cost, kept two-sided:** a path-spelling mismatch (trailing slash, symlinked `/tmp`) now **blocks the session loudly** rather than passing quietly. Judged strictly better than a silent disable under this repo's fail-closed doctrine. **PM's scoping lean is recorded as considered, not overlooked** — the fallback condition, had it been taken, was that bound 4 must name AC-2 explicitly as the guarantee that fails.
- 2026-07-28: **ITERATION 2 INCORPORATED — 21 docket items, 0 dismissed.** **The total is stated with its composition so a reader can DERIVE it rather than take it** (this epic produced two count errors that were asserted and never derived — see the Codex entry above and the note at the end of this one): **11** `cr-e279` originals re-verified (CR-1..CR-11) **+ 4** new from the same audit (NEW-1..NEW-4) **+ 5** PM findings (PM-9..PM-13) **+ 1** carried from the handoff seed (claude-architect's F9, landed verbatim) **= 21.** Of these, **CR-8 required no action** (already fixed in iteration 1) and **CR-9(a) was withdrawn by its own author** in favour of PM-9's stronger form; both were still dispositioned. **Nothing was dismissed as cosmetic, out-of-scope, or pre-existing.** The two design-judgment items (CR-1, CR-2) were routed to the design owner rather than resolved by PM, and `ca-e279`'s rulings on them are resolutions of those items, **not additional findings** — counting them again would double-count. A separate post-incorporation **validation pass by `ca-e279` added 2** (see below), giving **23 for iteration 2**.
  - **Design rulings (claude-architect), both confirmed by execution rather than inference.** *Ruling 1* — story 01 AC-1's RED had been gutted: its Given was satisfiable with a stubbed `git` and no directory, which the PRE-FIX guard also allows, so the test passed against unfixed code and AC-1 collapsed onto AC-5. Resolved by an **injectable worktree root** (`BB_WORKTREE_ROOT`) with bounds (four at the time; a fifth was added by Codex P2-4 — see the Codex entry above), because *directory-independence is only observable when a directory exists*. claude-architect owns the defective F6 wording that caused it. *Ruling 2* — the new gate is placed **FIRST** in `.githooks/pre-commit`, not merely above `:26-28`: "unconditional exit" was inaccurate (`:8-11` is an `exit 0` inside an `if`), and a positive first-placement rule needs no enumeration and cannot rot.
  - **AC-quality repairs.** Story 04 AC-8 was **unsatisfiable as written** — it quoted a path that is line-wrapped mid-token, so any repoint reflows the block and collides with the TN-4 ceiling the AC itself invokes; re-wrapping is now explicitly within the ceiling. Story 01 AC-6 **contradicted its own story**, forbidding both the header reconciliation and Ruling 1's bound 4; narrowed to five pinned code behaviors, with a new AC-6b requiring a per-site written verdict across four header sites and three RED forms — the third being a stale operator REMEDY (`:21-22` tells the operator to remove the directory, which now produces AC-2's DENY). Story 03 AC-5 **contradicted AC-1's ordering** and would have let an implementer squeeze out the repair window the epic exists to create.
  - **The written-verdict clause replaces site enumeration in three places** (story 02 AC-3, story 05 AC-4, story 01 AC-6b). Its justification is empirical: PM found a third PM-memory hit (`archived-epics.md:85`) whose correct verdict is "no change needed" and which the old removed-or-annotated binary would have damaged; `cr-e279` had surfaced that line in its own grep and not ruled on it, and withdrew its own enumeration-based fix in favour of this one. **A named-site list is satisfied by fixing the named sites and is silent about the rest; a per-line verdict is immune to an incomplete list.**
  - **Two residual-set corrections, both about self-consistency.** TN-8c and the OQ-2 sub-bullet stated the E-271 residual set at **four and five, eleven lines apart in this file**, against E-271's own six; with two sites `cr-e279` found by independent enumeration the true set is **eight**, now single-sourced in TN-8c. Those two sites say "a bash hook" and carry **none** of the sweep tokens — found by reading, not grepping.
  - **Recurring shape, recorded because it recurred three times in one planning cycle:** a right conclusion resting on a false premise. TN-7 item 1's own correction was itself false; TN-8a's `:630` sequencing premise was false; TN-12 item 1 listed a premise as unverified after it had been verified twice. All were caught by opening the file; none by re-reading the reasoning.
    - **The practical companion, and it is what made the catching possible rather than lucky — third codification candidate** (claude-architect's observation, raised so it would not stay in a message): **state a premise as a SEPARATE, CHECKABLE claim rather than folding it into the conclusion's prose.** The design owner wrote its rulings in the form *"here is the call, and here is the file line it rests on,"* which lets a second party open the reason **without having to re-litigate the call.** That is exactly how the `.gitignore` 56-58 range and the `epics/E-999-demo` two-file attribution were caught — in both, the conclusion was fine and the cited reason was standing on its own where it could be opened. The three-instance pattern above says a false premise under a correct conclusion **cannot** be caught by re-reading; this says what to do about it. A conclusion whose premise is dissolved into its prose is one nobody can check cheaply, because checking it looks like disputing the verdict.
    - ⚠️ **Note where this observation nearly died: in a SendMessage.** It was raised in a thread, about a thread, and would have reached nobody — the "a flag that lives only in a completion message is a handoff to nobody" failure `.claude/rules/context-layer-assessment.md` names. It is here because its author noticed that leaving it there would repeat the defect it describes.
  - **Scope held.** E-271's epic file remains unedited under a re-confirmed declination; the pre-commit rename fail-open found during this round shipped as **standalone operator-ruled commit `9b62395`, outside E-279** (TN-14), and its narrower `:8-11` sibling is filed as **[[IDEA-232]]** rather than bundled. **The epic grew by zero stories and zero files** — every finding landed as a correction inside the existing five-story structure.
  - **Post-incorporation validation (claude-architect): 2 further findings, both landed in story 01. Total for iteration 2 is 23.** *MUST* — AC-1b bound 2 ("a set-but-missing root fails CLOSED") and AC-5 ("registry readable, no worktree listed → mode 2") gave **opposite answers on the same input**, since git lists worktrees regardless of whether the configured root exists; bound 2 is now a configuration precondition evaluated BEFORE the registry read, with AC-3/AC-4/AC-5 not reached. **Its second half is the dangerous one and is now the loudest text in AC-1b:** generalising the bound to the DEFAULT root would deny every main-checkout write in every no-dispatch session, and because `/tmp/.worktrees` exists on the current machine it would likely PASS local testing and fail on a fresh boot or clean container — *a defect whose test environment hides it.* *SHOULD* — AC-1's RED named a revision that cannot exhibit it: the pre-fix hook globs `/tmp/.worktrees` unconditionally and is blind to an injected root, so it ALLOWS against the new test, and an implementer checking it out to demonstrate the failure would misdiagnose the test as broken. Reworded to pin the BEHAVIOUR (detection must not be a directory glob) rather than a historical revision.
  - **The consistency sweeps caught FIVE cascades. They are TWO patterns, not one, and the distinction is the more useful output — merging them costs the sharper one** (claude-architect's formulation, adopted; PM's first write of this entry wrongly called all of them one shape).
    - **Pattern A — a claim corrected where it was FOUND, surviving where it LIVES. Four instances, three authors.** `epic.md:103` kept the false two-file `E-999-demo` attribution after story 04 was corrected; the `ACMR` guidance stayed conditional ("*if* the commit has already landed") in two places after it landed; this very History entry read "21 findings" after validation made it 23; and during planning the `.gitignore` range was relayed from IDEA-230 as 56-58 and corrected only where it surfaced. **A correction naturally lands at the site that surfaced the error, not at the claim's other homes.** Remedy is mechanical: **when you correct a claim, grep for it before closing the correction** — the correction is not done when the sentence is right, it is done when no other copy is wrong.
    - **Pattern B — a retired claim REGENERATED from its source by a downstream summary. One instance, and it is nastier.** `IDEA-231`'s README row carried the unmeasured *"much of this backlog is evidence"* quantifier **after** it had been explicitly retired, with a written *"do not reintroduce a quantifier here"* in two artifacts. **This was not a missed copy.** An index row is written by SUMMARISING the artifact it points at, so it **regenerates** the retired claim from the source rather than inheriting it — which is why the "do not reintroduce" instruction in the source could not reach it, and why grepping the retired tokens at retirement time would not have prevented it either. The row was written later, by someone doing exactly the right thing. It is `doc-sweep.md`'s "retired claims survive in forms carrying none of their tokens" turned inside out: this one survived **with every token**, through regeneration rather than survival. **Remedy differs: a retirement must reach the artifacts that SUMMARISE the source — index rows, README tables, epic summaries — not only the source itself.**
    - Both belong in the closure codification (claude-architect will carry them). Pattern A is also the direct argument for single-sourcing any enumeration, which is why TN-8c's table is now the sole residual list rather than one of three.
  - **A VERIFICATION CLAIM EXPIRES WHEN THE ARTIFACT CHANGES — fourth codification candidate, and it is what caught Pattern A's fourth instance.** The plan skill's consistency-sweep phrase (*"Consistency sweep complete. All values are consistent across all files."*) had been earned by a sweep that **four later edits invalidated** — two validation fixes to story 01, a new idea file, and a README row. Re-sending the phrase would have been asserting a state that had not been checked since it was true. Re-running the sweep against the post-edit state is what surfaced the "21 findings" undercount. **"I already checked" is not a reason to skip a re-run when re-running is cheap**, and a pass that certifies a state and then keeps editing has un-certified it — which describes every consistency sweep, every review scorecard, and every closure gate in this repo. *(Raised by claude-architect against PM's own text, on the same ground PM had raised its third candidate: the line existed only in a message.)*
    - **The instance to lead with, because nobody argues with it and nobody forgets it: a sentence counting the defects, exhibiting the defect it counts.** The History entry said "21 findings" after validation made it 23. **The transferable half is not the recursion — it is that the error was caught by RE-RUNNING a check that had already PASSED**, not by noticing it while writing. A defect inside a sentence you authored is not visible to its author on a re-read (see the three-instance pattern above); it is visible to a re-run.
- 2026-07-28: **⚰ SUPERSEDED — ITERATION-2 SEED, retained as the handoff record.** Written by the outgoing PM (`pm2-e279`) at handoff. Its "fixes" were iteration 2's docket, not decisions taken; that docket is now incorporated per the entry above. Retained rather than deleted so the handoff is legible. Where this entry and the files disagree, **the files win**.
  - **⚠️ MOST IMPORTANT JUDGEMENT CALL FOR ITERATION 2 — CR-1, and the outgoing PM's own fix did not dissolve it and may have worsened it.** Story 01 AC-1 originally required a real directory at `/tmp/.worktrees/baseball-crawl-E-999`, contradicting AC-7's prohibition on tests creating such directories. The iteration-1 fix (F6) dropped the directory precondition and asserted directory-independence. **`cr-e279` had already priced that exact move and found it wanting**, in its own words: *"Alternatively restate AC-1 to drop the directory precondition and accept that its RED is unreachable, but then the story loses its defect-under-repair criterion."* Under the new predicate **AC-1 and AC-5 collapse onto the identical code path**, so a git-stubbed no-directory test verifies AC-5 and certifies nothing about AC-1 — **the epic's headline defect may now have no criterion that can go red anywhere.** CR's alternative was to extend AC-7's carve-out to AC-1 with guaranteed trap-based cleanup. This needs unhurried judgement, not a wording pass.
  - **MOVED-STATE MAP for `cr-e279`'s 11 findings** (derived first-hand from CR's audit text, not from a relay). **Already fixed, do NOT re-fix: CR-8** — story 02 AC-1's absolute grep; this is F1, landed, and CR's suggested wording is what shipped. **Half-addressed, residual live: CR-2** — the `--diff-filter=ACM` rename exclusion is sidestepped because F4 specifies `--no-renames`, but CR's second half is untouched and is a genuine fail-open: `.githooks/pre-commit:26-28` exits 0 when the staged list is empty, so an archive rename as the *only* staged change returns before any gate runs. **CR-9** — PM-5 landed `archived-epics.md:79` and PM-reconciles-at-closure, but CR found a second PM hit at `product-manager/e276-health-gate-triage.md:360`. **CR-10** — E-271's History now lists six residuals but **TN-8c still lists four**, and CR found two sites both lists miss (`E-271 TN-11:112`, `TN-12:119`). **Fully live, untouched: CR-1, CR-3, CR-4, CR-5, CR-6, CR-7, CR-11.**
  - **F9 (claude-architect, MUST FIX before dispatch, NOT yet landed) — a consequence of F4's AND, not a miss in iteration 1.** Story 04 AC-5 binds its own enumeration but never binds the hook's. `.githooks/pre-commit:24` builds `STAGED_ARR` with `--diff-filter=ACM`, which contains **no `D`**, so deletions are structurally invisible to it. Since the AND requires observing a `D` under `epics/E-NNN-`, an implementation reusing `STAGED_ARR` **can never fire the gate at all** — and an inert gate is shape-identical to one correctly declining to fire. The pull is structural: AC-9 instructs the implementer to preserve neighbouring hook machinery, so a ready-made staged-file array sits three lines from their work. **Not landed here deliberately — `cr-e279` is auditing story 04 live, and editing it now would recreate the moved-target problem CR flagged against iteration 1.** Land in iteration 2, verbatim: *"The trigger performs its OWN enumeration and MUST NOT reuse `STAGED_ARR` or any `--diff-filter=ACM` list… **RED state:** a trigger that cannot observe a staged deletion. The AC-5 test asserts the gate FIRES on a delete+add-shaped staging — a test that only asserts non-firing on the three RED-state commits passes identically against an inert gate and certifies nothing."* **One positive case is what makes the negative ones meaningful**, and AC-5's three RED states invite an all-negative suite.
  - **SEQUENCING correction (CR-5), changes cross-epic advice.** TN-8a says line 537 sits above E-271's `:630` anchor "so E-279 landing first shifts it." **The premise is false: `SKILL.md:630` is already a blank line**, as are `:462` and `:634`; four commits touched that file after E-271 was written on 2026-07-21. E-271's anchors are **already stale**, so dispatching E-271 first does **NOT** avoid the collision — TN-8a currently implies it might. The conclusion (E-271 must re-derive all anchors by phrase at dispatch) survives and is strengthened.
  - **TWO CORRECTIONS THE OUTGOING PM OWNS, still open.** (i) **`epic.md:16` compresses the IDEA-230 repro wrongly** — it says "a bash command whose only content was *assigning* a worktree path string," dropping the `git add` half the mechanism actually requires; the hook's Bash branch needs BOTH a worktree path and a `git add` match. Story 01's Context states it correctly ("near `git add`"); the epic summary is the wrong one. CR re-ran the premise and the repro HOLDS with both halves present. (ii) **TN-3's fixture claim is wrong for one of its two files** — `E-999-demo` appears 11 times in `tests/test_doc_pii_hook.py` and **zero** times in `tests/test_pii_scanner.py`; that file instead carries hard-coded `epics/E-129-…` literals at `:536/:539/:564`, and **E-129 is archived**, so those are live dead-path references rather than synthetic fixtures. The one-epic-scoping conclusion survives, arguably strengthened. Those E-129 literals are also a worked example for AC-4's owner routing: they are test *inputs* to a prefix predicate, therefore evidence, and must NOT be repointed.
  - **`epics/E-279-closure-machinery/` is entirely UNTRACKED (`??` in git status).** Any `git diff`-based review is structurally blind to it. Brief reviewers accordingly.
  - **What iteration 1 verified, so iteration 2 need not redo it:** CR gave PASS to 26 of 32 acceptance criteria; the six CONCERNs are its three MUSTs plus CR-7 and CR-8. Dependency reciprocity confirmed in all three places, no circular or one-sided edges. The settings.json enumeration confirmed byte-for-byte (block opens `:8`; four objects at `:12/:18/:24/:30`; send-counter object `:28-33`; SendMessage block `:36-46` sole-occupant). The E-271-03 reconciliation confirmed item-by-item against its recorded bound, ⚰ marker before the quoted text per `doc-sweep.md:45`. The iteration-1 consistency sweep ran and was clean.
- 2026-07-28: **Internal review iteration 1 incorporated — 15 findings landed, 0 dismissed** (claude-architect holistic F1-F8, PM sub-pass B PM-1..PM-8). **⚰ CORRECTED 2026-07-28 (iteration 2): this entry originally read "`cr-e279`'s spec audit did not return and the main session invoked the skill's skip-CR option." That is FALSE and would mislead a future reader into thinking the CR sub-pass was skipped for want of a reviewer.** What actually happened: the spec audit **DID return** — delivered to the main session in full — and the **delivery leg to `pm2-e279` failed**. The skip-CR option was invoked on that basis, so iteration 1 incorporated 15 findings from other sources while CR's 11 sat undelivered; they entered at iteration 2, where 2 were already fixed and 6 were still open. Corrected as EVIDENCE rather than deleted: this is the more useful record, because the session's recurring pathology was **delivery, not absence**, and the same leg failed again on iteration 2 before a direct resend succeeded. Attribution: the misstatement originated in the main session's framing, which is the only party that could observe the audit's arrival, and the correction is recorded on its own report. **Spec-meaning changes:** story 02 AC-1 rescoped to LIVE references with a planning-resolved evidence list, because an unqualified "no file references the script" was **impossible to satisfy correctly** — `SKILL.md:646` carries the reference as evidence anchoring a live rule and must not be edited (F1). Story 03's "sub-step 3a" replaced by an ordered (i)-(v) sequence, since a `3a` label reads as running AFTER sub-step 3 — the inverse of the required order — and "3a" is already taken in that file for the Step 3/3a assessments (F2, PM-8). Story 04's backstop now discriminates on ARCHIVING rather than presence, requiring a delete under `epics/E-NNN-` AND an add under `.project/archive/E-NNN-` for the same epic, enumerated with `--no-renames`; presence alone, and deletion alone, each admit false positives that would block unrelated commits with no override (F4). Story 02 AC-3's "flag, do not edit" clause corrected: it has **no live instance**, because all three agent-memory owners are on the dispatch team, and it would have told claude-architect to flag hits in its own directory to itself (PM-5). **Also landed:** the `git checkout -- .` prohibition and Step 7a preservation pinned as surviving properties of the rollback rewrite (F3); `SKILL.md:477`'s three self-contradictions about the sub-step 3/4 boundary added as a reconciliation criterion (F8, PM-7); story 01 AC-1 restated as directory-independent, resolving its pull against AC-7 (F6); the E-271 path citation replaced by a story-ID reference, since this epic's own file was the only non-archive instance of the defect its gate catches (F5); story 02's "four places" corrected to three (PM-1); AC-1's settings clause made dependent on AC-2 rather than independently satisfiable by deleting the Bash block (PM-2). **No action:** PM-4 (story 05 AC-3's reservation of the replacement judgement to software-engineer is deliberate and claude-architect concurred) and F7 (E-271's now-unachievable Success Criterion is recorded in E-271's own History for its closure to dispose of). **Closure obligation added:** TN-11 item 4, re-scoping IDEA-116.
- 2026-07-28: **`.claude/settings.json` deletion scope corrected from two co-tenants to four, caught by claude-architect.** PM had written that the Bash matcher "shares its matcher with `secret-read-guard.sh`" from a six-line grep window; the block holds **four** hook objects (`pii-check.sh`, `epic-archive-check.sh`, `secret-read-guard.sh`, `send-message-counter.sh`), so only the fourth object at lines 28-33 may be removed. Deleting the block would have unregistered a PII gate, a credential guard and the unarchived-epic gate **with no failing test** — and `epic-archive-check.sh` is the hook E-279-03's design cites as still clearing under the restructured sequence, so removing it would have falsified this epic's own safety premise. Story 02 AC-2 now enumerates the four objects by name and verifies the three survivors by grep, so it cannot be satisfied by deleting a matcher block. Provenance recorded in TN-13 rather than silently fixed: **a partial read can be accurate in everything it shows and false in what it implies** — the six-line window ended on a closing brace, which looks exactly like a boundary without being one.
- 2026-07-28: **Structure corrected to FIVE stories on a design-owner objection, and every story now has exactly one owner.** claude-architect objected that the software-engineer memory reconciliation could not be an AC inside a claude-architect story, citing `.claude/rules/context-layer-assessment.md` (Deletion-Side Eviction → Ownership): only the owning agent edits its own memory directory, and dispatch assigns stories rather than criteria. PM verified the clause on disk and adopted the objection — the work becomes **E-279-05** (software-engineer), **blocked-by E-279-02** so SE reconciles against the deleted state rather than an interim one. The objection's timing is itself a design fact: while OQ-1 was open the fix was a numbers correction, and the DELETE ruling turned it into a judgement about what survives in SE's memory, which is the reserved class. PM separately withdrew the E-271-03 reconciliation from story 02 and **executed it at planning time** (planning-artifact work is PM's planning function, and E-271 is dispatchable today while E-279 is not), leaving story 02 single-owner. Folded claude-architect's three DELETE consequences, each re-verified first-hand: the four-place deletion scope (new TN-13, including a corrected `.gitignore` range — 55-58, not the cited 56-58, since line 55 names the hook by path), `.git/info/exclude:19` left alone as a recorded decision (TN-11 item 3), and a combined story-1+2 success criterion that credits neither story alone. Corrected a surviving instance of the TN-6 unmeasured quantifier in story 04's Notes, which the earlier correction had reached in `epic.md` and IDEA-231 but not there.
- 2026-07-28: **Both open questions RESOLVED; no blocker remains to READY beyond spec review.** OQ-1 — operator ruled **DELETE** at an interactive gate (verbatim option "Delete entirely"); story 02 Branch A is live, Branch B retired in place as considered-and-declined. OQ-2 — main session **AUTHORIZED** editing E-271-03, reversing its own prior do-not-modify-E-271 instruction on the prohibited-AC argument; risk accepted by the main session and disclosed to the operator; the edit is bounded to AC reconciliation (not an E-271 re-scope). ⚰ **The two placement claims in this entry — "the edit is story 02 AC-5" and "story 02 consequently has three owners" — are SUPERSEDED by the entry above, same day.** Both were withdrawn: story 02 ends with one owner, and the E-271 edit was executed at planning time. Left visible rather than rewritten so the reversal is legible.
- 2026-07-28: PM corrected a claim it had inherited and restated unchecked — "much of the backlog is legitimately evidence" (TN-6, and the same sentence in IDEA-231) was claude-architect's unmeasured inference, not a finding. Both files now record that the scope decision is independent of the proportion at any value. Added TN-12 (premise confidence) so review targets the three unverified premises rather than sampling evenly.
- 2026-07-28: Created (DRAFT). Promoted from IDEA-228 + IDEA-230, both evidence-seeded. Design owner claude-architect; PM framed ACs, owns scope and sequencing. Four stories in a binding order (TN-1). Two claude-architect premises corrected during planning (TN-7); Residual 2 discharged by measurement, not assumption. PM scope decision on TN-6: one `src/` line in, 24 dead references out to an idea. Two open questions routed to the operator (OQ-1 blocking READY, OQ-2 on touching a READY epic).
