# E-072-01: Create ingest-session Skill File

## Epic
[E-072: Proxy Session Ingestion Skill](epic.md)

## Status
`TODO`

## Description
After this story is complete, a new skill file exists at `.claude/skills/ingest-session/SKILL.md` that defines the full workflow for processing a mitmproxy proxy session -- from reading session data, through endpoint comparison, to guiding the operator through selective raw data capture. The skill is self-contained and follows the established skill file format used by `ingest-endpoint`, `implement`, and other existing skills.

## Context
The operator captures GameChanger traffic through mitmproxy sessions. Today, there is no automated way to compare a session's captured endpoints against the 119 documented endpoints and identify what is new. This skill bridges that gap: it reads proxy session data, normalizes endpoint paths, compares against `docs/api/endpoints/`, and presents unknown endpoints to the operator for selective investigation.

The skill is the primary deliverable of E-072. It builds on extensive existing infrastructure: proxy session files (`proxy/data/sessions/*/`), the `bb proxy` CLI commands, the HTTP layer's dual-profile support, and the `ingest-endpoint` skill for single-endpoint follow-up.

## Acceptance Criteria
- [ ] **AC-1**: Given the file `.claude/skills/ingest-session/SKILL.md` does not exist, when this story is complete, then the file exists with a complete skill definition following the established format (Activation Triggers, Purpose, Prerequisites, Phases, Security Invariants, Anti-Patterns, Edge Cases sections).
- [ ] **AC-2**: Given the skill's Activation Triggers section, when this story is complete, then the Activation Triggers section explicitly lists these exact trigger phrases: "ingest session", "process proxy session", and "what's new in the proxy session". No open-ended fallback (no "or similar").
- [ ] **AC-3**: Given the skill's Phase 1 (Session Discovery), when the executing agent reads the skill, then it knows to: (a) locate the latest proxy session via the `proxy/data/current` symlink or accept an operator-specified session ID; if the `proxy/data/current` symlink is absent, list available sessions under `proxy/data/sessions/` and let the operator choose rather than failing silently, (b) read `session.json` for profile auto-detection (the `profile` field is authoritative; the per-entry `source` field may show "web" or "ios" and is informational only -- NOT used for profile determination), (c) check session status -- if `status == "active"`, present a warning about partial data risk and let the operator decide whether to proceed (NOT a hard refusal), (d) detect the endpoint-log format version by checking for the presence of `response_body` key in the first JSONL entry (legacy format: timestamp/method/host/path/query_keys/request_content_type/response_content_type/status_code/source; richer format adds: query_params/request_headers/response_headers/request_body/response_body), (e) read `endpoint-log.jsonl` directly (typically a few hundred lines per session) and filter to API-host requests only (host = `api.team-manager.gc.com`), (f) exclude OPTIONS preflight requests but retain ALL status codes (do not filter by status code -- classify instead), (g) if the endpoint-log is empty or contains zero API-host entries after OPTIONS filtering, present "No GameChanger API endpoints found in this session" and offer to skip to Phase 5 (session marking) or end the workflow.
- [ ] **AC-4**: Given the skill's Phase 2 (Endpoint Comparison), when the executing agent reads the skill, then it knows to: (a) list all files in `docs/api/endpoints/` to build a known-endpoints set, excluding files with `status: NOT_API` in frontmatter (e.g., `web-routes-not-api.md`), (b) normalize captured endpoint paths using concrete rules: UUID segments matching 8-4-4-4-12 hex pattern become parameter placeholders (e.g., `{team_id}`, `{game_stream_id}`); short alphanumeric slugs in `/public/teams/` or `/teams/public/` paths become `{public_id}`; multi-parameter paths normalize ALL parameter positions (e.g., `/teams/{team_id}/opponent/{opponent_id}`), (c) for truly unknown paths where named placeholders do not match any existing doc filename, fall back to `{id}` as the generic placeholder for all UUID-shaped segments and mark the candidate as "placeholder names uncertain -- requires human review" in the presentation, (d) deduplicate normalized paths before comparison (50+ raw paths may collapse to one template), (e) resolve ambiguous normalizations by matching against existing doc filenames, (f) convert normalized paths to the doc file naming convention (`{method}-{path-segments}.md`), (g) classify each endpoint as: unknown (no doc file), enrichable (doc exists but session shows query params not present in the doc's `query_params` frontmatter list -- status-code comparison is NOT used because the doc schema has no structured status-code inventory), cross-profile enrichment (doc exists but captured from different profile -- check `profiles.{profile}.status` in frontmatter), or known (fully documented), (h) classify by status code for priority signaling only: 2xx = confirmed/capture-worthy, 304 = cached/known (no response body), 401/403 = exists but needs auth, 404 = dead, 5xx = server error.
- [ ] **AC-5**: Given the skill's Phase 3 (Presentation), when the executing agent reads the skill, then it knows to present using the structured format from the epic's Phase 3 Format Spec: (a) a SESSION SUMMARY header with profile, session ID, and raw→normalized endpoint counts, (b) categorized sections (UNKNOWN, ENRICHABLE, CROSS-PROFILE, KNOWN) with sequentially numbered items across all categories, (c) inline priority signals per item in the UNKNOWN, ENRICHABLE, and CROSS-PROFILE categories only (KNOWN endpoints have no signals); priority rules are mutually exclusive, applied in order: HIGH = status 2xx AND path contains a coaching keyword (coaching keywords: lineup, schedule, stats, pitching, batting, spray, opponent, scout, season, roster, players, boxscore); MED = status 2xx but no coaching keyword, OR any non-2xx unknown endpoint, OR any enrichable/cross-profile endpoint; LOW = status 304, OR status 4xx/5xx on an unknown endpoint, (d) KNOWN endpoints collapsed to a count only (full list available on request), (e) the session profile (web/mobile) and its implications for header selection, (f) after presenting, the decision protocol: ask the operator which endpoints to capture with options for numbers/ranges (`1`, `1 3 5`, `1-4`, `all`), category shorthand (`all unknowns`, `all high`, `all 2xx unknowns`), or `skip`/`none` to go directly to session marking.
- [ ] **AC-6**: Given the skill's Phase 4 (Selective Capture), when the executing agent reads the skill, then it knows to: (a) check credential freshness (profile-dependent): for **web profile**, run `bb creds check --profile web` -- if API health shows `[XX]`, run `bb creds refresh --profile web` first; for **mobile profile**, read `GAMECHANGER_ACCESS_TOKEN_MOBILE` from `.env` and decode the JWT (split on `.`, take second segment, pad with `=` for valid base64, URL-safe base64 decode, JSON parse, read `exp`, compare against current time; use `python3 -c` via Bash tool) -- if expired, stop and tell the operator immediately, (b) **data-first**: for each chosen endpoint, check if the session's endpoint-log has a non-empty `response_body` for that path (richer format only); selection rule: among all JSONL entries matching the normalized template, prefer the largest non-empty 2xx `response_body` by byte length; ties broken by earliest timestamp; if no 2xx with non-empty body, skip data-first for this endpoint; if a qualifying entry exists, save the body to `data/raw/` with a filename reflecting the normalized endpoint path (e.g., `schedule-sample.json`; overwrite if exists) with PII redaction per `/.claude/rules/api-docs.md` -- save ONLY `response_body`, never `request_headers` (contains live credentials), (c) **curl fallback** (when no response data available) is profile-dependent: for **web profile**, route to `ingest-endpoint` per endpoint (operator captures individual curls from browser) -- quick-capture batch mode is NOT available for web because web access tokens are obtained programmatically by TokenManager and never persisted to `.env`; generating curl templates would require displaying the live gc-token value, violating the "never display gc-token" security invariant (this is a security policy constraint, not a temporary implementation gap); for **mobile profile**, single endpoints reference `ingest-endpoint`, multiple endpoints offer the quick-capture sub-mode with shell-variable-based curl templates using `$GAMECHANGER_ACCESS_TOKEN_MOBILE` and `$GAMECHANGER_DEVICE_ID_MOBILE` (never literal credential values), (d) before listing mobile curl templates, instruct the operator to export credentials from `.env`, (e) process endpoints serially -- if a 401 or auth error comes back, stop immediately and report rather than continuing, (f) for 304 responses, note that a fresh sample requires cache-busting curl headers (omit `If-None-Match`, `If-Modified-Since`), (g) note which header profile to use based on the session's profile, (h) **post-capture action** (applies to both data-first saves and curl responses): after each endpoint's response is saved to `data/raw/`, document it in `docs/api/endpoints/` following the `ingest-endpoint` skill's documentation procedure (create or update endpoint file, validate frontmatter, update README index if new).
- [ ] **AC-7**: Given the skill's Phase 5 (Session Marking), when the executing agent reads the skill, then it knows to offer marking the session as reviewed via `bb proxy review mark <session-id>` -- including when the operator chose "skip" in the Phase 3→4 decision protocol or when the session had no API endpoints.
- [ ] **AC-8**: Given the skill's Security Invariants section, when the executing agent reads the skill, then it finds: (a) the same credential safety rules as `ingest-endpoint` (never display gc-token/gc-device-id values, raw responses to `data/raw/`, placeholder values in docs), (b) when saving from richer-format endpoint-log, save ONLY `response_body` -- never save or display `request_headers` (contains live gc-token values), (c) quick-capture curl templates (mobile profile only) use shell variable references (`$GAMECHANGER_ACCESS_TOKEN_MOBILE`, `$GAMECHANGER_DEVICE_ID_MOBILE`) -- never embed literal credential values, (d) quick-capture batch mode is NOT available for web profile because the access token is not persisted in `.env` -- generating curl templates would require displaying the live token.
- [ ] **AC-9**: Given the skill's Anti-Patterns section, when the executing agent reads the skill, then it includes at minimum: (a) do not auto-fetch all unknown endpoints without operator selection, (b) do not modify proxy session files, (c) do not skip the endpoint comparison phase, (d) do not attempt to run proxy lifecycle commands from the devcontainer, (e) do not fetch multiple endpoints in parallel -- always serial with stop-on-auth-failure, (f) do not treat `bb proxy endpoints` output as normalized data -- always read JSONL directly and normalize in-agent, (g) do not save or display `request_headers` from the richer endpoint-log format (contains live credentials).

## Technical Approach
The skill file follows the established format from existing skills at `.claude/skills/*/SKILL.md`. Key reference files for the implementer:

- **Existing skill to model format after**: `.claude/skills/ingest-endpoint/SKILL.md` -- same domain (API endpoint ingestion), two-phase workflow pattern
- **Proxy session data format**: `proxy/data/sessions/*/session.json` (JSON with session_id, profile, status, reviewed, endpoint_count fields), `proxy/data/sessions/*/endpoint-log.jsonl` (two format versions -- see epic Technical Notes "Endpoint-Log Format" for complete field lists)
- **Endpoint doc naming convention**: `docs/api/README.md` (index), `.claude/rules/api-docs.md` (file naming spec and frontmatter schema)
- **BB CLI proxy commands**: `src/cli/proxy.py` (endpoints, report, review, refresh-headers subcommands)
- **HTTP profiles**: `src/http/headers.py` (BROWSER_HEADERS for web, MOBILE_HEADERS for mobile)
- **Proxy boundary rule**: `.claude/rules/proxy-boundary.md` (what agents can and cannot do with proxy)

The implementer should read the existing `ingest-endpoint` skill in full to understand the format conventions (section ordering, instruction block formatting, security invariant placement). The new skill is complementary: `ingest-session` processes a whole session and identifies unknowns; `ingest-endpoint` handles individual endpoint deep-dives.

Path normalization is the key technical challenge. The endpoint-log contains concrete paths with real UUIDs and public_ids (e.g., `/teams/aabb0011-ccdd-4eef-8899-ffeeddccbbaa/schedule`, `/public/teams/a1GFM9Ku0BbF/games`). These must be mapped to the parameter-templated paths used in doc filenames (e.g., `/teams/{team_id}/schedule`). Concrete normalization rules are defined in the epic Technical Notes: UUID regex (8-4-4-4-12 hex), public_id slug patterns, multi-parameter path handling. After normalization, paths must be deduplicated before comparison -- a session may have 50+ raw paths that collapse to a single template. The skill should document these rules clearly so the executing agent can apply them.

**Important**: The `bb proxy endpoints` command outputs raw, unnormalized paths (literal UUIDs). The skill must explicitly instruct the executing agent to read the `endpoint-log.jsonl` JSONL directly and perform its own normalization -- not use `bb proxy endpoints` output as the normalized set.

## Dependencies
- **Blocked by**: None
- **Blocks**: E-072-02 (context layer registration depends on skill file existing)

## Files to Create or Modify
- `.claude/skills/ingest-session/SKILL.md` (create)

## Agent Hint
claude-architect

## Handoff Context
- **Produces for E-072-02**: The skill file at `.claude/skills/ingest-session/SKILL.md` -- E-072-02 needs the activation trigger phrases from this file to write the CLAUDE.md Workflows entry.

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Skill file follows established format conventions
- [ ] No accidental modifications to files outside the skill directory (this is a context-layer-only story with no application code changes)

## Notes
- The endpoint-log JSONL is typically a few hundred lines per session (363-429 entries in observed sessions). A full file read is appropriate -- no need for streaming or chunked reads.
- The `proxy/data/current` symlink points to the active session directory. If the session is still active (status = "active" in session.json), the skill should present a warning about partial data and let the operator decide -- NOT hard-refuse (a crashed proxy may leave a permanently "active" session). If the symlink is absent, list available sessions and let the operator choose.
- `bb proxy endpoints` output is unnormalized (raw UUID paths). The executing agent must read the JSONL and normalize itself.
- The `source` field in endpoint-log entries may show "web" or "ios" (NOT "mobile"). This is informational only -- profile detection uses `session.json`'s `profile` field exclusively.
- Richer-format sessions (~2026-03-12+) contain `response_body` for ~45% of entries. This enables direct data extraction without curl recapture for many endpoints. The `request_headers` field in these sessions contains live credentials and must NEVER be saved or displayed.
