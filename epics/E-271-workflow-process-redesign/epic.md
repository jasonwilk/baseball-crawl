# E-271: Workflow / Process Redesign (E-267 process-audit remediation)

## Status
`READY`
<!-- READY set 2026-07-21 (freshness clock starts). Re-confirm against docs/ROADMAP.md if still READY on 2026-09-19 (60-day gate). -->
<!-- Dispatch requires separate operator authorization (Dispatch Authorization Gate). -->
<!-- CLOSURE OBLIGATIONS: (1) TN-13 — reconcile the Step-1c external-reference drift (codex-review/SKILL.md:153 + two agent-memory files) via the trigger-4 context-layer assessment before archive; (2) the Closure Evidence discipline (item 5) applies to E-271's OWN closure — report the verbatim measured context-ratchet.sh figure (operator pre-authorized the trigger-7 exception at ~+35-55L). -->
<!-- Lifecycle transition: DRAFT to READY on 2026-07-21. -->

## Overview
The E-267 independent audit's process verdict: the epic failed not for lack of checks but from too many SAME-CONTEXT checks manufacturing false confidence — 16+ internal review rounds (12 story-rounds + 4 Codex spec + 1 Codex code) all missed a live irreversible-data-loss defect, while the one INDEPENDENT no-continuity review caught it in a single pass. This epic is a NET-DELETION-plus-targeted-additions redesign of the process layer: drop/soften the machinery that manufactures false confidence (a hard send-cap, a redundant Codex round, an orchestrator told to adjudicate code it cannot read), and add the one thing that actually worked — an independent no-continuity review for destructive-path epics — plus a decide-first Autonomy Gate, a mechanical annotation-as-coverage check, and a verbatim Closure Evidence schema. Every change cites its defect (P-1..P-10 in `.project/research/E-271-e267-audit-findings.md`); the meta-layer is otherwise FROZEN.

## Background & Context
The consolidated audit findings (`.project/research/E-271-e267-audit-findings.md`) record ten process findings (P-1..P-10). The headline (P-1): shared context ⇒ shared blind spot — every internal review layer inherited the ACs and the stories' "unreachable in production" annotations, so the same false premise rode through all of them; the Fable audit, given the goal but not the rationale, falsified it. Critically, the sharpening round established that NO review layer was pure ceremony (Codex spec rounds caught genuine pre-code issues; story-04's circuit breaker caught a defect in the reviewer's own proposed fix) — the failure was false-confidence ACCUMULATION, not zero value. So this redesign targets redundancy and continuity-reliance, not review value: it caps a round, it does not eliminate review; it REPLACES (not supplements) the same-context closure review for destructive epics. claude-architect is the design owner (context-layer domain); this epic implements its design (below), with PM framing ACs and owning scope/sequencing.

## Goals
- Remove the false-confidence machinery the audit named: the hard SendMessage DENY cap (P-6), the second automatic Codex spec-review round (P-1/P-8), and the orchestrator's structurally-impossible finding-validity adjudication (P-7).
- Add the independence that worked: an independent, no-continuity, adversarial destructive-path review that REPLACES the same-context closure integration review for `Destructive-Path: yes` epics (P-1).
- Add a decide-first Autonomy Gate so the main session decides from permitted artifacts instead of stalling or over-escalating (P-7/P-9/P-10).
- Make two prose-only disciplines mechanical: annotation-as-coverage detection on destructive-path diffs (P-2) and safety-signal fail-open detection on the changed module graph (P-3).
- Make closure evidence verbatim and un-fabricable: a required Closure Evidence block (pytest summary + command, smoke scope incl. what it did NOT exercise, ratchet exit code + per-subtree deltas from the canonical script) (P-4/P-5).
- Keep the net context-layer change at or near zero (net-deletion is a design goal); price any residual via the trigger-7 ratchet.

## Non-Goals
- **No changes to E-270's scope** (its stories, ACs, or the `MAX_GAME_RETIREMENTS = 2` decision).
- **No agent-definition rewrites beyond what these stories require**, and **no new agents**. (Item 7's `code-reviewer-independent` is a freshly-spawned INSTANCE of the existing code-reviewer with a withheld-rationale assignment — NOT a new agent definition.)
- **No ratchet-surface widening** (the ratchet counts four subtrees; widening it to price CLAUDE.md or the epic corpus is out). Record only the "unpriced floor" note per TN-10.
- **No Step-8 closure machinery changes** and **no re-codifying already-codified footguns**.
- **No standalone mechanical fail-open detector** (item 8) — excluded by design; see TN-9.
- **Meta-freeze**: any process-layer change NOT citing a P-finding is out of scope. The one considered exception (a net-deletion offset consolidating a 3× restatement) is DECLINED in-scope and left as an operator decision point (TN-10, Open Questions).

## Success Criteria
- The SendMessage hook no longer denies (warn-only); a completion report can never be blocked by the counter.
- Task state (not message arrival) is the completion source of truth: implementers `TaskUpdate` before reporting; the orchestrator confirms via task-state poll.
- `workflow-discipline.md` carries an Autonomy Gate with an enumerated MUST-decide list, a decide-first test, and the named escalation round-trip cost, scoped explicitly as complementary to the Dispatch Failure Protocol.
- The code-reviewer adjudicates EXTERNAL (Codex) findings valid/invalid with a code-grounded reason (per-story CR findings are valid by construction → routed, not re-adjudicated, per TN-4); the main session only ROUTES (no code-reading adjudication).
- The code-reviewer's rubric mechanically greps destructive-path diffs for annotation-as-coverage tokens and for default-valued safety signals.
- The epic template carries a required Closure Evidence block and a `Destructive-Path` Technical-Notes field.
- The implement skill's Step 1c runs a two-mode closure review: `Destructive-Path: yes` → a freshly-spawned independent no-continuity reviewer with withheld rationale + adversarial charge (replacing the dispatch reviewer for that pass); `no` → today's integration review.
- The plan skill defaults to ONE Codex spec-review round (operator may opt into more; no automatic second iteration).
- Net context-layer line delta is recorded at closure via the canonical `context-ratchet.sh` (offset or operator-signed exception per TN-10).

## Stories
| ID | Title | Status | Dependencies | Assignee |
|----|-------|--------|-------------|----------|
| E-271-01 | implement-skill hardening (task-state completion, finding-routing, closure-evidence ref, independent-review mode) | TODO | None | - |
| E-271-02 | code-reviewer hardening (finding classification, annotation + fail-open mechanical checks, independent-review mode) | TODO | E-271-01 | - |
| E-271-03 | disjoint-file cluster (send-cap warn-only, Autonomy Gate, template Closure-Evidence + Destructive-Path field, Codex 1-round default) | TODO | None | - |

## Dispatch Team
- claude-architect

## Technical Notes
Design authored by claude-architect (design owner); PM frames ACs. Every item carries its P-finding citation (meta-freeze compliance). Source: `.project/research/E-271-e267-audit-findings.md`.

### TN-1: Story structure — cluster by FILE OWNERSHIP
Stories are clustered so each context-layer FILE is touched by exactly one story — the clean way to honor "same-file stories need dependency ordering" (`.claude/rules/project-management.md`): don't split one file across stories at all. E-271-01 owns `.claude/skills/implement/SKILL.md`; E-271-02 owns `.claude/agents/code-reviewer.md`; E-271-03 owns four disjoint files (`.claude/hooks/send-message-counter.sh`, `.claude/rules/workflow-discipline.md`, `.project/templates/epic-template.md`, `.claude/skills/plan/SKILL.md`). The ONLY inter-story edge is **E-271-02 blocked-by E-271-01**, because items 3 and 7 have a skill-side half (01) and a CR-side half (02) that must agree — 01 finalizes the orchestration the CR side reconciles to. Rejected: a mega implement-skill story (doesn't remove the collision, items 4/7 still need the CR file) and per-item stories all editing SKILL.md (a 5-deep dependency chain with reconciliation churn at every staging boundary).

### TN-2: Item 1 — send-cap warn-only + task-state as completion source of truth (P-6)
- **1a (E-271-03, `send-message-counter.sh`):** convert to warn-only by SWEEPING every DENY reference in the file (not a fixed line list — deletion-side reference sweep, E-271-03 AC-7): the DENY branch (`if NEW >= DENY_AT` block, ~:66-85), the `DENY_AT=25` constant (~:31), the THRESHOLD PROVENANCE comment that documents `DENY_AT` (~:21-24, CR M-1), the fail-open comment describing the denial JSON path (~:28, CR M-1), the header comment (~:11-12, "warn at 15, deny at 25"), and the warn message text (~:91, drop "of $DENY_AT"). KEEP `WARN_AT=15` and the advisory warn (~:89-99), plus the Bash staging-boundary logging and counter reset. `settings.json` needs NO change (the hook stays registered; only internal logic changes). A completion report can never be blocked, and no comment documents a deleted constant or a nonexistent denial path.
- **1b/1c (E-271-01, `implement/SKILL.md`):** make TASK STATE the completion source of truth. In the Phase 3 completion instruction (~Step 4, :234), the implementer runs `TaskUpdate` to mark its story task done BEFORE sending the completion report. In Step 5 (~:243) the orchestrator confirms completion by polling task state (`TaskList`/`TaskGet`), not by inferring from message arrival. Single-sourced in the spawn/completion template — no per-agent-definition edits. Coherence with 1a: once completion is task-state-driven, message volume is advisory-only, so the hard cap is unnecessary and the warn remains a pure visibility signal. Defect: P-6 (the DENY cap hard-blocked SE/PM completion reports — working agents looked idle; denial visible to sender only) + the message-inference stall.
**Runaway-ceiling residual (claude-architect F3, eyes-open — operator confirm at READY).** Warn-only removes the HARD runaway ceiling the DENY cap provided (E-256's 640k-char blowup was the original motivation for the cap). This is deliberate — P-6 shows the hard cap false-blocked legitimate completion reports — but the residual is recorded so it is an eyes-open decision, not a silent loss: with warn-only there is no hard runaway backstop; the remaining mitigations are (a) the warn-at-15 visibility signal, (b) task-state-driven completion (1b/1c) reducing message reliance, (c) operator session oversight, and (d) the context window as a natural ceiling. The operator should confirm accepting the loss of the hard ceiling in exchange for eliminating the false blocks (see Open Questions).

### TN-3: Item 2 — Autonomy Gate (E-271-03, `workflow-discipline.md`) (P-7/P-9/P-10)
A new subsection enumerating what the main session MUST decide itself from permitted artifacts (routing per `agent-routing.md`; story sequencing / next-eligible; whether a declared trigger fired from a permitted-artifact FIELD read — the `Destructive-Path` token, Dispatch Team, Agent Hint, ACs, the routing table; staging-boundary mechanics), plus the **decide-first test**:

**Permitted-artifact boundary IN the gate (binding, Codex P1-1 — the gate must not contain the contradiction the epic deletes).** The decide-first list contains ONLY decisions resolvable from permitted-artifact FIELD reads (planning/epic/story files, per `dispatch-pattern.md`'s "permitted orchestration: reading epic/story files for routing"). Anything requiring DIFF / grep / CODE inspection is NOT a gate decision — it is routed to the code-reviewer (where item-7a's destructive-diff backstop grep already lives). So "read the declared `Destructive-Path` field" is a main-session decision; "read the diff to see whether a trigger fired" is NOT. This is the SAME line item 7 draws (main session reads the declared field; CR greps the diff) — the two read as one consistent rule, and the gate cannot recreate the orchestrator-adjudicates-code-it-cannot-read contradiction item 3 exists to delete. (Out-of-scope note: a pre-existing Step-1a/Step-1d tension over diff-stat reads is NOT introduced or inherited here — the gate takes the conservative side (diff reads out) and must not lean on Step 1a's permissive reading; not P-cited, left alone under the meta-freeze.) *"name the permitted artifact you consulted and why it was insufficient; if none, consulting it IS the next action, not an escalation"*, plus the named escalation round-trip cost. **Boundary (binding, real tension):** this MUST NOT contradict the existing **Dispatch Failure Protocol** in the same file ("never improvise, escalate"). Scope them explicitly as complementary: the Failure Protocol governs genuine FAILURES (tool unavailable, spawn fails, no eligible stories); the Autonomy Gate governs DECISIONS the main session is equipped to make from permitted artifacts. State the boundary in the new subsection so they read as complementary, not opposed. Defect: P-9 (the live ~16-min Phase 1→2 mutual-wait stall) + P-7 (every circuit breaker ends "escalate to user" with no decide-first test; escalation cost named nowhere) + the DECISION-RULE half of P-10 (the over-escalation behavior gets a decision rule). **Scope note (CR S-2):** P-10 has TWO halves — the over-escalation decision-rule half (addressed by this gate) and the "main session has no durable record surface" half (the over-escalation incident exists only in the operator account, leaving no repo trace). This gate does NOT create a record surface, so the record-surface half is explicitly OUT of scope and captured as an idea — the P-10 citation must not be read as full closure.

### TN-4: Item 3 — finding-validity adjudication relocates orchestrator → code-reviewer (P-7)
The contradiction (verified): `implement/SKILL.md` tells the main session to classify finding validity, but `dispatch-pattern.md` bars it from reading source to verify claims — it is told to adjudicate code it structurally cannot read.

**Two sharp semantic points (claude-architect, GAP-A) that scope this precisely:**
- (a) The CR's OWN per-story findings are VALID BY CONSTRUCTION — the CR read the code and found them — so per-story handling is "ROUTE what the CR emits," not "re-adjudicate." The validity ADJUDICATION that genuinely needs a code-reader (and thus must NOT sit with the main session) is of **EXTERNAL findings** — Codex spec/code-review findings, which CAN be false positives about the code. Item 3 relocates the adjudication of EXTERNAL findings to the CR; per-story CR findings are simply routed.
- (b) EXPLICITLY OUT OF SCOPE: the **plan-skill** Phase 4 Step 3 PM-triage of SPEC-review findings. PM adjudicating specs is NOT the defect (PM can read specs). An implementer must NOT reach into `plan/SKILL.md` for item 3 — `plan/SKILL.md` is edited by E-271-03 for item 6 ONLY.

**Scope completeness (GAP-A — the defect appears in TWO places + several pointers, all in `implement/SKILL.md`, all in E-271-01):**
- Phase 3 Step 5 item 3 (~:278-284) — the DEFINITION.
- Phase 4 Step 3 item 2 (~:363) — the Codex CODE-review triage ("classify each finding valid/invalid using the same triage rules") — the same defect, must be rewritten too.
- Step 1a (~:406), 1b (~:630), 1c (~:448), 1d (~:501) — each POINTS at "triage per Phase 3 Step 5 item 3"; they must stay coherent with the relocated adjudicator.

**E-271-01 (skill):** rewrite the :278-284 DEFINITION so the CR is the adjudicator of external-finding validity and the main session only ROUTES (valid → implementer; invalid → dismiss with the CR's code-grounded reason) — a permitted-artifact action, no code-reading. The :278-284 rewrite MUST remain the SINGLE shared definition covering BOTH per-story-routing AND external-adjudication-routing — it must NOT collapse to pure per-story routing, or the passes pointing at it dangle (CR S-1). Keep every referencing pass coherent with the relocated adjudicator; because they reference it via the phrase "same triage rules as Phase 3 Step 5 item 3", the implementer MUST GREP that phrase — the referencing-line list below (Phase 4 Step 3, Step 1a/1b/1c/1d) is ILLUSTRATIVE and omits sites (CR found ~:460/:634). Preserve the "every finding reaches FIXED or DISMISSED; no deferral path" invariant.

**E-271-02 (CR):** the code-reviewer emits its adjudication of each (external) finding in the Structured Findings Format. **Emitted-format pin (pin-harder 1, resolves the 01↔02 drift Q2 asked about):** each adjudicated finding carries an explicit inline `[valid]` / `[invalid]` tag plus the one-line code-grounded reason, in the existing Structured Findings Format (NOT a separate subsection). The router (01) consumes exactly that token; producer and consumer therefore cannot drift. `dispatch-pattern.md` needs NO edit — its principle already supports this; the contradictory instruction lives only in the skill.

### TN-5: Item 4 — annotation-as-coverage MECHANICAL check (E-271-02, `code-reviewer.md`) (P-2)
Sharpen the EXISTING judgment-based Priority-3 item ("Annotations are defect markers", ~:149) into a mechanical step: grep the review diff for annotation tokens (`unreachable`, `cannot happen`/`can't happen`, `by construction`/`by-construction`, `impossible here`, `safe here`) on destructive-path lines (DELETE / retire / purge / drop context); each hit REQUIRES a discriminating test OR a PM-recorded exception, else it is a MUST FIX. **PM-recorded exception artifact (Codex P1-2 + claude-architect — single-source it, don't dangle):** the exception is recorded in a DISTINCT "Annotation Exceptions" History sub-block (NOT a Review-Scorecard row — the scorecard is per-PASS aggregate COUNTS, whereas an annotation exception is a per-FINDING JUSTIFICATION of different grain: annotation location + why a discriminating test is genuinely infeasible + PM sign-off). It must be CONSPICUOUS so a reviewer scanning History SEES a destructive-path annotation accepted without a test; these are RARE (default is "write the test"), which argues for a named list over a buried row. The sub-block FORMAT is defined once in the epic template (E-271-03, alongside the Closure Evidence block — the same "schema lives in the template" pattern as item 5); TN-5's rule and E-271-02 AC-2 reference it, never a bare comment. A rubric addition (~few lines), not a new file; the diff grep is a reviewer action (permitted). No skill edit. Defect: P-2 (the worst defect was one region re-annotated across 3 stories; CR's own pre-dispatch audit flagged the exact region then accepted a comment as resolving it; the rule exists in prose in `testing.md` with no mechanical check).

### TN-6: Item 5 — Closure Evidence block (P-4/P-5)
- **E-271-03 (`epic-template.md`):** add a required **Closure Evidence** block to the History schema (single source): the verbatim pytest summary line + the exact command that produced it; the smoke scope INCLUDING what it did NOT exercise; the ratchet exit code + per-subtree deltas taken from the canonical `context-ratchet.sh` run (NO hand-derived figures). Extends the existing Review Scorecard convention.
- **E-271-01 (skill):** a ONE-LINE reference in the Step 2 scorecard area (~:530): "PM records the Closure Evidence block per the epic template." Reference, do not duplicate. Defect: P-4 (closure evidence lived only in commit-message prose, zero in the epic History; smoke never exercised a genuine retire) + P-5 (five successive wrong hand-derived ratchet figures; no canonical measure-command wired into closure).

### TN-7: Item 6 — cap Codex spec review at ONE round default (E-271-03, `plan/SKILL.md`) (P-1/P-8)
In plan Phase 4 (~:353-462), RESTRUCTURE Step 7 (pin-harder 4 — it is a restructure, not only a `>=2` deletion): remove the `codex_review_iteration >= 2` circuit-breaker branch (~:449-460), the increment/reset logic (~:446, :459), and the iteration tracking (~:357) — AND rewrite the surviving `<2` branch so that after ONE round, advance-to-READY is the DEFAULT path and re-run is an explicit operator OPT-IN (not the current co-equal "offer re-run vs proceed"). Deleting only the `>=2` block while leaving `<2` presenting re-run as co-equal does NOT achieve "one round by default." An operator may still opt into another round explicitly — a user-choice, NOT an automatic loop. The restructure SWEEPS every description of the automatic second iteration across the whole file (E-271-03 AC-7), not only Step 7: the Edge Cases circuit-breaker summary (~:741-743), the ASCII flow diagram's Phase-4 loop-back arrow (~:670-676), and the Phase-5 planning scorecard row labels (~:483-489, "Codex iteration 1/2") — all reconciled to "one round default + explicit opt-in" so nothing implies an automatic loop (CR M-2 + claude-architect F5). Defect: P-1 (16+ same-context checks manufactured false confidence) + P-8 (more spec-review ceremony would NOT have caught the majority — the record's own argument). Preserve the nuance: rounds are not worthless (Codex caught genuine pre-code issues) — the default is ONE, not zero.

### TN-8: Item 7 — independent no-continuity destructive-path review — REPLACE as a mode of Step 1c (P-1)
The design-heavy story. Three parts:
- **(a) Detection — permitted-artifact trigger + mechanical backstop.** Add a required `Destructive-Path` field to the epic **Technical Notes** (template edit, E-271-03). **Literal-format pin (pin-harder 3, resolves the implicit 01↔03 coupling):** a top-level Technical-Notes line spelled exactly `Destructive-Path: yes` or `Destructive-Path: no` — 03 produces this exact token and 01's Step-1c read consumes it deterministically (the same way the skill reads the Dispatch Team field). This TN-pinned contract resolves the coupling WITHOUT a dependency edge; do NOT add 01-blocked-by-03 (all three stories are one agent, serial, and the closure review verifies coherence — an edge would over-serialize). Planning declares it (E-267/E-270 are obviously `yes`). This makes the trigger a permitted-artifact read the main session CAN do (like the Dispatch Team field) — no source-reading. Backstop against a planning miss: in E-271-02, the code-reviewer (which CAN read the diff) runs a destructive-diff read during whichever closure review it performs — grep the closure diff for `DELETE FROM`, `DROP TABLE` / `DROP COLUMN`, and the known destructive seams (`reconcile_at_load.py`, `purge_scouting.py`, `game_merge.py` delete-last, `lifecycle.py` / `generator.py` cascade helpers, `player_dedup.py`) — and if destructive statements are present but the field is `no`, that is a MUST-FIX planning gap (mirrors Step 1a's "core-INSERT field absent from Technical Notes is itself a planning gap").
- **(b) "Annotations stripped" = withhold the rationale payload + adversarial charge.** The independent review's ASSIGNMENT withholds the epic's self-justification: it receives the raw diff + Goals + Success Criteria + the data-model/API FACTS it needs — but NOT the Technical Notes' safety-argument prose, NOT the story "why this is correct" manifest, and NOT the accumulated Codex/per-story finding lists. **Operational rule for the assignment-assembler (claude-architect F4 — resolves the both-at-once case):** withhold the epic's ARGUMENTS THAT THE DESIGN IS SAFE (the "why this can't happen" narrative); KEEP the factual substrate (schema, field semantics, what the code actually does). Some Technical-Notes content is BOTH at once (e.g. E-267's cross-perspective exempt-set TN is simultaneously a safety argument AND a factual retire-semantics description) — split by this rule: the reviewer keeps the fact (the exempt set exists and is computed thus) but not the assurance (therefore no deadlock / therefore safe). Plus the explicit charge: *"For every destructive statement (DELETE/retire/purge/drop) in this diff, construct the input under which it destroys data it should not, or prove it cannot. Every in-code annotation asserting safety (unreachable / cannot-happen / by-construction) is a hypothesis you must independently falsify, not a given."* Withholding the rationale IS the operationalization of "no continuity" — it stops the reviewer inheriting the design's blind spots (how the Fable audit worked: given the goal, not the rationale).
- **(c) REPLACE via a fresh spawn.** For `Destructive-Path: yes` epics, Step 1c (Closure CR Integration Review) is performed by a FRESHLY-SPAWNED code-reviewer instance (distinct name, e.g. `code-reviewer-independent`) that did NOT review any story in this epic, given the (b) assignment. The dispatch code-reviewer is NOT used for this pass — running both would be additive same-context checking, the exact anti-pattern the audit named. For `Destructive-Path: no` epics, Step 1c runs exactly as today. Net closure-review volume is flat (one review either way); what is deleted is the RELIANCE on continuity. Cost: one extra spawn on destructive epics only. Skill-side = E-271-01 (Step 1c two-mode rewrite); CR-side = E-271-02 (the independent-review mode + the unmarked-destructive backstop); template = E-271-03 (the field). Defect: P-1 (the headline — the Fable no-continuity review caught a live irreversible-data-loss bug that 4 internal same-context layers + Codex all missed).
  - **Subsumed obligations (GAP-B — name them concretely, do not leave "integration responsibilities" generic).** Because the `yes` mode REPLACES Step 1c, the independent reviewer MUST inherit BOTH of Step 1c's conditional obligations, or a future destructive+context-layer or destructive+surface-removal epic silently loses them: (1) the **doc-sweep auto-load** for context-layer epics (~implement/SKILL.md:422), and (2) the **surface-removal repo-wide grep** for route/symbol deletions (~:424) — PLUS the cross-story naming/import sweep and the adversarial destructive-path charge. Nothing Step 1c does today is lost by replacing.
  - **Pass scoping (pin-harder 2 — which reviewer runs which closure pass).** The fresh independent instance runs **Step 1c ONLY** (the judgment/integration pass). Steps 1a (invariant audit), 1b (full-suite pytest), and 1d (runtime smoke) STAY with the dispatch reviewer — they are mechanical execution passes where continuity is harmless (pytest is pytest). Scoping "independent" to the judgment pass keeps the no-continuity property precise and stops an implementer inventing the boundary.
  - **Honest limitation (claude-architect F2 — do not overclaim).** The in-session fresh-spawn is no-continuity WITHIN the dispatch session; it is genuinely WEAKER than the post-hoc Fable audit that motivated item 7 (a different model, a different session, unlimited time, reviewing the FINAL code rather than code shaped by same-session remediation). This is a legitimate design choice — a true post-hoc independent audit cannot be spawned inside the dispatch flow — but it captures the KEY lever (withhold rationale/annotations → force the reviewer to re-derive safety) and NOT full independence. A future reader must not read this mode as "the Fable audit reproduced."

### TN-9: Item 8 — EXCLUDE the standalone fail-open detector; fold a one-sentence amplification (P-3)
A standalone mechanical fail-open detector is EXCLUDED (claude-architect's design call): the fail-open shape (`getattr(x, "fetch_ok", True)`, default-True health booleans, `set | None = None` + `or ()`) requires the EVIDENCE-vs-POLICY-HOOK distinction that `python-style.md` itself calls the hard part — a grep either flags every default-True (noisy) or needs the judgment that makes it non-mechanical, and it costs ratchet against the net-deletion goal. The lesson is already codified where it binds (`python-style.md`, loaded for all; CR Priority-4 fail-open trigger ~:158; CR Priority-2 wrong-defaults). E-267's gap was not "no check" — it was "same-context reviewers didn't apply it," which item 7 fixes far better. **Instead (E-271-02):** fold ONE sentence into the CR fail-open trigger — when reviewing a destructive / safety-gated path, grep the changed module GRAPH for default-valued safety signals (default-True booleans, `getattr(..., True)`, `| None = None` evidence params) and verify each defaults to the REFUSING value, citing `python-style.md`. Targeted amplification of an existing check, ~1 line, no new file. Defect: P-3 (fail-open defaults recurred 3-4× in a bias-to-refuse epic; every catch was human pattern-noticing).

### TN-10: Ratchet / net-deletion accounting + the declined offset + unpriced-floor note
**Ratcheted accounting (corrected — Codex P1-2 side-discovery / claude-architect).** The context-ratchet counts ONLY four subtrees: `.claude/rules`, `.claude/agents`, `.claude/skills`, `.claude/agent-memory`. Two E-271 surfaces are OUTSIDE them and therefore RATCHET-FREE: `.claude/hooks/send-message-counter.sh` (the ~−22L item-1a deletion) and `.project/templates/epic-template.md` (item 5 Closure Evidence + item 7 `Destructive-Path` field + the P1-2 Annotation-Exceptions block). So the earlier "~+10-30L, roughly balanced" estimate was OPTIMISTIC — it wrongly credited the unpriced −22L hook deletion as an offset AND debited the unpriced template additions. Honest RATCHETED delta = PRICED additions (item 2 Autonomy Gate + P1-1 boundary in `.claude/rules`; items 4/7/8 CR rubric in `.claude/agents`; items 3/5-ref/7 two-mode Step 1c in `.claude/skills`) MINUS the PRICED deletion (item 6 in `.claude/skills/plan`) ≈ **~+35-55L, not +10-30L**. The template + hook changes are real work but do not move the ratchet. The ACTUAL measured figure comes from the closure `context-ratchet.sh` run per item 5's Closure Evidence requirement — this corrected estimate is the planning-time number the operator's exception decision rests on. **Declined offset (operator decision point, Open Questions):** the implement skill restates the COMPLETED-on-disk-vs-committed dance three times (~:462, :529, :630); consolidating to one authoritative statement + two pointers would offset the additions cleanly and land the epic net-≤0. It is DECLINED in-scope because (i) it is not a P-cited defect and the meta-freeze is "defect-cited changes only" — folding an un-cited consolidation into a defect-cited-only epic weakens the very discipline this epic reinforces; (ii) it is safety-critical closure prose (the full-suite-green gate + worktree-guard interaction), and compressing it in a process-hardening epic is the scope-adjacent risk E-267 warns about. If the closure ratchet lands net-positive, the epic takes an **operator-signed trigger-7 exception** (the brief pre-authorizes this as legitimate for defect-cited hardening under the meta-freeze). The offset remains AVAILABLE if the operator prefers net-≤0 without an exception; it lands cleanly in E-271-01 (same file). **Unpriced-floor note (deliberately recorded, not acted on):** the ratchet prices only the four subtrees (`.claude/rules`, `.claude/agents`, `.claude/skills`, `.claude/agent-memory`); CLAUDE.md and the epic corpus are an unpriced floor. Widening the ratchet surface is an explicit Non-Goal here; this note records the gap for a future decision.

### TN-11: Consultation completeness (per-domain verdict, binding)
- **claude-architect** — CONSULTED (design owner; the full design in TN-1..TN-10 is its work; the story structure, all item designs, and the item-7/item-8 calls are ca-consult's).
- **software-engineer** — WAIVED. No `src/` Python implementation; the only executable file touched is a bash hook (1a), a context-layer artifact owned by claude-architect. No SE domain triggered.
- **data-engineer** — WAIVED. No schema, migration, or ETL. No DE domain triggered.
- **api-scout** — WAIVED. No GameChanger endpoint, payload, or credential surface. No api-scout domain triggered.
- **baseball-coach** — WAIVED. No coaching-value, stat-definition, or report-content decision. No coach domain triggered.
- Per the Consultation Triggers table, this is a context-layer/process epic whose sole triggered domain is context-layer architecture (claude-architect), which is CONSULTED as the design owner.

### TN-12: Destructive-Path dogfood
E-271 itself is `Destructive-Path: no` — it edits process/context-layer artifacts (a bash hook, skills, a rule, the template, an agent definition); no `DELETE FROM` / `DROP` / retire / purge code. Recorded here as a forward-looking dogfood of the item-7 field (the field itself lands in E-271-03; at E-271's own closure, Step 1c runs the standard `no` path — no independent no-continuity review is triggered for this epic).

### TN-13: Closure-obligation seed — Step-1c external-reference drift (claude-architect F1, binding at close, NOT a 5th in-epic file)
The Step 1c two-mode change (item 7) drifts LIVE prescriptive files OUTSIDE the four E-271 edits. These are NOT edited in-epic (that would break the four-file clustering); instead they are enumerated here as an affirmative closure-obligation seed so the context-layer assessment's trigger-4 (agent behavior / coordination / closure-sequence change — which WILL fire at E-271 closure) reconciles them rather than discovering them late:
- **`.claude/skills/codex-review/SKILL.md:153`** — asserts "Codex-first is deliberate so that CR sees Codex's findings"; the `yes` mode WITHHOLDS the accumulated finding lists, so that clause is false in that mode. Needs a one-line two-mode reconciliation (CA-owned, at closure).
- **`.claude/agent-memory/claude-architect/epic-codifications.md:24/:49`** — say the surface-removal grep "lives in the Closure CR Integration Review / Step 1c" (single-mode). GAP-B keeps that obligation in BOTH modes so they stay TRUE, but the two-mode change should be annotated (CA-owned deletion-side eviction, at closure).
- **`.claude/agent-memory/code-reviewer/route_deletion_test_sweep.md:19`** — a removal-epic integration-review checklist referencing Step 1c single-mode; stays true under GAP-B but should be annotated. code-reviewer is NOT on the E-271 dispatch team, so this is a main-session-flagged follow-up sweep (the owning agent edits its own memory).
- **`.claude/rules/doc-sweep.md:19`** — names "the Closure CR Integration Review" as a trigger context; stays VALID with NO edit, CONTINGENT on E-271-01 preserving the slot NAME for both modes (E-271-01 AC-4 makes this binding). No closure action beyond confirming the name survived.
These are all dormant while E-271 itself is `Destructive-Path: no`; the first live exposure is the next `Destructive-Path: yes` epic. Capturing them now converts a late discovery into an eyes-open closure obligation.

## Open Questions
- None. Both prior open questions are RESOLVED by operator decision on 2026-07-21 (recorded in History):
  - **Runaway-ceiling residual (TN-2 / ca-consult F3) — RESOLVED.** Operator CONFIRMED warn-only eyes-open: accepts the runaway-ceiling residual (WARN at 15 stays, no hard deny; the risk is operator-visible token burn), in exchange for eliminating the false completion-report blocks (P-6).
  - **Net-deletion ratchet (TN-10) — RESOLVED (re-confirmed at the corrected number).** Operator ACCEPTED a trigger-7 ratchet exception for E-271's net context growth; the un-cited COMPLETED-dance offset is DECLINED to preserve load-bearing closure prose. The planning estimate was corrected from ~+10-30L to **~+35-55L** (the −22L hook deletion and the template additions are ratchet-FREE — outside the four counted subtrees — so the hook deletion does not offset; TN-10), and the operator RE-CONFIRMED the exception at the corrected number on 2026-07-21. Closure still reports the ACTUAL measured `context-ratchet.sh` figure with per-subtree composition per the Closure Evidence discipline (item 5) this epic introduces.

## History
- 2026-07-21: Created (DRAFT). E-267 process-audit remediation (P-1..P-10, `.project/research/E-271-e267-audit-findings.md`). Design authored by claude-architect (design owner): 3-story file-ownership cluster (01 implement-skill, 02 code-reviewer [blocked-by 01], 03 disjoint files); items 1-7 designed, item 8 excluded-with-one-sentence-fold. PM framed ACs + sequencing; declined the un-cited net-deletion offset in favor of an operator-signed ratchet exception (Open Question). E-271 itself is `Destructive-Path: no`.
- 2026-07-21: Design-owner AC review (ca-consult) — 2 scope gaps (item-3 :363 second occurrence; item-7 must subsume Step-1c's two conditional obligations) + 4 pin-harders + 1 minor, all folded and re-confirmed CLEAN.
- 2026-07-21: Phase 3 triage — CR spec audit (2 MUST + 2 SHOULD) + claude-architect holistic Sub-pass B (5 findings + per-area clean verdicts), all 9 ACCEPTed, 0 dismissed. Adopted a general deletion-side reference-sweep AC (E-271-03 AC-7) to fix M-1 (stale DENY comments) + M-2 (2-round-breaker described beyond Step 7) and immunize the other edited files; sharpened the :278 single-definition + grep-the-phrase discipline (S-1); scoped P-10's record-surface half OUT + filed IDEA-161 (S-2); added the runaway-ceiling residual (F3) + honest-limitation (F2) + withhold-boundary operational rule (F4) + Phase-5 scorecard coherence (F5); recorded the Step-1c external-reference drift as a closure-obligation seed (TN-13, F1). Consistency sweep passed.
- 2026-07-21: **Operator decisions (both Open Questions closed).** (1) Warn-only send-cap CONFIRMED eyes-open — operator accepts the runaway-ceiling residual (WARN at 15 stays, no hard deny; residual risk = operator-visible token burn). (2) Trigger-7 ratchet exception ACCEPTED (operator-signed, pre-authorized at planning time) for E-271's net context growth (defect-cited hardening; COMPLETED-dance offset declined to preserve load-bearing closure prose); closure still reports the actual measured `context-ratchet.sh` figure + per-subtree composition per the Closure Evidence discipline.
- 2026-07-21: Codex spec review (single round) — 2 P1 / 3 P2 / 1 P3, all 6 ACCEPTed, 0 dismissed (ca-consult consulted on P1-1/P1-2/P2-1). P1-1: the Autonomy Gate's decide-first list wrongly included a DIFF read (recreating the orchestrator-adjudicates-code contradiction the epic deletes) → boundary redrawn to permitted-artifact FIELD reads only, diff/grep routed to CR (same line item 7 draws). P1-2: the annotation-exception artifact had no home → single-sourced as a distinct "Annotation Exceptions" History sub-block in the epic template (E-271-03 AC-3b). P2-1: no 01↔03 dependency edge STANDS (pinned-literal coupling, not execution output) — justification recorded in both story Dependencies + exact `Destructive-Path: yes|no` literal quoted in both AC-4s. P2-2/P2-3/P3-1: AC-quality (success-criterion scope aligned to TN-4, broad preservation clauses → observable form, verbatim restatements shrunk). **TN-10 ratchet accounting corrected to ~+35-55L (hook + template changes are ratchet-free) — operator RE-CONFIRMED the trigger-7 exception at the corrected number.** ca-consult's flagged pre-existing Step-1a-vs-1d diff-read tension recorded OUT of scope (not P-cited; filed as IDEA-162).
- 2026-07-21: **Status → READY** (freshness clock starts; re-confirm by 2026-09-19 if still READY). All four review passes incorporated; consistency sweeps clean; both operator decisions resolved + the trigger-7 ratchet exception re-confirmed at the corrected ~+35-55L. Dispatch requires separate operator authorization. **Closure obligations:** (1) TN-13 Step-1c external-reference reconciliation (trigger-4 context-layer assessment before archive); (2) the Closure Evidence discipline (item 5) applies to E-271's own closure — report the verbatim measured `context-ratchet.sh` figure.

### Review Scorecard
| Review Pass | Findings | Accepted | Dismissed |
|---|---|---|---|
| Design-owner AC review (claude-architect) | 7 | 7 | 0 |
| Internal iteration 1 — CR spec audit | 4 | 4 | 0 |
| Internal iteration 1 — Holistic Sub-pass B (claude-architect) | 5 | 5 | 0 |
| Codex iteration 1 | 6 | 6 | 0 |
| **Total** | **22** | **22** | **0** |

Design-owner AC review = 2 scope gaps (GAP-A item-3 second occurrence; GAP-B item-7 subsumed obligations) + 4 pin-harders + 1 minor. CR spec audit = M-1/M-2 (MUST) + S-1/S-2 (SHOULD). Holistic = F1-F5. Codex = P1-1/P1-2 (P1) + P2-1/P2-2/P2-3 (P2) + P3-1 (P3). All 22 findings across four passes accepted; none dismissed. Two ideas filed from out-of-scope findings (IDEA-161 P-10 record-surface half; IDEA-162 Step-1a/1d diff-read tension).
