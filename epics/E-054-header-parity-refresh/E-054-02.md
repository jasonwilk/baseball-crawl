# E-054-02: Operator integration and workflow docs

## Epic
[E-054: Header Parity Refresh from MITM Captures](epic.md)

## Status
`TODO`

## Description
After this story is complete, the header refresh command is documented in CLAUDE.md and the mitmproxy guide, and the operator has a clear workflow for keeping headers current: capture traffic, run one command, review the diff, commit.

## Context
E-054-01 creates the script. This story wires it into the operator's workflow documentation so it is discoverable and the process is clear.

## Acceptance Criteria
- [ ] **AC-1**: `CLAUDE.md` Commands section includes `python scripts/proxy-refresh-headers.py [--apply]` with a brief description.
- [ ] **AC-2**: `docs/admin/mitmproxy-guide.md` has a "Refreshing Header Fingerprints" section that documents the end-to-end workflow: (1) capture traffic, (2) run the refresh script in dry-run mode to preview, (3) run with `--apply` to update, (4) review `git diff` and commit.
- [ ] **AC-3**: The mitmproxy guide section explains which headers are excluded from auto-update and why (credentials, per-request, connection-level).

## Technical Approach
- Add command entry to CLAUDE.md Commands section.
- Add a new section to `docs/admin/mitmproxy-guide.md` (after the existing capture workflow sections).

## Dependencies
- **Blocked by**: E-054-01
- **Blocks**: None

## Files to Create or Modify
- `CLAUDE.md` -- add command entry
- `docs/admin/mitmproxy-guide.md` -- add workflow section

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing (no new tests needed -- documentation only)
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests

## Notes
- This story also covers adding the E-050 follow-up commands (`check_credentials.py`, `bootstrap.py`) to CLAUDE.md if E-053-04 has not already done so. If E-053-04 runs first, skip the duplicate entries.
