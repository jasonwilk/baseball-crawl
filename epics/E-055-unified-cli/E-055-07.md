# E-055-07: CLAUDE.md Commands Section Update

## Epic
[E-055: Unified Operator CLI](epic.md)

## Status
`TODO`

## Description
After this story is complete, the CLAUDE.md Commands section will document the `bb` CLI as the primary operator interface, with the full command map. The old `python scripts/*.py` commands remain documented (they still work) but are noted as also available through `bb`.

## Context
CLAUDE.md is the source of truth for how to operate the system. When the CLI is complete, it must be documented there so agents and operators know the preferred invocation pattern.

## Acceptance Criteria
- [ ] **AC-1**: The CLAUDE.md Commands section includes `bb --help` as the first command, with a note that it shows all available commands.
- [ ] **AC-2**: The Commands section includes `bb status`, `bb creds check`, `bb creds refresh`, `bb data sync`, `bb data crawl`, `bb data load`, `bb proxy report`, `bb proxy endpoints`, `bb proxy refresh-headers`, `bb proxy review`, `bb db backup`, `bb db reset` -- each with a one-line description.
- [ ] **AC-3**: The existing `python scripts/*.py` commands remain in the Commands section (they still work) but are grouped under a "Script aliases" or similar subheading to indicate the `bb` commands are preferred.
- [ ] **AC-4**: The proxy lifecycle commands (`cd proxy && ./start.sh` etc.) remain documented under "Script Aliases" with a note that they must be run on the Mac host (not via `bb`). The proxy analysis scripts (`proxy-report.sh`, `proxy-endpoints.sh`, `proxy-review.sh`, `proxy-refresh-headers.py`) are noted as also available via `bb proxy`.
- [ ] **AC-5**: No other sections of CLAUDE.md are modified.

## Technical Approach
1. Read the current CLAUDE.md Commands section.
2. Restructure it into two subsections: "CLI Commands" (the `bb` commands) and "Script Aliases" (the old `python scripts/*.py` and `proxy/*.sh` commands).
3. Keep the exact same information -- just reorganize and add the `bb` equivalents.

Reference files:
- `/workspaces/baseball-crawl/CLAUDE.md` (Commands section)

## Dependencies
- **Blocked by**: E-055-01 through E-055-06 (all CLI commands must be implemented first)
- **Blocks**: None

## Files to Create or Modify
- `CLAUDE.md` (modify Commands section only)

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing (N/A -- documentation only)
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests

## Notes
- This is a context-layer file change (CLAUDE.md), so it must be routed to claude-architect during dispatch.
