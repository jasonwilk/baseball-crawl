# E-267: Reconcile-at-Load Against the Fresh Crawl (forward-only retire-absent across three grains)

## Status
`READY`
<!-- Lifecycle: DRAFT → READY → ACTIVE → COMPLETED (or BLOCKED / ABANDONED) -->
<!-- READY 2026-07-19 (plan-skill re-plan of the ad-hoc original). Pre-READY complete: DE + api-scout +
     baseball-coach consultations, Codex spec review rounds 1-4, and a code-reviewer test-coverage audit —
     all incorporated and verified clean (see the Review Scorecard in History). NO open pre-dispatch
     prerequisites remain (roster cap LOCKED = MAX_ROSTER_DEPARTURES 2; game-grain probe DONE — Probe-2
     reschedule-id is a soft in-flight spot-check). NOT dispatched — awaits explicit user dispatch
     authorization. -->

## Overview
The scouting/report load pipeline is ACCUMULATE-ONLY: a re-scout upserts what is in the fresh payload but never retires what VANISHED from it. This epic makes the load path RECONCILE-AT-LOAD going forward — on every re-scout, a set-difference retire runs as part of the normal load across three grains: the **game grain** (a removed or rescheduled game), the **player-line grain** (a removed per-player stat row), and the **roster grain** (a departed roster player). This is PREVENTION-AT-LOAD, not a retroactive repair. The load-bearing safety rule: a genuinely removed row is retired, but a transient/postponed/not-yet-final absence NEVER deletes live data (bias-to-refuse).

## Background & Context
Surfaced by the 2026-07-19 accumulate-only re-run audit (master record: `.project/research/2026-07-19-rerun-accumulate-only-audit.md`). Season aggregates derive at QUERY TIME from `player_game_*` via `get_season_batting`/`get_season_pitching` (`src/api/db.py`), so a stale or duplicate row silently corrupts a coach-facing number. Three coach-facing accumulate-only gaps share one root and one fix shape:

- **Game grain** — IDEA-140 (rescheduled game re-inserted → double-counted) + CC-1 (removed/voided game persists in W-L, season lines, recent form, freshness N). `_find_duplicate_game` keys on `game_date` + unordered team pair, so a moved game does not match; nothing retires a deleted game.
- **Player-line grain (H1)** — a player dropped from a boxscore between runs keeps their `player_game_batting`/`pitching` row (UPSERT-only, no set-difference delete) → season SUMs stay inflated. Two-channel CONFIRMED/high.
- **Roster grain (H2)** — a departed player never leaves `team_rosters` (INSERT/UPSERT-only, no prune); the report roster grid renders ex-players indefinitely. Two-channel CONFIRMED/high.

**Operator design decision (2026-07-19, locks the approach):**
1. **Forward-only.** Do NOT build a retroactive `bb data` pass that retires/rewrites historical loaded data. The operator does not care about historical data or historical report content. The epic's job is prevention-at-load going forward — not correcting already-loaded rows.
2. **Clean slate by purge command.** After this epic lands, the operator starts from a clean data slate via the E-267-06 `bb db purge-scouting` command (a plain mass report-delete does NOT clean-slate — see the Clean-Slate section for the `teams.is_active` caveat). The "migration" for existing corrupted data is a wipe, not a repair. Design for a clean starting state post-purge.
3. Removed-vs-transient corroboration still matters for the FORWARD load path (don't delete live data on a transient absence during a normal re-scout), but there is NO retroactive-cleanup story.

CC-2 (cross-perspective score-misattribution) is DELIBERATELY OUT — different mechanism (COALESCE asymmetry), promoted to its own fix E-268.

## Goals
- Reconcile-at-load set-difference retire covering game, player-line, and roster grains, running as part of the normal re-scout load path.
- A removed-vs-transient corroboration decision (bias-to-refuse) so a transient absence never deletes live data on the forward path.
- Every retire path gated against the E-257 reconciliation-scoreboard ratchet (no fidelity regression).
- Every story ships a regression test that reproduces the corruption pre-fix and asserts the fix post-fix.

## Non-Goals
- **No retroactive `bb data` repair pass** and no rewrite of already-loaded historical rows — existing corruption is handled by the operator's clean-slate report deletion, not by this epic (per operator decision).
- CC-2 cross-perspective redirect score-misattribution (E-268 — different mechanism).
- Frozen-plays in-place refresh (H3 / IDEA-146) and orientation-driven `batting_team_id` re-derive (H4 / IDEA-147) — those REFRESH changed rows; this epic RETIRES missing rows.
- The accumulate-only hygiene cluster (IDEA-152) — no coach impact.
- Any multi-season / cross-team machinery (permanent project non-goal).

## Success Criteria
- Given a re-scout whose fresh crawl OMITS a game/player-line/roster entry that a prior run loaded, AND the absence is corroborated as genuinely removed, when the load runs, then the stale row is retired in the same load and the affected coach-facing number (season aggregate, W-L, roster grid) corrects.
- Given a rescheduled game (same real game at a new date), when the re-scout loads it, then it is deduped by the correct path (TN-3): a CROSS-perspective moved game is redirected/merged via the canonical `merge_duplicate_game` seam; a SAME-perspective reschedule is matched on the stable `event_id` and its `game_date` is UPDATED in place (no delete) — never double-inserted.
- Given a transient/postponed/not-yet-final absence, when the load runs, then it REFUSES to retire and the live data is untouched (bias-to-refuse), emitting one WARN per refusal.
- Given any story's change, when `bb report reconcile-scoreboard` runs before/after against live data, then no gated stat's abs-Δ increases, neither ratcheted axis counter increases, and `self_games` stays 0.
- Every story has a regression test that fails pre-fix and passes post-fix, INCLUDING a bias-to-refuse test.

## Stories
| ID | Title | Status | Dependencies | Assignee |
|----|-------|--------|-------------|----------|
| E-267-01 | Reconcile-at-load core: set-difference + removed-vs-transient corroboration (bias-to-refuse) | TODO | None | - |
| E-267-02 | Game grain at load: retire removed + redirect rescheduled games | TODO | E-267-01 | - |
| E-267-03 | Player-line grain at load: retire removed per-player stat rows (H1) | TODO | E-267-02 | - |
| E-267-04 | Roster grain at load: retire departed roster players (H2) | TODO | E-267-03 | - |
| E-267-06 | Purge scouting/report data, preserve user identity + auth (clean-slate command) | TODO | None | - |
| E-267-05 | Document the forward reconcile-at-load behavior + purge command (admin/reports docs) | TODO | E-267-02, E-267-03, E-267-04, E-267-06 | - |

## Dispatch Team
<!-- Advisory. DE authors the reconcile-at-load core (E-267-01) and OWNS the TN-8/TN-9/TN-12 decisions
     as advisory consultant on E-267-06; SE implements the in-pipeline grain passes (02/03/04) AND
     authors the E-267-06 purge command (Python/CLI, no schema — SE-routed per the 2026-07-19 SE
     consultation); docs-writer documents the forward behavior + purge command (05). -->
- data-engineer
- software-engineer
- docs-writer

## Technical Notes

**TN-1 — Delivery vehicle: RECONCILE-AT-LOAD (operator-decided).** The retire-absent set-difference runs as part of the normal re-scout load path (invoked from the loaders during `generate()`/`load_team`), NOT as a separate retroactive `bb data` operator-maintenance pass. There is no new operator command and no historical-repair sweep. This resolves the prior delivery-vehicle open question.

**TN-2 — Retire-absent corroboration = a HEALTH gate on the fresh crawl (DE-decided, bias-to-refuse).** NO stored "last successful crawl" snapshot table — the DB IS the prior-loaded set and the fresh in-memory crawl is the authority (keeps the epic migration-free). Corroboration is a completeness/health gate on the FRESH payload, not a diff against history. The shared primitive (E-267-01) is `classify_absences(prior_ids, fresh_ids, *, crawl_authoritative) -> {id: PRESENT | REMOVED | TRANSIENT_ABSENT}` — a PURE function over id sets (no DB handle, no logging, no I/O). The connection-in / no-commit / caller-owns-the-transaction contract, the actual DELETE, and the WARN-per-refusal belong to the grain RETIRE helpers (E-267-02/03/04), NOT the classifier. A retire is allowed ONLY for REMOVED. Universal bias-to-refuse: an absence is REMOVED only when the fresh crawl for that grain (a) fetched OK, (b) returned a NON-EMPTY payload, and (c) did not shrink catastrophically — `fresh_count >= prior_count * FLOOR_RATIO`, FLOOR_RATIO = 0.5. Any failure → the classifier returns ALL absences in that grain as TRANSIENT_ABSENT → the grain helper performs zero deletes and emits one WARN per refusal. Per-grain extra guards: GAME — a game present-but-scoreless / not-final is TRANSIENT (postponed/in-progress); only fully-absent-from-the-fresh-schedule is a removal candidate. PLAYER-LINE — scoped strictly per `(game_id, perspective_team_id)`; scored-but-EMPTY boxscores are the MODAL case, so an empty/absent boxscore must NEVER retire prior lines. ROSTER — reuse the existing empty-guard (`scouting_loader.py:343-345`) but use the STRICTER drop guard in TN-12 (NOT the flat floor-ratio, which is too loose for a 12-15 roster).

**TN-3 — Reschedule handling has TWO paths (DE risk 3; refined by TN-11 Probe 2).** A moved game is the same real game at a new date. (a) CROSS-perspective moved game → REDIRECT/merge via the canonical `merge_duplicate_game` seam (`src/db/game_merge.py`) — do NOT re-inline FK-child re-pointing; the perspective-specific `id` can't bridge perspectives, so the date+team-pair natural key is required here. (b) SAME-perspective reschedule (the common case: the same LSB team re-scouting the same opponent whose game moved date) — the stable `event_id` (public `/games` `id`) survives a GC reschedule PATCH-in-place (TN-11 Probe 2), so PREFER matching on that id and UPDATING the `game_date` in place (no delete, no double-insert); this also means the game never looks like a removal. The old-date-row-as-REMOVED hard-delete (full child surface per TN-10) is the FALLBACK only if id-matching is unreliable (the current `_find_duplicate_game` date+team-pair key breaks on a date move, which is what makes id-matching the better same-perspective key). E-267-02 must implement both paths (AC-2 cross, AC-3 same). A removed game (absent from the full array, corroborated removed per TN-2) is retired.

**TN-4 — HARD-DELETE, uniformly across all three grains (DE-decided; satisfies E-267-01 AC-3).** No soft-retire flag, no migration. Rationale: a soft-retire marker forces a `WHERE is_retired IS NULL` filter into EVERY reader (`get_season_*`, `_query_record`/recent-form/runs-avg, `_query_roster`, `_query_freshness`, `recon_scoreboard.py`) — miss one and the stale row silently re-inflates, the exact failure class this epic fixes. Hard-delete makes correctness structural. Auditability is delivered by a WARN-per-retire log line (what/why-REMOVED), NOT a reversible marker. Consistent with every existing seam (`merge_duplicate_game`, `player_dedup`, `lifecycle` all hard-delete).

**TN-5 — Ratchet gate (mandatory, all stories).** Every retire change MUST be gated against the E-257 reconciliation-scoreboard (`bb report reconcile-scoreboard`, baseline `.project/baselines/reconciliation-scoreboard.json`): no gated stat's abs-Δ increases, neither ratcheted axis counter (`dropped_pitch_events`, `no_plays_units`) increases, `self_games` stays 0. Manual operator diagnostic against live data (dev DB absent from worktrees/CI), recorded at closure — a hard release condition, not a CI gate.

**TN-6 — Canonical seams (context, not prescription).** `merge_duplicate_game` / `is_offline_same_game` (`src/db/game_merge.py`); `get_season_batting`/`get_season_pitching` (`src/api/db.py`, query-time season readers a stale player-line inflates); `_query_roster` (`src/reports/generator.py:626-641`, the roster grid H2 corrupts); `_query_record`/recent-form/runs-avg game-level reads a stale/duplicate game corrupts. The perspective-provenance invariant (`.claude/rules/perspective-provenance.md`) binds every touched write.

**TN-7 — Standing test requirement (operator directive 2026-07-19; CODE stories only).** Every CODE story (E-267-01/02/03/04/06) MUST ship a regression test that reproduces the corruption (fails pre-fix — inflated aggregate / duplicated W-L / stale roster row) and asserts the fix (passes post-fix), INCLUDING a bias-to-refuse test proving a transient absence is NOT retired on the forward path. For E-267-06 the hard test is: identity + login survive the purge while scouting/report tables empty. The docs story E-267-05 is EXEMPT — it ships no code; its verification is that the docs match the shipped behavior/command (E-267-05 AC-4).

**TN-8 — Purge keep-vs-purge partition + FK-safe order (E-267-06, DE-decided).**
- KEEP (identity/auth + bootstrap): `users`, `passkey_credentials`, `magic_link_tokens`, `sessions`, `webauthn_challenges`, `programs` (lsb-hs bootstrap; FK-safe parent), `_migrations`.
- PURGE (scouting/report — 20 live tables): reports, report_generation_runs, scheduled_report_runs, games, game_perspectives, player_game_batting, player_game_pitching, plays, play_events, spray_charts, reconciliation_discrepancies, team_rosters, scouting_runs, opponent_links, crawl_jobs, coaching_assignments, user_team_access, teams, players, seasons. (DE-verified 2026-07-19 vs live schema, migrations 001-011: `team_opponents` REMOVED — dropped by migration 008; `player_season_*` correctly absent — dropped by 011. KEEP(7) + PURGE(20) = 27 = complete live-table set; exhaustive and disjoint.)
- Ambiguous resolved by DE: `user_team_access` → PURGE (FKs teams; it is authorization not authentication — login survives via users/passkey/magic-link/sessions; admins see all teams, non-admin grants re-granted as teams regenerate). `coaching_assignments` + `crawl_jobs` → PURGE (dead but FK teams). `seasons` → PURGE (self-heals via `ensure_season_row`). `programs` → KEEP.
- FK-safe delete order (FK enforcement ON so a mistake aborts loudly): (1) play_events; (2) report_generation_runs, scheduled_report_runs; (3) plays, player_game_batting, player_game_pitching, spray_charts, reconciliation_discrepancies, game_perspectives, team_rosters, scouting_runs, opponent_links, crawl_jobs, coaching_assignments, user_team_access, reports; (4) games, players; (5) teams; (6) seasons.

**TN-9 — Purge production guard + HTML unlink (E-267-06; SE implementation notes 2026-07-19).** Mirror `src/db/reset.py`'s STRUCTURE (refuse on production unless `--force`, loud WARN with `--force`) but NOT its guard body: `reset.py`'s `check_production_guard` uses the inline `os.environ.get("APP_ENV",...).lower()==` compare (the IDEA-101 bypass class), so write a FRESH guard through the canonical `is_production()` seam (`src/api/helpers.py`, returns bool). Two more implementer-critical points SE flagged:
- **FK-ON is not free.** `sqlite3.connect()` defaults `PRAGMA foreign_keys=OFF` and `reset.py` never turns it on (it deletes the whole file). The FK-safe order (TN-8) only aborts-loudly on a mis-order if FK enforcement is actually ON — the purge module MUST explicitly `PRAGMA foreign_keys = ON` (or route through `get_connection()` in `src/api/db.py`, which sets it). A naive reset.py mirror leaves it OFF and silently orphans.
- **HTML unlink = the mechanism, not `cleanup_expired_reports()`.** That function filters `WHERE report_path IS NOT NULL AND expires_at < ?` (EXPIRED only) — the purge must unlink ALL purged reports' HTML regardless of expiry. Enumerate `SELECT id, report_path FROM reports WHERE report_path IS NOT NULL` (no expiry filter), resolve each against `_REPO_ROOT / "data"`, `unlink()` with per-row error isolation, and do it BEFORE the `reports` DELETE removes the rows (once gone, `report_path` can't be enumerated).
- **One transaction.** Wrap all ordered deletes in a single transaction — commit at end, rollback on error — so a mid-purge failure never leaves a corrupt half-state.

**TN-10 — Delete discipline (DE risks 1, 2, 4, 5, 6 — wired into E-267-01/02/03 ACs).**
1. **PLAYER-LINE grain only** — every set-difference AND retire DELETE on `player_game_batting`/`player_game_pitching` MUST include `AND perspective_team_id = ?` (those tables carry the column; GC gives different `player_id`s per perspective, so an unscoped delete corrupts the OTHER team's report — perspective-provenance applied to deletes). **ROSTER grain does NOT apply** (DE-confirmed 2026-07-19): `team_rosters` has NO `perspective_team_id` (PK `(team_id, player_id, season_id)`, from a single team-level roster crawl), so the roster retire scopes by the natural key `(team_id, season_id)` and deletes the team-season's rows whose `player_id` is absent from the fresh set — no cross-perspective collision is possible on this grain (one team-season = one roster source = one row set). Roster caveat (semantics, not an FK break): `game_loader._upsert_roster` (`game_loader.py:1548-1562`) also backfills `team_rosters` from boxscore appearances under the same `team_id`, so a player who appeared in an early game but is absent from the fresh roster crawl (e.g. cut mid-season) is retired from the roster display while KEEPING their `player_game_*` rows (stat tables FK to `players`, not `team_rosters`) — E-267-04 must state the intended departed-player semantics explicitly.
2. The player-line reconcile MUST run on RAW `(game_id, perspective_team_id)` boxscore ids BEFORE the `dedup_team_players` sweep (`scouting_loader.py:197-211`) — dedup merges `player_id`s, so reconciling after would false-flag "absent" and wrong-retire.
4. Game-grain retire deletes the FULL child surface atomically with the `games` row LAST (no `ON DELETE CASCADE` on any `games` FK child, so delete-last is load-bearing — a premature `games` delete aborts loudly). DRY against `game_merge._PERSPECTIVE_CHILD_TABLES` + `game_perspectives` + play_events-via-plays. A partial retire skews the E-257 scoreboard.
5. Ratchet framing: a CORRECT retire holds or improves fidelity; only a FALSE retire is dangerous, and the TN-2 corroboration gate IS the protection. Keep the before/after `bb report reconcile-scoreboard` diagnostic on every grain story (TN-5).
6. Player-line retire deletes ONLY the `player_game_*` leaf row, NEVER the `players` parent (other games/perspectives/roster reference it).

**TN-11 — api-scout consultation on the removed-vs-transient GC states (2026-07-19; closes the Codex-flagged missing api-scout review; game-grain probes RUN LIVE).** api-scout validated the three grains against `docs/api/`, the live loader, AND two live probes against the public `/games` endpoint (17 teams, 633 game records, 2026-07-19; doc updated `docs/api/endpoints/get-public-teams-public_id-games.md`). Player-line + roster EMPTY cases HOLD. Game-grain findings:
- **Probe 1 — postponed representation: CLOSES the false-delete risk (assumption CONFIRMED).** Live distribution: 615 `game_status:"completed"`, 17 with `game_status` key ABSENT, 1 `"new"`. No `"postponed"/"suspended"/"in_progress"` string exists. GC KEEPS not-final AND long-past-unplayed games in the array (April & June unplayed games still present 3+ months later), so a game FULLY ABSENT from the full array is a genuine removal/void, not a postponement (E-267-02 AC-6 discharged — no extra suspicion clause needed). Field-shape corrections now in the doc: (1) `game_status` for not-final can be the key ABSENT, not just `null` — `.get("game_status") == "completed"` handles both (the crawler already does this); (2) `"new"` is a real third value (created-but-unscored 0-0 stub).
- **Full-schedule threading (CRITICAL, evidence-validated).** The crawler at `src/gamechanger/crawlers/scouting.py:155` filters to `completed_games = [g ... game_status == "completed"]`. Because GC keeps not-final/unplayed games in the array, diffing the reconcile against that subset would misclassify every present not-final game as REMOVED → mass false-delete. E-267-02 MUST thread the FULL schedule array into the reconcile (E-267-02 AC-5).
- **Probe 2 — reschedule id-survival: strongly inferred SURVIVES (one spot-check open).** GC reschedule = `PATCH /teams/{team_id}/schedule/events/{event_id}` (edit-in-place, `scheduled_time` patchable), NOT delete-and-recreate; the public `/games` `id` IS the `event_id`, so a rescheduled game keeps its `id` and appears as same-`id`/new-`start_ts`. Implication: a SAME-perspective reschedule is best handled by matching on the stable `event_id` (in-place date UPDATE, NO delete — E-267-02 AC-3), which also means it never looks like a removal. The `id` is perspective-specific, so CROSS-perspective reschedule still needs `merge_duplicate_game`'s date+team-pair natural key (id can't bridge perspectives) — matches TN-3's two paths. Residual honesty: id-survival is INFERENCE from the PATCH-in-place mechanism, not a live before/after capture — keep bias-to-refuse on the game grain and spot-verify the id survives if a natural reschedule surfaces before relying on id-matching to skip the delete.
- **Player-line tightening.** Retire a prior line ONLY when the fresh boxscore for that exact `(game_id, perspective_team_id)` is a POPULATED 200 that lists players WITH stats and the specific prior player is absent — not merely a 200 (a scored-but-empty boxscore, the modal case, has envelope+categories present but per-player `stats: []`). Folded into E-267-03 AC-1/AC-2.
- **Roster partial-shrink is a FIX, not a footnote (see TN-12).** HS rosters are 12-15, so FLOOR_RATIO=0.5 only refuses a >50% shrink; a partial mid-edit roster (e.g. 9 of 14 = 64%) passes the floor and would false-retire the 5 missing. Only EMPTY vs FULL rosters have been live-observed — GC's partial-roster-transient behavior is unverified. The roster grain must NOT use the flat floor; it uses the stricter guard in TN-12. (The roster returns only `active` players, so a legitimate `status: "removed"` shrink IS a genuine departure the retire should catch — the guard must still permit a true single departure.)

**TN-12 — Roster-grain drop cap: LOCKED (DE-decided 2026-07-19).** An ABSOLUTE cap, NOT a ratio — roster N is small/bounded (12-15) and real churn is ~1 departure/crawl, so the flat FLOOR_RATIO=0.5 (which would permit deleting 7 of 14) is absurd here and fails to tighten as N shrinks. LOCKED RULE (exact, implementable):
- Constant `MAX_ROSTER_DEPARTURES = 2`.
- Per `(team_id, season_id)` per run: `absent = {DB roster player_ids} − {fresh-crawl player_id set}`.
- If `len(absent) > MAX_ROSTER_DEPARTURES` (≥3): REFUSE the roster retire this run — DELETE nothing, leave all roster rows intact, log ONE WARN carrying `team_id`, `season_id`, `roster_db_count`, `fresh_crawl_count`, `absent_count`, and the absent `player_id` list (operator review). Departed players persist until a clean crawl or operator action.
- If `len(absent) <= 2`: proceed with `DELETE FROM team_rosters WHERE team_id=? AND season_id=? AND player_id NOT IN (<fresh set>)`.
- GATING SCOPE: only DELETEs are capped; the ADD path (new fresh-crawl players) is NEVER gated (additions can't corrupt existing data). Value justification: 2 covers a legitimate double-cut without false-refusing; ≥3 absent in a 12-15 roster is a strong truncated/partial-crawl signal, not real churn. Defense-in-depth with the TN-2 health-gate.
- **Accepted benign fallback (baseball-coach-flagged 2026-07-19):** a PRESEASON TRYOUT CUT (trimming a 20-player pool to 12-15 in one edit) legitimately drops >2 at once, so the cap REFUSES it and leaves stale tryout names on the grid until a clean crawl or an operator purge. This is accepted — the failure mode is benign stale-grid clutter, never a corrupted STAT (unlike the game/player-line grains). No special-case is added; the WARN gives the operator the signal to purge if desired.

## Pre-Dispatch Prerequisites
No OPEN hard blockers remain — both prior blockers are resolved:
- **E-267-02 (game grain) — RESOLVED.** api-scout Probe 1 (2026-07-19, 633 live records) closed the postponed false-delete risk (absent-from-full-array = genuine removal, given the AC-5 full-schedule diff). Remaining SOFT item (not a blocker): Probe 2 (reschedule `event_id`-survival) is strong inference — if a natural reschedule surfaces during implementation, spot-verify the `id` survives before relying on id-matching to skip the delete (AC-3 keeps the delete-fallback + bias-to-refuse until then).
- **E-267-04 (roster grain) — RESOLVED.** The TN-12 drop cap is LOCKED as a real spec value (`MAX_ROSTER_DEPARTURES = 2`, absolute cap) — no dangling "calibrate before dispatch" decision.

**TN-13 — baseball-coach consultation (2026-07-19; per-grain coaching verdict).** Memory: `.claude/agent-memory/baseball-coach/e267_reconcile_at_load_review.md`.
- **Roster grain (E-267-04 AC-5) — ENDORSED, coaching-correct.** Retiring a cut/departed player from the roster DISPLAY while keeping their `player_game_*` stat rows is right: the roster grid answers "who's on this team now / who might I face today" (a departed player there is a false lineup option / false scouting alarm), while stat lines answer "what happened this season" and must survive. "Might return" is a non-risk — GC's roster crawl returns only `active` players, so an inactive/suspended player was already invisible pre-epic; a genuine return re-upserts on a future crawl.
- **Game grain (W-L/season-line) — strongly endorsed, with a BONUS not previously called out:** retiring stale/duplicate games also corrects FALSE PROACTIVE SAFETY FLAGS — a duplicated/stale game inflating a pitch-count/rest-day calc or a hot-streak read could trip a false safety flag or mask a real one. Safety-flag accuracy rides the same corrected game grain, so this epic strengthens compliance surfacing, not just season stats.
- **Player-line scored-but-empty guard (E-267-03) — endorsed.** **Hard-delete (TN-4) — endorsed** (the "never suppress, always contextualize" display philosophy governs low-N real stats, not entities that no longer exist).
- **SHOULD-HAVE guard (verify in E-267-04):** confirm season batting/pitching leaderboards resolve player display name via the `players` table, NOT gated on `team_rosters` membership — else a departed player's real in-season production could silently vanish from BOTH the roster grid AND the season stats (only the grid disappearance is intended). Folded into E-267-04 AC-6.

## Clean-Slate / Data Migration
Existing corruption is NOT repaired by this epic. Operator decision (2026-07-19, option C): after the epic lands, start from a clean data slate via a targeted purge command (**E-267-06**) rather than repairing historical rows. The `teams.is_active` cascade caveat is confirmed — a mass report-delete does NOT clean-slate (a report-delete purges team data only when it is the LAST report for the team AND `is_active=0`, so `is_active=1` subject teams and children strand). E-267-06 provides the FK-safe purge of all scouting/report data while preserving user identity + auth (existing logins survive), production-guarded via `is_production()`. Dev may alternatively use `bb db reset`; live uses E-267-06 to keep auth. This section is no longer held.

## Open Questions
- None. All three held questions resolved by the 2026-07-19 DE consultation: corroboration = a health-gate on the fresh crawl with no snapshot table (TN-2); TN-4 = hard-delete uniform; E-267-06 partition/order/guard specified (TN-8/TN-9). The 6 DE delete-discipline risks are wired into the E-267-01/02/03 ACs (TN-10).

## History
- 2026-07-19: **DRAFT → READY** (plan-skill re-plan of the ad-hoc original — the original epic skipped the pre-READY consultation + spec-review it now gets). **Review Scorecard** (all findings accepted/incorporated; verified clean by a main-session grep of every r4 fix + both safety-test ACs):
  - Expert consultations (3): data-engineer (health-gate corroboration, hard-delete TN-4, purge partition TN-8/9, roster delete-key + LOCKED cap TN-12), api-scout (2 live probes — game-grain full-schedule TN-11 + reschedule `event_id`), baseball-coach (per-grain coaching verdict TN-13) — all incorporated.
  - Codex spec review round 1 (ad-hoc origin): roster delete-key (P1), E-267-05 docs sync (P1), story-01 rescope (P2), api-scout game-grain gap (P2), roster floor + player-line populated-200 — all incorporated.
  - Codex spec review round 2 (re-plan, 7): team_opponents removal (P1), roster cap lock (P1), command-name pin (P2), user_team_access consequence (P2), baseball-coach recorded (P2), E-267-06 routing (P2), TN-7 scope-to-code (P3) — all ACCEPT.
  - Codex spec review round 3 (5, all ACCEPT): Success-Criteria two-path, classifier pure vs connection-in, leaderboard-AC repo-reality, `_migrations` in drift test, epic clean-slate propagation.
  - Codex spec review round 4 (3, all ACCEPT): WARN-ownership contract, TN-2 connection-in rescope, stale readiness text.
  - code-reviewer test-coverage audit (5 gaps): 2 SAFETY (GAP-1 mass-delete safety test, GAP-2 production-guard test) + 3 MODERATE (GAP-3 reconcile-before-dedup ordering, GAP-4 two-team cross-scoping, GAP-5 correction-path) + minor strengthening — all mandated as test ACs.
  - NO open pre-dispatch prerequisites. NOT dispatched — awaits explicit user dispatch authorization.
- 2026-07-19: Created (DRAFT) from the accumulate-only re-run audit. Promotes IDEA-140 (game grain, incl. CC-1); folds H1 (player-line grain) + H2 (roster grain). CC-2 → E-268 (out). Operator locked the approach to forward-only reconcile-at-load (no retroactive `bb data` repair). Clean-slate resolved via operator option C → E-267-06 (targeted purge preserving user identity/auth); `teams.is_active` cascade caveat confirmed (mass report-delete does not clean-slate).
- 2026-07-19: DRAFT → READY. DE consultation complete — corroboration = health-gate on the fresh crawl (no snapshot table, migration-free; TN-2), TN-4 = hard-delete uniform, E-267-06 purge partition/FK-order/guard specified (TN-8/TN-9), and the 6 DE delete-discipline risks wired into E-267-01/02/03 ACs (TN-10).
- 2026-07-19: READY → DRAFT pending spec-review re-run. Incorporated api-scout consultation (TN-11: game-grain FULL-schedule threading fix + un-captured postponed/in-progress false-delete guard; player-line populated-200 tightening; roster partial-shrink) and the Codex spec review: roster delete-key correction (TN-10 risk 1 → `(team_id, season_id)`, DE-confirmed), roster stricter drop guard (TN-12), story-01 rescoped standalone (primitive + unit tests only), E-267-05 synced to cover the purge command + Blocked-by E-267-06, and a Pre-Dispatch Prerequisites section (E-267-02 api-scout probe; E-267-04 DE drop-cap calibration).
- 2026-07-19: code-reviewer spec-level TEST-COVERAGE audit incorporated — mandated a test for every specified failure mode. SAFETY: E-267-02 AC-7 adds the GAP-1 mass-delete safety test (empty/failed/sub-floor fresh schedule retires ZERO games + child rows survive + WARN) plus table-by-table child-surface + WARN-on-refuse assertions; E-267-06 AC-9 adds the GAP-2 production-guard test (prod-refuses / `--force`-proceeds / whitespace-case `" Production "` still refuses — IDEA-101 class) + AC-10 rollback + unlink-isolation. MODERATE: E-267-03 AC-6f reconcile-before-dedup ordering; E-267-04 AC-7 two-team cross-scoping; E-268-01 AC-4 `preserve_scores=False` correction-path (over-gating guard) + AC-3 both-teams. MINOR: E-267-01 AC-6 PRESENT assertion; DoD ratchet lines reworded to "verified at CLOSURE by the operator (not self-checked from the worktree)". H4/IDEA-147 batting_team_id boundary kept OUT (deliberate). Consistency sweep complete (AC numbering sequential all stories). Still DRAFT.
- 2026-07-19: Plan-skill RE-PLAN — three expert consultations + round-2 Codex incorporated. DE: roster drop cap LOCKED `MAX_ROSTER_DEPARTURES = 2` (absolute, TN-12 — kills the dangling calibration blocker); `team_opponents` struck from TN-8 partition + FK-order (dropped by migration 008), full purge-list DE-verified exhaustive/disjoint (KEEP 7 + PURGE 20 = 27). baseball-coach (TN-13): roster-departure semantics + hard-delete + scored-but-empty guard ENDORSED; game-grain W-L correction also fixes false proactive safety flags (bonus); SHOULD-HAVE leaderboard-join guard → E-267-04 AC-6; tryout-cut = accepted benign fallback (TN-12). SE: E-267-06 re-hinted data-engineer→software-engineer (one story, reset.py precedent, no schema), command name PINNED `bb db purge-scouting` (propagated to E-267-05 AC-3), + implementation ACs (explicit `PRAGMA foreign_keys=ON` + reorder-raises test AC-3; fresh `is_production()` guard not reset.py's body AC-4; unlink mechanism not `cleanup_expired_reports()`, before the reports DELETE AC-5; single transaction AC-6; partition drift test AC-7; patchable `_REPO_ROOT` seam AC-8). Codex P3: TN-7 regression-test requirement scoped to CODE stories, E-267-05 docs EXEMPT. Codex P2: E-267-05 AC-3 states the `user_team_access` purge consequence. Consistency sweep complete. Still DRAFT — main session's spec audit + Codex re-run gates READY.
- 2026-07-19: api-scout ran both game-grain probes LIVE (633 records; TN-11 Probe 1/2). Probe 1 CLOSED the postponed false-delete risk — GC keeps not-final/unplayed games in the array, so absent-from-full-array = genuine removal (E-267-02 AC-6 discharged from a pending guard to a confirmed rule; field-shape corrected: `game_status` key can be ABSENT / `null` / `"new"`). Probe 2 (reschedule `event_id`-survival, strong inference) reshaped E-267-02 AC-3 + TN-3 to prefer stable-`event_id` in-place date UPDATE for same-perspective (no delete), cross-perspective still on the natural-key merge; delete-fallback + one spot-check retained. E-267-02 game-grain probe is no longer a hard pre-dispatch blocker (E-267-04 DE roster calibration was still open at that point — SINCE RESOLVED in the RE-PLAN entry: cap LOCKED `MAX_ROSTER_DEPARTURES = 2`, so NO open pre-dispatch blockers remain). Awaits the main session's spec-review re-run to re-confirm READY.
