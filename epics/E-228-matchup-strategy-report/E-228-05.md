# E-228-05: CUT (was: Deterministic matchup engine -- dataclasses + `compute_matchup()`)

## Status
`CUT`

## Cut Reason
Removed during E-228 v1 refinement (2026-04-28). Folded into **E-228-12** (combined matchup engine + input builder). The original story's three-gate LSB starter prediction (G1 rest / G2 ≥2 GS / G3 K/BB recipe match), 5-bucket loss recipe, decision_unknown 6th bucket, 4-template TL;DR slot-2 priority tree, and 14-constant tuning framework were ALL cut from v1 -- the deterministic engine in v1 produces a much smaller signal set (top-3 hitters, pull-tendency notes, stolen-base profile, first-inning pattern, 3-bucket loss recipe, eligible pitchers).

The dropped scope is captured in epic stub **E-230** (`epics/E-230-matchup-engine-v2/`) for v2 planning after v1 ships.

See `epic.md` History entry "2026-04-28" for the full v1 refinement context.
