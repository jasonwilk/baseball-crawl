# E-228-08: CUT (was: Generator orchestration -- wire matchup into `generate_report()` flow)

## Status
`CUT`

## Cut Reason
Removed during E-228 v1 refinement (2026-04-28). Folded into **E-228-14** (combined generator orchestration + renderer + docs). The `our_team_id=None` contract conflict (epic + E-228-08 said "skip matchup entirely" while E-228-06 preserved an opponent-only degraded path) was resolved during refinement: matchup requires LSB context; when `our_team_id` is None, the matchup section does not render and the existing scouting report renders byte-identically to today.

See `epic.md` History entry "2026-04-28" for the full v1 refinement context.
