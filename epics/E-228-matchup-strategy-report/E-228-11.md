# E-228-11: Context-layer closure pass -- rule updates

## Epic
[E-228: Matchup Strategy Report](./epic.md)

## Status
`TODO`

## Description
After this story is complete, the context layer reflects the matchup-strategy-report addition: `key-metrics.md` defines the matchup-engine derived signals that stabilized during implementation, `architecture-subsystems.md` extends the "Two-Tier Enrichment Pattern" section to list both predicted-starter AND matchup as canonical instances, and `scouting-data-flows.md` adds matchup as a concrete reports-only example. CLAUDE.md gets a canonical-helper line ONLY if `compute_matchup()` ends up with single-entry-point semantics by the end of implementation.

## Context
Per architect's discovery transcript (2026-04-27), the matchup epic has light context-layer impact: rule extensions, not new rules. All edits are bundled into this single story for a clean reviewable diff.

This story is the final piece before close. It runs after all code stories AND user-facing docs (E-228-10) are DONE, so the actual stabilized signals are visible and the doc surface is settled before context-layer codification.

## Acceptance Criteria

### Behavior

- [ ] **AC-1 (per Coach B-1 + Coach M-2 + ARCH-LOW-2 stabilization criterion)**: `.claude/rules/key-metrics.md` extended with matchup-engine derived signal definitions where the DEFINITION has stabilized (even if specific threshold values may evolve post-ship). Codify with a "thresholds are coach-tunable; current values in [code reference]" caveat. Specifically:
  - **"Loss recipe" categorization (5 buckets + decision_unknown fallback)**: use the canonical bucket names `starter_blew_up`, `pitching_duel_lost_late`, `bullpen_burn`, `got_blasted`, `close_loss` -- NOT the prior wrong list (`starter_blew_up / pitching_duel_lost_late / bullpen_burn / got_blasted / close_loss / decision_unknown`). Thresholds (HS-calibrated per Coach S-1, S-2): `bullpen_burn` uses ≥3 distinct pitchers (HS-norm, NOT MLB ≥4); `got_blasted` uses ≥6 (NOT ≥8). Codify the bucket DEFINITIONS; flag thresholds as "HS-calibrated, see `src/reports/matchup.py` for current values."
  - **"Three-gate starter prediction"**: rest cleared, ≥2 GS, K/BB recipe match within 30%. K/BB (NOT K/9) — variable name `target_k_per_bb` per Coach B-2.
  - **Top-hitter ranking signal**: OPS = OBP+SLG, computed in SQL via `get_top_hitters` helper; PA floor 10 (HS-calibrated).
  - **Defensive-shift trigger**: `_PULL_SHIFT_THRESHOLD = 0.55` — flag as HS-calibrated per Coach M-2; future cross-level work will need recalibration.
  - **decision_unknown fallback**: engine flags but never guesses pitcher of record when `player_game_pitching.decision IS NULL`.
  - **Stabilization criterion (per ARCH-LOW-2)**: a signal is "stabilized" when the DEFINITION is fixed (codify with caveat). A signal is "not stabilized" only when both the DEFINITION and the values are still being explored at ship time — defer until first-real-game feedback.
- [ ] **AC-2**: `.claude/rules/architecture-subsystems.md` "Two-Tier Enrichment Pattern" section extended to list both `predicted_starter` (`src/reports/starter_prediction.py` + `src/reports/llm_analysis.py`) AND `matchup` (`src/reports/matchup.py` + `src/reports/llm_matchup.py`) as canonical Tier 1/Tier 2 instances
- [ ] **AC-3**: `.claude/rules/scouting-data-flows.md` "Feature parity principle" section gets ONE new sentence: "matchup strategy is a reports-only feature because it depends on Tier 2 LLM synthesis with multi-second latency."
- [ ] **AC-4 (per ARCH-LOW-1 sharpened criterion)**: Add a CLAUDE.md canonical-helper line for `compute_matchup()` ONLY IF at least one of these three is true at end of implementation:
  - (a) `compute_matchup()` is the sole entry point — exactly one call site outside `src/reports/matchup.py` (in `generator.py`)
  - (b) Codebase has duplicate-implementation potential — another module would naturally compute matchup signals if not directed elsewhere
  - (c) Coach-domain logic in `compute_matchup()` is non-trivial enough that a future developer might re-implement without realizing the canonical version exists
  Default: NOT adding the line. Bar is "someone could plausibly write a parallel implementation," not "this is an important function." Mirrors the cascade_delete_team / ensure_team_row precedent.
- [ ] **AC-5 (per P1-C: edits MOVED from E-228-02 + strengthened to "edits" not "verified-only")**: This story PERFORMS the CLAUDE.md and `architecture-subsystems.md` edits that were initially scoped to E-228-02 but moved here per the routing-precedence collision. Specifically:
  - **CLAUDE.md "Scouting pipeline parity" line**: edit to reflect six stages (scouting crawl/load, gc_uuid resolve, spray crawl/load, post-spray dedup, plays crawl/load + reconcile)
  - **`.claude/rules/architecture-subsystems.md` "Scouting Pipeline" section**: edit to list six stages including the plays + reconciliation step
  - These are EDITS (not verified-only). E-228-02 does NOT touch CLAUDE.md or this rule file.
- [ ] **AC-6**: Per-trigger context-layer assessment recorded in the epic's History section per `.claude/rules/context-layer-assessment.md`. All six triggers get explicit yes/no verdicts based on actual implementation findings, not pre-implementation expectations. Expected (subject to verification):
  1. New convention/pattern/constraint: YES (citation-inline pattern, three-gate starter prediction)
  2. Architectural decision with ongoing implications: YES (Path C additive opt-in)
  3. Footgun/failure mode/boundary discovered: TBD -- update based on what surfaced during implementation
  4. Change to agent behavior/routing/coordination: NO
  5. Domain knowledge for future epics: YES (loss-recipe classification, recipe-match logic)
  6. New CLI command/workflow: YES (`bb report generate --our-team`, `--starter`)
- [ ] **AC-7**: Documentation impact assessment per `.claude/rules/documentation.md` recorded in History. By the time this story runs, E-228-10 has shipped user-facing docs -- this AC verifies and records "documentation assessment: complete via E-228-10."

### Test Coverage

- [ ] **AC-T1 (rule-file lint)**: After edits, run any existing context-layer file lints (e.g., markdownlint, frontmatter validators); assert no warnings introduced.
- [ ] **AC-T2 (cross-reference verification)**: For each rule cross-reference added or updated (e.g., key-metrics.md mentioning the matchup engine), assert the referenced file path exists. **Reviewer meaningfulness**: catches stale references where a file was renamed but the rule wasn't updated.

### Documentation

- [ ] **AC-D1**: Each rule edit is reviewable as a single commit; the commit message identifies which trigger fired
- [ ] **AC-D2**: The History section update in `epics/E-228-matchup-strategy-report/epic.md` is concrete, evidence-based (cites which stories surfaced which signals), not generic boilerplate

## Technical Approach
- This story is pure context-layer editing -- no source code changes
- Read every story's actual implementation result (not the spec) to determine which signals stabilized vs which are still tunable
- The CLAUDE.md decision (AC-4) is the only judgment call -- err on the side of NOT codifying

## Dependencies
- **Blocked by**: E-228-10 (user-facing docs must ship before context-layer codification reflects the documented surface)
- **Blocks**: None (this is the last story; closure assessment + archive happens after)

## Files to Create or Modify
- `.claude/rules/key-metrics.md` (extend with matchup signals — definitions stabilized per AC-1's stabilization criterion)
- `.claude/rules/architecture-subsystems.md` (AC-2: extend Two-Tier Enrichment Pattern; AC-5: edit Scouting Pipeline section to six stages)
- `.claude/rules/scouting-data-flows.md` (AC-3: one sentence in Feature parity principle)
- `CLAUDE.md` (AC-4: canonical-helper line conditional on three-criterion test; AC-5: pipeline parity invariant edit to six stages — NOT verification, this story PERFORMS the edit per P1-C)
- `epics/E-228-matchup-strategy-report/epic.md` (History section verdicts)

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Per-trigger context-layer verdicts filled in based on implementation reality
- [ ] No source code changes in this story (all changes are context-layer files)
- [ ] All edits peer-reviewable in a single commit

## Notes
Per `.claude/rules/agent-routing.md` Routing Precedence, this story MUST route to `claude-architect` -- all touched files are context-layer paths.

If implementation surfaces an unanticipated context-layer change (e.g., a new agent capability, a new hook), it might not fit this story and should spawn a follow-up story OR be added to AC-1 with a note.
