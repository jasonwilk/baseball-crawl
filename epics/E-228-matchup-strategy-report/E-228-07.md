# E-228-07: CUT (was: LLM Tier 2 wrapper -- `enrich_matchup()` with hallucination guardrails)

## Status
`CUT`

## Cut Reason
Removed during E-228 v1 refinement (2026-04-28). Renumbered to **E-228-13** with a tightened scope. The hallucination guardrail (player-name round-trip) and the OpenRouter integration mirror remain non-negotiable; the JSON output schema is simplified in v1 (single coach-voice paragraph + per-hitter cues + sub-section prose). The LLM wrapper signature was clarified during refinement to take `(MatchupAnalysis, MatchupInputs)` together -- this resolves the original Codex-flagged spec contradiction where AC-1 took only the analysis but AC-3 required the prompt to render inputs tables.

See `epic.md` History entry "2026-04-28" for the full v1 refinement context.
