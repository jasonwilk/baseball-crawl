# E-228-09: CUT (was: Renderer + Jinja2 "Game Plan" section -- inline citations and recent-form overlay)

## Status
`CUT`

## Cut Reason
Removed during E-228 v1 refinement (2026-04-28). Folded into **E-228-14** (combined generator orchestration + renderer + docs) with a tightened visual scope:
- The full UX visual hierarchy spec (font sizes, AVOID inline sub-bullet typography, 7pt small caps, B&W print survival), print stylesheet hardening (`break-inside: avoid` per sub-section AND per-hitter), mobile responsive at 375px viewport, and head-to-head banner/sidebar design are ALL deferred to **E-231** (`epics/E-231-matchup-visual-polish/`).
- The recent-form arrow overlay (Unicode ↑↔↓ at 0.100 OBP swing threshold, last-7G window) is deferred to **E-230** (`epics/E-230-matchup-engine-v2/`) along with the 4-template TL;DR slot-2 priority tree.
- The byte-identical baseline-fixture machinery (substitution scheme + regen script + checked-in fixture) is deferred to E-231; v1 ships with a lighter regression check that proves "matchup-off path produces the same report it does today."
- The suppress-with-placeholder vs suppress-hides contradiction (AC-1 vs AC-6) was resolved during refinement: suppress hides the section entirely, no placeholder.

The v1 renderer ships with light visual treatment using existing report typography classes.

See `epic.md` History entry "2026-04-28" for the full v1 refinement context.
