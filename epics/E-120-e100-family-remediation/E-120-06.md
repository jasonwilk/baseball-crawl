# E-120-06: Architecture documentation corrections

## Epic
[E-120: E-100 Family Code Review Remediation](epic.md)

## Status
`TODO`

## Description
After this story is complete, `docs/admin/architecture.md` will document the `opponent_links` table, have an accurate sub-nav description, correctly characterize `url_parser.py` as a pure string parser, include port 8001 access and complete teams table columns. `docs/admin/operations.md` will document bare UUIDs as valid URL parser input.

## Context
Six documentation issues across admin docs: (1) `architecture.md` does not document the `opponent_links` table (added by E-100/E-114). (2) The sub-nav description says "Users and Teams" but omits the Opponents section. (3) `architecture.md` claims `url_parser.py` uses the HTTP session factory, but it is a pure string parser. (4) `operations.md:66-68` URL parser documentation omits bare UUIDs as valid input. (5) `architecture.md:16` does not mention direct port 8001 access. (6) `architecture.md:110-122` teams table listing omits `source`, `last_synced`, `created_at` columns.

## Acceptance Criteria
- [ ] **AC-1**: `docs/admin/architecture.md` includes documentation for the `opponent_links` table (currently missing — `team_opponents` is documented at line 125 but `opponent_links` is not). The new section covers the table's purpose (tracking opponent resolution state), key columns (`our_team_id`, `opponent_name`, `public_id`, `resolution_method`, `is_hidden`), and its relationship to `team_opponents`. Reference `migrations/001_initial_schema.sql` for the authoritative column list.
- [ ] **AC-2**: The sub-nav description at line 158 ("Sub-navigation links Users and Teams pages") is updated to include the Opponents section that was added by E-114
- [ ] **AC-3**: Line 167 ("Both modules use the shared HTTP session factory") is corrected — only `team_resolver.py` uses the HTTP session factory; `url_parser.py` is a pure string parser (imports only `re`, `dataclasses`, `urllib.parse`). The description at line 164 is already correct; only the "Both modules" sentence needs fixing.
- [ ] **AC-4**: `docs/admin/operations.md` URL parser section (line ~66-68) documents bare UUIDs as valid input alongside the existing URL patterns
- [ ] **AC-5**: `docs/admin/architecture.md` network/access section mentions direct port 8001 access in addition to port 8000
- [ ] **AC-6**: `docs/admin/architecture.md` teams table column listing (line ~110-122) includes `source`, `last_synced`, and `created_at` columns (cross-reference `migrations/001_initial_schema.sql` for the authoritative column list)
- [ ] **AC-7**: Scan both documents for references to table names, module names, and route paths that no longer exist in `migrations/001_initial_schema.sql` or `src/api/routes/`. Fix any found; if none found, note "no additional stale references" in the commit message

## Technical Approach
Read `docs/admin/architecture.md` and `docs/admin/operations.md` in full and cross-reference against the current schema (`migrations/001_initial_schema.sql`) and module implementations. Make targeted corrections to the six identified issues. Scan both documents for any other stale references.

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Files to Create or Modify
- `docs/admin/architecture.md`
- `docs/admin/operations.md`

## Agent Hint
docs-writer

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests

## Notes
Reference `migrations/001_initial_schema.sql` for the authoritative `opponent_links` table definition.
