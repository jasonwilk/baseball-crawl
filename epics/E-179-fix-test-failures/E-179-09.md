# E-179-09: Fix test_dashboard.py Empty State and Stale Reference Failures

## Epic
[E-179: Fix Pre-Existing Test Failures from E-173 UI Changes](epic.md)

## Status
`TODO`

## Description
After this story is complete, all 8 failing tests in `tests/test_dashboard.py` will pass. Failures include a deleted template file reference (`FileNotFoundError` on `admin/opponent_connect.html`) and empty state UI assertion changes across batting, pitching, games, and opponents tabs.

## Context
This file tests the main coaching dashboard (not routes or opponent detail -- those are in separate files). Failures fall under TN-1 categories 2 (stale route reference) and 7 (behavioral drift in empty state UI). Specific failures:

- `TestTemplateStaleRefs::test_admin_opponent_connect_no_display_name` -- fails with `FileNotFoundError` because it reads the deleted template `admin/opponent_connect.html`. E-173 removed this template as part of the route consolidation to `/resolve`. The dashboard route (`dashboard.py:~1345`) still intentionally builds `/connect` URLs as admin shortcuts, so the `/connect` URL itself is not stale -- only the template file is gone.
- `TestJerseyNumberColumn::test_batting_empty_state_shows_yellow_card` -- empty state UI assertion
- `TestJerseyNumberColumn::test_pitching_empty_state_shows_yellow_card` -- empty state UI assertion
- `TestGameDetailJerseyNumber::test_game_detail_batting_table_has_jersey_column` -- game detail table assertion
- `TestEmptyStateUI::test_batting_tab_shows_yellow_card_for_no_data_team` -- empty state
- `TestEmptyStateUI::test_pitching_tab_shows_yellow_card_for_no_data_team` -- empty state
- `TestEmptyStateUI::test_games_tab_shows_yellow_card_for_no_data_team` -- empty state
- `TestEmptyStateUI::test_opponents_tab_shows_yellow_card_when_no_opponents_and_no_data` -- empty state

## Acceptance Criteria
- [ ] **AC-1**: `TestTemplateStaleRefs::test_admin_opponent_connect_no_display_name` passes. The test fails because it reads the deleted template `admin/opponent_connect.html`. Fix by updating the test to reference the replacement template (`admin/opponent_resolve.html`) or by removing the test if it is no longer relevant after the route consolidation. The `/connect` URL is still valid (dashboard still builds it) -- only the template file was removed.
- [ ] **AC-2**: All `TestJerseyNumberColumn` and `TestEmptyStateUI` empty state tests pass with assertion text updated to match current template output.
- [ ] **AC-3**: `TestGameDetailJerseyNumber::test_game_detail_batting_table_has_jersey_column` passes.
- [ ] **AC-4**: Running `pytest tests/test_dashboard.py -v` produces 0 failures, 0 errors.

## Technical Approach
Read the current dashboard templates to determine the exact empty state card text. Update all assertion strings to match. For `TestTemplateStaleRefs::test_admin_opponent_connect_no_display_name`, the failure is `FileNotFoundError` from reading deleted `admin/opponent_connect.html`. Update the test to reference `admin/opponent_resolve.html` instead, or remove it if the test scenario no longer applies after the route consolidation to `/resolve`.

## Dependencies
- **Blocked by**: None
- **Blocks**: E-179-07

## Files to Create or Modify
- `tests/test_dashboard.py`

## Agent Hint
software-engineer

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests
