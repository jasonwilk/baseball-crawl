# E-179-04: Fix test_dashboard_routes.py Empty State and Sort Test Failures

## Epic
[E-179: Fix Pre-Existing Test Failures from E-173 UI Changes](epic.md)

## Status
`TODO`

## Description
After this story is complete, all failing tests in `tests/test_dashboard_routes.py` will pass. Failures include empty state messages, opponent detail sort parameter tests, and any label/template changes on dashboard routes.

## Context
This file tests the coaching dashboard routes. Failures span TN-1 categories 1 (label renames in empty state messages and opponent sections), 4 (missing mocks if routes trigger background tasks), and 5 (data model changes affecting template rendering). The `TestOpponentDetailSortParams` class has 8 failing tests -- the sort logic itself is unchanged, but the template's CSS class order changed causing the name-extraction regex in tests to mismatch. The fix is updating test assertions to match the current template class ordering, not changing sort behavior. See TN-5 for confirmed current template text values.

## Acceptance Criteria
- [ ] **AC-1**: `TestEmptyState::test_batting_no_data_shows_no_stats` and `test_pitching_no_data_shows_no_stats` pass with updated empty state message text.
- [ ] **AC-2**: All 8 tests in `TestOpponentDetailSortParams` pass (`test_default_batting_sort_avg_desc`, `test_default_pitching_sort_era_asc`, `test_batting_sort_by_hr_desc`, `test_pitching_sort_by_k9_desc`, `test_zero_ab_sorts_to_bottom_any_direction`, `test_zero_ip_sorts_to_bottom_any_direction`, `test_unrecognized_bat_sort_falls_back_to_avg_desc`, `test_unrecognized_pit_sort_falls_back_to_era_asc`). Sort endpoint URLs, fixture data, and assertion text match current route and template behavior.
- [ ] **AC-3**: Running `pytest tests/test_dashboard_routes.py -v` produces 0 failures, 0 errors.

## Technical Approach
Read `/workspaces/baseball-crawl/src/api/routes/dashboard.py` and the corresponding templates to determine current empty state text, sort parameter handling, and opponent detail route signatures. Update test URLs, fixture data, and assertion strings to match. Add mocks for any background task triggers per TN-4.

## Dependencies
- **Blocked by**: None
- **Blocks**: E-179-07

## Files to Create or Modify
- `tests/test_dashboard_routes.py`

## Agent Hint
software-engineer

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests
