# E-009-08: CLAUDE.md and E-004 Update

## Epic
[E-009: Tech Stack Redesign -- Portable, Agent-Browsable, Cloudflare-Integrated](epic.md)

## Status
`TODO`

## Description
After this story is complete, CLAUDE.md accurately reflects the confirmed technology stack
(Option B: Docker + Cloudflare Access), local development commands, and production
deployment reference. The E-004 epic's Technical Notes and Overview are updated to target
FastAPI + Jinja2 instead of Cloudflare Workers + Pages. E-002 and E-003 epics have
history entries noting the D1/Wrangler references are superseded. The project documentation
is fully coherent with the implemented stack.

## Context
This is the PM-owned cleanup story that closes E-009. All implementation stories
(E-009-02 through E-009-07) must be DONE before this story runs, so the CLAUDE.md
commands it documents are verified and accurate.

Documents to update:
1. **`CLAUDE.md`**: Tech Stack section, Deployment Target section, Commands section.
   These currently reflect the old Cloudflare-native plan or are empty ("To be populated").
2. **`/epics/E-004-coaching-dashboard/epic.md`**: Technical Notes must be rewritten to
   reference the E-009 stack choices. Overview sentence references D1/Workers.
3. **`/epics/E-002-data-ingestion/epic.md`**: Add a history entry noting the load stories
   (E-002-06, E-002-07) must target SQLite via `apply_migrations.py` schema, not D1.
4. **`/epics/E-003-data-model/epic.md`**: Already has an architecture superseded note
   (auto-added during E-009 execution); verify it is accurate and complete.
5. **PM MEMORY.md**: Update the key architectural decisions to reflect the verified stack.

## Acceptance Criteria

- [ ] **AC-1**: `CLAUDE.md` Tech Stack section is updated to accurately reflect Option B:

  ```
  ## Tech Stack
  - Python for data extraction, processing, and the serving layer (end-to-end Python)
  - FastAPI + Jinja2 for the API and coaching dashboard (server-rendered HTML)
  - SQLite (host-mounted Docker volume, WAL mode) for structured data storage
  - Traefik as the internal reverse proxy (Host header routing, no exposed ports)
  - Cloudflare Tunnel (cloudflared) + Zero Trust Access for all inbound traffic and auth
  - Docker Compose for local development and production deployment (same compose file)
  - Linux VPS (Hetzner CX11 or equivalent) for production hosting
  ```

  The old references to "Cloudflare D1", "Cloudflare Workers", and "TypeScript/JavaScript
  for Workers and Pages" are removed or replaced.

- [ ] **AC-2**: `CLAUDE.md` Deployment Target section is updated:

  ```
  ## Deployment Target
  - **Local dev**: `docker compose up` starts the full stack at http://localhost:8000
  - **Production**: Docker Compose on a Hetzner CX11 VPS (or equivalent Linux server)
  - **Network**: Cloudflare Tunnel (no exposed ports); Zero Trust Access for auth
  - **Database**: SQLite at `./data/app.db` (host-mounted, WAL mode, Litestream backup)
  - See `docs/production-deployment.md` for the verified deployment runbook
  ```

- [ ] **AC-3**: `CLAUDE.md` Commands section is updated with verified working commands:

  ```
  ## Commands
  - `docker compose up` -- start full stack locally (app at http://localhost:8000)
  - `docker compose up -d` -- start in detached mode
  - `docker compose --profile backup up -d` -- start with Litestream backup (production)
  - `docker compose down` -- stop all services
  - `python scripts/reset_dev_db.py` -- reset dev database to seeded state
  - `pytest` -- run all tests (no Docker required)
  - `python migrations/apply_migrations.py` -- apply pending schema migrations
  ```

- [ ] **AC-4**: `/epics/E-004-coaching-dashboard/epic.md` Technical Notes section is
  updated to specify the correct stack:
  - Dashboard framework: FastAPI + Jinja2 (not Cloudflare Pages + Workers)
  - CSS: Tailwind CDN (no build step for MVP)
  - Data layer: SQLite via the same database as the API (`./data/app.db`)
  - Local dev: `docker compose up` at `http://localhost:8000`
  - Agent browsability: WebFetch `http://localhost:8000/dashboard` per
    `docs/agent-browsability-workflow.md`
  - MVP scope: Server-rendered HTML stat tables and cards; no client-side JS required

- [ ] **AC-5**: `/epics/E-004-coaching-dashboard/epic.md` Overview and Background sections
  are updated. The phrase "Cloudflare Workers" or "Cloudflare Pages" in the context of
  the serving layer is replaced with "FastAPI + Jinja2 on Docker." If E-004 has no stories
  yet (it should not -- DRAFT with no story files), note in its Background that it is
  blocked pending E-009 completion and the stack is now confirmed.

- [ ] **AC-6**: `/epics/E-002-data-ingestion/epic.md` History section has a new entry
  (use the actual date this story is executed, not a hardcoded date):

  ```
  - <YYYY-MM-DD>: E-009 stack decision (Option B) changes the load target. E-002-06
    and E-002-07 (load stories) must target SQLite via the `apply_migrations.py`
    schema (E-009-02), not Cloudflare D1. Before dispatching those stories, update
    their Technical Approach to reference `apply_migrations.py` and the SQLite
    connection pattern from `src/api/db.py`.
  ```

- [ ] **AC-7**: PM `MEMORY.md` at `/.claude/agent-memory/product-manager/MEMORY.md` has
  the E-009 section updated to reflect:
  - All stories E-009-02 through E-009-08 written (and their statuses)
  - E-009 approaching COMPLETED (set to COMPLETED when this story is DONE)
  - The verified stack choices and key file paths

- [ ] **AC-8**: All changes are coherent: after this story, there is no document in the
  project that still references "Cloudflare D1" or "Cloudflare Workers" as the serving
  layer without a note that the reference is superseded or historical.

## Technical Approach

This story is PM-only. No code is written. Steps:

1. Read E-009-02 through E-009-07 story files to verify the implemented stack and exact
   file paths before updating docs (commands must be accurate).
2. Read current `CLAUDE.md` and identify every section that references the old stack.
3. Edit `CLAUDE.md` with the verified commands and stack descriptions (AC-1, AC-2, AC-3).
4. Read `/epics/E-004-coaching-dashboard/epic.md` and update Technical Notes and Overview
   (AC-4, AC-5).
5. Add history entry to `/epics/E-002-data-ingestion/epic.md` (AC-6).
6. Verify E-003 epic history note (added automatically) is accurate (AC-8).
7. Update PM MEMORY.md (AC-7).
8. Mark E-009-08 DONE and E-009 epic COMPLETED.

## Dependencies

- **Blocked by**: E-009-07 (must be DONE -- this story documents verified commands and
  references the production runbook). In practice this means all of E-009-02 through
  E-009-07 must be DONE before this story starts.
- **Blocks**: Nothing. This is the final story in E-009.

## Files to Create or Modify

Modified files:
- `/Users/jason/Documents/code/baseball-crawl/CLAUDE.md`
- `/Users/jason/Documents/code/baseball-crawl/epics/E-004-coaching-dashboard/epic.md`
- `/Users/jason/Documents/code/baseball-crawl/epics/E-002-data-ingestion/epic.md`
- `/Users/jason/Documents/code/baseball-crawl/epics/E-009-tech-stack-redesign/epic.md`
  (update Stories table statuses, move epic to COMPLETED when all stories DONE)
- `/Users/jason/Documents/code/baseball-crawl/.claude/agent-memory/product-manager/MEMORY.md`
- `/Users/jason/Documents/code/baseball-crawl/epics/E-009-tech-stack-redesign/E-009-08.md`
  (this file -- update Status to DONE)

No new files are created.

## Definition of Done

- [ ] All acceptance criteria pass
- [ ] CLAUDE.md reviewed for any remaining stale references (global search for "D1",
  "Workers", "wrangler", "TypeScript" in serving-layer context)
- [ ] E-009 epic status updated to COMPLETED
- [ ] E-009 epic moved to `/.project/archive/E-009-tech-stack-redesign/` after all
  stories are DONE
- [ ] PM MEMORY.md updated
- [ ] Ideas review triggered: check `/.project/ideas/README.md` for newly unblocked
  candidates now that E-009 is complete

## Notes

- This story must run after E-009-07 is DONE so the commands in CLAUDE.md are verified
  against the real running stack. Do not write commands that have not been tested.
- When setting E-009 to COMPLETED and archiving, move the entire
  `/epics/E-009-tech-stack-redesign/` directory to `/.project/archive/`. Never delete.
- After archiving E-009, review `/.project/ideas/README.md`. IDEA-003 (Work Management
  as Agent Interface) and IDEA-002 (Web Scraping Fallback) should be reassessed.
- E-004 re-scoping: E-004 is currently DRAFT with no story files. After this story updates
  E-004's Technical Notes, the PM should write E-004 stories in a separate session once
  E-002 and E-003 are DONE and the data layer is queryable.
- Size: Small. All changes are documentation edits. The time cost is in reading the
  current state of all affected files carefully before editing.
