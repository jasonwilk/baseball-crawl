# E-230-03: Fix Game-Coverage Indicator Assertions to "(4 games)"

## Epic
[E-230: Fix the 56 Post-RTK Test-Suite Failures](../E-230-test-suite-fix/epic.md)

## Status
`TODO`

## Description
After this story is complete, the two game-coverage indicator tests assert the correct game count that `get_game_coverage()` actually returns given the test fixture. The fixture seeds four completed opponent games, the production code correctly counts four, and the stale assertions (which expect three) are corrected to "(4 games)". No production code changes.

## Context
This is Cluster 3 from discovery (2 failures): `tests/test_dashboard.py::TestGameCoverageIndicator::test_opponent_detail_shows_coverage_indicator` and `::test_opponent_print_shows_coverage_indicator`. Both assert `"Through Mar 1 (3 games)"`. The fixture seeds a 4th completed opponent game (`game-opp-2025`, `tests/test_dashboard.py:277`) that was added later for a different test; `get_game_coverage()` correctly returns `game_count=4`, so production and the format string are both correct — only the assertions are stale. See epic Technical Notes "Cluster 3". The intended behavior is all-time coverage; `get_game_coverage()` is NOT to be changed.

## Acceptance Criteria
- [ ] **AC-1**: `test_opponent_detail_shows_coverage_indicator` and `test_opponent_print_shows_coverage_indicator` (assertions at `tests/test_dashboard.py:3248` and `:3266`) assert the coverage string with `"(4 games)"` (matching the four completed games the fixture seeds and the count `get_game_coverage()` returns). The stale test docstring at `tests/test_dashboard.py:3242` (currently `"Through Mar 1 (3 games)"`) is also updated to `"(4 games)"`, so no stale `(3 games)` reference is left behind.
- [ ] **AC-2**: No change to `get_game_coverage()` or any production code — the fix is assertion-only, per epic Technical Notes "Cluster 3".
- [ ] **AC-3**: `python -m pytest tests/test_dashboard.py` reports 0 failed, and the full `python -m pytest tests/` shows these 2 failures cleared with no regressions. No `-x` / `-k` narrowing used as evidence.

## Technical Approach
Apply the decision framework in epic Technical Notes (reproduce → confirm `get_game_coverage()` returns 4 against the seeded fixture → fix the assertion, not the code). Verify the "Mar 1" date portion of the assertion still matches the fixture's latest completed game date; correct only the game-count portion unless the date is also stale.

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Files to Create or Modify
- `tests/test_dashboard.py` (two assertions at `:3248`/`:3266` + the docstring at `:3242` in `TestGameCoverageIndicator`)

## Agent Hint
software-engineer

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests

## Notes
Trivial assertion fix. Guard against masking a real regression: confirm first-hand that `get_game_coverage()` genuinely returns 4 for the seeded fixture (it counts all completed opponent games, all-time) before changing the assertion — do not blindly bump the number to make the test green.
