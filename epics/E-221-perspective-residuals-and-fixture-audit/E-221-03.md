# E-221-03: CI guardrail forbidding inline CREATE TABLE in tests/

## Epic
[E-221: Test Fixture Schema Parity Audit + Post-E-220 Perspective Residuals](../E-221-perspective-residuals-and-fixture-audit/epic.md)

## Status
`DONE`

## Description
After this story is complete: (1) the remaining test files containing inline `CREATE TABLE` are classified as "legitimate exceptions" (where inline schema is essential to the test's purpose, e.g., migration-runner tests) or "drift" (where the test should use `load_real_schema`), with drift files migrated in-place and legitimate files pragma-exempted; (2) a CI-enforced check fails any PR that introduces a new inline `CREATE TABLE` outside of the documented exception set. This eliminates residual fixture-schema-parity drift and prevents future regressions.

## Context
Story 02 migrates the 7 current offenders. Without a guardrail, future contributors (agents or humans) will re-introduce the pattern out of convenience. The enabling condition for round-by-round discovery during E-220 was precisely this drift. Without a check, we will hit it again.

The check should be noisy and clear: a failing test or lint step with a message naming the offending file, line number, and an explanation pointing at the shared helper.

## Acceptance Criteria
- [ ] **AC-1**: Given a PR that adds a file `tests/test_something.py` containing `CREATE TABLE foo (...)` outside of approved exceptions, when the test suite runs, then the guardrail check fails with a message that identifies the file and line.
- [ ] **AC-2**: Given the current repo state (post-Story-02), when the guardrail check runs, it passes. No false positives on legitimate usages in the shared helper or explicitly-exempted files.
- [ ] **AC-3**: The guardrail is implemented as a pytest test (not a shell script, not a pre-commit hook) so it runs in the same harness as the rest of the suite and cannot be skipped by a caller who forgets to run pre-commit hooks.
- [ ] **AC-4**: The guardrail allows a file-level opt-out pragma comment (e.g., `# noqa: fixture-schema` at the top of a file) for legitimate exceptions. Each pragma-exempted file must include a short rationale in its module docstring explaining WHY inline schema is essential to the test's purpose (e.g., "This file tests the migration runner itself; inline schema is the subject under test, not drift from it.").
- [ ] **AC-5**: The guardrail test's own failure message suggests the fix: "Use `load_real_schema(conn)` from `tests/conftest.py` instead. See E-221 for context."
- [ ] **AC-6**: The remaining test files that contained inline `CREATE TABLE` at the start of this story (as enumerated during dispatch) are each processed to one of two states: (a) migrated to `load_real_schema(db)` per the Story 02 pattern, fixing any NOT NULL / FK hygiene bugs surfaced along the way; or (b) retained with inline schema and pragma-exempted via `# noqa: fixture-schema` plus a docstring rationale. The final guardrail test passes cleanly — no uncategorized files remain.

## Technical Approach
Per TN-3 in the epic. Implement as `tests/test_no_inline_schemas.py` (or equivalent name). Walk `tests/**/test_*.py` with `Path.rglob`, read each file, grep for `CREATE TABLE` via a simple substring or regex check, assert none found except in allowed locations.

Keep the test under 50 lines. Do NOT attempt to parse SQL or handle complex edge cases — a substring check on the literal `CREATE TABLE` is sufficient because agents and humans write it literally, not via string concatenation.

**Additional scope (E-221-02 left residual drift)**: After Story 02, 15 additional test files still contain inline `CREATE TABLE` beyond the 7 listed in Story 02. This story expands to cover them. Expected classification split: files under `test_migrations.py`, `test_backup.py`, and similar that test schema-creation machinery itself are legitimate exceptions; files that use inline CREATE as a convenience shortcut for fixture setup are drift. Inspect each file's test content, not just its name, before classifying.

**Drift file enumeration (as of 2026-04-12 dispatch)**:

1. tests/test_api_health.py
2. tests/test_auth.py
3. tests/test_backup.py
4. tests/test_cli_data.py
5. tests/test_db.py
6. tests/test_dedup_integration.py
7. tests/test_ensure_team_row.py
8. tests/test_migrations.py
9. tests/test_opponent_seeder.py
10. tests/test_pitching_history.py
11. tests/test_player_upsert.py
12. tests/test_report_routes.py
13. tests/test_scouting_spray_crawler.py
14. tests/test_season_id_derivation.py
15. tests/test_trigger.py

Likely classification (informal, based on filename — inspect content to confirm):
- **Candidate legitimate exceptions**: test_migrations.py (tests migration runner), test_backup.py (tests backup script that creates DBs), test_trigger.py (tests SQL triggers), test_auth.py (may own auth-tables-only schema).
- **Candidate drift**: test_api_health.py, test_cli_data.py, test_db.py, test_dedup_integration.py, test_ensure_team_row.py, test_opponent_seeder.py, test_pitching_history.py, test_player_upsert.py, test_report_routes.py, test_scouting_spray_crawler.py, test_season_id_derivation.py.

SE must inspect each file's test content — not just the filename — before classifying. Any file's classification may differ from the above guess.

If SE's re-grep at dispatch start returns a count other than 15, that signals drift between this enumeration and the dispatch moment. SE should report the delta and pause for main-session adjudication.

## Dependencies
- **Blocked by**: E-221-02
- **Blocks**: None

## Files to Create or Modify
- `tests/test_no_inline_schemas.py` (new)
- 15 test files requiring migration or pragma-exemption (enumerated during dispatch)

## Agent Hint
software-engineer

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests
