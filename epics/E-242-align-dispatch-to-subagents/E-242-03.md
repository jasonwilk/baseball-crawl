# E-242-03: CLAUDE.md flag note + agent-memory platform facts

## Epic
[E-242: Align Dispatch/Plan/Implement Workflow to Subagent Framing](../E-242-align-dispatch-to-subagents/epic.md)

## Status
`TODO`

## Description
After this story is complete, `CLAUDE.md` carries a durable one-liner documenting that dispatch relies on long-lived resumable named subagents and the `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` flag dependency, and claude-architect's agent-memory platform facts are corrected (the backwards nested-teams claim, the removed `TeamCreate`/`TeamDelete` tools, and the flag→team-coordination-tools (`SendMessage` + shared `Task*` list) dependency). (The corresponding dated tooling note for the PM's own `lessons-learned.md` is handled by PM at closure, not in this story — see epic Technical Notes TN-9.)

## Context
The flag dependency must be stated once durably where it is most discoverable (`CLAUDE.md` Agent Ecosystem), not repeated in every spawn block (Technical Notes TN-4). CA's agent-memory currently carries a platform fact that is now BACKWARDS: it claims teammates can spawn teammates with no nesting limit. The current platform reality is the opposite — only the main session (lead) spawns subagents; subagents cannot spawn their own (no nesting). Leaving the stale fact in place would mislead future context-layer work. (The corresponding dated tooling note for the PM's own `lessons-learned.md` is NOT part of this story — PM adds it during closure as a normal PM-memory update, honoring the "PM updates its own memory" routing carve-out; see epic Technical Notes TN-9.)

## Acceptance Criteria
- [ ] **AC-1**: The `CLAUDE.md` Agent Ecosystem section gains exactly one durable one-liner stating that dispatch relies on long-lived resumable named subagents (PM and code-reviewer persist across an epic) and that this requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (set in `.claude/settings.json`), without which the team-coordination tools (`SendMessage` and the shared `Task*` task list) are unavailable and spawned agents are one-shot, per Technical Notes TN-4. The statement appears exactly once and states the fact plainly (no explicit-vs-inferred editorializing — that nuance lives in the epic Open Questions record only).
- [ ] **AC-2**: In `claude-architect/MEMORY.md` Platform Facts, the nested-teams claim is corrected from "teammates CAN spawn teammates (no nesting limit)" to the current reality: only the main session (lead) spawns subagents; subagents cannot spawn their own (no nesting).
- [ ] **AC-3**: In `claude-architect/MEMORY.md` Platform Facts, the `TeamCreate`/`TeamDelete` references are removed and replaced with a dated note that they were removed in Claude Code v2.1.178 (implicit formation, automatic teardown), and the flag→team-coordination-tools dependency (`SendMessage` + shared `Task*` list) is recorded, per Technical Notes TN-5.
- [ ] **AC-4**: In `claude-architect/agent-design.md`, the Team-Communication Tools list no longer includes `TeamCreate`/`TeamDelete`; it notes implicit formation and records the flag-gating fact consistently with TN-4 and the `CLAUDE.md` note — the flag gates the team-coordination surface (`SendMessage` + the shared `Task*` task list). (F-Codex-2 RESOLVED — see epic Open Questions.)
- [ ] **AC-5**: No instructional use of `TeamCreate`/`TeamDelete` remains in any file touched by this story (a dated historical/removal note is permitted per Technical Notes TN-5).

## Technical Approach
CA's inventory (line numbers are locators at design time; verify current location before editing):
- `CLAUDE.md` Agent Ecosystem section — add the durable flag one-liner (canonical text in Technical Notes TN-4).
- `claude-architect/MEMORY.md` (Platform Facts) — (1) flip the backwards nested-teams claim; (2) remove `TeamCreate`/`TeamDelete`, note removed in v2.1.178; (3) record the flag→team-coordination-tools dependency (`SendMessage` + shared `Task*` list).
- `claude-architect/agent-design.md` (~L118–132, Team-Communication Tools; L129 lists the removed tools) — drop them, note implicit formation, add the flag-gating fact.

Note: `product-manager/lessons-learned.md` is NOT in this story. The PM-memory tooling note (and ensuring the L108 "(TeamCreate required)" parenthetical reads as history) is PM closure work per epic Technical Notes TN-9 — it honors the "PM updates its own memory" routing carve-out and removes the documented cross-ownership exception entirely.

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Handoff Context
- **Produces for E-242-01 (F-Codex-1)**: the durable `CLAUDE.md` Agent Ecosystem flag note (AC-1) is the stable runtime home that S1's skill preambles point to. Soft ordering only — the reference is to a named location, not a hard dependency; the main session may prefer to run S3 before S1 for a clean per-story read.

## Files to Create or Modify
- `CLAUDE.md` (modify)
- `.claude/agent-memory/claude-architect/MEMORY.md` (modify)
- `.claude/agent-memory/claude-architect/agent-design.md` (modify)

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Tests written and passing (N/A — context-layer documentation edits; the epic closure full-suite-green gate covers regressions)
- [ ] Code follows project style (see CLAUDE.md)
- [ ] No regressions in existing tests

## Notes
Context-layer story — code-reviewer is skipped per dispatch convention; PM verifies ACs solo. File-disjoint from E-242-01 and E-242-02 (no shared files). The PM-memory tooling note is handled by PM at closure (TN-9), not in this story, so this story carries no cross-ownership routing exception.
