# E-136-04: Update Workflow Discipline for Patch-Apply Merge-Back

## Epic
[E-136: Atomic Epic Commits](epic.md)

## Status
`TODO`

## Description
After this story is complete, the Workflow Routing Rule section in `workflow-discipline.md` will list `git apply` (patch-apply merge-back) instead of `git merge` in the main session's permitted operations.

## Context
The workflow discipline file lists the main session's permitted direct file operations, including `git merge` for merge-back. The atomic commit workflow replaces `git merge` with `git apply` (patch-apply). This reference must be updated for consistency with the implement skill (E-136-01) and dispatch pattern overview (E-136-02).

## Acceptance Criteria
- [ ] **AC-1**: The Workflow Routing Rule section lists `git apply` instead of `git merge` in the main session's permitted git operations.
- [ ] **AC-2**: All other references to merge-back in the file are consistent with patch-apply (no surviving `git merge` references for merge-back).
- [ ] **AC-3**: No other sections of the file are altered.

## Technical Approach
The workflow discipline file at `/.claude/rules/workflow-discipline.md` contains a Workflow Routing Rule section that describes the main session's permitted operations. Locate all references to `git merge` in the merge-back context and replace with `git apply`. Read the full file to ensure no other merge-back references survive.

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Files to Create or Modify
- `/.claude/rules/workflow-discipline.md` (modified)

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] No regressions in existing rule structure
- [ ] Code follows project style (see CLAUDE.md)

## Notes
This file has `paths: "**"` frontmatter -- loaded for every agent interaction. The change is small but important for cross-file consistency.
