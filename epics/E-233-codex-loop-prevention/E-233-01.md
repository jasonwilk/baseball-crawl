# E-233-01: Paired feedback memory: iteration diminishing returns

## Epic
[E-233: Codex Review Loop Prevention](../E-233-codex-loop-prevention/epic.md)

## Status
`TODO`

## Description
After this story is complete, a new feedback memory file at the user's auto-memory directory will codify the "round 3+ produces diminishing returns and risks framing flips" guidance, paired with the existing `feedback_fix_real_findings.md`. Both files will load via the same MEMORY.md index and operate in tension: small real findings still get fixed in rounds 1-2, but rounds 3+ require a sharper "is this real or is codex chasing its tail" filter.

## Context
This is the documentation foundation for E-233. It sits independently of the skill changes in stories 02 and 03 — the file documents the principle the skills enforce. Mechanical ordering doesn't matter (skill text references the file by name as a string, so 02 can ship before 01 without breaking syntactically). The desired user-experience outcome is that the file exists when the hard-stop message links to it, so 01 should land before any closure where the round-3 cap might fire.

## Acceptance Criteria
- [ ] **AC-1**: A new file `feedback_iteration_diminishing_returns.md` exists in the project's auto-memory directory (on this machine resolves to `/home/vscode/.claude/projects/-workspaces-baseball-crawl/memory/feedback_iteration_diminishing_returns.md`) with frontmatter matching Technical Notes section TN-8 (the three frontmatter fields `name`, `description`, `type: feedback`). PM verifies at the resolved path on the implementer's machine, not by literal-string match.
- [ ] **AC-2**: The body has three sections in order: rule statement, `**Why:**` block, `**How to apply:**` block, with content per Technical Notes section TN-8.
- [ ] **AC-3**: The `**How to apply:**` block contains an explicit cross-reference to `feedback_fix_real_findings.md` describing the two files as operating "in tension."
- [ ] **AC-4**: The auto-memory directory's `MEMORY.md` index file (on this machine resolves to `/home/vscode/.claude/projects/-workspaces-baseball-crawl/memory/MEMORY.md`) has a one-line pointer to the new file under the existing "Feedback" group, in the shape specified in Technical Notes section TN-8. PM verifies at the resolved path.
- [ ] **AC-5**: The pointer line in MEMORY.md is under ~150 characters (per the auto-memory system instructions).

## Technical Approach
The auto-memory directory is the user's per-machine project memory. It is NOT under `.claude/agent-memory/` and NOT version-controlled. Use absolute paths for all file operations — the directory is outside both the main checkout and the epic worktree, but inside the user's home directory.

Reference Technical Notes section TN-8 for the full frontmatter and body content.

The pointer line in MEMORY.md should be placed thematically near the existing `feedback_fix_real_findings.md` entry, so a future reader scanning the index can see the pair together.

## Dependencies
- **Blocked by**: None
- **Blocks**: None

## Files to Create or Modify
- `/home/vscode/.claude/projects/-workspaces-baseball-crawl/memory/feedback_iteration_diminishing_returns.md` (create)
- `/home/vscode/.claude/projects/-workspaces-baseball-crawl/memory/MEMORY.md` (modify — add pointer)

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] No regressions in existing memory file structure (other entries in MEMORY.md unchanged)
- [ ] File loads via the auto-memory system (visible to future Claude conversations as part of the system context)

## Notes
This file lives outside the `/workspaces/baseball-crawl` repository in the user's home directory at `/home/vscode/.claude/projects/-workspaces-baseball-crawl/memory/`. The worktree-guard hook does not block writes to this path because the path does not start with `/workspaces/baseball-crawl/`. Implementer should use absolute paths to the auto-memory directory for both reads and writes.
