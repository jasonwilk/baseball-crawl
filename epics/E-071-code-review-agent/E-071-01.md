# E-071-01: Create code-reviewer agent definition

## Epic
[E-071: Code Review Agent](epic.md)

## Status
`TODO`

## Description
After this story is complete, a new code-reviewer agent definition will exist at `.claude/agents/code-reviewer.md` with an embedded review rubric, structured findings format, adversarial review stance, and tool restrictions (Read/Glob/Grep/Bash only -- no Write/Edit). The agent is sonnet-class with a focused mandate: find issues in implementer work before stories are marked DONE.

## Context
The current dispatch workflow has no dedicated quality gate between implementer completion and DONE status. The main session's AC verification is a rubber stamp. E-064 proved this -- 5 issues survived past DONE into codex/manual review. This agent fills that gap. See epic Technical Notes for the full CA assessment including the rubric priorities, structured findings format, and circuit breaker design.

## Acceptance Criteria

### Agent definition file
- [ ] **AC-1**: `.claude/agents/code-reviewer.md` exists with valid YAML frontmatter containing: `name: code-reviewer`, `model: sonnet`, a descriptive `description` field, `color` (distinct from existing agents), `memory: project`.
- [ ] **AC-2**: The `tools` field in frontmatter lists ONLY: `Read`, `Glob`, `Grep`, `Bash`. No `Write`, no `Edit`, no `WebFetch`. The reviewer finds issues; it does not fix them.

### Identity and stance
- [ ] **AC-3**: The agent definition establishes an adversarial/skeptical review stance. The reviewer's job is to find what's wrong, not to confirm what's right. The definition explicitly contrasts this with the implementer's constructive stance.
- [ ] **AC-4**: The definition states that the reviewer verifies BOTH code quality AND acceptance criteria satisfaction. It is the quality gate, not a style checker.

### Embedded rubric
- [ ] **AC-5**: The agent definition contains an embedded review rubric with 7 priority levels (in order): (1) AC verification, (2) bugs and regressions, (3) missing/inadequate tests, (4) credential and security risks, (5) schema drift, (6) convention violations (referencing CLAUDE.md, python-style.md, testing.md), (7) planning/implementation mismatch.
- [ ] **AC-6**: The rubric instructs the reviewer to read CLAUDE.md, relevant `.claude/rules/` files, the story file, and the epic Technical Notes before reviewing. These are the reviewer's context sources.

### Structured findings format
- [ ] **AC-7**: The agent definition specifies the structured findings format with three sections: MUST FIX (blocks DONE), SHOULD FIX (recommended, does not block), and AC VERIFICATION (per-AC verdict with evidence). The output ends with an explicit APPROVED or NOT APPROVED verdict.
- [ ] **AC-8**: The format requires file:line citations for every finding.

### Circuit breaker
- [ ] **AC-9**: The agent definition documents the circuit breaker: max 2 review rounds per story. If the 2nd review still has MUST FIX findings, the reviewer reports this to the main session for escalation to the user. The reviewer does not loop indefinitely.

### Work authorization
- [ ] **AC-10**: The agent definition includes a work authorization section: the reviewer only operates when assigned a review by the main session via SendMessage. It does not self-initiate reviews.

### Anti-patterns
- [ ] **AC-11**: The agent definition includes anti-patterns: (1) never write or edit code -- find issues, don't fix them, (2) never mark stories DONE or update status files -- the main session handles statuses, (3) never approve work that has MUST FIX findings -- quality is not negotiable, (4) never review without reading the story file and CLAUDE.md first.

## Technical Approach
Create a new agent definition following the established pattern (see any existing agent in `.claude/agents/` for frontmatter format and section structure). The rubric should be embedded directly in the definition -- it IS the reviewer's core job, not an external reference. See epic Technical Notes for the complete CA assessment including rubric priorities, findings format, and circuit breaker design.

Key reference files (all absolute paths):
- `/workspaces/baseball-crawl/.claude/agents/software-engineer.md` -- peer agent for format reference (sonnet, tools list, anti-patterns section)
- `/workspaces/baseball-crawl/.claude/agents/claude-architect.md` -- peer agent for format reference
- `/workspaces/baseball-crawl/.project/codex-review.md` -- existing review rubric (priorities incorporated into the new agent's rubric)
- `/workspaces/baseball-crawl/.claude/rules/python-style.md` -- conventions the reviewer must enforce
- `/workspaces/baseball-crawl/.claude/rules/testing.md` -- testing rules the reviewer must enforce

## Dependencies
- **Blocked by**: None
- **Blocks**: E-071-02

## Files to Create or Modify
- `.claude/agents/code-reviewer.md` (new)

## Agent Hint
claude-architect

## Handoff Context
- **Produces for E-071-02**: The code-reviewer agent definition at `.claude/agents/code-reviewer.md`. E-071-02 needs to reference this agent by name in dispatch-pattern.md, implement SKILL.md, and CLAUDE.md.

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] Agent definition follows established frontmatter format
- [ ] No conflicts with existing agent definitions

## Notes
- Color must be distinct from existing agents: yellow (claude-architect), green (PM), red (baseball-coach), orange (api-scout), blue (SE), purple (data-engineer), magenta (docs-writer), cyan (ux-designer). Choose from remaining options.
- The reviewer is sonnet-class (not opus) because it does focused reads and pattern matching, not deep reasoning. This bounds cost.
- The "no Write/Edit" tool restriction is critical. If the reviewer could edit files, the separation of concerns collapses -- the reviewer becomes a second implementer.
