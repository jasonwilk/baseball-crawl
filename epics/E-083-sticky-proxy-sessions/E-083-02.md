# E-083-02: Document sticky session behavior in CLAUDE.md

## Epic
[E-083: Bright Data Sticky Proxy Sessions](./epic.md)

## Status
`TODO`

## Description
After this story is complete, CLAUDE.md will document the Bright Data sticky session behavior: how it works, that it is automatic when proxy is enabled, the session ID lifecycle (per-GameChangerClient instance), and that `bb proxy check` uses rotating IPs. This ensures all agents and the operator understand the proxy session model.

## Context
E-083-01 adds sticky session support to the proxy layer. CLAUDE.md already has proxy documentation in the "Proxy Boundary" section and the HTTP Request Discipline section. The sticky session behavior needs to be documented so agents working with the proxy or HTTP code understand the session model.

## Acceptance Criteria
- [ ] **AC-1**: CLAUDE.md contains a description of sticky session behavior within the existing proxy-related documentation, covering: (a) what sticky sessions are, (b) that they are automatic when proxy is enabled, (c) session ID lifecycle (one per GameChangerClient instance), (d) that `bb proxy check` does not use sticky sessions, (e) the 5-minute idle timeout and why no keep-alive is needed.
- [ ] **AC-2**: The documentation does not include any example proxy URLs, credentials, or credential formats -- only the conceptual behavior.
- [ ] **AC-3**: No other CLAUDE.md sections are modified beyond adding the sticky session documentation.

## Technical Approach
The sticky session documentation belongs in `/workspaces/baseball-crawl/CLAUDE.md`, within or adjacent to the existing proxy-related content. The "Proxy Boundary (Host vs. Container)" section describes the proxy architecture. A new subsection or paragraph about sticky sessions should be added there or in the HTTP Request Discipline section, whichever reads more naturally after reviewing the final E-083-01 implementation.

## Dependencies
- **Blocked by**: E-083-01 (need to see final implementation before documenting)
- **Blocks**: None

## Files to Create or Modify
- `CLAUDE.md` -- add sticky session documentation

## Agent Hint
claude-architect

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] CLAUDE.md changes are clear, accurate, and consistent with existing documentation style
- [ ] No unrelated CLAUDE.md sections modified

## Notes
- This is a context-layer-only story (CLAUDE.md). No Python code changes.
