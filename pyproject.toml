[project]
name = "baseball-crawl"
version = "0.1.0"
requires-python = ">=3.13"

[project.scripts]
bb = "src.cli:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.pytest.ini_options]
timeout = 30
