# Epic Codification Records

Per-epic record of what architectural decision was made and WHERE it was codified in the context layer (CLAUDE.md / rules / agent defs). Chronological. The MEMORY.md "Key Architectural Decisions" section keeps only foundational/cross-cutting entries; detailed per-epic codifications live here.

## E-077 — Auth module architecture
`src/gamechanger/exceptions.py` (shared exceptions to break circular imports), `signing.py` (gc-signature HMAC-SHA256), `token_manager.py` (POST /auth refresh, caching, .env write-back via `atomic_merge_env_file()`), `client.py` (lazy token fetch, 401 retry). TokenManager uses standalone httpx client (NOT `create_session()`). `dotenv_values()` used throughout (does NOT populate `os.environ`). HTTP 400 on POST /auth = signing error (`AuthSigningError`); HTTP 401 = token error (`CredentialExpiredError`).

## E-228 — Team access model (admin-sees-all; surfaces trimmed E-239)
`_get_permitted_teams()` in `src/api/auth.py` returns ALL `teams` rows for admins, explicit `user_team_access` grants for non-admins (dev + prod). Canonical admin predicate is `_user_is_admin(conn, user)` / `user_is_admin(user)` in `src/api/auth.py` (admin = ADMIN_EMAIL match OR `users.role='admin'`). After E-239 the SURVIVING delegator is `reports_admin.py::_require_admin` — the old `dashboard.py::_is_admin_user` and `admin.py::_require_admin` were DELETED with the dashboard / admin-team-management surfaces (no second copy allowed). `bb db reset` produces an EMPTY DB (migrations + `programs` lsb-hs bootstrap only; seed_dev.sql + scripts/seed_dev.py deleted in E-228). Codified in CLAUDE.md Architecture (two canonical entries) + `.claude/rules/data-model.md` (empty-reset footgun).

## E-235 — Report generation telemetry
`report_generation_runs` (migration 002) — one WIDE row per report generation (mirrors `scouting_runs`), FK→reports CASCADE, UNIQUE(report_id); the audit foundation Epic E (scheduled reports) builds on. Three durable patterns codified: (a) wide run-record pattern, (b) canonical-function ADDITIVE-EXTENSION — live example `ensure_team_row`→`ensure_team_row_with_provenance`/`EnsureTeamResult` (legacy delegates, zero caller churn); the season-derivation instance of this pattern was DELETED in E-241 (year-only collapse), leaving `ensure_team_row` the sole live example, (c) per-run created-set orphan attribution via `EnsureTeamResult.inserted` (cross-process safe, no snapshot). KEY FOOTGUN — scored-but-empty games: a completed `games` row can have ZERO stat rows (scores written unconditionally, stats conditionally); scored-but-empty is the MODAL scouting case. Any coverage/freshness signal MUST be data-bearing (EXISTS on player_game_*, perspective-scoped), never a bare COUNT of scored games. Codified in `.claude/rules/architecture-subsystems.md` (a, b, c) + `.claude/rules/data-model.md` (data-bearing coverage trap + concurrent-INSERT recovery). No CLAUDE.md change — rules glob-trigger on src/reports, src/db, migrations, loaders (proportional/simple-first).

## E-236 — Report self-reporting integrity
`classify_stage_status(loaded, errors, expected)` in `src/reports/run_status.py` is the SINGLE per-stage status authority — every load/crawl stage routes its `*_status` through it via `STATUS_COMPLETED/PARTIAL/FAILED` constants (no literals). CORE PRINCIPLE: status is ERROR-DRIVEN not coverage-driven — plays/spray coverage shortfalls (scorekeeper-didn't-chart, null≠error) must NEVER be `partial`; only the boxscore crawl uses coverage-shortfall→partial. Failure-signal precedence: stages with own failure signal map to FAILED before the classifier. `overall_status` CHECK (running/completed/failed) NOT widened — "degraded" is DERIVED at read time, never stored. Operator-vs-coach honesty split (degradation → admin only). FOOTGUN: `_RUN_RECORD_COLUMNS` frozenset in generator.py silently DROPS writes to unlisted columns — add column to allowlist + prove with real-schema round-trip test. Codified in `.claude/rules/architecture-subsystems.md` (Reports Package: stage-status honesty + allowlist footgun), `.claude/rules/data-model.md` (load-status corollary cross-ref in Data-Bearing Coverage), `.claude/rules/testing.md` (pytest pipe-RC trap + disk-backed db.backup deadlock). No CLAUDE.md change — subsystem detail in already-referenced rules (simple-first).

## E-237 — Payload-first loaders + aggregate integrity
TWO codifications. (1) `canonical_recompute(conn, team_id, season_id)` in `src/db/season_aggregates.py` is the SINGLE entry point for `boxscore_only` season-aggregate recomputes (DELETE+INSERT, perspective-scoped, Option-B superset columns); `ScoutingLoader._compute_season_aggregates` + player-dedup path delegate. PROVENANCE OWNERSHIP: owns ONLY boxscore_only, never touches full/supplemented member rows. (2) MIXED-PROVENANCE SCOPE FOOTGUN (caused TWO real bugs): one (team_id, season_id) can legitimately hold both a member `full` row AND `boxscore_only` rows; any per-scope recompute/aggregate/compare/delete-rewrite of `player_season_*` MUST apply the same full/supplemented exclusion — fixed sites: `aggregate_parity.py::verify_aggregates`, `player_dedup.py::merge_player_pair`. (3) Payload-first loader pattern: `PlaysLoader.load_payload`/`GameLoader.load_payload` are in-memory entry points; path methods are thin file-reading wrappers over a shared core (removed last 2 temp-file bridges from reports core). Codified in CLAUDE.md Architecture (canonical helper entry) + `.claude/rules/data-model.md` (Season-Aggregate Parity: mixed-provenance footgun) + `.claude/rules/architecture-subsystems.md` (Reports Package: payload-first loaders). **[RETIRED E-259: codifications (1) `canonical_recompute` and (2) the mixed-provenance/parity apparatus are GONE — E-259 dropped the stored season tables (migration 011) and moved season totals to query-time derivation; the CLAUDE.md canonical bullet and the data-model Season-Aggregate Parity section were evicted. The surviving invariant is the query-time perspective filter (perspective-provenance.md MC-3). Loader pattern (3) further evolved in E-256 (disk twin removed). Preserved here as historical record.]**

## E-239 — Reports-first surface removal (ROADMAP D2)
DELETED the dashboard, the member-sync pipeline (`src/pipeline/` whole package), opponent discovery (`opponent_resolver.py`/`opponent_seeder.py`), the standalone member/plays/spray crawlers + loaders, `src/api/routes/{dashboard,admin}.py`, `config/teams.yaml`, `src/gamechanger/config.py`, `src/db/merge.py`, `finalize_opponent_resolution()`, and the `bb data crawl/load/scout/dedup/repair-opponents` CLI. Reports (`src/reports/`) is the SOLE scouting/delivery surface; admin SURVIVES as `src/api/routes/reports_admin.py` (reports + user management under `/admin`). Surviving `bb data` = reconcile/dedup-players/backfill (maintenance over already-loaded data) [E-256 update: `backfill-appearance-order` later DELETED in E-256-02; `backfill-game-dates`/`reload-annotated-pitches`/`fix-self-games` remain]; plays/spray now fetched in-memory by the report generator. Context layer: DELETED `.claude/rules/quarantine.md` (the `resolve_unlinked` follow→bridge→unfollow ban now lives SELF-CONTAINED in `gc-uuid-bridge.md`) and `.claude/rules/scouting-data-flows.md` (serving/lifecycle/naming conventions migrated into `architecture-subsystems.md` Reports Package); trimmed parity/pipeline-caller/opponent-resolution content from CLAUDE.md + 9 rules; cleaned dangling `paths:` frontmatter (dashboard.py, src/pipeline, templates/dashboard, scripts/*crawl*/*fetch*). KEPT `src/gamechanger/crawlers|loaders/**` + `parsers/**` globs (still match scouting/plays/game survivors). De-scoped permanently: cross-team identity (E-104), multi-season rollups, longitudinal tracking. "Quarantine" is now historical vocabulary — surfaces are GONE, not parity-excluded.

## E-239 — Removal-epic process footguns (learned)
(1) Surface-REMOVAL epics break tests in files NO story touches — e.g. `tests/test_auth.py` used `GET /dashboard` as a generic authenticated-200 probe; per-story review can't catch it (10 tests failed, surfaced only at Phase 4a integration review + the full-suite-green gate). Remedy: for removal epics, repo-wide grep the removed route/symbol across ALL tests during the integration-review step (codified: implement skill Phase 4a). (2) Large removal diffs blow the Codex input limit — the full E-239 diff was ~2.57M chars / 84 deleted files vs Codex's ~1.05M limit; scoping to added/modified/renamed only (ACMR — pure deletions have no content to review and dominate the byte count) brought it to ~445K and worked (codified: codex-review skill size-refuse remedy). Do NOT change `codex-review.sh`'s default `git diff main` — dropping deletions universally would hide removal-completeness signal; ACMR is a per-run remedy for oversized removal diffs only. **[Superseded by E-258 — see E-258 entry]**: the surface-removal-grep remedy is no longer in "Phase 4a" (that phase was dissolved) — it lives in the **Closure CR Integration Review** (Phase 5 Step 1c); and E-258-03 DID make the WORKDIR (epic-worktree) `codex-review.sh` `uncommitted` diff default to `--diff-filter=ACMR` (the standalone/base/commit default stays `git diff main`, so removal-completeness signal is preserved for normal reviews).

## E-241 — Season machinery removal → year-only collapse (ROADMAP-adjacent de-scope)
Ripped the multi-season bones out at the ROOT (the user's ~5×-asked deep de-scope, not another leaf patch). DELETED the `program_type`→suffix taxonomy (`_PROGRAM_TYPE_SUFFIX`, `spring-hs`/`summer-usssa`/`summer-legion`), the `season_fallback` telemetry flag+badge+chain (migration 006 drops the column), the `derive_season_id_for_team_with_fallback`/`SeasonDerivation` additive-extension variant, and the crawler's `season_suffix` param. SURVIVING load-bearing kernel: `derive_season_id_for_team` keeps its `tuple[str,int|None]` signature but is now YEAR-ONLY (`str(season_year or datetime.now().year)`) — the current-year fallback STAYS; `ensure_season_row` always writes `season_type='default'`; the scouting crawler's `_derive_season_id` is year-only and names BOTH the on-disk `data/raw/{season}/...` dir and the DB writes. Filesystem-vs-DB decoupling SURVIVES as a stronger invariant: loaders glob the season path component as an opaque literal (`*`) and NEVER parse the DB `season_id` from it (derive from team metadata instead), so a LEGACY compound on-disk tree may differ from year-only DB rows. Context layer (E-241-04): trimmed `architecture-subsystems.md` (Season_id Derivation rewritten year-only; additive-extension menu drops the dead `*_with_fallback` half + dead example; Filesystem-vs-DB section REVISED not retired). CLAUDE.md byte-untouched (tuple shape unchanged). TWO footguns codified in `.claude/rules/data-model.md` (Seasons Row section): (a) two writers (`_ensure_season_row` crawler + canonical `ensure_season_row`) both `ON CONFLICT DO NOTHING` → first-writer-wins is SILENT, so they MUST agree on all non-key columns (bug: crawler wrote `'unknown'`, suppressed `'default'`); (b) a seeded compound `season_id` literal in a test is NOT format-invariant if a loader DERIVES year-only from team metadata → silent `FOREIGN KEY constraint failed` (only the full suite proves invariance, grep-only recon insufficient).

## E-246 — Whole-project /simplify cleanup
TWO closure codifications. (1) NEW CANONICAL HELPER `resolve_db_path(override=None)` in `src/db/paths.py` — single entry point for SQLite DB-path resolution (precedence: explicit override > `DATABASE_PATH` env, relative→repo-root > default `<repo_root>/data/app.db`); 5 modules delegate (backup.py, reset.py, api/db.py, cli/report.py, cli/data.py). Codified as a new CLAUDE.md Architecture canonical-helper bullet ("New DB-path-resolution paths MUST route through `resolve_db_path()`"). (2) FALSE-PARITY FOOTGUN — aggregate-parity shares TWO axes with `canonical_recompute`, BOTH flowing from `BATTING/PITCHING_RECOMPUTE_KEYS` in `season_aggregates.py`: the SUM projection (`batting/pitching_recompute_select()`) AND the compared-column lists (`aggregate_parity.py::_BATTING_COLUMNS`/`_PITCHING_COLUMNS`, now DERIVED from the keys w/ `gp`/`gp_pitcher`→`games_tracked` stored-alias prefix). A projection col not flowing into `diff_columns` is recomputed but silently NOT compared (false parity). `test_diff_columns_track_shared_keys` pins it. Codified in `.claude/rules/data-model.md` (Season-Aggregate Parity, appended to the E-237 mixed-provenance bullet). Also deleted dead 3-tier resolver `src/gamechanger/resolvers/gc_uuid_resolver.py` (E-246-01) → removed stale pointer from `gc-uuid-bridge.md` line 120 (canonical bridge is `POST /search` filtered by `public_id`). **[RETIRED E-259: the FALSE-PARITY footgun (2) is moot — `aggregate_parity.py` and `canonical_recompute` were deleted in the query-time cutover, so there is no parity check to keep single-source; `resolve_db_path` (1) remains live.]**

## E-247 — Behavior-preserving consolidation (triggers 1 + 3)
THREE new canonical single-source seams codified as CLAUDE.md Architecture bullets (same MUST-route-through spirit as the existing list): (1) `is_gc_uuid(s)` in `src/gamechanger/url_parser.py` — single canonical-UUID predicate (`_UUID_RE` lives here only; `ParsedRef.is_uuid` delegates); consolidated 3 byte-identical copies across `url_parser.py`/`opponents.py`/`game_loader.py`; anchoring+IGNORECASE load-bearing for game_loader own-vs-opponent boxscore-key classification. (2) `get_app_url()` in `src/api/helpers.py` — single `APP_URL` read + `http://baseball.localhost:8001` dev default (rstrip /; default standardized on baseball.localhost:8001 at user direction during closure, matching APP_URL/.env.example/WebAuthn origin); generator `_get_base_url`, reports_admin, routes/auth magic-link all delegate; prod sets APP_URL explicitly. NOTE: `baseball.localhost:8001` is the canonical BROWSER/user-facing URL (resolves to 127.0.0.1); DIRECT in-container access (curl, health checks) stays `localhost:8001` (same app port, `localhost` Host header). (3) `resolve_gc_uuid_by_public_id(client, name, public_id)` in `src/gamechanger/search.py` — single paginate+filter-by-public_id loop routing through `search_teams_by_name` (NOT POST /search), owns the page-level short-circuits; generator + own-team opponent resolver delegate and layer per-match id validation. Module-private dedup helpers (_plays_scope, _total_bases, _decode_jwt_payload, _reconstruct_env_dict, _missing_table_503, _utcnow_iso, etc.) judged NOT CLAUDE.md-worthy (localized internal dedup, no cross-module contract).
TRIGGER 3 FOOTGUN (two bullets appended to `.claude/rules/data-model.md` Season-Aggregate Parity): (a) twin-method collapse must preserve each path's DISTINCT guards — Codex near-miss: pre-refactor in-memory load early-returned on empty boxscores while disk path ran a post-boxscore tail; naive single-core ran the tail unconditionally → would turn `canonical_recompute` into a stat-rewriting DELETE+rebuild on populated DBs (a 0-boxscore re-scout wipes/rebuilds boxscore_only aggregates + runs player-dedup). (b) idempotent-recompute characterization tests have NO TEETH on a fresh/empty DB — must seed a POPULATED state whose stored aggregate deliberately DISAGREES with the per-game sum, else green proves nothing. Also performed two review-time SECURITY PASSES (E-247-04 credential/auth core, E-247-07 API middleware/auth-route + APP_URL) — both SECURITY-CLEAN; key adversarial finding: get_login→`_get_authenticated_user` delegation is strictly STRICTER (orphan-session shows login vs old redirect-to-admin), no looser path. **[RETIRED E-259: the twin-method footgun's concrete `canonical_recompute` DELETE+rebuild hazard is moot — the recompute path and stored season tables were removed in the query-time cutover; the GENERAL "preserve each collapsed path's distinct guards" lesson still holds and stays in the code-reviewer twin-path checklist.]**

## E-252 — Scheduled-reports reliability (ROADMAP CE-2 slice)
FOUR new CLAUDE.md codifications, all closure-gate (Context-Layer Assessment). THREE new canonical seams appended to the Architecture canonical-seam list right after `get_app_url()` (same MUST-route-through shape): (1) `is_production()` in `src/api/helpers.py` — single `APP_ENV == "production"` prod-detection seam (default `development`); `email.py` (send-email tri-state + `validate_alerting_config`), morning-run alerting preflight, and `routes/auth.py::_is_dev_mode` (= `not is_production()`) delegate. TWO intentional NON-delegators documented so they aren't "fixed": `src/api/csrf.py:135` still inlines the check (tracked follow-up idea — leave it), and `src/api/auth.py:355` DEV_USER_EMAIL middleware guard uses case-insensitive `app_env.lower() == "production"` with an EMPTY default — DIFFERENT fail-safe semantics, MUST NOT be folded. (2) `get_operating_timezone() -> ZoneInfo` + `operating_today(now=None) -> date` in NEW `src/util/timezone.py` — single venue-local "today" seam reading `OPERATING_TIMEZONE` env (IANA, default `America/Chicago`, WARN-and-degrade on invalid, never raises); morning-run's default target date uses it (an evening run in a UTC container would otherwise roll to tomorrow's games). CROSS-EPIC RULE made explicit+bold: **CE-3 / E-253 `game_date` derivation MUST reuse this seam and MUST NOT introduce a second timezone convention.** (3) `get_connection(db_path=None)` in `src/api/db.py` — single SQLite connection factory for every writer on the shared cross-process WAL file (admin UI, interactive report CLI, morning-run cron); sets `busy_timeout=30000` + `synchronous=NORMAL` (+ WAL + foreign_keys) per-connection. INVARIANT: busy_timeout is false safety without commit discipline — a writer MUST NOT hold an open write txn across a network fetch on the shared file (morning-run commits after `ensure_team_row` before the crawl). Scoped to the scheduled-reports path; the `bb data` writers are an explicit tracked out-of-scope follow-up (NOT implied covered). FOURTH: rewrote the `bb report morning-run` sentence in CLAUDE.md Commands — target date now venue-local `operating_today()`/`OPERATING_TIMEZONE` (not container UTC), a non-dry-run alerting-config PREFLIGHT aborts non-zero if the operator-alert channel can't deliver (ADMIN_EMAIL unset, or prod without MAILGUN_API_KEY/MAILGUN_DOMAIN), and the exit-code contract (non-zero on run-body crash, failed/skipped summary send, or misconfigured channel — a cron/monitor keys on it). FLAGGED to docs-writer (NOT codified, out of context-layer domain): `OPERATING_TIMEZONE` is absent from `docs/production-deployment.md`'s env-var table, `docs/admin/operations.md`, and `.env.example` — there is NO central env-var registry in `.claude/rules/`, so the operator-facing registry is the docs/ deployment table (docs-writer's documentation-assessment-gate item). The env var IS now context-layer-documented via the operating-timezone seam entry.

## E-249 — Player-dedup "de-dup the dedup" (components + fork refusal)
ONE shared planner (`plan_player_dedup` + `execute_collapse`, `src/db/player_dedup.py`) now backs BOTH the load-path sweep (`dedup_team_players`) and the CLI (`bb data dedup-players`). FIVE durable invariants/footguns codified in a NEW `.claude/rules/data-model.md` section ("Player Dedup: Components, Fork Refusal, Transaction Ownership (E-249)", placed after Season-Aggregate Parity): (1) components MUST partition by `(team_id, season_id)` — keying by team_id alone unions prefix-pairs across seasons and mis-plans the unscoped CLI path (the E-249 Codex P1); (2) fork refusal "refuse, don't guess" — a component whose maximal members carry ≥2 DISTINCT names (Jo/John vs O/Oliver/Owen) is REFUSED not merged (prefix-match can't tell one human from two; only same-game co-occurrence can — deferred to Tier 2 / IDEA-089); equal-named maximal members still collapse; (3) per-component transaction ownership — `execute_collapse` owns the savepoint and calls `merge_player_pair(manage_transaction=False)`; `manage_transaction=True` self-commits and can't nest; (4) CLI default-isolation footgun — the CLI sqlite conn uses legacy isolation (rolls back uncommitted DML on close), so a standalone recompute MUST be followed by explicit `conn.commit()`; (5) KNOWN LIMITATION (user decision, de-scoped not fixed) — unscoped `--execute` over a 2+-season DB where one duplicate player_id is claimed by DIFFERENT canonicals across seasons commits one guessed merge and caught-errors the rest; unreachable on the single-season live DB, load path immune, cross-season identity is an explicit Non-Goal. CLAUDE.md Commands: rewrote the `bb data dedup-players` description (component grouping, unambiguous collapse, fork refusal w/ `--dry-run` preview + one WARN/fork on `--execute`, non-zero exit on merge failure, CLI owns+commits the recompute, shared-planner routing). docs/admin operator doc owned separately by docs-writer; epic/story files owned by PM.

## E-254 — Security & PII hardening (ROADMAP CE-4 slice)
FIVE-trigger closure codification. (A) **CORRECTED the E-252 `is_production()` note in CLAUDE.md** — E-254-01 FOLDED IN the two E-252-documented "intentional non-delegators": `csrf.py` (CSRF cookie `Secure`, line 136) and the `auth.py` `SessionMiddleware` DEV_USER_EMAIL guard (line 363) both now delegate to `is_production()`. The predicate is now STRICT-normalized: `get_app_env()` does `.strip().lower()` (kills whitespace+case variants, closing the bypass), and a NEW `validate_app_env()` refuse-to-start guard (RuntimeError + CRITICAL log at app construction, alongside the DEV_USER_EMAIL guard in `SessionMiddleware.__init__`) fires on a non-empty `APP_ENV` outside `{production, development, dev, test, staging}`. Full delegating consumer list: email.py, morning-run preflight, `routes/auth.py::_is_dev_mode`, csrf.py, auth.py DEV_USER_EMAIL guard. ONE real non-delegator remains: `src/db/reset.py:49` inlines `.lower()`-only `APP_ENV == "production"` on the destructive `bb db reset` guard (whitespace-variant bypass → IDEA-101, left intentionally). `main.py:68` is a diagnostic log, not a gate. A future dev following the stale E-252 note would have REOPENED the whitespace bypass — this correction is security-load-bearing. (B) **`.claude/rules/pii-safety.md` extended** (new "Scanner capabilities" section): credential patterns (`bearer_token`, `api_key_assignment`) compile `re.IGNORECASE` (email/phone stay case-neutral); `api_key_assignment` key alternation broadened to project token-key names (client/refresh_token, device_id) — TARGETED not blanket, value side keeps `[=:]`+16-non-space; `--staged` reads the STAGED BLOB via `git show :<path>` through shared `_scan_text`; unreadable staged blob FAILS CLOSED (non-zero exit + stdout refusal — Phase-4b hardened from warn-and-skip); `# pii-ok` suppresses benign credential-SHAPED lines in real credential modules (token_manager.py/credential_parser.py/cli/creds.py). Reviewer gotcha captured: editable install can shadow worktree patterns → force the worktree module. (C) **Doc-PII byte-gate codified** — `scripts/check_doc_pii.sh` (new committed PII-free harness, config/config.example split: real denylist = uncommitted `secrets/pii-denylist.txt`, committed fake example = `scripts/pii-denylist.example.txt`; exit 0/1/2/3; machinery-based self-test) documented in CLAUDE.md Security Rules + pii-safety.md. **IDEA-102 coverage footgun codified** (pii-safety.md new section): pre-commit scanner has `epics/`+`.project/` in SKIP_PATHS and can't regex-detect NAMES; byte-gate scoped to docs/api/ only → real identifiers (esp. minors) in idea/epic/planning artifacts rest on author discipline ALONE (a real minor's name landed in the IDEA-096 capture, caught by Codex) — convention added: never paste real names in `.project/**`/`epics/**`, use the api-docs taxonomy; systematic fix tracked IDEA-102. (D) **`.claude/rules/api-docs.md` F5 taxonomy gap**: added a one-line clarification that BARE 8-char UUID prose prefixes count as identifiers to scrub, AND switched the canonical placeholder example OFF the real team prefix (`72bb77d8-REDACTED` → `00000000-REDACTED`) so the taxonomy stops re-embedding a real prefix. Value/example-only, no API-behavior claim touched. (E) **Two SQLite-concurrency patterns codified in `.claude/rules/data-model.md`** (globs src/api/**, surfaces on route edits): (1) NEW "Atomic Count Cap" section — a hard per-call INSERT bound MUST be a single count-guarded `INSERT…SELECT…WHERE (SELECT count) < :cap` (SQLite single-writer serialization = true hard cap), NOT count-then-insert (TOCTOU overshoot to 101, the Phase-4b finding); canonical = KIND_LOGIN cap in `passkey_challenges.py` (E-254-03). (2) Extended the existing DELETE-is-the-arbiter section with "consume on POST, never a bare GET" — link-scanners/prefetchers issue GETs and burn single-use tokens; canonical = the `verify_token` GET-interstitial/POST-consume split (E-254-02). No new machinery invented; all five items verified against shipped worktree code before writing.

## E-257 — Plays→Boxscore Reconciliation Scoreboard (productization)

> **PARTLY SUPERSEDED 2026-07-26.** The one-way ratchet GATE described below was retired by operator decision (it cost more attention than it caught: 2 of the last 3 closures carried exceptions and the baseline ran ~4 epics stale). `bb report reconcile-scoreboard` survives as a pure diagnostic; `self_games == 0` survives as a standalone closure assertion; the code-canonical stat-definition constants survive unchanged. Everything below about baselines, gate exit codes, `--update-baseline`, and mechanical binding of the Operating Principle is HISTORY, not current state. The gate CODE is still in the CLI as a vestige pending a follow-up story.
THREE-part closure codification (context-layer assessment fired triggers 1/2/3/5/6). **(1) CLAUDE.md Commands (trigger 6)**: added `bb report reconcile-scoreboard [--json] [--update-baseline]` after the `verify-aggregates` entry — the standing plays-vs-boxscore reconciliation scoreboard + one-way ratchet gate, a READ-ONLY diagnostic (writes nothing). Per-stat fidelity (exact% + abs-Δ for gated outcome stats) + three axis counters (`dropped_pitch_events`/`no_plays_units`/`self_games`) from the live DB; diffs against `.project/baselines/reconciliation-scoreboard.json`; `--json` = stable machine surface E-256's closure smoke consumes; `--update-baseline` = operator-owned re-snapshot (no agent auto-refresh); exit codes 0 pass / 1 gated regression or precondition / 3 baseline absent / 4 baseline malformed; MANUAL operator diagnostic, NOT CI (dev DB absent from worktrees/CI); DISTINCT from `bb data reconcile` (the corrective ENGINE — reconcile-scoreboard MEASURES+GATES, `bb data reconcile` CORRECTS). **(2) CLAUDE.md Operating Principle (trigger 2)**: REPLACED the forward-looking "enforcement mechanism … being designed in E-245 … binds intent" caveat with the now-EXISTS statement — the scoreboard + ratchet gate exist (E-257), so the north-star principle now binds MECHANICALLY (gate: no gated stat abs-Δ increases, neither ratcheted axis counter increases, `self_games` stays 0). Preserved the "direction not a threshold / irreducible residual" nuance (ratchet bakes the accepted floor into the committed baseline; holding steady is a pass). **(3) CLAUDE.md Architecture bullet (triggers 1/3/5)**: NEW "Reconciliation-scoreboard conventions (E-257)" bullet after Shared-query-functions — `src/reports/recon_scoreboard.py` is the read-only pure core (sibling of `aggregate_parity.py`, in `src/reports/` not `src/db/` because it writes nothing); TWO conventions future work MUST honor: (a) SOURCE-OF-TRUTH FLIPPED — the plays-derived stat-def constants (SO/BB/H outcome-membership sets + AB-exclusion set) are CODE-CANONICAL in `recon_scoreboard.py`; the E-245 baseline doc MIRRORS them by hand (constant-pin + behavioral tests guard drift; do not re-derive elsewhere or treat the doc as canonical); (b) the ratchet gate is a MANUAL operator diagnostic (never CI) and the operator OWNS every baseline snapshot (no agent auto-refresh — the JSON diff is the human review point). **Trigger-7 offset**: net growth is load-bearing (a required new-command doc entry + the Operating-Principle edit which is largely a REPLACEMENT of the forward caveat, near byte-neutral). The one genuinely additive block is the Architecture conventions bullet; justified because it pins the source-of-truth flip (code canonical, doc mirrors) that a future agent editing stat defs would otherwise get backwards. No stale pointer retired (nothing was superseded except the now-corrected Operating-Principle forward-caveat, which the replacement itself handles). **[E-259 note: the two incidental references above — "after the `verify-aggregates` entry" and recon_scoreboard as "sibling of `aggregate_parity.py`" — are now historical; E-259 deleted both `verify-aggregates` and `aggregate_parity.py`. The reconcile-scoreboard convention itself is LIVE and unchanged; the CLAUDE.md `recon_scoreboard.py` bullet's "sibling" phrasing was updated in E-259-05.]**

## E-258 — Review-System Hardening (agentic-flow audit)
FOUR-part codification across the review machinery (all context-layer; the epic edits the review system itself, so all edits ride the closure patch and bind FUTURE epics only). **(1) `code-reviewer.md` rubric (E-258-01)**: nine rubric edits targeting CR-missed structural gap classes — cross-cutting consumer/twin-path audit (names the project twin pairs `game_loader`↔`scouting_loader`, `detect`↔`cleanup`, `recompute`↔`parity`), adversarial-assertion strength ("what wrong implementation would still pass this test?"), per-changed-function edge-case enumeration, a standing concurrency read-then-write question (three SQLite writers), a sensitive-path security trigger (replay / TOCTOU / fail-open-vs-closed / PII in ALL artifact types), TWO edit-in-place REVERSALS (Step-2 self-load-on-diff-evidence replacing "do not load … independently"; cumulative positive migration baseline replacing the assignment-provided dependence), a `| head`/`| tail` prohibition on integration-review reads, and a verbatim-test-evidence requirement. PLUS the AC×surface matrix as a Priority-1 extension, a dormant-migration review-time DEMAND in the deploy-time-safety region, and the machine-findable checklist DELIMITERS — `<!-- BUG-PATTERN-CHECKLIST:START/END -->` and `<!-- SECURITY-CHECKLIST:START/END -->` (authoritative tokens, each EXACTLY ONCE + START-before-END; SECURITY spans the whole Priority-4 span) with the delimiter contract documented in `code-reviewer.md` itself. **(2) implement-skill Phase 4/5 restructure (E-258-02)**: Phase 4 is now Codex-ONLY (still "and review"-gated); the CR integration review became an UNCONDITIONAL Phase 5 **"Closure CR Integration Review"** (Step 1c) — the LAST pre-merge review, after Step 1a and before the Step 8 merge — so a plain "implement E-NNN" always gets a combined-diff review, and Codex-first means CR adjudicates Codex's findings + the post-remediation diff instead of approving-then-reversing (E-239 / E-251 / E-253). The remediation spawn context was renamed to the phase-number-independent **"Remediation Spawn Context"**, defined once at the start of Phase 5; Step 1a gained a mechanical invariant-audit trigger checklist evaluable from PERMITTED artifacts (NOT NULL/FK migration in the diff; canonical-helper signature change in Technical Notes; new required core-INSERT field in Technical Notes). HARD CONSTRAINT honored: the full-suite-green gate mechanics + the "Step 1b" cross-file anchor label are UNCHANGED (only the remediation pointer was renamed). NEW `.claude/rules/doc-sweep.md` (globs `docs/**`, `.claude/**`, `epics/**`, `.project/**`): token-grep PAIRED WITH synonym expansion + a semantic read (E-250's "across games and seasons" survived a keyword-only sweep). **(3) codex-review single-source + tooling (E-258-03)**: `scripts/codex-review.sh` now extracts the Bug Pattern + Security checklists LIVE from `code-reviewer.md` (via the delimiters, resolved from the script's `REPO_ROOT` — NOT `--workdir`, which redirects only the git diff) at prompt-assembly, ending the hand-synced rubric drift; `.project/codex-review.md` dropped its abbreviated "Extended bug pattern checks" section AND its one-line Priority-3 security bullet (both now single-sourced, exactly-one-source-never-zero). The script tees output to a deterministic `RESULT_FILE` + emits a `RESULT_FILE=`/`wc -l`/`tail -n1` receipt (closes the 44/48 missed-manual-redirect fabrication hole; the codex-review skill Step-4 read-receipt gate consumes it), defaults the WORKDIR uncommitted diff to `--diff-filter=ACMR`, and FAILS CLOSED if any of the 4 delimiters is absent / not-exactly-once / out-of-order (the guard enforces the same contract `code-reviewer.md` declares). **(4) Trigger-7 offset**: net context-layer growth is load-bearing review-hardening (new rubric depth + the unconditional Closure CR Integration Review + the doc-sweep rule), OFFSET by the single-sourcing — the duplicated, drift-prone `.project/codex-review.md` bug-pattern + security abbreviation was DELETED and is now extracted from ONE source, retiring that second-copy maintenance surface. **SUPERSEDES the E-239 entry's "Do NOT change `codex-review.sh`'s default `git diff main`" decision** (the WORKDIR default is now ACMR; standalone/base/commit unchanged, so removal-completeness signal is preserved for normal reviews) and relocates the E-239 surface-removal-grep remedy from the dissolved "Phase 4a" to the Closure CR Integration Review (Phase 5 Step 1c). **Trigger-8**: the "Phase 4a"→"Closure CR Integration Review" and remediation-context→"Remediation Spawn Context" renames left NO stale LIVE pointer in the live prescriptive layer OR the auto-loaded agent-memory indexes (11 agent-memory `Phase 4a/4b` hits are frozen historical provenance — dated lessons / archived scorecards citing past epics — left untouched); the superseded `review-cycle-reordering.md` pending-plan (its proposal shipped, then its implement-side was reversed here) was RETIRED and de-indexed.

## E-256 — Post-Descope Simplification & Foundations (audit CE-6)
Most conventions codified IN-EPIC (the codifying stories were CA-owned, so their edits ARE the codification): **(1) Closure runtime smoke / Step 1d (E-256-11)** — `implement/SKILL.md` gained a conditional Phase 5 **Step 1d** (defined after Step 1b, mechanics wired into Step 8 as **sub-step 5b**, plus a conditional **sub-step 4b** dependency-reinstall), the reports flow's first LIVE runtime gate at closure (runs `bb report generate`→headline `reference_date` invariant→`curl /health`→`reconcile-scoreboard` axis counters→`morning-run --dry-run` exit-0 against the live dev DB when the closure diff touches a runtime surface or build input); env-FAIL (preflight) escalates+holds, epic-FAIL routes into the remediation loop. `code-reviewer.md` got TWO carve-outs (a second Test-Execution-Constraint exception + an Anti-Pattern #5 exception for the DB-mutating `bb report generate`) and self-evaluates the trigger. `.smoke-fixture` (two-field, gitignored, NOT `.env`) added to `.gitignore`. **(2) `pii-safety.md` suppression-scope + choice hierarchy (E-256-13)** — documented that BOTH suppressors (`# pii-ok` line-scoped, `synthetic-test-data` file-level) sit OUTSIDE the pattern loop so each suppresses ALL patterns incl. credentials; 3-tier hierarchy (change-the-data → line marker → file marker never on a credential file); `tests/**` blanket-exemption rejected on evidence + diff-visibility; §4g cited to `code-reviewer.md` (NOT CLAUDE.md — the epic §6 mis-cited it); review-visibility is why marker>exemption but NOT a structural closure (staged-blob residual → IDEA-112). **(3) `data-model.md` dead-by-descope note (E-256-12)** — `crawl_jobs`/`coaching_assignments` write-orphaned + ~100 season split/advanced columns unpopulatable post-E-239, retained by decision; `user_team_access` explicitly flagged LIVE (never sweep). **(4) Deletion-side eviction (E-256-15)** — the E-237 "payload core + thin file-reading wrapper" convention (`architecture-subsystems.md:72`) REWRITTEN, not symbol-stripped: it actively instructed rebuilding the deleted twin; now states `load_payload`/`load_from_data` is the sole entry path and new loaders must NOT add a file-reading wrapper. Re-pointed `cascade_delete_team`/`cleanup_orphan_teams` + helpers generator.py→lifecycle.py (CLAUDE.md:118 + architecture-subsystems.md:34), narrowed the Season_id-derivation rule (GameLoader presumes the row; entry-point loader+generator ensure it), removed the `backfill-appearance-order` command sentence+Footgun+two precedent citations from CLAUDE.md and struck the data-model.md ref. **AC-7 closed-loop guard proved load-bearing**: enumeration came from stories 01–04 Handoff Context (NOT the sweep grep) — the first grep silently returned "none" due to the ugrep BRE-alternation quirk (now codified in `tool-output-integrity.md`); driving eviction off it would have shipped a false-clean. **Trigger-3 (ugrep trap)**: added a tight silent-empty-grep entry to `tool-output-integrity.md` (universal `paths:"**"` load target). **Trigger-7 offset**: net growth is load-bearing (Step 1d is a substantial, defect-cited skill addition — it would have caught a physically-impossible FPS stat + a rest-day UTC bug that both shipped through green closures); NOT offset by deleting legitimate content; operator-signed ratchet exception surfaced at closure. **Deletion-side §15(c)**: reconciled off-team DE memory (`season_aggregate_writers.md` stale `GameLoader.load_file`, `season_tables_are_a_pure_cache.md` stale backfill ref) reconcile-not-strike. Merge-base note: E-260 (dispatch-cost-accounting) landed on main AFTER this worktree branched and touched `implement/SKILL.md`+`code-reviewer.md`+`tool-output-integrity.md` (NOT CLAUDE.md/architecture-subsystems.md/data-model.md/pii-safety.md); all E-256 additions were placed in E-260-untouched regions and verified clean against `git apply --3way`.

## E-259 — Query-time season aggregates + context-layer eviction
No IN-EPIC dedicated entry existed before 2026-07-12 (E-259's codifications lived only as scattered `[RETIRED E-259: ...]` annotations threaded into the E-237/E-246/E-247 entries above, plus the E-257 `recon_scoreboard.py` "sibling" phrasing note); this entry consolidates them. **(1) Query-time cutover (E-259-01..04)**: the stored `player_season_*` tables were DROPPED (migration 011) and season totals moved to query-time derivation — canonical readers `get_season_batting` / `get_season_pitching` in `src/api/db.py` derive from `player_game_*` at read time with the `perspective_team_id` filter. This retired the whole stored-aggregate apparatus: `canonical_recompute` (`src/db/season_aggregates.py`), the aggregate-parity check (`aggregate_parity.py`), the E-237 mixed-provenance/false-parity footguns, and the `verify-aggregates` CLI command — all now moot (see the `[RETIRED E-259: ...]` annotations on E-237/E-246/E-247). The SURVIVING invariant is the query-time perspective filter (`perspective-provenance.md` MC-3). CLAUDE.md eviction: the `canonical_recompute` Architecture bullet + the data-model Season-Aggregate Parity section were removed; the Shared-query-functions bullet now names `get_season_batting`/`get_season_pitching` as the season-aggregate seam. **(2) Deletion-side eviction generalized to per-agent topic files (E-259-05)** — this is the item retired from CA MEMORY.md "Pending Context-Layer Updates" on 2026-07-12. `context-layer-assessment.md` Deletion-Side Eviction (line ~53) was generalized from grepping only rules/agents/CLAUDE.md + the `MEMORY.md` INDEXES to grepping **every agent's OWN `.claude/agent-memory/<agent>/` directory — the index AND its topic files** — reconciled (strike-stale / keep-live) by the OWNING agent. Motivating hole: three of four stale data-engineer DE files hid in topic files (`season_aggregate_writers.md`, `season_tables_are_a_pure_cache.md`, `fixture_seed_not_rollup_consistent.md`), invisible to an index-only grep. **Durable design heuristic (the reusable lesson):** for deletion/rename eviction, an obligation MUST be a grep-and-reconcile over a LIVE pattern, NEVER a static file list — a frozen enumeration is a `feedback_descope_grep_gap`-class trap (certifies the tokens you thought of, silent on the rest); the E-259 static AC-5 file list was proven incomplete 3× in one session (memory 1→2→4, backfill 2→11). **(3) Runbook updates (E-259-06)**: operator-facing docs that referenced the retired season-aggregate tables/command were reconciled to the query-time model. **[E-262-04 filename reconciliation]** After E-259 stripped the stored-aggregate apparatus, the surviving `src/db/season_aggregates.py` was misnamed (what remained is only the query-time projection helpers `batting_recompute_select()` / `pitching_recompute_select()`); E-262-04 completed the IDEA-113 rename to `src/db/season_projection.py`. The `season_aggregates.py` path tokens in this entry and in the E-237/E-246 entries above are PRESERVED as accurate-at-the-time history (they describe retired code); the live module is now `season_projection.py`.

## E-260 — Dispatch Cost Accounting (the LAST meta-epic; meta-layer FREEZE)
Deletion-dominant epic (8 CA stories, operator-commissioned from two E-256-dispatch auditor reports), commissioned because `.claude/rules/` + `.claude/agents/` grew 1,652→4,061 lines in four months with no net-removing commit while three soft brakes (trigger 7, `context-layer-guard.md` ~150-line targets, `context-fundamentals` budget table) each accepted any answer and never fired a shrink. NOT run through spec-review/Codex (AC-2 — the reviewer stack has a conflict of interest reviewing its own constraints); the operator hand-read the closure diff. AC-1 met: net-negative diff to the four `.claude/` subtrees vs the merge-base (closed at −7). Facts codified IN-EPIC (E-260 IS its own codification — CA-authored context-layer edits):

**Two mechanisms ADDED (the only additions; both sit OUTSIDE the four measured subtrees):**
1. **Context-layer ratchet** — `.claude/hooks/context-ratchet.sh` counts `*.md`+`*.sh` lines across the four subtrees (`.claude/rules`, `.claude/agents`, `.claude/skills`, `.claude/agent-memory`) and diffs against the committed baseline `.project/baselines/context-layer-ratchet.json`; exits non-zero when the layer grows past baseline. A MANUAL operator diagnostic (not CI). The OPERATOR owns every baseline re-snapshot (`--update-baseline` is operator-only; no agent auto-refreshes it; the committed JSON diff is the human review point). Same instrument shape as E-257's reconcile-scoreboard, pointed at the config. This is the hard mechanism trigger 7 declined to be; `context-layer-guard.md`'s ~150-line CLAUDE.md / <150 MEMORY.md targets FOLDED into the ratchet baseline (no more standalone unenforced numbers). Wired into `context-layer-assessment.md` trigger 7.
2. **SendMessage counter** — `.claude/hooks/send-message-counter.sh`, a `PreToolUse` hook: `WARN_AT=15` / `DENY_AT=25` sends per staging boundary, counter reset when a `git add` command TARGETS the epic-worktree prefix `/tmp/.worktrees/baseball-crawl-E-`. The deny message names the operator-decision action (must be resolved by an operator decision, NOT by reinterpreting the rule in-session — Falsifier 4). Provenance in-comment: 15 ≈ the two-round breaker's implied 12–14-send budget; 25 is a single-epic (E-256) placeholder; only the operator edits either number. Writes a worktree-local dispatch log (epic ID + staging index + sends; rounds best-effort) that rides the closure patch — no committed cross-epic aggregate.

**Deletions / corrections (snapshotted first — 10 removed-text files in `.project/archive/E-260-removed/`):**
- **Verbatim-relay mandate removed** (E-260-01) — the `never summarize` / round-one-findings-verbatim apparatus struck from `implement/SKILL.md` + `dispatch-pattern.md`. Accepted trade-off: SendMessage drops revert to manual recovery (re-request or read the story file); no persist-then-message replacement added (that would be a new instruction this epic exists to avoid).
- **Closure diff base = merge-base, not `main`** (E-260-01) — the closure sub-step 3 diff/patch and the AC-verification base are `$(git merge-base epic/E-NNN main)`, so a parked epic's closure cannot revert later-landed work (the live E-256/E-260 landmine). Now the standard across `implement/SKILL.md` closure steps.
- **False Test Execution Constraint corrected** (E-260-02, 3 files) — the claim that worktree pytest tests the main checkout was FALSE (pytest resolves to the worktree because `tests/__init__.py` prepends the repo root to `sys.path`, shadowing the editable-install finder). Corrected in-config, not carried as new domain knowledge.
- **Two-tier severity floor** (E-260-03) — `code-reviewer.md`: every finding is MUST FIX or SHOULD FIX; a MUST FIX must name a functional consequence (behavior, security, data integrity, or test validity); everything else is SHOULD FIX (one message, no round). Replaced the old three-condition downgrade test.
- **worktree-guard mode-1 agent-memory exemption DROPPED** (E-260-05) — `worktree-guard.sh` mode 1 now blocks ALL Write/Edit to the main checkout incl. `.claude/agent-memory/`, so EVERY closure-time own-memory write (PM's Active→Archived flip + topic flushes, CA's codification) routes to the WORKTREE copy at the Step 8 sub-step-3 authoring window and rides the operator-approved closure patch. (Consultation-mode own-memory writes — no worktree present, mode 2 — are unaffected.) This is the current invariant in `worktree-isolation.md`.
- **tool-output-integrity framing scoped** (E-260-01/related) and **`context-fundamentals` relay sentence + stale budget table scope-corrected** (E-260-04).
- **trigger 8 gated, not deleted** (E-260-07) — promote-to-load-target now requires citing the specific defect it demonstrably caught AND fitting inside the ratchet baseline (an operator-signed exception otherwise); the ungated "promote NOW" was the growth pump.

**META-LAYER FREEZE (the standing constraint this leaves behind):** E-260 is the LAST meta-epic. Falsifier #3 records that an epic authored by AGENTS to sharpen E-260's own machinery = the loop re-entering through the brake. Forward rule: changes to the dispatch/review/context-layer machinery require a CITED defect (operator-directed), not agent-initiated refinement. Follow-up ideas filed instead of codified: IDEA-116 (cwd-based worktree attribution for the dispatch hooks + E-256 resume guard), IDEA-117 (scope `multi-agent-patterns` "Never summarize" to the dispatch context block), IDEA-118 (refresh `context-fundamentals` ambient figures).

## E-261 — Cross-Perspective Game-Dedup Fidelity
Feature/data epic (NOT a meta-epic — the E-260 freeze binds the dispatch/review/context-layer machinery, not domain code). Fixed two `GameLoader` dedup defects that left cross-perspective *twins* (the same real game persisted under two `game_id`s, once per team perspective) un-collapsed, rendering complete reports `degraded`: (A) the exact-score gate missed twins with scorekeeper score drift; (B) the redirect clobbered the canonical row's `game_stream_id` with a twin's id. Context-layer codification (4 triggers fired: T1/T2/T3/T6):

- **T1/T2 — canonical seam (CLAUDE.md Architecture)**: added a **Canonical duplicate-game merge** bullet — `merge_duplicate_game(conn, source, canonical) -> GameMergeResult` in new module `src/db/game_merge.py`, the single primitive collapsing a twin. Connection-in / **no-commit** (caller owns the tx, mirrors `merge_player_pair(manage_transaction=False)`); re-points all six `games` FK children (`game_perspectives` unioned; `player_game_batting`/`player_game_pitching`/`plays`/`spray_charts`/`reconciliation_discrepancies` re-pointed; `play_events` follows `plays` automatically) then deletes the losing `games` row **LAST** (no FK child has `ON DELETE CASCADE`, so delete-last is load-bearing). Same module houses `is_offline_same_game(...)`. This is the shared home for both the in-pipeline twin merge (`GameLoader` redirect) and the operator repair pass — the `player_dedup` "de-dup the dedup" precedent applied to games. New duplicate-game-merge paths MUST route through it.
- **T3 — footguns fixed fail-CLOSED (Codex-caught)**: (i) the AC-2 non-disjoint refusal is decided by **PRE-classification** (intersecting the two perspective sets BEFORE any write), NOT by catching a mid-merge `IntegrityError` — a refusal leaves ZERO rows modified. (ii) `is_offline_same_game` REQUIRES near-play-count corroboration — disjoint-cross-perspective + bounded score-tolerance ALONE must not merge (same-total doubleheaders would fool score-tolerance). (iii) the live `_find_duplicate_game` schedule-count same-game signal is TOLERANT and fail-CLOSED on an undercount (a missing count never masks a genuine twin). These are baked into code+tests; the durable rule captured in the CLAUDE.md canonical bullet + the "Prevention over cleanup" update.
- **T6 — CLI (CLAUDE.md Commands)**: added `bb data merge-duplicate-games` — idempotent operator repair pass for twins persisted BEFORE the fix. Groups completed games by the live natural key (`season_id`+`game_date`+unordered team pair), collapses each EXACTLY-2-row disjoint-singleton twin (corroborated by `is_offline_same_game`) via the canonical helper, then restores any `game_stream_id` clobbered by the pre-fix redirect (poisoned value corroborating as a redirect source → reset to `game_stream_id = game_id`). Bias-to-refuse (one WARN/group) on ≥3-row groups, non-disjoint pairs, failed corroboration. `--dry-run` default; `--execute` commits each per-item tx with per-item rollback isolation; exits non-zero iff any item failed (refusals alone do NOT fail).
- **Migration-010 corrected premise (in-epic, E-261-01)**: the 010 header's old claim that `game_stream_id` is "NULL for tracked / stable across perspectives for member games" described the RETIRED authenticated game-summaries model (removed E-239). Real shape under the sole surviving scouting/public populator: NON-NULL and PERSPECTIVE-SPECIFIC (self-keyed to each row's `event_id`), so twins carry DIFFERENT stream ids and are NOT caught by the partial UNIQUE backstop — collapsing them is the redirect's job. **Eviction sweep result**: the corrected premise lived ONLY in the migration header (already fixed in-epic); the context-layer `game_stream_id` references are all about the endpoint pipeline/schema (still factual) and CLAUDE.md L77's "stable across perspectives" is the CR-confirmed factual description of the retired endpoint (deliberate contrast — do NOT strike). No stale internal claims to evict.
- **T7 ratchet**: my additions land in `.claude/rules` (0 lines — none added there) and `.claude/agent-memory` (this entry + the MEMORY.md pointer). The ratchet was ALREADY +132 past the 2026-07-12 baseline BEFORE any E-261 context-layer edit (pre-existing agent-memory drift on `main`; the E-261 branch's context-layer diff vs merge-base was empty). So an operator-signed re-snapshot is needed regardless of E-261; my additions were kept tight and placed in `.claude/agent-memory` (the CLAUDE.md Commands/Architecture growth is NOT ratcheted — CLAUDE.md sits outside the four measured subtrees).

## E-262 — Post-Program Housekeeping (defect-cited residuals; freeze holds)
NOT a meta-epic — the E-260 freeze binds; every context-layer edit here is DEFECT-CITED (a concrete falsehood/staleness/procedure defect), NO new machinery. **The in-story edits ARE the codification** for the triggers that fired — no separate closure-codification pass adds machinery. Two CA stories:
- **E-262-05 (context-layer truth & staleness)** — five reword-in-place corrections: (1) CLAUDE.md L115 false "Store raw API responses before transforming" → the in-memory crawl-to-load reality (no stored raw tier), AND (2) doc-sweep sibling `http-discipline.md` L44 same false claim → api-scout-verified TWO-CASE reword (live in-memory pipelines persist no raw tier; the out-of-band proxy-capture `proxy/data/` + ingest-endpoint `data/raw/<endpoint>-sample.json` paths DO persist redacted raw) — this was the AC-1 doc-sweep completion, http-discipline.md added to scope mid-story by PM ruling; (3) CLAUDE.md L85 morning-run `--dry-run` prose clarified (dry-run sends NO summary email — matches `src/cli/report.py` `_emit_summary_if_needed` early-return); (4) `multi-agent-patterns/SKILL.md` L24 "Never summarize" scoped to the dispatch context block only (IDEA-117; cannot be re-cited to reintroduce the E-260-removed live-relay apparatus); (5) `context-fundamentals/SKILL.md` L28 + L193 stale ambient figures refreshed (IDEA-118; "614-886"→"730-1,000", "~750"→"~780", derived from live counts, consistent with the E-260-04-owned :70-90 whole-layer section). `perspective-provenance.md` field table: public-scorebook disagreement caveat on the scores row (E-261 12-4 vs 12-5) + "public games id" promoted Uncertain→definitively perspective-specific (IDEA-128). Two doc-sweep siblings surfaced but left OUT of scope by PM (new IDEA filed): the `context-fundamentals` worked-example block L160-190 (its own stale figures, deliberately scoped out by IDEA-118).
- **E-262-06 (Step 1d preflight + gate-ordering)** — `implement/SKILL.md` Step 1d, two procedure corrections: (1) creds preflight pinned to `bb creds check --profile web` (IDEA-122 — the bare multi-profile check exit-0-PASSES on a MIXED state where valid mobile masks dead web, and the smoke's `bb report generate` uses web; NO command-side fix, since "any valid profile = usable" is a legitimate contract); (2) [RETIRED 2026-07-26 — the ratchet gate this protected is gone, so the staticness requirement went with it; only the plays-rich half survives] generate→reconcile-scoreboard false-FAIL resolved at the ROOT via a **terminal-fixture requirement** (IDEA-123 — the `.smoke-fixture` `generate` target MUST be a terminal GC team page with high play-by-play coverage; a static corpus means generate ingests no net-new plays, so the post-generate scoreboard reading measures only the epic's own derivation effect, never a self-caused delta that false-trips the one-way ratchet). Order KEPT (not reordered), baseline re-snapshotted ONCE at operator-owned fixture bootstrap (E-257 precedent), with a data-bearing plays-coverage bootstrap check (count of games that actually carry play rows, not a bare games count — scored-but-empty is modal). The real fixture identifier stays ONLY in the gitignored `.smoke-fixture`; no GC identifier in tracked skill text. Settled Step 1d text relayed as the sync source of truth for story 07 (docs-writer mirror into `docs/admin/production-deployment.md`).
- **[E-262-04 rename]** IDEA-113 completed — `src/db/season_aggregates.py` → `src/db/season_projection.py`; the historical `season_aggregates.py` tokens in E-237/E-246/E-259 above are reconciled in place (see the E-259 entry's `[E-262-04 filename reconciliation]` note), preserved as accurate-at-the-time history.
- **T7 ratchet**: E-262's OWN four-subtree contribution is trivial and defect-cited — ~+3 net to `.claude/rules`+`.claude/skills` (stories 05/06) plus this closure's `.claude/agent-memory` reconciliation (DE/PM/CA). `context-ratchet.sh` FAILs (~+160 vs the 2026-07-12 baseline), but the FAIL is DOMINATED by ~+157 of PRE-EXISTING agent-memory drift identical on `main` and the worktree (prior epics' closure memory the operator has not re-snapshotted) — the exact same disposition as E-261's T7 note above (then +132). Correct resolution: an OPERATOR-SIGNED re-snapshot at closure (`--update-baseline`, operator-only per E-260), NOT an offset obligation on E-262. Requiring E-262 to delete unrelated pre-existing memory to "pay for" +3 defect-cited lines would be scope creep AND destruction of legitimate historical codification (the CORE LESSON: the simple-first principle guides FUTURE decisions, it does not license deleting existing working context).

## E-264 — League-Aware ERA Basis Fix
Feature/data epic (NOT a meta-epic — the E-260 freeze binds the dispatch/review/context-layer machinery, not domain schema/behavior). GameChanger computes ERA on a per-team-season game-length basis (`ERA = innings_per_game x ER / IP`, observed 6 or 7 innings); we were hardcoding 7, over/under-stating opponent ERA for teams playing a different regulation length. Fix: new nullable `teams.innings_per_game` (migration 012), fetched from `settings.scorekeeping.bats.innings_per_game` on `GET /teams/{gc_uuid}`, applied via a shared `era_basis_innings()` helper at the two ERA compute sites. Context-layer codification — ONE trigger fired (T1 data-model):
- **T1 — data-model.md entry (`.claude/rules/data-model.md`, new "ERA basis" bullet at L19, directly after the parallel "Season year" bullet)**: documents `teams.innings_per_game` as the per-team-season ERA (and K-per-game) game-length basis; the LOAD-BEARING invariant that **NULL is provenance** (never fetched → assumed-7 fallback via `DEFAULT_ERA_BASIS_INNINGS` + the report's "(assumed)" disclosure depends on it); the HARD prohibition **never add a DEFAULT or NOT NULL and never blindly backfill NULL→value** (either silently erases the assumed-vs-fetched distinction); and that it **self-heals like `season_year`** via `ensure_team_row(innings_per_game=...)` (NULL-safe, never clobbers a stored int). This gives the invariant a durable home so it no longer depends on the frozen migration-012 comment. The NULL-as-provenance + no-DEFAULT/NOT-NULL/backfill pattern mirrors `season_year`'s self-heal precedent exactly — the two bullets now sit side-by-side as a matched pair.
- **T2 verdict — NO CLAUDE.md canonical-seam note for `era_basis_innings()`** (PM concurred): it is a two-line, two-site DISPLAY-calc helper. The canonical seams in CLAUDE.md Architecture govern INSERT/dedup/connection/path invariants that many independent paths MUST route through on pain of CORRUPTION or a SECURITY downgrade (`ensure_team_row`, `merge_duplicate_game`, `get_connection`, `resolve_db_path`, `is_production`, …). Re-inlining `era_basis_innings()` risks at most a low-severity display inconsistency, not corruption — below the seam bar. The data-model.md bullet (which names the helper) is the right durable home; adding a CLAUDE.md note would be net-growth bloat against the freeze/ratchet with no defect to cite.
- **T5 verdict — NO baseball-coach agent-memory note** (PM concurred; also I may not author another agent's memory): the "K/9 is deliberately NOT rebased; the K-rate decision is deferred to IDEA-141" fact is already in the epic's Non-Goals/TN-9 and IDEA-141. It is an API-schema/scope fact, not a coaching-judgment heuristic — the class baseball-coach memory holds. No follow-up sweep warranted.
- **T7 ratchet**: my only measured-subtree contribution is this `.claude/agent-memory` entry (+ the MEMORY.md pointer); the data-model.md bullet lands in `.claude/rules` (~+1 net). Same standing disposition as E-261/E-262 above — the ratchet baseline carries pre-existing agent-memory drift resolved by an operator-signed re-snapshot at closure, not an offset obligation on E-264. Additions kept tight; a one-line durable-invariant bullet replacing a frozen-comment dependency is net-positive, not bloat.

## E-267 — Reconcile-at-Load Against the Fresh Crawl
Feature/data epic (NOT a meta-epic — the E-260 freeze binds the dispatch/review/context-layer machinery; the behavioral codifications below are all DEFECT-CITED, each naming a concrete instance from this epic's dispatch). Made the scouting/report load path RECONCILE-AT-LOAD across three grains (game / player-line / roster) instead of accumulate-only. Six triggers fired (T1/T2/T3/T5/T6/T8); T4 NO.

- **T1/T2 — canonical seam + destructive-reports fact (CLAUDE.md Architecture, two new bullets)**: **Canonical reconcile-at-load (retire-absent)** — `classify_absences()` + per-grain retire helpers in `src/db/reconcile_at_load.py`. NO snapshot table: the DB IS the prior set, the fresh crawl is the authority, so corroboration is a HEALTH GATE on the fresh payload, not a history diff (this is why the epic is migration-free). Grain contract: the classifier is PURE (id sets in, classification out — no DB handle, no I/O, no logging); the retire helpers are connection-in / no-commit / caller-owns-the-tx and own the DELETE + both WARN classes. HARD-DELETE uniform (a soft-retire marker would force `WHERE is_retired IS NULL` into every reader — one miss silently re-inflates the exact corruption being fixed). Two easily-broken invariants pinned: health-gate numerator and denominator drawn from the SAME population (`prior ∩ fresh` over `prior`), and `extra_guard` is narrowing-only (structurally cannot widen a retire). **⛔ THE FIRST INVARIANT AS PINNED HERE IS NECESSARY BUT NOT SUFFICIENT, AND AS WRITTEN IT PASSED THE DEFECT — corrected at E-276.** Same-population was satisfied throughout the broken code: both sides were drawn from the *polluted* post-upsert set, so the gate measured `|fresh| >= |stale|` and was not a health gate at all. The completing half is TEMPORAL and is the load-bearing one: on the grains that have a gate, that population must be the PRE-UPSERT one, captured by the CALLER before any of this run's writes to that grain's delete scope (`snapshot ∩ fresh` over `snapshot`). Never accept "same population, therefore sound" as proof — it is exactly what four review layers accepted here. And the invariant is no longer uniform: E-276 REMOVED the roster grain's floor entirely, so that grain has no ratio to state an invariant about; `MAX_ROSTER_DEPARTURES` is its sole guard. CLAUDE.md's seam bullet carries the corrected form. Second bullet: **report generation is now DESTRUCTIVE** (`generate_report()` → `load_team` → reconcile can hard-delete `games` + full child surface) — the reports surface's first destructive side effect, and CLAUDE.md previously described the flow as if it were purely additive.
- **T3 — three behavioral footguns, each placed where it BINDS rather than where it is merely recorded:**
  - `.claude/rules/python-style.md` (`**/*.py`) — **a missing safety signal defaults to REFUSE.** Three instances in ONE epic (`getattr(..., True)`, a `bool = True` health-flag default, an empty exemption set from a planner that raised); the sharpest went WAL-lock `OperationalError` → caught → retire proceeds → permanent identity split, no self-heal. Also names the defense that shields this shape — *"restores the prior behavior rather than blocking"* — which reads conservative while preserving the bug under repair.
  - `.claude/rules/worktree-isolation.md` (`**`) — **never `git checkout -- <file>` in a dispatch worktree.** With prior stories staged by the staging-boundary protocol, checkout restores the STAGED (previous story's) version, not HEAD. Property of the dispatch protocol, not of E-267: it bites any epic whose stories share a file, and the mutation-verification practice invites it. `cp` to scratchpad before mutating. **⚠️ GENERALIZED at E-278 — naming one SPELLING was the defect in my own codification.** The rule as I wrote it named `git checkout --` only; `git checkout-index -f` and `git restore` are the same operation, and an SE who had QUOTED this rule during the dispatch then ran `checkout-index` and lost work, pattern-matching on the command NAME. See the E-278 entry.
  - `.claude/rules/testing.md` — **stale `__pycache__` silently INVERTS mutation results.** `.pyc` invalidation is `(mtime, size)`, so size-PRESERVING mutations (reorder/move) do not self-invalidate, making ORDERING invariants the least-trustworthy mutation class by default. In-epic, the interpreter ran the mutant while grep, `inspect.getsource`, and the file on disk all read correct. Paired remedy (SE's hardened protocol): no-mutation control first, cache cleared BOTH ways, mutation asserted-applied (an unmatched target silently measures clean code and reports as an uncaught mutation), re-run after refactors.
- **T3 (CR-routed) — "annotating a fixture limitation is not covering it"**, homed in BOTH directions because it binds implementer and reviewer differently: `.claude/rules/testing.md` gets the implementer-side section (plus the compounding case — hardening a fixture against one hazard can make its neighbour structurally undetectable, as E-267's unique-name helper did); `.claude/agents/code-reviewer.md` Priority 3 gets the reviewer-side directive (treat the annotation as a defect marker; ask what would have to be true for the uncovered path to matter). Self-demonstrating: the rule predicted the defect the same review round then found. CR explicitly routed this to me rather than writing it into its own definition.
- **T3 — coherent-garble Read (`.claude/rules/tool-output-integrity.md`)**: cited as a CONCRETE CASE rather than restating the principle, because this is the rare instance where the existing rule was followed and WORKED. A Read returned nonempty, coherent, topically-WRONG content that rendered precisely the defect the review round was hunting; only a disagreeing second Read + a grep for the fabricated tokens exposed it. The durable addition is the trigger heuristic: **a read that hands you exactly the defect you were looking for is a cross-check trigger, not a finding.**
  - **RE-ADJUDICATED 2026-07-25 — the read was ACCURATE; there was no garble.** Transcript evidence (`5ba23781.../subagents/agent-aSE-*.jsonl` + `agent-aPM-*.jsonl`): SE wrote `_pop = any(b.populated…)` / `frozenset().union(…)` / the `team_id`-dropped SQL into the worktree file at 21:35:44Z as a mutation-testing mutant, PM's Read landed 21:36:02Z rendering them at 693-697, SE restored from `scratchpad/rcl3.bak` at 21:36:12Z, and PM's second Read (21:36:20Z) + grep (21:36:28Z) then correctly found nothing. **The file oscillated under a concurrent writer on a ~30s cycle.** What I codified as the sharpest known garble was a correct read of a transient state. The rule's own remedy (second read + grep) does NOT discriminate the two causes — it produces identical evidence for both — so the amendment (2026-07-25) adds a DIFFERENTIAL step (harness "modified on disk" note as primary evidence, `stat` mtime vs. read time, enumerate other writers, grep the writer's `subagents/*.jsonl` payload) and retargets this anecdote. **Garble is still real and kept** — its independent anchor is `.project/research/E-231-harness-repro/harness-output-reliability-report.md` (bare `echo` corrupted, 19/17/18 Reads against a stable `cat -n` 1-31, false-negative Glob), none of which a moving file explains. The misfile is expensive because the two causes demand OPPOSITE actions: had SE not restored, the garble diagnosis would have DISMISSED a mutation that was really in the tree. Same family as the measurement footgun below (a subtree other agents are writing is a reading, not a constant) — codified there for measurement, missed here for reads. Corrected copies: `.claude/agent-memory/code-reviewer/tool_gotchas.md`, `.claude/agent-memory/product-manager/feedback_clean_reread_before_defect.md` (its "if two of my own reads disagree, the problem is my read, not the file" was categorically false) + `lessons-learned.md` #8. Left standing as a historical record: the E-267 archived `epic.md` History block.
- **T5 — GC schedule-array domain knowledge (CLAUDE.md L77 `/games` bullet)**: api-scout's 633-record live probe. GC RETAINS not-final AND long-past-unplayed games indefinitely, so absent-from-full-array = genuine removal, NOT postponement; a reschedule is a PATCH-in-place preserving the `id`; `game_status` may be the key ABSENT, `null`, or `"new"` (membership must be `.get("game_status") == "completed"`); and a consumer diffing against the completed-ONLY subset would misclassify every present not-final game as removed (the mass-false-delete path). The modal-payload fact — a scored-but-EMPTY boxscore has envelope + categories present with per-player `stats: []`, so a bare 200 is NEVER authority to retire — went into the seam bullet, where the retire logic that must respect it lives.
- **T6 — CLI (CLAUDE.md Commands)**: added `bb db purge-scouting` (had ZERO CLAUDE.md footprint while the section is detailed about every other `bb db` command). Records the 20-purge/7-keep partition, `is_production()` guard + `--force`, single transaction with FK enforcement ON, all-reports HTML unlink — enumerate paths INSIDE the transaction before the DELETEs, unlink AFTER the commit (NOT `cleanup_expired_reports()`, which is expiry-filtered). That split is the E-267-06 ruling: AC-5 originally demanded unlink-before-DELETE, SE deviated because `unlink()` is not rollback-able, PM ruled the AC self-contradictory against AC-6. Accepted cost: a crash between commit and unlink orphans HTML on disk. **My first pass wrote "unlinked first" and flattened exactly this distinction** — caught at closure integration review. Lesson for future Commands-section entries: compressing an operator command into one dense sentence is where a deliberate ordering ruling gets erased into its pre-ruling form, and CLAUDE.md is the worst place to land a false invariant.

**STANDING CODIFICATION CHECK — verify CONCLUSIONS harder than STATEMENTS (two defects, one closure, E-267).** Both false claims I shipped this closure had identical shape: every constituent fact verified, the RELATION between them not. (1) purge-unlink — each fact true, the word "first" ordering them false; (2) the reconcile module docstring concluded that a `player_id` re-issue "should REFUSE rather than delete" from the true same-population premise, and it does NOT refuse (the DB read sits inside the load's uncommitted transaction AFTER the fresh upsert, so `prior` is `old ∪ fresh`; `comparable = prior ∩ fresh` collapses to `fresh` and the gate becomes `|fresh| >= |old|`, which passes at the boundary — **and the "benign, since dedup would have merged the rows anyway" ruling I attached to it was WRONG, REFUTED BY EXECUTION at E-276: a full player-id churn (9 brand-new ids vs 9 stored) hard-deleted all 9 live lines, uncapped.** The decisive reason is ORDERING, and it refutes the ruling structurally on its own: the retire runs BEFORE `dedup_team_players` by explicit design in both grains, and both call sites state why — so dedup cannot have merged anything yet. Two further reasons, each independently sufficient: `find_duplicate_players` joins `team_rosters` twice, so a churned id appearing only in a boxscore is invisible to dedup entirely; and merging is not resurrection — it re-points SURVIVING rows and cannot restore what a hard delete already removed. So the docstring's conclusion was false AND my dismissal of it was false). **The rule: when compressing a source docstring into the context layer, the claims to verify hardest are the ones the docstring CONCLUDES, not the ones it STATES — a stated fact can be grepped, a concluded one must be TRACED.** Corollary that saved the second one: also check whether the PREMISE I carry can be used to re-derive the false conclusion. "The DB IS the prior set" was true-as-architecture and misleading-as-read, so CLAUDE.md now pins that `prior` = `old ∪ fresh` at reconcile time, the operator-visible `user_team_access` consequence (login survives, grants do NOT auto-restore — explicit admin re-grant), and the standing obligation that a future KEEP→PURGE foreign key silently breaks the command while the partition drift test does not catch it. **⛔ E-276 EXTENDS THIS CHECK, because it did not save me from the clause immediately beside it: A MITIGATION IS A CONCLUSION TOO.** I traced the docstring's conclusion, found it false, wrote it up — and in the same sentence reached for a safety net (`dedup`) without checking whether it was DOWNSTREAM of the harm. It was not: the retire runs first, by design. **The transferable shape is *a mitigation named in prose, never executed, protecting a path it structurally cannot reach*** — and it is more dangerous than the false conclusion it excuses, because it converts a live defect into a closed item. So the check now has two halves: verify what the source CONCLUDES, and verify what YOU conclude about the consequences — in particular, for any "X would have caught this anyway", establish that X runs *after* the harm and can see it. A dismissal is the one claim nobody re-opens, because the finding above it was correct.
- **Eviction sweep**: nothing in the context layer asserted the reports surface was non-destructive or the load path accumulate-only, so the sweep was near-empty as predicted. ONE adjacent staleness risk fixed: `.claude/rules/data-model.md`'s plays "Never delete-and-reinsert" rule could be misread as barring the new game-grain retire — scoped in place to LOAD strategy, with the retire named as out of its scope.
- **T7 ratchet (recorded honestly, not offset)**: the raw ratchet FAILs at +749, but `main` ALREADY fails it by +564 independently (baseline `2026-07-13`, stale). **E-267's own context-layer contribution is +236**, measured DIRECTLY against main at merge (`.claude/rules` +16/−1, `.claude/agents` +2, `.claude/skills` 0, `.claude/agent-memory` +222/−3). My codification is +36 of that (rules +15, agents +2, agent-memory +19); the CLAUDE.md bullets are NOT ratcheted — CLAUDE.md sits outside the four measured subtrees. Composition is the part worth remembering: **93% of the growth is agent-memory; the rules and agent definitions that actually BIND future work cost +17 lines total.** Same standing disposition as E-261/E-262/E-264: resolved by an OPERATOR-SIGNED re-snapshot, not an offset obligation on this epic.

**MEASUREMENT FOOTGUN (I got this wrong three times running):** I reported +214, then +217, then +221 by taking a mid-closure snapshot of the epic's contribution (+185) as a fixed base and ADDING my own deltas to it. The arithmetic was right every time; the operand was stale. Other agents (api-scout, code-reviewer, docs-writer, baseball-coach, PM) kept writing their own memory throughout closure, so the "base" moved under me while I treated it as settled. **Rule: measure the epic's context-layer total DIRECTLY against main at the moment you report it (`git diff --numstat main -- .claude/rules .claude/agents .claude/skills .claude/agent-memory`) — never carry a base forward and add to it.** A snapshot of a subtree other agents are concurrently writing is a reading, not a constant. This is the same family as the two false claims above: each part verified, the RELATION between them (that the base still held) not.
- **T8 promotion gate — all five candidates PROMOTED on condition (a)**, with (b) recorded INDETERMINATE (unsatisfiable by any epic until the operator re-snapshots). PM's reasoning is worth keeping: the gate exists to PRICE promotions, not to make them hostage to unrelated drift accounting — the alternative reading would have the epic that produced the most reusable findings promote none of them. **The forward lesson for this gate: when (b) is blocked by inherited drift, promote on (a) and record the exception visibly rather than silently deferring.**

## E-273 — Reclaim Orphaned Reference Data After Report Deletion
Feature/data epic (not a meta-epic; the E-260 freeze binds only the dispatch/review/context-layer machinery). Added a reachability-based terminal sweep (`reclaim_orphan_reference_data`, `src/reports/lifecycle.py`) that hard-deletes `teams`/`players`/`team_rosters` no surviving report can reach — the reference tier a per-report imperative cascade structurally cannot reclaim ("is this team reachable?" is a GLOBAL question; a cascade answers only the LOCAL "what does this team own?"). Wired terminally into `_delete_report` + opportunistically into `cleanup_expired_reports` (which ALSO fires at every `generate_report` start). Context-layer verdicts: T1 YES, T2 YES, T3 YES, T4 NO, T5 folded, T6 YES-but-no-Commands, T7 inherited-drift exception, T8 one promotion.

- **T1/T2 — canonical seam + a SECOND destructive-reports axis (CLAUDE.md Architecture, one new seam bullet + a reworked destructive bullet)**: seam records the single-source id-set producers (`_orphan_team_ids`/`_orphan_player_ids` feed BOTH the DELETE and the invariant `count_orphan_reference_data`, so count and delete cannot drift — TN-8, the same "de-dup the dedup" hazard as recon_scoreboard/player_dedup); the THREE exclude-not-clear operator/security roots (`opponent_links.resolved_team_id`/`.our_team_id`, `user_team_access.team_id`) **[⚰ ANNOTATED 2026-07-27, NOT rewritten — this correctly records what E-273 codified; E-277-01 added a FOURTH root (`scheduled_report_runs.own_team_id`) and E-277-04 corrected the seam bullet to "four roots across three tables". The authority is `_TEAM_BASE_PRED` in `src/reports/lifecycle.py`, never this line. Found by sweeping my own memory after `pm4` found the identical stale count in `archived-epics.md` — the epic's signature defect surviving in the memory files of the epic that fixed it everywhere else]**; the six-game-child-FK belt-and-suspenders clause with WARN-on-fire (a corrupt non-participant row is gracefully SKIPPED, not an IntegrityError that aborts the whole sweep); and the pass-OWNS-its-transaction NAMED EXCEPTION (reap-then-gate + gate/compute/delete in ONE `BEGIN IMMEDIATE`, TOCTOU-safe; `deferred` is a liveness delay, not a safety hole). The key T2 move: the prior E-267 "report generation is DESTRUCTIVE" bullet was REWORKED into "report deletion AND generation are DESTRUCTIVE" so the two hard-deleting passes (reconcile-at-load on `games`; reclaim on the reference tier) read as ONE coherent fact — generation is now destructive on BOTH axes. Leaving E-267's bullet untouched and appending a second would have produced two adjacent bullets that each imply they name THE destructive behavior; codifying a new destructive side effect next to an existing one is a MERGE-the-statement job, not an append job.
- **T3 — the belt-and-suspenders game-child FK clause kept TIGHT in CLAUDE.md**: the seam bullet states the PRINCIPLE (exclude any team referenced by a surviving game's child FK across all six tables incl. `game_perspectives`, WARN-on-fire); the full per-column enumeration stays in code + epic TN-2. `game_perspectives` was the sixth child the original grep-based enumeration missed and Codex caught — the exact defect T8's promotion is built from.
- **T6 — new operational surface DELIBERATELY not added to CLAUDE.md Commands**: `scripts/reclaim_orphan_reference_data.py` is a THROWAWAY one-shot (imports and calls the same pass — DRY, zero duplicated predicate), not a `bb` subcommand, so it earns no Commands entry (bias to NOT grow CLAUDE.md for a throwaway; the E-273-05 vehicle is decoupled-from-generation, not a standing surface). The operator runbook (one-shot + baseline re-snapshot, TN-10) is docs-writer's `docs/admin/operations.md`, not CLAUDE.md.
- **T7 ratchet — inherited-drift exception, measured directly against merge-base (E-267 disposition)**: raw ratchet FAILs at +827 total, but the merge-base IS current `main` and `main` ALREADY fails by the SAME +827 before E-273 touches anything (identical delta in the worktree and the main checkout; the committed baseline `2026-07-13` is stale). **E-273's own four-subtree contribution is small (my code-reviewer.md checklist item + this codification entry; the exact `git diff --numstat <merge-base>` net is the number to trust at report time, never a carried-forward base — the E-267 measurement footgun).** CLAUDE.md edits are NOT ratcheted (CLAUDE.md sits outside the four measured subtrees), so the seam bullet + destructive-bullet rework cost the ratchet ZERO. Same standing disposition as E-261/E-262/E-264/E-267: resolved by an OPERATOR-SIGNED re-snapshot, not an offset obligation on this epic. Did NOT run `--update-baseline`.
- **T8 (E-273) — ONE promotion (lesson a), ONE decline (lesson b)**: promoted **"exhaustive-class claims — verify by independent enumeration, never by spec/code/test agreement"** into `code-reviewer.md`'s Bug Pattern Checklist (Priority 2, beside "Caller audit"), defect-cited to Codex-F1: AC-2 named 5 of the 6 `teams(id)` game-child FK tables, and the spec, the orphan-predicate code, AND the test all agreed on the incomplete 5 — a spec-matched enumeration structurally cannot catch a spec-sized gap; only an independent FK grep surfaces the 6th (`game_perspectives`), whose omission would have aborted the sweep with an IntegrityError. It BINDS the reviewer where it fires and rides into the Codex review prompt via the checklist delimiters, and it is the reviewer-side edge of the recurring complete-audit-first theme. DECLINED lesson (b) (a global sweep newly wired at generation-start ripples into unrelated tests seeding bare tracked teams): real but SITUATIONAL one-time integration friction, not a durable review-discipline rule — general fixture/behavior-preserving guidance already covers it, and it is not worth growing an over-baseline layer. Per the E-267 T8-gate disposition, a defect-cited promotion rides the inherited-drift exception rather than being held hostage to unrelated drift accounting.

## E-270 — Harden Reconcile-at-Load and Purge

- **T1/T3(a) — the WAL-pragma boundary placed AT the canonical bullet, not only at the defect site (CLAUDE.md)**: `preview_purge` opened via `get_connection()`, whose unconditional `PRAGMA journal_mode=WAL` is a PERSISTENT file property — so a "read-only" preview REWROTE a non-WAL target's journal mode before the operator confirmed anything, surviving every abort path (Codex P1). The fix is `mode=ro`. The codification move: the boundary went on the **canonical connection-factory bullet itself**, because that bullet is what a future agent reads before reaching for the factory — documenting it only in the purge paragraph leaves the misuse point unguarded. Framed as a SCOPE BOUNDARY, not an exception: the canonical rule already says "every SQLite **writer**", so a reader was never in scope; routing this reader through the writer factory was a misapplication, not a deviation. An exception invites re-litigation; a scope boundary does not.
- **The predicate is "must not MODIFY the target", NOT "is a reader"** — pinned explicitly because the obvious rewrite overshoots into a categorically false rule. Many legitimate readers use the factory and SHOULD (`src/api/db.py`'s health check, `get_pitching_workload`/`get_pitching_history`), for the `busy_timeout`. This epic shipped that exact overcorrection into CLAUDE.md before it was caught.
- **T2 — guard surface == delete surface (CLAUDE.md reconcile-at-load bullet)**: the cross-perspective protection predicate reads the SAME `_PERSPECTIVE_CHILD_TABLES` constant the whole-game delete loops, never a hand-list, so a future sixth child table extends BOTH at once. Same family as E-273's single-source-id-set and recon_scoreboard's "de-dup the dedup".
- **T3(b) — the CASCADE silent-success footgun (CLAUDE.md purge paragraph)**: a KEEP→PURGE FK aborts LOUDLY only for a DEFAULT-ACTION FK. `ON DELETE CASCADE` raises NOTHING — the purge COMMITS while cascade-deleting the preserved identity row; `ON DELETE SET NULL` also does not raise (but only blanks the reference, row survives). Verified by reproduction across all three actions. Consequence for the paragraph's framing: the ACTION-AGNOSTIC plan-time test is the PRIMARY guard and the runtime abort is a BACKSTOP — the inverse of how the text originally read.
- **T5 — domain knowledge placed as a CORRECTION to an existing ambient claim, not an append (CLAUDE.md public-endpoint bullet)**: the bullet said a game FULLY ABSENT from the array is "a genuine removal/void". True of GC's retention semantics, but a future epic diffing OUR STORED ids against that array would invert it into "absent ⇒ removal" — which api-scout's probe refutes: ~4% of stored ids (22 of ~583) are absent purely as cross-perspective TWINS, ALL false removals. Also recorded the truncation envelope (un-paginated; a truncated body is a JSON PARSE error, never a short valid array; `completed` terminal), which is why `MAX_GAME_RETIREMENTS` is a backstop against an UNOBSERVED mode. The durable evidence already lived in api-scout's `docs/api/endpoints/get-public-teams-public_id-games.md` — the gap was RECALL at the point of misuse, so the fix is a qualifier on the trap sentence, not a duplicate of the data.
- **T8 — TWO promotions, TWO folds (no standalone entries for 3 and 4)**: (1) **"an absence claim needs proof the mechanism COMPLETED CLEANLY, not merely ENTERED"** → `.claude/rules/testing.md` (path-scoped, `tests/**`+`src/**`). Its first formulation ("proof it RAN") was falsified INSIDE this epic — a `side_effect` spy records the call BEFORE invoking the wrapped function, so patching the reconcile to RAISE still satisfied `call_count: 1`, the expected ids, and "A lines survive: True". On this codebase the evidence is the RESULT OBJECT (`LoadResult.errors == 0`), because the swallow-and-count pattern is deliberate and load-bearing on the destructive paths (`game_loader` catches broadly, logs ERROR, `return 1` → `result.errors`). (2) **the prose-authoring rule** → `.claude/rules/tool-output-integrity.md` (universal-behavioral, `paths: "**"`), as a new "Prose you AUTHOR is a claim too" section: that file already governs what you READ, and the same discipline binds what you WRITE. Enumerate every symbol AND path cited, EXECUTE behavioral claims, cite a stable anchor. **Lesson 4 (stable anchor over line range) FOLDED into it** rather than standing alone — it is the same authoring act, and a separate rule would have cost lines for a clause. **Lesson 3 (mutant quality) FOLDED into `testing.md`'s existing `__pycache__` mutation-protocol bullet** — same paragraph, same activity; a new section would have duplicated its framing.
- **The safety-comment sub-class, localized**: alarming prose is self-protecting (a claim naming the SCARIER outcome feels right for a safety note and is least likely to be challenged) — sharpened to **the tidy general rule at the END of a safety note is where this lands, because that is the sentence that sounds most authoritative and gets checked least**. That position is also where OVER-correction lands. Six prose defects across five shapes drove this; the epic then reproduced the class twice more while correcting it.
- **Placement economics under an over-baseline ratchet**: CLAUDE.md is OUTSIDE the four measured subtrees, so T1/T2/T3/T5 cost the ratchet ZERO — and each of them belonged at a point-of-use in CLAUDE.md anyway, so this was not gaming the instrument. Only the two T8 promotions touch measured files. Same inherited-drift disposition as E-261/E-262/E-264/E-267/E-273: the layer was ALREADY over the stale `2026-07-13` baseline before this epic's codification, resolvable only by an OPERATOR-SIGNED re-snapshot. Did NOT run `--update-baseline`.
- **Eviction sweep — ran, ZERO evictions.** `.claude/rules/` and `.claude/agents/` carry no purge references at all; `docs-writer/MEMORY.md` was already reconciled to the E-270-02 surface (2026-07-25) and is CURRENT, not stale; PM's records are historical. The E-252 record calling `get_connection()` "the single factory" is NARROWED by the new boundary but not falsified (it describes the writer triad).

## E-272 — Season x Level -> League Classification (+ NRBL)

- **T1 — season as a first-class classification AXIS, and the precedence predicate is bracket PRESENCE (`.claude/rules/pitch-rules.md`)**: new "Season as a Classification Axis" section carrying the full ladder (DB fields > recognized `ngb` > any `\d+U` bracket > season-disambiguated level word > `unknown`). The subtle part is #3: the ladder keys on bracket PRESENCE, not on the bracket being MAPPED — `_league_from_age_bracket` returns `None` only when there is no bracket at all, so an UNMAPPED 14U also outranks every level word ("Lincoln 14U Reserve" -> `youth_travel`, NOT `nsaa_subvarsity`). I shipped a bullet TITLED "a *mapped* bracket" over a body that covered both, and CR caught it: the title named the wrong predicate rather than merely omitting a case, which is why the fix was to widen the title, not to add a sub-bullet. That case is also E-272's only change in the LESS-strict direction, so it now appears in the ladder itself rather than only in the Youth/travel paragraph 147 lines below.
- **T1(b) — distinct-constant-per-league extended to NRBL**: `NRBL == LEGION` byte-for-byte today but is its OWN constant, mirroring the `PITCH_SMART_15_18` precedent — separately-governed bodies that merely AGREE right now. Documented in the new pitch-rules NRBL section with the rationale sentence, and NRBL's rest curve is REFERENCED from the coach model doc (mirroring Sub-Varsity), never inlined.
- **T2 — SELECTION vs MAPPING, the seam that binds an undispatched epic**: E-263-02c (READY, not dispatched) moves LEVEL SELECTION from inference to an operator pick; E-272 changes level+season -> league MAPPING. Reworded pitch-rules' "Forward direction (E-263)" note to the split, with the load-bearing seam: **operator pick authoritative WHEN SET; the unset branch DELEGATES to `detect_league_level`** — because `bb report morning-run` has no operator at the keyboard and always infers, so forcing Pitch Smart on unset would re-introduce the 18U-Legion bug on the strategic forward surface. Without this the doc would assert two conflicting "how the level is chosen" stories.
- **T3(a) — the load-bearing swallow-everything handler (NOT codified in a file I own; routed)**: `generator._fetch_public_team_info` sets all five API signals inside ONE `if status == 200:` block under a bare `except Exception`. That coarseness is what makes the league gate FAIL-SAFE — a fetch failure nulls every signal together, so detection returns `unknown` and the card SUPPRESSES rather than silently mis-classifying. A "cleanup" making the handler granular enough for `season` to fail independently would CREATE a silent under-rest exposure. The dangerous change here is the obvious code-quality improvement. IDEA-168 records it, but an idea file is not where a refactorer looks and the handler carries NO comment — recommended to the lead for SE routing, since `src/` is not mine to edit.
- **T3(b) — the word-boundary plural trap**: `\breserve\b` did not match "…Reserves", so a plural-named team fell to `unknown` and rendered the SUPPRESSED "League not detected" card with no rest calls. Fixed to `\breserves?\b`. Recorded, not promoted to a rule: the failure is silent (a drop to a fallback, never an error), but it is now pinned by a test and one instance does not earn lines in an over-baseline layer.
- **T5 — domain knowledge, placed where each piece binds**: NRBL exists as Nebraska's reserve-tier SUMMER league, ALB-equivalent today, with NO `programs.program_type` — so its mapping row is marked **inference-resolved** in both DB columns rather than fabricating a `program_type` it does not have. The season vocabulary is **observed, not specified** (live probe n=18: `summer` x17, `spring` x1 — an OPEN vocabulary, not a closed enum; IDEA-168). And the NSAA-Varsity-vs-Legion crossover: NSAA Varsity UNDER-rests relative to Legion at 46-50p, 61-70p, 81-90p, plus 110 vs 105 post-April.
- **The defect this epic kept re-finding — a retired claim's residue.** TN-4 asserted the season-absent default is "the stricter table, so an ambiguous season over-rests". True for the sub-varsity branch, BACKWARDS for varsity. I declined to restate it while drafting the axis section and did the arithmetic instead; it had already reached a shipped code comment (`starter_prediction.py`, since remediated), IDEA-168's urgency rating, and my own draft. Five survivals across four structural positions plus CR's sixth shape.
- **T8 — ONE promotion, SPLIT across two rules because it is two different failures.** The authoring half -> `tool-output-integrity.md`, as **"A claim you RELAY is a claim you AUTHOR"** inside the existing "Prose you AUTHOR" section: that heading is itself the loophole, since the defect travels by INHERITANCE and a spec that passed internal review plus a Codex pass is borrowed authority, not verification. The DETECTION half -> `doc-sweep.md`, as "Retired Claims Survive in Forms Carrying None of Their Tokens", with the five-shape taxonomy (title / index row / **derived rating sharing none of the claim's words** / compressed adjective / hidden behind a legitimate neighbouring use of the same token) and the operative instruction: when the swept concept is a RETIREMENT, enumerate the JUDGEMENTS THAT DEPENDED on the claim, not just rephrasings. The derived-rating shape is what forced the split — a rating has no sentence to check, so a prose-authoring rule structurally cannot catch it.
- **Placement correction worth remembering — verify where a memory file LIVES before proposing to extend it.** PM suggested extending `feedback_descope_grep_gap` (the same lesson in the de-scope direction) as the cheap option, and I leaned that way. It lives in `/home/vscode/.claude/projects/*/memory/` — the MAIN SESSION's harness memory: outside the repo, outside the ratchet, not version-controlled, and loaded ONLY into the main session. The lesson binds code-reviewer and implementers doing doc sweeps, so it would have landed where it could never fire. Cheap and inert beats nothing, but not by much; `doc-sweep.md` auto-loads for the reviewer via the Step-2 rule-glob on exactly the affected paths.
- **T7 ratchet — same inherited-drift disposition, measured not carried forward**: E-272's OWN four-subtree contribution is **+79** (`league-pitch-rules.md` +42, `pitch-rules.md` +37, `baseball-coach.md` 0) against a total overrun of **+950**, so **+871 (91.7%) predates this epic** (E-262/E-270/E-273 deferrals against the stale `2026-07-13` baseline). Codification added a further ~11 lines to `.claude/rules`. Operator-signed re-snapshot, not an offset obligation on E-272. Did NOT run `--update-baseline`.
- **Eviction sweep — ran repo-wide plus a CONCEPT sweep; both removed symbols clean, two stale hits found in a dir I do not own.** `_NAME_KEYWORDS` and `test_subvarsity_same_rest_tiers_as_pre_april` survive only in correct historical/retraction contexts (a test docstring naming "the old table", planning artifacts, and coach's explicit retraction). The token sweep alone would have passed — the CONCEPT sweep (the retired "sub-varsity implementation gap" claim, which shares no tokens with either deleted symbol) found two live stale assertions in baseball-coach's memory: `MEMORY.md`'s index row and `probable-starter-model.md`'s cross-reference, both asserting a gap the topic file itself retracted on 2026-07-18. Reported to the lead for routing to coach. This is the E-259 topic-file lesson recurring, and an instance of the very shape T8 codifies.

## E-276 — Reconcile-at-Load Health Gate: Capture the Prior Before the Run's Own Writes
Feature/defect epic correcting the E-267 gate I codified. Five triggers fire (T1/T3/T4/T7/T8); T2/T5/T6 no. **The gate I pinned as an invariant at E-267 was satisfied by the broken code** — same-population held on both sides because both sides were polluted — so this closure is partly a correction of my own record. Operator ruling at this gate: promotions capped at ~100 lines total; **measured contribution 46 lines** (see T7).

- **T1 — the identifier sweep gets two BOUNDS (`.claude/rules/python-style.md`, appended to the existing "sweep the IDENTIFIER" sentence)**: (1) *whose contract this change touches* INCLUDES *whose CALLER SET it touches* — caller removal, sole-guard promotion, and reachability changes all move a contract without editing a line of it, so **never discard a hit because "this story does not edit that function"**; the scope was right and the TERMINATION CONDITION was wrong, a different defect from diff-anchoring. (2) The sweep covers every tree that DESCRIBES the contract — callee, caller, module docstring, `.claude/rules/`, and **`docs/`**, where the last copy found was operator-facing and read exactly backwards. Defect-cited: four prose sites missed by a sweep that was complete against its own inventory.
- **T3/T8 — one new section in `.claude/rules/tool-output-integrity.md`, "A check that RAN is not a check that WORKED", four paragraphs that must travel together.** (a) **A figure inside a correct conclusion is checked for whether it SUPPORTS the conclusion, never whether it could have been PRODUCED** — `18 of 18` rode four artifacts through six passes; the true value was `9 of 18`, which inverts the gate from refuse to permit. Re-derive from what would produce it; agreement with the conclusion is not evidence. **Chosen as the top promotion on PM's criterion: code-reviewer applied this cut by hand, unprompted, before it was named — a rule reinvented under load beats a rule agreed to.** (b) The detection record: class A (false premise under right conclusion) caught by EXECUTION 4-of-4 and by careful re-reading 0-of-4; class B (missing enumeration member) caught by reading but **never by the author** — **8 of 8, the author never caught their own**. (c) **The running can lie**: a broken check's output is SHAPE-IDENTICAL to a working one's (a line-wrapped positive control manufactured a phantom tool failure; a mutation probe reported green having never mutated, its anti-vacuity alarm printed above the summary line nobody read) — so validate the check's own preconditions and never filter away the evidence that the instrument OPERATED. **(c) BOUNDS (b) and must not be split from it**: (b)'s remedy is "run it", and (c) is the failure of running it. (d) **Severity ordering is a claim, and the least-checked kind, because every finding under it is true.**
- **⛔ T8(a) SHIPPED UNDER THE WRONG NAME, AND THE CORRECTION ADDED A SECOND RULE RATHER THAN REPLACING THE FIRST.** I promoted the paragraph above as *"PM's criterion-versus-evidence cut"*. **It is not that rule.** The label had **no written definition anywhere** — three occurrences, all naming an APPLICATION of it, never stating it — so I reconstructed its content from the primary and reconstructed the wrong lesson. Both are real and both now ship, adjacent and explicitly distinguished, because conflating them is how one gets lost: (i) **the PRODUCIBILITY check** — how you VERIFY a figure (support is not producibility), which is what I promoted; (ii) **the CRITERION-vs-EVIDENCE cut** — what you DO with a stale one, in its author's words at `E-276-02-game-grain.md` (the *"Do NOT sweep the `4207` in AC-8"* note): ***a figure is a CRITERION when a reader must meet it, and EVIDENCE when a reader must see what was observed. Correct the first; preserve the second.*** Its sharpest instance is one number carrying both roles four criteria apart in ONE file — the same `4207` was a stale acceptance baseline (CRITERION → **removed**, not updated, since updating an absolute baseline a preceding story will move again merely resets a timer) and the suite size at which a measurement was actually run (EVIDENCE → preserved; editing it falsifies the record). Applied again by CR in review: two surviving `season-ambiguous` references, in an archived epic record and in the review scorecard, left alone as evidence-not-criteria. **The reinvented-under-load provenance belongs to THIS rule, not to the producibility check — I had attached it to the wrong paragraph and moved it.**
- **The irony is PM's, in its own words, and it belongs here: *"My own top trigger-8 candidate, chosen on my own reinvented-under-load standard, had no written definition anywhere. It lived in the shared context of the people using it and was always a name for an application, never a statement of a rule — which is why it survived promotion while losing its content."*** That is this epic's spec-integrity obligation — *a design settled in an artifact on no filesystem is a citation that resolves to nothing* — landing on the closure record's top promotion, authored by the party who wrote that obligation into the epic. **And the detection route is a new member of this dispatch's one unbroken pattern (the author never catches their own): not PM re-reading it, not a reviewer checking PM, but ME TRYING TO USE IT. A term that cannot be promoted without being defined is tested by the act of promotion** — which makes codification a detector for undefined shared vocabulary, not merely a consumer of it.
- **T8(b) — asymmetric framing, folded INTO the existing safety-comment sub-class rather than added beside it**: the check is not *"does this sound appropriately grave"* but ***"are BOTH sides of the trade stated?"*** — neither the alarming nor the reassuring qualifier is the defect, the asymmetry is, and the two-sided form is the only one a non-author can run from the text alone. Folded because a second paragraph on qualifier discipline in the same file would have paid twice for one class.
- **T3 (reviewer-side) — `.claude/agents/code-reviewer.md`, at the MUST/SHOULD classification line, not in the Bug Pattern Checklist** (it governs the reviewer's own DISPOSITION, not a code smell): ***a SHOULD FIX accepted on a cost argument that turns out to be wrong is not a SHOULD FIX*** — the cost WAS the disposition. **The only shape in this epic where checking the premise FLIPS the verdict**, everywhere else the verdict held; and its detector is a third act — not re-reading, not reproducing, but **re-opening your own reasoning**.
- **T4 — `.claude/rules/context-layer-assessment.md`, two additions at the lifecycle**: (1) the deletion-side sweep now triggers on a **retired CLAIM**, not just a deleted identifier, sweeps for the JUDGEMENTS that rested on it, and requires every hit outside your own dir to be **filed as an idea against the owning agent with a resolution trigger** — a flag living only in a completion message is a handoff to nobody (IDEA-187, IDEA-191). (2) SE's memory-write discipline, **re-homed from `software-engineer/testing-gotchas.md` where it was misfiled**: *never write a number from a live thread into memory; wait or record the MECHANISM instead of the MEASUREMENT* — a handoff is read once soon by someone who might notice; **a memory file is read cold months later by someone with no thread to check it against.** SE asked for its own copy to be evicted once this lands; that edit is SE's, not mine.
- **`.claude/rules/doc-sweep.md` — the retirement shape running in REVERSE**: the REFUTATION can be the sentence carrying none of the claim's tokens. A 41-line memory file held *"a wrong delete self-heals"* and, four paragraphs down, the material that falsifies it, sharing not one word — **no grep for a retired claim can surface the sentence that kills it**, so only reading finds them together. Companion: fresh correct material landing beside a retired sentence makes the whole hit read as accounted for.
- **CLAUDE.md (story 05, not a promotion) — the seam bullet now separates TWO prior sets**: the CANDIDATE population (live read, `old ∪ fresh`, uniform, unchanged and CORRECT) from the HEALTH-GATE population (per grain). **The three grains did not converge and the bullet says so**: game and player-line take a caller-supplied pre-upsert snapshot; **roster has NO floor gate at all** and ends with LESS gating than it started with, on an operator ruling. My E-267 entry's *"benign, since dedup would have merged the rows anyway"* is corrected in place as refuted by execution — **ordering is decisive alone: the retire runs BEFORE `dedup_team_players` by design.** The lesson I attached to the STANDING CODIFICATION CHECK is the one to carry: **A MITIGATION IS A CONCLUSION TOO** — I traced the docstring's conclusion correctly and reached for a safety net in the same sentence without checking it was downstream of the harm. A dismissal is the claim nobody re-opens, precisely because the finding above it was right.
- **T8(c) — three items REINSTATED when the operator lifted the ~100-line cap mid-pass** (*"you can let it grow; we're about to refine the context layer anyway"*). Recorded together with WHY they had been cut, because that is the cheapest possible input to the coming refinement pass: (1) **the grep-narrowing class → `tool-output-integrity.md`, placed beside the ugrep quirk it is NOT** — a CORRECT pattern silently narrows when markup moves beneath it (emphasis interpolated mid-phrase, 2 hits vs 7 normalized; blockquote nesting, 1 where there had been 2, *indistinguishable from the passage having been deleted*; a line-wrapped phrase returning EMPTY; hyphenation; letter case, missed by this catalogue's own author inside the check the catalogue exists to protect). Remedy is three normalizations, and CR-2's generalization is codified as written: **an unexpected COUNT is a cross-check trigger, never a finding — ANY count you did not predict, in EITHER direction.** (2) **A prose sweep cannot see code that still PARSES → `doc-sweep.md`**: a retirement strands probes and harnesses that import cleanly and report plausible numbers about a configuration that no longer ships; nothing in them is stale to READ, so the sweep needs an import/path scan or a run — and a substring match is not that scan (`t_game` also matched `first_game`, `next_game` "and forty other files"). (3) **The review loop is structurally BLIND to an untracked file → `code-reviewer.md` Step 1**: absent from `git diff` AND `git diff --cached`, so every per-story review passes without ever seeing it; one `git status` caught it pre-commit, and committing from the index as it stood would have dropped an entire closure assessment block with every review green.
- **DECLINED — now on MERIT ALONE, since the cap was lifted before this entry closed**: *"retiring a claim is an edit PLUS a sweep"* — `doc-sweep.md` already carries the retirement section and its judgement-expansion step; only the REVERSE shape was new, and that is what landed. The **outside-seat staffing recommendation** — a staffing change rather than a verification rule, and the operator's call to make rather than mine to encode; it stays parked in the epic's Codification Recommendation where the operator meets it directly. **Nothing else was cut**: both items I had declined on a per-line basis are reinstated above. **A size decline is not a merit decline**, and a compression pass needs to know which was which — which is why this bullet keeps them apart.
- **T7 ratchet — OPERATOR-SIGNED EXCEPTION, and the baseline is deliberately NOT re-snapshotted.** Main already fails by **+2,871 before this epic merges** (inherited across four deferrals; `.claude/agent-memory` carries +2,670 of it). E-276's own contribution was ~196 before this pass. **The ratchet's own measure is the number to trust — `git diff --numstat main -- .claude/rules .claude/agents .claude/skills .claude/agent-memory` = +252 / −21 = 231 NET**, whole epic, every agent, staged and unstaged. **My own total across all eight files I touched is +82 / −9 = 73 net** (`git diff HEAD --numstat -- <the eight paths>`), of which the hook's +30/−3 falls OUTSIDE the ratcheted subtrees. **Measured FOUR times across this pass and stale three times** — before the entry carrying the figure existed; omitting the `MEMORY.md` index rewrite; after the cap lifted and three items were reinstated; and again after the provenance-block rewrite. I stopped reporting it as a per-window sum and now give ONE command against a fixed base, because window arithmetic is a carried-forward base by another name — the E-267 footgun in disguise. **⚠️ Note the four-subtree total FELL from 229 to 227 while I was adding**, because software-engineer evicted its longer local copy (−14/+3) once its lesson was re-homed to `context-layer-assessment.md`: **that promotion's net cost was NEGATIVE, which is the strongest argument available for re-homing over duplicating.** It also means a `.claude`-wide numstat taken during closure is a reading and not a constant — SE was writing that subtree while I measured it, the E-267 measurement footgun recurring on the very line that reports it, which is why the figures above are scoped to named paths. The hook change (+12/−1) sits OUTSIDE the four subtrees, as does CLAUDE.md, so both cost the ratchet nothing. The operator declined `--update-baseline` so the inherited debt stays VISIBLE. Same disposition as E-261/E-262/E-264/E-267/E-270/E-272/E-273 — **eight consecutive epics deferring; that streak is now the finding, not the individual exception.**
- **Hook defect fixed in place (`.claude/hooks/send-message-counter.sh`) — the `rounds` column has NO producer and never could**: the hook sees a PreToolUse `git add` event, which carries nothing about review rounds, so the column has been blank on every row since E-260. Documented at the schema, where a log reader looks: **blank means "not recorded", never "zero rounds"**; and **a row is a STAGING BOUNDARY, not a story** — E-276 story 01 cost 41 sends logged as 37 + 4, so a reader treating rows as stories under-counts. **The operator-owned threshold ASSIGNMENTS were never touched**, only the comment around them. **⚠️ AND THE TWO-TREE READING IS WORTH KEEPING, because it is a failure mode no single-tree review can reach.** I reported that the relayed raise to 60 *"exists in no file"*; the lead reported it as live. **Both reads were correct and neither tree was wrong**: the worktree branched from HEAD and carried the committed 15/25, while the operator's raise sat UNCOMMITTED in the main checkout's working tree. It is now committed (`dc1cc9e`, 40/60). **A dispatch worktree and the main checkout are two trees, and every agent here works in exactly one of them** — so an uncommitted operator edit is invisible to the entire review loop, the same shape as the untracked file CR found, one level up. Flagging-and-stopping on an operator-owned value was what surfaced it; changing it would have silently reverted the operator. **Then that same commit falsified the `THRESHOLD PROVENANCE` block sitting directly above my insertion — nobody edited a sentence, and the edit that broke it was correct and necessary.** Rewritten (comment only, with a clear gap left above the assignments so the closure hunk cannot span them and carry the old values back through a `--3way` apply): current values 40/60, set by the operator on E-276's data, **replacing** the E-256 placeholder; the per-story costs 41/18/32/11/8; that the cap is counted PER STAGING BOUNDARY and so must fit the LARGEST single story, never the average; that `WARN_AT` moved because a warn far below the deny cap fires on nearly every story and stops carrying signal; and that the stale *"revisit with data"* instruction has been DISCHARGED, so no reader restarts it. **A claim can go stale with nobody at fault — which argues for co-locating a value with its provenance, not for more care.** *(In the worktree the new comment reads 40/60 while the assignments still read 15/25; that is expected and resolves on apply, since the comment is written against main's post-`dc1cc9e` state.)*
- **Eviction sweep — the story-05 residue sweep IS this epic's eviction pass**, recorded durably at `.project/research/E-276-residue-sweep.md` (three categories: corrected / left-alone-with-reason / flagged-not-edited) rather than in a completion message, because two of its three categories carry forward obligations onto agents not on this team. Its operator-facing `docs/admin/operations.md` hit — the roster cap described as applying *"in addition to the shrink ratio"* — **was routed and FIXED by docs-writer during closure**; verified in the staged diff rather than assumed.

## E-277 — Reclamation Follow-Up Repairs
Feature/repair epic. Story 04 (mine) corrected two stale `generator.py`→`lifecycle.py` citations in `data-model.md`, recorded the reclamation sweep as a THIRD team deleter, and fixed `canonical-seams.md`'s root enumeration from three to **four roots across three tables**. Closure codification migrated TN-15's dispatch findings out of an epic about to be archived.

- **Placement was the whole job, and the constraint was ambient cost**: `.claude/rules/tool-output-integrity.md` loads on EVERY interaction for EVERY agent (21KB before this pass). Four AC-authoring rules were moved OFF it into `product-manager.md`'s Quality Checklist (reachable-RED, property-not-action, no-decorative-AC, naming-a-site-is-not-enforcement), with `code-reviewer.md` given a POINTER rather than a copy. Universal file got only what binds any agent at any moment: the fourth way grep lies (**a narrowed pattern can return the WRONG MEMBER and look successful** — the one case the unexpected-count reflex cannot catch), the JOIN remedy, sweep-is-a-worklist, positive-control-by-design, status-claims-decay-like-verdicts, quote-the-literal-bytes, verify-by-absence. Also `dispatch-pattern.md` (receipts + disagreement adjudication), `doc-sweep.md` (tombstone over-match + false credit), `canonical-seams.md` (diff boundary ≠ behavior boundary).
- **I BLEW MY OWN STATED BUDGET 2× and it is recorded because the miss is the lesson**: committed publicly to "<2.5KB net on `tool-output-integrity.md`", landed **+5,034**. A compression pass recovered 640. The estimate was made before drafting and published as a constraint. **Next codification: measure a draft before quoting a budget.**
- **The ratchet under-measures this pass and that must not read as restraint**: it counts LINES, and this repo's rule files use very long lines, so ~+17.7KB of prose across seven files cost a small line delta. **A byte-cheap line count is an artifact of the instrument, not evidence of a small addition.**
- **A promotion was proposed, refuted, and HELD — the best outcome in the pass.** TN-15 left the intra-document family's promotion OPEN to claude-architect with a PRE-REGISTERED criterion (a third instance from a different author). I counted PM4's instance as the third; PM4 objected and asked me to place it before counting it. Re-derived: the family's property is the falsifier being **co-located with the claim in one document**, and PM4's falsifier was the delivered CODE — a different artifact. That is the FRAME family. Intra-document stays `se` ×3, one author; criterion NOT met; **held**. **The criterion did its job by NOT firing, which is the only result that distinguishes a real pre-registration from a decorative one.**
- **The miscount was INHERITED, and that is the class this epic catalogued**: TN-15's own text asserted the instance came "from a DIFFERENT document and a DIFFERENT author" — a **false premise under a correct conclusion** (PM4's actual ruling, do-not-promote, was right throughout), so every check asking "was the call right?" passes it. Only being asked to PLACE the member forced the re-derivation. **I also declined to shop for a replacement member** though a live candidate existed: selecting evidence after publishing a conclusion is rescuing a position.
- **Stale-count sweep of my OWN memory, prompted by PM4 finding one in its own**: this file's E-273 T1/T2 entry said "the THREE exclude-not-clear roots". **ANNOTATED, not rewritten** — it accurately records what E-273 codified; the annotation points at `_TEAM_BASE_PRED` as the authority. The epic's signature defect was sitting in the memory files of the epic that fixed it everywhere else.
- **Died with the epic, deliberately**: the baton/ACK lock and instruction-outliving-withdrawn-authority findings (artifacts of a two-coordinator window that is NOT the standing topology — codifying them documents an anomaly as the pattern), per-story sub-label uniqueness, story-03's three-occurrence bound, the un-recordable tally. **RE-HOMED rather than killed** (PM4's correction, better than my decline): the two-coordinator findings go to the sub-lead experiment post-mortem, because their audience arrives if the operator ever grants the `Agent` tool to named agents. **Run the audience test FORWARD as well as backward.**
- **NOT promoted standing-alone: "awareness confers no immunity."** It fired repeatedly, including inside the messages transmitting the rule being defeated. Its own content is that *look harder* is what already failed, so it is unactionable as a rule — it became the RATIONALE line inside the structural remedies instead. No tally recorded, deliberately: this epic retired three tallies for exactly that error, one of them a tally about self-correction.

## E-278 — Game Identity: Dates, Duplicates, Ties
Defect epic. Two independent date-derivation mechanisms mis-dated rows in OPPOSITE directions (an unresolvable-tz alias giving +1 day; a full-day calendar event localized as an instant giving −1 day), plus a same-perspective duplicate from GameChanger double-listing one game in a team's own schedule. Operator signed a **trigger-7 exception**, so this pass was not net-neutral. Five edits across four rule files, plus one finding I refused to codify.

- **A1 — `.claude/rules/data-model.md`, and the diagnosis I was handed was INCOMPLETE.** The relay said the line's absolute (*"All queries across `src/` filter on `'completed'`"*) had been falsified by `_query_record`'s new no-status query. **It had never been true.** A `status = 'completed'` predicate appears at nine sites in `src/`, against far more `games` queries than that — and decisively, **`_query_record`'s three immediate SIBLINGS in the same file** (`_query_recent_games`, `_query_runs_avg`, `_query_freshness`) gate on `home_score IS NOT NULL AND away_score IS NOT NULL` with no status filter either, as do bare `SELECT COUNT(*) FROM games`, the merge lookups, and the lifecycle deletes. So the fix is not "state the exception" — it is **stop asserting a census and state the DIRECTIVE**, plus a second bullet homing the recorded `_query_record` decision. PM's framing of the urgency is right and is why this was A1: **a stale rule misleads; this one RECRUITED** — it named the exact remedy (add the filter) that E-278-01's AC-6 declined on measurement, in a file that auto-loads for every agent touching `src/`.
- **⛔ AND I DECLINED TO RATIFY A CLAIM I WAS TOLD WAS TRUE.** The brief carried a PM instruction: SE's docstring asserts *"Every other game query in `src/` carries one, and this is a knowing exception"* — *"that claim is still TRUE. Do not 'fix' the docstring."* **Under any literal reading it is false**, refuted by the three siblings above. I did not edit `src/` (not my domain, not in scope) and I did not write "the docstring's claim is true" into the rule — the rule now states the convention without any census, so it is correct whichever way the docstring is later read. **This is `tool-output-integrity.md`'s inherited-claim rule catching a live one during the very pass promoting a new member of it**: the instruction arrived with a correct VERDICT (the docstring's substance — the exception is knowing, not an oversight — holds), and the verdict is what protected the false premise from being checked. Routed back to main rather than fixed.
- **A2 — `.claude/rules/canonical-seams.md`**: `derive_local_date`'s contract now names **three** `None` causes (absent instant / unparseable instant / present-but-unresolvable zone) and, more importantly, the **fail-CLOSED direction**: it used to return the UNCONVERTED UTC date, which for an evening game past 00:00Z is the next day, so `US/Central` and `America/Chicago` split one game into two dates. Explicitly bars the plausible "improvements" (substitute the operating zone, or an alias→canonical map) and records that this **deliberately DIVERGES from `get_operating_timezone()`**, which still falls back because that zone is OUR config with a documented default. Do not re-align them.
- **T3 — `.claude/rules/worktree-isolation.md`: the hazard is the OPERATION, not the spelling.** My E-267 codification named `git checkout -- <file>`. **Three spellings restore from the INDEX**: `git checkout --`, `git checkout-index -f`, and `git restore` — the last being the modern recommended form, with `-W/--worktree` as its DEFAULT and `--source` defaulting to the index (verified via `git restore -h`, not assumed). Evidence is unusually strong: the SE who hit it **had internalised the rule and had quoted it during this dispatch**, and pattern-matched on the command name. The list is now declared OPEN, with the instruction to check what any new spelling restores FROM.
- **T3 — `.claude/rules/dependency-management.md`: an inferable fact is not a stated one.** The "Add a runtime dependency" procedure said *"3. Run `pip-compile`"*, singular, and never stated that a runtime dep recompiles BOTH lockfiles (`requirements-dev.in` constrains against `requirements.txt`, so `requirements-dev.txt` pins runtime deps too). The File Layout table made it inferable. An implementer followed it literally and shipped 3 of the 4 files; that would have failed CI's lockfile-drift gate AND **silently stripped `tzdata` from every fresh devcontainer**, which installs from `requirements-dev.txt`. Fixed all three procedures (add/upgrade/remove) plus a warning callout. **The generalizable half is the verification miss, not the rule gap**: PM verified the STORY's "Files to Modify" list rather than deriving the artifact set from the rule — so the callout says *derive the artifact set from this table, not from a story's file list.*
- **T8 — two promotions into `.claude/rules/tool-output-integrity.md`, both as MEMBERS/MIRRORS of existing content rather than new sections** (that file loads on every interaction for every agent; E-277 already paid for its size once).
  - **Class A's MIRROR: a TRUE clause welded to a FALSE INFERENCE.** Placed immediately after the detection record, because the two are only legible together — Class A defeats *"was the conclusion right?"*, and the mirror defeats *"was the premise right?"*, which is the remedy Class A hands you. Worked instance: implementer had a true premise + false inference; PM's refutation had a true premise + its own false inference (improper subset); **neither party held a false premise, and every individual sentence in the episode was true.** Also carries the topology point — three carriers formed a TREE, not three independent sightings, established by asking the third party directly.
  - **A diff hunk is an inherited claim** (code-reviewer's, attributed). Folded INTO the relay enumeration rather than added beside it, and **the enumeration's own closedness is now called out**: *"an epic spec, a story, or an upstream doc"* read exhaustive and was not. The transferable mechanism is the **mode switch that never happens** — one artifact carrying primary evidence (`+`/`-` lines) and hearsay (the prose between them) under a single apparent epistemic status. CR's own words: *"a reviewer reading a diff has no reason to think of itself as relaying anything."* Note "read diffs carefully" would have caught nothing.
  - **A THIRD, promoted on my own judgment from an empirical pattern PM flagged without recommending: the vacuity concentrates in the ANTI-VACUITY line itself.** All four vacuous assertions in this epic sat INSIDE guards (`assert local != X` after `assert local is None`; a `!= _SENTINEL_DATE` conjunct comparing two module constants; a `fetchone()` returning identically either way; an `assert "15-5-<" not in html` that could never fire because the header renders a newline). Each was **written as the check protecting a criterion**, and each was found by someone other than its author — consistent with the file's existing 8-of-8 authorship record, which is why it belongs in "A check that RAN is not a check that WORKED" rather than standing alone. Verified two of the four in the tree before writing it. Remedy aimed one line lower than feels necessary: **make the guard FAIL on purpose**, and prefer assertions whose negative form is constructible (regex lookahead, `pytest.raises`) over substrings and inequalities whose non-match has many innocent causes.
- **Declined / not mine**: TN-18 lessons 1, 2 and 5 stay as epic record. Lesson 1 (a plausible mechanism is not the mechanism) is a restatement of the read-the-discriminating-evidence discipline already on the page. Lesson 2 (**check where the claim IS, not where it is ABOUT**) is genuinely sharp — a false claim about `src/cli/data.py` was living in `tests/test_cli_data.py` — but `doc-sweep.md` step 2 already governs it and TN-18 says so itself. Lesson 5 (**closure patch base is `$(git merge-base epic/E-NNN main)`, NEVER `main`**) belongs to the implement skill / main-session procedure, which is not mine to edit from a codification pass; **flagged to main rather than silently dropped** — a moved `main` would make the closure patch carry a reverse application of the operator's own hook change.
- **Boundaries honored**: `docs/admin/operations.md` left to docs-writer (parallel). Other agents' memory read-only — PM filed IDEA-224/225/226 for api-scout, data-engineer, baseball-coach; SE reconciled its own.
- **TN-18 lesson 5 — codified AFTER the fact, on explicit main-session authorization** (I had flagged it as outside a codification pass; main asked for it and noted `.claude/skills/**` is mine per `context-layer-assessment.md`). `.claude/skills/implement/SKILL.md` Step 8 sub-step 3. **The command was ALREADY correct in both places that generate a patch — the gap was the WHY, which is the interesting part**: main nearly shipped a `main`-based closure diff during E-278's own closure despite the written command, and it surfaced as a phantom 85-line change to a hook no story touched (the operator had committed `c990446` to main after the branch point). Codified three things: (a) this is the one closure command whose wrong form is **silent and destructive** — a moved-`main` patch reverse-applies main's own commits, and nothing downstream tests for it (`git apply --check` passes, the full suite passes, the staged diff shows the reversal as an ordinary hunk); (b) the merge base binds **every** closure diff, not just the patch-generating one, since a `main`-based sanity check manufactures phantom findings that halt a good closure; (c) **the discriminator is WHICH TREE you are in** — `main` as a base is safe exactly where it equals `HEAD` (the main checkout, e.g. sub-step 8's operator presentation) and wrong everywhere else. (c) exists to stop a later reader "fixing" all ~8 remaining `main` diff bases in the file, most of which are worktree-relative and genuinely hazardous while one is correct. **Flagged the remaining worktree-relative sites to main rather than rewriting them** — changing implementer and reviewer procedure is a bigger scope than the reason-codification I was authorized for.
- **Same-pass sweep of the residual sites (main-session ruling: fix now, not as an idea — *"leaving a rule in a file alongside eight live counterexamples is the worst of the three options"*).** Seven worktree-relative diff bases moved from bare `main` to `$(git merge-base epic/E-NNN main)` in `implement/SKILL.md` — the implementer spawn context, the implementer Permitted list, the staging-boundary cumulative view, the migration trigger read, the CR integration review diff (+ its empty-diff sentence), large-epic handling, and the Step 6 user summary. **ONE site deliberately LEFT and now annotated against a future sweep**: sub-step 8's `git diff --cached --stat main` is taken in the MAIN CHECKOUT, where `main` IS `HEAD`, so it is a staged-vs-HEAD diff and is correct. **The general rule, and it is the reusable half: `main` as a diff base is safe exactly where it equals `HEAD`.**
- **⚠️ Line 590's "stale citation" was NOT just a citation — the stated REASON had gone false, and I did not preserve it as instructed.** Step 7a said a commit there "would cause Step 8's `git diff --cached main` to generate reverse patches." Verified on the live graph: merge base `c484a43`, main tip `c990446` — **the merge base does NOT move when main advances**, so that mechanism cannot fire under the corrected base. Main's instruction was *"the reasoning still holds, correct the citation and leave the reasoning"*; writing the old reason under a merge-base citation would have authored a false claim, so I substituted the reason that actually binds (**the closure is ONE atomic commit; a Step 7a commit advances HEAD and puts those files outside it**), kept a second real hazard (a path in both trees can clobber or conflict at `git apply`), and marked the correction inline so no reader restores the dead mechanism. Reported the substitution to main rather than making it silently. **This is the pass's third instance of one shape: a correct INSTRUCTION carried by a reason that had quietly gone false** — after the `_query_record` docstring and the A1 absolute. The instruction being right is what keeps anyone from re-reading the reason.
- **OUTCOME of the refusal, and the detection route is the part to carry.** Code-reviewer adjudicated the `_query_record` docstring claim independently and confirmed the refusal on every point (nine `status = 'completed'` sites in `src/`, zero in `generator.py`, all three siblings unfiltered); SE holds the resulting MUST FIX. CR then corrected its OWN Step 1a finding on the same grounds — it had also reported the `data-model.md` absolute as NEWLY falsified — in nearly the same words: *"I had a correct conclusion and it stopped me checking the premise underneath it."* The main session likewise accepted that its line-590 instruction had been wrong, having ruled on two instances of this shape in the same message that carried a third. **Deliberately recording no tally** (E-277 retired three tallies for exactly the error of counting statements as observations), but the carriers are worth naming: the docstring census, the `data-model.md` absolute, line 590's reverse-patch mechanism, and CR's own Step 1a write-up. **The route: the relay chain PM → main session → me carried a CORRECT VERDICT at every hop and a false premise the whole way down, and it was caught by applying the brief-is-a-relay rule to an INSTRUCTION FROM ABOVE rather than only to material I had inherited sideways.** That is the refinement to habit (5) in `MEMORY.md` — the rule is normally read as governing what upstream artifacts hand you, and its highest-yield application this dispatch was on a direct instruction from the agent coordinating the work. An instruction is an artifact written at one moment and executed at another, which is the whole basis of the relay rule; nothing about its coming from the coordinator exempts it.
