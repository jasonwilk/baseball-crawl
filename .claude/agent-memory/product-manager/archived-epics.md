# Archived Epics -- Key Milestones

Canonical source for the full archive: `ls /.project/archive/`

This file preserves only key milestones and architectural decision points from the project's history.

## Foundation (E-001 — E-030)
- **E-001**: GameChanger API Foundation — credential parser, API client, endpoint docs
- **E-002**: Data Ingestion Pipeline — 13 stories, 615 tests. Crawlers + loaders for all core data
- **E-003**: Data Model and Storage Schema — core schema, coaching_assignments, seed data
- **E-006**: PII Safety System — pre-commit hook + Claude Code hook for credential scanning
- **E-013**: Agent Buildout — data-engineer and software-engineer from stubs to full operational manuals

## Infrastructure (E-042 — E-100)
- **E-042**: Admin Interface and Team Management — URL-based onboarding, admin CRUD, opponent discovery
- **E-077**: Programmatic Auth Module — gc-signature HMAC, token refresh, client integration
- **E-088**: bb CLI Unification — Typer-based CLI replacing standalone scripts
- **E-096**: Production Deployment — Docker Compose on home Linux server with Cloudflare Tunnel
- **E-097**: Opponent Scouting Data Pipeline — bb data scout, scouting crawler and loader
- **E-100**: Team Model Overhaul — fresh-start schema rewrite. Programs, INTEGER PK, membership_type, TeamRef
- **E-104**: Athlete Profile Endpoint Probe — **ABANDONED + archived 2026-07-05 (E-250-07)**, never dispatched. The cross-team-player-identity probe (own→opponent `athlete_profile_id` access). Closed because cross-team identity is a permanent non-goal (reports-first reframe) and E-250-02's migration 008 dropped its schema anchor `players.gc_athlete_profile_id`; `docs/ROADMAP.md:205` had already directed closing it. No probe work performed.

## Process Evolution (E-112 — E-149)
- **E-112**: Context Layer Optimization — CLAUDE.md 508→152 lines, 4 new scoped rules, zero info loss
- **E-136/E-137**: Atomic Epic Commits + Worktree Isolation — single commit per epic, git worktree dispatch
- **E-140**: Planning Skill — formalized plan→spec review→triage→refine→READY workflow
- **E-149**: Review Methodology Retro — 6 new CR bug pattern checklist items from E-147/E-148 gaps

## Data Enrichment (E-155 — E-212)
- **E-155**: Combine Duplicate Teams — atomic merge across 16 FK cols in 13 tables
- **E-158**: Spray Chart Pipeline — full pipeline + dashboard integration, matplotlib rendering
- **E-173**: Fix Opponent Scouting E2E — resolution write-through, auto-scout, unified resolve page
- **E-195**: Plays Data Ingestion — FPS% and QAB from play-by-play, 2-table schema, parser/loader split
- **E-196**: Pitching Availability — migration 014, game ordering convention, shared workload query
- **E-197**: Derive season_id from Team Context — canonical utility, decoupled filesystem vs DB
- **E-198**: Reconciliation Engine — plays-vs-boxscore detection + BF-boundary correction
- **E-199**: Plays-Derived Stats in Reports — FPS%, QAB%, P/BF, P/PA on both scouting surfaces
- **E-204**: Starter vs. Relief Tracking — appearance_order, GS/GR display, backfill CLI *(the `bb data backfill-appearance-order` CLI was later DELETED by E-256-02, 2026-07-12 — live-DB `appearance_order` NULL count = 0, so the recovery path was unneeded; E-204's history stays true, it did ship the CLI)*
- **E-212**: Predicted Starter — first LLM integration, two-tier enrichment pattern, both surfaces
- **E-214**: Fix Predicted Starter Rest Day Anchoring — `reference_date` threading, `FEATURE_PREDICTED_STARTER` flag
- **E-215**: Fix Player-Level Duplicates — `ensure_player_row()` canonical upsert, prefix-matching detection, atomic merge, two-hook post-load dedup sweep in scouting pipeline
- **E-216**: Cross-Perspective Game Dedup — pre-load dedup via natural key (`game_date` + unordered team pair) with doubleheader tiebreakers, post-load validation (game duplicate check + roster count). Prevention-over-cleanup pattern.
- **E-217**: NSAA Pitch Count Availability Rules — replaced ad-hoc exclusion heuristics with NSAA-compliant frozen dataclass rule engine (tiered rest, consecutive-days, doubleheader aggregation, null pitch count handling). Bullpen shows available/unavailable with reasons. LLM rest table injection. Context-layer `pitch-rules.md` codifies NSAA/Legion/USSSA/PG rules.

## Process + Reports-First Reframe (E-218 — E-251)
- **E-218**: League/Level Detection for Pitch Rules — cascading priority (DB fields → NGB+age_group → name keywords → unknown); Legion + NSAA-subvarsity rules; unsupported leagues suppress with warning.
- **E-221**: Test Fixture Schema Parity Audit + post-E-220 perspective residuals — canonical cascade consolidation via `generator.py::cascade_delete_team` (Option 2 refactor-delegate). *(⚰ **path accurate-as-of-E-221; the symbol now lives in `src/reports/lifecycle.py`** — E-256-04 extracted it. Annotated, not rewritten, per the same convention as the E-259 note below.)*
- **E-223**: E-220 Adopter Audit — fixed 4 IDEA-071 pre-provenance paths (admin delete counts, reconciliation dedup, spray game+perspective gate, backfill deprecated).
- **E-224**: Pytest Interaction Guardrails — SUPERSEDED by E-229 (all compensating machinery later removed); historical record only.
- **E-226**: Closure Commit Approval Gate Enforcement — Phase 5 Step 7 approval gate (named command `git diff --cached --stat main` + the four approval words).
- **E-227**: Closure Workflow Structural Remediation — Phase 5 → single atomic `feat(E-NNN)` commit; worktree cleanup first-class Step 9.
- **E-228**: Reset Dev Env — `bb db reset` → empty schema; admin-sees-all dashboard via `_get_permitted_teams` widening + canonical admin predicate `_user_is_admin`/`user_is_admin`.
- **E-229**: Remove RTK — removed the dev token-proxy + its compensating pytest-discipline rule/hooks; plain pytest output honest again. IDEA-072 (retrospective audit) open.
- **E-230**: Test Suite Fix — full suite green (0 product bugs; all test-maintenance). Established the **unconditional full-suite-green closure gate** + COMPLETED-flip deferral to Step 8 (set only on green).
- **E-231**: Harness Output Reliability — `tool-output-integrity.md` rule + `edit-verify.sh` PostToolUse hook (project's first) + relay-integrity rule + read-receipt triage gate.
- **E-232**: Starlette Deprecation Migration — request-first TemplateResponse + client-instance cookies + asyncio loop-scope; no version bump.
- **E-233**: LLM JSON Hardening — `src/llm/json_extract.py::extract_json_object` helper + `json_object` response_format baseline + single-source canonical default model slug.
- **E-234**: Report Regression Guards (ROADMAP A) — golden stat tables + `bb report verify-aggregates` parity (Epic C cutover gate); established the roadmap-tracking convention (§0 table + `## Roadmap` ref).
- **E-235**: Report Run Records (ROADMAP B) — `report_generation_runs` audit table; canonical-function additive-extension pattern (`ensure_team_row_with_provenance`, the sole surviving live example); in-memory per-run created-set concurrency pattern; data-bearing coverage (N=games-with-stat-rows); `no_games` shareable terminal state.
- **E-236**: Report Integrity Hardening (ROADMAP B2) — `src/reports/run_status.py::classify_stage_status` ERROR-driven (not coverage-driven) helper; migration 003 count cols; honest no-data copy. IDEA-077 Option A delivered.
- **E-237**: Payload-First Loaders (ROADMAP C) — `src/db/season_aggregates.py::canonical_recompute` single boxscore_only recompute; provenance-ownership model (full/supplemented member-authoritative, boxscore_only recompute-owned); parity column-set vs player-set drift are orthogonal. *(Historical: `canonical_recompute` retired by E-259; the module `season_aggregates.py` was later renamed to `season_projection.py` by E-262-04 — this record is accurate-as-of-E-237.)*
- **E-238**: D1 Quarantine + Nav/Passkey (ROADMAP D1) — central `quarantine.md`; nav retarget /dashboard→/admin/reports; migration 004 `webauthn_challenges` table; **single-use-token consume (DELETE-is-the-arbiter)** footgun (gate on rowcount==1, never read-then-delete).
- **E-239**: Decouple and Remove (ROADMAP D2) — removed dashboard/member-sync/opponent-discovery (−59k lines/139 files); reports is the SOLE surface; admin = `reports_admin.py`; importer-gate deferral pattern. Migration NEXT=005.
- **E-240**: Morning Scheduled Reports (ROADMAP E) — cron `bb report morning-run`; migration 005 `scheduled_report_runs` (report_id ON DELETE SET NULL = audit outlives report); opponent resolution ladder + `opponent_links` revival (3 states keyed on resolution_method); sequential invariant; `bb report map-opponent`. Migration NEXT=006.
- **E-241**: Remove Season Machinery — removed cross-season derivation residue at the root; year-only season derivation from both producers; `season_fallback` gone end-to-end (migration 006); `programs.program_type` KEPT (pitch-rule selection). Zero stat-value change. Migration NEXT=007. IDEA-081 filed.
- **E-242**: Align Dispatch/Plan/Implement Vocabulary to Subagent Framing (COMPLETED + archived 2026-06-29) — context-layer vocab/breakage fix after Claude Code v2.1.178 removed the `TeamCreate`/`TeamDelete` tools. **Option B** (user-confirmed): keep `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, adopt named-subagent vocabulary (implicit team formation on first `Agent`-tool spawn, automatic teardown, `SendMessage` resumption), drop the explicit-team ceremony. **SURGICAL scope**: remove ONLY the removed-tool calls + the `[team-name]` placeholder + explicit create/delete-team STEP framing; KEEP "team"/"teammate" collective nouns (the flag is ON, we ARE using agent teams). Flag dependency stated ONCE durably in `CLAUDE.md` Agent Ecosystem (+ CA memory). `dispatch-pattern.md` = documented no-edit retain (descriptive "creates the team" framing is accurate under implicit formation). **No behavioral change (TN-2)** except one in-scope correction: `multi-agent-patterns/SKILL.md` L55 parallel→serial. CA-led, 3 file-disjoint stories (S3 CLAUDE.md+CA memory; S1 plan+implement skills; S2 rules+ancillary skills). E-076 anti-fabrication Patterns 1-3 preserved/reframed onto the single spawn primitive. Scorecards: spec 16/16/0; dispatch 3/2/1 (1 CR SHOULD FIX deferred to TN-9 PM-own-memory note, not invalid; Codex 2/2/0). Full suite green (3467 passed). Context-layer T1/T2/T3/T4 fired (epic's own deliverable, codified in-epic). No doc impact.
- **E-243**: Probable-Starter Usefulness — HARD rest-discount re-rank tiebreaker (fully-available > discounted, stable partition); youth/travel labeled-estimate fallback (`PITCH_SMART_15_18`, `is_estimate`); ranked "Most Likely Arms" card + `unavailable_arms` sub-block + `suppress_reason`-keyed softened copy (never leak raw `data_note`); Tier-2 Variant-A prompt + `google/gemini-2.5-flash-lite`@0.0 as tracked default; corrected baseball-coach `probable-starter-model.md`. NOT a ROADMAP slice. IDEA-085 deferral. Dispatch Scorecard 8/6/2.
- **E-244**: File Plays & Spray Under Canonical Game IDs After Cross-Perspective Dedup — bug fix (pre-existing, introduced by E-237). `GameLoader` now records a `{source_event_id: canonical_game_id}` redirect map (new `LoadResult.redirect_map` field, single-assigned in scouting_loader both flows); generator threads it into plays (precheck + DB-write key + reconcile loop + deduped canonical return) and spray (dict-key remap before load). Deduped games now file plays/spray and run reconcile under the canonical id instead of being FK-skipped → restores FPS%/QAB%/pitches-per-BF/PA coverage for the scouted perspective. Load-keying only (fetch still by source id, `perspective_team_id` unchanged). 1 story, 10 ACs, genuine `_find_duplicate_game` deduped-game fixture (9-test suite). Planning Scorecard 12/12/0; closure CR+integration+Codex all 0/0/0. Context-layer triggers 1&3 fired (CA guardrail note). No ROADMAP slice, no doc impact.
- **E-245**: High-Fidelity Play Ingestion (COMPLETED + archived 2026-06-29) — recovers annotated pitches the parser silently dropped (`(75 MPH Curveball)` suffix → `pitch_count`/FPS collapse; ~5,841 events / 29 games, ~5,328 on team-133) + captures `pitch_type`/`pitch_speed_mph` (migration 007, **storage-only → IDEA-086**, no CHECK on type). Parser strip-then-match annotation grammar (gated: strip only when post-strip base is a known pitch template). Mandated **in-place offline reload-from-`raw_template`** (`src/gamechanger/loaders/plays_reload.py::reload_game_plays` — UPDATE-only, no clear / no API / no `parse_game`; clearing would destroy `raw_template`, the only DB copy) repairs already-loaded games (whole-game idempotency means they don't self-heal): re-derives `pitch_count`/`is_first_pitch`/`is_first_pitch_strike`/`batting_team_id` (fresh from games row, TN-3b) and recomputes `is_qab` via an **exclusion-first OR-merge** — `_QAB_EXCLUDED_OUTCOMES` (IBB/Dropped 3rd Strike/Catcher's Interference) → 0 FIRST, else `stored OR 2S+3 OR ≥6 pitches`; HHB-only QABs survive, no from-scratch `_compute_qab` (final_details not persisted). Report: FPS%/P-PA/P-BF denominators → **charted PAs only** (`pitch_count > 0`), QAB% KEEPS all-PA; "Pitch-charted: N of M games" badge + inline "(N charted games)" + 2 never-suppress zero notes (no-plays vs plays-but-uncharted). Self-game (`home==away`, 23 games) fix: `game_loader.py` always resolves a DISTINCT opponent (by-name → "Unknown Opponent" sentinel) + home≠away invariant guard in `_upsert_game` (removed the misleading `opp_team_id=own_team_id` branch); `bb data fix-self-games` corrects the 23 (boxscore API re-fetch + in-place `reload_game_plays`, NO plays clear). New CLI: `bb data reload-annotated-pitches`, `bb data fix-self-games`. **Migration NEXT=008.** Scorecard: spec 16/16/0; dispatch 6/5/0 (+1 deferred → IDEA-088, no dismissals). AC-6 in-dispatch spec iteration (QAB exclusion-first guard, CR MUST FIX). Codex closure: 2/2/0 (shared-connection partial-commit-on-failure → `conn.rollback()` in per-item CLI except). Docs updated: `operations.md` + `standalone-reports.md` + `understanding-stats.md`. Context-layer T1/T2/T3/T5/T6 fired (CA codified). IDEA-086 (pitch-mix/velocity), IDEA-087 (cause-4 attribution drift, +23 BF `e283438c` NOT a self-game), IDEA-088 (per-game sentinel for genuinely no-name opponents) filed. **Operator-deferred (need creds/live DB, TN-9): the live `reload-annotated-pitches` pass + the 23→0 `fix-self-games` run** (team-133 FPS 3.4%→~64%, P-PA→~2.7 verify post-run).
- **E-246**: Dead-Code Removal & Low-Risk Consolidation (COMPLETED + archived 2026-06-30) — wave 1 of the whole-project /simplify sweep (waves 2/3 = E-247/E-248, dispatched after). 7 file-disjoint stories, SE + DE. **Deletions**: dead 3-tier `gc_uuid_resolver.py` + ~900-line test (zero importers post-E-239; resolvers/ package removed); dead/vestigial residue (`format_season_display`, `scouting._resolve_team_id`, `freshness_hours`/`_is_scouted_recently` + orphan tests, signing unreachable-None branch, parser redundant `strikes < 2` tautology); dead GC client verbs `post()`/`delete()` + 10 tests (shrank E-248 to 4 live verbs — Option-A scope fork). **Consolidations** (byte-identical / algebraically-equal): 5× DB-path cascade → canonical `resolve_db_path()` (`src/db/paths.py`; the ONE intended behavior change — `bb data` now honors `DATABASE_PATH`, matching `bb report`; api/db.py relative-path now repo-root-based, CR-ruled functionally inert); aggregate-parity SUM projections **+ compared-column list** → shared `batting/pitching_recompute_select()` builders + `*_RECOMPUTE_KEYS` (HARD GATE; AC-2 proven via hand-authored rollup-consistent fixture as independent oracle, NOT a tautology); spray `play_type→contact→marker` vocab → one `_CONTACT_MARKERS` table; CLI/safety dups (`ALL_PROFILES`, `_AVAILABILITY_SIGNALS`, `_echo_match_rate`/`_echo_present`, `_scannability_skip_reason` single security-relevant predicate). **Zero stat-definition changes.** opponents.py M2 residue did NOT exist (clean E-240 crawler) → no change there (nil E-247-03 overlap). **Phase 4b Codex 1/1/0**: aggregate_parity kept a SECOND manual column list (`diff_columns`) separate from the hoisted KEYS → AC-3 "no second edit site" held only for the projection, not end-to-end (latent, no live gap); DE remediated via single-source derivation + guard tests, CR byte-preservation APPROVED (`cells_compared` stays 74). Closure gates: full-suite **3441 passed / 0 failed**; blocking operator `bb report verify-aggregates` clean (**10208 cells, 0 mismatches**). Context-layer T1+T3 fired → CA codified (CLAUDE.md `resolve_db_path` canonical bullet + data-model.md two-axis single-source parity footgun); E-246-01 also remediated a stale gc-uuid-bridge.md ref in-dispatch. No doc impact. Per-story CR all APPROVED round-1 (E-246-01: 1 SHOULD FIX fixed; rest 0); integration 0/0. **Migration NEXT=008** (unchanged).
- **E-247**: Twin-Method & Duplicated-Block Extractions (COMPLETED + archived 2026-06-30) — wave 2 of the /simplify sweep; 7 stories each collapsing a twin-method/duplicated-block pattern to one source (loader in-memory/disk twins, reconciliation detection block, public_id→gc_uuid search seam + `is_gc_uuid` helper, credential/auth core, reports plays-scope SQL + `_EMPTY_PLAYS_TEAM`/`_utcnow_iso`, renderer/prediction stat-math + dates, API middleware/auth 503-handler/auth-lookup/`get_app_url`). All byte-identical except the sanctioned APP_URL dev-default unification — REVERSED at closure to `baseball.localhost:8001` for auth-origin coherence, net magic-link change ZERO. HARD GATE via per-story golden/characterization pytest. **Phase 4b Codex caught a real regression**: E-247-01 dropped the in-memory empty-boxscores early-return → `canonical_recompute` would DELETE+rewrite populated-DB boxscore_only aggregates; remediated by restoring the per-path guard + a populated-DB characterization test. Full-suite 3499/0; verify-aggregates clean (10208 cells, 0). Context-layer: CLAUDE.md canonical-seam bullets + dev-URL standardized to `baseball.localhost:8001`; data-model.md twin-method-collapse + populated-fixture footgun.
- **E-248**: GC API Client Error-Ladder Refactor (COMPLETED + archived 2026-06-30) — wave 3 of the /simplify sweep, sole HIGH-blast-radius theme (H5). Pure zero-behavior-change dedup of the 4 LIVE client verbs (get/get_public/get_paginated/post_json) into one private `_send_with_retries` helper (parameterized via `requires_auth` for get_public's no-401 path); old `_get_with_retries` subsumed; net −129 lines in client.py. Test-first: E-248-01 pinned a 6-test per-verb characterization suite anchored on the public verbs (NOT the private helper — Codex P2), filling genuine matrix gaps; E-248-02 kept it green with ZERO assertion changes (94 passed against the refactored code) — the unchanged suite is the equivalence proof. Dead verbs post()/delete() were deleted in E-246-07 (Option-A scope fork), dissolving the original 5xx-gap → no behavior change here. 1 CR integration-review SHOULD FIX (stale comment naming the renamed method) remediated; Codex clean. Full-suite 3505 passed/0 (no verify-aggregates gate — no aggregate surface). No doc impact; context-layer all six triggers NO (the helper is a localized private seam, not a project-wide convention). **Migration NEXT=008** (unchanged).
- **E-249**: Player-Dedup Stale-Worklist Fix (COMPLETED + archived 2026-06-30) — confirmed bug from a live team_id=196 scan (51-vs-19 roster, ~15-20 caught `PlayerMergeError`s). Root cause: `find_duplicate_players` built the merge worklist UP FRONT; load path (`dedup_team_players`) + CLI (`bb data dedup-players`, inlined its own loop) iterated the FROZEN list serially while each merge DELETEd a player → stale worklist, redundant-edge errors, residual duplicates. KEY REFRAME: the current code's FAILING redundant edge was load-bearing no-cross-merge protection — a naive union-find/chase-to-survivor fix would REGRESS into a silent two-human cross-merge (Mode B, the coach-flagged trust-killer). **Fix (Tier 1)**: per-`(team_id, season_id)` connected-components grouping (`plan_player_dedup`); collapse ONLY single-terminal-NAME components, REFUSE any fork = ≥2 terminals with DISTINCT names (equal-named two-UUID members still collapse via the existing tiebreak — DE-review must-fix; keying on terminal COUNT would have regressed the modal same-name cross-perspective duplicate, the `{Jon,Jon}` case); no cross-merge by construction; executor-owned one-txn/component (`execute_collapse`, inner `merge_player_pair(manage_transaction=False)`); season rows via `_delete_or_repoint_season_rows` (E-237 preserve). ONE shared planner consumed by BOTH paths (CLI stopped re-inlining + got a recompute-commit fix — old CLI never committed the recompute → rolled back on close — + non-zero exit on merge failure + refused-fork surfacing in dry-run preview/WARN on execute). Refused forks = WARN-log-only (user decision; durable surfacing + co-occurrence auto-collapse of genuine same-human forks deferred → IDEA-089 Tier 2). **Phase 4b Codex P1 (FIXED)**: planner originally grouped components by team_id ONLY → unscoped CLI unioned prefix-pairs across seasons (2025 {Jo,John} + 2026 {Jo,Jon} → one refused fork instead of two per-season collapses); remediated by the per-`(team_id, season_id)` partition (`find_duplicate_players` now carries season_id; load path always season-scoped → immune). **KNOWN LIMITATION (documented per explicit user "document + close", NOT fixed/NOT an idea)**: unscoped multi-season execute where the SAME dup player_id has DIFFERENT canonicals across seasons commits one guessed cross-season resolution — UNREACHABLE on the live single-season (2026) DB, load path immune, cross-season identity is a Non-Goal. Scorecards: spec-review 10/10/0; post-dev 6 findings/5 fixed/0 dismissed/1 documented. Doc trigger fired (docs-writer refreshed `bb data dedup-players` in `operations.md`); context-layer 4/6 fired (2,3,5,6 → CA codified CLAUDE.md + data-model.md). Full-suite **3524 passed/0**. **Operator follow-up owed**: re-run dedup on live DB, count team_id=196 refused-fork residuals (validates IDEA-089). **Migration NEXT=008** (unchanged).
- **E-251**: Dispatch-Machinery Repair (COMPLETED + archived 2026-07-05) — CE-1 of the 2026-07-04 platform audit, **dispatched FIRST** (before E-250) because F-H4/F-H5 degrade every subsequent epic's execution incl. its own closure. 5 context-layer stories (all claude-architect; per-story CR skipped → PM AC-verification alone): (01) implement-skill closure-path repair — F-H5 red-suite reset `git checkout -- .`→`git apply -R --3way` (reverses patch-created files, no re-apply deadlock) + abort-path ancillary-destruction fix: abort reverses all three Step-8 actions (patch via `git apply -R`; archive rename via `git mv` back; sub-step-7 PM-memory Active→Archived flip via a **SURGICAL PM-driven section-move** — Phase-4b ② refinement, NOT whole-file `git checkout -- MEMORY.md` which would destroy a Step-7a memory edit) while preserving ALL Step-7a ancillary edits (vision-signals, ideas, PM memory); (02) F-H4 — `agent-routing.md` table routes Python/DB/API rows to `software-engineer`/`data-engineer`/`api-scout` by name (was bare `general-purpose` → specialist defs/checklists/model-effort/memory never loaded); (03) `software-engineer.md` status-ownership contradiction removed (work procedure clean 1-4 + affirmative never-own line); (04) hook hardening — worktree-guard double-slash normalization (both modes) + `..`-segment reject (post-staging fold, also closes a `.claude/agent-memory/../src` allowlist escape), `git -C` commit-interception in BOTH `pii-check.sh` + `epic-archive-check.sh` + space-separated/quoted global-option forms (`--git-dir /x`, `-C "a b"`; post-staging fold), `pii-check.sh` output-pattern (`[PII BLOCKED]` marker) infra-vs-detection labeling (both branches fail-closed); (05) skill/context prose — codex-review closure→Phase 5, plan Step 2a file-form artifact staging glob (drop trailing slash), filesystem-context current main-session-spawns dispatch model, context-fundamentals phantom CLAUDE.md "Workflow" citation removed (real section is "## Workflows"). `.claude/**`-only (no src/tests/migrations/scripts/docs). Scorecard: per-story CR skipped (context-layer); Phase 4a CR integration 0 findings APPROVED r1; Phase 4b Codex 3 findings — ② accepted+fixed (abort survivor-set), ① dismissed-invalid (worktree-guard relative-path — guard acts only on absolute main-prefix paths, AC-1 met), ③ deferred→CE-5 (hooks/README.md stale "fail open" prose, pre-existing); CR remediation re-review APPROVED 0 new; + external post-staging review 2 findings both accepted+fixed in E-251-04 scope (commit-regex space-separated options + worktree-guard `..` reject), CR re-verify APPROVED 0 MUST FIX. Two low-severity hook-hardening residuals deferred as an epic-scoped History note (worktree pass-through `..`-climb-out + trailing-`\s+commit` over-intercept; IDEA-093 reserved if promoted). Full-suite **3524 passed/0**. No doc impact; context-layer triggers 3+4 fire but codified in-place (the epic IS the codification). **Migration NEXT=008** (unchanged).
- **E-250**: Root Cross-Season/Multi-Season De-Scope (COMPLETED + archived 2026-07-05) — dispatched between E-251 and E-252 in the platform-audit sequence (predates the audit; not a CE epic). 7 stories; `season_id` KEPT as the single-season partition key throughout (user decision — remove cross-season LOGIC, not the partition column). **Migration 008** drops `players.gc_athlete_profile_id` + the whole `team_opponents` table + `seasons.season_type` (direct `ALTER … DROP COLUMN` on SQLite 3.45.1, layered on 001; ALL `src/` refs + ~29+2 `season_type` fixtures/reads removed atomically with the NOT NULL drop = the green-gate proof). `bb data dedup-players` season-scoped (E-250-01): `season_id` required + auto-derived (sole season/no-op-on-zero/error-at-2+), `season_id=None` unrepresentable in `plan_player_dedup`/`find_duplicate_players`, NO multi-season execute loop (design lock). `ensure_season_row` fail-loud (`int(season_id)` raises on non-numeric/compound) + scouting crawler's private `_ensure_season_row` folded into the canonical seam (TN-9/AC-11 → makes arch-subsystems "consolidation complete" claim TRUE). `is_team_eligible_for_cleanup` 4 guards → 2 (is_active + no-other-report; behavior-preserving on the empty table). **F-H1 completeness caveat recorded**: the surviving 2 guards are NOT complete deletion safety — the shared-game/live-report guard is owned by CE-3/E-253 (user operator-hold: no report deletions until E-253 lands it); removing the 2 dead `team_opponents` guards here neither creates nor worsens F-H1 (they were empty-table no-ops). Class-1 compound-slug fixtures → year-only (Class-2 opaque tokens seeded via direct INSERT + Class-3 filesystem/crawl slugs LEFT; two distinct years 2025+2026 KEPT in exclusion fixtures; `plays_parser.py:22` fixed, `plays_loader.py:32` Class-3 left). Context-layer prose de-scoped (CLAUDE.md/`.claude/rules/**`/`.claude/agents/**`): no multi-season/longitudinal/cross-team-identity live-capability framing; `PlayerTeamSeason` (never-existed table) removed from ALL loaded context; guard prose renumbered 1-2 to match code; fresh-start philosophy KEPT. Human docs corrected (architecture/roadmap/operations; delete-cascade 4→2 + an F-H1-consistent "not complete deletion safety" caution). API-doc softening (athlete-profile + /me/* cross-season VALUE verdicts removed; schemas/behavior KEPT; cross-TEAM-in-current-season fenced) + a **user-directed fidelity remediation** (restored the dropped factual "across all seasons" scope token; deleted the added product-scope editorial Notes). E-104 (athlete-profile identity probe) archived ABANDONED (E-250-07). **Ancillary pulled forward from E-255 (per user)**: `PlayerTeamSeason` token scrubbed from agent-memory (DE MEMORY.md + endpoint-schema-notes.md, baseball-coach coaching-decisions.md, CA agent-blueprints.md); E-255 dockets annotated so its broader own-memory sweeps stay in scope. Scorecard: per-story CR 01/02/03/05/06 APPROVED 0 MUST FIX (04 context-layer + 07 PM-housekeeping CR-skipped by design, PM AC-verified); CR integration APPROVED, 1 SHOULD FIX (framing + E-255 cross-epic coordination, no code); Codex 4 findings — 3 accepted+remediated post-review (operations.md:508 operator-safety caution → docs-writer; `ensure_season_row` fail-loud negative tests, 13 passed → DE; CLAUDE.md:46 "across games and seasons" cross-season line → CA) + 1 dismissed (stale-worktree-copy PM-memory divergence artifact; committed main surface clean). **Two durable lessons codified**: (1) the **API-doc-fidelity principle** (API docs record how the API BEHAVES; never degrade factual endpoint docs; soften only our own coaching-relevance value verdicts) → context-layer trigger 1, CA codified into the API-doc rules/api-scout guidance; (2) **de-scope token-grep gap** — a token-grep sweep for cross-season framing missed the natural-language "across games and seasons" (both CA's sweep and PM AC-6 missed it; Codex P5 caught it) → pair grep with a prose read. Doc trigger fired, handled in-epic; context-layer triggers 1(YES→CA API-doc rule)/2(YES→in-epic E-250-04)/3,5(covered by 1)/4,6(NO). Full-suite **3526 passed/0**. **Migration NEXT=009.**
- **E-252**: Scheduled-Reports Reliability (Cron-Grade Morning-Run) (COMPLETED + archived 2026-07-06) — CE-2 of the platform audit; makes `bb report morning-run` cron-grade. 10 stories (SE+DE), NO migration (all code/doc). **Contention fix (06+07, two halves of one fix)** *(NARROWED by E-270, not falsified: the factory governs WRITERS. A READ that must not modify the target file MUST NOT use it — its unconditional `PRAGMA journal_mode=WAL` is a persistent file mutation, which is exactly how E-270's `preview_purge` mutated a purge target before confirmation)*: `get_connection()` extended to the single factory with `busy_timeout=30000`+`synchronous=NORMAL` + optional `db_path` (routed across the scheduled-reports triad — admin UI, interactive report CLI incl. `map-opponent` [E-252-03 AC-7 fold], morning-run cron; `bb data`/loader writers OUT → IDEA-099); PLUS `commit()`-after-`ensure_team_row`-before-network + per-slot `_upsert_slot` isolation + **reserve-before-generate** (auto_resolved+delivery_status=NULL marker, 600s lease, in-memory `suppress_persist` so a fresh-reservation skip never clobbers a concurrent run's NULL marker — P1#1 fix). **Invariant codified: never hold a write txn across a network fetch on the shared SQLite file** (busy_timeout-without-commit-before-network = false safety). **F-H2 slot-wipe** (01): idempotency skip carries the prior slug so `_upsert_slot` doesn't null `report_id`/`report_slug` → no wasteful duplicate regen. **Per-team isolation** (02): broadened beyond 403 to transient(5xx/connect)+429 with distinct run-level `transient`/`rate_limited` tallies + `conn.rollback()` in every shared-conn catch (TN-10); **systemic-429 escalate-and-stop** on 2nd occurrence; bare 401 stays run-fatal (catch order load-bearing — ForbiddenError ⊂ CredentialExpiredError). **429 client cap** (04): `_MAX_RETRY_AFTER_SECONDS=60`, within-cap→sleep+single-inline-retry, over-cap→raise-no-sleep (kills the `Retry-After:3600` hour-stall); `RateLimitError` isolated per-game in the SCOUTING crawler (not plays loop) preserving the `CredentialExpiredError` re-raise. **Operating-tz seam** (05): `src/util/timezone.py::get_operating_timezone`/`operating_today`, env `OPERATING_TIMEZONE` default `America/Chicago`, invalid→warn+fallback; **CE-2 INTRODUCES, CE-3/E-253 REUSES via `ZoneInfo.key`** (only the morning-run default target date here; game_date is CE-3). **Summary-email heartbeat guaranteed** (03): try/finally fires even on crash + retry-once + non-zero exit on failed/misconfigured; new `src/api/helpers.py::is_production()` single-source prod seam (`_is_dev_mode`→`not is_production()`); alerting-config preflight requires ADMIN_EMAIL + (prod) BOTH MAILGUN_API_KEY & MAILGUN_DOMAIN (P1#2 fix). **Stuck-`generating` reaper** (08, `STALE_GENERATING_SECONDS=3600`) on the lifespan + cleanup triggers + orphan-HTML unlink. **team_resolver** (09): `except httpx.TimeoutException`→`RequestError` family (ConnectError no longer crashes the run); **report-serve 500-vs-404 race** (10): `read_text()` in `except OSError`→uniform 404 (tolerates 08's reaper + cleanup as concurrent unlinkers). GC 429 UNOBSERVED (designed-not-tuned; api-scout added the policy note to `docs/api/error-handling.md`). Scorecard: per-story CR 8 clean + 1 advisory (map-opponent→folded to 03 AC-7) + 1 SHOULD FIX (08 `_REPORTS_DIR`, fixed); 10/10 PM AC-verify, 0 rejections; Phase-4a integration CLEAN (2 SHOULD FIX both dismissed out-of-scope→ideas); Phase-4b Codex 5 findings→4 remediated (P1#1 reservation-clobber, P1#2 MAILGUN_DOMAIN, +2 tests) + 1 doc-fix (E-252-06 Description over-claim narrowed), 0 dismissed-cosmetic. Doc gate fired (docs-writer: operations.md + production-deployment.md + .env.example; PM AC-verified incl. an Exit-codes completeness fix). Context-layer T1/T2/T3/T6 fired → CA codified the 3 canonical seams (is_production, operating-tz, get_connection busy_timeout factory + no-write-txn-across-network) + the morning-run CLAUDE.md description. Full-suite **3589 passed/0**. Closure follow-ups → IDEA-097 (team_resolver proxy/pacing posture), IDEA-098 (unify prod-detection via is_production/csrf.py), IDEA-099 (broaden busy_timeout to non-triad writers). **Migration NEXT=009** (unchanged).
- **E-255**: Truth Sweep — Context Layer, API Docs, Runbooks (COMPLETED + archived 2026-07-08) — CE-5 of the platform audit; brings the context layer, `docs/api/`, operator runbooks, and each agent's own memory into line with the reports-first, single-season reality. 9 dispatched stories (01–09) + a **pre-dispatch Step 0** run in MAIN (not a worktree story): the **R-01 fact-verification spike** (`.project/research/E-255-verified-facts.md`) + the two root-runbook `git mv`s into `docs/admin/`, both committed before the worktree branched. R-01 settled 6 load-bearing facts from ground truth (proxy/cached/live/code) because the prose under correction couldn't be its own source: **one id serves BOTH boxscore AND plays** (public-games `id` = `event_id`, NOT `game_stream.id` — the authenticated `event_id ≠ game_stream.id` is a real schema fact but does NOT force a two-id split), flat **`team_season`** shape (`season` bare string, `year` flat, singular record keys), **`pitches_7d`** 3-state (0 = no window appearances / NULL = appeared-but-all-unrecorded / SUM), the E-211 tracked-team `gc_uuid` self-heal OVERWRITE (membership_type='tracked' guard), the `bb creds refresh` recovery command (no `bb creds login`), and the `docker compose exec [-T] app …` host-exec form (package installed in-container only). Highlights: (01) CLAUDE.md + data-model/arch-subsystems/migrations truth corrections; (02) rules files + hooks README + **deleted `docs/agent-browsability-workflow.md`** + new **`secret-read-guard.sh`** (denies Read/Bash on `**/.env*`+`secrets/**` except `*.example`) + `edit-verify.sh` memory-path carve-out; (03) agent-charter refocus (ux/docs-writer REPURPOSED, not retired) + **§3 curation codified → context-layer assessment now EIGHT triggers** (7 = context-growth counterweight, 8 = reusable-behavioral-lesson closure gate) + the Learning-Loop Lifecycle (Load-Target Classification, deletion-side eviction, memory retirement) + two immediate promotions into `tool-output-integrity.md`; (04) `docs/api/` accuracy incl. the ~12-file id-routing consistency sweep + canonical "120 files (119 endpoints + 1 web-routes reference)"; (05) fresh-machine-executable runbooks (phantom migrations, plays-alive, health-URL, host-exec) with the 2 root runbooks now under `docs/admin/`; (06) PM hygiene — ROADMAP header/§4/§5 reconciled to executed, ideas README reconciled 1–107, `docs/E-221-HANDOFF.md` deleted, **E-193 + E-073 archived ABANDONED**, **E-072/E-174/E-175 demoted READY→DRAFT under the new READY Freshness Gate (READY >60d = STALE)**, IDEA-103..107 captured; (07/08/09) coach/DE/ux own-memory sweeps (TN-7: the ghost `Lineup`/`PlateAppearance`/`PitchingAppearance` list replaced with real tables `player_game_batting`/`player_game_pitching`/`plays`/`play_events` consistently across coach memory + DE memory + DE charter). Scorecard: planning = 1 internal (CR spec audit 3 + 6 holistic → 9 accept/4 dismiss) + 3 Codex iters (all folded); dispatch ≈ **13 findings → 12 accepted/fixed, 1 deferred-to-IDEA-107, 0 dismissed** (per-story CR on the 4 non-context-layer stories; E-255-05 needed a CR reject + 2 remediation rounds for a plays-module-path slip + the operations.md host-exec class; Phase-4a integration 1; Phase-4b Codex 5 cross-story id/terminology drifts all accepted incl. F4 endpoint-return-vs-flow-filter). **Context-layer assessment (8 triggers): 1,3,4,5 YES + 7 PASS/offset + 8 YES**; fired triggers codified in-epic or by CA (own-memory-worktree footgun → `worktree-isolation.md`) or in PM memory (trigger 8 → [[feedback-verify-cited-facts-before-approving]]). Doc impact handled in-epic (story 05). Not a ROADMAP §5 slice. Full-suite **3775 passed/0**. **Operator follow-up owed**: E-174 + E-175 live re-confirm (both real bug fixes demoted procedurally; one-line re-promote if still reproducing). **Migration NEXT=011** (unchanged — no migration).
- **E-258**: Review-System Hardening (COMPLETED + archived 2026-07-08) — NOT a CE epic; the AGENTIC-FLOW-REVIEW.md §6 review-system hardening package (rows 4-7 + 25 + routed-in row 2), the last §6 cluster without an epic. 4 stories (E-258-01..04), dispatched with "and review" per the TN-7 dogfooding reminder (E-258's own closure ran under the OLD pre-E-258 skill, so the new unconditional pass was requested explicitly). **(01, CA)** `code-reviewer.md` — the nine agentic-flow rubric edits (twin-path/consumer audit; adversarial-assertion rubric; per-changed-function edge-case enum; standing concurrency read-then-write w/ three-SQLite-writers; sensitive-path security trigger; two edit-in-place REVERSALS = self-load-on-diff-evidence for API/migration context + cumulative-migration baseline; `| head`/`| tail` prohibition; verbatim pytest evidence) + the Priority-1 **AC×surface matrix** + the dormant-migration **review-time DEMAND** (demand implementer's before/after scope + dry-run evidence, NOT CR executing) + machine-findable delimiter pairs (`BUG-PATTERN-CHECKLIST` + `SECURITY-CHECKLIST` START/END, each exactly once, SECURITY spanning all of Priority 4) with a documented delimiter contract. **(02, CA)** `implement/SKILL.md` Phase 4/5 restructure — Phase 4 = Codex-ONLY (still "and review"-gated); the CR integration review relocated to Phase 5 as the unconditional **Closure CR Integration Review** (Step 1c, the LAST pre-merge review, after Step 1a invariant audit, before Step 8 merge); **Codex-first** so CR adjudicates a real finding list instead of approve-then-reverse; the phase-number-independent **Remediation Spawn Context** defined once at the start of Phase 5; mechanical Step-1a invariant-audit triggers each naming a permitted-artifact source (diff / Technical Notes); new **`.claude/rules/doc-sweep.md`** glob rule (token-grep PAIRED WITH synonym expansion + semantic read; auto-loads via CR Step-2 rule-glob) closing the E-250 "across games and seasons" keyword-only-sweep gap; `workflow-discipline.md` L104 pointer fix. Full-suite-green gate mechanics (Step 1b label, Step 8 sub-step 5, abort + 2-round breaker) UNTOUCHED but for the forced remediation-context pointer rename. **(03, CA)** `codex-review.sh` + `codex-review/SKILL.md` + `.project/codex-review.md` — runtime delimiter-block extraction of CR's Bug-Pattern + Security checklists (resolved from REPO_ROOT/main checkout, NOT --workdir) ending rubric drift; the abbreviated bug-pattern section removed + the enumerated security bullet REPLACED with a content-free single-source pointer (exactly-one-security-source invariant, ENFORCED by a fail-closed marker guard that exits if any of the 4 markers is absent); deterministic `RESULT_FILE` tee + receipt (closing the manual-`>`-redirect fabrication hole, 44/48 skipped it) across all 3 mode branches via a shared `run_codex_review()` with pipefail preserved; WORKDIR `--diff-filter=ACMR` default (pure deletions no longer exhaust Codex budget on removal epics); self-contradicting line-243 note rewritten + best-effort-pytest note. **(04, PM)** the deferred item-25 **CR-vs-Codex gap re-measurement** trigger installed as a PM pending-work obligation (NOT run — precondition unmet: needs ≥5 epics archived after 2026-07-08 under the canonical Review Scorecard schema; count is 0) → see Pending Process Obligations + `project_cr_codex_gap_remeasurement.md`. Scorecard: planning = 20 findings / 20 accepted / 0 dismissed (CR spec audit 8 + architect holistic 8 + Codex spec-review 4); dispatch/closure = **4 findings / 4 accepted / 0 dismissed** (E-258-01/02 context-layer-only → PM AC-verified, CR skipped; E-258-03 1 SHOULD FIX = AC-4 letter-vs-intent resolved by AC-4/TN-1 wording reconciliation, implementation unchanged/strictly-stronger; E-258-04 CR-verified for author-independence, 0; Phase-4a CR integration 0; Phase-4b Codex 3 = SKILL.md L94/L104 read-receipt contradiction + guard-vs-claim exact-once/in-order gap + stale rubric-source claim, all fixed). **Context-layer assessment (8 triggers): 1,2,3,4 YES (codified in-epic) + 5,6 NO + 7 YES (growth load-bearing, offset by single-sourcing removing the duplicated `.project/codex-review.md` rubric) + 8 YES (Phase-4a rename deletion-side eviction = all agent-memory hits historical/provenance, no rename edit; 2 lifecycle actions — retired superseded `claude-architect/review-cycle-reordering.md`, annotated E-239 supersession in `epic-codifications.md`)**; all yes-codification done in-epic + the 2 lifecycle actions, no further CA codification pending. No documentation impact (internal agentic-review/dispatch-workflow change; self-documenting in the context-layer files). Sequence was E-255 → **E-258** → E-257 → E-256; the "E-258 before E-256, both edit code-reviewer.md" ordering is now satisfied (E-256's carve-out lands onto the hardened rubric). Full-suite **3775 passed/0**. **Migration NEXT=011** (unchanged — no migration).
- **E-260**: Dispatch Cost Accounting (COMPLETED + archived 2026-07-12) — meta-layer epic from the E-256-dispatch auditor reports; deletion-dominant (net **−9** to the four context-layer subtrees vs the merge-base). 8 claude-architect stories, one hand-read set. **NOT run through spec-review/Codex (AC-2)** — the reviewer stack has a conflict of interest reviewing its own constraints; the operator hand-reads the closure diff. Removed the verbatim-relay mandate apparatus (01, + closure sub-step-3 diff base fixed to `$(git merge-base epic/E-NNN main)`), corrected the false Test-Execution-Constraint at 5 sites (02 — worktree pytest exercises the WORKTREE's `src/` via `tests/__init__.py` `sys.path[0]`), two-tier severity floor (03 — MUST FIX names a functional consequence), scoped context-fundamentals relay sentence + re-derived the stale budget table (04), dropped the worktree-guard mode-1 agent-memory exemption + **relocated ALL closure-time own-memory writes into the sub-step-3 worktree patch** (05 — hook+skill atomic; E-260 closed under the OLD hook), added TWO mechanisms OUTSIDE the four measured subtrees: **`.claude/hooks/send-message-counter.sh`** (06 — SendMessage counter: warn 15/deny 25, worktree-keyed `git add` reset, operator-owned thresholds) + **`.claude/hooks/context-ratchet.sh`** (07 — MANUAL operator diagnostic, E-257-shaped: counts `*.md`+`*.sh` across the four subtrees vs committed baseline `.project/baselines/context-layer-ratchet.json`, exit 0/1/3/4; **trigger 7 → the ratchet** [soft-prompt disclaimer removed], **trigger 8 GATED not deleted** [cite-defect + fit-baseline; eviction/retirement hygiene kept], guard ~150/<150 numbers folded); baseline bootstrapped + AC-1 net−9 verified (08). Scorecard: per-story CR + PM AC-verify on all 8; **1 accepted finding total** (E-260-01 `plan/SKILL.md:390` AC-5 orphan, PM-AC-sourced, fixed r1), 0 dismissed, 0 MUST FIX; no Codex/Closure-CR rows (AC-2). Context-layer 8-trigger: 1,2,3,4,6 YES + 7 SATISFIED(−9) + 5,8 NO — **E-260 IS the codification, no further CA pass** (operator meta-layer freeze + Falsifier #3 bar re-expansion). No doc impact. Follow-ups → IDEA-116 (cwd worktree attribution for the `head -1` hooks + E-256 resume guard), IDEA-117 (multi-agent-patterns:24 scope), IDEA-118 (context-fundamentals ambient-figure refresh). **Operator follow-ups owed**: sign off / commit the ratchet baseline (untracked, staged at closure); re-run `--update-baseline` if the closure ledger pushed the tree above 12079; before resuming parked E-256, `rm -rf` its stray `.dispatch-log/`. **Migration NEXT=011** (unchanged — no migration).
- **E-257**: Plays→Boxscore Reconciliation Scoreboard (productization) (COMPLETED + archived 2026-07-09) — NOT a CE epic; homes the E-245-deferred scoreboard-productization thread surfaced by the 2026-07-03 platform audit. Makes CLAUDE.md's north-star Operating Principle ("Always Get Closer to Byte-Identical Play Ingestion") bind MECHANICALLY — the principle previously "bound intent" pending a scoreboard; the scoreboard now exists. DE-owned (baseline author; grain/axis are data-model calls). 3 stories, NO migration. **(01, DE)** new pure read-only core `src/reports/recon_scoreboard.py::compute_scoreboard(conn) -> ScoreboardResult` (mirrors `aggregate_parity.py`) + `bb report reconcile-scoreboard` CLI (human Rich table + net-new `--json`): per-stat plays-vs-boxscore fidelity (exact% + abs-Δ) at player-grain, perspective-scoped, no-plays-excluded, for pitching {BF,SO,BB,H,HBP}/batting {AB,H,BB,SO,HBP}; three axis counters `dropped_pitch_events`/`no_plays_units`/`self_games` as distinct named fields (stable JSON keys E-256's closure smoke consumes); dropped-pitch predicate REUSES the parser's `_PITCH_TEMPLATES`+`_PITCH_ANNOTATION_PATTERN` (not naive `LIKE '%(%)%'`); **stat-def source-of-truth FLIPPED — code constants in `recon_scoreboard.py` are canonical, the E-245 baseline doc now MIRRORS them** (pinned by constant + behavioral tests); batting {AB,H}-only residual label; perspective-only miss = display-only sub-line under `no_plays_units` (TN-5). **(02, DE)** committed-JSON-baseline + one-way ratchet gate (`evaluate_gate`): fires only on a WORSENING gated abs-Δ (batting {AB,H,BB,SO}/pitching {H,SO,BB}) or ratcheted axis (dropped_pitch/no_plays) INCREASE; `self_games>0` = HARD ZERO (aligns E-256 smoke); improvements/hold-steady pass; metadata header ignored in the diff; `--update-baseline` operator refresh; exit codes 0 pass / 1 regression-or-precondition / 3 baseline-absent-bootstrap / 4 malformed-baseline (the 4 added CR-round-2). **Implementer/operator split**: implementer delivered gate code + SYNTHETIC fixtures only; the real `.project/baselines/reconciliation-scoreboard.json` is an OPERATOR closure artifact (live DB absent from worktrees) — **operator follow-up owed**. **(03, SE)** report-time TEAM-level plausibility WARNING (`_check_rate_plausibility` in `generator.py`): `team_fps_pct` outside 30–75% (stored as a FRACTION 0–1 → compared 0.30–0.75) or `team_pitches_per_pa` outside 3.0–4.5 → advisory WARNING log, report ships unchanged; team aggregates only (per-player would flood on small-sample relievers); persisted-trust-flag form deferred → IDEA-108. Manual operator diagnostic, NEVER a CI gate (dev-only host-mounted DB absent from CI). Scorecard: planning = 24 findings + 1 emergent AC / all accepted / 0 dismissed (CR spec audit 7 + holistic 10 + Codex ×2 = 7); dispatch = **10 findings / 10 accepted / 0 dismissed** (per-story CR E-257-01 4 [1 MUST+3 SHOULD] / E-257-02 4 / E-257-03 1 + Closure CR Integration Review 1; no Codex row — plain "implement"). Doc gate FIRED → docs-writer added `docs/admin/operations.md` §Reconciliation Scoreboard + §Report Plausibility Warnings (with an explicit "do not conflate with `bb data reconcile`" comparison table); PM AC-verified. **Context-layer (8 triggers): 1,2,3,5,6 YES → CA codified** (CLAUDE.md Commands entry + Operating-Principle "now binds mechanically" rewrite + Architecture reconciliation-scoreboard-conventions bullet [source-of-truth flip + manual-operator-diagnostic/operator-owned-baseline]) + 7 offset load-bearing + 4,8 NO. AC-8 subprocess smoke green in main at closure. Full-suite **3849 passed/0**. Hard UPSTREAM of E-256 (feeds its closure smoke + query-time-aggregate cutover sign-off). **Migration NEXT=011** (unchanged — no migration).
- **E-256**: Post-Descope Simplification & Foundations (COMPLETED + archived 2026-07-12, commit `8d66727`) — candidate epic CE-6 of the 2026-07-03 platform audit; retires the machinery the reports-first reframe made vestigial and lays the foundations the project never grew. **16 stories** (SE + CA + docs-writer). **Deletions/restructure (01–05, SE):** deleted the production-dead disk-based twin load flow (`PlaysLoader.load_all`/`GameLoader.load_file`/`ScoutingLoader._load_team_from_disk` + private disk helpers; net −19 on story 01 alone, ~24 true deletions + 24 ports) — `load_payload`/`load_from_data` is now the SOLE loader entry path, no `src/` module reads `data/raw/`; deleted the dead `bb data backfill-appearance-order` command (02, live-DB NULL count 0 → recovery path unneeded); dead-code sweep of `bridge.py`/`discover_opponents`/ghost dirs + `_utcnow_iso` consolidation (03); extracted a client-free `src/reports/lifecycle.py` from `generator.py` (04, 3031→2356 lines) AND **relocated the pure season SQL fetch to `src/api/db.py` as `get_season_batting`/`get_season_pitching`** (DE's relocation contract — sets up E-259's in-place SQL cutover; `_query_*` stay in generator.py as thin wrappers; golden zero-diff bracketed); fixed the rest-day reference date at all three UTC sites (05, the orphaned third site of the E-252/E-253 systemic finding). **Foundations (06–10):** `.dockerignore` (06) + CI static gate `.github/workflows/ci.yml` (09, pytest + PII pattern-scan + ruff + lockfile-drift + docker-build); dependency refresh (07 — **32 known vulns across 10 packages closed, not the 2 named**, incl. a starlette MAJOR 0.41→1.3 across the webauthn crypto backend); ruff F-class (08); scheduled off-disk backups + Step-1d smoke doc + seeds correction + backfill-runbook eviction in `docs/admin/` (10, docs-writer). **The consequential gate (11, CA):** Phase-5 **Step 1d closure runtime smoke** — the reports flow's first LIVE runtime gate at closure (main-checkout, post-apply, live dev DB; env-FAIL preflight escalates, epic-FAIL remediates; CR self-evaluates the trigger; two `code-reviewer.md` carve-outs), the thing that would have caught the physically-impossible FPS stat + the rest-day UTC bug that both shipped through green closures. **PII/context (12–16):** data-model dead-by-descope note (12, `crawl_jobs`/`coaching_assignments`/~100 split columns retained-by-decision; `user_team_access` stays LIVE); PII suppressor true-scope doc + 3-tier choice hierarchy (13); doc-PII byte-gate extended to planning trees `epics/`+`.project/` (14, promotes IDEA-102); context-layer eviction of the deleted surfaces (15 — the **E-237 convention REVERSAL** at `architecture-subsystems.md:72`: "new loaders must NOT add a file-reading wrapper," + CLAUDE.md canonical-seam re-points [team-deletion→lifecycle.py, Season_id narrowing] + `backfill-appearance-order` prose strike); the two pre-existing PII hits fixed (16, CI-PII-green gate closed — tier-1 fixture value change + tier-2 line-scoped `# pii-ok`, whole-tree scan 453 files/0/exit 0). **Scorecard (dispatch): 2 dismissals total, both story 11** (the `SKILL.md:480` stale-pre-E-260-worktree false-positive + CR's own `:650` SHOULD) + 2 accepted-SHOULD residues (06 `.dockerignore` anchoring comments, 10 footer label); context-layer-only stories 12/13/15 PM-verified-alone (CR skipped); Step 1c Closure CR Integration Review APPROVED. **Closure:** Step 3 doc-assessment triggers 1/2/5 fire but FULLY DISCHARGED in-epic by story 10 (no docs/api impact — deleted `bridge.py` ≠ live API UUID-bridge endpoint); **Step 3a context-layer 8-trigger: 1,2,3,4,5,6,8 YES (codified in-epic 11/12/13/14/15 + CA: ugrep-trap→`tool-output-integrity.md`) + 7 FIRES → operator-signed +93-line ratchet exception** (est. 12081→12174, driven by Step 1d's +59 `.claude/skills`, a defect-cited load-bearing gate with no offset; operator runs `--update-baseline` post-merge). **Two Step-1d LIVE-RUN findings → IDEA-122** (`bb creds check` exits 0 on dead creds — the Step 1d preflight must parse output, not exit code) **+ IDEA-123** (Step 1d runs `generate`, which can ingest net-new plays, BEFORE `reconcile-scoreboard`, tripping the ratchet vs a pre-generate baseline). IDEA-119 (closure-sequence merge-base) DISCARDED-moot (E-260-01 AC-6 already shipped it). **Merge landmine handled:** the E-256 worktree branched pre-E-260, so closure diffed against `git merge-base epic/E-256 main` and a cross-branch IDEA-116/117 collision was renumbered to 120/121 during dispatch. Full-suite green gate + Step 1d ran post-apply at Step 8. **Migration NEXT=011** (unchanged — no migration). Hard PREREQUISITE of E-259 (which rewrites the relocated season-fetch SQL in place).
- **E-259**: Query-Time Season Aggregates (COMPLETED + archived 2026-07-12) — split out of the E-256 stub (CE-6); hard-DOWNSTREAM of E-256 (rewrites the E-256-relocated season fetch in place). Cuts season totals from the stored `player_season_*` tables to query-time derivation, then deletes the whole stored-aggregate + parity apparatus. 6 stories (DE + CA + docs-writer), **net ≈ −3k lines**. **(01, DE — cutover):** `get_season_batting`/`get_season_pitching` SQL bodies rewritten IN PLACE to SUM from `player_game_*` (perspective-filtered) by subquery-wrapping the shared `batting/pitching_recompute_select()`, ORDER BY reproduced over the per-game SUMs; **golden zero-diff bracket** via the `_query_*` wrappers. **#1 hazard (perspective-doubling) has GENUINE teeth** — a two-perspective fixture (same `team_id`, different `perspective_team_id`) + a `test_input_is_genuinely_doubled` control proving the unfiltered sum doubles, so the `WHERE perspective_team_id = ?` filter (not GROUP BY) is load-bearing. **AC-3 latent-fixture correction (user-approved):** the golden pitching season row was internally self-contradictory (season 3/54/22 vs profile 4/69/26); regenerated to the per-game SUM (verified to the digit), a confined sanctioned regen, zero-diff still binds every other section. **(02, DE — retire writers):** removed `canonical_recompute`, `ScoutingLoader._compute_season_aggregates`, the player-dedup recompute; the merge member-row re-point retired (`_delete_or_repoint_season_rows` → `_delete_duplicate_season_rows`, a plain FK-safe DELETE); **zero INSERT/UPDATE on `player_season_*` in `src/`**. Q1: deleted `tests/test_aggregate_parity.py` after PORTING its projection/KEYS coverage to new `tests/test_season_projection.py` (PM-verified faithful; the `*_RECOMPUTE_KEYS` orphan RESOLVED — the port consumes them, so they survive story 04). Q3: `_COMPLETENESS_RANK` DEFERRED (vestigial-in-production but independent of the cutover + defends a schema-permitted state) → **IDEA-124** (DE). **(03, DE — migration 011):** drops both tables behind a **refuse-on-member-row preflight** (two connection-local temp triggers each `RAISE(ABORT)` naming the offending table, fired by one sentinel INSERT inside the E-253 transactional runner → a `full`/`supplemented` row aborts the whole file, both tables intact, no `_migrations` row = corrected-re-run guarantee); leaf status verified. **Q2 FK-DELETE handoff DISCHARGED atomic with the DROP:** the two `cascade_delete_team` season DELETEs (`lifecycle.py`) removed + `_delete_duplicate_season_rows` removed entirely. D1: deleted `tests/test_validate_plays_stats.py` (errors post-DROP) after CR confirmed validate-plays-only coverage. **(04, DE — delete apparatus, −1,266):** `git rm` `src/reports/aggregate_parity.py` + `scripts/validate_plays_stats.py`; `bb report verify-aggregates` CLI removed (`report.py` + `__init__.py` help); **`src/db/season_aggregates.py` KEPT** (the live home of `*_recompute_select()` + `*_RECOMPUTE_KEYS` the query-time readers consume — aggregate_parity only aliased them, so its deletion orphaned nothing); 3 `recon_scoreboard.py` design-rationale prose refs reworded (rationale preserved); a pre-existing F401 kept (CR-confirmed genuinely dead). Spec-hygiene: story-04's stale test-deletion listing was trimmed pre-dispatch (both test files already gone in 02/03), CR git-truth-pinned. **(05, CA — context-layer eviction, net shrinkage / NO substitution):** 4 rules reconciled (`data-model.md` Season-Aggregate Parity → query-time tombstone that PRESERVES the surviving perspective-filter invariant; `key-metrics.md`/`perspective-provenance.md` MC-3/`architecture-subsystems.md`); `code-reviewer.md` Bug-Pattern worked example reconciled INSIDE the `BUG-PATTERN-CHECKLIST` delimiters (`player_season_batting` → surviving `player_game_batting`), markers byte-intact; **the E-256-11 Step 1d `verify-aggregates` HARD sub-check STRUCK** from `implement/SKILL.md` + `code-reviewer.md` + its Test-Execution-Constraint list (PLAIN DELETION with a "no aggregate-integrity gate remains" note — protects E-259's OWN closure Step 1d smoke from invoking a deleted command); CLAUDE.md cleaned; CA's `epic-codifications.md` annotated `[RETIRED E-259]`. **AC-6 = trigger-8 promotion:** `context-layer-assessment.md` Deletion-Side-Eviction generalized to grep each agent's OWN `.claude/agent-memory/<agent>/` dir (index AND topic files), reconcile-not-strike. **(06, docs-writer — runbook eviction across 3 `docs/admin/` files):** removed operations.md's "Verifying Scouting Aggregate Integrity" subsection, production-deployment.md's Step-1d line, rebuild-procedure.md's "No double-counted aggregates" SQL — **no blind substitution** (pure-parity steps removed, not swapped for `reconcile-scoreboard`); staleness footers on all 3; migration-011 row added; docs-writer own MEMORY.md re-anchored (discharges CA's docs-writer closure seed). **The surviving invariant is the perspective filter** (every query-time season sum scopes by `perspective_team_id`; perspective-provenance MC-3). Scorecard: planning 0 dismissals on E-259 (2 Codex iters); dispatch = per-story CR + PM AC-verify on all 6, all APPROVED, 0 MUST FIX. Context-layer: genuine net shrinkage (command + parity apparatus + Step 1d sub-check struck, nothing added); trigger-8 fired+discharged in-epic (story-05 AC-6); full 8-trigger + trigger-7 ratchet verdict recorded in the epic History at closure. Full-suite **3754 passed/0** at story-06 DONE (closure green gate finalizes). Per-agent memory sweep: DE 5 / CR 3 / PM own-dir reconciled, docs-writer discharged via 06, SE clean. Follow-ups → **IDEA-124** (Q3 `_COMPLETENESS_RANK`), **IDEA-113** (rename the now-misnamed `season_aggregates.py` — *DELIVERED by E-262-04: renamed to `src/db/season_projection.py`, byte-identical rename, PROMOTED*); **operator: DB backup before migration 011 runs on deploy.** **Migration NEXT=012** (011 = the season-aggregate-table drop). *(Historical note: the `season_aggregates.py` references in this E-259 record are accurate-as-of-E-259, which KEPT the module; E-262-04 subsequently renamed it to `season_projection.py`.)*
- **E-261**: Cross-Perspective Game-Dedup Fidelity (COMPLETED + archived 2026-07-13) — from an operator bug report; fixes GameLoader's two cross-perspective dedup defects that rendered otherwise-complete reports `degraded`. 5 stories (SE + DE), NO migration (query-time/loader-path fix, migration set unchanged through 011). **(01, SE — Defect A first half):** `_upsert_game` conflict clause → keep-existing `game_stream_id = COALESCE(games.game_stream_id, excluded.game_stream_id)` (EXISTING-first, NOT the prefer-new order of the adjacent start_time COALESCE — de-trapped) so a cross-perspective redirect stops clobbering the canonical row's self-keyed stream id; `migrations/010`'s header premise corrected (tracked `game_stream_id` is non-null/perspective-specific/self-keyed — the old "NULL/stable" claim described the retired auth game-summaries model; index DDL unchanged). **(02, DE — merge primitive):** new `src/db/game_merge.py::merge_duplicate_game(conn, source, canonical)` — pure connection-in/no-commit, re-points all six FK children (game_perspectives unioned + 5 tables; play_events follow plays), **PRE-classification refusal** of non-disjoint-perspective pairs (never a mid-merge IntegrityError catch), delete-losing-row-LAST (no-cascade reality). **(03a, SE — Defect B):** exact-score gate replaced by a **schedule-count** discriminator (PRIMARY, only standalone trigger; score-tolerance corroboration-only; DEFAULTS OFF without count context so the line-598 test still holds), applied **perspective-agnostically** across the whole `_find_duplicate_game` candidate loop (closes the post-merge same-perspective re-dup / idempotency gap); count precomputed in `ScoutingLoader._build_schedule_counts` (shared `_derive_game_date` seam, fail-open→fail-closed on unresolved opponent) and threaded to GameLoader; redirect **score-ownership** deterministic (canonical/first-loaded wins, same-perspective reload still corrects); shared OFFLINE predicate `is_offline_same_game` (disjoint + score-tol + REQUIRED near play-count) factored into `game_merge.py`. **(03b, SE — Defect A second half):** redirect site now merges an already-persisted source-event twin in-pipeline (captured original event_id BEFORE `replace()`), `try/except sqlite3.Error`→rollback→`LoadResult(errors=1)`, honors the helper's structured refusal — self-healing + idempotent on regeneration. **(04, DE — operator repair):** `bb data merge-duplicate-games` (dry-run default, `--execute`) — offline duplicate-pair detection reusing `is_offline_same_game` with a hard cardinality gate (exactly-2-disjoint, ≥3 REFUSED), poisoned-`game_stream_id` restore (tracked-only, member-skipped, hard corroboration, idempotent), **continue-per-item** failure model (rollback + continue + non-zero exit iff any failed). Scorecard: planning 27/27/0 (internal Phase-3 + Codex iter-1 + api-scout GC-claim consult; story 03 pre-split 03a/03b); dispatch/closure = **5 findings / 5 accepted / 0 dismissed** (per-story CR: 03a 1 [producer-test gap, fixed r2]; Closure CR Integration 1 [doc SHOULD FIX → docs-writer]; Codex code review 3 — P1 `_build_schedule_counts` doubleheader-undercount fail-open→fail-closed, P2 twin-merge read-then-write TOCTOU `GameMergeError` now caught, P3 vestigial `--dry-run` flag removed). Doc gate FIRES (new `bb data merge-duplicate-games` → docs-writer). Context-layer 8-trigger: 1,2,3,6 YES (canonical merge helper + game_merge.py home + single-home offline predicate + fail-open/TOCTOU footguns + migration-010 premise + new bb data subcommand → claude-architect) + 7 evaluate-and-offset (ratchet) + 4,5,8 NO. **Blast radius bounded** — no stat double-counting (perspective filter intact); damage was degraded badge + dropped spray + orphan/poisoned rows. **Operator closure steps (live DB):** reconcile-scoreboard before/after (ratchet must not regress; `no_plays_units` may improve as perspectives align) + regenerate Pat Hagge Patriots Reserve → confirm non-degraded. IDEA-134 = the out-of-scope play-ROW cross-perspective dup sibling (E-261 dedups game ROWS). Full-suite **3798 passed/0**. **Migration NEXT=012** (unchanged — no migration).
- **E-262**: Post-Program Housekeeping — Audit Residuals, Fold-In Ideas, and Live Bugs (COMPLETED + archived 2026-07-13) — the 2026-07 platform-program tail (7 in-scope audit residuals + 10 mechanical fold-in ideas + 4 live bugs). **8 active stories (01–07, 09); 08 ABANDONED** (coaching-docs rewrite premise already delivered by E-239); NO migration (set unchanged through 011). **(01, SE):** dedup `--dry-run`/`--execute` mutual-exclusion (SystemExit before any DB/planner call), reload-annotated-pitches non-zero exit on errored games, `bb status` DB-path via `resolve_db_path()` (module-const → call-time + `.relative_to` ValueError guard). **(02, SE):** 7 loader/crawler docstrings de-banned, empty `tests/test_crawlers/` deleted; **AC-3 ruled SATISFIED-AS-IS** — the story's docker-compose "override carries no port mappings" premise was contradicted by the file on disk (override.example DOES carry both dev ports; base comment already accurate), rewording would have injected a falsehood. **(03, SE):** `detect_league_level` extended to GC's free-text `age_group` range form (`"Between 13 - 18"` → `youth_travel` → PITCH_SMART_15_18); name-only wrong-team badge false-positive fixed by re-stamping name_only→anchor guarded on a REAL anchor (`public_id IS NOT NULL AND rowcount > 0`, NOT rowcount alone — the NULL→NULL trap). **(04, DE):** `src/db/season_aggregates.py` → `src/db/season_projection.py` byte-identical `git mv` (delivers IDEA-113; symbol names kept for git-blame). **(05, CA):** 5 context-layer truth/staleness corrections (CLAUDE.md raw-archive claim → in-memory reality + morning-run --dry-run prose; multi-agent-patterns:24 scoping; context-fundamentals :28/:193 ambient figures; perspective-provenance public-scorebook caveat + public-games-id promotion) + an **AC-1 doc-sweep scope-completion** (`http-discipline.md:44` raw-persistence reworded two-case: in-memory pipelines store no raw tier / proxy+ingest paths persist redacted captures). **(06, CA):** Step 1d `bb creds check --profile web` preflight (mixed-multi-profile false-green fix; no `creds.py` change) + terminal-`.smoke-fixture` generate/reconcile ordering (static corpus, one operator bootstrap re-snapshot, no reorder; real GC id stays in gitignored file). **(07, DW):** admin-docs hygiene — getting-started port-map verify-only, architecture.md schema-changelog reconciled to real migrations 001–011, operations.md creds-recovery recipe collapsed to `bb creds check --profile web`, production-deployment.md Step 1d synced to the settled skill text. **(09, api-scout):** docs/api boxscore path-variable rename (`game_stream_id`→`event_id`, token-scoped, 0 dangling) + scouting-flow stat audit (near-no-op) + `age_group` range-form doc note. **PII SAFETY (story 09):** a real GC exposure surfaced pre-existing in api-scout-owned files story 09 touched — 3 full UUIDs (`c05a5413`/`1e0f8dfc`/`3cab6a64`, proven real by a dated live-test recurrence) + team name "Nighthawks Navy" — all redacted in-epic to the `<prefix>-REDACTED` convention (verified 0 in docs/), CR re-approved R2. **Scorecard:** planning 25/25/0; dispatch/closure = **3 findings / 2 accepted / 1 dismissed-as-pre-existing** (per-story CR 01–04/07/09 all 0 MUST-FIX except 09's PII fixed-R2; 05/06 context-layer-only PM-alone; Closure CR Integration 0/APPROVED; Codex Phase-4 gpt-5.6-terra 2 — F1 operations.md bare-`bb creds check` fixed→`--profile web`, F2 /plays token proven byte-identical pre-existing → IDEA-138). Doc gate: updated in-epic (07+09). **Context-layer 8-trigger:** 1,3,4,5,8 YES (all codified in-epic; E-260 freeze held — every edit defect-cited); 2,6 NO; **trigger 7 = FAIL nominal ~+160 but operator-signed re-snapshot, NO offset owed by E-262** (own contribution ~+3 defect-cited; ~+157 is pre-existing agent-memory drift identical on main+worktree, same as E-261's +132). **Recurring lesson (trigger 8):** "re-verify an idea's target still exists AND shows the defect before folding" fired hard (audit #6/#8 false premises, IDEA-078/092 already-fixed, IDEA-010/105/111 drifted anchors, AC-3 disk-contradiction) — already at target in [[feedback_reverify_idea_before_folding]]. **Follow-ups:** IDEA-137 (corpus-wide docs/api doc-PII certification sweep — 6 more files carry "Nighthawks Navy" + UUID `14fd6cb6-...` + public_id `smgRExWHuBJJ`; operator runs `check_doc_pii.sh` w/ real denylist), IDEA-138 (/plays token, 7-file, Codex-corroborated), IDEA-139 (context-fundamentals worked-example figure refresh). **Operator decisions carried to summary:** ratchet `--update-baseline` re-snapshot + IDEA-137 PII sweep. Full-suite green gate at closure. **Migration NEXT=012** (unchanged — no migration).
- **E-264**: League-Aware ERA Basis Fix (COMPLETED + archived 2026-07-16) — from a live api-scout finding: our report computed ERA on a hardcoded 9-inning basis (`ER×27/ip_outs`), overstating the GC-app value ~29%. GC computes ERA on a per-team-season game-length basis (`innings_per_game`, empirically 6 or 7, NOT age-derived — read the per-team field). 4 stories (DE/SE/SE/api-scout), **migration 012** (the only DDL). **(01, DE):** migration `012` adds bare-nullable `teams.innings_per_game` (NULL = load-bearing "assumed" provenance — an explicit no-DEFAULT/NOT-NULL invariant the display contract depends on); `get_season_pitching` carries the raw basis at the OUTER wrapper level (LEFT JOIN teams, no SQL COALESCE, `pitching_recompute_select()` untouched); `ensure_team_row`/`_with_provenance` gain an `innings_per_game` param + `_backfill_innings_per_game` mirroring `_backfill_season_year` (NULL→value fills; a later None never clobbers a stored int). **(02, SE):** the report pipeline fetches the basis from the authenticated `GET /teams/{gc_uuid}` at the gc_uuid-resolution seam (non-fatal on 403/`CredentialExpiredError`/absence, runs independent of spray, writes via `ensure_team_row`+commit so it's visible to the later read); both ERA sites (`generator.py` `_compute_pitching_rates`, `renderer.py` `_era_raw`) compute `ER × (basis × 3) / ip_outs` via the NEW shared `era_basis_innings()` helper + `DEFAULT_ERA_BASIS_INNINGS=7` in `src/api/helpers.py` (explicit `is not None else 7` — a bare `if not None` would crash `None*3` on the assumed path); **K/9 (three sites) + WHIP UNCHANGED** (scope (b) ERA-only, TN-9); golden regenerated to the basis-6 values; seed.sql seeds BOTH provenance cases (a stored 6 + a NULL). **(03, SE):** verbatim TN-7 basis disclosure — `_era_basis_disclosure()` derives `{basis, assumed=innings_per_game is None, footnote}`; the Pitching ERA `<th>` renders `ERA (N-inn)` / `ERA (N-inn)*`, a conditional one-time footnote (`* Game length not available…`) only when assumed, + the inline key-player-card label `4.50 (N-inn)`/`*`; no raw field name user-facing. **(04, api-scout):** corrected `get-teams-team_id.md` (`innings_per_game` = authoritative per-team-season ERA/K-per-game basis, NOT age-derived; removed the "likely 9 for HS varsity"/"7 for travel ball" speculation + the K/9/BB/9-are-GC-fields implication) + the K/G "per-9" mislabel in BOTH `season-stats.md` and `player-stats.md` (the only two occurrences; adjacent K/BB/K/BF/BB/INN left as-is). **Scorecard:** planning 21/21/0 (6 review passes: 4 expert holistic + CR spec audit + Codex); dispatch/closure = **2 findings / 2 accepted / 0 dismissed** (per-story CR 0/1[SHOULD, fixed r2]/0/0; Closure CR Integration 0; Codex code review 1 P1 = migration-012 deploy-safety scope assertion, **satisfied as a review artifact, 0 code changes** — the migration is the safest metadata-only shape, already test-asserted). Doc gate: docs-writer light-touch `standalone-reports.md:35` note (ERA column names its game-length basis + assumed asterisk/footnote) + staleness header; K/9/BB/9 untouched. **Context-layer 8-trigger: trigger 1 YES → CA added `teams.innings_per_game` to `data-model.md:19`** (load-bearing-NULL provenance + hard never-DEFAULT/NOT-NULL/blind-backfill invariant, parallel to the `season_year` entry); CA resolved both judgment calls NO (`era_basis_innings()` not a canonical seam; no baseball-coach pointer) + 2,3,4,5,6,8 NO + 7 no-concern (epic itself touched zero `.claude/`; the CA+docs codification rides the closure patch). K-rate stat basis (K/9 vs K/G vs K/BF) deliberately deferred → **IDEA-141** (folded into E-265); dead `member` membership_type residue → **IDEA-142**. Hard UPSTREAM of E-265 (Pitcher Outings), which reads `teams.innings_per_game` + reuses `era_basis_innings()`. Full-suite green gate + Step 1d smoke at closure. **Migration NEXT=013** (012 = the `teams.innings_per_game` add).
- **E-265**: Pitcher Outings Breakdown (COMPLETED + archived 2026-07-16) — promotes coaching value from the E-263/pitcher-outings consultations; a flag-gated (`FEATURE_PITCHER_OUTINGS`, mirrors `FEATURE_PREDICTED_STARTER`, off by default) per-pitcher game-by-game Outings Breakdown section on the standalone report. GREEN-highlight-only, NO red/exploit flags; per-outing ERA on E-264's `teams.innings_per_game` basis. 4 stories (SE/UXD/docs), NO migration (query-time derive from stored boxscore + plays; opponent season-stats is 403). **(01, SE — derivation):** new `src/reports/pitcher_outings.py` (read-and-derive, writes nothing → `src/reports/` per the `starter_prediction.py`/`recon_scoreboard.py` precedent) — per-appearance boxscore (reuses `get_pitching_history`) + plays-derived FPS% (charted-PA `pitch_count>0` denom) & HR-allowed (`plays.outcome='Home Run'`, module-local `_HR_OUTCOMES`), driven OFF the completed-game boxscore outings (F12, no phantom outings); per-outing ERA reads `teams.innings_per_game` scalar + reuses E-264's `era_basis_innings()` (`is not None`→7, does NOT touch E-264's two ERA sites); season line = K/BF+BB/INN+K/BB+H/BF with small-sample flags (`ip_outs<45`, `bb<5`, distinguishable zero-BB); green-outing flag = OR of 4 TN-4 criteria (Aggression gates charted-PA not raw BF); role filter `perspective_team_id=scouted AND batting_team_id!=scouted` (two-perspective no-double-count); `is_pitcher_outings_enabled()` flag reader owned here. **(03, UXD — layout spec):** `.project/research/E-265-outings-layout-spec.md` (column tiers via existing `mob-hide`/`mob-hide-extra`, native `<details>` disclosure + `<summary>` green dot, `.outing-strong` green-only row, inline middot season line, print-pagination override, Opp ellipsis, section-level plays-derived note; field names reconciled to E-265-01's shipped dataclasses). **(02, SE — render):** flag-gated INLINE section in `scouting_report.html` (Resolved Decision #2 — no `{% include %}` framework; IDEA-144 tracks the deliberate split) + generator wiring INSIDE the DB-connection scope + 3 render-boundary Jinja filters (`pct`/`rate`/`rate2`) in `renderer.py`. **(04, docs):** coaching how-to `docs/coaching/pitcher-outings-breakdown.md`. **Scorecard:** planning 23-raw→21-distinct/21/0 (CR spec audit + 4 expert holistic + Codex spec review, 2 consistency sweeps clean); dispatch/closure = **14 findings / 14 accepted / 0 dismissed** (per-story CR 01 1 / 03 4[2 MUST+2 SHOULD] / 02 2 / 04 3; Closure CR Integration 1[stale comment]; Codex code review 3 — F1/F2/F3). **Codex F1 = the notable closure call:** CR refuted it as a code defect on the always-on-CSS predicted-starter precedent, but **PM ruled FIX on AC intent** — epic Success Criterion's "byte-identical / adds and removes nothing" is the GOAL, "mirroring FEATURE_PREDICTED_STARTER" only the how-guide; gating the outings `<style>` under the same `{% if show_pitcher_outings %}` is the sole reading where both clauses hold + makes the off-by-default kill switch provably inert (flag-off now byte-identical to a pre-feature render, body AND CSS). F2 = `games_started`→`int|None` (renders "—" not a false "0 GS" for an all-NULL `appearance_order` scope, mirroring `get_season_pitching`'s unknown-vs-zero NULL semantics — real coach-facing correctness). F3 = per-pitcher blocks sorted season-IP-desc. **Doc gate FIRES** (E-265-04 how-to in-scope + closure docs-writer added `FEATURE_PITCHER_OUTINGS` to `docs/admin/operations.md` feature-flags table). **Context-layer 8-trigger:** 1,2,4,5,6 NO; **3 YES** (two footgun lessons recorded-not-codified: derived read-surface must mirror unknown-vs-zero NULL semantics [F2]; flag-gated section must gate its CSS too [F1]); **7 YES/FAILS → operator-signed exception 2026-07-16** (+304 = +275 pre-existing agent-memory drift since the 2026-07-13 baseline + +29 E-265's UXD design memo; no offset; baseline cleanup deferred to a separate operator pass); **8 candidate-but-NOT-promoted** (F1/F2 defect-cited but gated on the over-baseline ratchet + no prior-epic recurrence). No new codification → claude-architect NOT spawned; no deletions → no eviction sweep. IDEA-141 PROMOTED (folded); extended Group-C whiff/batted-ball → IDEA-143; deliberate `scouting_report.html` template split → IDEA-144. Full-suite green gate + Step 1d smoke at closure. **Migration NEXT=013** (unchanged — no migration).
- **E-269**: Harden the PM READY Quality-Checklist Gate (COMPLETED + archived 2026-07-19) — context-layer, CA-designed + CA-implemented; fixes the READY-gate leak that let E-267 (silently-skipped api-scout consult on GC-payload ACs) and E-268 (phantom `E-268-01` story row) reach READY malformed. 1 story (E-269-01), sole deliverable `.claude/agents/product-manager.md` Quality Checklist section. Added 2 binding items — (1) **story-file existence** iterating the Stories-TABLE rows (a row with no file, OR a stub with TBD/placeholder sections, FAILS; iterate the table not the stories-in-context), (2) **consultation completeness** = explicit per-domain CONSULTED (input in Technical Notes)/WAIVED (one-line reason) verdict vs the Consultation Triggers table, silent omission ≠ waiver, GC-payload/data-availability ACs trigger api-scout — + tightened the soft L257 line to point at the per-domain verdict. **Honest ceiling stated (TN-4, no overclaim):** converts a *silent* skip into an *explicit, visible* verdict the user can challenge; does NOT guarantee a correct waiver, no hard "must consult" gate. AC-5 retrospective walkthrough (planning-time artifact in the story Notes) proves both leaks would have failed the two new items. **Rejected alternatives:** (b) force Codex spec-review on every ad-hoc epic (heavy), (c) hard "all epics route through the plan skill" (unenforceable) — option (a) chosen, and NECESSARY for the phantom-story case (the plan-skill Phase-3 CR spec audit's 6 criteria don't check story-file-per-row, so the shared PM Quality Checklist is the ONLY gate both paths cross). **Scorecard:** Codex spec-review r1 2/2 accepted (P1 scope AC-5-vs-AC-6 → TN-5 deliverable-vs-planning-doc split; P2 rationale accuracy). Dispatch: per-story CR skipped (context-layer-only, PM verified ACs alone); Closure CR Integration 0/0/0; **Codex code review r2 2/2 accepted/0 dismissed** — F1 a second soft-blanket note at `product-manager.md:86` (Consultation Triggers row) contradicting the new gate, reconciled by CA (**PM ruled the widened locus in-spirit under AC-6** — same file, no new rule/workflow/plan-skill files, completing the same TN-3 anti-contradiction change); F2 stale status mirrors (epic.md:6 comment → closure to-do; MEMORY/status → expected mid-dispatch timing). Documentation assessment: NO impact (agent-def internal gate, no docs surface). **Context-layer 8-trigger: 1,3,4 YES (codified in the deliverable itself) + 7 FAIL (+564 total but E-269's own delta +4 lines → operator-signed exception 2026-07-19; +560 is pre-existing agent-memory/rules drift, the separate E-262 re-snapshot follow-up) + 2,5,6,8 NO.** No new codification beyond the deliverable → CA already the implementer. Full-suite green-gated at closure. **Migration NEXT=013** (unchanged — no migration).
- **E-273**: Reclaim Orphaned Reference Data After Report Deletion (COMPLETED + archived 2026-07-24) — report deletion left orphaned reference data (`teams`/`players`/`team_rosters` unreachable from any surviving report) that per-report imperative cascades structurally cannot reclaim ("is this team still reachable?" is a GLOBAL question). Three root causes: RC#1 order-dependent retention leak (a `teams` row retained by a still-referencing `games` FK is never revisited once the game is later deleted), RC#2 opponent stubs never in any single cascade's scope (the largest share), RC#3 players never deleted (`_delete_team_scoped_data` removes rosters, never the `players`). 5 stories (SE), NO migration/NO permanent CLI. **(01):** `reclaim_orphan_reference_data(conn) -> ReclaimResult` in `src/reports/lifecycle.py` — a reachability-DAG two-phase terminal sweep (`reports→games→teams→team_rosters→players`), single-source id-set producers `_orphan_team_ids`/`_orphan_player_ids` feeding BOTH the DELETE and `count_orphan_reference_data` (TN-8, can't drift); reap-then-gate on `status='generating'` + the whole gate/compute/delete in ONE `BEGIN IMMEDIATE` the pass OWNS (named exception to caller-owns-tx, TOCTOU-safe), `deferred=True` liveness delay on refusal; pre-delete-team read → team delete → post-team-delete player read (TN-1 ordering, catches the ~4,900 roster-only players); three roots excluded-not-cleared (`opponent_links.resolved_team_id`/`.our_team_id` [Option B], `user_team_access.team_id`) *(⚰ **accurate-as-of-E-273; the predicate now keeps FOUR roots across THREE tables** — E-277-01 added `scheduled_report_runs.own_team_id`. Annotated, not rewritten: this records what E-273 did. **`_TEAM_BASE_PRED` in `src/reports/lifecycle.py` is the authority for the count, never this line or any document** — E-277 exists because a three-root enumeration went stale in `canonical-seams.md`, and a reader who regenerates the count from one document instead of the predicate reproduces the defect one file over.)*; belt-and-suspenders game-child clause + WARN-on-fire converts a corrupt-non-participant `IntegrityError` abort into graceful skip; TN-3 `plays`(batter+pitcher) inclusion prevents falsely deleting plays-only stubs. **(02):** wired into `_delete_report` (unconditional, fresh conn after conn1/conn2) + `cleanup_expired_reports` (NOT `_cleanup_orphans`) — so the sweep ALSO fires at `generate_report` START (safe by construction for the current run's not-yet-committed team). **(03):** generator team+report atomicity refactor via connection-injection (one txn, FK order teams→reports→run-record, failure rolls back both) — closes the pre-`reports`-row window. **(04):** flagship order-dependent cross-perspective batch invariant test with the mandatory mid-sequence anti-vacuity retention assertion (proves the RC#1 orphan was REALIZED before the final invariant==0). **(05):** throwaway `scripts/reclaim_orphan_reference_data.py` one-shot (imports the pass, three-way exit keyed on `deferred` NOT count: 0 success/2 deferred/3 residual/1 error) + TN-10 operator sequence (operator-owned baseline re-snapshot → run → exact no-diff). DE+SE CONSULTED, api-scout+coach WAIVED. **Scorecard:** planning 21/21/0 (CR-spec 5 + DE 5 + SE 4 + Codex-spec 7); dispatch/closure = per-story CR 01–05 all APPROVED (round-2 deltas: 02 AC-5 hardening, 03/F1) + Closure CR Integration 1 doc-only + **Codex code review F1 P1 MUST-FIX** = `game_perspectives.perspective_team_id` was the 6th same-class game-child `teams(id)` FK missed by the belt-and-suspenders clause (all 3 layers — spec/code/test — agreed on the incomplete 5-table enumeration; the **spec-completeness lesson**: pair an AC's FK-class enumeration with an authoritative `grep 'REFERENCES <parent>('` audit) → remediated (6-table clause + AC-2 re-verified + complete 22-live-column FK re-enumeration proves the class closed) + F2 docstrings accepted; F2-counts + F2(c) `_delete_report` flash dismissed as design-intended. **Two-fixture-crossings provenance lesson:** wiring a global destructive sweep into the generation-START hook rippled into `test_report_generator.py` (02) + `test_report_negative_paths.py` (03) fixtures that seed bare `tracked` teams — the TN-15 partition prevented intra-epic collisions but didn't anticipate this class. Live-DB diverged from the brief's 681/14,326 backlog to ~599 games via re-crawl — design is COUNT-AGNOSTIC (keys on invariant==0). **Documentation FIRES** → docs-writer `docs/admin/operations.md` reclamation runbook. **Context-layer 8-trigger: 1,2,3,6 YES + 8 candidate** (spec-completeness + global-sweep fixture-ripple lessons) → claude-architect codified the `reclaim_orphan_reference_data` seam + "deletion AND generation are destructive" bullet (report generation is now destructive at generation-start, not only deletion) + code-reviewer.md exhaustive-enumeration lesson; **7 ratchet net +12 inherited-drift operator-re-snapshot exception.** Full-suite green gate + Step 1d smoke at closure. **Migration NEXT=013** (unchanged — no migration). §12 Post-Cascade Probe operator-blindness deferred → IDEA-163.
- **E-268**: Cross-Perspective Redirect Score-Misattribution Fix (CC-2) (COMPLETED + archived 2026-07-19) — from the 2026-07-19 accumulate-only re-run audit (IDEA-153), two-channel CONFIRMED/high (Codex `gpt-5.6-terra` xhigh + independent subagent, each an executable in-memory repro through the real `ScoutingLoader`). `_upsert_game` wrote the game orientation tuple NON-atomically: `home_team_id`/`away_team_id` overwritten unconditionally from `excluded.*` while `home_score`/`away_score` gated on `preserve_scores` — so on a cross-perspective redirect (`preserve_scores=True` + incoming `home_away=None` own-defaults-to-home flip + tolerant schedule-count redirect) the frozen scores were re-attributed to the swapped team-ids, silently re-crediting runs to the wrong team on BOTH reports (`_query_record` W-L, `_query_recent_games` recent form, `_query_runs_avg` runs-for/against, plus `plays.batting_team_id` + home/away splits). 1 story (E-268-01, SE), NO migration. **Fix:** extended the score-gating `CASE WHEN ? THEN COALESCE(games.<col>, excluded.<col>) ELSE excluded.<col> END` to both team-ids so the four-field tuple `{home_team_id, away_team_id, home_score, away_score}` moves atomically — all keep-existing on a cross-perspective redirect, all take-incoming on first-insert / same-perspective reload (correction path intact); 14 placeholders / 14 binds (4 trailing `preserve_flag`). The exact analogue of E-261's `game_stream_id` keep-existing treatment. **HARD regression test** `test_cc2_redirect_preserves_orientation_tuple_and_reports` drives the real two-loader `load_payload` redirect (A-home 5-3 → B reloads home_away=None), asserts A-home 5-3 survives AND all three reads credit correctly for BOTH teams — fail-pre (`(2,1,5,3)==(1,2,5,3)`) / pass-post, CR-confirmed genuine; plus a SQL-level unit test + the AC-4 over-gating guard `test_upsert_game_correction_path_takes_incoming_orientation` (`preserve_scores=False` takes incoming). **Consultation:** data-engineer CONSULTED/SOUND (torn-write / orientation-tuple analysis), baseball-coach WAIVED (recorded — corrects misattribution on existing surfaces, no stat/coaching-logic change). **Scorecard:** dispatch/closure = per-story CR APPROVED (no findings) + Closure Integration CLEAN + independent 14/14 bind-parity; Codex P1–P4 no code findings, P5 = AC-5 operator-closure false positive (CR concurred). Tests: `112 passed` (game_dedup + game_loader), broader loaders sweep 470 passed. **Documentation: NO impact** (internal attribution fix, no command/schema/workflow/agent change). **Context-layer 8-trigger: all NO except 7** (FAIL +564 over baseline but E-268 touches ZERO context-layer files → **operator-signed exception 2026-07-19**, overage is pre-existing drift, E-262 re-snapshot tracks it) — no claude-architect codification. **AC-5** (E-257 reconcile-scoreboard ratchet) = operator post-merge closure step against live dev DB (absent from worktree). H4/IDEA-147 (`plays.batting_team_id` re-derive on the same orientation root) stays OUT. SEPARATE from E-267 (distinct COALESCE-asymmetry mechanism). **Migration NEXT=013** (unchanged — no migration).
- **E-270**: Harden Reconcile-at-Load and Purge (COMPLETED + archived 2026-07-25) — E-267 9-agent-audit remediation; HARDENING of a shipped destructive surface, not a feature. 6 stories, NO migration. **(01, SE)** `MAX_GAME_RETIREMENTS = 2` absolute cap on the game grain (the floor alone let 8-of-30 = 27% through) + the [[IDEA-159]] stripped-perspective guard, sharing ONE predicate `_game_is_cross_perspective_protected` so the cap's exempt set and the loop's refusal cannot drift; the foreign-child check queries the `_PERSPECTIVE_CHILD_TABLES` constant (guard-surface == delete-surface). Cap counts `absent − exempt`, never raw `len(absent)` — api-scout's 636-record probe found ~4% of stored game_ids absent from a team's OWN array and ALL were cross-perspective twins, which a raw count would trip constantly. **(02, SE)** purge guard: non-overridable `validate_app_env()` typo guard (`APP_ENV=prod` no longer fails open, NOT overridable by `--force --yes`), informed preview (resolved path + per-table counts), TYPED production confirm, **`--force`/`--yes` split (BREAKING for scripted `--force`)**, fail-closed auto-backup. **(03, SE)** the first true E2E destructive-path test — `generate_report()` driven TWICE against a real temp DB with a REAL ScoutingLoader. **(04, SE)** player-line per-game companion tests; its mutation run PROVED the audit's annotation-as-coverage claim (bypassing the global short-circuit leaves the old test passing). **(05, SE)** no-op deletion + ordering comments. **(06, CA)** CLAUDE.md purge paragraph. **Scorecard: 37 findings / 37 accepted / 0 dismissed** across six passes — and the 0% dismissal rate means no reviewer's judgment was ever tested against a finding the team judged wrong. **THE LESSON WORTH CARRYING: two findings landed inside ACs PM had ALREADY VERIFIED.** Codex P1 — `preview_purge` was specified read-only, PM verified it 8/8 and re-verified after amendment, but it opened via `get_connection()` whose unconditional `PRAGMA journal_mode=WAL` PERSISTENTLY mutates a non-WAL target BEFORE any confirmation, surviving every abort path (a mistyped `--db-path` mutates an unrelated SQLite file, then aborts with "no such table"). Closure-CR finding 3 — the run-time FK clause E-270-06 had JUST corrected was itself overstated: under `ON DELETE CASCADE` the purge does not raise, it **COMMITS and reports SUCCESS while destroying the preserved identity rows**. Both ACs specified properties (`read-only`, `aborts loudly`) that were TRUE AS WRITTEN and FALSE IN THE CODE — **AC verification structurally cannot surface that**, which is the argument for unconditional closure passes. Six prose/claim defects total (one caught pre-review by SE, four by review, one inherited from E-267), five sub-shapes; `.claude/rules/testing.md` + the prose rule codified two T8 lessons, two more folded. Four AC/TN texts were reconciled UP mid-flight (TN-1 exempt population, AC-5 resolved-path backup, AC-1/TN-6 injection seam + the unimplementable `data/raw/` payload source), each recorded in History with date and rationale. Doc gate FIRED → docs-writer rewrote `operations.md` "Purging Scouting and Report Data" (and `cr`'s scoped check caught a **safety-relevant** defect there: the runbook read as though the typed production confirm is mandatory, but `--yes` skips BOTH prompt forms). Context-layer 8-trigger: **1,2,3,5,8 YES** (CA codified; WAL boundary placed on the canonical connection-factory BULLET, not only the defect site) + **4,6 NO** + **7 FAIL, inherited** (the layer was already over the 2026-07-13 baseline before this epic; E-270's own codification is ~+30 lines — operator-signed exception). Follow-ups → IDEA-164/165/166. **Migration NEXT=013** (unchanged — no migration).
- **E-272**: Season × Level → League Classification (+ NRBL) (COMPLETED + archived 2026-07-25) — a live pitch-count-safety mis-classification: a scouted 18U American Legion opponent rendered the youth Pitch Smart ESTIMATE instead of the shipped binding Legion rules, because a `\d+U` bracket was dispositive of `youth_travel` in BOTH ngb-empty paths. Makes **season a first-class classification axis** (spring→NSAA family, summer→Legion/NRBL) and adds **NRBL** (Nebraska Reserve Baseball League). 4 stories (SE×2, coach, CA), NO migration, NO new crawling (season is an existing public-API field). **(01, SE)** `NSAA_SUBVARSITY` rest tiers 0/1/2/3→**1/2/3/4** — a pre-existing UNDER-rest bug (byte-identical to Varsity pre-April; the authoritative curve is stricter by one day at every tier), inherited from E-263-02c's old AC-7; the named stale test asserting `== NSAA_PRE_APRIL` was INVERTED, and SE added a literal-pin test beyond the ACs so a future Varsity-side change cannot drag Sub-Varsity along via the relative assertion. **(02, SE)** the core: `season` kwarg + TN-2 precedence (DB → recognized-ngb → **mapped bracket dispositive over ALL name keywords** → level-word × season → unknown); `_NAME_KEYWORDS` **replaced WHOLESALE** by `_league_from_age_bracket` + `_league_from_level_word`/`_LEVEL_WORD_PATTERNS` (NOT a reorder — a naive reorder breaks `test_14u_juniors_is_youth_travel`); distinct `NRBL` constant == `LEGION` today; 3-edit season threading in `generator.py`; 46 new tests incl. the three-scope Reserve discriminator (summer→nrbl / spring→nsaa_subvarsity / absent→nsaa_subvarsity) carried through to the rule TABLE. **(03, coach)** model doc + NRBL table. **(04, CA)** `pitch-rules.md` + the `baseball-coach.md:33` agent-def fix. **THE EPIC'S MOST IMPORTANT FINDING — its own TN-4 carried a FALSE safety claim.** TN-4 asserted the season-absent default "over-rests, never under-rests." TRUE for sub-varsity (`NSAA_SUBVARSITY` ≥ `NRBL` at every count), **FALSE for Varsity: NSAA Varsity and LEGION CROSS OVER** — 50 pitches needs 1 rest day under NSAA Varsity vs 2 under Legion, also looser at 61-70 and 81-90. Caught by CA at E-272-04 by REFUSING to restate a claim it had not checked against the constants; PM re-derived before accepting. Corrected framing: **spring-is-likelier, NOT spring-is-safer** — the decision stands on base rates, only the justification was wrong. **That false generalization then propagated FIVE times inside this epic across FOUR structural positions** (PM's IDEA-168 body, its **title**, the README row's **closing sentence**, a **"No urgency" rating** *derived from* the claim and therefore ungreppable, and two compressed `conservative` adjectives) — three of them found by PM's own sweep AFTER a deliberate correction pass on that very file. **T8 lesson: when you retire a claim, sweep for its DERIVATIVES, not its restatements** — the label that names it, the rating justified by it, the adjective compressing it; a token-grep finds none of these (same gap as `feedback_descope_grep_gap`, running in the correction direction). Also ruled: **`"14U Reserve"` moved `nsaa_subvarsity`→`youth_travel`** (compelled by TN-2, correct on merits, but numbers get LOOSER — non-uniformly, 4 of 7 shared ranges, plus the daily cap 90→105 which PM surfaced as the more consequential half the 3-row disclosure table omitted); **operator ruled it ships as implemented, settled via E-263-02c's operator pick — but ONLY for the interactive path: `bb report morning-run` is unattended and always infers**, so IDEA-168's fix keeps standalone value and must NOT be closed as superseded. **Scorecard: 32 findings / 26 accepted / 6 dismissed** (planning 18/14/4; dispatch 14/12/2) — the 2 Codex dismissals are **EVIDENCE-BASED** (refuted by an api-scout live 28-request probe: the endpoint ignores `Accept`, `Vary` lists `Origin,Accept-Encoding`; season present 18/18 without the vendor header), distinguished in the methodology line from reasoning-based ones. The Closure-CR MUST FIX was **the ORCHESTRATOR's** (reported "everything is staged" when three idea files had landed after its last `git add -A`; unfixed it would have shipped IDEA-169 as an orphan and dropped IDEA-170). **Documentation FIRES** (trigger 5) → docs-writer on `docs/coaching/standalone-reports.md:52`, and **docs-writer found the line's premise was ALREADY wrong pre-E-272** and rewrote around the real trigger rather than inheriting PM's flagged-uncertain reading — the one instance this epic where the inherited-claim chain was broken deliberately. **Context-layer 8-trigger: 1,2,3,5,8 YES** (CA codified; three footguns — `_` is a word character so `\bfreshman\b` cannot match `high_freshman` and the naive fix preserves the bug through itself; **`_fetch_public_team_info`'s swallow-everything handler is LOAD-BEARING SAFETY** — it nulls all five signals together so the card SUPPRESSES rather than mis-classifying, and tightening it into per-field handling would CREATE the exposure; retired claims survive in degraded forms) + **4,6 NO** + **7 FAIL** (own +101 incl. +22 from the T8 codification itself, inherited +871, total +972 = E-272 is 10.4% of the overrun — FOURTH consecutive exception; baseline stale since E-262). **LATE FINDING (operator + api-scout), does NOT reopen E-272 but reframes what's next: `age_group` is a polymorphic THREE-FAMILY LEVEL field, not an age bracket** (school `high_varsity`/`high_junior_varsity`/`high_freshman`/…; travel `NNU`; recreational `"Between N - M"`), already on `GET /public/teams/{public_id}`, populated 18/18, discriminating live — **materially weakens E-263-02c's premise** (though the value is self-reported by the opposing coach, so it is a strong signal not an authority), substantially upgrades IDEA-171's diagnosis, and seeds suggested **E-274**. Hard limit: **GC has no "reserve" level** (3 HS values vs LSB's 4), so it cannot fully replace inference. 5 ideas filed: **168** (season drift + absence, two triggers one fix), **169** (Accept-header divergence, dismissed-but-recorded), **170** (doc-PII gate cannot see `src/`), **171** (age_group level field), **172** (`\bvarsity\b` outranks Legion, MASKED by the season signal). 168/171/172 all point at `detect_league_level` — promote one and the others are nearly free. Full suite 4204 passed. **Migration NEXT=013** (unchanged — no migration).
- **E-072**: Proxy Session Ingestion Skill (**ABANDONED** + archived 2026-07-26) — never dispatched, zero changes to any tree. An `ingest-session` workflow skill (read a mitmproxy session → normalize paths → diff against `docs/api/endpoints/` → selective capture). **Abandoned on VALUE + a new COST, not on invalidated premises** — every surface it assumed still exists (`proxy/addons/*.py`, `src/cli/proxy.py`, `ingest-endpoint`, and exactly 120 endpoint docs = its own "119 + `web-routes-not-api.md`" count, undrifted). Three reasons: no ROADMAP slice; no pull in ~4.5 months; and **trigger 7's ratchet did not exist when it was planned** — a very large new SKILL.md would now need an operator-signed exception on top of an already-stale baseline. **The generalizable lesson: a spec can be fully refined, review-clean across four passes (CA + SE + UXD + three Codex rounds), and still not be worth building** — "is it correct?" and "is it worth it?" are different gates, and this one only ever passed the first. Extracted to **[[IDEA-192]]** (path-normalization ruleset + the data-first insight: the richer endpoint-log carries `response_body`, so most endpoints need no curl recapture — which was the fact that would have made the whole workflow cheap). IDEA-192 supersedes E-072 as **[[IDEA-014]]**'s unblock pointer; IDEA-014 annotated in the same pass. Closure gates: doc = no impact; 8-trigger all NO with per-trigger verdicts recorded (trigger 7 = no growth, **explicitly NOT an offset** — declining to grow is not reducing).
- **E-277**: Reclamation Follow-Up Repairs (COMPLETED + archived 2026-07-27, commit `f32d3b9`, 41 files / 4,468 insertions) — targeted repairs to E-273's orphan reclamation from a Fable-audit handoff. 5 stories (SE ×4, CA ×1), NO migration. **(01)** `scheduled_report_runs.own_team_id` added as a **FOURTH reachability root** in `_TEAM_BASE_PRED` — an own team whose only morning-run slots were placeholder deferrals was being swept with its non-regenerable audit history. **(02)** `_require_clean_connection` fail-fast at **three** entry points (`reap_stale_generating_reports`, `cleanup_expired_reports`, `reclaim_orphan_reference_data`) — the passes had been silently committing and then deleting a caller's uncommitted work on a borrowed connection. **(03)** chunking-path test. **(04, CA)** cascade-citation repair in `data-model.md` + `canonical-seams.md`. **(05)** per-cause WARN collapse on the game grain. **THE HANDOFF WAS A RELAY AND FOUR OF ITS CLAIMS WERE FALSIFIED BEFORE ANY AC WAS PINNED** — MAJOR-1's framing (migration 005's comment is scoped to REPORT deletion; its CASCADE MIRROR INVARIANT already REQUIRED the audit-row deletion, so the pin is compliant and no contradiction existed — **behaviour reproduced, justification collapsed**); `BEGIN IMMEDIATE` **RAISES** rather than implicit-committing (two agents, independently, by execution); the WARN-storm direction stated backwards; and the chunking test as specified **would have been VACUOUS** because this build reports **`SQLITE_LIMIT_VARIABLE_NUMBER = 250000`, not 999**. **Signature defect: the ONE-SIDED CLAIM — true of the scope checked, false of the reachable surface — which fired repeatedly INCLUDING inside the fixes for earlier instances.** Deliberately **no running tally**: the epic retired three of its own counts as non-re-derivable. **Closure: doc gate FIRED** (`operations.md:634` became FALSE — the pass no longer deletes "any team no longer reachable from any surviving report"; docs-writer verified BY ABSENCE; **the exit-code-3 question was flagged UNVERIFIED and routed to `se` rather than documented on a premise**). **8-trigger: 1,2,3,4,5,8 YES · 6 NO · 7 FAILS at +445, operator-signed** — **NOT inherited drift: baseline re-snapshotted at `625940e`, `16672 − 445 = 16227`, E-277 owns all of it** (rules+agents +42; agent-memory +403 ≈ 91%). **An offset WAS made and is UNCREDITABLE** — a 312-byte cut from an every-turn file moved the line count by zero. **Step 1d: keep-root UNEXERCISABLE (`scheduled_report_runs` 0 → 0) — a green smoke over an empty table; the keep-root is pinned by the synthetic tests and nothing else.** Ideas out: **[[IDEA-198]]** (pin-enumeration drift, operator DECLINED building it), **[[IDEA-199]]**, **[[IDEA-200]]**, **[[IDEA-215]]** (`ReclaimResult` docstring describes intent not outcome — **three fields, TWO failure modes**), **[[IDEA-216]]** (sweep ACs must FORBID "correcting" an accurate citation). **[[IDEA-103]]**'s stale `generator.py` path corrected at closure. **Sub-lead dispatch experiment (P3): ANSWERED NEGATIVE — but F1's cause is CONFIGURATION (`tools:` frontmatter grant), not a platform limit, and only half of risk #2 is answered; named-teammate/SendMessage semantics at depth 2 remain UNPROVEN.** Post-mortem: `.project/research/2026-07-26-fable-audit/sublead-experiment-P3-postmortem.md`. **Migration NEXT=013** (unchanged).
- **E-278**: Game Identity — One Real Game, More Than One Row (COMPLETED + archived 2026-07-28, 57 files / +3386−176) — forward accuracy only under the operator's ruling *"we can reset all prod data … we only need to ensure we are accurate moving forward"*; **repaired nothing historical.** 4 stories, ALL software-engineer, **NO migration** (a PM routing exception to edit `migrations/` comments was granted and **WITHDRAWN the same day** on DE's standing ruling — *mechanical unenforceability is not permission*, and decision routing binds PM inside a domain owner's domain; refiled as [[IDEA-223]]). Order FIXED **04 → 02 → 01 → 05**. **(04)** `tzdata` declared as a runtime dependency + fail-closed degradation + `is_full_day` honoured at ingest. **(02)** same-perspective double-listing collapsed at load — score agreement TRIGGERS, sub-second delta NARROWS. **(01)** record header renders W-L-T. **(05)** `GameSummaryEntry.last_scoring_update` → `date_source_instant`. **THE HEADLINE FINDING IS A DATE DEFECT, NOT A DEDUP DEFECT: two independent mechanisms shifting dates in OPPOSITE directions** — an unresolvable legacy tz alias (`US/Central` does not resolve in `python:*-slim`) yields the raw UTC date, **+1 day**; a full-day event's `start_ts` is a **DATE MARKER** localized as an instant, **−1 day**. *A uniform date-shift repair would fix one population and corrupt the other.* **⛔ THREE TRAPS WORTH THE ENTRY.** (1) **Fail-closed alone is a NO-OP at `_derive_game_date`** — its existing fallback is `<field>[:10]`, **identical BY CONSTRUCTION** to the fail-open output for any ISO-8601 string (six shapes executed), so an AC phrased on the return value goes green while every mis-dated row stays wrong. **STILL LIVE in shipped code under the new name.** (2) **GC's own record is NOT ground truth** — a profile record is a raw count of *that team's own schedule LISTINGS*, inflation is **per-team not global**, so on a double-listed team **matching GC means reproducing GC's error and our DEDUPED record is the better number**; *"matches the GC profile record" is forbidden as an acceptance target anywhere*. And **a CLEAN GC record is not evidence no double-scoring occurred** — the opponent side reconciles exactly *because* it got one listing. (3) **The stat-row `EXISTS` gate was proposed as defence-in-depth and REJECTED by baseball-coach** (record = games PLAYED, not games we have DATA for), backed by measurement: **20 genuine completed-and-scored games across 12 of 28 teams** carry no own-perspective stat rows and would have silently vanished from twelve coaches' records. **⚠️ TWO THINGS CLOSURE DID NOT DISCHARGE: `tzdata` does not reach production until the next image rebuild, and story 02 is PREVENTION ONLY so the record header stays wrong until the operator reset runs.** **Signature defect class: FOUR vacuity defects, every one INSIDE A GUARD** (an assertion after `is None` that cannot fail; a constant-true `!= SENTINEL` conjunct; an AC-10 test passing against the unfixed codebase via `fetchone()`; an inert `not in html` the renderer's newline defeats) — *the anti-vacuity assertion is the least-checked line in a test because it reads as the rigour*, and **every one was found by someone other than its author.** Second class: **TWO remediation-regressions** — a fix that created a fresh instance of the defect it fixed (a blanket rename broke the author's own minutes-old forwarding sentence). **Method finding that outranks the counts:** the two most serious findings (an un-regenerated `requirements-dev.txt`; a `TypeError` path) came from **constructing an input** and **sweeping a file no diff touched** — *neither from reading the diff*; and in the Step 1a audit, token grep found 4 items, synonym expansion 2, and the **semantic read found the consequential one** (`data-model.md`'s now-false absolute *"All queries across `src/` filter on `'completed'`"*, which does not merely mislead — **it RECRUITS**, naming the exact "fix" AC-6 had declined). **That rule's census was simply WRONG** — three sibling queries in `generator.py` already violated it; CA established this by **refusing a relayed instruction from PM and checking**, the only upward application of file-wins all dispatch. Dispatch scorecard **5 MUST FIX / 17 SHOULD FIX / 1 dismissal** (Codex), kept per-PASS and separate from the planning scorecard — *they count against different artifacts*. **Closure: doc gate FIRED** (`operations.md` three-tier → five outcomes; the api-doc item downgraded to "possibly ambiguous", **NOT "falsified"**). **8-trigger: 1,2,3,5,8 YES · 4,6 NO · 7 FAILS, OPERATOR-SIGNED EXCEPTION** — E-278 owns **+222** (all SE agent-memory); the **+1714 pre-existing overhang is EXPRESSLY NOT DISCHARGED and stays its own open question** (`.claude/agent-memory` is +1672 of it); **the baseline was NOT re-snapshotted, so the floor is unchanged and this exception is not precedent for the overhang.** Ideas out: [[IDEA-222]], [[IDEA-223]] (planning); **[[IDEA-224]]/[[IDEA-225]]/[[IDEA-226]]** (closure — the same defect in data-engineer's, api-scout's and baseball-coach's memory, none fixable by the epic that falsified them: **an epic that changes a dependency or a degradation path falsifies claims in the memory of agents not on its team, and no gate detects it**). [[IDEA-217]] SUPERSEDED, [[IDEA-218]] RESOLVED, [[IDEA-220]] CLOSED-check-done. **Migration NEXT=013** (unchanged).
- **E-279**: Closure Machinery — the write-guard wedge and the archive-rename deadlock (COMPLETED + archived 2026-08-01) — 5 stories (claude-architect ×4, software-engineer ×1), NO migration. **(01)** the write guard reads authoritative state (`git worktree list`) instead of inferring dispatch mode from directory existence — strictly stronger, since a crashed dispatch now still enforces dispatch-active mode. **(02)** the dispatch-log telemetry mechanism DELETED entire — hook file, both `settings.json` registrations, the `.gitignore` stanza. **(03)** the archive rename moves INTO the epic worktree; closure becomes an ordered (i)-(v) with **no sub-step 6** and both rollback paths collapsing to `git reset HEAD` + `git apply -R --3way`. **(04)** `scripts/check_archive_refs.sh` + two call sites. **(05, SE)** software-engineer's memory reconciled to the deleted hook. **The wedge class is closed by 01 and 02 JOINTLY and neither is creditable alone.** ⚠️ **THE STRUCTURAL FACT WORTH THE ENTRY: E-279 CANNOT USE ITS OWN FIX — it is the last epic subject to the deadlock it removes**, so its four blocking sites were repaired by hand in the sub-step-3 window under the OLD sequence (TN-2). CR dress-rehearsed the gate and got `[archive-refs: BLOCKED] EXIT=1` against E-279 itself; at the commit the only exit is `--no-verify`, **which also disables the PII scan.** **⭐ SIGNATURE FINDING — FOUR DISPOSITION FAILURES, ZERO DETECTION FAILURES: detection was never this team's weak point; what happens AFTER detection is.** Companion class, four instances in one hour and two of them PM's: **LABEL-TO-QUANTITY** — a git status `M` read as a magnitude (priced two agents' work off a file contributing **1.3%**), *"pre-branch"* read as *"inherited"* (withdrew a precedent that did not apply), a commit SUBJECT read as a file list, and PM twice authoring an **unverified PATH** (`concept → path`: a design doc placed in `.project/research/` that lives in `.claude/agent-memory/`, corrupting the scope PM had just handed the main session for the ratchet count — **inside the message arguing that a number silently excluding a category is worse than no number**). **Remedy that worked every time: derive it; the skipped step was always the one connecting a LABEL to a QUANTITY.** **Two rules from one origin landed opposite ways and the pair is the lesson:** CR's contiguity condition, validated only against the case that produced it, **flagged correct work as a defect** and was rewritten (*every line a grep could match must be self-evidently historical ON ITS OWN TEXT — a same-line marker is the cheapest sufficient means, not the requirement*); PM's tombstone property, tested against a second case, **caught an enumeration failure in a different file by a different author.** **`.claude/hooks/epic-archive-check.sh` FOUND INERT for modern status lines** (bare-line regex at `:44`) — a safety gate this epic cites has not fired for ≥6 epics → idea filed at closure. **Closure: doc gate FIRES and is UNRESOLVED** — the pre-commit archive gate can refuse the OPERATOR's commit, so `docs/admin/operations.md` is owed; recorded as FIRING, not satisfied. **8-trigger: 1,2,3,4,6,8 YES · 5 NO · 7 FAILS at +350 and is UNRESOLVED — neither offset nor exception recorded.** ✅ **Ownership SETTLED and it closed AGAINST us — E-279 owns ALL +350: +286 at planning (`25a9eea` +113, `fa3e1bf` +173, every counted line `.claude/agent-memory/`) and +64 at dispatch. The E-261/E-262 re-snapshot precedent does NOT apply** — that ruling turned on the drift being *another epic's* closure memory awaiting a snapshot; this is E-279 paying for its own. **Settled by `cr-e279` reading the as-committed BLOBS rather than reasoning from filenames: the two code-reviewer files carry ZERO `E-276`/`E-277` tokens, one cites E-279-04's AC and the other records an error made auditing this epic — PM's timing-not-attribution hypothesis FALSIFIED, not merely unsupported.** ⚠️ `regenerate_the_population_not_the_pair.md` carries one `E-271` token — **the audit TARGET, not the recording epic; a token count misreads it, only the enclosing line resolves it.** **Trigger 7 BLOCKS every growth-side trigger-8 promotion** — condition (b) *fits within the baseline* is unsatisfiable while the ratchet fails, so all promotions are candidates only. **INSTRUMENT FINDING, ownership-independent and the only claim in the thread that never moved: `context-ratchet.sh` is NOT registered in `settings.json`, accrues continuously, is charged per-epic to whichever epic closes next, and +286 accrued in 22h33m — so an epic closing more than about a day after a snapshot FAILS BY CONSTRUCTION regardless of its own discipline.** **The disposal pass was RUN and produced a SCORED list, not a pruned one** — epic-file entries cost the ratchet **zero** (`epics/` is in none of the four subtrees) and are **evidence, not criteria**, so pruning them would be a fabricated offset plus record destruction. **Closure obligations OUT: five off-team agent dirs never swept for surviving send-cap claims (main session); `.git/info/exclude:19` still carries `.dispatch-log/` per clone, uncommittable; `docs/` and `.project/` unswept and the sweep was identifier-based.** **Migration NEXT=013** (unchanged).
- **E-175**: Fix `bb creds import` for POST /auth Curls (**ABANDONED** + archived 2026-07-26) — never dispatched, zero changes to any tree. **All three cited defects re-verified STILL PRESENT** (client tokens discarded by `_resolve_web_token_key`; `import_creds` never executes the curl so response-body tokens are unreachable; the misleading warning verbatim). Abandoned on **value**: it fixes a FALLBACK path with three working alternatives (`bb creds setup web` primary; paste the JSON response body; GET curl carrying a refresh token), so two stories of curl-replay machinery is disproportionate. **⛔ THE ENTRY WORTH REMEMBERING — this epic's own demotion reason was FALSE and survived 18 days under a correct verdict.** The 2026-07-08 stale-READY note demoted it partly on "E-254's auth hardening changed the auth/credential code materially"; E-254 hardened *app-internal* auth in `src/api/` (magic links, passkeys, prod detection), a different subsystem that never touched `src/gamechanger/credential_parser.py`. Verdict right, reason wrong, nobody re-checked a premise supporting a conclusion they agreed with — the shape already codified in `tool-output-integrity.md` ("a verdict's stated REASON rots independently of the verdict"). Correction recorded IN the epic file rather than silently overwritten. Extracted to **[[IDEA-193]]** (the warning-message fix — live, actively misdirecting, a handful of lines, and flagged so TN-8's forward-looking clause is NOT carried over since it promises the capability that will now never ship) and **[[IDEA-194]]** (full execution design; explicit high promotion trigger; three must-survive constraints: never execute a logout curl, don't route through `create_session()`, never log the password body). Closure gates: doc = no impact (the admin docs describe today's working behavior and are accurate as they stand); 8-trigger all NO with per-trigger verdicts recorded.
- **E-266**: Pitcher Outings Breakdown — Expand-in-Place & Print (COMPLETED + archived 2026-07-19) — re-specs E-265's shipped-but-broken Outings Breakdown: a post-ship ux browser eval found the E-265-03 `<details>` layout **blank in print (P0)** + a collapsed on-screen view with no scannable stats — a **flawed spec faithfully built** (every gate verified generated-HTML-vs-spec; none rendered a browser; the smoking gun was a PASSING `test_outings_table_page_break_override` asserting print-CSS *strings*). Operator-driven prototyping LOCKED a pivot that **dissolves** the defect class. 6 stories (SE×4 + CA×2), NO migration, `FEATURE_PITCHER_OUTINGS` stays OFF (OQ-2 flag-default = operator closure decision, raised at closure). **(06, SE — derivation, INVERTS E-265's presentation-only scope):** `pitcher_outings.py` gains six per-`Outing` fields — **XBH** (plays-derived, `plays.outcome IN _XBH_OUTCOMES` importing the parser set, inheriting `_plays_by_pitcher_game`'s `perspective_team_id=scouted AND batting_team_id!=scouted` dedup clause → charted-games-only, under the widened caveat), **Outcome** (TEAM W/L/T from `games`, NOT pitcher decision), **Score** (scouted-runs-first), **S/R** (`appearance_order` 1→S/≥2→R/NULL→"—"), **#P**/**S%** (boxscore; **`pitches` denominator governs em-dash**, a legit 0% preserved when `total_strikes==0 & pitches>0`) + `get_pitching_history` gains a `total_strikes` SELECT (additive shared-seam); the green "strong-outing" derivation (`_is_strong_outing`/`_GREEN_*`/`is_strong`) fully removed (`.depth-badge-strong` the zero-walk "0 BB" badge PRESERVED — the false friend). **(01, SE — template):** the standalone `<details>` section is eliminated; the log interleaves INTO the Pitching table as a hidden `<tr class="outing-detail-row" hidden>` toggled by a JS accordion (deliberately NOT `<details>` = the Chromium `content-visibility` bug), correlated by `player_id` via a renderer-built `pitcher_outings_map`; collapsed row **pixel-identical** (only flag-gated `pitcher-row` class + `role`/`tabindex`/`aria-*` + a `:focus-visible`-only outline); Treatment-B bundled 15-col `Game` cell (colored `.form-chip-*` result+score span, 56px min-width for opponent alignment); **print = collapsed always** (`@media print { tr.outing-detail-row { display:none } }` — P0 DISSOLVED); flag-off byte-identical (every addition gated behind `{% if show_pitcher_outings %}`, an AC-10 negative-assertion over EVERY addition vector). **(02, SE):** `playwright~=1.61` → `requirements-dev.*` (pure-addition recompile). **(03, CA):** devcontainer postCreate `playwright install --with-deps chromium` + `.claude/rules/devcontainer.md` browser-test section (dev/main-only NOT-CI mirroring Step 1d, fail-closed convention, `SKIP_BROWSER_TESTS` opt-out, both footguns). **(04, SE — the thesis):** new fail-closed `tests/test_outings_browser.py` (real Chromium via `file://`, on-the-fly synthetic PII-safe render) with a genuine three-state + accordion + alignment assertion; hard-FAILS on missing chromium (only `SKIP_BROWSER_TESTS` skips). **(05, CA):** codifies the discipline — new `paths:`-scoped `.claude/rules/browser-render-testing.md` (design-validation-WIDE across all rendered surfaces, reports print-CSS the worked example; self-limiting test; email + `routes/**` exclusions) + a `code-reviewer.md` hook DEMANDING render+print evidence with `Catches: E-265`. **The epic's thesis proven LIVE:** E-266-04's alignment backstop caught a sub-pixel `min-width:50px` defect (opponent-x 1.06px > ≤1px tolerance) on first run that every string gate missed → PM Option-1 ruling: the `50→56px` fix rode 04's changeset (E-266-01 was AC-compliant as written — "~50px"; SE fixed the value not the tolerance). **Scorecard:** per-story CR 0/0/0/0/0 (E-266-05 context-layer-only → CR skipped, PM sole-gate incl. self-verifying the Codex-extraction delimiter-marker contract); Closure CR Integration 0; Codex code review **2/2 accepted/0 dismissed** (F1 P1 drop plain `:focus` per the TN-1↔AC-2 collapsed-identical conflict [hard constraint wins over literal selector text]; F2 P2 positive print-visible assertion closing the whole-section-blank gap), both PM-re-verified. **4 PM dispatch rulings** (AC-8 06↔01 coupling-boundary; golden-regen in-scope ripple of the `total_strikes` SELECT; 04-caught-01-alignment Option-1; focus-outline conflict). **Documentation assessment: NO `docs/admin`/`docs/coaching` impact** (ships flag-OFF, operator step in context-layer devcontainer.md; revisit `docs/coaching/` how-to if OQ-2 flips the flag). **Context-layer 8-trigger: 1,2,3,5 YES (codified in-epic by E-266-03/05 — new rule + CR hook + devcontainer doc; NO extra CA dispatch) + 8 YES (the render-in-a-browser lesson IS the deliverable) + 4,6 NO + 7 FIRES → operator PRE-AUTHORIZED per TN-8 ("build the playwright instinct into the context layer"), design-validation-wide within the same envelope, NO offset.** Full-suite **3947 passed / 0 failed / 0 skipped** (browser test executed). Follow-ups: OQ-2 (flag default), IDEA-143/144 (E-265 backlog, untouched). **Migration NEXT=013** (unchanged — no migration).
