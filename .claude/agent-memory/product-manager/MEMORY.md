# Product Manager -- Agent Memory

## Numbering State
- Next available epic number: E-241 (E-240=morning-scheduled-reports **COMPLETED + archived 2026-06-20** [.project/archive/E-240-morning-scheduled-reports/] — ROADMAP slice E [§5 Epic E, §0→COMPLETED], THE forward feature: cron-invoked `bb report morning-run [--date][--dry-run] <team-urls...>` reads AUTHED schedule → resolves opponents via ladder → calls EXISTING generate_report(public_id). Closure full-suite GREEN **3370 passed/0**. 7 stories serial (01 SE authed schedule+opponents-registry crawlers `src/gamechanger/crawlers/{schedule,opponents}.py`+own-team public_id→gc_uuid resolver; 02 api-scout schedule endpoint doc + opponent-resolution flow-doc refresh; 03 DE migration 005 scheduled_report_runs+additive team-deletion cascade line+revive opponent_links as mapping store [NO new opponent DDL]; 04 SE resolution ladder `src/gamechanger/opponent_ladder.py` rungs a-d; 05 SE map-opponent CLI; 06 SE `src/api/email.py` generic send_email extract+3 operator alerts; 07 SE `src/reports/morning_run.py` orchestration [integration, heaviest]). **ESTABLISHES (reusable): (1)** `scheduled_report_runs` audit table — one row/scheduled slot, `report_id` FK **ON DELETE SET NULL** (audit OUTLIVES report cleanup — the deliberate INVERSE of E-235 report_generation_runs CASCADE; test asserts row survives report-delete w/ report_id nulled), `opponent_root_team_id TEXT NO FK` (GC root_team_id namespace, never gc_uuid), `UNIQUE(own_team_id,opponent_root_team_id,game_date)` + **NULL-distinct UNIQUE footgun**→loader guarantees non-NULL key via `root_team_id = opponent_id or f"unknown-{event_id}"`. **(2)** opponent resolution ladder + `opponent_links` REVIVAL (migrations/001, no DDL): 3 states read from public_id+resolution_method ONLY [not-resolved=both NULL / resolved-positive=public_id set,method progenitor|search|operator / resolved-negative=public_id NULL,method='no_presence']; **terminality gate keys on `resolution_method IS NOT NULL` NOT public_id** (no_presence row has public_id NULL→public_id-gate would resurrect+re-attempt every run). **C1 RULING (held end-to-end): auto-ladder NEVER emits no_gc_presence** — operator-declared ONLY via `bb report map-opponent <root> --no-presence` (the SOLE no_presence writer); a zero-hit/no-match stays unresolved_mappable indefinitely. **(3) THE BINDING TIGHTENING (PM-flagged at 04 AC-verify, enforced at 07 AC-3): E-240-07 maps to scheduled_report_runs.resolution_outcome on `LadderResult.method` NOT the enum alone** — cached no_presence surfaces from ladder as UNRESOLVED_MAPPABLE+method='no_presence', and `map_outcome_to_vocabulary` keys method=='no_presence'→'no_gc_presence' (else→'unresolved_mappable'); regression test asserts BOTH persisted no_gc_presence AND not-re-queued (closes the run-record-layer resurrection bug — mapping on the enum alone would re-alert the operator every morning). **(4)** TN-1 protected-core HELD: morning_run.py CALLS generate_report via injectable generate_fn, never modifies it; generator.py's ONLY epic change = 03's 8-line additive cascade DELETE; Epic A goldens + verify-aggregates parity unchanged. **(5)** TN-2 SEQUENTIAL invariant (plain loop, no concurrency primitives — respects Epic B cleanup_orphan_teams lock); per-game try/except isolation (one opponent's failure records+continues, never aborts); preflight cred liveness check ONCE at top→PreflightError→alert+abort; **403-vs-401: catch ForbiddenError FIRST (subclass of CredentialExpiredError)→per-team skip+continue; let CredentialExpiredError (true 401) propagate run-fatal**; local-date filter derives UTC start+IANA tz→local before --date compare (B3, late-evening game crossing UTC midnight). 403 OVERLOADED 3 meanings — pins schedule=event:list 0.2.0/opponents=opponent_team:list 0.0.0/search=post_search 0.0.0/reverse-bridge GET-teams-{progenitor}=team+json 0.10.0/me-user-preflight=user+json 0.3.0. **C3:** own-team public_id→gc_uuid reuses team_resolver.resolve_team+search_teams_by_name (real NAME not slug), NOT protected-core _fetch_public_team_info. **D4:** dry-run eyeball line shows resolved name+public_id+W-L record (TeamProfile has no games field). Scorecards: PLANNING 26/26/0 [internal iter-1 16 B1-B16 + Codex iter-1 5 C1-C5 + Codex iter-2 5 D1-D5, all accepted]; DISPATCH 9/8/1 [per-story CR 01=1/02=1-dismissed('auto'→'progenitor' already-correct per TN-11)/03-05=0/06=1/07=1; CR integration=1 (shared no_presence/operator constant dedup); Codex code-review=4 incl 2 P1 (exception-path slot misclassified unresolved_mappable→now auto_resolved/failed; single-page own-team search resolver→now paginates like sibling)]. Doc gate FIRED→docs-writer operations.md morning-run runbook [cron line, --date early-start, map-opponent workflow, OPERATOR-BURDEN expectation: tournament-heavy + per-team-opponent-pairing key both enlarge operator queue BY DESIGN]. Context-layer FIRED→CA data-model.md [scheduled_report_runs+opponent_links revival+NULL footgun] + CLAUDE.md [morning-run+map-opponent commands] + ROADMAP §0 slice E→COMPLETED [main session]. OPERATOR-LOCKED preserved: 14d expiry kept; coach email DEFERRED→IDEA-080; admin-free [crontab line IS config]; opponent mapping keyed root_team_id NOT typed name. **Migration NEXT=006.** E-239=decouple-and-remove **COMPLETED + archived 2026-06-17** [.project/archive/E-239-decouple-and-remove/] — ROADMAP slice D2 [§0 D2→COMPLETED], the REMOVAL epic after D1. Closure full-suite GREEN **3208 passed/0** (count dropped from 4725 ∵ ~30% code+tests deleted). OUTCOME: −59,096 lines/139 files (84 del/53 mod/2 new: src/api/routes/reports_admin.py + tests/test_app_import_isolation.py). api/db.py now=7 reports/core survivors; admin surface=reports_admin.py (reports+user-mgmt, NOT programs). **AC-3 IMPORTER-GATE pattern** (reusable): config.py/teams.yaml deletion DEFERRED 04→05 because opponent_resolver.py:63 imported config at MODULE level — gate fired + flagged to PM exactly as specced, absorbed into 05 after the importer was removed. **§G honored**: all 6 quarantined tables stay INERT, report-delete cascade DELETEs are no-ops, NO cascade rewrite (live helper _delete_team_scoped_data generator.py:~2477); table-drops=future epic. **§H Epic-C unlock**: post-D2 player_season_* readers=single-season only + writer=canonical_recompute only (season_stats_loader gone) — BUT full/supplemented provenance guards STAY (legacy rows persist, DE-S3). **§E preserved against naive-sweep trap**: gc_uuid_resolver.py + team_resolver.py (Epic-E-reserved, 0 importers post-D2). Codex-4b caught closure-suite blocker (10 surviving tests asserted GET /dashboard→200). Review Phase4/5 17/17/0. Doc gate FIRED(T5)→docs-writer admin+coaching reports-first rewrite. Context-layer T3+T4 YES→CA codify (removal-epic footguns: tests break in files no story touched; large removal diff exceeds Codex input→scope review to added/modified files; CA agent-memory stale 'quarantined not deleted'). **Migration NEXT=005.** (Planning blow-by-blow + recon artifacts [.project/research/E-239-deletion-inventory.md + E-239-season-machinery.md] in archive.) E-238=d1-quarantine-nav-passkey COMPLETED + archived 2026-06-16 — ROADMAP slice D1 [§0 D1→COMPLETED]. Delivered: quarantine [mark-deprecated NOT delete=D2] of dashboard/member-sync/opponent-discovery via central `.claude/rules/quarantine.md` [4 semantics: deprecated/unmaintained/parity-excluded/closed-to-new-work + escalate-to-PM] + CLAUDE.md/agent-routing pointers + resolve_unlinked follow→bridge→unfollow ban in gc-uuid-bridge.md + code/template deprecation banners; nav retarget /dashboard→/admin/reports [5 auth.py redirects+root+passkey/error templates+`is_admin_page` bottom-nav suppression+removed reports.html dashboard link; six D2 admin-template header links left OUT]; passkey/magic-link challenges→migration-004 `webauthn_challenges` TTL SQLite table [src/api/passkey_challenges.py helper store/get/consume/sweep; PK(kind,lookup_key); PARENTHESIZED datetime() defaults; byte-identical login lookup-key BOTH store+lookup; multi-worker+restart-safe]; expired-report cleanup [`cleanup_expired_reports()` opportunistic at generate-start + `bb report cleanup`, unlink HTML+NULL report_path, KEEP row]. 7 stories: 01 CA quarantine.md+pointers; 02 CA 4 surface-parity sites [NOT data-model.md Season-Aggregate Parity/canonical_recompute/verify-aggregates — naive-grep trap, verified byte-untouched]; 03 CA resolve_unlinked ban; 04 SE banners; 05 SE nav retarget; 06 DE passkey table; 07 SE cleanup. **ESTABLISHES: Single-Use Token Consume (DELETE-is-the-arbiter)** footgun [data-model.md] — multi-worker single-use-token replay protection MUST `consume`-return-rowcount + caller gate on rowcount==1, NEVER read-then-delete [TOCTOU]. **KEY in-closure: Codex F1(P1) replay-atomicity** — AC-2 "replay-proof" COVERS concurrent multi-worker [read-verify-delete TOCTOU let 2 workers both consume one challenge]; FIXED in 06 [consume_challenge→rowcount; login verify gates rowcount==1 else 401/no-session/no-sign_count; 3 concurrency regression tests]. **LESSON: a "replay-proof"/correctness AC under-tested by sequential-only tests is NOT out-of-scope — when multi-worker is the epic's raison d'être, the AC covers the concurrent case.** F2(P5 /admin/reports→/admin/users→/dashboard via users.html) DISMISSED — story 05 AC-7+TN defer the six admin-template header links to D2; Success Criteria enumerates the in-scope nav surfaces [users.html not among them]. Scorecard: Planning 18/18/0; Dispatch&Post-Dev 2/1/1 [per-story CR 04-07=0, Phase4a integration=0, Codex 2: F1 accepted+fixed/F2 dismissed]; 01/02/03 context-layer-only [CR skipped, PM-only AC verify]. Doc gate FIRED→docs-writer operations.md `bb report cleanup` subsection [PM caught quoted-output-string inaccuracy on first pass, re-verified after fix]. Context-layer T1/2/3/6 YES→CA codified CLAUDE.md [bb report cleanup + quarantine pointer] + migrations.md staleness fix + data-model.md Single-Use-Token footgun; T4/5 NO. feedback_delivery_parity.md auto-memory annotated SUPERSEDED-IN-PART [dashboard quarantined, reports SOLE forward surface]. PROTECTED-CORE-not-quarantined: helpers.py/charts/spray.py/db.py-pitching/year-only-season-derivation. **Migration NEXT=005.** Closure full-suite 4725 passed/0. ||| E-237=payload-first-loaders COMPLETED + archived 2026-06-16 — ROADMAP slice C [§0 C→COMPLETED]. Removed BOTH reports-core temp-file bridges (plays tempdir; boxscore temp-file) via `load_payload` entry points (plays=BATCH dict, boxscore=PER-CALL, NO shared base class; `load_all`/`load_file` stayed thin file-reading wrappers — member pipeline + existing tests untouched), and collapsed 2 divergent boxscore_only season-aggregate writers into 1. ESTABLISHES: **`src/db/season_aggregates.py::canonical_recompute(conn,team_id,season_id)`** — the ONE canonical boxscore_only recompute (Option B superset = ScoutingLoader's parity-checked set batting-16/pitching-14-incl-gs PLUS dedup extras pa/singles/xbh+w/l/sv populated for EVERY player; perspective-scoped, scope `(team_id,season_id)`, DELETE-boxscore_only+INSERT, **per-table `NOT EXISTS` provenance guard never touches full/supplemented**); both ScoutingLoader load paths route via thin signature-preserving `_compute_season_aggregates` delegate + `dedup_team_players(recompute_aggregates=False)` suppression (Hook-2 `trigger.py:742` keeps default True — MUST still recompute). **PROVENANCE-OWNERSHIP MODEL** (full/supplemented=member-authoritative, boxscore_only=recompute-owned) now the canonical mental model. 3 stories serial (01 plays SE, 02 boxscore SE, 03 aggregate DE blockedBy 02). **In-closure scope additions (2 same data-loss class as AC-8): Phase-4b Codex P1** = `aggregate_parity.verify_aggregates` false-positived on MIXED-provenance scopes (built `stored` from boxscore_only-only but compared every player → preserved full players = bogus stored=None); fixed via per-player provenance exclusion mirroring canonical's NOT EXISTS. **LESSON (TN-7 reasoning gap): parity column-set drift and parity PLAYER-set drift are ORTHOGONAL — a provenance-preserving recompute creates mixed scopes the player-set must exclude even when no column changes.** **Step-1a invariant audit**: `player_dedup.merge_player_pair` unconditionally DELETEd member full rows on merge (same class, MERGE path) → provenance-scoped DELETE + re-point full→canonical + canonical-wins; closes data-loss for MERGED players (canonical guard closes it for non-merged). AC-7 user-run gates PASS (prod report eyeball: stat tables identical + 17 spray imgs byte-identical, zero stat movement across E-235/236/237; `bb report verify-aggregates` prod: 816 cells, 0 mismatches). Scorecard 25/25/0 dismissed (19 planning + 6 dispatch). Doc gate: NO. Context-layer: T1/2/3/5 YES → CA codifying canonical_recompute pattern + mixed-provenance invariant. Closure full-suite 4668 passed. E-236=report-integrity-hardening COMPLETED + archived 2026-06-15 — ROADMAP slice B2 (§0 B2→COMPLETED), post-B/pre-C. 9 stories closed 6 same-class report self-reporting gaps (#1-#6) across the operator run-record + coach surfaces with ZERO stat-value change (Epic A goldens/parity stayed green). generator.py serial chain 01-06,09 + 07,08. ESTABLISHES: shared `classify_stage_status(loaded,errors,expected)` helper [src/reports/run_status.py] — ERROR-driven NOT coverage-driven (the linchpin: plays/spray coverage shortfall is the MODAL no-scorebook case, must NOT false-alarm 'partial'); a stage's own 'failed' signal mapped BEFORE the classifier (precedence); migration 003 = four nullable count cols [boxscores_fetched/load_errors/plays_errors/spray_games_with_data] on report_generation_runs; **_RUN_RECORD_COLUMNS allowlist footgun** (a written col MUST be added to the allowlist or _update_run_record silently drops it — prove via real-schema round-trip); SQ1 all-blocked (M>0, games_crawled==0)→hard `failed` [Jason FINAL]; no_games two-case copy (M=0 vs M>0/N=0, interpolate M) + CLI exit-0+URL; Option A dropped season_fallback from coach degraded_confidence (now name-only ONLY) [→ IDEA-077 delivered]; derived operator-degraded (admin-only, read-time, NO schema col); both-surfaces E2E [tests/test_report_e2e_degraded.py] proves the invariant. PM RULINGS: load stage routes through classify_stage_status(1,errors,1) [degenerate single-unit coverage, NOT LoadResult.loaded] to honor Success-Criteria bullet 1; cred-expiry NULL plays_errors consciously deferred (renders honestly). Review: 6 findings + 2 PM rulings + 1 obs, ALL accepted / 0 dismissed (Codex 3/3: spray-count scope→report-query scope, CLI no_games M-vs-N, admin NULL→'—'). SE rotated after story 04 (pytest|tail RC-mask + db.backup deadlock). Doc gate FIRED→docs-writer operations.md; context-layer FIRED→CA 3 rules. ALWAYS glob before assigning. E-235=report-run-records COMPLETED + archived 2026-06-14 — roadmap Epic B, ROADMAP §0 slice B→E-235/COMPLETED; E-234=report-regression-guards COMPLETED + archived 2026-06-13 — roadmap Epic A; E-233=llm-json-hardening COMPLETED + archived 2026-06-08; E-232=starlette-deprecation-migration COMPLETED + archived 2026-06-08; E-231=harness-output-reliability COMPLETED + archived 2026-06-07; E-230=test-suite-fix COMPLETED + archived 2026-05-31)
- Next available idea number: IDEA-081 (IDEA-080=coach-facing-scheduled-report-delivery CANDIDATE, captured 2026-06-17 by E-240 — coach email link delivery deferred from E-240 [E-240 shipped operator alerts only]; carries baseball-coach email-content MUST-HAVEs [opponent+date subject, at-a-glance coverage summary body, auto-provenance note, low-coverage subject warning, name-only-match note] + the deferred stable-URL/extended-expiry option; parent E-240. **E-240 COMPLETED 2026-06-20 → blocker cleared → now PROMOTABLE, the natural next slice after E-240** (stays CANDIDATE until user prioritizes coach delivery; surfaced to user at E-240 closure). 3 NEW E-240-discovery vision signals (all 2026-06-17, in vision-signals.md) feed IDEA-080: coach email as SUMMARY not just link [top-2 pitchers/OBP/steal-rate in body]; pre-season --dry-run ritual before Opening Day [operator onboarding]; "Regenerate now" admin-reports button. IDEA-079=rich-starter-narrative CANDIDATE, captured 2026-06-16 — make predicted-starter/bullpen LLM narrative reliably rich [pitch counts+rest+event-type USSSA/NSAA+co-ace+rule-in/out], per HEAD-vs-prod-stable-2026-05-30 side-by-side; identical deterministic inputs, only prose varied; OPEN: (a)pin-deliberate-prompt/model-change vs (b)stabilize-nondeterminism; FIRST STEP diff starter-narrative prompt+src/llm across prod-stable-2026-05-30..HEAD; related IDEA-065 eval harness, IDEA-066/E-218 league detection, E-233 json-hardening. E-238/D1-closure dispositions [advisory, surfaced to user — NOT yet actioned in backlog]: IDEA-064 dashboard-report-parity + IDEA-036 dashboard-program-awareness + IDEA-035 opponent-page-redesign now MOOTED→DISCARD candidates [target surfaces quarantined]; IDEA-078 now MORE actionable [D1 codified quarantine]→promotion candidate. Also: vision-signals.md large unprocessed backlog [last curated 2026-03-13] w/ older signals contradicting reports-first reframe → recommended "curate the vision". IDEA-078=coaching-docs-dashboard-staleness CANDIDATE — **being DELIVERED by E-239 doc gate** (docs-writer reports-first rewrite of docs/admin+coaching: scouting-reports.md remove/replace + README rewrite) → flag delivered once docs-writer lands. **E-239/D2-closure idea dispositions [advisory, surfaced to user]: DISCARD candidates IDEA-034(programs admin deleted §J), IDEA-018+IDEA-043(opponent-resolution/dedup machinery deleted), IDEA-022(scouting-data-flows.md deleted→mooted); IDEA-070 KEEP→re-scope to reports_admin delete path. Vision: 30+ signals (last curated 2026-03-13) mostly predate+contradict reports-first now that D2 physically removed surfaces → 'curate the vision' STRONGLY recommended.**; IDEA-077=reevaluate-season-fallback-trust-flag PROMOTED — Option A (drop season_fallback from coach degraded line) DELIVERED by E-236-06 2026-06-15; machinery-removal residual (fallback_used) → ROADMAP D2; IDEA-076=spray-value-regression-guard CANDIDATE, captured by E-234 — Epic A E2E value-guards plays not spray; IDEA-075=harness-output-reliability PROMOTED to E-231, COMPLETED+archived 2026-06-07; IDEA-074=starlette-deprecation-migration PROMOTED to E-232 2026-06-07; IDEA-073 = full-suite CI gate, deferred-from-E-230; IDEA-072 = token-proxy compression retrospective-audit)
- Before assigning numbers: ALWAYS `ls /epics/` and `ls /.project/ideas/` to avoid collisions

## Project Context
- Project: baseball-crawl -- GameChanger API -> database -> coaching dashboard
- Tech stack: Python end-to-end. FastAPI+Jinja2 serving layer. Docker Compose + Cloudflare Tunnel. SQLite.
- Architecture: src/ for source (gamechanger/, api/, http/, safety/, cli/), tests/ for tests, data/ for local dev outputs, migrations/ for SQL
- Operator CLI: `bb` command (Typer) -- unified entry point for all operator scripts. src/cli/ package. Entry point in pyproject.toml. Devcontainer-only (not in production image).
- Credentials: short-lived, profile-scoped (_WEB/_MOBILE env keys). Primary web path: `bb creds setup web` (email+password → full login bootstrap, auto-generates device ID). Fallbacks: mitmproxy extractor (auto-detects web/ios), `bb creds import` (curl paste). Mobile: mitmproxy only (iOS client key unknown). Auth-module rule at `.claude/rules/auth-module.md`.
- See CLAUDE.md for full project conventions

## Active Epics

For full details, read the epic file in `/epics/`. Only READY and ACTIVE epics are listed here.

- **E-072** (READY): Proxy Session Ingestion Skill
- **E-073** (READY): API Documentation Validation Sweep
- **E-104** (READY): Athlete Profile Endpoint Probe
- **E-174** (READY): Fix Key Extractor to Search Asset Chunks
- **E-175** (READY): Fix `bb creds import` for POST /auth Curl Commands
- **E-193** (READY): Browser Automation Infrastructure

E-235 (report-run-records) COMPLETED 2026-06-14 -- archived at `.project/archive/E-235-report-run-records/`. **Roadmap slice B** (reports-first reframe; keystone — Epic E builds on it). Made every standalone report generation auditable + added coach trust signals + hard quality gates, with NO change to report stat values (E-234 goldens stayed green). 7 stories, serial generator.py chain 02→03→04→05 + 06←03/05 + 07←03: 01 (DE) migration 002 `report_generation_runs` — ONE wide row/generation (mirrors `scouting_runs`, NOT a child table; do NOT ALTER `reports`), per-stage status + counts + trust flags, `UNIQUE(report_id)` 1:1, FK ON DELETE CASCADE; 02 (SE) restructured `generate_report()` into a `_ReportGeneration` class of named stage methods writing the run record (refactor-only, two-tier fail contract + snapshot boundary preserved); 03 (SE) three gates — (a) no-completed-games ABORTS to `reports.status='no_games'` + minimal shareable HTML page (NOT 404), (b) season-fallback FLAGS, (c) name-only identity FLAGS; introduced the **canonical-function additive-extension pattern** `ensure_team_row_with_provenance` + `derive_season_id_for_team_with_fallback` (variant returns a result object, legacy wrapper delegates, ZERO caller churn); 04 (SE2) closed the cross-process concurrent team-deletion race via an **in-memory per-run created-set** (captures INSERTED-not-matched teams via `EnsureTeamResult.inserted`, threads through ScoutingLoader→GameLoader; replaces global snapshot diff; no lock/migration) + IntegrityError-on-partial-unique recovery (re-match→`inserted=False`); 05 (SE2) cleanup-mirror — report-delete FK CASCADE on conn1 removes run rows (the two-connection trap: pragma must be ON on the reports-delete connection); 06 (SE2) admin reports-list + `bb report list` surface run records via shared `list_reports_with_runs` LEFT-JOIN helper (`src/api/db.py`), operator-only trust-flag badges + `no_games` badge; 07 (SE2) coach footer trust block — three coverage-severity tiers (quiet≥80%/flagged 50-79%/loud<50% on N/M) + a **degraded-confidence line INDEPENDENT of severity** (shown in all three states, generic "contact your operator", specific flags stay operator-only). **Phase-4b Codex HIGH-1 (PM scope decision, coach-authoritative):** N (`completed_games_with_data`) must be DATA-BEARING (distinct completed games with ≥1 `player_game_batting`/`pitching` row), NOT score-based — `_query_freshness` counted SCORED games (score NOT NULL) but GameLoader writes scores unconditionally + stats conditionally, so scored-but-empty games (the MODAL scouting case) overstated coverage AND defeated the no_games gate. Fixed at source (count + MAX date both EXISTS-scoped); coach: ONE honest count + honest DATE everywhere ("two numbers erode trust"). Option (a) — NO E-234 golden moved (golden fixtures have stats for all scored games → data-bearing==scored there). Dispatch+code review: **8 findings / 8 fixed / 0 dismissed** (per-story CR 02=2/03=1/04=1, others 0; 4a integration=0; 4b Codex=4: HIGH-1 data-bearing N, HIGH-2 honest plays/recon `failed` telemetry, MEDIUM-1 race-recovery winner-row backfill, MEDIUM-2 no_games shareable from lists). Doc gate FIRED (docs-writer: `docs/admin/operations.md` run-records + `architecture.md`). Context-layer gate: T1/2/3/5 YES → CA codified. Closure full-suite gate GREEN (4610 passed/0 failed). ROADMAP §0 slice B→COMPLETED. **Establishes:** `report_generation_runs` audit-trail pattern (Epic E builds on it); the canonical-function additive-extension pattern (variant + delegating wrapper, zero caller churn); the in-memory per-run created-set cross-process concurrency pattern; data-bearing coverage (N=games-with-stat-rows) as the canonical freshness semantic; the `no_games` shareable terminal state.

E-234 (report-regression-guards) COMPLETED 2026-06-13 -- archived at `.project/archive/E-234-report-regression-guards/`. **Roadmap slice A** (reports-first reframe; first of A→E, prerequisite for B–E). Built a verifiable regression-guard layer around the standalone reports flow — additive tests + ONE new operator command, ZERO reports-pipeline behavior change. 5 stories, all independent: 01 (SE) golden stat tables — `tests/test_report_golden.py` deep-equals the full `_query_*` + `src/api/db.py` pitching surface against committed `tests/fixtures/golden/report_stats.json` (test never writes; regen via standalone `scripts/regen_report_golden.py`; FIXED `reference_date` for determinism; spray/plays EMPTY shape-only since seed.sql has no such rows); 02 (DE) **aggregate parity** — `src/reports/aggregate_parity.py::verify_aggregates` + `bb report verify-aggregates` diffs stored `player_season_*` vs a perspective-filtered recompute from `player_game_*` (recompute mirrors `ScoutingLoader._compute_*_aggregates` verbatim incl. gs NULL-safe CASE), **IS the Epic C cutover gate**; 03 (SE) `bb report generate` subprocess smoke (--help canary + invalid-URL parse-before-network); 04 (SE) negative-path characterization — pins CURRENT ready-but-empty behavior (zero-games/zero-errors) as Epic B before-anchor, NOT a fix; 05 (SE) transport-only respx E2E driving real `generate_report()` against api-scout-curated sanitized `tests/fixtures/e2e/` payloads, asserting a hand-computed oracle (W-L, batting, pitching, FPS%). **Key gotchas codified** (`.claude/rules/data-model.md` "Season-Aggregate Parity"): parity row-scope is the UNION of stored `boxscore_only` scopes + recompute-source scopes with NO stored row → missing-aggregate detection; member-exclusion is CROSS-TABLE (member-loaded = `full`/`supplemented` in EITHER season table, since season-stats and per-game come from independent loaders → asymmetric scopes possible); `cells_compared > 0` proves OVERALL not PER-SCOPE non-vacuity. Dispatch review: 7 findings / 6 accepted / 1 dismissed (per-story CR 02 = 1 MUST FIX multi-scope-gap + 1 SHOULD FIX, 01/03/04/05 = 0; Phase 4a CR-integration 2 → 1 schema-idiom accepted / 1 seed-dup dismissed out-of-scope; Phase 4b Codex 2 ACCEPTED: P1 parity false-green→cross-table fix, P2 E2E /search contract; P1 re-review 1 SHOULD FIX→cross-table provenance, CR R2 APPROVED). **IDEA-076** captured (spray-value-regression-guard — Epic A E2E value-guards plays not spray). Doc gate FIRED (docs-writer: `docs/admin/operations.md` verify-aggregates subsection). Context-layer gate: triggers 1/3/5/6 YES → CA codified CLAUDE.md `bb report verify-aggregates` command + "Roadmap-Derived Epics" pointer note (TN-6 — binds roadmap epics to a `## Roadmap` ref + ROADMAP §0 table update) + `.claude/rules/data-model.md` parity subsection. **TN-6 roadmap-tracking convention established + dogfooded**: ROADMAP §0 table flips slice→epic→status at planning-commit and closure; E-234's own closure flipped slice A → COMPLETED. Closure full-suite gate GREEN (4538 passed/0 failed). **Establishes:** `bb report verify-aggregates` parity command (Epic C cutover gate); golden-file committed+regen-script pattern; the roadmap-tracking convention now binding all roadmap-derived epics.

E-233 (llm-json-hardening) COMPLETED 2026-06-08 -- archived at `.project/archive/E-233-llm-json-hardening/`. Hardened the reports Tier-2 LLM "predicted starter" JSON parse (root cause confirmed by reproduction: `json.loads(content)` couldn't tolerate ```json fences/prose that capable models occasionally emit; model swap explicitly NOT the fix). 4 SE stories, serial 01→02→(03→04): 01 pure model-agnostic `extract_json_object(content)->dict` helper in `src/llm/json_extract.py` (recovers bare/fenced/fence-no-tag/leading-prose/trailing-prose + prose-with-braces; string-aware brace matching; clean-fail→LLMError; NO domain validation -- src/llm owns extraction, llm_analysis owns domain validation); 02 wired helper into `enrich_prediction` + exactly ONE retry at temp=0 on parse failure (single `_invoke` closure so kwargs ride initial+retry) + dedup default model (model_used now read from `response["model"]`, NOT the constant -- wrong when OPENROUTER_MODEL set); 03 additive optional `response_format` pass-through on `query_openrouter` (omitted when None) + `enrich_prediction` opts into `{"type":"json_object"}` (LOCKED baseline, no json_schema upgrade) + canonical default slug `_DEFAULT_MODEL="anthropic/claude-haiku-4.5"` (api-scout-verified live id; old dated slug `claude-haiku-4-5-20251001` resolved to NO live id, only worked because prod overrides via OPENROUTER_MODEL); 04 operator-detectable Tier-2 generation status (`success`/`unavailable-no-key`/`failed`) at the generator enrichment site -- `failed` is CAUSE-AGNOSTIC (read from `except`, not exception type; cause in WARNING exc_info). Observability is Medium/operator-log-level ONLY -- NO coach-visible label, NO renderer/template change (Non-Goal; coach concurred, with a vision-signal flag at vision-signals.md:39 to revisit if Tier-2 becomes regular pre-game workflow). Review: 15 planning (all accepted) + 6 dispatch (per-story CR 01=0/02=1 comment-mislabel/03=1 .env.example/04=1 type-hint-MUST-FIX-round-2; CR integration=0; Codex=3 ACCEPTED: P1 parser prose-with-braces, P2 retry-gate over-broad [now scoped to extract failures only; malformed-envelope+HTTP errors propagate un-retried], P3 architecture-subsystems.md:77 stale LLM note→CA). Doc gate: NO (coach behavior unchanged; new status is log-only; no existing admin/coaching doc documents LLM/report-gen). Context-layer gate: triggers 1-3 YES → CA updated `.claude/rules/architecture-subsystems.md` (LLM Package note); http-discipline.md evaluated, no change. Closure full-suite gate GREEN (4519 passed/0 failed). **Establishes:** reusable `extract_json_object` helper + json_object `response_format` baseline now available infra for future LLM-JSON consumers (lowers cost for IDEA-018 fuzzy LLM opponent resolution, IDEA-065 LLM eval harness); single-source canonical default slug; extraction-vs-domain-validation separation.

E-232 (starlette-deprecation-migration) COMPLETED 2026-06-08 -- archived at `.project/archive/E-232-starlette-deprecation-migration/`. Promoted IDEA-074. Cleared the test-suite deprecation warning surface (pre-existing, surfaced after E-229/E-230). 2 SE stories, independent/any-order: 01 migrated 2 `TemplateResponse(name,{...})` arg-order error-handler calls in `src/api/main.py` to request-first form (status_code preserved) + 4 per-request `.post(...,cookies=)` calls in `tests/test_admin_merge.py` to client-instance cookie delivery; 02 added `asyncio_default_fixture_loop_scope = "function"` under `pyproject.toml [tool.pytest.ini_options]`. **NO version/requirements/dependency change** (request-first signature works on pinned Starlette 0.41.3). Verification via MODULE-SCOPED `-W` filters (`:starlette.testclient` cookies family, `:starlette.templating` 500-handler) — bare `-W error::DeprecationWarning` would couple the 2 order-independent stories since `PytestDeprecationWarning` subclasses `DeprecationWarning`. 404 handler verified statically (unreachable: route-level 404s bypass custom handler; auth middleware redirects unauth requests to /auth/login). Dispatch review: 0 findings across all 4 passes (per-story CR x2, CR integration, Codex) — exceptionally clean. Doc gate: NO. Context-layer gate: all six triggers NO (module-scoped `-W` detail kept in epic TN, niche not reusable). Closure full-suite gate GREEN (4483 passed/0 failed).

E-231 (harness-output-reliability) COMPLETED 2026-06-07 -- archived at `.project/archive/E-231-harness-output-reliability/`. Promoted IDEA-075. Built a deterministic detect-and-defend layer for harness tool-output flakiness (empty/truncated/garbled drops under concurrent multi-agent dispatch; transport bug itself is Anthropic-internal/non-fixable). 5 stories, ALL claude-architect, all independent/any-order: 01 always-loaded `paths:"**"` output-integrity rule (`.claude/rules/tool-output-integrity.md`) -- failure taxonomy + cross-check/retry/escalate protocol + assert-unseen & co-batch prohibitions; 02 (anchor) project's FIRST PostToolUse hook (`.claude/hooks/edit-verify.sh` + `settings.json` `Edit|Write`) -- re-reads each edit target, transient-retry-then-warn vs real-absent top-level `decision:"block"` detect-and-signal (PostToolUse CANNOT block/rollback -- the write already landed), fail-open-but-announced on missing jq; 03 required pre-triage read-receipt gate in both review skills (codex-review, codex-spec-review); 05 relay-integrity rule in `dispatch-pattern.md` + 3 skill cross-pointers (closes orchestrator-relay fabrication surface); 04 honest upstream bug-report artifact (`.project/research/E-231-harness-repro/`) using this very session as primary evidence. Dispatch review: 4 findings / 3 accepted / 1 dismissed (per-story CR: 02=1 SE-surfaced transient-read gap fixed, 04=1 SE-attribution fixed, 01/03/05=0 PM-only context-layer CR-skip; Phase 4a CR-integration=0; Phase 4b Codex=2: P1 ACCEPTED Write-path substring→whole-file-equality + TestWriteEquality regression, P2 DISMISSED empty-VALUE no-op out-of-scope + trailing-newline a documented accepted limitation to preserve AC-8 no-false-alarm). Closure full-suite gate GREEN (4483 passed / 0 failed). Doc gate: NO. Context-layer gate: T1/T2/T3/T4 all YES but codification IS the epic's own delivered files (CA authored as the 5 stories) -- no residual CA pass. **Establishes:** the tool-output-integrity rule + edit-verify PostToolUse hook + relay-integrity convention + read-receipt triage gate now bind every agent/session going forward; the edit-verify hook is the project's first PostToolUse hook and the reference pattern (detect-and-signal-only, top-level `decision` JSON, fail-open-announced) for any future ones.

E-230 (test-suite-fix) COMPLETED 2026-05-31 -- archived at `.project/archive/E-230-test-suite-fix/`. Made the full suite green (56 failed / 4397 passed -> 4472 passed / 0 failed) after E-229 removed RTK's output compression and unmasked the failures. **NET FINDING held: zero product bugs -- all failures were test-maintenance, each proven production-correct (reproduce -> git-trace -> verify-production).** 4 stories: 01 [SE] populated `gs` in 3 pitcher-fixture helpers + hardened the gs guard (`pitcher.gs is undefined or pitcher.gs is none`) across all 4 templates + new `tests/test_template_gs_guard.py` (renders each real template's gs cell via Jinja); 02 [SE] realigned the 2 spray-geometry tests to the confirmed-square (3,3) cropped output (near-square aspect band 0.85-1.18, `src/charts/spray.py` untouched); 03 [SE] corrected the 2 game-coverage assertions + docstring to "(4 games)" (`get_game_coverage()` untouched); 04 [claude-architect] added the EXECUTABLE Phase 5 full-suite-green closure gate. **Baseline restated 56->58**: AC-7 micro-cluster (2 stale last-outing assertions in `test_report_workload.py`, surfaced inside AC-5's named-command surface, folded into 01 via the in-AC-surface scope rule). **Dispatch review: 17 findings / 17 accepted / 0 dismissed** (12 planning + 5 dispatch: per-story CR 01=1 em-dash-honesty-fix/02=0/03=0, E-230-04 context-layer-only PM-only-AC, Phase 4a CR-integration=0, Phase 4b Codex=4 [F1/F2 SKILL.md gate-logic, F3/F4 test-teeth -- the same masked-pass class the epic targets, all remediated+re-verified]). **E-230-04's gate (the standing change):** every epic closure now runs `python -m pytest tests/` in the MAIN CHECKOUT and the closure COMMIT is blocked until 0 failed; the COMPLETED status flip is DEFERRED from Step 2 to Step 8 sub-step 5 (set only on a green suite) -- option-(a) deferral, chosen after a dispatch-time AC-1 internal-contradiction (run-in-main + before-COMPLETED + at-Step-1b can't all hold; the editable-install meta-path-finder makes pre-merge pytest test main's pre-epic code). **Dogfood: E-230-04's own gate ran for the first time at this epic's closure (4472/0) and gated this very commit.** **Closure design gap found + fixed in-epic:** option-(a)'s original "PM flips COMPLETED in main at Step 8" is unreachable under `worktree-guard.sh` (blocks PM main-checkout writes except `.claude/agent-memory/*` while the worktree exists) -- corrected so PM flips COMPLETED in the WORKTREE and it rides the closure patch. Doc gate: NO (test-maintenance + internal process gate). Context-layer gate: T1 (testing.md inverse rule) / T3 (editable-install-pytest boundary) / T4 (Phase 5 gate + COMPLETED-deferral) all YES but ALL codified IN-EPIC by CA as the E-230-04 implementer -- no separate CA pass. Captured **IDEA-074** (Starlette deprecation migration -- 14 pre-existing warnings: 11x test-client `cookies=` + 3x `TemplateResponse` arg-order; user wants to plan next). IDEA-073 (full-suite CI gate) stays deferred; IDEA-072 (RTK retrospective audit) stays separate. **Establishes:** the unconditional full-suite-green closure gate is now standing -- every future epic's closure commit is blocked until `python -m pytest tests/` reports 0 failed in main.

E-229 (remove-rtk) COMPLETED 2026-05-31 -- archived at `.project/archive/E-229-remove-rtk/`. Removed every in-repo trace of the dev token-proxy tool across two devcontainer install lanes (Claude-lane `devcontainer.json` clause + Codex-lane `post-create-env.sh` block), the context layer (`.codex/config.toml`, `AGENTS.md`, the claude-context-bridge skill, the implement-skill bullet), and the operator doc (`docs/admin/codex-guide.md`). Deleted `scripts/check_codex_rtk.py` + its test; cleaned `.gitignore`. FULL-DROPPED the pytest output-discipline machinery (the pytest-discipline rule + two PreToolUse hooks + their `settings.json` registration, atomically in story 02) with NO replacement -- plain pytest output is honest again now that the token proxy is gone. Three stories: 01 (SE, provisioning + deletes), 02 (CA, atomic context-layer scrub + hook drop), 03 (docs-writer, codex-guide). Recomputed `context-fundamentals/SKILL.md` context-budget figures; a user-authorized deviation reclassified `context-layer-guard` universal->triggered (path-scoped, not session-universal), giving a corrected 6-universal-file/303-line basis. Epic-level closing-grep gate PASSED: tree resolves to exactly two files / six lines, all the `rtkn` JWT refresh-token field false positive (`docs/api/auth.md`, `docs/api/endpoints/post-auth.md`), neither modified. Dispatch review: 3 findings / 3 accepted / 0 dismissed (CR E-229-02 context-layer-guard double-count + CR E-229-03 header AC-5 substring, both user-triaged/fixed/round-2 APPROVED; Codex Phase-4b Priority-5 resolved at closure). Doc gate did NOT fire. Context-layer gate: only T4 fired (two Bash hooks deregistered) and was self-codified within the epic by CA story 02 -- no extra dispatch. PM closure obligation: reworded the E-224 archival note into an E-224->E-229 supersession note clean of live token-proxy references. **Establishes:** the dev token-proxy and its compensating pytest-discipline tooling are fully removed from baseball-crawl going forward; IDEA-072 (token-proxy compression retrospective-audit) remains OPEN as the backward-looking counterpart. Host-level `~/.claude` cleanup is the user's, out of epic scope.

E-228 (Reset Dev Env) COMPLETED 2026-05-30 -- archived at `.project/archive/E-228-reset-dev-env/`. Two SE stories. E-228-01: `bb db reset` now produces an EMPTY schema (migrated tables + migration-001 `programs`=`lsb-hs` row only) -- deleted `data/seeds/seed_dev.sql`, `load_seed()`/`_SEED_FILE` in `src/db/reset.py`, `scripts/seed_dev.py`; cleaned dead seed-error handling + stale "rows inserted" messaging in `src/cli/db.py` + `scripts/reset_dev_db.py`; `reset_database()` keeps `(int,int)` return as `(table_count, 0)`; added TN-1 dynamic-enumeration empty-schema inverse assertion. E-228-02: admin-sees-all dashboard -- admin (via `ADMIN_EMAIL` match OR `users.role='admin'`) resolves to ALL team ids with zero `user_team_access` grants, zero manual SQL. **Root cause** (confirmed by live runtime data; two earlier hypotheses refuted): coaching dashboard hard-gated on `user_team_access` with no admin bypass, while every provisioning path granted only `member` teams -- operator's 27 tracked / 0 member teams left dashboard permanently dark. **Fix:** single widening of `_get_permitted_teams` (`src/api/auth.py`) at the shared chokepoint (covers dev+prod) + two-entry-point canonical admin predicate `_user_is_admin(conn,user)` (injected, used by middleware) and `user_is_admin(user)` (own-connection wrapper, used by routes); `dashboard.py::_is_admin_user` and `admin.py::_require_admin` now delegate (one copy of the security check; `_get_user_role_by_id` removed). **Option A** backfill removal: removed ONLY the `_handle_dev_bypass` empty-permitted backfill; `_create_dev_user`'s `_assign_member_teams` call + helper STAY (not dead code) -- keeps "non-admin unchanged" honest. Non-admin gating + `DEV_USER_EMAIL`/`APP_ENV=production` guard unchanged. Dispatch review: 6 findings / 4 accepted / 2 dismissed (E-228-01 CR 2 stale-doc→doc gate; E-228-02 CR 2 pre-existing test-infra dismissed; CR integration clean; Codex 2 missing-test remediated). Doc gate FIRED (docs-writer: getting-started.md, agent-browsability-workflow.md, post-reset-guide.md). Context-layer gate FIRED (claude-architect: admin-sees-all access model + predicate-delegation pattern; admins bypass user_team_access dev+prod; empty-reset starting state; `bb db reset` operational change). Establishes: admin-sees-all is the operator dashboard-access path post-reset (not member-team assignment).

E-227 (Closure Workflow Structural Remediation) COMPLETED 2026-04-25 -- archived at `.project/archive/E-227-closure-workflow-remediation/`. Phase 5 restructured to a single atomic `feat(E-NNN)` commit (closure merge patch + archive rename + PM memory update) that eliminates the cycle dependency with `.claude/hooks/epic-archive-check.sh`. Phase 5 shrinks 12 → 10 top-level steps; worktree cleanup is now a first-class numbered step (Step 9) using `git worktree remove --force`; the `chore(E-NNN): archive epic` commit convention is removed. Implement skill Prerequisites gains a committed-plan check (`git status --porcelain -- epics/E-NNN-slug/`) that backstops the F1 plan-commit failure mode on the standalone path; skipped on the plan-skill compound-trigger handoff path. Codex Phase 4b found and remediated 2 Priority 1 issues (`--force` flag for worktree cleanup; documented reject-restoration sequence). **Process note:** dispatch was severely degraded — 4-day pause, then claude-architect / software-engineer / product-manager all entered broken states (silent idle or corrupted output dumps); under user explicit "limp through this" authorization the main session made E-227-02's edit, both Codex remediations, and AC verification directly as a one-time exception. Anti-Pattern #1 violation, but the alternative was dropping the structural fix.

E-226 (Closure Commit Approval Gate Enforcement) COMPLETED 2026-04-20 -- archived at `.project/archive/E-226-closure-commit-approval-gate/`. Restructured implement skill Phase 5: closure-commit approval gate is now a first-class numbered step (Step 7) parallel to the other numbered steps, naming the exact command (`git diff --cached --stat main`), the four required approval words ("yes", "commit", "approve", "go ahead"), and the explicit non-approval list. Step 11 archive commit unchained from `git add -A` with a preflight `git status --porcelain` anomaly check (allow-listed paths only: `.project/archive/`, `.claude/agent-memory/product-manager/`). Workflow Summary and Anti-Pattern #5 cross-references aligned. Single context-layer-only story; 17 review findings across CR integration + Codex (16 accepted, 1 dismissed as false positive). No doc impact; context-layer impact is the codification itself. **Follow-up observation now being addressed by E-227:** the pre-commit hook enforces archival before the closure commit, so Step 9's `git mv` is folded into Step 8's closure commit; Step 11's archive commit will often be empty (or contain only PM memory updates) in practice. E-227 restructures Phase 5 into an atomic single-commit sequence. Also: E-226's own closure exposed three closure-workflow failures (plan commit missing, hook vs. ordering conflict, worktree cleanup skipped) — E-227 remediates all three.

E-218 (League/Level Detection for Pitch Rules) COMPLETED 2026-04-15 -- archived at `.project/archive/E-218-league-level-detection/`. League/level detection with cascading priority (DB fields → NGB+age_group → name keywords → unknown). Legion pitch count rules (105-pitch max, 5 rest tiers). NSAA subvarsity rules (90-pitch max year-round). Unsupported leagues suppress predictions with warnings. 73 new tests. No doc or context-layer impact.

E-221 (Test Fixture Schema Parity Audit + Post-E-220 Perspective Residuals) COMPLETED 2026-04-13 -- archived at `.project/archive/E-221-perspective-residuals-and-fixture-audit/`. All three R8-P1 residuals closed; canonical cascade consolidation via `src/reports/generator.py::cascade_delete_team` (Option 2 refactor-delegate) established as the established pattern. Details in `archived-epics.md`.

E-223 (E-220 Adopter Audit -- Fix Pre-Provenance Code Paths) COMPLETED 2026-04-14 -- archived at `.project/archive/E-223-e220-adopter-audit/`. All four IDEA-071 findings resolved: admin delete confirmation counts mirror cascade two-pass surface, reconciliation summary deduplicates across perspectives/runs, spray loaders gate on game+perspective, backfill script deprecated. No doc or context-layer impact.

E-224 (Pytest Interaction Guardrails) COMPLETED 2026-04-14, then SUPERSEDED by E-229 (2026-05-31). E-224 added then-current pytest output-discipline tooling (a pytest-discipline rule, two PreToolUse hooks, and an implement-skill one-liner) to compensate for the dev token-proxy's output compression. E-229 removed that token proxy entirely and FULL-DROPPED all of E-224's compensating machinery (rule + both hooks + their settings.json registration) with no replacement -- plain pytest output is honest again. Net: E-224's deliverables no longer exist in the tree; the epic remains a historical archived record only.

## Key Architectural Decisions
- Storage: SQLite (WAL mode). Host-mounted at ./data/app.db. Simple file backup via scripts/backup_db.py (no Litestream).
- Serving layer: FastAPI + Jinja2 (Python). Single monolithic app. No TypeScript.
- Deployment: Docker Compose (local + prod). Home Linux server (no VPS, no hosting cost). Cloudflare Tunnel (ingress). App-internal auth (magic links + passkeys). Production: https://bbstats.ai.
- Migrations: numbered SQL scripts (migrations/001_*.sql). No Alembic. apply_migrations.py at startup.
- HTTP layer: src/http/headers.py + src/http/session.py. Dual-header system: BROWSER_HEADERS (Chrome 145/macOS) + MOBILE_HEADERS (iOS Odyssey). create_session(profile="web"|"mobile"). Profile-aware proxy: PROXY_ENABLED + PROXY_URL_WEB/PROXY_URL_MOBILE env vars.
- Dependency management: pip-tools + pyproject.toml dual-source (E-190). Runtime deps in both `pyproject.toml` (`>=` ranges) and `requirements.in` (`~=` ranges). `.in` -> `.txt` via pip-compile. Rule: `.claude/rules/dependency-management.md`.
- ip_outs: innings pitched stored as integer outs (1 IP = 3 outs)
- Mobile credentials (E-075, 2026-03-08): Mobile client key CONFIRMED DIFFERENT from web. iOS app is purely native (no JS bundles). Programmatic mobile refresh blocked on unknown client key.
- FK-safe orphan handling: unknown player_ids get a stub row inserted before the stat row
- Data model (revised 2026-03-31, E-195): Fresh-start schema rewrite in single migration `001_initial_schema.sql` (old 001-008 archived). Key changes: programs table (hs/usssa/legion umbrella entity); teams use INTEGER PK AUTOINCREMENT with gc_uuid/public_id as UNIQUE external identifier columns; `membership_type` (member/tracked) replaces is_owned; `classification` replaces level (varsity/jv/freshman/reserve/8U-14U/legion); team_opponents junction table for opponent relationships; TeamRef dataclass pattern (id/gc_uuid/public_id) for pipeline code; enriched stat columns -- game_stream_id + all counting stats now populated by E-117 (game loader: 12 stats + game_stream_id; season stats: 47 batting + 47 pitching; scouting aggregates: 5 batting + 6 pitching cascade). E-158 added: migration 006 (spray_charts indexes + columns: event_gc_id, created_at_ms, season_id). spray_charts now populated by SprayChartCrawler + SprayChartLoader. E-167 added: migration 007 (idx_teams_name_season_year COLLATE NOCASE index); `src/db/teams.py` with `ensure_team_row()` -- canonical team creation function used by all 8 pipeline INSERT paths. E-195 added: migration 009 (plays + play_events tables with pre-computed is_first_pitch_strike and is_qab flags; partial index for FPS% queries). Plays pipeline: PlaysCrawler + PlaysParser + PlaysLoader. Still unpopulated: bats/throws, splits, batting_order. Auth tables simplified (users/sessions/magic_link_tokens/passkey_credentials/coaching_assignments). E-143 added: migration 002 (users.role column), migration 003 (crawl_jobs table). E-197 added: migration 011 (fix season_id for Rebels 14U -- data correction, USSSA program creation, team 126 assignment). `derive_season_id_for_team()` canonical utility in `src/gamechanger/loaders/__init__.py`. E-198 added: migration 012 (reconciliation_discrepancies table -- per-signal per-player per-team discrepancy records with 5-value status lifecycle). `src/reconciliation/engine.py` with `reconcile_game(conn, game_id, dry_run=True)` and `reconcile_all()`. BF-boundary pitcher attribution correction in execute mode. E-200 added: migration 013 (fix stale season_ids for teams without program_id -- corrects games, plays, player_season_batting, player_season_pitching, team_rosters, spray_charts). E-196 added: migration 014 (start_time TEXT + timezone TEXT on games table). `get_pitching_workload()` in `src/api/db.py` -- shared query for pitching Rest/Last and P(7d) columns on dashboard and standalone reports. E-204 added: migration 015 (`appearance_order INTEGER` on `player_game_pitching`). Game loader populates via `enumerate(stats, start=1)`. Scouting aggregation computes `gs` from `SUM(CASE WHEN appearance_order = 1 ...)`. `bb data backfill-appearance-order` CLI for historical rows. GS/GR display: `{gs}/{g - gs}`, derived at template layer. E-215 added: `src/db/players.py:ensure_player_row()` -- canonical player upsert with length-based name preference (all 7 loader paths migrated). `src/db/player_dedup.py` -- duplicate detection + merge (same-team, prefix-matching names). `bb data dedup-players` CLI (--dry-run/--execute). Post-load dedup sweep in scouting pipeline (two hooks: after boxscore load + after spray load). Cross-perspective UUID mismatch is permanent GC API behavior. `gc_athlete_profile_id` column exists but unpopulated (awaits E-104). E-216 added: `GameLoader._find_duplicate_game()` -- pre-load dedup using natural key (`game_date` + unordered `{home_team_id, away_team_id}`) with doubleheader tiebreakers (`start_time` then score total). `ScoutingLoader._check_duplicate_games()` + `_validate_roster_count()` -- post-load validation (WARNING-only, non-fatal). Prevention-over-cleanup pattern established. Next migration: 016.
- Routing model (2026-03-03): Orchestrator removed (E-030). PM is the direct entry point for all work.
- Auth model (revised 2026-03-26, E-157): ALL users = magic link email + optional passkey. No separate admin login path. Cloudflare Access present but passive (no enforcing policies). Dev bypass via DEV_USER_EMAIL. Role-based access: `users.role` column (admin/user, migration 003). `_require_admin()` accepts EITHER `ADMIN_EMAIL` env var match OR `role='admin'`. Auth tables in 001_initial_schema.sql (folded in during E-100 rewrite).

## User Preferences
- Build it right, no rush
- Coaches see dashboards; user (operator) runs the system
- Multi-team (4 Lincoln levels), multi-season, player tracking across orgs
- CLAUDE.md and shipped code comments describe current implemented reality, NOT future planned state
- Epics/stories describe future work until that work is done
- Archived files are frozen historical records -- do not modify

## Ideas Backlog

Full ideas list: `/.project/ideas/README.md`. Promotable ideas (trigger met or immediately actionable):

- **IDEA-005**: Directory-Scoped Intent Nodes — trigger met (E-002+E-003 complete)
- **IDEA-009**: Per-Player Game Stats + Spray Charts — trigger met (E-002+E-004 complete)
- **IDEA-012**: Crawl Orchestration and Scheduling — all blockers resolved, promotable
- **IDEA-038**: Query-Time Splits and Streaks — trigger met (E-117 complete). Coach MUST HAVE: recent form, doubleheader, season phase.
- **IDEA-040**: Optimistic Pitching Column API Audit — trigger met (E-117 complete)
- **IDEA-043**: Fuzzy Duplicate Team Detection — trigger met (E-155 complete)
- **IDEA-064**: Dashboard-Report Feature Parity — immediately promotable (E-212 shipped). 7 gaps identified.
- **IDEA-066**: League/Level Detection for Pitch Rules — blocked on E-217 completion + first non-HS team tracked
- **IDEA-067**: Catcher-Pitcher Restriction (NSAA) — blocked on E-217 completion + catching innings data availability

## Key Workflow Contract
- Routing model: planning (user -> PM), dispatch (user/main session -> implementers directly). PM plans and closes; main session dispatches.
- PM modes: discover, plan, clarify, triage, close, curate
- Epic lifecycle: DRAFT -> READY -> ACTIVE -> COMPLETED (or BLOCKED / ABANDONED)
- READY gate: must be READY/ACTIVE before dispatch. PM sets READY explicitly.
- Dispatch: Main session creates team, spawns implementers + code-reviewer + PM, assigns stories, routes to PM for AC verification and status updates, manages merge-back and cascade.
- Direct-routing exceptions (no PM needed): api-scout, baseball-coach, claude-architect
- Implementing agents needing work auth: software-engineer, data-engineer, docs-writer
- Agent ecosystem: 9 agents (claude-architect, product-manager, baseball-coach, api-scout, data-engineer, software-engineer, docs-writer, ux-designer, code-reviewer)
- Before assigning epic numbers: ALWAYS ls /epics/ to avoid numbering collisions

## Topic File Index
- [archived-epics.md](archived-epics.md) -- Key milestones and architectural decision points (canonical source: `ls /.project/archive/`)
- [lessons-learned.md](lessons-learned.md) -- Epic authoring patterns, dependency patterns, process patterns, platform constraints
- [mcp-research.md](mcp-research.md) -- MCP server evaluation findings (E-009-R-05, R-06)
- [feedback_fix_all_real_findings.md](feedback_fix_all_real_findings.md) -- Fix all real review findings, dismiss only false positives
- [feedback_domain_expert_designs.md](feedback_domain_expert_designs.md) -- For context-layer epics, CA designs stories; PM frames ACs
- [feedback_acceptance_command_surface_scope.md](feedback_acceptance_command_surface_scope.md) -- Dispatch failure inside an AC's named command/file is in-scope, not scope expansion (E-230-01)
- [feedback_clean_reread_before_defect.md](feedback_clean_reread_before_defect.md) -- Clean re-read + quote literal text before reporting any AC defect; never assert from a garbled read (E-230-01)
- [feedback_dont_rationalize_weak_assertions.md](feedback_dont_rationalize_weak_assertions.md) -- Don't rationalize a no-teeth assertion as "correct-by-intent"; apply the delete-the-behavior teeth test (E-230)
