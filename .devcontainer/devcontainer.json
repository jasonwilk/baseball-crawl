{
  "name": "baseball-crawl",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.13"
    },
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "moby": false
    },
    "ghcr.io/devcontainers/features/node:1": {},
    "ghcr.io/devcontainers/features/github-cli:1": {},
    "ghcr.io/rocker-org/devcontainer-features/apt-packages:1": {
      "packages": "git,curl,jq,tmux,sqlite3"
    }
  },
  "postCreateCommand": "sudo chsh -s /usr/bin/zsh vscode && git clone --depth=1 https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/plugins/zsh-autosuggestions && git clone --depth=1 https://github.com/zsh-users/zsh-syntax-highlighting ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/plugins/zsh-syntax-highlighting && sed -i 's/^plugins=(.*)/plugins=(git z zsh-autosuggestions zsh-syntax-highlighting)/' $HOME/.zshrc && curl -fsSL https://claude.ai/install.sh | bash && npm i -g @openai/codex && pip install pip-tools && pip install -r requirements-dev.txt && ./scripts/install-hooks.sh && .devcontainer/post-create-env.sh && cp .devcontainer/tmux.conf ~/.tmux.conf && pip install -e . && ((curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/refs/heads/master/install.sh | sh && rtk init -g --auto-patch) || echo \"rtk install failed -- skipping\") && mkdir -p ~/.docker && echo '{}' > ~/.docker/config.json",
  "customizations": {
    "vscode": {
      "settings": {
        // Per-port binding defaults for forwarded ports.
        "remote.restoreForwardedPorts": false,
        "remote.portsAttributes": {
          "8000": {
            "localPortHost": "localhost"
          },
          "8001": {
            "localPortHost": "localhost"
          },
          "8180": {
            "localPortHost": "localhost"
          }
        },
        "terminal.integrated.defaultProfile.linux": "ZSH",
        "terminal.integrated.profiles.linux": {
          "ZSH": {
            "path": "/usr/bin/zsh",
            "icon": "terminal",
            "color": "terminal.ansiGreen",
            "overrideName": true
          },
          "Bash": {
            "path": "/bin/bash",
            "icon": "tools",
            "color": "terminal.ansiBlue",
            "overrideName": true
          }
        }
      },
      "extensions": [
        "ms-python.python",
        "ms-python.debugpy",
        "Anthropic.claude-code",
        "openai.chatgpt",
        "ReprEng.csv"
      ]
    }
  },
  "remoteUser": "vscode",
  // In Docker-in-Docker devcontainers, compose-published ports are reachable
  // in the devcontainer namespace. Host access requires VS Code forwarding.
  "forwardPorts": [8000, 8180],
  "mounts": [
    "source=${localEnv:HOME}/.claude,target=/home/vscode/.claude,type=bind"
  ],
  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:/home/vscode/.local/bin",
    "LANG": "en_US.UTF-8",
    "CODEX_HOME": "/workspaces/baseball-crawl/.codex-home"
  },
  "sshAgent": "local"
}
